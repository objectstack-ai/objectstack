---
---

docs(pm-dispatch,os-dev): 认领、查重、标签与验证方法论纪律修订 —— 本周期三起真实事故固化 (#5166)

释放内容为零(`.claude/` 非发布包);空 frontmatter 仅为满足 changeset 门禁。

- **pm-dispatch 认领协议三条硬规则**,各配事故实证:选单前读完全部评论
  (#4829 —— 只读正文就建议删 ADR-0045 §3 Accepted 的访问门);立单前查重
  (#5039 六分钟后关 duplicate);assign 后动手前重读评论区、session ID 先后
  是唯一判据、让路 + 交接诊断(#5032 的 20 秒撞车与 26 分钟内落地的交接)。
  认领 = assign + 评论成对原子落地,限流也不许拆开。
- **标签纪律**:`pm:on-hold`(决定已做、暂缓)与 `needs-user-decision`
  (决定待做)语义分开;on-hold 必配日期/理由/重启条件三要素评论;
  `pm:blocked` + `Blocked-by: #N` 与解锁扫描;标签消费者原则;状态变更不过夜。
- **派单措辞**:issue 正文是线索不是规格;报告契约新增
  `premise_still_valid` 字段,false 且无 PR 是合法产出(#4832/#4250/#5047/#4930)。
- **os-dev 验证方法论**:反向验证三向判据(红/增多/倒转,#5046、#5009/#5018、
  #4984);key-vs-value 可达性判据;fixture 三分法(#5096);消费半径扫描
  (#5046 返工);工具链陷阱清单;字节纪律(#4763/#4890/#5140/#5157);
  模板是工具不是真理。
