---
"@objectstack/spec": minor
---

feat(spec): `ISecurityService` gains `resolveEffectiveObjectPermissions`, the effective-object-permission reader (#19539)

Clause-②: yes (widening)

One new OPTIONAL member on the published `ISecurityService` contract. It answers the effective object permissions for one principal — object name to the server-resolved entry, merged across that principal's permission sets the way the endpoint derives the enforcement path's answer — the same rule, and in one corner STRICTER than that path: the derivation merges per object KEY while `PermissionEvaluator` falls back to `'*'` per SET, so a non-super-user `'*'` grant in one set never reaches an object another set names explicitly. ADR-0124 D4 allows that direction (the client told less than the server permits), so a "no grant" in this map is not proof that the server would refuse. The type is the `objects` map of `GetEffectivePermissionsResponse`, i.e. the same bytes `/auth/me/permissions` already serves, named rather than restated so the two ends cannot drift key by key.

```ts
resolveEffectiveObjectPermissions?(
  context?: SecurityContext,
): Promise<Readonly<Record<string, EffectiveObjectPermission>>>;
```

- **Why the whole map, when every other reader on this contract takes an `object`.** The first consumer is the ObjectQL engine populating `EvalContext.permissions` for `current_user.can(object, verb)`, and the object name is chosen by the AUTHORED PREDICATE at evaluation time, not by the caller. A per-object reader would make the caller guess which objects a predicate names, and a guess that comes up short does not fault: an absent entry reads as "no grant" (`objectPermissionGrants`), so the missed object answers `false` and a correct predicate silently hides an option from someone entitled to it. `@objectstack/formula`'s `EvalPermissions` says the same thing from the consumer side — "pass the whole effective set the endpoint returned, never a hand-picked subset".
- **Why not a caller-side fold of `resolvePermissionSetsForContext`.** That method hands over its INPUT — the sets, unmerged, in resolution order — because two consumers legitimately project different subsets of them. The merge onto one entry per object is not one of those projections; it is a single rule with a measured history of being re-implemented and drifting (#7608, #7555, #6334), and ADR-0124 D4 puts it on the server. Those sets are also not the whole INPUT: the server's derivation reads object SCHEMAS that a `PermissionSet[]` does not carry — `enable` (API exposure) for the restricted-object seed, `managedBy` / `userActions` for the managed-write clamp. Measured in `packages/plugins/plugin-hono-server/src/current-user-endpoints.ts`, where both are taken from the schema lookup rather than from any permission set.
- **Entries, not verdicts.** Values are the effective `allow*` + super-user entries, so the fold from entry to verdict stays in `objectPermissionGrants` — the one reading shared with `PermissionEvaluator.checkObjectPermission`. An implementation that pre-folded into per-verb booleans would be a fourth copy of that rule, and would drop the distinctions the fold depends on (`allowCreate` has no super-user bypass; `allowExport` is `grant ∧ read`).
- **Throws on resolution failure**, exactly as its two `resolve*` siblings do, and never degrades to an empty or partial map: `{}` is a real answer meaning "this subject holds nothing", so a failure wearing that costume is a silent denial of everything.
- **OPTIONAL, so nothing existing breaks.** A security service that predates the method still satisfies the contract and consumers feature-detect. The fallback when it is absent is scoped by CONSUMER CLASS, and the two arms are not interchangeable: a consumer that POPULATES a permission map for something else to answer from (the engine filling `EvalContext.permissions`) passes NOTHING rather than inventing an empty or locally merged map, which is what makes `can()` refuse loudly instead of denying quietly; a consumer that SERVES a projection of its own keeps its existing caller-side fold of `resolvePermissionSetsForContext` — on the `objects` axis that is the `/auth/me/permissions` handler alone, `/me/apps` serving a projection of `systemPermissions` and `tabPermissions` only and never reading `objects`.

The same edit reconciles the `resolvePermissionSetsForContext` docblock, which until now assigned the `objects` merge to the caller and named its rule ("most-permissive") — a sentence that would otherwise ship in the same `.d.ts` as this member's "ask instead of folding", telling one consumer to fold `objects` and the next never to. On this head the two sentences sit at `security-service.ts:386-390` and `:476-477` — the anchor the earlier draft of this note left off a bare line count. Both now state the same rule with the same two arms, split by consumer class rather than left to whichever paragraph a reader met first, and the `objects` axis is documented as preferring this method. The other three axes (`fields`, `systemPermissions`, `tabPermissions`) are untouched.

⛔ **Declaration only — no behaviour moves in this release.** No implementation is wired: `@objectstack/plugin-security` does not gain the method here, and the `packages/objectql` / `evaluateOptionVisibility` threading that would consume it stays on #18783, which declares `Blocked-by:` this card. Measured on this tree at the branch head: the diff is 2 source files, both under `packages/spec/src/contracts/` — the declaration and its contract test — plus this changeset, and the `security` service has zero implementations of the new member, so every deployment answers exactly as it did before.

No generated carrier moves. `api-surface/` and `export-origins/` record exported DECLARATION names, and this change adds no exported symbol — it adds a member to an interface already listed in **both** (`api-surface/contracts.json:151`, `export-origins/contracts.json:151`). `declaration-map/` is **not** a third carrier here: it maps schema declarations to registry names, its 14 files include no `contracts.json`, and `ISecurityService` appears nowhere under it — measured with a lit control, since `PermissionSet` *is* found there (`declaration-map/security.json`), so the zero is an absence rather than a dead scan. Verified by running the repo's own checkers rather than by inspection: `check:api-surface`, `check:export-origins`, `check:declaration-map` and `check:generated` all report no drift, and `pnpm --filter @objectstack/spec gen:api-surface` + `gen:export-origins` + `gen:declaration-map` leave the tree byte-identical.
