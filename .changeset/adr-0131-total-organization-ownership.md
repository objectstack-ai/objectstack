---
---

Docs-only: adds ADR-0131 (total organization ownership — retire NULL `organization_id`,
a platform organization owns deployment-level rows, sharing is declared). Releases nothing —
no package changes. The ADR is Proposed; implementation cards are cut from it after the
maintainer's hand-merge (Prime Directive #14), never ahead of it. Refs #13564.
