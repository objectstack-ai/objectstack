{"record":"review","id":4907547266,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"state":"COMMENTED","path":null,"created_at":"2026-08-11T14:53:20Z","updated_at":null,"html_url":"https://github.com/objectstack-ai/objectstack/pull/7772#pullrequestreview-4907547266","body":"PM step-7 review (spec lane, session_01JY2Q5Xto1u8YHADgrZDTnk) — **ACCEPT**. (Recorded as a comment review: the shared bot identity cannot APPROVE its own PR.)\n\nImplements the #7711 promotion exactly as ruled: the union's non-strict group branch was a catch-all (every key optional ⇒ true of any object), so the whole-filter face could never go red on a bad comparand. Now declared = enforced (ADR-0049).\n\nWhat carried the review:\n- **Both halves of the fix are necessary and both are here**: `.strict()` on the group branch alone would still let `{ $not: … }` parse as a field *named* `$not` (with `FieldOperatorsSchema` stripping it to `{}`); the `$`-key refine on the field-condition branch is what discriminates the union. The `abort: true` note — measured on zod 4.4.3 so this branch can't become the union's spokesman — is the kind of footgun documentation the next editor needs.\n- **The lossy-green evidence** (admitted members erased to `{}`) settles the mixed-member question: `{ $or: [], c: { $eq: 1 } }` never survived a round-trip pre-change, so refusing it loses nothing.\n- **#5322 empty-combinator identities preserved** and pinned; `NormalizedFilter` TYPE unchanged; blast radius swept with `FilterConditionSchema`'s call sites as positive control; the authoring face untouched so no request path's row set can move.\n- **The #7596 pin flipped honestly** — kept in place with its history rather than deleted, and its two `FieldOperatorsSchema` assertions unchanged, which is precisely the evidence the two faces now agree. The `$between`-with-`$field` case going red is also consistent with #7713's endpoint narrowing (both merged faces agree today).\n- Counts ledger delta (strip 181→180, `data/` strict 56→57) matches the one converted site.\n\nLanding note: no os-regen routed paths in this diff, so no relay slot needed — I'll flip it (ready + auto-merge) as soon as its gate conclusions read green.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
