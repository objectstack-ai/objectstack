{
  "number": 2646,
  "kind": "pull_request",
  "title": "feat(packages): PATCH /packages/:id to edit a package manifest",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/m",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-05T16:35:38Z",
  "updated_at": "2026-07-05T23:19:54Z",
  "closed_at": "2026-07-05T23:19:52Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-05T23:19:52Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/2646",
  "body": "## Problem\n\nPackages can be **created** (`POST /packages`) and acted on via lifecycle sub-routes (enable/disable/publish/revert/duplicate/delete/…), but a runtime/base package's **manifest** — `name`, `description`, `version` — could never be edited after creation. The ObjectUI Studio \"Package info & settings\" sheet has no edit affordance because there was no endpoint behind it.\n\n## Change — the edit path, end to end\n\n- **`SchemaRegistry.updatePackageManifest(id, patch)`** (`objectql`) — merges `name` / `description` / `version` into the in-memory manifest, **preserving lifecycle state** (`enabled` / `status` / `installedAt`). It's a metadata edit, not a reinstall, so a disabled package stays disabled. Only fields present in the patch are overwritten.\n- **`protocol.updatePackage({ packageId, patch })`** (`metadata-protocol`) — merges via the registry, then re-persists the merged manifest to `sys_packages` (best-effort, non-fatal — same contract as `installPackage`) so the edit survives a restart.\n- **`PATCH /packages/:id`** in `handlePackages` (`runtime`) — a partial patch: accepts the fields flat or under a `{ manifest }` wrapper, validates a non-empty `name` and a semantic `version`, `404`s an unknown package, and falls back to the registry when no `protocol` service is present.\n\n`id` / `scope` / `type` remain identity/structure and are **not** editable here.\n\n## Tests\n\n- `registry.test.ts` — merge preserves lifecycle (disabled stays disabled), partial-patch only overwrites sent fields, unknown id → `undefined`.\n- `http-dispatcher.test.ts` — PATCH via `protocol.updatePackage` (trims name, forwards only sent fields), `{ manifest }` wrapper, empty patch → `400`, non-semantic version → `400`, registry fallback → `404` for an unknown package.\n- `@objectstack/objectql`, `@objectstack/metadata-protocol`, `@objectstack/runtime` build clean (tsup DTS = full typecheck).\n\n> Note: one unrelated pre-existing test (`POST /packages/:id/publish-drafts applies published seed rows`) fails in this sandbox with or without this change — it needs a real DB for seed application. All other dispatcher + registry tests pass.\n\n## Companion\n\nConsumed by ObjectUI Studio (objectstack-ai/objectui#2297 merged the surrounding package management; the edit form wiring is a follow-up there).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01DxVTSDSXrdnfqHNBkHB6yi\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01DxVTSDSXrdnfqHNBkHB6yi)_"
}
