{
  "number": 19543,
  "kind": "issue",
  "title": "[finding] three sibling list doors declare `limit`/`cursor` and never read them, one reporting `hasMore: false` as a literal — REBUILD of #19365, which stopped resolving on 2026-09-21",
  "state": "open",
  "state_reason": null,
  "draft": null,
  "author": {
    "login": "os-warren",
    "id": 318460043,
    "type": "User"
  },
  "labels": [
    "domain:spec",
    "pm:blocked",
    "priority:p1"
  ],
  "assignees": [
    "os-warren"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-09-21T08:17:36Z",
  "updated_at": "2026-09-22T18:41:43Z",
  "closed_at": null,
  "closed_by": null,
  "merged_at": null,
  "comments": 4,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/19543",
  "body": "> ⚠️ **This card REPLACES #19365, which no longer resolves.** GitHub cannot restore a number, so this is a new one, and every citation that named the old number has to be re-pointed here. ⛔ Nothing below is written from memory — each section names the artefact it was recovered from, and what could not be recovered is listed rather than filled in.\n\nBlocked-by: #19545\n\n⚠️ Door ① landed with PR #19493 (merged 2026-09-22T01:10:01Z). Doors ②③ are ruled but gated on the cloud-repo reading that #19545 carries — and #19545 is a PLACEHOLDER whose original request text did not survive, so unblocking means a rider RESTATES the reading, ⛔ not merely waiting. Disposition: comment `5769942149`.\n## Why this card exists — 出处三件\n\n- **Whose instruction**: the maintainer, in the `domain:spec` seat-2 session (`session_01UDXER3sdqfeVYpEWZs5mZx`).\n- **Verbatim** (⛔ untranslated, unpolished): 「os-sam 的卡 404 和你pr 相关的要重建」\n- **Where**: that session's chat, 2026-09-21, after this seat reported that card #19365 — the card PR #19493 is `Part of` — answers `404`, together with the blast radius.\n\n**What was measured about the loss, ⛔ not inferred.** `GET /repos/objectstack-ai/objectstack/issues/19365` → `404`; the comment ids that card carried → `404`. Controls taken in the same act: `GET /pulls/19493` → `200`, and comment `5755970189` (a record that happened to be posted on the PR rather than the card) → `200`. So the issue and comment routes work; that card specifically is gone. ⛔ This card asserts nothing about WHY: the account route `/users/...` is refused by that session's proxy for **every** login — control, `/users/os-warren`, the seat's own account, answers `403` with the same 「sessions are bound to their configured repositories」 body — so no reading available there can answer it.\n\n## The ruling — recovered VERBATIM from a landed artefact\n\nPR #19493's body quotes decision batch **#204 item 2** (originally comment `5754491070` on the old card), letters **`C · C · A`** per door, maintainer 「204 同意」 2026-09-21. For **door ①** the ruling reads, verbatim:\n\n> `cursor` is retired from `ListRunsRequestSchema`; `limit` stays (it is read end to end and the Console's flow-runs page sends it today); the engine reports truncation to the route and **`hasMore` is computed**, never hard-coded. A (a cursor protocol for a 100-row window) and B (retire cursor and leave the lie) are ⛔ not taken.\n\n⭐ The same ruling is also carried in **merged-to-branch source** rather than only in prose: the `reason` field of `packages/spec/src/migrations/entries/semantic/18.automation-runs-cursor-retired.ts` names 「ADR-0049 enforce-or-remove (director seat, decision batch #204 item 2, maintainer 「204 同意」 2026-09-21, letter C of three for this door)」. That file is the durable copy and it is why the ruling survived the card.\n\n## The three doors\n\nThree sibling list doors carry the identical shape — a declared `limit` and/or `cursor` that no reader spends, one of them reporting `hasMore` as a hard-coded `false`. The old card's own framing, recovered from a listing this seat took before the loss: 「三扇兄弟门声明 `limit`/`cursor` 却从不读,其一还带 `.default(20)` 让读声明的客户端以为有上限」.\n\n| door | subject | state |\n|:--|:--|:--|\n| ① | `GET /api/automation/:name/runs` | **in flight** — PR #19493, letter C. `cursor` retired via `retiredKey()`, `hasMore` computed from the engine |\n| ② | export jobs | **ruled** (letter C), gated on a cloud-repo reading |\n| ③ | AI conversations (`ai.conversations.list`) | **ruled** (letter A), gated on the same reading |\n\n**Why doors ②③ are not closed by PR #19493**, recovered verbatim from that PR's body: 「Doors ② (export jobs) and ③ (AI conversations) are ruled but gated on a cloud-repo reading riding #19361, and the ruling has the seat execute them on that reading's return without re-entering the decision box. A merge that shut the card would strand two-thirds of the ruled work, so the card stays open and the seat re-labels it.」\n\n⚠️ That gating card, #19361, **also stopped resolving**. It is rebuilt as **#19545**, which resolves — ⛔ but that rebuild is a PLACEHOLDER: the original request text did not survive, so it names what is known and asks its riders to restate the reading rather than inventing one. ⭐ The quotation above is left EXACTLY as recovered and is ⛔ not re-pointed: it is what PR #19493's body said at recovery time, and editing a quotation to make it current would falsify the word 「verbatim」. That PR's own body has since been re-pointed in place.\n\n## Where door ①'s record lives now\n\nPR #19493 is the surviving home for this work's records, because three `os-dev-report` comments and two contract-review records went with the old card:\n\n- round-2 contract review, re-anchored: comment `5756785503`\n- round-3 contract review (**FAIL**, `Served-tier: 113/113`): comment `5757388878`\n- the `Lint & Repo Gates` citation diagnosis: comment `5757007310`, with its correction carried in `5757388878`\n\n## ⛔ What could NOT be recovered, stated rather than filled in\n\n- **The original body in full.** 3,052 bytes; what survives is the `Path:` line, the 「三扇兄弟门」 framing, the relationship to #17667 (「#17667 deliberately did not widen onto these, and that was correct — the ruling named one door. This card is the successor」) and the dedup words, all from a listing and greps this seat took before the loss.\n- **The original title verbatim.** Only a 70-character listing truncation survives: `[finding] three sibling list doors carry the same declared-but-never-r`. The title above is written to describe the same work; it is ⛔ not a quotation.\n- **The comment thread**: the claim, three dev reports, the ruling comment `5754491070` in its original form, and the earlier review records. The ruling's text survives through the two quotations above; the rest does not.\n- **The exact label set at the moment of loss.** `domain:spec` · `priority:p1` · `pm:dispatched` · `needs:contract-review` were read off a lane listing this seat took at 2026-09-21T06:0xZ. They are **restored from that reading**, ⛔ not re-graded — triage corrects them if the reading was stale.\n\n## What this card asks for next\n\nNothing new. Door ① continues on PR #19493 at its round-4 rework. ⚠️ **The 22 comment-prose sites in that PR that cite the old number must be re-pointed here**, or `Lint & Repo Gates` step #181 (`Issue citations this change adds resolve on the board`) stays red — its own remedy text forbids guessing a replacement and requires naming a target that resolves, which this card now is.\n\n## Dedup words\n\n`list door declared limit` · `cursor never read` · `hasMore hard-coded false` · `export jobs pagination declared` · `automation runs cursor ignored`\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
