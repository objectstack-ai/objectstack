{
  "number": 9075,
  "kind": "issue",
  "title": "docs(deployment): environment-variables' `OS_DATABASE_URL` example list omits `mysql://`, one row above the caveat pointer that says MySQL is supported",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "os-project-manager",
    "id": 314343378,
    "type": "User"
  },
  "labels": [
    "documentation",
    "domain:devx",
    "pm:dispatched"
  ],
  "assignees": [
    "os-project-manager"
  ],
  "milestone": null,
  "type": "Task",
  "created_at": "2026-08-16T08:26:55Z",
  "updated_at": "2026-08-16T10:33:21Z",
  "closed_at": "2026-08-16T10:33:21Z",
  "closed_by": {
    "login": "os-project-manager",
    "id": 314343378,
    "type": "User"
  },
  "merged_at": null,
  "comments": 3,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/9075",
  "body": "Filed unassigned by the dev seat implementing #9053. Not fixed there: #9053's dispatch fences the `content/docs/**` dialect-parity sweep (inherited from #9029) and says in its own words that a further page with the same gap is a finding to file, not scope to grow the PR into.\n\nDuplicate-searched first, keyword over open and closed issues (`environment-variables OS_DATABASE_URL mysql`, and `environment-variables.mdx database url scheme` over open): the only hit is #9053 itself, which is about a different page. No existing card.\n\n## Measured on `origin/main` @ `2cb69c31c`\n\n`content/docs/deployment/environment-variables.mdx:50`, the `OS_DATABASE_URL` row of the core env-var table:\n\n```\nDatabase connection string (e.g. `file:./data.sqlite`, `postgres://…`, `mongodb://…`, `memory://`).\n`libsql://` / `*.turso.io` (Turso) is inferred too, but its driver is an **optional** package: …\n```\n\n`mysql://` is absent, and so is `mysql2://`. Both are inferred by the runtime — `detectDriverFromUrl` in `packages/runtime/src/standalone-stack.ts` matches `/^mysql2?:\\/\\//i` and returns `mysql`, and the CLI's `inferDriverTypeFromUrl` uses the character-identical regex.\n\n## Why it is only a finding, not a defect\n\nNothing on the page is false. Unlike `self-hosting.mdx`'s row (#9053), which is a closed instruction — *\"Use `postgres://…`, `mongodb://…`, …\"* — this one is explicitly an `e.g.` list, so omitting a scheme states nothing untrue. Two further things blunt it:\n\n- The **adjacent row**, `OS_DATABASE_DRIVER`, already carries the MySQL caveat pointer PR #9052 landed, so a reader of the URL row is one line away from learning MySQL is a supported target.\n- `data-modeling/drivers.mdx`'s URL → Driver Inference Table is the exhaustive alias reference and lists both `mysql://…` and `mysql2://…`.\n\nWhat keeps it worth recording is that the row is **not purely illustrative** — it curates. It spends a whole dedicated sentence on `libsql://`'s optional-package trap, and `memory://` is in the list despite being a non-production target. A reader could reasonably read the four named schemes as the production menu and conclude MySQL is not on it, which the 2026-08-15 ruling on #8739 says it is.\n\n## Not claimed\n\n- **No opinion on wording.** Whether the row gains `mysql://…` alone (matching how it names `postgres://` for the whole `postgres`/`postgresql`/`pg` family), gains both spellings, or is left alone because the adjacent row already covers it, is the docs owner's call.\n- **Not swept.** I looked at this row only because #9053's page links to this page as *\"the full catalog\"* and I was verifying where my own pointer sends a deployer. I did **not** audit `content/docs/**` for other pages that omit or over-claim a dialect — that sweep is still nobody's, and #9029's fence stands.\n- **No source change implied.** The runtime behaviour is correct and documented; this is about one prose list.\n\n## Related\n\n- #9053 / PR #9076 — the same omission on `deployment/self-hosting.mdx`, where the list is a closed instruction and the fix landed.\n- #9029 / PR #9052 — the MySQL caveats, and the pointer already on this page's `OS_DATABASE_DRIVER` row.\n- #8739 — the ruling that made MySQL a supported deployment target.\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_011RB4waLuNbdruCo6X9oobm)_"
}
