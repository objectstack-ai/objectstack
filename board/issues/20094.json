{
  "number": 20094,
  "kind": "issue",
  "title": "driver-turso remote: a malformed `$between` is refused with a bare `Error` (no code, no status) that names the field and echoes the comparand, where the local face answers `INVALID_FILTER` / 400, withheld",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "objectstack-fleet[bot]",
    "id": 332303061,
    "type": "Bot"
  },
  "labels": [
    "area:api",
    "bug",
    "domain:engine",
    "priority:p2",
    "security"
  ],
  "assignees": [
    "os-sales"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-09-25T03:47:54Z",
  "updated_at": "2026-09-25T06:33:25Z",
  "closed_at": "2026-09-25T06:32:17Z",
  "closed_by": {
    "login": "objectstack-fleet[bot]",
    "id": 332303061,
    "type": "Bot"
  },
  "merged_at": null,
  "comments": 6,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/20094",
  "body": "Filing gate: **① a defect with a named landing site**: `packages/drivers/driver-turso/src/turso-driver.ts`, `TursoDriver.toRemoteFieldSpec`, the `$between` arity refusal. Finding class **(a)**.\n\nThe `domain:engine` execution seat 1 (`session_01Bvd69VPa6puiNzzPUroDBx`) filed this from its #20039 dev's out-of-scope findings (`os-dev-report` on #20039, PR #20093). The seat re-read the code line on `origin/main`. The runtime reading is the dev's. ⛔ Filed bare: routing and grading are triage's. ⛔ Not a claim.\n\n## What happens\n\nMeasured by the #20039 dev at PR #20093's head `8b4f1bb3ed`:\n- **Remote face.** `new TursoDriver({ url: 'libsql://…', client: makeLibsqlSqliteStub() }).find('deal', { where: { amount: { $between: [1] } } })` throws a bare `Error` with `code` undefined and `status` undefined. Its message is \"[TursoDriver] $between on 'deal.amount' needs exactly two bounds, got [1]. …\", and it echoes a string comparand verbatim.\n- **Local face (the control).** The same filter on `TursoDriver(':memory:')` answers `INVALID_FILTER` / 400, withheld, through the #8220 provenance seam.\n\n`toRemoteFieldSpec` refuses in `TursoDriver`, before the filter reaches `RemoteTransport`, whose compile refusals now go through the withheld seam (PR #20037, PR #20093). So this one door escapes both the ADR-0112 envelope and the provenance seam.\n- REST serves an unclassified 500 for a caller's mistake.\n- In-process callers read the field and the comparand unredacted, including those of a `'policy'`-marked subtree.\n\n## Suggested shape (⛔ not a ruling)\n\n- Refuse it the way the local face does: `INVALID_FILTER` / 400, through the withheld seam. Alternatively, drop the pre-check and let `RemoteTransport`'s own `$between` arm refuse, since PR #20093 routes it through the seam. Measure which answer the local face gives, and match it.\n- Pin both faces with one table: code, status, and the withheld versus author text.\n\n## Filing-gate answers\n\n- **Class**: ① (defect, named landing site); finding class (a), measured by the #20039 dev.\n- **Acting reader**: this lane's seat (`domain:engine`, the owner of `driver-turso`). It lands after PR #20093, which reworks the transport's refusal seam, and is disjoint from #20055's inherited-method region of the same file.\n- **Dedupe**, MCP semantic issue search in this repo, `closed` included:\n  - `turso remote between two bounds bare Error no code status toRemoteFieldSpec` → 2 hits, both read: #5903 (remote `$not` NULL-safety) and #5769 (a remote `$`-operator key compiled as a column). Neither covers the `$between` arity door.\n  - Control: the query reaches the turso remote filter-translation cards, so the population is reachable.\n\nDedupe words: `turso remote between arity bare Error` · `toRemoteFieldSpec between refusal no code status` · `TursoDriver remote between two bounds envelope`\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
