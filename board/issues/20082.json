{
  "number": 20082,
  "kind": "issue",
  "title": "objectql: a formula field or a CEL `defaultValue` that calls `current_user.can()` gets no permission data — the formula reads a silent `null` on every read, the default is left unset with a warn (`applyFormulaPlan`, `applyFieldDefaults`)",
  "state": "open",
  "state_reason": null,
  "draft": null,
  "author": {
    "login": "objectstack-fleet[bot]",
    "id": 332303061,
    "type": "Bot"
  },
  "labels": [
    "area:api",
    "bug",
    "domain:engine",
    "pm:dispatched",
    "priority:p2"
  ],
  "assignees": [
    "os-sales"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-09-25T02:21:23Z",
  "updated_at": "2026-09-25T08:24:17Z",
  "closed_at": null,
  "closed_by": null,
  "merged_at": null,
  "comments": 4,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/20082",
  "body": "Filing gate: **① a defect with a named landing site**: `packages/objectql/src/engine.ts`, `applyFormulaPlan` and `applyFieldDefaults`. Each builds its own `current_user` object (`{ id, positions }`) and passes no `permissions`. Finding class **(a)**.\n\nBlocked-by: #18783\n\nThe `domain:engine` execution seat 1 (`session_01Bvd69VPa6puiNzzPUroDBx`) filed this from its #18783 dev's census (`os-dev-report` on #18783, PR #20079; seat decision Q3 in amendment 5825601266). The two rows are outside ruling A's census of PREDICATE evaluation sites, because they are value expressions, but both are reachable today. ⛔ Filed bare: routing and grading are triage's. ⛔ Not a claim.\n\n## What happens\n\nMeasured by the #18783 dev at PR #20079's head `f3fe6d6cfd`, with the new permission resolver registered:\n- **`applyFormulaPlan`:** a formula field whose expression calls `current_user.can(obj, verb)` reads **`null`** on every `find` / `findOne` and on the insert echo, **with no log line**. That is a silent wrong value.\n- **`applyFieldDefaults`:** a CEL `defaultValue` calling `current_user.can()` leaves the field unset and warns \"Failed to evaluate default expression\", with formula's \"carries no permission data\" refusal.\n\n`can()` is a published formula capability (PR #18781). PR #20079 wires it for option visibility only.\n\n## Suggested shape (⛔ not a ruling)\n\n- Route both sites through the plumbing PR #20079 adds (`ObjectQL.registerEffectiveObjectPermissionsResolver` → `toEvalPermissions`). Resolve once per operation, not per row. Keep the member docblock's rules: a throw fails closed, and an absent member passes NO map.\n- Decide per site what \"no permission data\" should do: a formula field that cannot evaluate should not read a silent `null`. Measure and pin both.\n- Declare per `references/compile-surfaces.md` if a filter face is touched (not expected).\n\n## Filing-gate answers\n\n- **Class**: ① (defect, named landing site); finding class (a), measured by the #18783 dev.\n- **Acting reader**: this lane's seat (`domain:engine`, the owner of `packages/objectql`), after PR #20079 lands (`Blocked-by: #18783`).\n- **Dedupe**, MCP semantic issue search in this repo, `closed` included:\n  - `formula field defaultValue current_user.can returns null no permission data applyFormulaPlan applyFieldDefaults` → 0 hits;\n  - `formula field current_user can permissions null` → 0 hits.\n  - Control: `current_user.can receiver-only EvalContext permissions bound` → 1 hit (#18783), so the population is reachable.\n\nDedupe words: `formula field can() null` · `applyFormulaPlan permissions` · `defaultValue can() unset` · `applyFieldDefaults permissions`\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
