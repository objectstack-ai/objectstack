{
  "number": 6553,
  "kind": "pull_request",
  "title": "fix(objectql,driver-sql): 播种解析按声明的 suffix 定位计数器,两侧收敛 (#6468)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "baozhoutao",
    "id": 6194462,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/l",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-08-08T04:29:47Z",
  "updated_at": "2026-08-08T05:04:21Z",
  "closed_at": "2026-08-08T05:04:20Z",
  "closed_by": {
    "login": "baozhoutao",
    "id": 6194462,
    "type": "User"
  },
  "merged_at": "2026-08-08T05:04:20Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/6553",
  "body": "Fixes #6468\n\n自增号格式允许序号槽 `{0..0}` **后面**还有 token —— `renderAutonumber` 的 `suffix`\n是**声明返回值**,契约写死为 `prefix + zero-padded(seq) + suffix`\n(`packages/spec/src/data/autonumber-format.ts:165-214`)。这类格式渲染出的值**序号\n不在串尾**,而两侧播种解析都假定「串尾的数字就是计数器」,并且各错各的。\n\n## 前提复核表(动手前在 `origin/main` 逐条实测)\n\n| 前提 | 结论 | 证据 |\n|:---|:---|:---|\n| **P1** 两处解析仍是「取串尾数字段」形状,双错读数仍在 | ✅ 成立 | 锚点已随 #6467 漂移,按内容重定位:`engine.ts:2182` `seedAutonumber`(分诊锚 :2178)、`sql-driver.ts:3033` `scanMaxNumericTail`(锚未动)。探针直调 spec 的 `parseAutonumberFormat` / `renderAutonumber` 后复刻两侧解析,复现 issue 正文原表(见下) |\n| **P2** spec 侧契约原样正确,只读引用 | ✅ 成立,零 diff | `:165` `suffix: string;`、`:172` \"Final value: prefix + zero-padded(seq) + suffix\"、`:199-200` `width === null` 前后分流、`:213` |\n| **P3** #5495 无在飞认领、无要求动这两个函数 | ✅ 成立 | #5495 的认领已由 PM 释放(comment 5216947320:摘 `pm:dispatched` → `pm:queue`、清 assignee),且重锚结论指向 `getNextSequenceValue` 的序列重同步 —— 与本单两处解析零函数交集 |\n| **P4** 本单解析在 #6467 的新扫描形状下生效 | ✅ 成立 | 引擎侧解析的输入正是 `keysetWalk` 分页返回的行(`engine.ts:2206-2226` 就在 `for await (const page of walk.pages())` 内);扫描结构未触碰,并有 pin 断言 `$startsWith` 下推形状不变 |\n\nP1 实测(`origin/main` @ `2fdb36eb9`,真实计数器均为 1):\n\n```\nfmt=\"{000}-{YYYY}\"  value=001-2026   prefix='' suffix='-2026' engineSeed=2026 sqlDriverSeed=12026 agree=false\nfmt=\"D-{0000}\"      value=D-0001     prefix='D-' suffix=''    engineSeed=1    sqlDriverSeed=1     agree=true\nfmt=\"{0000}\"        value=0001       prefix='' suffix=''      engineSeed=1    sqlDriverSeed=1     agree=true\nfmt=\"\"              value=1          prefix='' suffix=''      engineSeed=1    sqlDriverSeed=1     agree=true\n```\n\n`{000}-{YYYY}` 是很常见的单号写法。三行 `001-2026`/`002-2026`/`003-2026` 之后,引擎\n把**年份**当计数器播种成 2026,下一个号跳到 `2027-2026`;driver-sql 把 `001` 和 `2026`\n拼成 12026,下一个号是 `12027-2026`。**同一份元数据、同一批行,换个驱动号段就不一样**,\n而中间跳过的号已经烧掉、事后无法回收。\n\n## 双侧收敛设计\n\n**一条规则,两侧同一对字符串。** `prefix` 与 `suffix` 都取自 `renderAutonumber`\n的返回值,由调用方算好后传入:\n\n- **prefix / suffix 任一非空 ⇒ 计数器「有锚」**:取 prefix 之后的**首个**数字段,\n  并在该行确实带有声明的 suffix 时先剥离;\n- **两者皆空 ⇒ 「无锚」**:各自既有读法**逐字保留** —— 引擎取整串最后一个数字段,\n  driver-sql 拼接全部数字。无 `{0..0}` 槽的格式渲染的就是串尾裸计数器,而早于格式\n  存在的历史值根本没有锚可依。\n\n**suffix 只在匹配时剥离,绝不要求匹配。** 计数器 scope 是渲染后的 **prefix**,\n`{000}-{YYYY}` 的 prefix 为空 ⇒ 全局一个计数器、只有显示的年份在变。去年的\n`007-2025` 因此持有计数器 7,必须计入。同理 **SQL 谓词保持 `like 'prefix%'`,不下推\n`%suffix`**:按当前 suffix 过滤会把这些行整批漏掉、播种**低于**真实 max —— 正是 #6249\n修掉的重复单号伤害,自己再造一遍。suffix 只在 JS 侧逐行使用,不匹配即「换了个 suffix、\n同一个计数器」。两侧各有一条 case 直接 pin 这个方向。\n\n### 依赖方向取舍(报告项)\n\n`@objectstack/objectql` 与 `@objectstack/driver-sql` **互不依赖**,两者都只依赖\n`@objectstack/spec`;`@objectstack/core` 反过来依赖 spec,不在两者之下。因此能同时被两侧\nimport 的共享位置**只有 `packages/spec`**,而本单裁定 spec 只读引用。取舍如下:\n\n- **两侧都不再自行理解格式** —— 定位参数(prefix / suffix)一律由 spec 导出的\n  `renderAutonumber` 产出,调用方算好传入,driver 只收参数(分诊给出的 fallback 形态)。\n  两侧都没有第二套 token 解析。\n- **残余重复**是「拿到两个字符串后怎么用」的那几行。为此加了**跨侧一致性测试直接断言**\n  两侧答案相等(见下),使收敛由测试强制而非靠人记住。\n- 更彻底的做法是把这条「读回」规则作为 `renderAutonumber` 的逆放进\n  `packages/spec/src/data/autonumber-format.ts`(该文件的头注释本就写明它存在的理由是\n  「shared by the ObjectQL engine and the SQL driver」)。这超出本单裁定的文件面,\n  **未做**,已在报告中作为待定问题交回维护者。\n\n`getNextSequenceValue` **只多转发一个位置参数**(签名末尾 `suffix = ''` + 调用处一行),\n其锁/插入/更新与序列语义**一行未改** —— 给 #5495 的预期落点留出干净的接续面。\n\n## 测试\n\n新增三个文件,共 **23** 个用例:\n\n| 面 | 文件 |\n|:---|:---|\n| ① 引擎侧(9) | `packages/objectql/src/engine-autonumber-seed-suffix.test.ts` |\n| ② driver-sql 侧(9) | `packages/drivers/driver-sql/src/sql-driver-autonumber-suffix.test.ts` |\n| ③ 跨侧一致性(5) | `packages/runtime/src/autonumber-seed-cross-side-parity.integration.test.ts` |\n\n③ 之所以落在 `packages/runtime`:objectql 与 driver-sql 互不依赖,**能同时看见两侧实现的\n最小包就是 runtime**(它同时依赖 objectql / driver-memory / driver-sql,且已有\n`bulk-write-real-driver.integration.test.ts` 这一真实驱动集成先例)。仅新增测试文件,\nruntime 源码零改动。用例用**真实** `InMemoryDriver`(`supports = {}`,正是引擎兜底路径)\n与**真实** `SqlDriver`(better-sqlite3)各持同一批 fixture 行,断言\n`expect(engineValue).toBe(sqlValue)`。\n\n④⑤ 对照 pin:无后缀格式(`D-{0000}`、`{0000}`)两侧现行为不变;无格式的 legacy 无锚\n读法两侧逐字保留(含 driver-sql 对 `SO-2024-0007` 仍拼成 20240007 的显式 pin)。\n\n## 反向验证(方向先写死,再运行;预测 vs 实测)\n\n预测在执行前写入 `predictions.md`。⚠️ **`packages/runtime` 从 `dist` 解析工作区依赖**,\n所以每次回退都重新构建了对应包 —— 未重建时跨侧用例会假绿(§9 陈旧产物陷阱,已实测踩到一次)。\n\n| 回退 | 用例 | 预测 | 实测 |\n|:---|:---|:---|:---|\n| 引擎 | 引擎 A `{000}-{YYYY}`(2 例) | 🔴 | 🔴 `2 failed / 7 passed` |\n| 引擎 | 引擎 B `CASE-{000}-{YYYY}` | 🟢 **反向** | 🟢 |\n| 引擎 | 引擎 ④ 对照 / ⑤ legacy | 🟢 | 🟢 |\n| 引擎 | driver 侧 9 例 | 🟢 | 🟢 |\n| 引擎 | 跨侧 A + 跨年 | 🔴 | 🔴 `expected '2027-2026' to be '004-2026'` |\n| 引擎 | 跨侧 B + 对照 | 🟢 **反向** | 🟢 |\n| driver | driver A(3)+ B(2) | 🔴 | 🔴 `5 failed / 4 passed` |\n| driver | driver ④ 对照 / ⑤ legacy | 🟢 | 🟢 |\n| driver | 引擎侧 9 例 | 🟢 | 🟢 `9 passed` |\n| driver | 跨侧 A + B + 跨年 | 🔴 | 🔴 `3 failed / 2 passed` |\n| driver | 跨侧 ④ 对照 | 🟢 | 🟢 |\n\n**全部命中,含一处预期内的方向反转。** 派发模板写的是「回退引擎侧 ⇒ ① ③ 转红」;这对\nfixture **A**(`{000}-{YYYY}`,prefix 为空)成立,对 fixture **B**(`CASE-{000}-{YYYY}`,\nprefix 非空)**不成立** —— 引擎的 prefix 分支在 main 上本来就把 B 读对了,**B 只有\ndriver 一侧是坏的**。这里如实记录方向,而不是把用例硬凑成模板预设的红。回退引擎后跨侧\n断言给出的 `expected '2027-2026' to be '004-2026'`,正是 issue 正文预言的那个烧号。\n\n## 命令输出\n\n```\npnpm --filter @objectstack/objectql --filter @objectstack/driver-sql test\n  driver-sql   Test Files  71 passed | 4 skipped (75)    Tests  976 passed | 46 skipped (1022)\n  objectql     Test Files  147 passed (147)              Tests  2463 passed (2463)\n\npnpm --filter @objectstack/runtime test\n  Test Files  111 passed (111)                           Tests  1611 passed (1611)\n\ntypecheck(objectql / driver-sql / runtime)          三包 Done,无错误\ncheck:engine-double-contract   OK — 84 pinned, 133 in the DEBT ledger, 2 exempt.\ncheck:query-options-erasure    OK — ratchet holds; test surface 263 site(s), 未新增(无 `{} as any`)\ncheck:verify-stand-in          OK — 2 guarded, 5 exempt, 10 call sites, 0 asserted driver arguments\ncheck:type-check-debt          OK — none above its recorded number(台账只缩不抬;先跑了\n                               turbo run build 覆盖 packages 全量,70/70 成功,\n                               满足 #6510 的「拒绝在未构建闭包上测量」)\nnode scripts/check-nul-bytes.mjs   OK (scanned 6132 tracked text file(s))\n```\n\n三个新测试文件各自的 `tsc --noEmit` 贡献为 **0** 条错误(逐文件过滤确认)。\n\n## 未触碰\n\n- `getNextSequenceValue` 的序列逻辑(#5495 预期落点)—— 仅末尾多一个转发参数\n- #6467 的 `keysetWalk` 扫描结构 —— 并有 pin 断言下推形状不变\n- `packages/spec` —— 零 diff\n\n## 顺带发现(未在本 PR 修)\n\n无格式的 autonumber 字段两侧**渲染**结果不同:driver-sql 用 `{0000}` 兜底\n(`sql-driver.ts:4322` / `:4404` `const fmt = rawFmt || '{0000}'`)渲染出 `0001`,而引擎\n`engine.ts:2115` 用空格式渲染出裸 `1`。这与本单的**播种解析**缺陷机制不同(在渲染兜底,\n不在解析),按 Prime Directive #10 单独开单。\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_019Q7oc7ASjh8yxyS3Yz78We)_"
}
