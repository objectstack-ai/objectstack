{
  "number": 11093,
  "kind": "issue",
  "title": "check-cross-package-test-inputs: a resolve() call whose literal sits on a following line yields no roster name — multi-line new URL() is recognised, multi-line resolve() is not",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "domain:devx"
  ],
  "assignees": [
    "os-steve"
  ],
  "milestone": null,
  "type": "Task",
  "created_at": "2026-08-22T16:44:11Z",
  "updated_at": "2026-08-23T23:37:10Z",
  "closed_at": "2026-08-23T23:36:17Z",
  "closed_by": {
    "login": "os-steve",
    "id": 317605050,
    "type": "User"
  },
  "merged_at": null,
  "comments": 5,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/11093",
  "body": "Observed while widening a spec pin's population on #10848 (PR #11092), measured on that branch.\n\n## The asymmetry\n\n`scanPathExpressions` in `scripts/check-cross-package-test-inputs.mjs` recognises a read whose `new URL()` argument spans lines (self-test: \"flags a read whose argument is a multi-line new URL()\"), but a `path.resolve()` seed call whose relative-literal argument sits on its own line yields NO roster name. Measured, same file, same literal:\n\n```ts\n// NOT seen — glob went \"held by nothing\", gate exit 1:\nconst P = path.resolve(\n  HERE,\n  '../../../../.claude/skills/spec-property-retirement/SKILL.md',\n);\n// seen — gate exit 0:\nconst P = path.resolve(HERE, '../../../../.claude/skills/spec-property-retirement/SKILL.md');\n```\n\nThis is inside the header's stated limit family (\"an argument it cannot fold\" yields no name, never a wrong one), and the per-glob staleness check (#10566) did catch it — the system failed safe. Two costs remain:\n\n1. The multi-line spelling is what a formatter produces for a long path, so the miss is the DEFAULT spelling for long relative literals, not an exotic one.\n2. The failure text for the caught case leads with \"Delete the glob (and its turbo.json input) if the read is gone\" — for a read that is real and merely unseen, the first-listed remedy deletes a correct declaration. The `heldBy` escape hatch is mentioned, but nothing in the message says \"or your read may be spelled across lines; single-line it\", which is the actual fix.\n\n## Suggested disposition\n\nEither teach the resolver the line-spanning `resolve()`/`join()` argument shape (with a `--self-test` case, per the header's own rule), or add the multi-line spelling to the failure text's remedy list. No urgency: the reverse check holds the line today.\n\nRefs: #9763 (the reconstruction that added the RESOLVED half) · #10566 (the per-glob staleness leg that caught this).\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01MsbKEG4LtERSLaDrbehM3e)_"
}
