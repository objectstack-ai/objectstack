{
  "number": 2513,
  "kind": "pull_request",
  "title": "fix(console): 打包 console 时注入本仓库 client,杜绝新 UI + 旧 client 漂移",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "baozhoutao",
    "id": 6194462,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/s",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-02T04:48:09Z",
  "updated_at": "2026-07-02T12:29:34Z",
  "closed_at": "2026-07-02T12:29:33Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-02T12:29:33Z",
  "comments": 1,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/2513",
  "body": "Fixes #2512\n\n## 问题\n\n`@objectstack/console@11.5.0` 运行时报 \"The connected @objectstack/client does not support async import jobs.\"。\n\n根因:`build-console.sh` 按 **objectui 自己的 lockfile** 安装依赖后打包,console bundle 里的 client 版本由 objectui lockfile 决定。发版时新 client 还没上 npm,objectui lockfile 不可能指向它 → 发布产物 = 新导入 UI + client 11.2.0(无 `data.createImportJob`)。已在 os-tianshun-ehr 逐环验证(实际 serve 的 dist 中 `import/jobs` 出现 0 次)。\n\n按旧机制,今后凡是给 client 加新 API 且 UI 依赖,都要「发 framework → objectui bump pin → framework 再发一版」两段式。本 PR 终结这个问题。\n\n## 改动(仅 `scripts/build-console.sh`)\n\n1. 构建 console 前 `export OBJECTSTACK_CLIENT_DIST=<framework>/packages/client`,vite 把**本仓库、本版本**的 client 打进 bundle(objectui 的 `apps/console/vite.config.ts` 钩子,main 已有;pinned SHA 缺钩子时报错退出,不静默漂移)。client dist 缺失时先构建。\n2. 构建拆两步:deps 走 turbo(可缓存、与 env 无关),console 本体直接跑自己的 build 脚本——**turbo v2 strict env 会剥掉未声明的环境变量**,第一次注入尝试就是这样静默失败的(实测复现)。objectui 侧同步在 turbo.json 声明该 env(objectstack-ai/objectui#2146)。\n3. 产物 canary 断言:构建完成后 `dist/assets` 必须含 `import/jobs`(`CONSOLE_BUNDLE_CANARY` 可换),否则脚本失败——旧 client 再也不可能静默发布出去。\n\n## 验证\n\n在**当前 pinned SHA `1432efe81`**(lockfile 仍解析 client 11.2.0)上完整跑 `scripts/build-console.sh`:\n\n- 修改前产物:`import/jobs` 0 次(即线上 11.5.0 的状态)\n- 修改后产物:`import/jobs` 进入 `dist/assets`,canary 断言通过,脚本 exit 0\n\n即:**不等 objectui 换 SHA,以现 pin 重打 dist 即可发 `@objectstack/console@11.5.1` 修复线上。**\n\n另外 objectui 侧真实链路 E2E(真 console + npm client@11.5.0 + 真后端,后台导入→历史→撤销)已通过,见 objectstack-ai/objectui#2146。\n"
}
