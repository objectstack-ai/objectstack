{
  "number": 11907,
  "kind": "issue",
  "title": "Local turbo cache served a DTS-less `@objectstack/plugin-auth` build in a fresh worktree with `OS_SKIP_DTS` unset — its own typecheck then reds on a diff that never touched it",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "os-warren",
    "id": 318460043,
    "type": "User"
  },
  "labels": [
    "domain:devx"
  ],
  "assignees": [
    "yinlianghui"
  ],
  "milestone": null,
  "type": "Bug",
  "created_at": "2026-08-25T00:02:11Z",
  "updated_at": "2026-08-25T08:16:13Z",
  "closed_at": "2026-08-25T08:15:20Z",
  "closed_by": {
    "login": "yinlianghui",
    "id": 6219465,
    "type": "User"
  },
  "merged_at": null,
  "comments": 6,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/11907",
  "body": "Observation from working #11377 (PR #11906) in a fresh per-task worktree; recorded, not diagnosed. Filed unassigned.\n\n## Measured (2026-08-25, fresh worktree, `pnpm install` clean, `OS_SKIP_DTS` verified unset in the environment)\n\n1. `pnpm exec turbo run typecheck --filter '...@objectstack/driver-sql' --filter '...@objectstack/objectql'` (which builds dependencies via `^build`) produced a `packages/plugins/plugin-auth/dist/` containing `index.js` / `index.mjs` / maps but **zero `.d.ts` files**. `@objectstack/plugin-auth#typecheck` then failed its own second program (`tsconfig.examples.json`): `examples/basic-usage.ts(13,28): error TS7016: Could not find a declaration file for module '@objectstack/plugin-auth'`.\n2. A direct `pnpm --filter @objectstack/plugin-auth build` emitted the DTS files fine (`DTS dist/index.d.ts 272.91 KB` etc.) — the build itself is healthy.\n3. The next `turbo run typecheck` **restored the DTS-less artifact over the good dist** (cache hit on the unchanged hash) and failed identically — so the bad artifact lived in the local turbo cache under the current input hash, and every turbo run re-materialized it.\n4. `turbo run build --filter @objectstack/plugin-auth --force` replaced the cache entry; all subsequent runs green (48-package closure typecheck, 0 TS errors).\n\n## Why it is worth a record\n\n- `OS_SKIP_DTS` is declared in `turbo.json` `globalEnv`, and it was unset here — so the documented \"skip-DTS build hashes differently\" defense should have applied, yet a DTS-less artifact sat under the hash of a normal build. Mechanism unestablished: candidates include the tsup DTS worker dying without failing the parent build under memory pressure (the shape #4845 recorded for spec's DTS build at a different scale), or something else entirely.\n- The failure presents exactly as \"your change broke plugin-auth\" on a diff that never touched it — the AGENTS.md §9 stale-artifact class, but through the **cache**, so the §9 remedy (`pnpm build`) does NOT clear it: a plain rebuild is a cache hit that restores the same bad artifact. The working remedy is `--force` (or clearing the entry).\n- CI is immune (fresh checkout); this only costs local/worktree rounds.\n\nNo fix proposed here; recording the signature and the `--force` remedy so the next agent recognizes it in one step instead of re-diagnosing per gate.\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01Rxnd8cyFnoU8V5y21PaTsy)_"
}
