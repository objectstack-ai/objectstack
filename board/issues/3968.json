{
  "number": 3968,
  "kind": "pull_request",
  "title": "feat(runtime,datasource): default-datasource factory seam + adopt pre-built drivers via one connect path (#3826)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "dependencies",
    "documentation",
    "size/m",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-30T00:12:42Z",
  "updated_at": "2026-07-30T00:31:14Z",
  "closed_at": "2026-07-30T00:31:13Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-30T00:31:13Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/3968",
  "body": "#3826 收尾的 framework 侧:为 cloud 仓库的最后收敛铺接缝,并补上 #3869/#3886 留下的三处欠账。开源侧行为零变化(不注入 factory 时逐字节走原路径)。\n\n## 为什么需要接缝\n\ncloud 组装(`environment-kernel-factory` 的每环境主 driver、control-plane preset)有两个共享 factory 覆盖不了的性质:driver kind 在开源之外(`turso`),以及**实例池化超出单个 kernel**(control-plane driver 同时是每个环境 kernel 的 proxy 基座;每环境 driver 跨 kernel 重建缓存 —— 按定义重建会成倍开 pool)。没有接缝,cloud 只有两条路:留在旧 `DriverPlugin` 路径(判决在 `ObjectQLEngine.init()`,恰是 #3826 要退役的第二实现),或者 fork 整套 connect 编排。两条都会重演 #3741 → #3758。\n\n## 改动\n\n- **`DefaultDatasourcePlugin` 接受注入的 `IDatasourceDriverFactory`**(缺省 = 共享开源 factory,不传时行为不变)。factory 只改变 `create()` 返回什么;policy-free init connect、`bootCritical` fail-fast、`OS_ALLOW_DRIVER_CONNECT_FAILURE`、start() replay 进留存状态 —— 注入与否完全一致,新测试钉死了这一点(连不上的被收养实例吃到**同一份**判决)。\n- **`createPrebuiltDriverFactory(driver, { driverId?, fallback? })`**(service-datasource):第一轮讨论里缺失的「adopt 现成 driver」入口,落成 **factory 形态**而不是第二个 connect 入口 —— `create()` 每次返回**同一实例**,构造/池化/复用留给 host,只有判决收敛。TSDoc 明确写了它不是常规用法:能表达成 `{ driver, config }` 的就该用定义。\n- **verify dogfood harness 改走 declared default**(`sqlite-wasm` 定义经 `DefaultDatasourcePlugin`),不再用预构建 `DriverPlugin` —— dogfood gate 从此覆盖 `objectstack dev`/serve 真实使用的收敛路径。ADR §Risk 承诺的 \"behind the dogfood gate\" 此前其实没兑现,这次兑现。顺带删掉 verify 对 `driver-sqlite-wasm` 的直接依赖(经 factory 懒加载)。\n- **ADR-0062 header 修正**:自 #3869/#3886 起 header 里 \"the `default` driver still has its own connect + failure path\" 对开源侧已不成立(#3836 批评它高估自己,合并后又变成低估)。现在:D1 对每条开源 boot 路径 implemented;cloud 组装是最后的第二实现。D1 正文补 fourth-pass 段落记录接缝设计,余项表述从 `cloud-stack.ts` 修正为真正的主 driver 组装点(`environment-kernel-factory` + control-plane preset)。\n- **`degraded-boot-parity.test.ts` 头注释重写**:原文还在把 `default` 描述成 `driver.*` kernel service + engine-init 连接(pass 2 之前的形态)。现在如实说明 engine 侧判决为什么仍是活代码(boot 再验证、`DriverPlugin` 逃生舱、cloud 未收敛),以及守卫为何在 cloud 收敛后仍然保留(只要 `init()` 还能抛 connect 判决,它就必须与 service 的判决一致)。测试本体不动。\n\n## 验证\n\n- service-datasource 133(+7 新:supports/create 语义、fallback 派发、经 `DatasourceConnectionService` 的收养 —— 身份而非等价、自然名保留、fail-fast 判决一致)\n- runtime 839(62 files,+2 新:注入 factory 收养 host 实例后端到端读写、收养实例连接失败吃同一 bootCritical 判决 + 同一逃生阀)\n- verify 7、showcase dogfood 58(10 files)、app-crm 20 —— 全部经新 harness 路径\n- `pnpm turbo build` 受影响包全绿;根配置 ESLint 对本 diff 触及文件零告警(verify 包有 3 处 `@typescript-eslint/no-explicit-any` 规则解析报错,stash 对照确认是既有环境问题,不在本 diff 行内)\n\n## 后续\n\ncloud 仓库用本接缝收敛 `environment-kernel-factory` + control-plane preset(随后另开 PR);收敛后 #3826 才可关闭。\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01GK5nd3gtehcAhrF6Zzp2on)_"
}
