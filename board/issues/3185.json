{
  "number": 3185,
  "kind": "pull_request",
  "title": "fix(spec): declare `ownership` as a first-class ObjectSchema field (#3175)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/s",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-18T06:40:20Z",
  "updated_at": "2026-07-18T06:53:33Z",
  "closed_at": "2026-07-18T06:53:30Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-18T06:53:30Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/3185",
  "body": "## Context\n\nFixes #3175 — the `ownership` collision surfaced during #3168 (#3058).\n\nThe object-level **record-ownership model** — `ownership: 'user' | 'org' | 'none'`, which drives the registry's `owner_id` auto-provisioning (`applySystemFields`) — sat in a contract hole:\n\n- **engine-read** via `(schema as any).ownership` (`registry.ts:272`), with a tested opt-out (`registry.test.ts`),\n- **validator-rejected**: `ObjectSchema.create()` threw on `ownership` as an unknown top-level key (ADR-0032 \"no silent failure\" / #1535), so the tested opt-out couldn't be authored through the sanctioned path,\n- **scaffold-emitted** as a now-invalid `ownership: 'own'` (`generate.ts`),\n- **CLI-misdisplayed** as the unrelated package-contribution kind `own`/`extend` (`info.ts`).\n\nPer the direction chosen on #3175, this **formalizes the property the engine already honors** (Option A) rather than renaming it or removing the author surface.\n\n## Changes\n\n- **spec** — `ObjectSchema` declares `ownership: z.enum(['user','org','none']).optional()` with a describe + JSDoc that explicitly distinguishes it from the `own`/`extend` contribution kind. The registry now reads `schema.ownership` off the typed schema (no `as any`). A retired `ownership: 'own' | 'extend'` value fails with guidance pointing at the record-ownership model and noting `own`/`extend` is set via `registerObject`, not on the object schema.\n- **cli** — the `object` scaffold no longer emits the invalid `ownership: 'own'` (owner injection is the default; consistent with the app/plugin scaffold cleanup in CHANGELOG #3876); `objectstack info` labels the record model with the correct `user` default.\n- Regenerated `content/docs/references/data/object.mdx` (one new row); added the `ownership` liveness-ledger entry and a changeset.\n\n**No runtime behavior change**: `applySystemFields` and its `owner_id` injection logic are untouched — this makes the already-honored property legally authorable and consistently typed.\n\n## Verification\n\n- `@objectstack/spec` — full suite 6914 pass (+3 new `ownership` create() tests: opt-out values accepted, omitted → undefined, retired `own` rejected with guidance); `check:liveness` green; reference doc regenerated deterministically.\n- `@objectstack/objectql` — `registry.test.ts` 63 pass (opt-out behavior equivalence); `registry.ts` typechecks with the `as any` removed (verified against the built spec dts).\n- CLI `info`/`generate` have no type surface change (`obj` is `any`; scaffold is a template string).\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_014343Qv6DFAykuAJc9yjANb\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_014343Qv6DFAykuAJc9yjANb)_"
}
