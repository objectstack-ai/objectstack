{
  "number": 15884,
  "kind": "issue",
  "title": "check-half-states: read the landed release rule — H1/H24 prose says the definitional readings, and a new report-only row catches a `Release:` newer than the last `Claim:` with the assignee still set (and the exit that left no record)",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "claude[bot]",
    "id": 209825114,
    "type": "Bot"
  },
  "labels": [
    "domain:skills",
    "priority:p3",
    "tooling"
  ],
  "assignees": [
    "os-steve"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-09-05T12:29:29Z",
  "updated_at": "2026-09-05T14:43:19Z",
  "closed_at": "2026-09-05T14:29:35Z",
  "closed_by": {
    "login": "os-steve",
    "id": 317605050,
    "type": "User"
  },
  "merged_at": null,
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/15884",
  "body": "Filed by the `domain:skills` seat (session `session_019RfFHiRCSs3JXLK4cwcfox`, os-steve) as the follow-up the seat named on PR #15848's verdict. Self-triaged `priority:p3`, `pm:queue`, pure code (`scripts/pm/check-half-states.mjs`).\n\n## What landed (PR #15848, merged 2026-09-05T11:52Z)\n\n`.claude/skills/pm-dispatch/SKILL.md` now states, as rules: `pm:queue` 卡恒无 assignee,有即半态 (line 107); `pm:dispatched` 恒带 assignee (line 109); 释放是显式动作:让卡离手者同笔清 assignee + `Release:` 行(会话/因/去向);下一任重新认领 (line 465); the dead-claim reclamation routes through 释放回队 with the `Release:` line carrying the reason (line 493). `references/state-machine.md` carries the releaser line beside the applier and remover.\n\n## What is stale in the sweep\n\n- H1 (`h1DispatchedNoAssignee`) — the header inventory (line 88) still explains the row as \"dispatch marks a claim; a claim is …\" rather than as the definitional reading the rule now gives it (`pm:dispatched` always carries an assignee).\n- H24 (`h24QueuedWithAssignee`, sentence at ~4425–4440) still narrates the measured origin (\"a state ROLLBACK that swapped the label and left the field … only dead-claim reclamation ever said 「同笔摘 assignee」\") — the rule now says every exit from ownership clears the assignee in the same stroke and leaves a `Release:` line; the sentence should name that rule and its remedy (post the `Release:` line and clear the field, or re-claim), not the history.\n- No row reads the new record: a `Release:` line newer than the last `Claim:` on a card whose assignee is still set is a half-done release (the comment was written, the field was not cleared); the inverse — a `pm:queue` card with no assignee and no `Release:` after its last `Claim:` — is an exit that left no record. Both are report-only patrol input in the H1/H24 family.\n\n## What this card does\n\n1. H1 and H24 prose aligned to the landed rule (quote the SKILL.md lines, never paraphrase them into new Chinese); predicates unchanged.\n2. One new report-only row over the population H1/H24 already read (no new fetch class: `Claim:` and `Release:` lines come from the comment threads H2 already reads; the row is judged only where those comments are already fetched, and says UNJUDGED where they are not): a `Release:` newer than the last `Claim:` with the assignee still set → finding; a `pm:queue` card whose thread has a `Claim:` and no later `Release:` and no assignee → finding, stated as the lower bound it is. Declared in the registry as `H plus digits`, band `state`, with self-test cases in both directions and an ablation.\n3. Self-test count does not drop; `SUMMARY_CLAUSE_ANCHORS` gains the row's clause; `SWEEP_COUNT_KEYS` gains any new counter.\n\n## Serial\n\n`scripts/pm/check-half-states.mjs` is in no flight and no open PR (PR #15795 and #15755 landed). Pure code ⇒ in-seat review, then ready + auto-merge by the seat.\n\n## Dedup\n\nREST enumeration 2026-09-05T10:3xZ (1,200 items, 797 issues, positive controls hit) found no card on a release act or the assignee's exit; #15846 (closed by PR #15848) is the rule half, and its body names this sweep half as the follow-up.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
