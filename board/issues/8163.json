{
  "number": 8163,
  "kind": "issue",
  "title": "[finding] field-level `currency` is rejected with no pointer to `currencyConfig.defaultCurrency` — the natural spelling gets history, not a prescription",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "huangyiirene",
    "id": 7665279,
    "type": "User"
  },
  "labels": [
    "pm:dispatched"
  ],
  "assignees": [
    "os-zhuang"
  ],
  "milestone": null,
  "type": "Task",
  "created_at": "2026-08-12T18:24:00Z",
  "updated_at": "2026-08-16T09:47:54Z",
  "closed_at": "2026-08-16T09:47:54Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": null,
  "comments": 9,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/8163",
  "body": "Found while implementing #7918 (currency `precision` vs ISO 4217 digits). Recorded, not claimed.\n\n## Measured\n\n`FieldSchema` is strict, and a field-level `currency` key is not declared, so writing the natural spelling is rejected — but the rejection carries only the surface history line, with no suggestion and no prescription:\n\n```\nUnrecognized key(s) on this field: `currency`. Until #4001 closed this shape these were\ndropped silently — the field was still created, minus whatever the key was meant to\nconstrain, protect or compute.\n```\n\nMeasured against the built spec at `main` 37b82ed5b (`FieldSchema.safeParse({ name: 'amount', label: 'Amount', type: 'currency', currency: 'JPY' })`). Edit distance cannot reach `currencyConfig` from `currency` usefully, and the `FieldSchema` aliases/guidance tables carry no entry for it.\n\n## Why the spelling is not hypothetical\n\n- objectui's `resolveFieldCurrency` (`packages/i18n/src/currency.ts`) reads `field.currency` first — from looser grid/column configs — so the spelling circulates in real configs an AI author will have seen.\n- The #7918 ruling itself reached for the phrase \"field-level `currency`\" as a statically-known-currency form; in the spec the only declarable form is `currencyConfig` in `fixed` mode. An author who intuits the field-level spelling gets no bridge to the declarable one.\n\n## Fix shape (one line of judgement, not a rename)\n\nThe right target is a NESTED key, so an `aliases` rename entry does not fit — this is the `storageNotNull`-style `guidance` prose case on `FieldSchema`'s strict-object options, e.g.:\n\n> `currency` is not a field key — a fixed currency is declared as `currencyConfig: { currencyMode: 'fixed', defaultCurrency: 'JPY' }`. A field without one uses the tenant default at runtime.\n\nSame class as #7887 (a key family with no vocabulary to point an author at). Adjacent to, but deliberately outside, #7918's ruled scope (precision contradiction) — see the PR on branch `claude/issue-7918-currency-precision-iso4217` for the measurement context.\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_0123k4cam2jEAkPmbJeoaY3r)_"
}
