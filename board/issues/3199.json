{
  "number": 3199,
  "kind": "pull_request",
  "title": "feat(spec)!: remove form-surfaced dead metadata props + correct 3 misclassified-live entries (#2377)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/l",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-18T08:11:15Z",
  "updated_at": "2026-07-18T12:14:45Z",
  "closed_at": "2026-07-18T12:14:44Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-18T12:14:44Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/3199",
  "body": "The next enforce-or-remove slice of #2377 (ADR-0049). Reconnaissance into **both** framework and objectui turned up two ledger misclassifications and one 35-file blast radius, which shaped the scope below. Versioned `minor` per the launch-window policy.\n\n## Removed — dead, no runtime reader (verified in framework **and** objectui)\n- **field**: `columnName`, `index`, `referenceFilters` → **empties the field dead set**.\n  - `columnName` was inert even for managed objects (the SQL driver's `createColumn` hardcodes column = field key; introspection reads `row.column_name`, the inverse). So it also removed the now-moot **ADR-0062 D7** rejection lint (`validate-expressions.ts`), the dead `StorageNameMapping.resolveColumnName`/`buildColumnMap`/`buildReverseColumnMap` helpers, and **closes ADR-0062 R10** — external physical-column mapping is `external.columnMap` only.\n  - `index` → declare in object `indexes[]`; `referenceFilters` → `lookupFilters`.\n- **object**: `tags`, `active`, `abstract` → now **rejecting tombstones** in `UNKNOWN_KEY_GUIDANCE`.\n- **agent**: `tenantId`.\n\nDropped from the authoring forms (`field/object/agent.form.ts`), the hand-crafted `metadata-protocol` form-schema mirror, and the **regenerated metadata-forms i18n bundles** (all 4 locales, 112 key deletions). Reference docs + ledger README regenerated.\n\n## Corrected to `live` — the ledger was wrong (readers verified by reading the code)\n- **object `isSystem`** — `plugin-sharing` `effectiveSharingModel:74` defaults a no-`sharingModel` `isSystem` object to public; also read by `validate-security-posture.ts:98`. **Kept.**\n- **object `enable.searchable`** — `metadata-protocol` `protocol.ts:2967` uses `enable.searchable === false` as a global-search opt-out. **Kept.**\n- **action `type:'form'`** — objectui `ActionRunner.executeForm` routes it to the FormView at `/forms/:target` (fixed the \"Log Time does nothing\" report); a build-time lint validates the target. **Kept.**\n\n## Deliberately deferred\n- **`object.enable.trash` / `enable.mru`** — dead, but inert `default(true)` flags set by ~35 `sys-*.object.ts` files; removing them is high-churn / low-value. Left `dead` (authorWarn-skipped).\n- **objectui designer `active`/`enabled` toggle** (`MetadataObjectsPage.tsx`) — reads `raw.active` off objectui's own loose local type and writes it back; the runtime registers objects via `ObjectSchema.parse()` which **strips** unknown keys, so this is **non-breaking** (not a hard error). Removing the inert enable/disable control is an objectui UX call, tracked as a separate optional follow-up.\n\n## Verification\n- spec **6873** tests + DTS typecheck + **liveness gate** + `check:docs` (256 files in sync) all green; `api-surface.json` unchanged.\n- lint `validate-expressions` **30**, cli `lint-liveness-properties` **13**, metadata-protocol **46** green.\n- No production `*.object.ts` sets any removed prop.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_011XchBKhKdjbhQZymt9Gn9B)_"
}
