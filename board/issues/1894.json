{
  "number": 1894,
  "kind": "issue",
  "title": "[P2] Inverse drift — renderers read undeclared props",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "priority:p2"
  ],
  "assignees": [
    "os-zhuang"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-06-15T06:58:26Z",
  "updated_at": "2026-07-24T12:13:11Z",
  "closed_at": "2026-07-24T12:13:11Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": null,
  "comments": 1,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/1894",
  "body": "Part of the metadata liveness audit umbrella #1878 (P2 — spec hygiene, cluster #6).\n\n## Problem\n**Inverse drift**: renderers depend on props the spec does **not** declare. These break a strict `Schema.parse()` — the renderer needs a key that a spec-validating author would never be allowed to write.\n\n## Checklist\n- [x] dashboard: `component` / `data` / `rowField` / `columnField`\n- [x] view: `ObjectView` form-adapter keys\n- [x] app: `accentColor` / `badgeVariant` / `separator` (also listed under naming drift)\n\nFor each: either add the prop to the spec (if it's legitimately authorable) or remove the renderer's dependency on it so strict validation holds.\n\n## Resolution\nAll three reconciled so strict validation holds:\n- **dashboard** — `component`/`data` + the pre-ADR-0021 inline-analytics keys (`rowField`/`columnField`/…) are quarantined and rejected by the strict `DashboardWidgetSchema` error map (`dashboard.zod.ts`), pointing authors at `dataset`/`dimensions`/`values` (renderer-only extras → `options`). *(already on `main`)*\n- **app** — `accentColor`/`badgeVariant`/`separator` declared on `AppSchema` (`app.zod.ts`). *(already on `main`)*\n- **view** — the structured `buttons`/`defaults` (added to `FormViewSchema` under #2998 Track A) are now consumed by the objectui `ObjectForm` renderer, so the two liveness-ledger entries flip `experimental → live`. The remaining flat form-adapter keys (`customFields`/`className`/`fields`) stay classified as renderer-only ObjectUI extensions, not spec surface (objectui#2545). Landed as a coordinated pair:\n  - objectstack-ai/objectui#2790 — `ObjectForm` folds `buttons`/`defaults` onto its flat props (merged `6dee2cb`)\n  - #3417 — flips the ledger to `live`, drops the ADR-0078 markers, regenerates the `view` reference doc, updates the audit docs (merged `c4df271`)\n\nBoth merged with green CI. Closing.\n\n## Evidence\n- `docs/audits/README.md` cluster #6\n- `docs/audits/2026-06-dashboardschema-property-liveness.md`, `2026-06-viewschema-property-liveness.md`, `2026-06-appschema-property-liveness.md`"
}
