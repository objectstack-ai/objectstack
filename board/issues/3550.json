{
  "number": 3550,
  "kind": "pull_request",
  "title": "docs(spec): correct field.readonly describe re: import readonly-strip exemption (#3493)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/xs",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-27T03:57:30Z",
  "updated_at": "2026-07-27T04:41:22Z",
  "closed_at": "2026-07-27T04:41:20Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-27T04:41:20Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/3550",
  "body": "## What & why\n\nSmall doc correctness follow-up to #3493/#3497.\n\nThe `FieldSchema.readonly` describe ended with:\n\n> …A stripped INSERT field still falls back to its `defaultValue`; **system-context writes (import, seed replay, migration) are exempt.**\n\nThat clause is wrong on two counts, and #3493 is exactly what surfaced it:\n\n1. **A normal data import is NOT system-context.** Its write context is session-derived (`isSystem: false`, per `resolve-execution-context.ts`) — that's *why* #3493 existed (the strip *did* apply to imports). Listing \"import\" alongside `seed replay`/`migration` as an `isSystem` exemption is misleading.\n2. It predates #3497's **`preserveAudit` whitelist** — the new opt-in path by which a *historical* import bypasses the strip for a bounded whitelist.\n\n### Change\n\n`packages/spec/src/data/field.zod.ts` — the `readonly` describe now reads:\n\n> …A stripped INSERT field still falls back to its `defaultValue`. Exempt from the strip: `isSystem` writes (seed replay, migration), and an opt-in \"historical\" import (`preserveAudit`, #3493) — which admits a whitelist (the audit/timestamp family plus author-declared business `readonly` fields). A normal (non-system) import is NOT system-context and still strips.\n\nRegenerated `content/docs/references/data/field.mdx` to match (`gen:schema && gen:docs`; `check:docs` in sync).\n\n## Notes\n\n- Describe-string + regenerated reference doc **only** — no schema shape, behavior, or api-surface change. Empty changeset (releases nothing).\n- `check:api-surface` shows a diff locally, but it's **stale-dist noise** unrelated to this change (it lists `MessageFormat`/`WebhookReceiver`/`Breakpoints`/`JobRetryPolicy` from other recent merges); the branch's spec `dist` wasn't rebuilt after resetting onto `main`. CI builds fresh, so it will compute the current-`main` surface and pass. `spec tsc --noEmit` is clean.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01B5rdfBKjkbcoEif4KUV6xE)_"
}
