{
  "number": 3657,
  "kind": "pull_request",
  "title": "feat(lint): reference-integrity validation for object and action names (#3583)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/xl",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-27T13:13:34Z",
  "updated_at": "2026-07-27T13:57:38Z",
  "closed_at": "2026-07-27T13:57:36Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-27T13:57:36Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/3657",
  "body": "实现 #3583 的对象名 / 动作名引用完整性校验(方案文档见已合并的 #3640)。\n\n## 根因:前缀猜测\n\n现存所有交叉引用检查,回答「这个名字会不会来自别的包?」用的都是**前缀猜测**(`startsWith('sys_')` 直接跳过)。这个猜测分不清 `sys_user`(真实)和 `sys_approval_process`(虚构 —— ADR-0019 废除,任何包都不注册),所以每一个虚构的平台前缀引用都畅通发布。\n\n因此先在 spec 落一份策展注册表 `PLATFORM_PROVIDED_OBJECT_NAMES`(73 个本仓库对象 + 5 个 cloud 对象),沿用 `PLATFORM_CAPABILITY_NAMES` 先例,lint→spec 单向依赖不变。注册表由一个 conformance 测试扫描各包 `*.object.ts` 声明来保证不漂移 —— 一份过期的注册表比它取代的启发式更危险,因为使用方现在会信任它。\n\n## 新增规则(同一 PR 内同时接入 `os validate` 与 `os lint`,避免再次接线漂移)\n\n- **`validate-object-references`** —— action param 的 `reference`/`objectOverride`、仪表盘 `globalFilters[].optionsFrom.object`、导航 `requiresObject` 能力门。严重级按可解析性分档:未前缀且解析不到 → **error**(纯 typo 类,`object: 'user'` 而平台对象是 `sys_user`);平台前缀但注册表里没有 → **warning**(第三方包仍可能提供,advisory 是诚实的上限)。\n- **`validate-action-name-refs`** —— 按名字绑定动作的各个面:列表视图 `bulkActions`/`rowActions`、页面 `record:quick_actions` 的 `actionNames`、导航 action 项。解析不到 → **error**,与既有 `DASHBOARD_ACTION_TARGET_UNDEFINED` 同档;运行时没有任何内置动作名注册表,所以「解析不到」就是死引用,没有中间地带。\n\n## 顺带修掉的洞(都是画引用面时发现的)\n\n- `defineStack` 完全跳过 `app.areas[].navigation` —— areas 结构的 app **一条导航引用都没校验过**;并且只递归 `group` 的 children,漏掉 `object` 导航项的 children。\n- i18n 覆盖率只处理 `options` 的 record-map 形态,而规范形态是 `{value,label}[]` **数组** —— 规范形态的 select 字段选项翻译覆盖检查从未生效过。\n- hook `condition` 在 `object` 为数组时丢失全部字段感知;现改为逐目标校验并对目标无关的诊断去重。\n- widget 完全没有 `dataset` 时,在 raw-config 路径(`lint`/`doctor`)会静默旁路掉全部绑定与图表检查;`dataset` 是 schema 必填,现按已解析路径的同一标准报出。\n\n## 验证\n\n- 误报地板:对 `examples/app-crm`、`app-showcase`(全家桶)、`app-todo` 三个真实应用跑新规则,**零 finding**。这是 ADR-0072 D1 的信任契约 —— 一条死报警,作者就不再信任 linter。\n- 测试:lint 390 / spec 6669 / cli 639 全通过;三个包 build(含 DTS 类型检查)与 eslint 均通过。\n- 每条规则都带以 HotCRM 真实实例为原型的 fixture(`reference: 'user'`、`bulkActions: ['mass_update','mass_delete','assign_owner']`、`sys_approval_process` 导航)。\n\n## 未覆盖(方案 §5,需先决策,不在本 PR)\n\n- `agent.knowledge.indexes` 在 stack 里没有定义位(`KnowledgeSourceSchema` 未接入 `stack.zod.ts`)—— 给一个无定义位的命名空间写规则会把缺口制度化,应先做 spec 决策 D1。\n- hook body 写入是不透明 JS 源码,静态不可分析(D4)。\n- 页面字段引用、报表/列表 chart 轴、翻译孤儿键 —— 方案里的 Phase 2/3,各自独立 PR。\n\n关联:#3583 · #3640(方案)· ADR-0072 / ADR-0078 / ADR-0049\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01GBks5G7AkwrvgF2kL5rfkT)_"
}
