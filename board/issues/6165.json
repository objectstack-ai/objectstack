{
  "number": 6165,
  "kind": "pull_request",
  "title": "feat(spec,objectql): transaction 契约收紧第一批 —— `opts.require` fail-closed 与 `owned` 信号 (#5696)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "baozhoutao",
    "id": 6194462,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/l",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-08-07T04:34:44Z",
  "updated_at": "2026-08-07T07:56:27Z",
  "closed_at": "2026-08-07T07:56:24Z",
  "closed_by": {
    "login": "baozhoutao",
    "id": 6194462,
    "type": "User"
  },
  "merged_at": "2026-08-07T07:56:24Z",
  "comments": 4,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/6165",
  "body": "Fixes #5696\n\n本 PR 是 #5696「契约收紧半边」的第一批,也是 #5351 裁 A 引擎行为 PR 的前序(PR 序:契约先行)。落地 #5696 三点中的**第 1 点与第 3 点**,并把 transaction 契约 TSDoc 里被实测证伪的那句改写。\n\n**第 2 点(跨驱动业务写响亮拒绝)不在本 PR** —— 2026-08-06 维护者裁决把它与 #5351 的系统写 carve-out 绑成同批同 PR:单独先落拒绝会「响亮化但不修复」(审计 hook 的 try/catch 把拒绝吃成日志,合规行照丢)。它随 #5351 的引擎行为 PR 一起落。\n\n---\n\n## 契约 diff 逐条(给 spec 席确认单用)\n\n`packages/spec/src/contracts/objectql-engine.ts`:\n\n1. **新增导出 `EngineTransactionOptions`**(interface,1 个可选成员 `require?: boolean`)。语义:驱动无 `beginTransaction` 时,`require: true` 抛错而非静默降级;`undefined`/`false` 保持 ADR-0119 D1 已声明的降级行为,一字未改。刻意只放一个成员 —— 按本文件 header 的 evidence bar,消费者证明需要才扩。\n2. **新增导出 `EngineTransactionInfo`**(interface,1 个必选成员 `owned: boolean`)。`true` = 本次调用开启了事务并拥有提交/回滚;`false` = join 了外层 ambient 事务(ADR-0067 D2),**或**处在降级路径上(那里没有事务可拥有)。\n3. **成员签名变更** —— `transaction` 由\n   `transaction< T >(callback: (trxCtx: any) => Promise< T >, baseContext?: any): Promise< T >`\n   变为\n   `transaction< T >(callback: (trxCtx: any, info: EngineTransactionInfo) => Promise< T >, baseContext?: any, opts?: EngineTransactionOptions): Promise< T >`。\n   两处新增都是**追加**:既有单参数回调、两参数调用点全部不受影响。\n   ⚠️ 选型说明(留给 spec 席否决):`owned` 走**回调第二参数**,而不是挂在 `trxCtx` 上。理由是 `trxCtx` 会被调用方原样透传成 `{ context: trxCtx }` 一路进 `buildDriverOptions`,往里塞一个纯诊断字段等于把它送进执行上下文与 driver options;第二参数是纯附加、零污染。\n4. **TSDoc 事实性改写**。原文:「objects routed elsewhere by `setDatasourceMapping` are written outside it」。实测不成立 —— 引擎无条件把 ambient 事务句柄穿给目标驱动,语句在**错误的连接**上执行(#5351 实测真 SQL driver 报 `no such table`;PR #5724 用纯 in-memory 双数据源独立复现,收到的是 `{ __trx: 'primary' }`)。新文案按实测陈述,并声明随后落地的两条语义:业务写跨驱动**响亮拒绝**;`lifecycle.class` 为 `audit`/`telemetry`/`event` 的系统账本 **carve-out**(移出事务、在自己的连接上执行、承担孤儿行语义,#5351)。\n5. 段落头注释 `── Transactions (ADR-0119 D1) ──` → `── Transactions (ADR-0119 D1, revised by #5696/#5351) ──`。\n\n## 为什么本 PR 不是纯 spec 改动\n\n派发令原定 PR 1 只动 `packages/spec`。实测下来那样不可交付,两个原因,都指向仓规:\n\n1. **纯声明会让 `@objectstack/objectql` typecheck 转红。** 实测报错:\n   ```\n   src/engine.ts(6671,9): error TS2416: Property 'transaction' in type 'ObjectQL' is not assignable\n     to the same property in base type 'IObjectQLEngine'.\n       Target signature provides too few arguments. Expected 2 or more, but got 1.\n   ```\n   (`ObjectQL implements IObjectQLEngine` 是 #4251 B3 刻意留的验证步。)\n2. **声明了不兑现,正是 PD#10 明令禁止的形状。** 只声明 `opts.require`/`owned` 而引擎不实现,就是「advertise a capability the runtime doesn't deliver」。哪怕只存在到下一个 PR 合入,也不该进 `main`。\n\n所以本 PR 同时带上第 1、3 点的引擎实现(纯追加、默认行为不变),把耦合裁决真正约束的那一点(第 2 点拒绝)完整留给 #5351 的 PR。维护者裁决的硬约束由此**完全保持**。\n\n## 实现要点\n\n- `packages/objectql/src/transaction-errors.ts`(新)—— `TransactionUnsupportedError`,`code: 'ERR_TRANSACTION_UNSUPPORTED'`(按 `DriverConnectError` 的既有惯例用 `code` 而非 `instanceof` 跨包识别),已从包根导出。\n- `ObjectQL.transaction`:join 分支给 `{ owned: false }`;降级分支先判 `require` 再 warn;开启分支给 `{ owned: true }`。\n- `ScopedContext.transaction`(`ctx.api.transaction`,沙箱 hook/action 体)同步支持 `opts` 与 `owned` —— 同一原语的第二份实现不该变成第二种方言。\n- `@objectstack/core` 的 `EngineWithTransaction` 从手抄签名改为 `transaction: IObjectQLEngine['transaction']`。它自称「Mirrors `IObjectQLEngine['transaction']`」而实际已开始漂移;ADR-0119 D1 早就为窄宿主接口定了这个写法(窄可以,漂移不行)。\n\n## 测试\n\n新增 `packages/objectql/src/engine-transaction-contract.test.ts`(15 例):`require` 拒绝(抛型/`code`/`datasource`/消息含两半修法/回调未运行/一行未写/不再多打 warn)、`require` 在可事务驱动上无副作用、**不传 `require` 仍降级 + warn-once 的回归钉**、`require: false` 不等于 true、`owned` 三态(开启 true / join false / 降级 false)、单参数回调不受影响、第三参数在场时 `baseContext` 仍正常穿透、沙箱面三例。\n\n真实输出:\n\n```\n ✓ src/engine-transaction-contract.test.ts (15 tests)\n Test Files  1 passed (1)\n      Tests  15 passed (15)\n```\n\n受影响包全跑(合入 `origin/main` 36fc93832 之后):\n\n```\npackages/spec test:               Test Files  329 passed (329)   Tests  8399 passed (8399)\npackages/core test:               Test Files   27 passed (27)    Tests   498 passed (498)\npackages/metadata-protocol test:  Test Files   49 passed (49)    Tests   502 passed (502)\npackages/objectql test:           Test Files  133 passed (133)   Tests  2195 passed (2195)\npackages/runtime test:            Test Files  105 passed (105)   Tests  1506 passed (1506)\n```\n\ntypecheck:\n\n```\npackages/spec typecheck: Done\npackages/objectql typecheck: Done\npackages/runtime typecheck: Done\n```\n\n生成物(AGENTS §10 / `.gitattributes` os-regen 四步序:build → check:generated → --fix → commit),十道门全绿:\n\n```\n  ✓ check:spec-changes         ✓ check:upgrade-guide     ✓ check:skill-docs\n  ✓ check:skill-refs           ✓ check:react-blocks      ✓ check:authorable-surface\n  ✓ check:api-surface          ✓ check:docs              ✓ check:strictness-ledger\n  ✓ check:test-typecheck\n```\n\n`api-surface/contracts.json` 的 delta 恰为本 PR 意图的两条(`+ EngineTransactionInfo`、`+ EngineTransactionOptions`)。合入 main 时 os-regen 驱动把该文件记为 pending,已按规程重生成并由 `pre-commit` 复核清账。\n\n其它门:\n\n```\ncheck-nul-bytes: OK (scanned 5900 tracked text file(s); ... no raw ASCII control bytes)\ncheck-engine-double-contract: OK — 73 pinned, 133 in the DEBT ledger, 2 exempt.\n```\n\n## 反向验证(方向先写死,再运行)\n\n**肢 C —— 去掉 `require` 判定**(引擎面与沙箱面各删一处 `if (opts?.require === true) throw`)。\n\n- **预测**:4 条引擎面拒绝断言 + 1 条沙箱面拒绝断言转红,共 5 红;`owned` 三例、降级默认回归钉、`baseContext` 穿透等 10 例保持绿(它们不经过被删的分支)。\n- **实测**:完全一致 ——\n  ```\n  × throws TransactionUnsupportedError instead of running the callback\n     AssertionError: promise resolved \"undefined\" instead of rejecting\n  × carries the boundary-crossing code, the datasource, and the fix\n     AssertionError: expected undefined to be 'ERR_TRANSACTION_UNSUPPORTED'\n  × writes nothing — the refusal is not a rollback, it is a non-start\n     AssertionError: expected [ { object: 'thing', …(2) } ] to have a length of +0 but got 1\n  × is silent about the degrade it refused — the throw IS the report\n     AssertionError: expected [ { level: 'warn', …(2) } ] to have a length of +0 but got 1\n  × refuses under require: true, with the same error\n     AssertionError: promise resolved \"undefined\" instead of rejecting\n   Tests  5 failed | 10 passed (15)\n  ```\n- **匹配**:是(红,常规方向)。已还原,还原后 15/15 绿。\n\n其余两肢(去掉同源校验、关掉系统写 carve-out)作用于 #5351 的引擎行为 PR,在那边做。\n\n## 依据\n\nADR-0119 D1(transaction 契约与两条 caveat;旧文里的「ADR-0118 D1/D4」按该 ADR 自己的改号说明即本记录)、ADR-0119 D4(`batchData` fail-closed 先例)、ADR-0067 D2(join 语义)、ADR-0057 §3.6(lifecycle 分流)、#4619(母单)、#5724(可发现性半边)、#5351(2026-08-06 裁 A 与耦合硬约束)。\n\n⚠️ 本 PR **不含** ADR 修订文案 —— 孤儿行语义与 P1 边界决定属于 #5351 引擎行为 PR 的落点,一份修订一处写,避免两个 PR 各改同一段 ADR 撞车。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_019Q7oc7ASjh8yxyS3Yz78We)_"
}
