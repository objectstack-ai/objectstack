{
  "number": 3999,
  "kind": "pull_request",
  "title": "feat(datasource,runtime): teardown through the one datasource path, honoring adopted pools (#3993); ADR-0062 status flip (#3992)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/m",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-30T02:21:54Z",
  "updated_at": "2026-07-30T02:33:30Z",
  "closed_at": "2026-07-30T02:33:28Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-30T02:33:28Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/3999",
  "body": "Closes #3992。Closes #3993。同一主题(ADR-0062 收尾)合一个 PR:teardown 代码与它要写进 ADR 的语义互为表里,ADR 终局翻转顺带一次改完。\n\n## #3993 — teardown 归一,owned vs adopted\n\n**前提事实(逐码核实,已写进 D5 增补):** kernel 的 teardown 阶段是 **`destroy()`** —— `Plugin` 契约根本没有 `stop()`,`performShutdown` 只按注册逆序调 `destroy()`。所以 `ObjectQLPlugin.stop`、control-plane preset wrapper 的 stop 转发,都是从未被调用的死钩子;`default`(以及 declared/runtime datasource)的池在优雅关闭时无人断开,是「零份实现」而非分叉。\n\n对称补齐 connect 那半:\n\n- **`disconnect(name, { asDefault })`**:default 按**自然名**解析(#3826 的既定规则——`getDriverByName('default')` 永远找不到它;旧签名对 default 调用时会静默跳过 driver.disconnect,这不是行为回退而是从未生效)。\n- **`disconnectAll()`**:只关**本 service 打开的池**(`'connected'` 状态)。`already-registered` 属于别人(`onEnable` 桥、default 的幂等 replay)——绝不碰。\n- **`DatasourceDriverHandle.ownership: 'factory' | 'host'`**:connect 时记录进留存判决。`createPrebuiltDriverFactory` 盖 `'host'` 章:被收养实例的池比 kernel 长寿(cloud control driver 兼任所有环境 kernel 的 proxy 基座、每环境 driver 跨 kernel 注册表缓存)——**cloud 的 LRU evict(`kernel.shutdown()`)清判决但绝不关池**,否则会把共享池从其他消费者脚下抽走。cloud 仓**零改动**即得到正确行为(它已经在用 prebuilt factory)。\n- 挂点:`DefaultDatasourcePlugin.destroy()` + `DatasourceAdminServicePlugin.destroy()`(后者已有 destroy,扩入),均 best-effort——teardown 失败不得遮蔽关闭路径。\n- 顺带的真修复:file-backed `sqlite-wasm` default 的 `persist: 'on-disconnect'` 此前在优雅关闭时从不触发,现在真的会 flush。\n\n## #3992 — ADR-0062 终局翻转\n\n- Header:D1 **fully implemented across both repos**(#3968 + cloud#915),D5 teardown 半边随本 PR 落地;不再有 \"last remaining second site\"。\n- D1 的 \"Remaining second site\" 段改写为**完成记录 + 逃生舱分类清单**(telemetry sibling、cloud proxy、objectos 路由 shell、`'cloud'` 别名注册、测试注入)。\n- parity guard 的角色在 ADR 里与 test 注释对齐:收敛后保留,守「逃生舱不漂移」。\n- D5 新增 teardown 增补段(上述全部语义)。\n\n## 验证\n\n- service-datasource **136**(+3:adopted 盖章且 teardown 不关池+清判决、factory-built 经 `asDefault` 自然名解析被关、`disconnectAll` 只关自己开的/跳过 already-registered)\n- runtime **890**(65 files;+2:owned default 在 `kernel.shutdown()` 时被 disconnect、adopted 的 `disconnects === 0`)\n- verify 7;degraded-boot-parity 12 继续全绿;`pnpm turbo build` 两包及依赖图全绿;根配置 ESLint 对触及文件零告警\n\n## Review 重点\n\n- `disconnectAll` 的过滤判据(只 `'connected'`)是否有我没想到的第三态该关;\n- admin plugin 的既有 `destroy()`(原来只置空 service)扩入 `disconnectAll` 是否影响某个我没跑到的宿主拓扑。\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01GK5nd3gtehcAhrF6Zzp2on)_"
}
