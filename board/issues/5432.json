{
  "number": 5432,
  "kind": "issue",
  "title": "objectui: the two `selectFirst` gate hints join controlling-field names with hardcoded separators — and with two DIFFERENT ones",
  "state": "closed",
  "state_reason": "not_planned",
  "draft": null,
  "author": {
    "login": "yinlianghui",
    "id": 6219465,
    "type": "User"
  },
  "labels": [
    "pm:queue",
    "repo:objectui"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-08-05T11:11:46Z",
  "updated_at": "2026-08-10T02:08:19Z",
  "closed_at": "2026-08-10T02:08:19Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": null,
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/5432",
  "body": "Observation-class finding, noticed while fixing objectstack#5407. Nothing a user hits today with any urgency — it needs a field gated on two or more controlling fields, which is rare — so it carries `finding` and no `pm:queue`. Recording it because #5407 has just established the mechanism that fixes it.\n\n#5407 item 4 established that a list separator is a **locale property**, and added `validation.formInvalidJoiner` (per-pack: `\", \"` for Latin scripts and Korean, `\"、\"` for zh/ja, U+060C + space for Arabic) after measuring `Intl.ListFormat` and finding its output unusable for a truncated list.\n\nTwo other sites join user-visible names with a hardcoded separator, and they do not even agree with each other:\n\n| Site | Separator | Sentence |\n|---|---|---|\n| `packages/fields/src/widgets/LookupField.tsx` | `\", \"` | `lookup.selectFirst` |\n| `packages/components/src/renderers/form/form.tsx` (option-field gate) | `\" / \"` | `fields.options.selectFirst` |\n\nBoth fill the `{{fields}}` slot of what is deliberately one shared sentence — the form renderer's own comment says the two callers exist so \"the gate can never read differently depending on which side produced it\" — yet a field gated on two parents reads `Select Account, Owner first` from the lookup and `请先选择Account / Owner` from the select. Under zh/ja both separators are wrong for the script on top of that.\n\n### Suggested fix\n\nReuse the mechanism #5407 added rather than inventing a second one: either read `validation.formInvalidJoiner` at both sites, or (if the gate hint wants its own punctuation) add one `fields.gateJoiner` key across the ten packs. Either way it has to be **one** key read by both callers — two keys would recreate the divergence the shared sentence exists to prevent.\n\nNote for the implementer: `LookupField` receives the labels through the `dependsOnLabels` prop #5407 added, so the joining happens in `dependsOnFieldsText` there; the form's copy is in the `gatedHint` memo.\n"
}
