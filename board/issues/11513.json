{
  "number": 11513,
  "kind": "issue",
  "title": "Studio save of a package-declared permission set forks it into a silent, undiscoverable overlay",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "os-sam",
    "id": 318158314,
    "type": "User"
  },
  "labels": [
    "domain:services"
  ],
  "assignees": [
    "os-sam"
  ],
  "milestone": null,
  "type": "Feature",
  "created_at": "2026-08-24T01:52:51Z",
  "updated_at": "2026-08-24T20:22:34Z",
  "closed_at": "2026-08-24T14:01:37Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": null,
  "comments": 8,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/11513",
  "body": "Split out from #9952's maintainer ruling (2026-08-20), which named it explicitly:\n\n> ⚠️ Follow-up worth its own card, filed by whoever lands this if not absorbed: **the overlay-shadow mechanism itself** (a Studio save of a package-declared set silently forking it from the package, `customized` flag untouched) — arguably the real product defect the field report uncovered.\n\n#9952 built the *surfacing* half (a `drift_status`/`drift_detail` diagnostic + a \"Needs Attention\" Setup view + a sanctioned \"Discard Overlay\" action) — an independent detector that catches an overlay-shadowed set even when the row's own `customized` flag reads wrong. It does **not** fix the underlying mechanism this card is about:\n\n## The mechanism\n\nA `sys_metadata` overlay of a package-declared (artifact-backed) `permission` set can exist today — either a legacy row created while `permission` still had `allowOrgOverride: true` (pre-#6483/PR #6608, i.e. before ADR-0094 D5-R, 2026-08-09), or a fresh one created through the documented `OS_METADATA_WRITABLE=permission` operator hatch. Once it exists:\n\n- `reconcilePermissionSetProjection`'s env door (ADR-0094 D4 step 1) re-projects that overlay's content onto the record on **every boot, unconditionally**, keyed only on overlay presence — so the overlay wins forever regardless of what the package ships next, with no expiry.\n- `upsertEnvPermissionSet`'s `customized` flag is only trustworthy when the row's `managed_by` is *already* correct (`'package'`); on a row whose `managed_by` predates provenance tracking (the `provenance_skip` mechanism #9952 also documents), the flag is **forced `false`** even though an overlay genuinely shadows the row — measured on the field-reported environment: `customized` stayed `0` for two weeks while the overlay froze the set.\n- There is no diff, no \"the package has moved on since this overlay was taken\" signal, and (pre-#9952) no link from the generic \"1 unpublished change\" console badge to which set, which surface, or the fact that a shipped declaration is being shadowed.\n\n## Why it is a product defect, not just a missing diagnostic\n\n#9952's `drift_status` field tells an admin *that* a set has drifted and *why*, and gives them a button to fix it — but nothing stops the fork from happening again the same way, and nothing makes `customized` itself correct independent of `managed_by`. An admin who has never opened the new \"Needs Attention\" view has no reason to know a Studio save on a package-declared set (via the operator hatch) silently committed them to a permanently-shadowed set.\n\n## Candidates worth considering (not scoped/decided — for whoever picks this up)\n\n- Make `customized`'s computation independent of `managed_by` correctness (compute it the same way #9952's `drift_status` overlay-shadow branch does — an active overlay for the name, checked directly against `sys_metadata` — rather than gating on `existing.managed_by === 'package'`).\n- An expiry/diff signal: when the shipped artifact has changed since the overlay was captured, say so (not just \"an overlay exists\").\n- Reconsider whether the `OS_METADATA_WRITABLE=permission` hatch should be able to mint a *new* overlay on an artifact-backed set at all, now that ADR-0094 D5-R's stated intent is \"no new overlay of a packaged set can be minted.\"\n\nRelated: #9952 (parent field report — surfacing + discard action), #8672 (open — org-less materialization, same file).\n"
}
