{
  "number": 6736,
  "kind": "issue",
  "title": "Authored RLS update-wideners are silently ineffective on the bulk write path — buildWriteFilter ANDs them away (fewer rows, no error)",
  "state": "open",
  "state_reason": null,
  "draft": null,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "domain:services",
    "pm:on-hold"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-08-08T13:49:21Z",
  "updated_at": "2026-08-19T07:27:10Z",
  "closed_at": null,
  "closed_by": null,
  "merged_at": null,
  "comments": 3,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/6736",
  "body": "Restart-when: a consumer is measured exercising update({multi}) / delete({multi}) against app-authored RLS wideners, or any PR touches the bulk-write composeAnd branch in packages/plugins/plugin-sharing/src/sharing-plugin.ts — bulk composition semantics must be specified in a new ruling before implementation\n\nSplit out of #5493 by the maintainer's 2026-08-08 ruling (Q2 = A1: the by-id half ships a verdict-shaped surface; the bulk half is **deliberately priced separately** — filed here so it cannot sink inside a ruled thread, the #6216-documented failure mode of decision text dying with its issue).\n\n## Mechanism (code-read by the #5493 round-2 dev; not yet probed end-to-end)\n\nThe bulk write path (`update({multi})` / `delete({multi})`) ANDs `SharingService.buildWriteFilter` into the query AST. An app-authored RLS update-widener therefore cannot widen the row set there either — the same defect family as #5493, but **silent**: the statement touches fewer rows than the declaration admits, with no error, no 403, nothing in any envelope. The by-id half at least refuses loudly (`FORBIDDEN`), which is how HotCRM's sweep caught it; this half is invisible to a status-code sweep by construction.\n\n## Relationship to the ruled fix\n\n- #5493's ruled surface (`checkAuthoredRowWrite`, verdict-shaped, by-id — see #6735) **cannot** reach this path: a per-row verdict has no seat in AST composition.\n- The alternative that would have covered both (a filter-shaped authored-only write `FilterCondition`, OR-composed into the bulk write filter, pre-image-matched for by-id) was considered and **deferred** by the ruling: no measured pull (HotCRM's probes are single-record PATCH), and shipping a second composition site ahead of demand is what the startup-focus principle defers. If bulk pull materializes, that decision must specify the bulk composition semantics **explicitly in the ruling** — not leave them to whoever implements.\n\n## What acceptance would need (when priced in)\n\nAn end-to-end probe first (the mechanism above is a code read): member with an authored update-widener runs `update({multi})` spanning admitted-by-declaration rows they don't own → count today vs count declared. Then the design question above.\n\nFiled by the identity seat (PM loop round 2). Routing labels left to the triage seat.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
