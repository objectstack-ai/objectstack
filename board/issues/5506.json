{
  "number": 5506,
  "kind": "issue",
  "title": "objectui: five more hardcoded English chrome strings found in #5430's sweep radius (tab-count `N items`, `Resize drawer`, `Open as full page`, `Record Detail`, CommentThread's reaction tooltip)",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "yinlianghui",
    "id": 6219465,
    "type": "User"
  },
  "labels": [
    "bug",
    "pm:dispatched",
    "pm:queue",
    "repo:objectui"
  ],
  "assignees": [
    "yinlianghui"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-08-05T14:31:58Z",
  "updated_at": "2026-08-06T02:47:19Z",
  "closed_at": "2026-08-06T02:46:45Z",
  "closed_by": {
    "login": "yinlianghui",
    "id": 6219465,
    "type": "User"
  },
  "merged_at": null,
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/5506",
  "body": "Swept while implementing objectstack#5430. Same class as that issue — accessible names and visible defaults still hardcoded in English — but outside its four-string scope, so filed rather than fixed there.\n\n| String | Site | Kind |\n|---|---|---|\n| `{count} items` | `packages/components/src/renderers/layout/containers.tsx:573` — the `page:tabs` count badge's `aria-label`, built by template literal | icon/number-only, so the label IS the badge to a screen reader; also English-pluralized by construction |\n| `Resize drawer` | `packages/components/src/custom/navigation-overlay.tsx:357` — the drag handle's `aria-label` on `role=\"separator\"` | handle has no visible label |\n| `Open as full page` | `packages/components/src/custom/navigation-overlay.tsx:279` — `expandLabel` prop **default** | feeds both `aria-label` and `title` of an icon-only button; overridable by callers, but the default is what ships |\n| `Record Detail` | `packages/components/src/custom/navigation-overlay.tsx:282` — `resolvedTitle = title \\|\\| 'Record Detail'` | **visible** overlay heading, not just an a11y name |\n| `1 reaction` / `{n} reactions` | `packages/collaboration/src/CommentThread.tsx:520` — reaction tooltip, `userIds.length === 1 ? '1 reaction' : ...` | English pluralization built in, exactly the shape #5430 replaced in `ReactionPicker` |\n\nAlso in the same file, three copies of the prose `Record detail overlay for {resolvedTitle}.` (`navigation-overlay.tsx:377`, `:442`, `:526`) feed `SheetDescription`/`DialogDescription`.\n\n## Notes for whoever picks this up\n\n- The `CommentThread` one **must** use the repo's two-key plural convention (`detail.reactionCount` / `detail.reactionCountOne`, added to all ten packs by #5430), not an i18next `_one`/`_other` pair — `all-locales-key-parity` rejects the suffix scheme because zh/ja/ko have no separate singular form. The two keys already exist and interpolate `{{emoji}}` and `{{count}}`; `CommentThread`'s tooltip has no emoji, so it likely needs its own pair rather than reuse.\n- `{count} items` has the same pluralization problem and needs the same two-key treatment.\n- `packages/collaboration` is a different package from #5430's three; check its i18n wiring before assuming `useSafeTranslate` is available.\n- Watch the consumption radius: `e2e/live/inline-edit-polish-2572.spec.ts:61` addresses the header toolbar by its English accessible name, and `packages/plugin-view/src/__tests__/ObjectView.test.tsx` addresses the overlay close by `getByLabelText('Close panel')`. Both still pass after #5430 (no provider means the English fallback resolves), but a locale-parameterized run would need updating.\n\nThe `Record Detail` default is the only **visible** string here; the rest are accessible names and hover tooltips.\n\n_Generated by [Claude Code](https://claude.ai/code)_\n"
}
