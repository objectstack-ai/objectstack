{
  "number": 9416,
  "kind": "issue",
  "title": "automation resume: a known top-level key with a type-mismatched value is silently dropped — the submission is still treated as empty",
  "state": "closed",
  "state_reason": "completed",
  "draft": null,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "domain:cli",
    "pm:dispatched"
  ],
  "assignees": [
    "os-zhuang"
  ],
  "milestone": null,
  "type": "Bug",
  "created_at": "2026-08-17T23:58:51Z",
  "updated_at": "2026-08-18T15:58:31Z",
  "closed_at": "2026-08-18T15:58:31Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": null,
  "comments": 3,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/9416",
  "body": "Found while implementing #8796 (the resume body's outer envelope became a closed key set). Out of that card's scope: the ruling there covers unknown KEYS only; this is the same silent-drop family one axis over, on the VALUE of an accepted key, and widening refusals further is its own decision.\n\n## Measured on the resume branch, `packages/runtime/src/domains/automation.ts`\n\nThe signal assembly (deliberately field-by-field, per #3801) type-guards each accepted key and silently skips a value that fails the guard:\n\n```ts\nconst inputs = (b.inputs ?? b.variables);\nif (inputs && typeof inputs === 'object') signal.variables = inputs;\nif (b.output && typeof b.output === 'object') signal.output = b.output;\nif (typeof b.branchLabel === 'string') signal.branchLabel = b.branchLabel;\n```\n\nSo after #8796's closed set lands, `POST .../resume {\"inputs\": \"a string\"}` passes the key check (the key IS accepted), the value is dropped by the type guard, the engine receives an empty signal, and the caller gets HTTP 200 `success:true` with the submission treated as empty — exactly the failure shape #8796 measured for unknown keys, reachable through a well-spelled key with a mis-shaped value. Same for `{\"output\": 42}` and `{\"branchLabel\": 7}`. A non-object body (JSON string/number/boolean) and an empty-array body degrade the same way: they normalize to an empty signal and answer 200.\n\n## The decision this needs\n\n- **A — refuse a type-mismatched value on an accepted key** (400, located, naming the key and the expected type), matching the toggle arm's treatment of `enabled` (a truthy non-boolean is refused there, not coerced or dropped).\n- **B — forward the raw value and let the engine judge**, keeping the transport thin — but the engine contract types `variables`/`output` as objects, so this hands a service a shape its contract excludes (the same class #7300 closed for `?limit=abc` on `listRuns`).\n\nOption A is the shape the sibling arms already chose; filed rather than fixed because it widens refusals on an accepted public surface beyond the #8796 ruling's text.\n\n## Refs\n\n- #8796 — the outer envelope's closed KEY set (this is the value axis of the same defect family)\n- #4477 — the inner bag's declared=enforced treatment\n- #3899 — toggle arm precedent: a truthy non-boolean `enabled` is refused, not dropped\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_012WKSnqAaoqtW3QX7SSf1Vk)_"
}
