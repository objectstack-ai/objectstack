{
  "number": 4294,
  "kind": "pull_request",
  "title": "refactor(spec,plugin-security)!: QueryAST 不再声明没有执行器运行的成员 — joins/windowFunctions 墓碑化,liveness ledger 接管请求面 (#4286)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/xl",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-31T03:56:12Z",
  "updated_at": "2026-07-31T04:22:35Z",
  "closed_at": "2026-07-31T04:22:33Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-31T04:22:33Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/4294",
  "body": "落实 #4286 的第 **1、2、5** 步(机械层 + 闸门)。第 3 步(`having`)与第 4 步(`cursor`/`distinct`)按 issue 自己的定性**留给维护者裁决**,本 PR 只在新 ledger 里把三者记为 `dead` 并写明证据与待决事项——所以**不**关闭 #4286。\n\n## 处置一:`joins` / `windowFunctions` 移除(墓碑化)\n\n- 两个键都换成 `retiredKey()` 墓碑(`BaseQuerySchema` 非 strict,裸删除会被 zod 静默剥离——ADR-0104 那一类):作者侧 `tsc` 直接报错,运行时 parse 给出带 FROM→TO 的处方,连 `joins: []` 也拒绝。\n- **孤儿 schema 集群随键删除**(#3950 先例):`JoinNode`/`JoinNodeInput`/`JoinNodeSchema`/`JoinType`/`JoinStrategy` 与 `WindowFunction`/`WindowSpec`/`WindowFunctionNode`(api-surface 少 10 个导出;`data/JoinNode` 等 6 个 JSON schema 出清单)。\n- window 集群多查出一层:唯一活门 `SqlDriver.findWithWindowFunctions()` 吃的是**驱动自己的扁平形状**(`{ function, alias, partitionBy?, orderBy? }`,见 driver 测试与 `buildWindowFunction`)——spec 节点声明的 `field`/`over`/`frame` 那个门**从未读过**。保留 spec 词汇表只会一层之隔地重演「声明 ≠ 执行」,所以随键删除;文档里驱动门示例已改为真实形状。\n- ADR-0087:注册为 protocol-18 **semantic migration**(`query-joins-retired` / `query-window-functions-retired`)——`QueryAST` 是请求面、从不落盘,链无源可改写,循 #4196(`query-field-node-object-form-retired`)先例;`gen:schema` 闸门 (b) 据此放行。\n- **REST 兼容注记**(issue 发现 4):墓碑期内两个名字**仍占据** `RESERVED_LIST_QUERY_PARAMS`(`retiredKey` 保留 `keyof QueryAST` 成员),行为零变化;去保留发生在墓碑到期删除时,已写进 `QUERY_AST_KEYS` 注释提醒后人。\n- 安全侧联动(issue 发现 3):predicate-guard 删掉 `windowFunctions` 遍历(条款已不存在,无可泄漏);`having` 与 `aggregations[].filter` 的遍历**保留**——它们仍在声明面上,guard 就绪正是日后 enforce 的安全前提。\n\n## 处置二:六个 search 旗标 + `AggregationNode.filter` 标记 experimental\n\n`search.{fuzzy,operator,boost,minScore,language,highlight}`(ADR-0061 展开只读 `query`+`fields`)与 `aggregations[].filter`(SQL/内存两路都不应用)加 `[EXPERIMENTAL — not enforced]` describe 标记。无 wire/兼容影响;ledger 直接从标记解析状态。\n\n## 处置三(第 5 步,核心):liveness ledger 接管请求面\n\n- `QuerySchema` 经 `SPEC_ONLY_SCHEMAS` 进入 governed 类型 `query`(`webhook` 先例,但这是**常驻**覆盖——请求面没有 registry 可回折)。issue 点名的缺口正在此:ledger 只管作者写进元数据的东西,没人管调用者写进 query 的东西。\n- 新 `liveness/query.json`:27 个成员全分类——**15 live**(逐条 file:line 证据)/ **7 experimental**(describe 标记)/ **5 dead**。`cursor`(Tier A,`QueryBuilder.cursor()` 在产、无驱动实现)、`distinct`(**mis-wired 而非单纯 dead**:唯一效果是 protocol.ts:3816 的 count 抑制,ADR-0078 从严)、`having`(`engine.aggregate()` 重建 AST 时根本不带它——最强的 enforce 候选)各自带 `verifiedAt` 与待决注记。\n- ledger README 更新为 17 个 governed 类型 + query 行。\n\n## 验证\n\n- spec **7119** / objectql **1338**(含 protocol-data 结构键用例)/ plugin-security **677** / metadata-protocol **122** / driver-sql advanced **35** 全绿;`tsc --noEmit` 干净。\n- `packages/spec` 14 个闸门全 PASS(`check:liveness` 报 `query 27 classified (live 15, experimental 7, dead 5)`);`check:generated` 全部最新——api-surface/docs 按其裁定窄向重生成,strictness ledger 行随四个 `z.object` 站点的删除更新(9→5)。\n- 墓碑 pin 测试:处方正文、`joins: []` 亦拒、`undefined` 放行、替代写法(`expand`/点路径)可解析。\n- 文档同步:`query-syntax.mdx` / `data-modeling/queries.mdx` 的 joins/window 小节改为移除记录,`objectstack-query` skill(SKILL.md + rules/aggregation.md + evals)不再教「schema-reserved」;三处导航卡片文案不再宣传 joins(Prime Directive #10 的「declared ≠ enforced 不得广告」)。\n- changeset:`@objectstack/spec` major(FROM→TO 映射与一行修复齐备)+ `@objectstack/plugin-security` patch。升级指南/spec-changes 待 PROTOCOL_MAJOR 翻到 18 时由生成器自动收录(与 #4196 同轨)。\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_012K2BX3WQwPBA2ZBehqJUBX\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_012K2BX3WQwPBA2ZBehqJUBX)_"
}
