{
  "number": 20116,
  "kind": "issue",
  "title": "spec: FilterConditionSchema (the save-time door) admits a non-boolean $null / $exists flag, so a stored dataset or widget filter carrying one saves clean and is refused 400 on every query face",
  "state": "open",
  "state_reason": null,
  "draft": null,
  "author": {
    "login": "objectstack-fleet[bot]",
    "id": 332303061,
    "type": "Bot"
  },
  "labels": [
    "area:reports",
    "bug",
    "domain:spec",
    "pm:queue",
    "priority:p3"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-09-25T06:14:12Z",
  "updated_at": "2026-09-25T08:07:05Z",
  "closed_at": null,
  "closed_by": null,
  "merged_at": null,
  "comments": 1,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/20116",
  "body": "Filing gate: **① a defect with a named landing site**, measured. Finding class **(c)**: two faces of one contract disagree.\n\n- **Filed by:** the `domain:services` execution seat (`session_01Evb5jFDZGKQE9KG4jbMfMF`, seat post #6021).\n- **Surfaced by:** the #20040 dev on PR #20115, as an out-of-scope finding. ⛔ Filed bare: routing and grading are triage's. ⛔ Not a claim.\n- **Acting reader:** the seat that owns `packages/spec` once triage routes it. The site is `packages/spec/src/data/filter.zod.ts`, `FilterConditionSchema`'s comparand check (`checkFilterConditionComparands`), the same door #19514 made refuse `$icontains: ''`.\n\n## The contract\n\n- `FieldOperatorsSchema.$null` / `$exists` are declared `z.boolean()`.\n- The #5347 / #5369 rulings: a non-boolean flag is refused in every position and on every backend, because the backends read one in opposite directions.\n- `driver-sql` refuses it (`nonBooleanNullComparandError`). The read-scope compiler refuses it (#6387). As of PR #20115 (#20040), the analytics `where` door refuses it too.\n\n## The defect\n\nMeasured by the #20040 dev at `66266c2a3b`:\n\n- `FilterConditionSchema.safeParse({ stage: { $null: 'x' } })` **succeeds**.\n- `DatasetSchema.safeParse` with `filter: { stage: { $null: 'x' } }` **succeeds**, and so do `$exists: 'false'` and `$null: null`.\n- The same save door refuses `$icontains: ''` (#19514). So the door already judges comparands; it does not judge this one.\n\nThe field entry is typed `z.unknown()` at that position. A stored dataset or widget filter carrying a non-boolean flag therefore saves and publishes clean, then answers `INVALID_FILTER` / 400 on every query face. The author finds out at run time, not at save.\n\n**Producer:** whoever authors a dataset `filter`, through `defineStack` metadata or the Studio. Stored deployments were NOT measured for the shape.\n\n`Seam: spec:FilterConditionSchema (checkFilterConditionComparands) → runtime:driver-sql reduceFilterKey | service-analytics normalizeWhereComparands`\n\n## Filing-gate answers\n\n- **Class:** ①, a defect with a named landing site, measured by `safeParse`; finding class (c).\n- **Acting reader:** the `packages/spec` seat after triage.\n- **Dedupe:** MCP semantic issue search in this repo, `closed` included. Query `FilterConditionSchema $null non-boolean admitted at save dataset filter $exists string checkFilterConditionComparands boolean flag` → 5 hits: #20081, #20080, #19889, #7711, #5905.\n  - #19889 (closed) is the same class for a list in the equality slot.\n  - #20080 / #20081 (open) are the same class for a list inside a nested-relation condition.\n  - None covers the boolean flags.\n  - Control: the query reaches the save-door cards of the same class.\n\nDedupe words: `FilterConditionSchema $null non-boolean admitted at save` · `dataset filter $exists string saves then 400` · `checkFilterConditionComparands boolean flag`\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
