{
  "number": 3535,
  "kind": "pull_request",
  "title": "feat(storage): 遗留文件值回填 — ADR-0104 D3 wave 2 (PR-6)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/l",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-27T02:59:34Z",
  "updated_at": "2026-07-27T05:13:45Z",
  "closed_at": "2026-07-27T03:24:00Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-27T03:24:00Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/3535",
  "body": "承接 #3527（PR-3 独占所有权）。把 cutover 之前存在字段里的遗留形态,转换成引用形态。\n\n## 转换什么、不转换什么\n\n`file`/`image`/`avatar`/`video`/`audio` 字段在 cutover 前可能持有 inline 元数据 blob（`{url, name, size, …}`）或裸 URL 字符串。`backfillFileReferences()` 把它们转成引用形态——一个由该记录字段拥有的不透明 `sys_file` id。\n\n| 遇到的值 | 处理 | 是否搬字节 |\n|---|---|---|\n| 指向**本平台 resolver** 的 URL（`…/storage/files/:id`） | 它本来就已经指明了一个 `sys_file`,字段改写为裸 id | ❌ 不搬 |\n| **`data:` URI** | 字节就在值里,上传 → 建 `sys_file` → 字段改写为新 id | ✅ |\n| **外部 URL**（`https://cdn.example.com/…`） | **只报告,绝不转换** | ❌ |\n\n**为什么外部 URL 不转换**:那意味着去抓取第三方内容并悄悄地重新托管它——这是带宽、许可和隐私上的决定,**不是一次迁移该替用户做的**。ADR-0104 R7 的方向是把它们迁到显式的 `url` 字段,而在 AI 写元数据的语境下这正是重点:**它让\"受管文件\"和\"外部链接\"不再是同一个声明**,AI 就没法把两者搞混。\n\n## 安全性质\n\n- **默认 dry run**:不加 `apply` 什么都不写,而且 dry-run 报告和实际执行的报告**同构**,所以计划可以先审、事后可以对差异。\n- **幂等**:已经是引用形态的值记为 `already_id` 并原样保留,所以跑到一半中断可以安全重跑。\n- **绝不自己写 `ref_*` 列**:它只改写记录,由 PR-3 的 claim hook 观察到这次写入并记录所有权。**只有一条认领路径**,也就不存在两条路径互相矛盾的可能——有一条测试专门盯着这点,一旦回填开始直接碰 `sys_file` 就会失败。\n\n## 测试（11 项）\n\n本地 resolver URL 改写（裸串 + 包在 inline blob 里两种）、`data:` URI 上传并正确解出 mime/size/name、外部 URL 既不改也不上传、`multiple:true` 逐元素处理（三种值混在一个数组里）、dry run 零写入、幂等（第二次 converted=0）、无存储服务时 `data:` 值保持原样、无 file 字段的对象跳过、分页 500 条 + 截断标记,以及上面那条\"绝不写 ref_*\"的断言。\n\n全量:service-storage **136** ✅ · nul-bytes ✅ · lint ✅\n\n## 位置\n\n按 ADR 2026-07-27 addendum 的重新排序,回填**必须先于开启回收**:一本账在被回填并对账证明与现实一致之前,不该被授予对不可逆删除的权力。跑完这个之后应当运行 `verifyFileReferences()`（#3534）确认两边一致。\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01SHpGw3GBA9aFpfwVArRWfd\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01SHpGw3GBA9aFpfwVArRWfd)_"
}
