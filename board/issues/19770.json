{
  "number": 19770,
  "kind": "pull_request",
  "title": "docs(pm): rest-channel names the fleet-write relay as the channel to the App identity",
  "state": "open",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "objectstack-fleet[bot]",
    "id": 332303061,
    "type": "Bot"
  },
  "labels": [
    "documentation",
    "size/s",
    "skip-changeset"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-09-23T01:39:22Z",
  "updated_at": "2026-09-23T02:03:02Z",
  "closed_at": null,
  "closed_by": null,
  "merged_at": null,
  "comments": 1,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/19770",
  "body": "Part of #19739\n\nClause-②: no\n\n## What changed\n\nOne file, `.claude/skills/pm-dispatch/references/rest-channel.md`, section 〈通道边界〉: one paragraph of nine rule lines stating how the App identity is reached.\n\n- `objectstack-fleet[bot]` is reached through the `fleet-write` relay; the four write tools (`post-stamped`, `label-write`, `issue-create`, `close-cards`) and `with-fleet.sh --via auto` share one selector.\n- `dispatch` only when three conditions hold — `CCR_AGENT_PROXY_ENABLED=1`, a session id, a live relay — otherwise `direct`, with one printed line.\n- The session id is derived from the container's `CLAUDE_CODE_REMOTE_SESSION_ID`; `OS_FLEET_SESSION` overrides it for local checkouts and tests only.\n- ⛔ Never prefix a command with it: the allow rules are literal prefixes, a prefixed write drops onto the classifier and the seat stalls.\n- Live = the workflow file on `main` AND its Actions state `active`; the maintainer disabling it in the Actions UI turns the whole fleet back to `direct`.\n- The dispatch goes to objectstack with the session token; the run executes on the runner with a per-run App token narrowed to the target repo.\n- The closed op list lives in `scripts/pm/fleet-write/ops.mjs`; reads stay direct.\n- A `post-stamped` body over 60,000 bytes takes `direct` for that one write, with one printed line, as the seat's own user.\n- A failed run (`post-stamped`/`close-cards` 4, `label-write`/`issue-create` 5) and UNCONFIRMED (6) are read, ⛔ never retried.\n\nSpellings are taken from the #19755 branch `claude/issue-19755-fleet-write-derive-session` at 6470e4e (PR #19760): `sessionSource` in `scripts/pm/fleet-write/dispatch.mjs`, `CONTAINER_SESSION_ENV` in `ops.mjs`, `relayLive`'s Actions-state read, the `with-fleet.sh --via auto` paragraph, and `sizeRoute` / `MAX_BODY_BYTES` in `post-stamped.mjs`. Everything else is read from `main` at 544767d (`.github/workflows/fleet-write.yml`: `repositories:` = the validated target, `permission-issues: write`, `permission-pull-requests: write`, `permission-metadata: read`).\n\n**One correction to the card's item 3 wording.** The failed-run exit is not `4` fleet-wide. Each tool books it in its own register on that branch: `post-stamped` 4 (`EXIT_NOT_STORED`, via `relayExitFor`), `close-cards` 4 (`EXIT_HALF_WRITE`, and the same 4 for an unconfirmed close), `label-write` and `issue-create` 5 (`exitForResult` → `EXIT_PLATFORM_REFUSAL`). UNCONFIRMED is 6 in every tool that takes the relay. The paragraph states the registers as they are on that branch; the rule — read, never retry — is the same in all four.\n\n## Line budget — ceiling 82, headroom 0, paid in place (+9 / −9)\n\nNine lines are retired. Each is a restatement; the line that keeps the rule is named beside it (line numbers on `main` at 544767d).\n\n| retired (`rest-channel.md` line) | the rule survives at |\n|---|---|\n| 9 — closed session gate: repo-scoped 403, no `gh`, MCP and git fine | `platform-readings.md` 108 (the 403 shape), 114–115 (git first, MCP third), 142 (no `gh` in the container) |\n| 10 — run one repo-scoped probe before choosing a channel | `platform-readings.md` 107, 127 |\n| 13 — a rate-limit refusal binds to the refused identity; switching channels is a retry | `platform-readings.md` 122, 125 |\n| 14 — the other side is a way out only under a different identity (`GET /user` vs the refused id) | `platform-readings.md` 125 (the `GET /user` test), 96 (one identity inside a seat) |\n| 38 — label/assignee writes go through `label-write.mjs` (four steps, read-back) | `SKILL.md` 394; `os-dev.md` 304; the tool's own header |\n| 44 — every write carries `Content-Type: application/json`; the 415 and its discriminant | `platform-readings.md` 119–120 |\n| 54 — never MCP `update_pull_request`; ready/draft and auto-merge take the ccr routes | `.claude/settings.json` deny list; `SKILL.md` 610; the ccr rows at 48 and 52 of this file |\n| 60 — a REST-gated seat falls back to one targeted MCP `search_issues` | `platform-readings.md` 115, 211 |\n| 72 — whether auto-merge goes through the queue is answered by trying the action, not a field | `platform-readings.md` 9; rows 73–75 of this file keep the three criteria |\n\nNo line was re-wrapped; every line is ≤ 120 bytes (`check:pm-skill-ratchet` green on the head).\n\n## Landing order\n\n⛔ This PR lands only after #19755 (PR #19760) merges: the paragraph states what that PR makes true (the container-derived session, the Actions-state half of the liveness read, the size route). Not enqueued by this dispatch. Tier S (`.claude/**`), `CONTRACT_REVIEW_TIER` — the director seat renders the `## Contract review` of record. `skip-changeset`: nothing published from any released package; this dispatch forbids label writes, so the label is the seat's to apply.\n\n## Verification\n\nGates derived by `node scripts/pm/dispatch-gates.mjs --repo objectstack-ai/objectstack --commands` on the edited tree at 9f49c33; each run alone, exit captured before any pipe (`cmd > log 2>&1; ec=$?`).\n\n| command | exit | verdict line |\n|---|---|---|\n| `node scripts/check-closing-keyword-parity.mjs` | 0 | • packages/spec/CHANGELOG.md -- 6080503 bytes exceeds the sweep's 2097152-byte cutoff for UNREGISTERED files |\n| `node scripts/check-closing-keyword-parity.mjs --self-test` | 0 | ✓ check-closing-keyword-parity --self-test: 40 assertions, 5 mutations of the shipped parsers each driven to r |\n| `node scripts/check-comment-mask-corpus.mjs` | 0 | ✓ comment-mask corpus sweep [scripts/js-comment-mask.mjs]: 7011 files, 0 disagree, 0 unparseable, 92.9s (compa |\n| `node scripts/pm/check-harness-current.mjs --self-test` | 0 | check-harness-current --self-test: all 26 cases passed. |\n| `pnpm --filter @objectstack/lint run check:doc-formula-expressions` | 0 | #11673). |\n| `pnpm check:agent-test-spelling` | 0 | which drives this same sweep RED over a temp tree on disk. |\n| `pnpm check:cross-package-test-inputs` | 0 | OK: 29 package(s) read outside themselves, all declared, and turbo.json hashes every declared glob (6 of them  |\n| `pnpm check:doc-authoring` | 0 | ✓ doc authoring guard: sibling-package prose ids hold the baseline — 819 pinned site(s) across 231 file(s), 90 |\n| `pnpm check:driver-memory-census` | 0 | check-driver-memory-census: OK — every declaration is ledgered, every ledger entry is live, and every ruled fi |\n| `pnpm check:gitlink-declared` | 0 | check-gitlink-declared: OK (9246 index entries -- 0 gitlink(s) at mode 160000; no .gitmodules in the index, so |\n| `pnpm check:nul-bytes` | 0 | check-nul-bytes: OK (scanned 9239 text file(s) -- 9239 tracked, 0 untracked-not-ignored; skipped 7 binary; no  |\n| `pnpm check:pm-governed-merges` | 0 | live: the real generator declared 9 output(s) and certified this tree |\n| `pnpm check:pm-skill-id-lint` | 0 | ✓ check-skill-id-lint: 30 file(s) clean (pattern /#[0-9]{3,}/g). |\n| `pnpm check:pm-skill-ratchet` | 0 | ✓ check-skill-line-ratchet: declared cross-file moves: 1, total ceilings down 9 lines. |\n| `pnpm check:refd-timer-probe` | 0 | 1 code site(s), all inside the approved module, which is present and still reads it. |\n| `pnpm check:skill-frame-sync` | 0 | binding sentence present in it; 1 count mention(s) agree; 77 markdown files scanned for undeclared copies. |\n| `pnpm check:watch-hint-literal` | 0 | ✓ check-watch-hint-literal: 71 declaration(s) across 4 rostered name(s) -- ROOT_DIR_WATCH_HINTS 47, ROOT_FILE_ |\n\nAlso run on this tree, named by the dispatch and not in the derived list: `pnpm check:skill-frame-freshness` exit 0 · `pnpm check:pm-governed-prose` exit 0. `--ran` reconciliation: ✓ dispatch-gates --ran: 17 derived famil(ies) accounted for — 17 run, 0 NOT-MEASURED (a DERIVED zero — all 17 recorded an exit code and none of them is 3).\n\n## Acceptance notes\n\n- `scripts/pm/with-fleet.sh --read -- true` in this cloud container (a probe of the direct path before opening the PRs): the mint `POST /app/installations/{id}/access_tokens` is refused by the egress proxy (HTTP 403 \"not permitted through this proxy\"), `classifyHttp` read that 403 as `ratelimit`, and `write-pace` wrote a 30-minute STOP MARKER for the App token's key (`f665bb8f6447`). A proxy path refusal is not a back-off signal; the marker blocks nothing this session writes (the session token's key `f07d7417ce88` carries no marker), but it would block a seat that can mint. Reported on the card for the seat to file; not fixed here (`scripts/pm/**`, outside this diff).\n- `#19755 remains open`; its PR #19760 is the head this text was read from. If that branch changes the reason text, exit registers or the cap before it merges, this paragraph is re-read against the merged text before the contract review.\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01XPUwWGnmky4PXPofNsf2w7)_\n\n"
}
