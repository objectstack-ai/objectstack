{
  "number": 4099,
  "kind": "pull_request",
  "title": "feat(spec)!: RLS 策略 / 共享规则 / 岗位 schema 拒绝未知键（#4001 第 2 步）",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/m",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-30T09:38:10Z",
  "updated_at": "2026-07-30T10:01:50Z",
  "closed_at": "2026-07-30T10:01:48Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-30T10:01:48Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/4099",
  "body": "#4001 棘轮的第二格（第一格：flow + permission，#4071），覆盖账本 next-target 清单里剩余的小型安全类可授权面，仍在 v17 RC 窗口内。\n\n## 做了什么\n\n- **`security/rls.zod.ts`** — `RowLevelSecurityPolicySchema` `.strict()`。RLS 策略上被静默剥离的键 = 作者写的行级限制从未被编译进过滤器。别名：`roles`/`role`→`positions`（ADR-0090 D3 改名）、`withCheck`→`check`（PostgreSQL 拼法）、`condition`/`filter`/`where`→`using`。运行时形状（`RLSUserContextSchema` / `RLSEvaluationResultSchema`）保持宽松；退役的 `priority` 沿用既有 `retiredKey` 墓碑（#3896）。\n- **`security/sharing.zod.ts`** — 规则基座 + criteria 扩展 + `sharedWith` 接收方全部 `.strict()`（实测确认 zod v4 的 `.extend()` 会携带 catchall 和 error map，所以 strict 挂在基座上即可覆盖扩展）。别名里最有价值的一条：`criteria`→`condition`——持久化行拼作 `criteria_json`，作者侧是 CEL `condition`，正是 #3896 里跑偏过的那对词。`ownedBy` 带被移除的 owner 型规则处方。\n- **`identity/position.zod.ts`** — `PositionSchema` `.strict()`，并补上 `protection` 块 + ADR-0010 运行时保护信封——**关闭 #4071 账本里挂账的同类隐患**（`applyProtection` 对每个已注册类型盖章，position 是最后一个无法表达盖章的 schema）。guidance 把 `permissionSets`/`users` 指向运行时绑定表、`parent` 指向平结构规则。\n\n## 账本第 4 条 finding\n\n`position.test.ts` 一直在断言一个**虚构的层级能力**：ADR-0090 之前的「should accept position with parent」及四个「真实世界层级」示例都在传 `parent` 键——只因 `.strip` 吃掉了它才「通过」。position 树从未存在（ADR-0090 D3 定稿平结构；层级在 business-unit 树上）。测试在把剥离时代的幻觉固化成预期行为，已改写为断言拒绝 + 平结构指引。已记入账本 findings log。\n\n## 迁移\n\n被拒的键此前全部被静默丢弃、从未有过运行时效果——删除或按错误提示改名，行为零变化。changeset（major）含完整 FROM→TO 表。\n\n## 零破坏实证\n\n- `@objectstack/spec`：268 文件 / **6973 用例**全过，`tsc` 干净\n- 12 个 `check:*` 闸门**一次全过**\n- 下游：plugin-security 677 / lint 546 / metadata 276 / platform-objects 239 / metadata-protocol 99 全过\n- **dogfood 全套 72 文件 / 418 用例本地全过**（#4071 的教训：这次推送前跑了）\n- showcase（9 岗位 + 8 权限集）/ crm / todo `objectstack validate` 全过\n\n## 后续（不在本 PR）\n\n账本 next-targets 顺延：`ui/app.zod.ts`（导航递归 union 需单独设计错误呈现）→ `automation/approval.zod.ts` → `data/hook/datasource`；长尾走 lint 告警层，v18 收口。\n\nRefs #4001\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_0147tNF4Snk7Ry1KGt4a5PY4\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_0147tNF4Snk7Ry1KGt4a5PY4)_"
}
