{
  "number": 3534,
  "kind": "pull_request",
  "title": "feat(storage): 字段文件的受管下载 + R4 可执行验收门 — ADR-0104 D3 wave 2 (PR-4)",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/l",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-07-27T02:55:20Z",
  "updated_at": "2026-07-27T03:10:58Z",
  "closed_at": "2026-07-27T03:10:57Z",
  "closed_by": {
    "login": "os-zhuang",
    "id": 277994282,
    "type": "User"
  },
  "merged_at": "2026-07-27T03:10:57Z",
  "comments": 2,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/3534",
  "body": "承接 #3527（PR-3 独占所有权）。这个 PR 交付 wave 2 的**安全装置**——在开启回收（PR-5b，不可逆、已 gated）之前必须先存在的两样东西。\n\n## 1. 字段文件的受管下载\n\n被某条记录的字段拥有的文件（`sys_file.ref_object` / `ref_id`,由 PR-3 写入),现在下载时和附件一样要授权:调用者必须能 **READ 该文件的 parent 记录**,或者是它的上传者。此前只有 `attachments` scope 的文件被 gate,所有字段文件都保留匿名 capability URL。\n\n**两个面的 parent 解析方式不同,而这个不对称正是所有权模型的意义所在:**\n\n| | parent | 可读集合 |\n|---|---|---|\n| 附件 | 可能挂在**多条**记录上 | 所有 `sys_attachment` join 行的**并集** |\n| 字段文件 | 恰好属于**一条**记录 | 就是那一条记录的,仅此而已 |\n\n如果字段引用走共享模型,它也必须取并集——那正是\"把一个 fileId 拷进更公开的记录\"会无声扩大访问权限的机制。\n\n拒绝时返回 `FILE_DOWNLOAD_DENIED`(403),和附件路径的 `ATTACHMENT_DOWNLOAD_DENIED` 区分开:这个文件**属于**一条记录,而不是**挂在**若干条上,提示语不该说错。\n\n**`acl: 'public_read'` 是 opt-out**,并且现在是一个**显式声明**,而不再是每个字段文件默认白拿的东西。真正需要公开的图片(任何嵌在 `<img src>` 里、无法携带 bearer token 的)必须声明它。\n\n### ⚠️ 不会 gate 任何今天开放的东西\n\ncutover 之前的字段存的是 inline blob 或外部 URL,**从来不是 `sys_file` id**,所以现存文件没有一个记录了 owner,也就没有一个会开始被 gate。只有字段真的认领过的文件才进入 gate,所有权释放后又自动退出。这两条都有回归测试。\n\n## 2. `verifyFileReferences()` — R4 验收门的可执行形态\n\nADR 原本把开启回收的前提写成\"reference counting is verified\"——这是愿望,不是可检查的条件。这个函数让它可运行。\n\n它把**地面真相**(记录的 file 类字段实际持有什么)和**记录在案的所有权**对账,并按\"开启回收后会不会导致数据丢失\"给每一类分歧分级——因为只有那一类该阻断发布:\n\n**阻断:**\n| kind | 含义 | 后果 |\n|---|---|---|\n| `unowned_reference` | 记录持有一个没人拥有的文件 | 开了回收就看起来是空闲的,字节被删 |\n| `foreign_owner` | 记录持有的文件被别的槽位拥有 | 这条引用对生命周期不可见,登记的所有者一释放,字节就没了 |\n| `shared_reference` | 一个文件被两个槽位持有 | 独占性被破坏(copy-on-claim 没跑),且一个文件的 ACL 在服务两个 parent |\n\n**仅提示:**\n- `stale_owner` — 拥有但已不再持有。倒向保留,文件只是永远不被回收。\n- `unreferenced_file` — committed 但无人指向。是存储成本,不是正确性问题。\n\n扫描**只读**:不写、不打墓碑、不删除。要点在于——**一本账在被证明与现实一致之前,不该被授予对不可逆删除的权力**。所以它是设计来反复运行的:必须在真实租户数据上、连续多次报告零阻断项,被 gate 的开启回收那一步才允许合并。\n\n## 测试\n\n- `storage-routes.test.ts` +6:字段文件被 gate（scope 不是 attachments 也 gate）、401 未认证、授权放行、`public_read` opt-out,以及两条**双模式回归**——未认领的文件不 gate、所有权已释放的文件不 gate。\n- `verify-file-references.test.ts` +13:三类阻断项各自断言、两类提示项不阻断、attachments scope 被正确排除、inline blob / URL 值下**pre-cutover 租户读出全清白**、分页(1200 条)、截断标记、按对象限定范围,以及一条锁定\"哪些 kind 算阻断\"的断言。\n- 全量:service-storage **144** ✅ · nul-bytes ✅ · lint ✅\n\n## 后续\n\n剩下的 wave 2 步骤按 ADR 2026-07-27 addendum 的重新排序执行:写切换(PR-5a)→ 回填(PR-6)→ 对账 soak → **开启回收(PR-5b,不可逆,仍 gated)**。PR-5b 必须在同一个 change 里同时放松 `scope === 'attachments'` 护栏**和**扩展 reap guard 的扫描时复核——只做一半会让每一次释放都变成保证性的字节删除。\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)\n\nhttps://claude.ai/code/session_01SHpGw3GBA9aFpfwVArRWfd\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01SHpGw3GBA9aFpfwVArRWfd)_"
}
