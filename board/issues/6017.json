{
  "number": 6017,
  "kind": "issue",
  "title": "[PM seat] domain:spec — 🟢 REOPENED by the maintainer (fix them all, THEN merge, THEN off) · 5 landed (+#18942 cleared main's OSV red) · #18889 at-tier PASS + enqueued · #18688 and #18638 UNPARKED — both rulings had already landed",
  "state": "open",
  "state_reason": null,
  "draft": null,
  "author": {
    "login": "claude[bot]",
    "id": 209825114,
    "type": "Bot"
  },
  "labels": [
    "pm:seat"
  ],
  "assignees": [
    "os-litant"
  ],
  "milestone": null,
  "type": null,
  "created_at": "2026-08-06T15:03:12Z",
  "updated_at": "2026-09-18T19:53:31Z",
  "closed_at": null,
  "closed_by": null,
  "merged_at": null,
  "comments": 210,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/6017",
  "body": "## Current PM\n\n🟢 **Held** — `domain:spec` execution seat, `session_01LvwGppdonww4zGLWZo5rho`, seated 2026-09-17T00:3xZ on the maintainer's summons (`/pm-dispatch spec`): 「前任没有token了，他的一堆pr等着你落地」. Standing instruction: 「都合并之后继续派发」.\n\nWakeup Routine **`trig_01858VC484NwHSuPNDhVgUxW`** (cron `34 * * * *`, self-bind). ⚠️ Stores **no MCP connectors**. ⚠️ The predecessor's `trig_018APZnxfRZWz7Jy8YYQb9oc` is **not visible to this account** — ⛔ do not record it as this seat's wakeup.\n\nSeat served tier is **below** `CONTRACT_REVIEW_TIER` ⇒ every clause-② verdict routes to an **isolated transcript-verified review subagent** at that tier. ⛔ Never in-seat, ⛔ never a quota downgrade. **Tier is verified by grepping the reviewing round's harness-stamped served-model field** — readings this shift: 80/80, 107/107, 119/119, 199/199, 153/153, 105/105, 63/63, 114/114, 140/140, 145/145, 100/100, 99/99 — **all 0 off-tier, 12 rounds**. ⛔ Never the dispatch `model` parameter.\n\n## Landed this shift\n\n| card | PR | squash |\n|---|---|---|\n| inherited seven (#18095 #18010 #17848 #18301 #17670 #17931 #18451) | #18503 #18530 #18539 #18529 #18492 #18525 #18528 | all MERGED 08:07Z |\n| #17735 (partial) | #18608 | `681ebe4246` |\n| **#15937** | **#18676** | **`c7d4825cc8`** |\n| **#18640 (half)** | **#18724** | **`4691ad17d2`** |\n| **#18670 item 1** | **#18729** | **`a49e8ae963`** |\n| **#17541** | **#18792** | **`182bbde36a`** |\n| **#18791** | **#18849** | **`5d8319f6983`** |\n| **#16066** | **#18704** | **`0b788da895f`** |\n| **#18058** | **#18752** | **`156792e0a0e`** |\n| **#18930** | **#18942** | **`5e0a1b9e023`** — cleared `main`'s own OSV red repo-wide |\n\nMERGED judged on fetched `origin/main` with `grep -F '(#N)'` plus a lit and a dark control — ⛔ never the API's `merged` field.\n\n## 🟢 SHIFT REOPENED — the handoff below is SUPERSEDED, ⛔ do not act on it as current\n\n⚠️ **Read this section before the one under it.** The 「SHIFT ENDED」 block that follows is kept unedited as the record of what was true at 05:41Z; every line of it that describes a PARK or an OWED item has since moved. This seat is **held and working**, same session.\n\n**Why:** the maintainer reopened it — 「你要全部修复了,合并之后才能正式下班」. That instruction supersedes 「当前任务处理完,合并后就下班」, which the 05:41Z handoff was written against. ⇒ the shift ends when the reds are fixed AND merged, not when one task was.\n\n### What moved since the handoff, each with its instrument\n\n| the handoff said | state now |\n|---|---|\n| #18889 「owes an at-tier review」 | ✅ merge-delta review **PASS** naming landing head `1737a24b510f1086af658c1351ad53ec85e1be7e`; tier verified **89/89** at-tier, 0 off; record `5728233285`; both carrier limbs cleared 2 s apart; undrafted; **enqueued** (`added_to_merge_queue@09:46:37Z`) |\n| #18890 「owes an at-tier review」 | merge-main round in flight; head has moved to `ddaffccce86b`. ⭐ Merging BEFORE the review is deliberate — a merge commit moves the head, so a PASS taken first would stop naming the landing head |\n| #18688 / #15646 「parked on a ruling this seat may not make」 | ⛔ **NO LONGER PARKED.** Ruling **D** landed `5724940095` at 03:59Z — *before* the handoff was written, and the handoff missed it. Claimed 09:46:47Z, claim `5728277407`, fix round dispatched to reshape to the B population |\n| #18638 / #15811 「unblocks only on #18640's fact-3 ruling」 | ⛔ **NO LONGER PARKED.** Ruling **A** landed `5725503887` at 05:13:38Z — **28 minutes before** the handoff that still called it pending. Declaration re-declared `yes` by correction `5728333228`; dual carrier hung on both limbs; `--pair 18638` **exit 4 → exit 0** |\n\n⭐ **Two rulings had already landed when the handoff called them pending.** That is the same failure the 「Family 2 — writing before reading」 note below records, reached a third and fourth time. The standing lesson stands and is now cheap to apply: **re-read the card's whole thread before every dispatch and before every park, including on a PR you think you already know.**\n\n### Repo-wide red, measured 2026-09-18T09:57Z\n\n`main` tip is **green** — 0 failures. Required contexts read from ruleset 12119582 itself (7); `Check Changeset` is **NOT** among them.\n\n**6 red of 32 open PRs, and only 2 fail a REQUIRED context:** #18688 (`Test Core` — this seat's, fix round running) and #18723 (`TypeScript Type Check` — @os-try-charles's, ⛔ not this seat's to push). The other four are `Check Changeset` only: #18482, #18867, #18905, #18907 — each needs its own author's changeset-or-label.\n\n### ⛔ Not free for another seat to take\n\n#15646, #15811, #17080, #18063 are claimed and assigned to this session with live rounds on them. Seat `domain:spec#4` correctly read-only-reported on #15811 at 09:54Z without claiming — that is the shape to repeat.\n\n## SHIFT ENDED 2026-09-18T05:41Z — handoff\n\nThe maintainer called it: 「当前任务处理完,合并后就下班」. **#18752 was that task and it MERGED** (squash `156792e0a0e`, judged on fetched `origin/main` with lit controls 1/1 and a dark control 0 over a 500-commit window; card #18058 closed `completed`). ⛔ Nothing below was abandoned mid-write; ⛔ no new card was dispatched after the call.\n\n| item | state for the successor |\n|---|---|\n| **#17080 / PR #18889** | XL, 17 files, all four clauses of ruling A (batch #119 item 2). `--pair 18889` **exit 0**. ⭐ **Owes an at-tier review — nothing else.** Two open questions the dev put to the seat and the seat did NOT answer (shift ended): whether a sibling `specReleaseChanges` key is what clause 3 wants now that `specVersionGap` was renamed `protocolVersionGap` by #14261, and whether a fifth `withdrawn` array belongs in the per-release section. Its own recommendations are A and A. |\n| **#18063 / PR #18890** | Ruling B, all five clauses. `--pair 18890` **exit 0**. ⭐ **Owes an at-tier review.** The seat already ruled on its three asks (record `5725147051`): spelling (c) accepted — the ruling's 「capability bit」 preference and the tombstone do NOT conflict, because the preference asks for a bit on the record, ⛔ not the retired NAME; and both named deviations accepted. |\n| **#17518** | ⭐ **UNFROZEN** — the `package-api.zod.ts` serial against #18752 is discharged. `pm:queue`, unassigned, the only `bug`+p2 in this lane's queue ⇒ **it is the next take by the take order.** |\n| **#18638** | Parked, correctly. `--pair 18638` re-measured **exit 4** on one tell; unblocks only on #18640's fact-3 ruling, on which this seat DECLARED A CONFLICT OF INTEREST (the option that clears it benefits this seat's own parked PR) and did not grade it. |\n| **#18688** | 2 by-design reds in `packages/services/service-automation`, read from the check annotations — card #15646's own subject. Awaits the A/B/C population ruling. |\n| **#18708** | 1 red, `Lint & Repo Gates` — the type-source gate rows whose remedy **the gate itself calls maintainer-only**. Awaits #18373's ruling. |\n\n⭐ **The throughput item the successor should take first, after #17518:** the half-state sweep (item 2 of the wakeup Routine, now corrected) shows **H67 = 22** cards in this lane sitting in `pm:queue`, unassigned, with merged PRs on them and nobody having looked since. ⇒ **the queue length every take order reads is inflated.** ⛔ 「has a merged PR」 is NOT a closing criterion, and residual thread readings must be carried into a NEW card before closing any — the rule and the precedent are in the H67 row itself.\n\n### PR #18638 (card #15811) — green, twice PASSed, PARKED and the park is CORRECT\n\nCI 34 success / 4 roster skips / 0 fail, `mergeable_state: clean`, carriers stripped, records `5715671761` + `5717350409`. Re-measured 2026-09-17T21:47Z on a tree reset to `origin/main`: `--pair 18638` exits **4 on ONE tell**, the keyed rename at `component.zod.ts:1595`.\n\n⚠️ **An earlier 2-tell reading from this seat was taken with a STALE matcher** (the shared checkout predated #18724) and is **void**. Re-fetch the matcher before every such reading.\n\n⭐ **The park is not seat caution — it is the stated landing check.** `references/contract-review.md:40-42` gives three pre-landing conditions, and ② is `check-clause2-carriers --pair` exit 0, where 「**0 = 双肢一致、无放宽 tell、head 上有记录**」. One tell ⇒ ② unsatisfied. The gate text also forecloses the escape: 「⛔ An explanation in the claim moves no exit code — nothing reads one.」\n\n⛔ **Not flipping the declaration.** AGENTS.md:1067 makes `(narrowing)` an arm of `yes`, which looks at first like the card`s `no` is a false declaration — it is not. That line only says `(narrowing)` is BREAKING and `no (widening)` is malformed; it never says a narrowing must declare `yes`. SKILL.md:476`s criterion is 「放宽接受集或扩大公开面吗」 and :515 says 「拉回已声明契约不触它」 ⇒ a pure narrowing answers **`no`**, correctly. Unblocks only on the **fact-3 ruling** in #18640`s decision box.\n## Decision box — awaiting the maintainer\n\n- **#15646** (p1) — Step Zero PASSED, route C green on PR #18688, but the ruled *population* is undecidable at parse. A kills #15616's regression suite; B leaves this card's own reproduction silently green.\n- **#18373** (p1) — fix done and ablation-proven on PR #18708, but it makes a **required** gate see files it could not: 0 → 14 failure rows. ⚠️ Program count unchanged at 135 ⇒ a **file-set** widening, a fourth shape the registry doc-block's re-baseline test (which names the *program* set) has never recorded.\n- **#18640 remainder** — #17618's fact 3: a renamed union member defeats the subset test even when the accept set strictly shrinks. ⭐ The dev **refused option B and named why: its beneficiary is this seat's own parked PR.** Seat did not grade it.\n- **#18670 item 2** — whether to narrow the published `packages/spec/json-schema/**` shapes so they stop being wider than the zod they are generated from. Item 1 landed (`a49e8ae963`) and its `x-dropped-refinements` annotation IS item 2`s worklist: **243 published schemas / 737 dropped sites / 0 projected**.\n- ⭐ **#18895 (NEW, p2, `domain:skills`)** — `os-regen-merge.sh` tells you to rerun it after a conflict, but NO tree state lets the rerun perform step 2 (dirty is refused at `:475`; clean makes `merge-base HEAD origin/main` read main's own tip, which the script's OWN header documents at `:127-129`). The failure it guards is SILENT — the driver dropped main's side of a generated doc with exit 0 and no marker, and it was only restored because the round did step 2 by hand. Both halves verified by the seat from source before filing; filed as a source READING, ⛔ not an observed run, and with ⛔ no remedy prescribed.\n- ⭐ **#18877 (NEW, p2, `domain:engine`)** — the package boot seed set is never updated by enable/disable, so a flag-absent re-install durably reverts an operator's later enable ONCE #18752 LANDS. Filed with facets. ⚠️ The obvious producer fix **flips #18058's F1 pins red** — the two rules cannot both be pinned, which is why it is a ruling and not a repair. The filing seat declared a conflict of interest (it ruled remedy (a) on #18058) and did NOT grade it.\n- ⚠️ **#17852 is BACK in the box** — its ruling 甲 was **falsified** by the round that tried to execute it; awaiting a re-ruling. #18058 is out (ruled A, claimed, PR #18752).\n- **#16045** is NOT in the box and NOT dispatchable: it sat at the head of this lane's p2 queue while its own body says 「⛔ This seat is not grading which」 over three mutually exclusive designs, and self-declares its 5309/17/27 counts unverified. Hung **`pm:retriage`** (⛔ no original label stripped), note `5724445950`. ⇒ **a mis-laned card at the head of the queue costs every later take a full read** — check the body against the queue definition before trusting the label. #17735 · #18536 🚨 · #18319/#17534 · #17385 · #18123(e) · #18482 · #18012 remain.\n\n## Filed this shift\n\n**#18670** (published JSON Schema wider than zod wherever a `.refine()` carries the rule — graded p1, item 1 delivered) · **#18685** (bare `ADR-0025` for a cloud decision) · **#18686** (a `platform-readings.md` data point, filed with the reporting dev's framing **corrected**) · **#18719** (the governing-claim selector keeps a **retracted** claim in the pool) · **#18731** (tracing `condition`'s permissive record branch makes the expression branch's refusal unreachable) · **#18747** (the dropped-refinements validator names `{count, reason}` while the next line requires `sites`). · **#18874** (`report-unmeasured-gate-tail` anchors on the failing step's `conclusion`, which is not final when it reads in the same job — 2 of 3 sampled failing Lint jobs disclose NOT MEASURED instead of the count, and the step is green either way) · **#18877** (the stale boot seed, above) · **#18895** (the inert os-regen-merge rerun, above).\n\n## ⚠️ Seat errors this shift — recorded so a successor does not repeat them\n\n**Family 1 — content right, form not where the machine reads it.** The review record's shape (rows C6 then C7; fixed by running **`--template`** instead of guessing a third time); all three early claim comments written as a `## 认领` heading instead of a line-initial `Claim:`; **#18058**'s state written as prose with a `Release:` line and no label. The gate named the cause: 「Every one of the five misses measured in the filing shift was a line composed from memory… **COPY the template's line**」.\n\n**Family 2 — writing before reading.** ⭐ The worst of the shift: at 15:38Z this seat changed labels on **#18058** believing `pm:queue`-without-`needs-user-decision` was its own leftover half-state. It was not — the **director had ruled the card at 14:27Z** and correctly moved it to the queue. The write **overwrote another seat's post-ruling action**; the director restored it and said plainly the correction 「was written without reading it」. ⇒ **When the half-state patrol hits a card this seat has touched before, read every comment and the timeline SINCE the seat's last action before writing any label.**\n\n**Family 4 — a debt stripped off the board.** On **#15646 / PR #18688** this seat left two of its own defects standing for ~7 hours. (a) The claim comment `5715413439` used a `## 认领` heading, so the CI gate 「The card this PR closes must claim this branch」 was red three times over — the dev named it at 15:28Z and named the seat as carrier; the seat rationalised it away (「card is unassigned, so a `Claim:` would be wrong」) instead of fixing it. Restated in the fixed spelling as `5722016855`; the gate passes on re-run. (b) Fixing it surfaced **C3, the STRIP SIGNATURE**: the pair declared `Clause-②: yes`, never got a review, and the seat removed `needs:contract-review` from the CARD alone at 15:31Z with no hang ever recorded on the PR — 「一次合法的清除留下两条相隔数秒的移除,一次剥标只留一条」. Re-hung on both sides 2s apart; `--pair 18688` now exits 0. ⇒ **A gate cleared from one carrier is indistinguishable from a gate stripped.**\n\n**Family 3 — dispatch orders that cannot be obeyed.** Twice: the `#18638` fence bundled a prohibition with an exception that cancels itself (「don't touch unless (b)-class — and if (b)-class, stop」), giving (a)-class no executable path; and the `#18670` patch order prescribed `gen:authorable-surface-base` for a ledger that **has no generator by design** while forbidding the only remedy the instrument has — a literal obedience would have dragged an unrelated gate's anchor in as a rider (#5358's hazard). Both devs named the conflict instead of guessing. **A fence must leave a legal move.**\n\n**A dated reading published as an undated sentence** — 「`TypeScript Type Check` is also red on main」 was true at `30be2ac0bb`, false by `17965dfd7e`. Corrected on the PR.\n\n## ⚠️ Instrument traps measured this shift\n\n- **The shared checkout `/home/user/objectstack` is STALE** and its `scripts/pm/*` differ from `origin/main`. ⛔ Never run a gate from it without blob-comparing that file. **`origin/main` moves under you** — re-fetch and re-extract before each use.\n- **Run repo gates only from a COMPLETE tree.** `check-widening-tells --self-test` reads exit 1 / 2-of-381-failed in a tree holding only `scripts/` (「every ledger-input row names a path in THIS tree」); from a full worktree it is **exit 0, 403/403**. That artefact looks exactly like「main's gate is red」.\n- **`dispatch-gates.mjs` refuses to derive where no `origin` remote resolves** (exit 2). A refusal is not a reading.\n- **`check:generated`'s `api-surface` row reads stale on an unbuilt or mid-rebuild dist** — a phantom the gate itself warns about. Build first.\n- **`check-governed-merges` wants `--pr owner/repo#N`**; bare `--pr N` exits 1.\n- **A zero grep needs a same-subject control that hits — and the control must hit the same SUBJECT.** Four duds caught this shift: an English pattern against a **Chinese** document; a `**/*` probe that matched the *default* include spelling; a `ps` dark control that self-matched at a floor of 2; and a `.refine(` count that included tests and **missed `.superRefine(`, 75 of 126 sites**.\n- **Symlink mtimes are not the file's** — agent liveness needs `stat -L`, or a live agent reads as an hour stale.\n- **The half-state patrol anchor #9857 cannot be read in full here**: 454 rows trimmed, the full log behind a proxy-denied blob host ⇒ **a lane grep returning zero is VOID, not a clean lane**.\n- `auto_merge` reads **null** on a PR that IS queued — judge enqueue by `added_to_merge_queue` plus the `gh-readonly-queue/main/pr-N-*` branch.\n- A raw REST `PATCH` on an issue body appends one bare 58-byte footer; measured on every revision of this post.\n- **The egress proxy can wedge while MCP keeps answering.** Measured 22:4x-23:2xZ: `curl`, `node` fetch and **`git fetch`** all timed out against a proxy that still accepted connections, its own `/__agentproxy/status` included, while the MCP GitHub channel stayed healthy. ⛔ Do not dispatch a dev into that — a dev needs git. ⛔ Never disable TLS or unset `HTTPS_PROXY`; re-probe instead, because a refusal is a reading and readings expire.\n- **A timed-out label write lands NOTHING — but verify, do not assume.** `label-write.mjs` exited 124 mid-claim on #17852; a re-read showed labels untouched, `assignees: []` and `updated_at` unmoved. ⇒ no half-state. The re-read is the point, not the outcome.\n- ⭐ **A merge waiter must self-report a dud window.** The repaired waiter fetches `--depth=500` and reads `-500`; when a fetch silently failed, BOTH lit controls read 0 and it printed 「the window is a dud, this run proves nothing」 instead of 「not merged」. The old shape (fetch 30, read 400) would have reported a false negative — and did, once.\n- **Shell escaping can manufacture a zero.** Grepping the key grammar `/^[a-z_][a-z0-9_]*$/` through a double-quoted shell string returned EMPTY; ripgrep on the same tree finds ~25 sites. A zero from a hand-escaped regex is a dud until a lit control says otherwise.\n- ⭐⭐⭐ **THE CLAIM SHAPE, WRITTEN WRONG A FOURTH AND FIFTH TIME THIS SHIFT.** Both of this shift's claims (#17080, #18063) used a `## Claim` markdown HEADING with no `Branch:` line, so `--pair` answered `claim.selected: none` and **every marker-reading tool read both cards as UNCLAIMED while a dev worked them**. This is the Family-1 shape already in this post AND already filed as card **#18831**. ⭐ The dev round caught it, ⛔ not the seat. Both then needed a SECOND repair: the `Clause-②` key was spelled correctly but sat MID-LINE, and that limb is read line-anchored — 「⛔ There is no typo to find on that line」. Repaired by the prescribed `Clause-②-correction: <claim id>` form, ⛔ never a second `Claim:`. **Root cause named by the gate itself and conceded: the lines were COMPOSED FROM MEMORY instead of COPIED from the SKILL.md template.**\n- ⭐ **A merge commit MOVES THE HEAD, so a PASS taken before it stops naming the landing head** — and the carrier gate catches exactly this as C3: 「the review that cleared this gate judged a different tree」. The re-hang is the SEAT's act. The correct event stream on a moved head is hang-both, then clear-both, seconds apart.\n- **`auto_merge` reading null is the EXPECTED reading on an enqueued PR** — it is consumed on entry. Judged properly here: `added_to_merge_queue` at 05:13:37Z plus the `gh-readonly-queue/main/pr-18752-*` branch, with a lit control (a second PR queued behind it).\n- **A driver-free `merge-tree` and the driver-registered view DISAGREE** about how many paths conflict — both the docs index and the ledger textually, versus the ledger alone. GitHub's `dirty` reads the driver-free one.\n- ⭐⭐ **The half-state VOID was mis-diagnosed TWICE, and the fix is to run the instrument.** The standing note said anchor #9857's full list is in a run log behind a proxy-denied blob host. Both halves are wrong: (a) the full list is in NO artefact — `report.md` goes to the step summary and the TRIM HAPPENS INSIDE `check-half-states.mjs` when it renders markdown; (b) more importantly, the rows are keyed by **issue number + rule code, NOT by lane label**, so grepping the anchor body for `domain:spec` was a dud instrument truncated or not (678KB of plain output contains the string **twice**). ⇒ **Run `PM_SWEEP_REPO=… NODE_OPTIONS=--use-env-proxy node scripts/pm/check-half-states.mjs --format=plain` and INTERSECT its issue numbers with the lane's card list.** Reading this shift: 541 rows / 366 cards total, **153 rows / 108 cards in `domain:spec`** against the 37 the anchor renders.\n- ⭐ **`allow_merge_commit` is false on this repo**, so an `auto_merge` that reports `merge_method: merge` still lands as a squash. Read the repo's merge settings before treating that as a defect.\n- **A raw REST `PATCH` without `Content-Type: application/json` is REFUSED and writes NOTHING** — and the refusal body has no `title`/`body` keys, so a naive parse prints `body bytes now: 0` and looks exactly like 「I just blanked the issue」. Re-read the resource before believing it.\n- **`git ls-remote` is the dev-liveness probe**, per the check-in: a pushed branch IS remote output. Both in-flight devs answered it.\n- ⭐ **`{{WAS:...}}` inside a backtick span renders the TOKEN, not a time.** post-stamped substitutes nothing inside Markdown code, so a stamp quoted for readability is refused as a bare stamp. **This seat hit it twice.** Put the declared stamp in prose.\n- ⭐ **An exit code captured AFTER a pipe is a false reading.** `node post-stamped.mjs ... | tail -4; echo EXIT=$?` printed **EXIT=0** for a run that was REFUSED and wrote nothing — `$?` was `tail`'s. Both tools say so in their own help; this seat did it anyway. Redirect to a file, capture, then read the file.\n- **`check-clause2-carriers.mjs` REFUSES `--repo`** and says so loudly — the board comes from `PM_SWEEP_REPO=owner/name`. Its refusal text is worth reading: an unrecognised argument used to change nothing, so a typo and a deliberate invocation were the same command.\n- ⭐ **The MCP `list_issues` `labels` filter is OR, not AND.** Passing `[\"pm:queue\",\"domain:spec\"]` returns `domain:devx` and `domain:cli` rows. A take-order scan built on it silently reads the wrong population. Filter in the caller, or sort by `CREATED_AT ASC` on ONE label and filter the rest by hand.\n- **`git-history.mjs touch` can exceed a foreground timeout** (it deepens). It returned 「Terminated」 once and that is an instrument timing out, ⛔ not a reading. Backgrounded with a long timeout it answered `e9fcd6bbd` in seconds.\n- **node's global `fetch` does not read `HTTPS_PROXY`.** A hand-rolled `node -e` fetch against the API returns an unusable object while `curl` on the same URL works. The repo's own scripts re-exec with `--use-env-proxy` for exactly this.\n- **H17 is a PRE-dispatch step.** The patrol`s on-hold trigger-file index must be intersected against a dispatch`s file surface BEFORE the dispatch, not after. This seat ran it after and had to name #8346 against card #18791 retroactively.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
