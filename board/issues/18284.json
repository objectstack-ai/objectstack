{
  "number": 18284,
  "kind": "issue",
  "title": "[finding] post-stamped: a malformed `WAS` token (non-timestamp payload) is neither rendered nor refused — the raw token and its payload are stored verbatim",
  "state": "open",
  "state_reason": null,
  "draft": null,
  "author": {
    "login": "claude[bot]",
    "id": 209825114,
    "type": "Bot"
  },
  "labels": [
    "domain:skills",
    "pm:dispatched",
    "priority:p3"
  ],
  "assignees": [
    "os-zhuang"
  ],
  "milestone": null,
  "type": "Task",
  "created_at": "2026-09-15T07:48:17Z",
  "updated_at": "2026-09-15T08:05:02Z",
  "closed_at": null,
  "closed_by": null,
  "merged_at": null,
  "comments": 1,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/issues/18284",
  "body": "Filed by the `domain:skills` execution PM seat, session `session_01HZfg2AwVX191qCizp88gQr`, at 2026-09-15T07:48Z, from its own write at 2026-09-15T07:47Z (PR #18279, comment 5676662783, since repaired in place with an edit note).\n\n## The reading (live, this seat's own artefact)\n\n- The seat's landing-provenance draft carried a `WAS` token whose payload was filled from a shell variable; the shell read failed and the variable held a multi-line Node error dump instead of an instant. The token slot therefore read `WAS:` followed by 「[eval]:1 …SyntaxError…」 and the closing braces, all inside the double-brace form.\n- `scripts/pm/post-stamped.mjs --dry-run` printed its DRY RUN line (the seat's guard) and the live write printed 「comment posted」. The stored body carries the opener, the error dump and the closer verbatim; nothing was substituted and nothing was refused. Read back and repaired by a REST `PATCH` of the comment with an edit note at 2026-09-15T07:48Z.\n- The tool's contract (its own usage text) covers the two recognised tokens, a bare stamp on the opening line, and a quoted stamp later than the clock. A token-shaped opener whose payload does not parse as `YYYY-MM-DDThh:mmZ` falls outside every rule and passes through.\n\n## What is asked (⛔ not asserted — the skills seat grades)\n\nRefuse any double-brace opener that is not exactly a recognised token: `NOW` bare, or `WAS:` followed by a payload that parses as the documented instant form and is not later than the clock. Print `post-stamped: REFUSED` with the offending span (first 60 bytes) and exit non-zero, on `--dry-run` too. Self-test cases: a `WAS` with a non-timestamp payload, a `WAS` with a multi-line payload, an unknown token name, an unclosed opener. `scripts/pm/post-stamped.mjs` + its self-test only — non-governed; the seat's own guard (post only after the tool's DRY RUN line) stays.\n\nGrading (lane `finding` self-triage; the seat's own tooling): p3 · Task · `pm:queue` · `domain:skills`. Serial: `scripts/pm/post-stamped.mjs` is free (no open PR touches it).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"
}
