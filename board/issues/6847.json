{
  "number": 6847,
  "kind": "pull_request",
  "title": "fix(spec): wait-timeout 处方改印能真正解析的 `timerDuration: '60000'`（#6758）",
  "state": "closed",
  "state_reason": null,
  "draft": false,
  "author": {
    "login": "os-project-manager",
    "id": 314343378,
    "type": "User"
  },
  "labels": [
    "documentation",
    "size/m",
    "tests",
    "tooling"
  ],
  "assignees": [],
  "milestone": null,
  "type": null,
  "created_at": "2026-08-09T00:20:06Z",
  "updated_at": "2026-08-09T01:22:13Z",
  "closed_at": "2026-08-09T01:16:46Z",
  "closed_by": {
    "login": "os-project-manager",
    "id": 314343378,
    "type": "User"
  },
  "merged_at": "2026-08-09T01:16:46Z",
  "comments": 5,
  "locked": false,
  "html_url": "https://github.com/objectstack-ai/objectstack/pull/6847",
  "body": "Fixes #6758\n\n## 问题\n\n`waitEventConfig` 上有**两处**面向作者的文案让人写 `timerDuration: 60000`，而 `timerDuration` 是 `z.string()`：\n\n1. `timeoutMs` 墓碑（`retiredKey()`）—— 升级作者写了已retire的键时看到的处方；\n2. `timeout` 拼写错误的 `strictObject` `guidance` 条目 —— 更糟，因为作者没有\"第一次失败\"可学，这是 schema 对这个块说的**第一句话**。\n\n照着处方写的作者，先在编写处吃 TS2322，再在解析时吃一个**不带任何处方**的裸 `invalid_type`——正是墓碑存在的意义所要挡掉的那两个错误。\n\n代码库自己早就知道正确答案。ADR-0087 转换的文档注释直言：\n\n> `timeoutMs` moves to `timerDuration` … Moving the number unstringified would produce a block that no longer parses.\n\n它写的也确实是 `String(next.timeoutMs)`。只有散文说反了。\n\n## 实测：`timerDuration` 到底接受什么\n\n不是猜的，是跑出来的（`parseIsoDuration`，`wait-node.ts`）：\n\n| 写法 | 结果 |\n| --- | --- |\n| `'60000'`（裸数字字符串） | 60000 ms ✅ |\n| `'PT1M'`（ISO 8601） | 60000 ms ✅ |\n| `60000`（裸数字） | helper 收，但 **schema 先拒**，到不了 helper |\n| `'60s'` 这类简写 | `undefined` ❌ 没有这种格式 |\n\n所以处方值是**加引号的** `'60000'`，与转换的输出、以及 `conversions/registry.ts` 的 fixture 完全一致。\n\n## 改了什么\n\n两处处方都改印 `timerDuration: '60000'`，并给出等价的 `'PT1M'`，且**说明为什么要加引号**（键是字符串；裸数字字符串按毫秒读）。同一段 TSDoc 顺带订正 issue 点名的两处：`retired in 18` → `17`（两处墓碑与转换的 `toMajor` 都是 17，#4350 那一类），以及把「`parseIsoDuration` accepts a bare number」改成「reads a bare numeric *string*」——作者遇到的是 schema，不是那个 helper。\n\n## 接受面逐字节不变\n\n改动全部落在 `retiredKey()` 的 guidance 参数、`strictObject` 的 `guidance` 取值、TSDoc 注释里。`retiredKey()` 返回的始终是 `z.never({ error: () => guidance }).optional()`，`guidance` 也只为一个**已被拒绝**的键提供文案。没有碰任何 Zod 类型、键集合或 `superRefine`。\n\n> ⛔ 明确**没有**做的事：把 `timerDuration` 放宽成 `z.union([z.string(), z.number()])` 让原处方\"变成真的\"。那会改变接受面，且需要就 ADR-0087 转换是否继续 stringify 作出裁决——属于 `domain:spec` 协议席位，不在本 lane。\n\n## 验证：自校验的 pin，方向先预测后运行\n\npin 不比对硬编码副本，而是**从活的报错消息里把处方值提取出来**再喂回去——硬编码 `'60000'` 会在有人改写文案的那一刻变绿，而那正是最需要检查的时刻。两个 pin 都带反空转保护（把 guidance 条目删掉会红，而不是因为\"没找到东西可查\"而绿）。\n\n- `packages/spec/src/automation/flow.test.ts` —— 两个渠道印出的**每一个** `timerDuration` 值都必须过 schema。\n- `packages/services/service-automation/src/builtin/wait-node.test.ts` —— 走完作者真正要过的**两道闸**：schema，然后 `parseIsoDuration`。并把墓碑的**等式主张**（\"`timeoutMs: N` 和 `timerDuration: X` 是同一个 wait\"）两边都从消息里读出来对拍。\n\n**预测方向：把散文回退到 `origin/main`、pin 保留 ⇒ 两个 pin 全红。实测一致：**\n\n```\nAssertionError: the `timeoutMs` tombstone prints `timerDuration: 60000`, but the schema\nREJECTS it: [{\"expected\":\"string\",\"code\":\"invalid_type\",\n\"path\":[\"waitEventConfig\",\"timerDuration\"],\n\"message\":\"Invalid input: expected string, received number\"}]\n```\n\n修复后两个 pin 全绿。\n\n一个值得记下的子预测，也应验了：**红是 schema 那道闸给的，不是 reader**。`parseIsoDuration(60000)` 返回 `60000`，所以**单独**的 round-trip 断言在这个缺陷上会是绿的——helper 的宽容在这条路径上根本不可达，这也正是当初写错文案的由来。两道闸缺一不可。\n\n## 为什么两个 package 都要有 pin\n\n`timerDuration` 是 `z.string()`，所以 schema 对**任何**字符串都放行：只有 spec 侧的 pin，文案被改成 `timerDuration: 'about a minute'` 依然全绿。而 `packages/spec` 不依赖 service-automation，够不到 reader。所以 round-trip 钉在两者都能 import 的那一侧。\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_018ffcE95NaMJcL9XJ9VDYgk)_"
}
