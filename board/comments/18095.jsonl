{"id":5659152294,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-14T04:46:06Z","updated_at":"2026-09-14T04:46:06Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18095#issuecomment-5659152294","body":"## 分诊定级 / Triage — `domain:spec` · `priority:p2` · `pm:queue`\n\n落点 `packages/lint`。车道表 `SKILL.md:246/239` 的例外条:`packages/lint` **围着 spec 契约转的归 `domain:spec`**,余留 devx —— `check-reference-carrier-shape` 判的是契约引用形,归 spec。卡面自提 `domain:spec`,与车道表一致。\n\n`priority:p2`:一道**判得出却判漏**的门禁(JSON-Schema properties map + 纯字面量形),属类别 (b) 声明契约被违反。⛔ 非 p1:无主干红、无已发布面矛盾。\n\n<sub>分诊席位 · `session_01PAMZt3owWHe7CMyTzrDkwF` · R+233 · 经 REST 通道以 `claude[bot]` 续跑 · 本评论来自分诊座位</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5696260417,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-16T10:50:03Z","updated_at":"2026-09-16T10:50:03Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18095#issuecomment-5696260417","body":"Claim: PM loop, `domain:spec` execution seat\nSession: `session_01KB5PFtxuy1x3dcR5gxudx6`\nBranch: `claude/issue-18095-reference-carrier-shape-blind-spot`\nWorktree: `objectstack-issue-18095`\nDomain: `domain:spec`\nFile surface: `packages/lint/scripts/check-reference-carrier-shape.mjs` and its tests; `packages/services/service-automation/src/builtin/screen-nodes.ts` READ-ONLY as the specimen.\nClause-②: no\n\nThis card asks for a **reading** first and a fix only if the reading confirms. The card's own recipe is the acceptance, and its third step is load-bearing: a gate declining a shape it was never scoped to may be CORRECT, in which case the defect is an undeclared scope (an ADR-0049 question) and the right outcome is a decision card, not a patch. A measured \"no defect, here is the evidence\" is a complete answer to this dispatch and is not counted as rework.\n\n`Clause-②: no` on the mechanical floor: a holder rule added to a gate script adds no exported symbol and no key to a published payload, and a gate that judges MORE shapes is stricter, not wider. If the diff ends up adding either, the dev reports it and the seat posts a `Clause-②-correction:` — the dev neither hangs nor strips the carrier.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5696425487,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-16T11:04:11Z","updated_at":"2026-09-16T11:04:11Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18095#issuecomment-5696425487","body":"os-dev-report\n\n{\n  \"issue\": 18095,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-18095-reference-carrier-shape-blind-spot\",\n  \"pr\": null,\n  \"session\": \"session_01KB5PFtxuy1x3dcR5gxudx6\",\n  \"premise_still_valid\": false,\n  \"outcome\": \"(b) — the lead's MECHANISM confirms, the gate is CORRECT to decline, and its scope for this shape is undeclared. No patch written, no PR opened.\",\n  \"summary\": \"The reading the card was missing is delivered, and it splits the lead in two. CONFIRMED: no holder rule exists for a JSON-Schema `properties` map. Census row on origin/main 55fd5ee436 (2026-09-16T10:54:35Z): `unresolved / non-literal / (none) / packages/services/service-automation/src/builtin/screen-nodes.ts:138 / LOOKUP_TARGET_COLUMN` — `(none)` means zero of R1/R2/N1/N2/N3 fired, exactly as reported. NOT CONFIRMED: the gate does NOT exit 3. It exits 0 on 55fd5ee436 (10:54:22Z) and again on the live tip b9598e9cab (11:00:54Z), identical census both times: 6793 files, 681 `reference` sites, 584 field-def string carriers, 0 field-def non-string, 45 unresolved + 12 conflicting, 0 material. The specimen was already defused before the lead was filed: the column value is NAMED (`LOOKUP_TARGET_COLUMN`), so it classifies `non-literal` = unjudged, not `non-string-literal` = material. screen-nodes.ts lines 49-68 document that the naming is deliberate and load-bearing, and end with 'the missing `properties`-map rule is reported to the maintainer rather than patched from inside this PR'. THE COUNTERFACTUAL REPRODUCES THE LEAD EXACTLY: inlining the column (ablation, trap-restored) gives EXIT 3 and the refusal line `packages/services/service-automation/src/builtin/screen-nodes.ts:138 ... [rules: (none)]`. That named-vs-inline pair IS the card's step 2 — the named value the literal-only predicate declines to judge, with its lit control — measured on the real specimen rather than a synthetic fixture. WHY THE GATE IS CORRECT TO DECLINE, in three measured parts. (1) The decline is LOUD, not blind: an unresolved position holding a non-string literal hits the documented refusal branch ('the gate refuses rather than picking one'), which is the opposite of a blind spot. (2) The literal-only predicate is an already-DECLARED non-claim: the script's 'What it does NOT claim' section names the variable/helper/spread population at 511 fixtures and routes it to 'a runtime hook, not a scan, and is a different card'. (3) Widening it is already adjudicated AGAINST and is not bounded: packages/cli/test/data-model-rules.test.ts:803-807 hides a non-string carrier behind `const nonStringReference` on purpose, and its own comment says the value must stay non-inline because the assertion is that it resolves to NOTHING, ending 'do not weaken the gate or add a path ignore there — it has no baseline and wants none, by design'. Widening the predicate would newly RED that file. #13103 had already measured and refused the broad variant (838 fixtures across 303 files, invalid on purpose). WHAT IS ACTUALLY UNDECLARED, and it is a real gap: the refusal text prescribes exactly three remedies and forbids a path ignore, and for a JSON-Schema `properties` holder all three are unspellable — measured, not quoted. R1 cannot fire (the holder sits under `properties:` inside `items:`, not under `fields:`). R2/N3 cannot fire (the holder's own `type` key holds a sub-schema object, not a string literal). N2 cannot fire (0 of its 12 keys lie outside `data/Field`'s 74 authorable keys; control: `xRef` and `zzz_not_a_key` both absent from that set). So an author meeting this shape has no sanctioned move, and the fourth route actually taken — name the value — is nowhere in the prescription and removes the site from judgement entirely. Whether that fourth route is sanctioned, or a coverage hole to close, is the maintainer's scope call. WHY I DID NOT PATCH IT. A narrow N4 ('holder sits under a `properties:` key ⇒ not a carrier') is UNFALSIFIABLE against the tree: exactly ONE of 681 `reference` sites tree-wide sits under a `properties` map, and it is already immaterial, so N4 would change ZERO verdicts on main and could only ever be exercised by its own synthetic self-test. It would also import a foreign vocabulary — every existing N-rule reads its authority from packages/spec at run time (the FieldType enum, the ratcheted authorable surface), whereas `properties` is JSON Schema's word, and the script warns at length that judging by key name alone 'reds four generated i18n bundles and three driver assertions on the day it lands'. Three different scopes are live (narrow key-name N4; a structural recogniser additionally requiring an enclosing `type: 'object'`; or keep the refusal and add a FOURTH prescribed remedy to its text), they are different architectures, and screen-nodes.ts already records the maintainer as the decider. PROSPECTIVE COST, which is the argument for deciding rather than shelving: exit 3 is a WHOLE-GATE refusal, not a per-file finding, and lint.yml runs this gate unconditionally in the required `Lint & Repo Gates` job. So one author writing an inline `reference:` sub-schema into any N2-blind `properties` map reds that required job for EVERY open PR in the repo until someone defuses it. Measured population: 866 `properties:` object maps tree-wide, 49 of them N2-blind (every key inside data/Field's authorable surface), 1 already carrying a `reference` key (the specimen). The other 48 are live tripwires, and the designer forms among them are the likely ones because they mirror Field's column set by construction, with builtin-node-form-zod-ledger.test.ts reconciling that mirror both ways.\",\n  \"tests\": \"No diff was produced, so no build, no package test and no gate family is owed — confirmed mechanically, not assumed: `node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack` exits 2 with 'this branch changes nothing against origin/main (merge base 55fd5ee43) — nothing to derive', and it verified --repo against this checkout's origin remote. `git status --porcelain` is empty. MEASUREMENT RUNS, every exit code captured by redirect-then-$? ON THE COMMAND, never through a pipe (the mistake this card exists to not repeat). [1] Baseline, origin/main 55fd5ee436, 2026-09-16T10:54:22Z: `node packages/lint/scripts/check-reference-carrier-shape.mjs` EXIT 0 — 'OK — 6793 file(s) scanned, 681 reference site(s) ... 584 string literal(s), 0 non-string literal(s) ... 45 site(s) + 12 conflicting — 0 material'. [2] Same command on the live tip b9598e9cab, 11:00:54Z (fetched and measured in one block, in a throwaway detached worktree that has since been removed): EXIT 0, byte-identical census. [3] --census, EXIT 0: the specimen row is `unresolved / non-literal / (none) / packages/services/service-automation/src/builtin/screen-nodes.ts:138 / LOOKUP_TARGET_COLUMN`. [4] --self-test, EXIT 0. [5] ABLATION, the lit control, on the real specimen. Mutation: line 138 `reference: LOOKUP_TARGET_COLUMN,` replaced by an inline sub-schema literal. ON-DISK PROOF before reading any result, both anchors counted: old anchor 1 then 0, new anchor 0 then 1; blob hash moved c9f88d74df30174720a85e1747e7a5ae5c1681ef to 8a4d4a197dbc521a1e0a967d8bf9114a40f7957a, and the script aborts with a FATAL if the hash had not moved. MUTATED LEG: gate EXIT 3, refusal text 'packages/services/service-automation/src/builtin/screen-nodes.ts:138  { type: string, title: Lookup object, xRef: { kind: object }, desc   [rules: (none)]'; census row becomes `unresolved / non-string-literal / (none)`, census EXIT 3. RESTORE LEG: `trap restore EXIT INT TERM` with absolute paths seeded from `git rev-parse --show-toplevel`, restoring with `git checkout HEAD -- ABSOLUTE_PATH` (never a bare `git checkout --`), proven by hash back to c9f88d74df30174720a85e1747e7a5ae5c1681ef AND `git diff HEAD` empty AND `git status --porcelain` empty — not by an exit code. An empty hash is treated as FAILURE, not as nothing-to-compare. No dist/build leg exists in this ablation and none is owed: the gate reads the specimen as raw source text via readFileSync from the repo root, so nothing resolves through any package's dist. [6] N2 measurement with a same-subject control: 12 of 12 holder keys are in data/Field's 74 authorable keys (0 outside); control words `xRef` and `zzz_not_a_key` both return false against the same set, so the set is not a predicate that answers true to everything. [7] Holder-context probe (read-only, mutates nothing, replicates the gate's own recogniser): it independently reproduces the gate's 681 sites and 45 no-rule sites — that agreement is the control proving the probe is faithful — and finds exactly 1 of those 45 sitting under a `properties` map (screen-nodes.ts:138), the other 44 being 40 with no property-assignment parent and 4 under `params` arrays; control: 26 `reference` sites sit under a `fields` container, so the parent-key extraction is not returning null for everything. [8] Recurrence probe: 866 `properties:` object maps tree-wide, 49 N2-blind, 1 already carrying `reference`; control: the authorable key count reads 74, non-zero, so the surface read worked. [9] Widening blast radius, read directly from the census: of the 15 field-def sites the literal-only predicate leaves unjudged, packages/cli/test/data-model-rules.test.ts:807 resolves to `const nonStringReference = { object: 'project' }` — a deliberate counter-example that would newly RED under any predicate widening, and whose in-tree comment forbids exactly that change.\",\n  \"mcp_calls\": \"0 — no MCP GitHub tool was called at any point in this run (no read tools, no write tools). Every GitHub read and the one write went through the REST proxy with curl.\",\n  \"api_writes\": \"1 — POST /repos/objectstack-ai/objectstack/issues/18095/comments (this report). No POST /pulls and no POST /issues/18095/labels: no PR was opened, so neither is owed. `git push -u origin claude/issue-18095-reference-carrier-shape-blind-spot` ran once as the write-route probe before the first edit and succeeded (pre-push printed 'check:commit-card-trailers: 0 commit message(s) ... carry no card relation and no model identifier'); the branch is on origin carrying zero commits, as the landing marker for claim comment 5696260417.\",\n  \"clause_2\": \"Not exercised and therefore neither confirmed nor contradicted: the round produced no diff at all, so it adds no exported symbol and no key on a published payload, which is the seat's `Clause-②: no` floor holding vacuously. No PR body line was written because no PR exists. `node scripts/pm/check-clause2-carriers.mjs --pair PR_NUMBER` was NOT run and has no exit code to report — there is no PR number to pair. `needs:contract-review` is NOT present; issue 18095 carries exactly `priority:p2, pm:dispatched, domain:spec` (REST read, 2026-09-16T11:01Z). It is the seat's label and I neither hung, stripped nor waited on it.\",\n  \"os_dev_md_divergences\": [\n    \"L51 — loaded copy says GitHub writes are signed 'claude[bot]' by the App; origin/main WINS: attribution is the session ID IN THE TEXT, explicitly NOT `user.login`. Followed origin/main.\",\n    \"L53 — loaded copy adds 'user-account attribution, suspension hides it' plus a ban on enumerating boards and broad searches; origin/main WINS: the token is per-session (installation gives claude[bot], user-to-server gives the user). Followed origin/main; I enumerated nothing and ran no search either way.\",\n    \"L287 — loaded copy says a `Clause-②: yes` claim means the dev hangs `needs:contract-review` when opening the PR; origin/main WINS: the label is the SEAT's, the dev neither hangs, strips nor waits on it, and reports its presence plus the `--pair` exit code as a reading. Followed origin/main (reported above under clause_2).\",\n    \"L301 — loaded copy says a refused write means stop and report `blocked`; origin/main WINS: report the endpoint and the status code, the seat posts on your behalf, never `blocked` and never via MCP. Not triggered this run — no write was refused.\",\n    \"L366 — origin/main ADDS a `session` field to the report JSON that the loaded copy omits entirely: \\\"this run's harness-stamped id (transcript Claude-Session: line; subagent = parent's)\\\". Included above.\",\n    \"L370 — loaded copy defines `mcp_calls` as a bare count; origin/main WINS and is stricter: MCP GitHub calls WITH TOOL NAMES, and a write tool appearing in that list means the report is refused. Reported in that form above.\",\n    \"L380 — cosmetic reflow only: origin/main states the `premise_still_valid: false` rule in one line where the loaded copy splits it across two. No behavioural difference; origin/main's wording followed.\"\n  ],\n  \"open_questions\": [\n    {\n      \"question\": \"What is `check-reference-carrier-shape`'s declared scope for a holder that is a JSON-Schema `properties` map — in scope and needing a rule, or out of scope and needing the refusal text to say so and to offer a spellable remedy? Today it is neither: no rule fires, the refusal prescribes three remedies of which none is spellable for this shape, and the only working escape (name the value) is undocumented and silently removes the site from judgement.\",\n      \"options\": [\n        \"A. Narrow N4 — a holder sitting under a `properties:` key is provably not a field-def carrier. Cost: unfalsifiable on today's tree (measured: it changes 0 verdicts; 1 of 681 sites is affected and is already immaterial), and it imports JSON Schema's vocabulary into a gate whose every other rule reads its authority from packages/spec at run time, against the script's own warning about key-name-only judgement.\",\n        \"B. Structural recogniser — same as A but additionally requiring the enclosing object to declare `type: 'object'`, so it recognises a JSON Schema rather than the word `properties`. Cost: more code and still unfalsifiable on today's tree; benefit: it cannot be tripped by a non-JSON-Schema object that happens to have a `properties` key.\",\n        \"C. Keep the refusal, fix the PRESCRIPTION — add a documented FOURTH remedy ('bind the sub-schema to a name; the predicate judges literals only') to the refusal text and to the 'What it does NOT claim' section. Cost: the site stays unjudged, which is honest but is a permanent hole of exactly one shape; benefit: zero new judging surface, and it legitimises what an author already had to invent unaided.\",\n        \"D. Do nothing beyond recording the reading. Cost: the next author to meet the shape rediscovers it from a refusal whose three remedies all fail, and 48 N2-blind `properties` maps stay live tripwires on a REQUIRED, repo-wide job.\"\n      ],\n      \"recommendation\": \"C, then reconsider B only if the shape recurs. C is the only option whose effect is measurable today: it changes the text an author actually reads at the moment they are stuck, which is where the whole cost of this defect currently lands. A and B are both unfalsifiable against the tree (1 affected site, already immaterial), and landing an unexercisable rule into a gate that deliberately carries no baseline is the shape its own docblock warns against. D leaves a required, repo-wide job with a documented trap and no prescription.\"\n    },\n    {\n      \"question\": \"Should the literal-only predicate ever be widened to resolve a named value (half B's second claim)?\",\n      \"options\": [\n        \"A. No — it is a declared non-claim with a measured population (511) and a named follow-up route (a runtime hook, a different card).\",\n        \"B. Yes — resolve module-local const initialisers.\"\n      ],\n      \"recommendation\": \"A, and I regard this one as already settled rather than open. B is measured to be unbounded: packages/cli/test/data-model-rules.test.ts:803-807 deliberately hides a non-string carrier behind a name because the assertion under test is that such a value resolves to NOTHING, and its comment ends 'do not weaken the gate or add a path ignore there'. #13103 had already measured and refused the broad variant at 838 fixtures across 303 files. Widening would red a fixture whose own text forbids the change.\"\n    },\n    {\n      \"question\": \"Process: my dispatch words did not carry the four-axis decision framework that pm-dispatch SKILL.md holds the single copy of, and my instruction file forbids me from substituting axes of my own invention.\",\n      \"options\": [\n        \"A. Accept the options/cost/recommendation analysis above as-is.\",\n        \"B. Send me the four axes and I will redo the analysis in that form.\"\n      ],\n      \"recommendation\": \"A, unless the seat wants the decision card to carry the formal escalation shape — in which case B, and it is a cheap re-run because every measurement it would need is already in `tests` above.\"\n    }\n  ],\n  \"decision_card_material\": {\n    \"title_suggestion\": \"check-reference-carrier-shape: declare the scope for a JSON-Schema `properties` holder, and give its refusal a spellable remedy\",\n    \"dedupe_words\": [\"check-reference-carrier-shape\", \"properties map holder rule\", \"N2-blind authorable key set\", \"refusal remedy unspellable\", \"LOOKUP_TARGET_COLUMN\", \"exit 3 rules none\"],\n    \"note\": \"dev does not POST /issues; filed here for the seat. Every number it needs is in this report's `tests` field.\"\n  },\n  \"out_of_scope_findings\": [\n    \"noted, not filed: the refusal is whole-gate (exit 3 stops the entire run) rather than per-site, on a gate that lint.yml runs unconditionally in the REQUIRED `Lint & Repo Gates` job — so a single unresolved non-string-literal anywhere in 6793 files reds every open PR at once. This is not a separate defect, it is the cost term of the decision above, and it belongs in that decision card rather than in a card of its own. Who will hit this file next: whoever adds a column to a builtin node designer form (19 `properties:` maps across 9 files in service-automation alone), which is the population that mirrors Field's column set by construction.\",\n    \"noted, not filed: `check-reference-carrier-shape.mjs` is invoked by PATH from lint.yml and is deliberately absent from packages/lint/package.json's scripts. Not a finding — scripts/check-self-test-wired.mjs and scripts/pm/dispatch-gates.mjs both encode that by-path form explicitly and assert on it. Recorded only so the next reader does not re-derive it as a wiring gap. Successor: none; two gates already own this fact.\"\n  ]\n}\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5696500167,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-16T11:10:42Z","updated_at":"2026-09-16T11:10:42Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18095#issuecomment-5696500167","body":"# ⚖️ 转入决策箱 —— 读数已交付,并且**证伪了本卡自己的前提**\n\nPM 席位 `session_01KB5PFtxuy1x3dcR5gxudx6`,2026-09-16T11:0xZ。`pm:dispatched` → `needs-user-decision`(一笔写入,回读匹配);assignee 已清;⛔ 无 PR —— 这一轮**没有产生任何 diff**,这是按派发令预设的三种合格结局之一,⛔ 不计返工。\n\n完整读数在 `os-dev-report` 评论 `5696425487`。\n\n## 维护者速读\n\n我们有一道门禁负责检查「引用字段写对了没有」。本卡原本报告说它对某一种写法**坏掉了**。实测:**它没坏,今天是绿的** —— 那个具体地点早就被人绕开了(把值取了个名字,门禁就不看了)。\n\n但把那个绕法还原回去一测,报告描述的故障**一字不差地重现**:门禁拒绝,并给出三条补救建议 —— 而这三条对这种写法**一条也写不出来**。作者唯一能用的办法(给值取个名字),文档里根本没写,而且用了以后这个地点就**永远不再被检查**。\n\n真正要紧的是代价形状:这道门禁一旦拒绝,**是整道门停摆,不是报一个点**,而它跑在每个 PR 都必过的必需任务里。全仓有 **48 处**同样写法的地方还空着,任何一个人往里面写一行,**全仓所有开着的 PR 会同时变红**,直到有人去拆。\n\n要您裁的是:这种写法到底算不算这道门禁该管的?\n\n**A 加一条窄规则排除它 / B 加一条结构化规则排除它(更严谨) / C 门禁照旧拒绝,但把「取名字」写进它的补救说明 / D 只记录不动**\n\n## 一句话问题\n\n一个作者按我们自己文档里的三条建议去修,**三条都写不出来**,而唯一能修好的办法我们从没告诉过他 —— 用了之后这个地方就再也没人检查了。\n\n## 选项 × 真实代价表\n\n| 选项 | 做什么 | 客户/作者可感知的后果 |\n|---|---|---|\n| **A** | 加窄规则:holder 落在 `properties:` 键下 ⇒ 不是字段定义载体 | 作者不再撞墙。**实测代价**:今天全树 681 个 `reference` 地点里只有 **1 个**落在 `properties` 图里,而且它已经免疫 ⇒ 这条规则**改变 0 个判定**,只能被它自己的自测执行 |\n| **B** | 同 A,但额外要求外层声明 `type: 'object'`,即认的是「这是一份 JSON Schema」而不是「这个词叫 properties」 | 同 A,且不会被一个碰巧有 `properties` 键的普通对象误伤。代价:代码更多,今天同样无法被真实树证伪 |\n| **C** | 门禁照旧拒绝,但补救说明里加**第四条**:「把子结构绑到一个名字上;这个谓词只判字面量」,并写进「本门禁不主张什么」一节 | 该地点仍然不受检查 —— 诚实,但是一个永久的洞。好处:**零新增判定面**,并且把作者本来就只能自己瞎猜出来的办法正式化 |\n| **D** | 只记录读数,不动 | 下一个撞上的人,面对一条给了三个都失败的建议的拒绝,自己重新摸索一遍;48 处空位继续当地雷躺在**必需的、全仓级**任务上 |\n\n## 业务含义直译\n\n- **A / B** = 在验收清单上加一条「这类图纸不归我们审」。B 是看图纸的**结构**判断,A 是看图纸**标题上的那个词**。\n- **C** = 清单不改,但把「遇到这种图纸该怎么办」写进说明书 —— 今天说明书上写的三种办法,对这类图纸全都不适用。\n- **D** = 什么都不写,让下一个人再摔一次。\n\n## 四轴(从业务立场)\n\n- **① 长远合理性** —— A/B 往门禁里加一条**今天打不着任何东西**的规则,这是特例增生:每加一条都是永久维护义务,而它唯一的验证者是它自己的自测。C 不新增任何判定面,只补文档 ⇒ ① 指向 **C**。\n- **② 实际业务拉动** —— 今天**零拉动**:门禁是绿的,唯一的地点已免疫。但撞上时的代价不是个人的:整道必需门停摆 = 全仓 PR 同时变红。⇒ ② 指向 C(零拉动默认不扩面),但把「复发一次就重议」钉死。\n- **③ 防 AI 犯错** —— ⚠️ **这一轴指向 B,和 ① 不同向,我如实报告而不是抹平**。今天的形状是最差的:先**响亮**地拒绝(整道门红),而唯一的出路是给值取名字,取完之后该地点**静默**退出检查。C 把这个静默出口**正式化**了。B 则让「不判」出于一个写明的结构理由,而不是出于作者把值藏起来。\n- **④ 创业阶段不扩散** —— remove 优于 declare-and-maintain。A/B 是 declare-and-maintain 一条不可证伪的规则 ⇒ ④ 指向 **C**。\n\n## 防 AI 犯错轴:出错时谁看到什么\n\n作者写下一行内联的 `reference:` 子结构 ⇒ **整道门禁 exit 3**,必需任务 `Lint & Repo Gates` 红,**全仓每个开着的 PR 一起红**。他读到的拒绝文本给三条建议,实测**三条都不适用**。他若自己摸索出「给值取个名字」,门就绿了 —— 而这个地点从此**静默**不再被判定,没有任何东西告诉他这一点。\n\n## 推荐 + 回退 + 置信缺口\n\n**推荐:C。** 按纪律,推荐只由 ① 得出:C 是唯一今天就能被度量的选项 —— 它改的是作者卡住那一刻真正读到的那段文字,而这正是这个缺陷今天全部的代价落点。A 与 B 对着真实树都不可证伪(1 个受影响地点,且已免疫),而往一道**刻意不带基线**的门禁里塞一条无法被执行的规则,正是它自己的 docblock 警告过的形状。\n\n**回退:B**,预先武装 —— 一旦出现**第二个** `reference` 地点落进 `properties` 图,或 48 处空位里任何一处真的触发过一次全仓红,就改判 B,⛔ 不再重开一轮讨论。\n\n**置信缺口,明说:** 我看不见那 48 处的**增长速率**。它们是设计器表单,按构造镜像 Field 的列集,所以它们恰恰是最可能长出 `reference` 键的那一批 —— 但没有任何读数能告诉我这会是下个月还是永远不会。如果您对「全仓同时变红」这个事故的权重高于特例增生的代价,**B 是今天就该选的字母**;我按纪律不替您翻这个字。\n\n**自检行:只看①选 C;②③④ 是否翻转:否 —— ③ 偏向 B,记为不同向并列在上面,但按「②③④ 只调时序分期 ⛔ 不翻字母」,它落成 B 的武装回退而不是改推荐。**\n\n## 裁后执行段\n\n- 裁 **C** ⇒ 本卡转 `pm:queue` 原样派发:改 `check-reference-carrier-shape.mjs` 的拒绝文本与「What it does NOT claim」一节,加第四条补救;自测补一条钉住该文本的用例。⛔ 不动任何判定逻辑。\n- 裁 **A/B** ⇒ 本卡转 `pm:queue`,派发令写明:新规则必须配**反事实自测**(把 specimen 的列内联回去 ⇒ 仍须 decline;一个真正的 `fields:` 载体 ⇒ 仍须响),因为真实树给不出证伪样本。\n- 裁 **D** ⇒ 关 not planned,理由与本读数落关单评论。\n\n## 相关\n本卡是 #18061 的半 B(半 A 由 PR #18094 落地)· 分诊评论 5657095122 · #13103(已测并否决过宽版本:838 fixtures / 303 files)· `packages/cli/test/data-model-rules.test.ts:803-807`(树内明文禁止放宽该谓词)\n\n<!-- os-decision-facets -->\nos-decision-facets\n① 项目长远合理性:C 不新增判定面只补文档;A/B 往门禁加一条今天改变 0 个判定、只能被自测执行的规则,是特例增生与永久维护义务。\n② 实际业务拉动:今天零拉动(门禁绿,唯一地点已免疫);撞上时代价是整道必需门停摆、全仓 PR 同时变红,48 处同形空位为潜在触发点。\n③ 防 AI 犯错:⚠️ 本轴与①不同向,指向 B —— 现状是「响亮拒绝 + 三条都写不出的建议 + 静默的唯一出口」,C 把该静默出口正式化,B 让不判出于写明的结构理由。\n④ 创业阶段不扩散:remove 优于 declare-and-maintain;A/B 是维护一条对真实树不可证伪的规则,C 不增加任何已声明键。\n\nPrior rulings read: reference-carrier-shape,properties,holder,gate,scope,literal-only,json-schema → 79 hits; ADR-0068 D1, ADR-0068 D4, ADR-0124 D3, ADR-0029 D4, ADR-0057 D1, ADR-0057 D7, ADR-0057 D8, ADR-0058 D1\n\n⚠️ 上面 8 条**逐条读过标题,没有一条管本卡**,均为宽词碰撞:ADR-0068 D1/D4 是用户上下文与 v1 gating;ADR-0124 D3 是服务端权威;ADR-0029 D4 是 `sys` 保留;ADR-0057 D1/D7/D8 是 ERP 授权 scope-depth / honesty pass / conformance 矩阵;ADR-0058 D1 是 CEL pushdown 编译器。⇒ 按 SKILL.md 〈升级与决策〉③ 判定:**本卡不是执行,是真决策。**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
