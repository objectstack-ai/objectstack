{"id":5798602595,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T16:28:01Z","updated_at":"2026-09-23T16:28:01Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19901#issuecomment-5798602595","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**3** anchor(s) derived from **1** changed package(s); no hand-written page names any of them, so this run has **nothing to list** — **not a clean bill of health**. This check sees only pages that NAME a derived anchor: one that documents this change in prose, or enumerates it in an authoring dialect, names none and stays invisible to it on every run.\n\n<details><summary>What this run could not see</summary>\n\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **16** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 029d8a47109c63986a27f53581ec6c980b8e2f14` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `a0931ee2dc5b77139b0ede7dec2318297b87e4f2` — the merge of head `67015feb5f1107858566df986bb8eaeb7b3420fe` into base `029d8a47109c63986a27f53581ec6c980b8e2f14`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin a0931ee2dc5b77139b0ede7dec2318297b87e4f2 && git checkout a0931ee2dc5b77139b0ede7dec2318297b87e4f2\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 029d8a47109c63986a27f53581ec6c980b8e2f14 67015feb5f1107858566df986bb8eaeb7b3420fe && git checkout -B drift-repro 029d8a47109c63986a27f53581ec6c980b8e2f14 && git merge --no-ff 67015feb5f1107858566df986bb8eaeb7b3420fe\n\nnode scripts/docs-audit/affected-docs.mjs --json 029d8a47109c63986a27f53581ec6c980b8e2f14\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5798812881,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T16:42:24Z","updated_at":"2026-09-23T16:42:24Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19901#issuecomment-5798812881","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `67015feb5f1107858566df986bb8eaeb7b3420fe`\n\nHead = PR head; merge-base `dabf8d795e`; one commit, 5 files, +159/−18. `@objectstack/metadata` was built with tsup at the merge-base and at the head in throwaway worktrees (removed afterwards), and both dists were probed read-only. No derived gate was re-run.\n\n### ① Derived judgments\n\n- (a) \"already honoured by the `json` and `yaml` formats\": TRUE. `YAMLSerializer` passes `sortKeys` to js-yaml, which sorts every mapping at every depth. The two sorts differ only for integer-like keys (js-yaml compares strings; a rebuilt JS object enumerates integer keys first). The changeset claims identity with `JSONSerializer`'s sort, not yaml's, and the typescript format wraps a JSON body, so JSON is the right reference.\n- (b) Deep, array order kept: TRUE. `sortObjectKeys` is `JSONSerializer`'s previous private body moved verbatim; measured at head across three nesting levels and a mixed array.\n- (c) `sortKeys` absent or false is byte-identical: TRUE. Base dist vs head dist, `json` / `yaml` / `typescript` / `javascript` × five option sets: 20/20 identical; through `NodeMetadataManager.save()` → `FilesystemLoader.save()`: 8/8 identical.\n- (d) Both paths honour it: TRUE. `renderModule()` is the single reader, called by `serialize()` and `serializeTypeScriptForMetadataType()`; measured for `typescript` and `javascript` through both. On the base dist the `javascript` loader call ignored it, which reproduces the card.\n- (e) Unreachable, declarations unchanged: CONFIRMED. No entry re-exports `sort-object-keys.ts`. Over 10 emitted declaration files, only `index.d.ts` / `index.d.cts` differ, by one comment hunk above `private sortObjectKeys;` in `JSONSerializer`; no declaration line changed.\n- (f) Round trip unchanged: TRUE; the base dist's `deserialize` also reads the head's sorted files.\n- Every changeset sentence, docblock, comment, PR-body section and test name is true at the head. Checks: 34 runs, 31 `success`, 3 roster skips, 0 failure.\n\n### ② Semver level\n\n`@objectstack/metadata: patch`, `Clause-②: no`, confirmed. It honours an option `MetadataSaveOptionsSchema` and `SerializeOptions` already declare and accept; no accept set widens, no interface changes, no export is added, and the emitted declarations change by one comment.\n\n### ③ Boundary flags\n\n1. yaml and json/typescript key order differ only for integer-like keys: pre-existing, not claimed otherwise. ACCEPTABLE.\n2. The one-comment delta also appears in `index.d.cts`: same entry, no declaration change. ACCEPTABLE.\n3. The `#19872` tracker number lands in a published declaration comment: comments may carry tracker numbers; `check:doc-authoring` is green. ACCEPTABLE.\n4. `packages/metadata/README.md` credits \"optional key sorting\" to `JSONSerializer` alone: an understatement, not false. ACCEPTABLE.\n5. `javascript` needs a `formats` opt-in on the loader path; the PR pins it through `serialize()`, as claimed, and the loader path was measured. ACCEPTABLE.\n6. No pin covers array element order: the sort is moved verbatim and the behaviour was measured. ACCEPTABLE.\n\nImplemented-by: `claude/issue-19872-ts-serializer-sortkeys`\nReviewed-by: `session_01TEhopqrWQYBycZzyJHpAZr`\n\n**VERDICT: PASS**\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
