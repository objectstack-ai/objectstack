{"id":5825025087,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-25T01:15:13Z","updated_at":"2026-09-25T01:54:53Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/20065#issuecomment-5825025087","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**5** anchor(s) derived from **1** changed package(s); no hand-written page names any of them. ⚠️ **1** changed file(s) yielded no anchor (`packages/sdui-parser/objectui-lockstep.json`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n<details><summary>What this run could not see</summary>\n\n- **1** changed file(s) yielded no anchor (`packages/sdui-parser/objectui-lockstep.json`) — pages documenting those are invisible to this run\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **2** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 66960564d9b628c446be3c8f8a8f0217c367a17f` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `16949f8349c03a32d52dbcd81bacf4fe5cd83127` — the merge of head `1fb16a27f61f562b54658045c87590574ecc4b12` into base `66960564d9b628c446be3c8f8a8f0217c367a17f`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin 16949f8349c03a32d52dbcd81bacf4fe5cd83127 && git checkout 16949f8349c03a32d52dbcd81bacf4fe5cd83127\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 66960564d9b628c446be3c8f8a8f0217c367a17f 1fb16a27f61f562b54658045c87590574ecc4b12 && git checkout -B drift-repro 66960564d9b628c446be3c8f8a8f0217c367a17f && git merge --no-ff 1fb16a27f61f562b54658045c87590574ecc4b12\n\nnode scripts/docs-audit/affected-docs.mjs --json 66960564d9b628c446be3c8f8a8f0217c367a17f\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5825375506,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T01:52:00Z","updated_at":"2026-09-25T01:52:00Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/20065#issuecomment-5825375506","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `bfb082b0b817f78917639c7ebffb2c2b3feb746b`\n\n### ① Derived judgments\n\n1. **TRUE. The port matches objectui.** `packages/sdui-parser/src/validate.ts:33` (`CHILD_LIST_KEY`), `:57-59` (`acceptsChildren`, a name-only match) and `:216` (the branch) are token-identical to objectui `validate.ts` at `f8a9d0fb0596`, lines `33`, `57-59` and `220`.\n   - The PR's own extractor, run on both files, reduces each to blob `76c18fb95d1f`.\n   - No `isContainer` read, no fallback.\n   - An unknown component draws `unknown-component` and never reaches the containment branch, on both sides.\n   - `5ea623ea` is an ancestor of the pin.\n2. **TRUE. The lockstep pin is real.** `scripts/check-sdui-lockstep.mjs:346-433` extracts the single `not-a-container` branch plus the module-level declarations it reaches; it refuses on 0 or 2 branches. `judge` emits `containment-drift` or `containment-unreachable`.\n   - Run in a scratch worktree: the gate exits 0 and `--self-test` exits 0 (a 10-case battery, floor 7).\n   - Ablation: `:216` reverted to `!comp.isContainer` gives exit 1 with `[containment-drift]`. Reverted afterwards.\n3. **TRUE. Five verdicts flip, both ways.** In the head manifest (59 components):\n   - `badge`, `alert`, `button` declare a `children` slot with the flag off, so they are no longer warned;\n   - `page:tabs`, `page:accordion` have the flag on and no `children` input, so they are now warned;\n   - no other component disagrees.\n4. **TRUE. No new public export.** `index.ts` is untouched and still exports only `validateTree` from `validate.ts`. The `types.ts` changes are two JSDoc blocks.\n5. **TRUE. The lint change is test data.** Only `packages/lint/src/validate-jsx-pages.test.ts` changes (+4/-1): the `flex` fixture gains the `children` slot.\n6. **TRUE, with one gap.** The changeset prose is accurate: warning severity, no `isContainer` read, five components changed, `page:tabs` and `page:accordion` render `item.children` only (objectui `containers.tsx:891`, `:1079`). The gap: its last sentence names only `os validate --strict`. `validateJsxPages` also runs under `os lint`, and `os lint --strict` promotes warnings too (`packages/cli/src/commands/lint.ts:991-995`). `os build` / `compile` has no warning promotion.\n\n### ② Semver level\n\n`not-a-container` is `severity: 'warning'` (`validate.ts:218`, unchanged). `os validate` and `os lint` fail on a warning only under `--strict` (`validate.ts:716-740`, `lint.ts:983-995`). So the default save gate's accept and refuse sets do not move. Only the opt-in `--strict` face moves, in both directions.\n\n**Judgment: `patch` + `Clause-②: no` is correct.** The repo's Clause-② criterion is directional: relaxing the accept set or enlarging the public surface (`execution-duties.md:65`, `scripts/pm/check-widening-tells.mjs:23-27`). This diff:\n- adds no key, no enum member, no reachable export and no registration;\n- removes nothing an author can write (`children` stays a base prop).\n\nIt pulls the save gate back to the renderer's declared contract. The `--strict` movement changes when an advisory fires, correcting a false-positive / false-negative pair. The newly refused shape was already inert at render. A `(narrowing)` arm is reserved for removing or renaming authorable surface under ADR-0087, and nothing here fits it. `check-widening-tells --declaration no` exits 0.\n\n### ③ Boundary flags\n\n- **Governed surfaces:** none of the 9 paths. The `.changeset` prose is reviewed above.\n- **Lint fixture:** the minimal test-data update the predicate move forces. No `packages/lint` production source changes, and no baseline row is affected.\n- **Check-runs on the head:** 41 — 36 success, 5 skipped, 0 failed.\n- **Out of scope, card-worthy, not blocking:** objectui's `index.ts` also exports `acceptsChildren` / `CHILD_LIST_KEY`, while this copy deliberately does not. objectui's `validate.ts:150` also feeds `acceptsChildren` into a `body-dialect` check this tree does not carry.\n\nImplemented-by: `claude/issue-19969-sdui-containment-slot`\nReviewed-by: `session_01VDtqoecgES7ScQYGbFVDRv`\n\n**VERDICT: PASS**\n\nLanding condition: amend the changeset's final paragraph so it names `os lint --strict` beside `os validate --strict` (`os build` does not promote). Keep `patch` and `Clause-②: no`. The re-issue on the new head covers exactly that one-sentence delta.\n\nIsolated at-tier reviewer, adopted by the `domain:devx#1` seat.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5825658808,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-25T02:25:24Z","updated_at":"2026-09-25T02:25:24Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/20065#issuecomment-5825658808","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `1fb16a27f61f562b54658045c87590574ecc4b12`\n\n### ① Derived judgments\n\n1. **TRUE.** `git diff bfb082b0b 1fb16a27f` touches exactly one file, `.changeset/19969-sdui-containment-declared-children.md` (+1/-1), in one commit. The replaced sentence now names both `os validate --strict` and `os lint --strict`, and says both run `validateJsxPages`. Verified accurate:\n   - `os validate` fails on a warning only under `flags.strict` (`packages/cli/src/commands/validate.ts:716-740`).\n   - `os lint` does the same only under `--strict` (`lint.ts:991-995`).\n   - `validateJsxPages` runs in both (`packages/lint/src/authoring-rules.ts:1032-1041`).\n   - `compile.ts` carries no warning promotion.\n\n   `patch` and `Clause-②: no` are unchanged. Judgments 1-6 of the record on `bfb082b0b` carry over, because no other byte moved.\n\n### ② Semver level\n\nUnchanged from the record on `bfb082b0b`. `not-a-container` stays `severity: 'warning'`, and the default gate's accept and refuse sets do not move. The opt-in `--strict` movement is a corrected advisory that pulls the save gate back to the renderer's declared contract. `patch` + `Clause-②: no` is correct.\n\n### ③ Boundary flags\n\n- **Governed surfaces:** none; the delta is a `.changeset` file only.\n- **Check-runs on `1fb16a27f`:** 34 — 31 success, 3 skipped, 0 failed, 0 in progress.\n\nImplemented-by: `claude/issue-19969-sdui-containment-slot`\nReviewed-by: `session_01VDtqoecgES7ScQYGbFVDRv`\n\n**VERDICT: PASS**\n\nNo landing conditions remain: this delta satisfies the one condition from the prior record.\n\nIsolated at-tier reviewer, adopted by the `domain:devx#1` seat.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
