{"id":5748565165,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-20T08:05:02Z","updated_at":"2026-09-20T08:53:16Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19265#issuecomment-5748565165","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n⚠️ **3** changed file(s) yielded no anchor (`packages/spec/liveness/README.md`, `packages/spec/liveness/field.json`, `packages/spec/liveness/state-counts.md`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files. Nothing else in this diff resolved to a documentable surface (no symbol, route or SDK anchor derived from **1** changed package(s)).\n\n<details><summary>What this run could not see</summary>\n\n- **3** changed file(s) yielded no anchor (`packages/spec/liveness/README.md`, `packages/spec/liveness/field.json`, `packages/spec/liveness/state-counts.md`) — pages documenting those are invisible to this run\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **136** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 1739f71879f27d10c07fbde77fcf0728ed9abb81` → `packageMentionDocs`.\n</details>"}
{"id":5748698060,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-20T08:33:36Z","updated_at":"2026-09-20T08:33:36Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19265#issuecomment-5748698060","body":"## Contract review\n\n```\n7f9df934bba1efdd90398faeb7fefc8871cb8c52\n```\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `7f9df934bba1efdd90398faeb7fefc8871cb8c52`\n\nIndependent contract review of PR #19265 delivering card #19187. Readings taken in THIS act, 2026-09-20T08:33Z, against `origin/main` `7d0f911da9ab9af94b72c0dfcfed16f676879bd5` (the PR's merge-base, measured) and a fresh worktree at the head above. Seat comments on the card and the PR were read and adopted for nothing; every number below was re-taken here, and where a seat had stated a measurement I re-took it and say so.\n\n### ① Does the diff do what the card asked, and only that?\n\n**Flip condition — verified first-hand, not relayed.**\n\n- `.objectui-sha` at `origin/main` reads `53ded82bf7a494f54e344e19099dbf00854b8694` (read from the pinned worktree).\n- objectui#4664 is a closed issue (state_reason `completed`, closed 2026-08-31) whose closing PR is objectui #6946 (merged); its merge commit is `d796c8dde54dcd572ce6abb07c137ba3333b9084` — 「feat(detail): derived related lists consume the FK's relatedListFilter — AND-composed query, badge counts the same set」. `packages/console/CHANGELOG.md` line 474 at `origin/main` records that commit shipping objectui#4664.\n- **Shallow-clone check.** The session's objectui checkout at `/home/user/objectui` IS shallow: `git rev-parse --is-shallow-repository` prints `true`, 50 commits, `.git/shallow` present. I did not use it for ancestry. I made a fresh blobless clone (`git clone --filter=blob:none --no-checkout`) of objectstack-ai/objectui in the scratchpad: `is-shallow-repository` prints `false`, 10488 commits reachable from objectui `origin/main`. In that clone `git merge-base --is-ancestor d796c8dde 53ded82bf7` exits **0**. Lit controls on the same clone: objectui `origin/main` (`c2f0f48329`) `--is-ancestor 53ded82bf7` exits **1** (the instrument can say no), and `53ded82bf7 --is-ancestor origin/main` exits **0**; `git rev-list --ancestry-path --count d796c8dde..53ded82bf7` prints **293**.\n- **Consumer at the pin**, read with `git show 53ded82bf7:path` in that clone. `packages/app-shell/src/utils/deriveRelatedLists.ts`: `function isDeclaredFilter` at line 176, `export function deriveRelatedLists` at line 212, `...(isDeclaredFilter(fieldDef.relatedListFilter) ? { filter: fieldDef.relatedListFilter } : {})` at lines 276-277. Control: the same file at objectui `origin/main` carries that read at 301-302, so the card's relayed 301-302 was a main-tree reading and the PR's pin-tree correction is right. `packages/app-shell/src/views/RecordDetailView.tsx` at the pin: `const synthParts = useMemo` at line 1867, `filter: declaredFilter` destructured at line 2041, `...(declaredFilter ? { filter: declaredFilter } : {})` at line 2079. The badge half is also there at the pin: `packages/components/src/renderers/layout/containers.tsx` line 541 reads the node's `filter` for the count probe and `packages/components/src/hooks/related-count-store.ts` composes it through `mergeFilterNodes` (import at line 30). ⇒ the row's own flip condition is met at the pin this repo builds against.\n\n**Scope of the diff** — 5 files, +150 / −22, measured with `git diff --stat origin/main...HEAD`:\n\n- `packages/spec/liveness/field.json` — the flip itself. Row keys at this head: `status, verifiedAt, evidenceScope, evidence, note`; `authorWarn` and `authorHint` gone. Shape matches its three `relatedList*` siblings (all `live`, `evidenceScope: cross-repo`, no `producer`, no `authorWarn`) — read from the four rows at this head. `git check-attr merge` reads `unspecified` for this path.\n- `packages/spec/liveness/state-counts.md` — regeneration, `merge: os-regen` (see ③).\n- `packages/lint/src/lint-liveness-properties.test.ts` — the two pins that used this row as their witness, re-dispositioned, plus two comment corrections that name the key. A consequence, not a widening: the removed `REAL LEDGER: field.relatedListFilter (planned)` case cannot pass once the row is live.\n- `.changeset/19187-related-list-filter-liveness-flip.md` — `@objectstack/spec: patch`. The ledger ships in the tarball: `packages/spec/package.json` `files[]` includes `liveness` (read here).\n- `packages/spec/liveness/README.md` — one hand-written append to the `field` Notes cell. Recording a flip there is the ledger's own convention (the `app` row records its flips; `readme-table.mts` lines 589-590 instruct a re-read of the Notes cell when a verdict changes), so the append is not widening — **but its content carries a false sentence: F1.**\n\nNothing else is touched: no `packages/spec/src/**`, no objectui code, no other ledger row.\n\n**F1 — a false sentence written into the ledger README (must change).** The appended cell says the flip is 「this type's first flip of that direction」. Measured on `origin/main` history: `git log -p -- packages/spec/liveness/field.json` shows commit `fa125f3bfe` (#15316, 「enforce Field.valueDomain on the write path … flip the liveness row」) changing `valueDomain` in this same file from `\"status\": \"planned\"` to `\"status\": \"live\"`; at `origin/main` that row is `live` with `verifiedAt` 2026-09-04. Lit control on the same walk over all ledgers: five commits removed a `\"status\": \"planned\"` line — `ecd2158734`, `fa125f3bfe`, `6acb11a0ee`, `8d0bb797f4`, `a831df1bea` — so the search does find flips. ⇒ `relatedListFilter` is not this type's first `planned` → `live` flip. The card's governing line is that a wrong sentence in AI-facing docs is a product defect; this diff adds one to the file whose job is to say true things about the ledger. Scope of the fix: delete or correct that one clause in the `field` row of the README Current-state table (line 905 at this head). The rest of the cell that I checked holds: four `relatedList*` members (measured at this head), and the #10068 / #11442 precedent flips exist in history (`8d0bb797f4`, `6acb11a0ee`).\n\n### ② Is `Clause-②: no` a true reading of this diff?\n\nJudged from the diff against the gate's own definition (`scripts/check-changeset-no-major.mjs` header: clause ② is 「a new key on a published payload」; `check-widening-tells.mjs` tells: a schema key, a closed-set member, a published export, a registry entry):\n\n- No `.zod.ts`, no `packages/spec/src/**`, no export, no registry entry, no closed set moves. `FieldSchema.relatedListFilter`'s accept set is byte-identical — the diff does not touch it.\n- What does move: one ledger row's keys (gains `verifiedAt` / `evidenceScope` / `evidence`, loses `authorWarn` / `authorHint`) and a generated count table — ledger data inside the tarball, not a key on a payload an author writes or a consumer receives. The repo's own precedent grades ledger-only changes `no` (`.changeset/17385-chartconfig-liveness-drill.md`, `18582-connector-analytics-cube-liveness-ledgers.md`, `18614-…`, `18739-…` at `origin/main`, read here).\n- The one observable behaviour change is that `packages/lint`'s advisory stops emitting one warning. That lint 「NEVER fails the build」 (module header, line 17); an advisory narrowing is not an accept-set change in either direction.\n\n⇒ `Clause-②: no` is a true reading and `patch` is the right level. On this head `node scripts/check-adr-0087-registration.mjs --base origin/main` exits **0** (no declared-breaking changeset) and `node scripts/check-changeset-no-major.mjs --base origin/main` exits **0** (level axis not applicable offline; CI's `Check Changeset`, which has the PR payload, reads `success`). I found no ground to overturn the declaration in either direction. For the record: `node scripts/pm/check-clause2-carriers.mjs --pair 19265`, run from `origin/main`, exits **4** on C6 alone — no `## Contract review` record on this head yet; it reads the pair's declaration as `no` through the correction comment.\n\n### ③ Is the evidence sound, and is anything unmeasured?\n\n**The evidence pointer** is real at the cited pin and locations (① above): both symbols exist, both cited line numbers are the lines that carry the read, and the pin cited is the pin `.objectui-sha` names. The pointer's prose beyond the two cited files (badge parity through the tab strip's count probe) also holds at the pin. Note `check:liveness` resolves only repo-local paths and merely counts foreign ones (`check-liveness.mts` lines 862-864), so the cross-repo reading is this review's, not the gate's.\n\n**The regenerated `state-counts.md`** reproduces byte-identical: `pnpm --filter @objectstack/spec gen:liveness-counts` exits **0** and the file's blob before / after / at HEAD reads `7c5f099e68` in all three; `git status --porcelain` empty afterwards. `field` 91 / 0 / 0 / 1 / 1 / 93, total 915 / 5 / 1 / 160 / 11 / 1092.\n\n**Gates — real exit codes**, run in the review worktree at this head after `pnpm install --frozen-lockfile` (exit 0) and `pnpm --filter @objectstack/spec build` (exit 0):\n\n| command | exit |\n|---|---|\n| `pnpm --filter @objectstack/spec check:liveness` | **0** — its own pass line, and 「state-counts.md is current」 |\n| `pnpm --filter @objectstack/spec check:generated` | **0** — 「All 16 generated artifacts are up to date」 |\n| `pnpm --filter @objectstack/lint test` | **0** — 105 files, 3976 passed, 5 skipped |\n| `pnpm --filter @objectstack/lint typecheck` | **0** |\n| `node scripts/check-adr-0087-registration.mjs --base origin/main` | **0** |\n| `node scripts/check-changeset-no-major.mjs --base origin/main` | **0** |\n\n**The re-dispositioned pins — can they still fail?** Direct measurement first: at this head `authorWarnedProperties('field')` returns `[]` (object → `externalSharingModel`, translation → `flows`), and `field.json` carries 0 `authorWarn` and 0 `experimental` rows (control: `object.json` carries 1). So the PR's premise — `loadWarnMap(dir, 'field')` empty, the field walk gated off by the `fieldWarn.size` greater-than-zero guard at line 493 — is true. Then three ablations on `src/lint-liveness-properties.test.ts` (baseline 71 passed), each restored with `git checkout HEAD -- path` and proven by blob identity with `git status --porcelain` empty:\n\n- **A** — the ledger row reverted to the `origin/main` blob (`a9642c5687`, planned + authorWarn): **4 failed / 67 passed**, by name 「no longer warns on relatedListFilter …」, 「stays quiet however many fields author the filter」, 「the related-list silence is a real verdict …」 and the narrowed #11385 「field walk: a malformed fields array …」 case. ⇒ none of the four is vacuous; a half-reverted flip is caught.\n- **B** — the `packages/spec/liveness` directory moved away (the failure the guard names, 「cannot resolve the shipped ledgers」): **17 failed / 54 passed**, including the guard and the narrowed field-walk case. ⇒ the guard fires for the failure it names.\n- **C** — `field.json` alone removed, `object.json` intact: **71 passed, exit 0**. Nothing in the file goes red. Cause, read in the module: `loadWarnMap` returns an empty map silently when the type's file is missing (line 77) or unparseable (lines 81-83), and the guard's second warning comes from `object.json`, a separate load (lines 485-486).\n\n⇒ **F2 — the guard's stated coverage overstates what it measures (wording must change).** The test comment claims 「same process, same ledger load, one warning and not two」 and the test name claims the silence is 「not a lint that stopped loading ledgers」. Measured: the guard distinguishes a lint that cannot resolve the ledger DIRECTORY, not one that fails to load the FIELD ledger — with the row live, a missing or unparseable `field.json` is observationally identical to the flipped row from inside `lintLivenessProperties`. The pins themselves are sound (A); the sentence describing their coverage is not. Scope: narrow that comment and test name to what ablation B shows (directory resolution). The per-file blind spot is `loadWarnMap`'s silent returns, is shared by the #6774 / #10068 precedent blocks the PR copies, and is not this card's to fix — recorded, not charged to this diff.\n\n**NOT MEASURED, and why:**\n\n- CI convergence on this head: at read time `Lint & Repo Gates`, `Type Check · workspace`, `Test Core` 1/3/4/5/6, `Dogfood Regression Gate` 2/3 and `Temporal Conformance` were `in_progress`; `Check Changeset`, `Spec property liveness`, `Governed Surface Queue Guard`, `Build Core`, `Type Check · source gates / consumer gates / debt ledger`, `Test Core 2/6` and `Dogfood 1/3` read `success`. I did not wait for the rest.\n- `check:react-declaration-parity --strict` — not run here; `check:generated` names it as not run by that aggregate, and I did not wire the tracked manifest in.\n- Runtime behaviour of the pinned console: I read the consumer's source at the pin; I did not boot objectui at `53ded82bf7` and render a filtered related list. The AND-composition and badge parity are asserted by objectui #6946's own tests, which I did not execute.\n- The PR's other reported exit codes — `check:empty-state`, `check:strictness-ledger`, `check:nul-bytes`, `check:merge-driver`, `check:cross-package-test-inputs`, `check:test-source-alias`, `check:published-files`, `check-changeset-fixed`, the eslint narrowing and the spec-side vitest selection — not re-run.\n\n**What must change (the FAIL's scope).** F1: correct or delete the clause 「this type's first flip of that direction」 in the `field` Notes cell of `packages/spec/liveness/README.md`. F2: narrow the anti-vacuity guard's comment and test name in `packages/lint/src/lint-liveness-properties.test.ts` to the coverage ablation B measures. Nothing else needs to move: the flip, its evidence pointer, the changeset level, the `Clause-②: no` declaration and the regenerated counts all hold at this head.\n\nImplemented-by: `claude/issue-19187-related-list-filter-liveness-flip`\nReviewed-by: `session_01JbZnqu8bt6YqfJsr9vaFb3`\n\n**(a) Measured first-hand in this act:** the `.objectui-sha` pin; the objectui#4664 / #6946 / `d796c8dde` identities and the console CHANGELOG line, from the GitHub API and `origin/main`; the ancestry and its two controls in a non-shallow clone I made; the cited symbol and line locations at the pin, with their control at objectui main; every ledger count and row shape quoted above; the `fa125f3bfe` history reading behind F1; the diff's file list and merge attributes; the six gate exit codes, the `state-counts.md` reproduction, the direct `authorWarnedProperties` call and the three ablations, all in a worktree at head `7f9df934bb`.\n**(b) Taken from somebody else's reading and therefore NOT verified here:** the CI check-run conclusions (read from GitHub, not re-run, several still in progress); the PR's exit codes for the gates I did not re-run (listed above); objectui #6946's own test evidence for AND-composition and badge parity; the eslint-narrowing argument in the PR body.\n\n**VERDICT: FAIL**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5748900209,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-20T09:17:15Z","updated_at":"2026-09-20T09:17:15Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19265#issuecomment-5748900209","body":"## Contract review\n\n```\nf091bc16ae035649777b0d126c83b7bd2f6c9d59\n```\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `f091bc16ae035649777b0d126c83b7bd2f6c9d59`\n\nIndependent contract review of PR #19265 delivering card #19187, readings taken 2026-09-20T09:17Z. This head moved after the earlier `## Contract review` record on this thread (that record judged `7f9df934bb`); nothing below is inherited from it — every reading was re-taken in this act against `origin/main` `215840f435` (6 commits past the PR's merge-base `7d0f911da9`; `git diff --stat` between the two over `packages/spec/liveness`, `.objectui-sha`, `packages/lint`, `packages/spec/scripts` and `packages/spec/src/liveness` is empty, so the base drift touches nothing this PR reads) and a worktree at the head above. Seat comments on the card and the PR, and the seat-correction block inside the PR body, were read as a party's assertions and adopted for nothing.\n\n### ① Does the diff serve the card, and only that?\n\n**The flip condition — verified first-hand, in a non-shallow clone.**\n\n- `.objectui-sha` read directly at `origin/main` `215840f435`: `53ded82bf7a494f54e344e19099dbf00854b8694`; the PR head carries the same pin (the diff does not touch it).\n- The session's objectui checkout at `/home/user/objectui` **is shallow** — `git rev-parse --is-shallow-repository` prints `true`, `.git/shallow` holds 2 grafts, 50 commits reachable — so I did not take any ancestry reading from it. I made a fresh blobless clone (`git clone --filter=blob:none --no-checkout`) of `objectstack-ai/objectui` in the scratchpad: `is-shallow-repository` prints `false`, 10490 commits reachable from its `origin/main` (`078f2e4f7c`).\n- objectui#4664 is closed `completed`, closed by objectui PR #6946 (`merged: true`, merged by `os-sam`). In the full clone, `d796c8dde54dcd572ce6abb07c137ba3333b9084` is the squash commit carrying that PR (subject ends `(#6946)`, parent `836e97971b`). `git merge-base --is-ancestor d796c8dde 53ded82bf7` exits **0**. Lit controls on the same clone: objectui `origin/main` `--is-ancestor` the pin exits **1** (the instrument can say no); the pin `--is-ancestor origin/main` exits **0**; `git rev-list --ancestry-path --count d796c8dde..53ded82bf7` prints **293**. `packages/console/CHANGELOG.md:474` at `origin/main` records `d796c8dde` shipping objectui#4664.\n- **The evidence pointer resolves at the cited pin, at the cited locations**, read with `git show 53ded82bf7:path` in the full clone: `packages/app-shell/src/utils/deriveRelatedLists.ts` has `function isDeclaredFilter` at line 176, `export function deriveRelatedLists` at line 212 and the `...(isDeclaredFilter(fieldDef.relatedListFilter) ? { filter: fieldDef.relatedListFilter } : {})` read at lines 276-277. `packages/app-shell/src/views/RecordDetailView.tsx` has `const synthParts = useMemo` at line 1867 closing at line 2126 (so it encloses both cited lines), `filter: declaredFilter` destructured at line 2041 and `...(declaredFilter ? { filter: declaredFilter } : {})` at line 2079. The badge half the pointer's prose names is also there at the pin: `packages/components/src/renderers/layout/containers.tsx` documents `filter` as the declared scope the count probe reads (lines 392-407) and `packages/components/src/hooks/related-count-store.ts` imports `mergeFilterNodes` at line 30. Control at objectui `origin/main`: the same reads sit at 201 / 237 / 301-302 and 1873 / 2047 / 2085 — the card's relayed `301-302` was a main-tree reading, and the PR's pin-tree numbers are the ones that hold where this repo builds. Note `check-liveness.mts` counts foreign (`objectui: …`) anchors and never resolves them (its own pass text says so), so this cross-repo reading is the review's, not the gate's.\n\n**Scope of the diff** — 5 files, +167 / −22 against merge-base `7d0f911da9`, measured with `git diff --stat`; no governed path (`docs/adr/**`, `.claude/**`, `skills/**`, `AGENTS.md`, `CLAUDE.md`) is touched:\n\n- `packages/spec/liveness/field.json` — the flip. Row keys at this head: `status: live`, `verifiedAt: 2026-09-20`, `evidenceScope: cross-repo`, `evidence`, `note`; `authorWarn` and `authorHint` gone. Shape matches its three `relatedList*` siblings (all `live`, `cross-repo`, `objectui: packages/app-shell/src/utils/deriveRelatedLists.ts…` pointers, no `producer`, no `authorWarn` — read from the four rows at this head). `authorWarn: true` per ledger file at the merge-base: `field.json` 1, `object.json` 1, `translation.json` 1; at this head: `object.json` 1 (`externalSharingModel`, `planned`), `translation.json` 1 (`flows`, `planned`), `field.json` 0, and `field.json` carries 0 `experimental` rows — so the PR's premise that this was the only warned field row at any depth is true, with the two sibling files as the lit control on the same walk.\n- `packages/spec/liveness/state-counts.md` — regenerated (`field` 90/2 → 91/1, total 914/12 → 915/11); the only `merge: os-regen` path in the diff (`git check-attr` read per file; the other four are `unspecified`). Reproduction in ③.\n- `packages/spec/liveness/README.md` — one append to the `field` Notes cell. Every factual clause I could test holds: four `relatedList*` keys exist at this head, so 「the fourth member of the related-list family」 is right; 「NOT this type's first flip of that direction」 is right — `fa125f3bfe` (#15316) changed `valueDomain` in this file from `\"status\": \"planned\"` to `\"status\": \"live\"` (`git show fa125f3bfe -- packages/spec/liveness/field.json`), and at `origin/main` that row reads `live` / `verifiedAt 2026-09-04`; the `app.navigation.runAction` (#10068) and `list.map` (#11442) precedent flips exist as `8d0bb797f4` and `6acb11a0ee` (each removes a `\"status\": \"planned\"` line on its ledger; the README's `view` row cites #11442 for `list.map`, `live` / `verifiedAt 2026-08-24`); the seeding commit is `8b9eba51b5`; the lint-side line numbers the cell quotes (`if (fieldWarn.size > 0)`) sit at `lint-liveness-properties.ts:493`.\n- `packages/lint/src/lint-liveness-properties.test.ts` — the two pins that used this row as their witness re-dispositioned (③), plus comment corrections. The module the tests drive, `lint-liveness-properties.ts`, is untouched (`git diff` over it between merge-base and head is empty). The comment's claim that `#10262` resolves in neither repo: measured through the GitHub API in this act — `#10262` answers **404** in both `objectstack-ai/objectstack` and `objectstack-ai/objectui`; lit control `#7079` answers **200** in both. The lint-module line citations in the new comments are exact: `if (!existsSync(file)) return map;` at `:77`, the parse `catch` returning the empty map at `:81-83`, the two `loadWarnMap` calls at `:485-486`, the field-walk guard at `:493`, `if (!isRecord(field)) continue` at `:495`, `if (!dir) return []` at `:481`.\n- `.changeset/19187-related-list-filter-liveness-flip.md` — `@objectstack/spec: patch`, `Clause-②: no`. The ledger ships in the tarball: `packages/spec/package.json` `files[]` includes `liveness` (read at `origin/main`), so `patch` rather than `skip-changeset` is the right call.\n\nNothing else moves: no `packages/spec/src/**`, no objectui code, no other ledger row, no pin bump. The acceptance note about `README.md:548` is real (that path names `packages/cli/src/utils/lint-liveness-properties.ts`, which does not exist; `packages/lint/src/lint-liveness-properties.ts` does) and is correctly left out of this diff.\n\n### ② Is `Clause-②: no` a true reading of this diff?\n\nJudged from the diff against the gate's own definition (`scripts/check-changeset-no-major.mjs` header: clause ② is 「a new key on a published payload」; `scripts/pm/check-widening-tells.mjs` T1-T4: a schema key, a closed-set member, a published export row, a registry registration):\n\n- No `.zod.ts`, no `packages/spec/src/**`, no `exports` map, no registry entry, no closed set moves. `FieldSchema.relatedListFilter`'s accept set is byte-identical — the diff never opens `field.zod.ts`.\n- What moves is one ledger row's keys and a generated count table: ledger data inside the tarball, not a key an author writes or a consumer receives. Repo precedent grades ledger-only changes `patch` + `Clause-②: no` (`.changeset/18582-connector-analytics-cube-liveness-ledgers.md`, `18614-conversion-registry-retryconfig-liveness-claim.md` at `origin/main`, read here).\n- The one observable behaviour change is that `packages/lint`'s advisory stops emitting one warning; that lint 「NEVER fails the build」 (module header, line 17). An advisory going quiet is not an accept-set change in either direction.\n- Mechanical reading: `node scripts/pm/check-widening-tells.mjs --declaration no --diff <merge-base..head diff>` exits **0** with no tell; lit control on the same instrument, a synthetic diff adding `newKey: z.string().optional()` to `field.zod.ts`, exits **4** naming the tell. `node scripts/check-changeset-no-major.mjs --base origin/main` exits **0**; `node scripts/check-adr-0087-registration.mjs --base origin/main` exits **0**.\n\n⇒ `Clause-②: no` is a true reading and `patch` is the right level.\n\n### ③ Does any assertion in this PR prove nothing?\n\nWeighed hardest, as the review order asks. The flip empties the only `authorWarn` row on `field.json`, so the two pins that used that row as their witness were re-dispositioned into a **silence pin**, an **anti-vacuity guard** and a **narrowed claim**. Each was judged by mutation, not by reading.\n\n**Direct premise check first.** At this head `field.json` carries 0 `authorWarn` and 0 `experimental` rows (`object.json` and `translation.json` carry 1 each on the same walk), so `loadWarnMap(dir, 'field')` is empty and the field loop is gated off at `lint-liveness-properties.ts:493`. The PR's premise is true.\n\n**Ablations** on `packages/lint/src/lint-liveness-properties.test.ts`, run in the review worktree from the committed state with `pnpm --filter @objectstack/lint exec vitest run src/lint-liveness-properties.test.ts`; baseline **71 passed, exit 0**. Each mutation was proven on disk before the run and each restore was proven by blob identity (`git hash-object` == `git rev-parse HEAD:path`, `git status --porcelain` empty) after it:\n\n- **A — the ledger row reverted** to the merge-base blob `a9642c5687` (`planned` + `authorWarn`, one `\"authorWarn\": true` line back in the file): **4 failed / 67 passed, exit 1**. By name: 「no longer warns on `relatedListFilter` — the derived list applies it since objectui#4664 (#19187)」, 「stays quiet however many fields author the filter」, 「the related-list silence is a real verdict, not a lint that never resolved the ledger directory」, and the narrowed #11385 case 「field walk: a malformed `fields` array under a well-formed object neither throws nor stops the object walk」. ⇒ all three re-dispositioned assertions, and the second silence pin, go red under exactly the half-reverted flip they exist to catch; none is vacuous. The block's fourth `it` (「stays silent on fields that author no warned key」) stays green under A by construction — its fixture authors no `relatedListFilter` — so it is the block's weakest assertion: a no-false-positive pin, the same shape the #6774 and #10068 blocks carry, whose name claims nothing more than it measures. Recorded, not charged.\n- **B — the whole `packages/spec/liveness` directory moved away** (the failure the guard names): **17 failed / 54 passed, exit 1**, including the new guard and the narrowed field-walk case, alongside the dashboard, navigation, translation and area-gate guards. ⇒ the guard fires for the failure it names.\n- **C — `field.json` alone removed**, `object.json` intact: **71 passed, exit 0**. Nothing in the file goes red.\n- **D — `field.json` alone made unparseable** (`{ not json`): **71 passed, exit 0**. Nothing goes red.\n\n**The guard's scope claim, judged against B/C/D.** Removing the directory and removing (or corrupting) the one file produce **different verdicts** — 17 red versus 0 red — and the guard's wording at this head matches the one it actually guards: its test name says 「not a lint that never resolved the ledger **directory**」, and its comment states, as a measured non-coverage, that a missing or unparseable `field.json` alone leaves it green, citing `loadWarnMap`'s silent returns at `:77` and `:81-83` and the separate `object`/`field` loads at `:485-486`. Every one of those line citations is exact at this head (the module is unchanged by the diff). The per-file blind spot is `loadWarnMap`'s, is shared by the two precedent blocks, and is correctly left uncharged to this card.\n\n**The narrowed #11385 claim.** Its name asserts the object walk reaches its own finding past a malformed `fields` array and that no `relatedListFilter` finding appears. Both halves discriminate (red under A and under B, in opposite directions), and the comment says in terms that the 「kept walking past the null FIELD」 half is not asserted because the loop it would exercise is gated off — the vacuous shape is named and refused rather than written.\n\n**`state-counts.md` reproduces byte-identical**: `pnpm --filter @objectstack/spec gen:liveness-counts` exits **0**; the file's blob before / after / at HEAD reads `7c5f099e68` in all three, `git status --porcelain` empty afterwards. `field` 91 / 0 / 0 / 1 / 1 / 93; total 915 / 5 / 1 / 160 / 11 / 1092.\n\n**Gates — real exit codes**, worktree at `f091bc16ae`, after `pnpm install --frozen-lockfile` (exit 0) and `pnpm --filter @objectstack/spec build` (exit 0), heavy legs run through `scripts/pm/os-verify-lock.sh` (acquired after 0s, its own VERDICT line `command-exit 0`):\n\n| command | exit |\n|---|---|\n| `pnpm --filter @objectstack/spec check:liveness` | **0** — its own pass line, and 「state-counts.md is current — the same 39 row(s)」 |\n| `pnpm --filter @objectstack/spec check:generated` | **0** — 「All 16 generated artifacts are up to date」 |\n| `pnpm --filter @objectstack/lint test` | **0** — 105 files, 3981 passed |\n| `pnpm --filter @objectstack/lint typecheck` | **0** |\n| `pnpm --filter @objectstack/spec typecheck` | **0** |\n| `pnpm --filter @objectstack/spec gen:liveness-counts` | **0** (reproduction above) |\n| `node scripts/check-changeset-no-major.mjs --base origin/main` | **0** — level axis 「NOT APPLICABLE」 offline, no `pull_request` payload; CI's `Check Changeset` on this head reads `success` |\n| `node scripts/check-adr-0087-registration.mjs --base origin/main` | **0** — 「adds no declared-breaking changeset (1 non-breaking changeset(s) seen)」 |\n| `node scripts/pm/check-widening-tells.mjs --declaration no --diff <pr.diff>` | **0** (control diff adding a schema key: **4**) |\n\n**NOT MEASURED, and why:**\n\n- CI convergence on this head: at read time `Test Core (1/6)`, `Test Core (6/6)`, `Type Check · workspace` and `Lint & Repo Gates` were `in_progress`; `Build Core`, `Temporal Conformance`, `Governed Surface Queue Guard`, `Spec property liveness`, `Check Changeset`, the other four `Test Core` shards, all three `Dogfood Regression Gate` shards and the other three `Type Check` jobs read `success`. I did not wait for the rest.\n- `check:react-declaration-parity --strict` — not run here; `check:generated` names it as unrun by that aggregate (needs `MANIFEST=`), and this diff touches no spec schema or registry input.\n- Runtime behaviour of the pinned console: the consumer's source was read at the pin; objectui was not booted at `53ded82bf7` and no filtered related list was rendered. The AND-composition and badge parity are asserted by objectui #6946's own tests, which I did not execute.\n- The PR's other reported exit codes — `check:empty-state`, `check:nul-bytes`, `check:merge-driver`, `check:cross-package-test-inputs`, `check:test-source-alias`, `check:published-files`, `check-changeset-fixed`, the eslint narrowing and the spec-side vitest selection — not re-run.\n\n### Independence pair\n\n**(a) Measured first-hand in this act:** the `.objectui-sha` pin at `origin/main` and at the head; the objectui#4664 / #6946 identities via the GitHub API; the `d796c8dde` ancestry and its three controls in a non-shallow clone I made; every cited symbol and line at the pin, and their control at objectui main; the per-file `authorWarn` counts at merge-base and head; the four sibling row shapes; the `fa125f3bfe`, `8d0bb797f4`, `6acb11a0ee` and `8b9eba51b5` history readings; the `#10262` / `#7079` 404-vs-200 pair; the lint-module line numbers; the diff's file list, merge attributes and governed-path check; the widening-tells run and its control; the gate exit codes, the `state-counts.md` reproduction and the four ablations, all in a worktree at `f091bc16ae` after `pnpm install --frozen-lockfile` (exit 0) and `pnpm --filter @objectstack/spec build` (exit 0).\n**(b) Taken from somebody else's reading and NOT verified here:** the CI check-run conclusions (read from GitHub, not re-run, four still in progress); the PR's exit codes for the gates I did not re-run (listed under NOT MEASURED); objectui #6946's own test evidence for AND-composition and badge parity; the maintainer ruling of 2026-08-15 the ledger note quotes; the eslint-narrowing argument in the PR body.\n\nImplemented-by: `claude/issue-19187-related-list-filter-liveness-flip`\nReviewed-by: `session_01JbZnqu8bt6YqfJsr9vaFb3`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
