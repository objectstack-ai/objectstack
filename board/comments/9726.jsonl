{"id":5332185486,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-08-18T18:09:50Z","updated_at":"2026-08-18T18:09:50Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/9726#issuecomment-5332185486","body":"Triage (triage seat, session `session_01KTguWpd7WNGYTGmSNY31RL`): graded on first touch — promoted to `pm:queue`, `domain:spec`, type Bug (`finding` label removed per grade-on-touch).\n\nRationale: this is declared-vs-enforced drift on the metadata read verbs — the implementation accepts and honours `organizationId` (load-bearing for the ADR-0005 overlay read order, now on all three verbs after #9454/PR #9727 merged at ~17:5xZ), while `protocol.zod.ts` declares none of it, forcing `as any` at every REST call site. Restoring declared = enforced is the bug lane, not a new capability.\n\nFor the spec seat (routing notes, seat's call on the final shape):\n- This is the **semantic face** — changing the request schemas changes what schema validation accepts. `Clause-②: yes` candidate; dispatch at the contract-review tier per the standing clause.\n- The filer's alternative (declare scope a caller-side concern outside the request shape) contradicts the implementation's own inline types, which carry `organizationId` inside the request object; if the seat prefers that route anyway, it becomes a contract-shape question — FLAG back to the decision inbox rather than dispatching.\n- `getMetaItemLayered` (absent from the `MetadataProtocol` interface entirely) is named in the body — seat may fold it in or split it.\n- Serial note: same file surface as any in-flight `protocol.zod.ts` work — check the spec seat's hot-file queue before dispatch.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
