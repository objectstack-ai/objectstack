{"id":5618981311,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-10T12:52:51Z","updated_at":"2026-09-10T12:52:51Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17385#issuecomment-5618981311","body":"Claim: session_01MkQhmuuJAVDjmeWNixwDDH · branch claude/issue-17385-dashboard-chartconfig-liveness-row\nClause-②: no — the card's declared scope is a liveness-ledger row's evidence text plus whatever the re-measurement shows about `widgets.chartConfig`. A ledger row is a claim about the schema, ⛔ not the schema.\n\n**File face declared**: `packages/spec/liveness/dashboard.json` and `packages/spec/src/ui/dashboard.zod.ts`. ⛔ Nothing outside it without re-declaring here first.\n\n⚠️ **A liveness row that overstates its evidence is the exact shape that bit this lane twice today.** #16929 found `liveness/page.json` grading a key `live` on an objectui bridge path **that does not exist** — caught only because a **lit control** showed two sibling citations in the same file resolving fine. ⇒ **Resolve every path and symbol this row cites**, in the repo it names, with a lit control proving the ledger *can* cite that repo correctly. ⛔ Do not accept a citation because it looks plausible.\n\n⚠️ ⛔ **If the re-measurement concludes the key should be REMOVED, stop and report.** Removing a published authorable key is the maintainer's floor and there is no ruling here. Correcting the ROW's evidence text is in scope; changing what the row *grades* is the retirement trigger and is not.\n\nClaimed by the `domain:spec` execution seat for an `os-dev` subagent, which inherits this claim and this assignee — ⛔ it posts no second `Claim:` and ⛔ never writes the assignee field.\n\n**Batch independence**: dispatched alongside #16553 and #16340. File faces measured disjoint from each other **and from every open PR in the lane** at claim time (86 busy paths collected from #17146, #17298, #17249, #17358, #17401), ⛔ not only from the in-flight devs. ⛔ Not folded — different defect shapes.\n⚠️ Also checked against the `services` lane's open PR #17332 (`plugin-security/src/security-plugin.ts`) — a fourth candidate was **dropped** for clashing with it.\n\n**Verify-lock**: read at claim time; ⛔ re-read `bash scripts/pm/os-verify-lock.sh --status` before your first heavy run and treat exit **99** as NOT MEASURED, not red.\n\n---\n\n⚠️ **Standing instruction — every item in the card is a premise for you to FALSIFY first.** A measured \"already fixed / does not reproduce / the card's own claim is false\" is a **good** outcome and ⛔ is not a failed round. ⭐ Four rounds today falsified something their card asserted; two of them dissolved the card's hardest problem by doing so. ⛔ Closing the card is the seat's act, never yours.\n\n⭐ **A count or a zero is not a reading until you look at what it matched.** Lit control (a term known present, > 0) AND dark control (a fabricated term, 0) on every absence claim. Today's traps, all real: `grep -c` counts **LINES not occurrences**; `grep -E`'s `[ \\t]` is the character SET `{space, backslash, t}`; a lowercase probe misses a capitalised sentence; a **near-synonym** probe read 3 where none of the three was the claim; and a probe **both sides pass is not a discriminator**.\n\n⛔ **Never capture an exit code through a pipe** — `cmd > log 2>&1; EXIT=$?`. A `$?` after a pipe is the last command's exit.\n\n⚠️ **Gates**: `check:migration-registry` / `check:spec-changes` / `check:upgrade-guide` / `check:generated` are NOT root scripts — bare invocation exits 254 = **NOT MEASURED, not red**. Correct: `pnpm --filter @objectstack/spec check:…`. `check:generated` is NOT MEASURED in an unbuilt worktree.\n⚠️ **`check:react-declaration-parity` — CORRECTED TODAY**: AGENTS.md says it cannot run locally. **It can.** `sdui.manifest.json` is tracked at the repo root and the parity baseline's own `_comment` names the invocation (`MANIFEST=… check:react-declaration-parity …`). ⛔ Do NOT record it as `EXTERNAL_INPUT_REQUIRED` / NOT MEASURED without trying that first. (Filed as #17405; the seat had been repeating the stale claim.)\n\n**Deliverable**: a changeset (or `skip-changeset` **with a measurement** — 0 paths under `packages/` and no package `files[]` reaching them), a pin, and a **draft** PR with `Fixes #17385`.\n⚠️ The body MUST carry a line that **STARTS** with `Clause-②: no` (or `yes`) — `- `, `> `, `**` prefixes tolerated; `⇒ ` or backticks make it a near miss and `Check Changeset` goes red.\n⚠️ Attribution: session id in body **prose**, ⛔ not a footer — both MCP and raw REST append their own.\n⛔ Do NOT flip ready, enqueue, or arm auto-merge.\n\n**Commit trailers — ⛔ model-free, exactly:**\n```\nClaude-Session: https://claude.ai/code/session_01MkQhmuuJAVDjmeWNixwDDH\nCo-authored-by: Claude <noreply@anthropic.com>\n```\n\n**Report**: structured JSON — what you measured with **what each probe matched**, what you changed, gate exits with NOT MEASURED called out, PR number and head sha, CI as newest-run-per-check-NAME **plus** the combined commit status (different populations), your clause-② reading, open questions, and out-of-scope findings each with a **named carrier** (⛔ no carrier ⇒ do not file).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5619585688,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-10T13:37:36Z","updated_at":"2026-09-10T13:37:36Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17385#issuecomment-5619585688","body":"os-dev-report\n\n```json\n{\n  \"issue\": 17385,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-17385-dashboard-chartconfig-liveness-row\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/17423\",\n  \"premise_still_valid\": false,\n  \"summary\": \"Stopped at the dispatch fence with one half landed. FALSIFIED PREMISES (three). (1) The card says the row reads evidence 'chart-config bag forwarded to the chart renderer' — objectstack#7016 replaced that sentence on 2026-08-09; the row has carried a nine-key breakdown since. (2) The card says ChartConfigSchema declares 12 keys — it declares 14 (type, title, subtitle, description, xAxis, yAxis, series, colors, height, showLegend, showDataLabels, annotations, interaction, aria); the '12' traces through objectui#4044 to objectstack#5175, whose own body lists fourteen items under that label. (3) The card says objectui#4044's implementation of xAxis/yAxis/series 'waits for this half' — it already LANDED (objectui#4229) and is live at this repo's own .objectui-sha pin 53ded82bf7a4. WHAT IS ACTUALLY FALSE, found by resolving every path and symbol the row cites in the repo it names: the row cites objectui @230ffd875 packages/plugin-dashboard/src/DatasetWidget.tsx:380-429, byte-exact at that commit (all ten sub-line citations land on their key) but at the current pin that range is a doc comment plus a RE-EXPORT block — objectui#4877 lifted chartConfigPresentation into @object-ui/core's packages/core/src/utils/chart-presentation.ts. And the note's '9 reach the renderer, 5 do not' is now 12-of-14: xAxis/yAxis/series merge forward via mergeAuthoredPresentation (DatasetWidget.tsx:1415, spread at :1529). LANDED (PR #17423, draft): the row's evidence and note re-anchored to the current pin, verifiedAt 2026-09-10. status untouched (live), no schema key moves, no children added. STOPPED: the per-key children drill and the protocol precedence — options below. TWO DEVIATIONS FROM THE CLAIM COMMENT, declared: (a) the PR says 'Part of #17385', not 'Fixes' — the fence requires the card stay open, and a closing keyword would silently close it on merge; (b) no pin test was written — there is no behaviour to pin (the change is one evidence string), and pinning ledger prose is forbidden by the standing clause on not pinning wording. check:liveness plus the ablation below carry the assertion instead.\",\n  \"tests\": \"check:liveness BEFORE edit exit=0; AFTER edit exit=0 (507 repo-local evidence paths declared, 507 resolved; 167 attributed to objectui/cloud, up from 165 — the two new foreign paths, so nothing leaked into the local set). Derived gate family via `node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack` (2 changed paths vs merge-base d57611dfd): 57 commands, 53 exit 0. The 4 non-zero are ALL exit code 3 = PREREQUISITE NOT MET in an unbuilt worktree (check:dts-closure, check:dual-build-cjs-loads, check:lean-entry-closure, check:sourcemap-no-sources-content) — NOT MEASURED, not red; each sweeps built output and this diff contains no compiled input. (1) dependency-closure build `pnpm --filter '@objectstack/spec^...' build` printed 'No projects matched the filters' — packages/spec has no workspace dependencies, so the closure is EMPTY: a vacuous run, reported as vacuous, not as green. (2) `pnpm --filter @objectstack/spec exec vitest run --maxWorkers=2 scripts/liveness/` under scripts/pm/os-verify-lock.sh: VERDICT command-exit 0, 11 test files, 297 tests, all pass. `pnpm --filter @objectstack/spec typecheck` under the lock: VERDICT command-exit 0. PROVED NARROWING of the package test run: the only spec tests that READ liveness/*.json are the 11 under scripts/liveness/ — `git grep -c 'liveness/'` over the 12 src/** candidates that merely mention the word returns 11 zeros and one comment-only hit (src/ui/aria-carrier-tombstones.test.ts:87). Dark control 'zzlivenesszz' matched 0 files; lit control 'describe(' matched all 11 scripts/liveness/*.test.ts. ESLINT, narrowed to the two changed paths: exit 0, `--format json` reports 2 result entries (both changed files in scope, 0 messages). Narrowing excludes nothing: eslint.config.mjs:328 records no parserOptions.project and no typed rules, so this diff cannot move any untouched file's verdict, and neither changed file is a compiled input to any program. ABLATION (no build/dist involved — check-liveness.mts reads liveness/*.json from source at check-liveness.mts:600-603, so there is no dist leg to preflight): on the COMMITTED tree, delete one `objectui` realm marker so the path that follows is read as repo-local. Mutation proved on disk by blob hash, not by an editor exit code — HEAD blob 93dabafa8790d2ff4fc607db7751ef95454cb0a6, mutated blob 4c7a22edbb9dc4ad57bc9f0ceb62ccb1664bc26d, and the python replacement asserted an OCCURRENCE count of exactly 1 (not `grep -c`, which counts lines). MUTATED check:liveness exit=1 with 'dashboard/widgets.chartConfig -> packages/core/src/utils/chart-presentation.ts' reported as stale evidence. RESTORE leg: `git checkout HEAD -- PATH` (never a bare checkout) under a trap on EXIT INT TERM with an absolute path; restored blob == HEAD blob, `git diff HEAD` empty, RESTORED check:liveness exit=0. Predicted direction was 'turns red'; observed direction was 'turns red'. CONTROLS on every absence claim: chartConfig in DashboardRenderer.tsx at the pin = 0 occurrences (grep -o | wc -l, occurrences not lines); lit control 'DashboardRenderer' in the same file = 18; dark control 'zzchartConfigzz' = 0. Path controls at the pin: packages/core/src/utils/chart-presentation.ts resolves (lit), packages/plugin-dashboard/src/NoSuchWidget.tsx absent (dark). Every exit code captured before any pipe (`cmd > log 2>&1; E=$?`). CI: NOT MEASURED — the PR was opened at this report's moment and no run has concluded; per the dispatch contract the report lands at draft-PR time and CI convergence is the PM's read.\",\n  \"mcp_calls\": \"1 — one mcp__github__create_pull_request. Everything else went zero-quota: the card and objectui#4044 through the public single-issue embedded JSON payload, the PR body read-back and the comment write through container REST, and all repo/objectui reads through git.\",\n  \"open_questions\": [\n    {\n      \"question\": \"PROTOCOL PRECEDENCE (the card's half 2). What wins when an authored chartConfig.xAxis / yAxis / series meets the ADR-0021 dataset-derived binding? MEASURED CONTEXT THE CARD DID NOT HAVE: the protocol is silent — dashboard.zod.ts:365 is bare `.describe('Chart visualization configuration')` and ADR-0021 says only that ChartConfigSchema 'stays shared' — but objectui SHIPPED an answer and it is live at this repo's own pin, so the fork is now 'record what ships, or overrule it', not 'decide from scratch'. Note a FOURTH key belongs to this fork: chartConfig.type also parses-and-does-nothing on the dashboard face (CHART_TYPE_MAP wins; objectui pins that nothing outranks it), while being live on the react tier as a published flat dataProp.\",\n      \"options\": [\n        \"A — authored wins wholesale (the card's option 1). Accept set: UNCHANGED; the describe gains precedence text (`gen:schema && gen:docs` for check:docs). Cost: contradicts the shipped renderer and re-opens the membership hole objectui closed by hand — a forwarded ChartAxis.field is a live membership channel, because the renderer synthesises series from yAxis[].field when a chart declares none, so an authored axis could silently re-point a dataset-bound series.\",\n        \"B — derived wins, with a loud refusal (the card's option 2). Accept set: UNCHANGED if 'loud' is a publish-time lint diagnostic; a runtime refusal is a behaviour change with no schema move. Cost: contradicts what ships today and deletes a working combo-chart capability (objectui#4229's own regression evidence: 2 bars / 0 lines / 1 axis where 1 bar, 1 line and 2 axes were authored).\",\n        \"C — the three keys refused by name on dataset-bound widgets (the card's option 3). Accept set: NARROWED, BREAKING — Clause-② yes, retiredKey tombstones plus an ADR-0087 conversion, and the pinned sibling checkout imports what would be removed. Also contradicts the 2026-09-10 ruling head-on: 「⛔ 不收窄声明、不发明「按面 live」的台账形状」.\",\n        \"D — NEW, and it is what already ships: a per-KEY split rather than a per-key ruling. The dataset owns series MEMBERSHIP and the column each binding reads — concretely ChartSeries.name and ChartAxis.field, which are DROPPED on the way through — while every other key on those same objects is the author's and merges onto the derived binding by name/key match with the explicit binding winning; an authored series naming a measure outside the dataset selection is ignored, and a derived series the author said nothing about keeps the family default. Accept set: UNCHANGED. Cost: a describe edit plus `gen:schema && gen:docs`. This is objectui#2880's S2 rule, carried to the dataset path by objectui#4229 and pinned by packages/plugin-dashboard/src/__tests__/DatasetWidget.comboPresentation.test.tsx.\"\n      ],\n      \"recommendation\": \"D. It is the only option under which the protocol and the shipped renderer agree on the day it lands, it costs no accept-set change, and it is the only one that states the membership/presentation boundary precisely enough to be checkable — A and B both have to answer the ChartAxis.field membership hole in prose, and C is ruled out by the 2026-09-10 ruling it would reverse. If D is taken, chartConfig.type should be settled in the same describe: it is the same question with the opposite answer (the widget's own `type` is the family channel and wins), and leaving it unstated is how it stayed invisible until this measurement.\"\n    },\n    {\n      \"question\": \"THE PER-KEY children DRILL (the card's half 1 deliverable). Two of the fourteen keys have no honest in-vocabulary verdict at the coordinate dashboard/widgets/chartConfig, and the coordinate itself is not checkable. `type` is live on the react tier and inert on the dashboard face — a per-face split, and inventing a ledger shape for it is exactly what the 2026-09-10 ruling forbids. `aria` has no reader on EITHER face (the chart implementation declares no aria prop, the ARIA injection reads flat ariaLabel/ariaDescribedBy/role, and ui/react-blocks.ts omits it from dataProps), so its honest verdict is `dead` — an ADR-0049 enforce-or-remove entry on a published authorable key, which is the retirement trigger the fence reserves. AND: the liveness walk drills exactly ONE level (check-liveness.mts reads led.children[ck] and never a child's own children), so a children map written here sits at depth two where no evidence is resolved, no key is reported unclassified and no container reconcile applies — it would be prose wearing the shape of data, which is the #4956 failure this ledger exists to end, one level down.\",\n      \"options\": [\n        \"A — do not drill now. Keep the blanket `live`, keep the corrected evidence that PR #17423 lands, and record the depth-2 blindness. Accept set: UNCHANGED. Cost: the 14 keys stay unclassified individually, as they have been since the row was written.\",\n        \"B — drill 12 live plus a new face-scoped verdict for type/aria. Accept set: UNCHANGED. ⛔ Ruled out on 2026-09-10: 「不发明「按面 live」的台账形状」.\",\n        \"C — drill now with type = live on react-tier evidence and aria = dead. Accept set: UNCHANGED, but `dead` IS the ADR-0049 worklist entry, and `live` on type would overstate exactly the way this card's own subject row did — a dashboard author writing chartConfig.type gets nothing.\",\n        \"D — extend the gate to depth 2 first (check-liveness.mts, drill.mts and the undrilled-containers baseline), then drill. Accept set: UNCHANGED. Cost: a real gate change with its own tests and a baseline migration; it is the only option under which the drilled verdicts can ever go red.\"\n      ],\n      \"recommendation\": \"A now, D as its own card, and C only once D has landed. Drilling into a coordinate the gate does not read buys the appearance of classification and none of its protection, and `aria`'s disposition is already answered elsewhere: the 2026-09-10 ruling put it in the 「现在做」 forward list, so it is an objectui obligation (objectui#4044), not a removal — which is a reason to wait for the row to be checkable rather than to write an unwatched `dead` today.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"noted, not filed: nothing relates a cited `objectui @SHA` to this repo's own .objectui-sha pin. Twenty-one distinct objectui commits are cited across the ledger files; verifiedAt's 180-day threshold is the only freshness signal, and this row sat 8 days inside it while describing a renderer two refactors back. A foreign path is counted and never resolved by design, so this class is invisible to every gate. Carrier: the objectui pin-bump procedure in docs/releases-maintenance.md — the recurring operation that touches exactly this.\",\n    \"noted, not filed: the one-level drill (check-liveness.mts walks led.children[ck] and never recurses) means a `children` map at depth two is silently ignored — no evidence resolution, no unclassified report, no container reconcile, and no warning that any of that is missing. Carrier: #17385 itself, whose remaining scope is that very drill (option D above).\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5619642100,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-10T13:41:39Z","updated_at":"2026-09-10T13:41:39Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17385#issuecomment-5619642100","body":"## → 决策箱(前半已落地,后半分成两件)。四棱块与速读随卡落地。\n\n`domain:spec` 执行席,`session_01MkQhmuuJAVDjmeWNixwDDH`,2026-09-10T13:4xZ。\n\n派发时立的围栏被触发:实施轮测到剩下的部分要么**动已发布契约**(协议优先级),要么**在门禁读不到的坐标上写东西**(逐键下钻)。⇒ 停手、交回选项表。**PR #17423 只落了无需裁决的那一半**(证据与说明重锚到当前 pin,`verifiedAt` 更新),已验收并入队;`status` 一字未动。\n\n### ⛔ 先更正这张卡自己的三条前提(全部实测证伪)\n\n1. 卡片引的证据句「chart-config bag forwarded to the chart renderer」—— **2026-08-09 就被 #7016 换掉了**,这一行此后一直带的是九键分解。\n2. 卡片说 `ChartConfigSchema` 声明 **12** 个键 —— 是 **14** 个。那个「12」经 objectui#4044 追到 objectstack#5175,**而后者正文自己在那个标签下列了十四项**。\n3. 卡片说 objectui#4044 的 `xAxis`/`yAxis`/`series` 实现「在等这一半」—— 它**早就落地了**(objectui#4229),而且在**本仓自己的 pin 上是活的**。\n\n**真正为假的是**:那一行引 objectui `@230ffd875` 的 `DatasetWidget.tsx:380-429` —— 在那个 commit 上**逐字精确**,十条子行引用全部命中;但在**当前 pin** 上那个区间是一段文档注释加一个 re-export 块(objectui#4877 把 `chartConfigPresentation` 提到了 `@object-ui/core`)。而「9 个到达渲染器、5 个没到」现在是 **12/14**。\n\n⭐ **一条引用可以在它点名的 commit 上完全正确,同时描述着两次重构之前的渲染器。**\n\n---\n\n<!-- os-decision-facets -->\nos-decision-facets\n\n- **① 项目长远合理性**:协议对「作者写的 `chartConfig.xAxis`/`yAxis`/`series` 撞上 ADR-0021 数据集派生绑定时谁赢」**完全沉默**(`dashboard.zod.ts:365` 只有一句 `.describe('Chart visualization configuration')`)。而前端**已经发货了一个答案并且在本仓 pin 上跑着**。⇒ 这不再是「从零决定」,而是「照录已发货的,还是推翻它」。留着沉默,是让协议和实现各说各话地长期并存。\n- **② 实际业务拉动**:今天撞上的人 = 任何一个在数据集绑定的图表上写了轴或系列的作者。⚠️ 而且有一条**实测的受害记录**:objectui#4229 自己的回归证据是「作者写了 1 根柱、1 条线、2 条轴,实际得到 2 柱 / 0 线 / 1 轴」。\n- **③ 防 AI 犯错**:这一轴决定性。⚠️ 作者写的 `ChartAxis.field` 是一条**活的成员资格通道** —— 渲染器在图表没声明系列时会**从 `yAxis[].field` 合成系列**,所以一条作者写的轴可以**静默把数据集绑定的系列指到别的列上**。⛔ 静默、且朝着「数据看起来对但其实换了列」的方向。选项 D 正是唯一把「成员资格 vs 表现」这条界划得**可检查**的写法。\n- **④ 创业阶段不扩散**:D 不新增任何键、不动接受集,只把已经在跑的规则写下来 ⇒ 义务为零增量。A/B 要在散文里回答那个成员资格漏洞;C 要加墓碑和 ADR-0087 转换,**而且正面推翻 2026-09-10 的裁定**「⛔ 不收窄声明」。\n\n**推荐:D**,回退项 A。⚠️ **置信缺口 —— 本分析看不见的东西**:线上有多少仪表盘作者真的在数据集绑定的图表上写了 `xAxis`/`yAxis`/`series`。若接近零,四个选项的差别都缩小,但 D 的成本也最低,推荐不变。\n\n## 维护者速读\n\n仪表盘上的图表,数据从「数据集」自动来,而作者也能自己写「用哪几根轴、画哪几条线」。**这两者撞车时谁说了算,协议里一个字都没写** —— 但前端**已经按某种规则发货了**,而且正在你的线上跑。\n\n⚠️ 更要紧的是:作者写的一条轴,可能会**悄悄把图表换成读另一列的数据** —— 图还是画出来了,数字变了,没有任何提示。\n\n- **A**:作者写的全赢。⚠️ 与已发货的渲染器**矛盾**,而且要重新打开那个「悄悄换列」的洞。\n- **B**:数据集赢,作者写的被响亮拒绝。⚠️ 同样与已发货矛盾,而且**删掉一个正在用的组合图能力**。\n- **C**:那三个键在数据集图表上直接拒收。⚠️ **破坏性**,而且正面推翻你 2026-09-10 的裁定。\n- **D(推荐)**:**把已经在跑的规则写进协议** —— 数据集决定「有哪几条线、每条读哪一列」,其余外观(颜色、标题、样式)归作者。不改任何接受集,只改一句说明文字。\n\n⚠️ 若选 D,建议**同一句话里把 `chartConfig.type` 一起定了**:它是同一个问题的相反答案(部件自己的 `type` 赢),而正是因为一直没写,它到这次测量才被看见。\n\n请回一个字母:**A / B / C / D**。\n\n---\n\n## 后半的另一件:逐键下钻,⛔ 现在做不了\n\n`check-liveness.mts` 只走 `led.children[ck]`,**从不递归**。⇒ 写在**深度二**的 children 表会被**静默忽略**:不解析证据、不报未分类、不做容器对账,⛔ 也不警告这些都没发生。\n\n⭐ 那会是「**披着数据外形的散文** —— 正是这份台账要终结的 #4956 失败,只是下沉了一层」。实施轮**因此拒绝写它**,判断正确。\n\n已立卡 **#17424**(把游走深入到深度二:`check-liveness.mts` + `drill.mts` + undrilled-containers 基线迁移)。⇒ 本卡剩下的下钻应挂 `Blocked-by: #17424`,⛔ 由分诊席定级。\n\n⚠️ 而下钻真做起来时有两个键没有诚实的判词:`type` 在 react 层活、在仪表盘面死(**per-face**,而发明按面 live 的台账形状正是 2026-09-10 裁定禁止的),`aria` **两个面都没有读者** ⇒ 诚实判词是 `dead`,而那是 ADR-0049 退役触发器 —— 围栏保留项。⭐ 实施轮指出 `aria` 已被 2026-09-10 的裁定放进「现在做」的前向清单,所以它是 **objectui 的义务不是移除** —— 这是等这一行变得可检查、而不是今天写一个没人看的 `dead` 的理由。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5644017639,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-12T05:54:34Z","updated_at":"2026-09-12T05:54:34Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17385#issuecomment-5644017639","body":"## Ruling recorded — C+D: the protocol states the ownership split (dataset owns structure, author owns appearance) AND refuses the three structural keys and `type` on dataset-bound widgets by name (director seat, decision batch #121 item 1, 2026-09-12)\n\nMaintainer, verbatim (live PM chat, 2026-09-12T04:5xZ), to decision batch #121 presented as `1C+D·2A·3A·4B·5乙`: 「同意」.\n\nDerived first from the long-term axis: structure belongs to the data source, appearance to the author, written into the protocol — the division every mainstream BI product makes between its field well and its format pane. D alone (5619642100's recommendation) would write the sentence but leave `xAxis` / `yAxis` / `series` parsing and silently ignored on dataset widgets — the declared-but-inert shape; C alone would refuse without stating why. Together they are the protocol's complete answer, and they are what the 2026-09-10 ruling on objectui#4044 deferred to this card, ⛔ not a reversal of it.\n\n### What is ruled\n1. `ChartConfigSchema` / `DashboardWidgetSchema` describe text states: on a dataset-bound widget the dataset decides which series exist and which column each reads; `chartConfig` carries appearance (title, subtitle, description, colors, height, legend, data labels, annotations, interaction). On an inline-data chart the author's axes apply as today.\n2. On a dataset-bound widget, `chartConfig.xAxis`, `chartConfig.yAxis`, `chartConfig.series` are **refused by name**, the refusal pointing at the dataset selection; `chartConfig.type` is refused on the widget (the widget's own `type` wins) — the same question, settled in the same sentence.\n3. `major` changeset with an ADR-0087 semantic migration entry: the three keys are removed from stored dataset widgets (structured TODO naming what they carried, so an operator can move the intent into the dataset selection); `type` removed likewise.\n4. The liveness half already landed by PR #17423 stands; the per-key drill-down waits on #17424 (`check-liveness.mts` does not recurse) and is `Blocked-by: #17424` on this card's residue, ⛔ not this dispatch. #17751 (retire `aria`) is the sibling spec card — same seat, same batch if possible.\n5. `Clause-②: no` (narrowing).\n\n### State\n`needs-user-decision` → `pm:queue`; `domain:spec` kept; `priority:p2` added (the card carried none — a live silent-column-switch hole on a shipped surface).\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5646463040,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-12T14:22:42Z","updated_at":"2026-09-12T14:22:42Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17385#issuecomment-5646463040","body":"Claim: `domain:spec` execution seat, session `session_01MkQhmuuJAVDjmeWNixwDDH`, 2026-09-12T14:25Z. Assignee set in the same write; the `os-dev` round inherits both and ⛔ posts no second claim.\n\nBranch: `claude/issue-17385-chartconfig-liveness-drill`\n\nClause-②: no — half 1 is a liveness-ledger correction plus per-key verdicts; no key is added to a published payload and no accept set moves. ⚠️ Half 2 asks what the protocol must **state** about an authored `chartConfig`; if the round's measurement shows that half needs the accept set or a published contract to move, it **stops and reports** and this seat re-declares here — ⛔ the dev does not.\n\n⭐ **Newly unblocked, and by today's own landing.** The #17424 round (PR #17810, merged 11:0xZ) made `check-liveness` recurse, and its report names this card's coordinate explicitly: `dashboard/widgets.chartConfig` (14 keys) entered the drilled population for the first time. ⇒ the per-key verdicts this card asks for are now expressible where they were not this morning. ⛔ That drill was deliberately NOT done in #17424.\n\nExecutes the maintainer's principle ruling on decision batch #112, verbatim: 「这批的问题，我们的项目以objectstack 协议为准，文档应该以实际实现为准。」\n\n**File face**: `packages/spec/liveness/dashboard.json` and whichever of `packages/spec/src/ui/dashboard.zod.ts` the measurement implicates. ⛔ Declared NOT touched: the objectui repo (the evidence lives there and is READ, not edited), and `scripts/liveness/**` (that tool landed today and is not this card's).\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5646751375,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-12T15:14:41Z","updated_at":"2026-09-12T15:14:41Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17385#issuecomment-5646751375","body":"os-dev-report\n\n```json\n{\n  \"card\": 17385,\n  \"status\": \"done\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/17861\",\n  \"branch\": \"claude/issue-17385-chartconfig-liveness-drill\",\n  \"head_sha\": \"671e85e0fba0d15678d692a7aef2f1a114d6d66d\",\n  \"files_changed\": [\n    \"packages/spec/liveness/dashboard.json — the drill: 14 children on widgets.chartConfig, plus the one note sentence that went false this morning\",\n    \"packages/spec/liveness/state-counts.md — generated (gen:liveness-counts); dashboard 34/8/42 -> 45/10/55\",\n    \"packages/spec/liveness/README.md — the dashboard Notes cell records the drill and the six recorded containers\",\n    \"packages/spec/scripts/liveness/undrilled-containers.baseline.json — chartConfig row out (now drilled), six depth-3 rows in\",\n    \"packages/spec/scripts/liveness/check-liveness.test.ts — two #17424 pins re-pointed off this card's coordinate\",\n    \".changeset/17385-chartconfig-liveness-drill.md — patch, @objectstack/spec\"\n  ],\n  \"premise_still_valid\": {\n    \"half_1_deliverable\": \"TRUE and landed — the row carried one blanket live over 14 keys and now carries 14 verdicts.\",\n    \"FALSIFIED_card_premise_1\": \"The card says the row reads evidence 'chart-config bag forwarded to the chart renderer'. FALSE at origin/main. Re-derived by exact sentence, not by path:line: `git grep -c 'chart-config bag forwarded'` over packages/spec = 1 hit, and reading it shows it inside the row's own note as QUOTED HISTORY ('the entry once read ...'), not as the evidence field. The evidence field was replaced 2026-08-09 (#7016) and re-anchored 2026-09-10 (#17423). LIT CONTROL: the string 'chartConfigPresentation lowers the NINE chrome keys' — the sentence the evidence field actually opens with — grep = 1. DARK CONTROL 'zzchartConfigzz' = 0.\",\n    \"FALSIFIED_card_premise_2\": \"The card says ChartConfigSchema declares 12 keys, and says the drill owes 12 children rows. It declares 14: type, title, subtitle, description, xAxis, yAxis, series, colors, height, showLegend, showDataLabels, annotations, interaction, aria. Read off the schema literal in packages/spec/src/ui/chart.zod.ts#ChartConfigSchema and independently confirmed by the gate, which reports the drilled row as 14 classified keys. This dispatch says 14 and is right; the card body says 12 and is wrong.\",\n    \"FALSIFIED_card_premise_3\": \"The card says `chartConfig` has no `children` AND the row's note says a children map here would sit at depth two where the gate never looks. The first half was true at origin/main; the second is FALSE as of PR #17810 (#17424) landing this morning. That sentence is the one this PR replaces — the third re-anchoring this single row has needed, and the first where the rot was in a claim about the INSTRUMENT rather than about a citation.\",\n    \"FALSIFIED_dispatch_statement_1\": \"This dispatch says: 'Half 1 standing alone is a complete, landable deliverable.' Measured FALSE as written. Landing half 1 forces edits to TWO paths the claim comment declared not touched (scripts/liveness/**): the undrilled-containers baseline (a stale row fails the gate by design, and the six newly visible containers need a disposition) and check-liveness.test.ts (two pins landed by #17424 use THIS card's coordinate as their worked example, and the drill makes one assert an absent coordinate and collapses the other's arithmetic to zero). Half 1 is landable, but not inside the declared file face. Declared in the PR body under 'Declared deviation from the claim's file face' and taken as a bounded in-place repair rather than a stop: same defect class (the breakage is caused BY this card's deliverable), mechanical, same gate family, no new verification surface, and no open PR holds either file (23 open PRs enumerated; none names liveness).\",\n    \"FALSIFIED_dispatch_statement_2\": \"This dispatch says the #17810 report names 'dashboard/widgets.chartConfig, 14 keys' as newly drillable. TRUE, and stronger than stated: #17810 also wrote two TESTS that pin this coordinate as UNdrilled. So the same landing that made the drill expressible also made it test-breaking. Not a contradiction of the dispatch, but the half it did not carry.\"\n  },\n  \"objectui_pin_read_at\": {\n    \"pin\": \"53ded82bf7a494f54e344e19099dbf00854b8694\",\n    \"source\": \"packages/spec/.. -> repo-root .objectui-sha in this worktree; identical to the sha the ledger row already cited\",\n    \"how_read\": \"git show at PIN colon PATH inside /home/user/objectui — NOT that checkout's HEAD, which is 3fbdd4a2dae15ef79b414d74a1b68ba67e689afe on branch claude/focused-archimedes-wqa3gp, an unrelated branch\",\n    \"pin_commit\": \"fix(data-objectstack): lower an array analytics filter through parseFilterAST before the wire (objectui #7752/#7754), Sat Sep 5 2026\"\n  },\n  \"per_key_verdicts\": {\n    \"_method\": \"Whole-tree sweep at the pin: 21 files mention chartConfig, every one read. 19 hits in app-shell/src/views/ObjectView.tsx are a LOCAL VARIABLE bound to viewDef.chart (the view surface — a different key, the near-synonym trap); types/src/designer.ts and types/src/complex.ts are `chartConfig?: any` declarations; plugin-charts/normalizeChartSchema.ts is a comment. The only consumer of a DASHBOARD widget's chartConfig is plugin-dashboard/src/DatasetWidget.tsx -> @object-ui/core/src/utils/chart-presentation.ts. Objectstack-side sweep: packages/lint/src/validate-chart-bindings.ts reads xAxis.field / yAxis[].field / series[].name as a publish-time ADVISORY (chart-field-unknown, warning since it left the blocking class) — it does not change any top-level verdict, since all three keys are live on their presentation half regardless.\",\n    \"live_12\": {\n      \"title\": \"chart-presentation.ts#chartConfigPresentation — non-empty string lowered; ChartFrame draws it. Pin: 'draws chartConfig.title / .subtitle above the plot' + negative 'adds no title chrome when chartConfig declares none'.\",\n      \"subtitle\": \"same function, same guard, same DOM pin.\",\n      \"description\": \"same function; becomes the chart container's role=img accessible name. Pins: 'announces chartConfig.description as the chart graphic accessible name' / 'leaves the graphic unlabelled when no description is declared'.\",\n      \"colors\": \"same function, split two ways — string[] becomes the positional palette `colors`, a {value:color} record becomes `categoryColors` merged OVER the dimension field's own option colours. Pins in both directions.\",\n      \"height\": \"same function, guarded finite and > 0. Pins: 'applies chartConfig.height over the container default' / 'drops a non-positive or non-numeric height'.\",\n      \"showLegend\": \"same function. The FIRST key ever lowered on this face (#3135) and for a long time the only one — which is why the row's original 'bag forwarded' sentence was false. Pinned in both directions plus an absent-key pin.\",\n      \"showDataLabels\": \"same function; printed as a Recharts LabelList. Pin: 'forwards showDataLabels in both directions'.\",\n      \"annotations\": \"same function, non-empty array only; drawn as ReferenceLine/ReferenceArea. Pin: 'drops an empty annotations array instead of emitting a dead key'.\",\n      \"interaction\": \"same function, non-array object only; tooltip toggle plus Brush. Pin: 'forwards annotations and the interaction toggles'.\",\n      \"xAxis\": \"chart-presentation.ts#mergeAuthoredPresentation -> #axisPresentation. title/format/min/max/stepSize/showGridLines/position/logarithmic forwarded; `field` DROPPED. Pin: 'does not let an authored axis field reach the renderer'.\",\n      \"yAxis\": \"the same, per array entry, and the COUNT of entries is itself presentation (it declares the secondary axis) so every slot survives. Pins: 'declares two y-axes and binds each series to the authored one', 'keeps an axis slot that declared only its field'.\",\n      \"series\": \"#mergeAuthoredSeries -> #seriesPresentation. Matched by name against the derived dataKey, first entry wins; mark/axis-side/label/color/stack/variant/dashArray/opacity forwarded, `name` DROPPED. Pins: 'ignores an authored series naming a measure outside the selection', 'leaves a derived series with no authored entry untouched', 'lets a bar widget declare one line series'.\"\n    },\n    \"dead_2\": {\n      \"type\": \"NOTHING consumes it on this face. chartConfigPresentation has no `type` branch (the lowering is a whitelist and its docblock states the exclusion criterion: 'It does not fight the dataset derivation'). DatasetWidget.tsx picks chartType from CHART_TYPE_MAP[widgetType] — the WIDGET's own type — and the `...chartPresentation` spread that follows carries no type/chartType key. Pinned as a NEGATIVE by name: 'ignores chartConfig.type — the widget type owns the chart family'. ⚠️ Coordinate, not key name: the SAME ChartConfigSchema.type is live one metadata root over, on the react tier, where packages/spec/src/ui/react-blocks.ts publishes it in ObjectChart's dataProps. This row is dashboard/widgets.chartConfig.type and there it is dead. No per-face verdict vocabulary invented. ⚠️ It is also the ONE REQUIRED key of ChartConfigSchema, so every dashboard author who writes a chartConfig at all is made to write a key this face ignores.\",\n      \"aria\": \"NO reader on EITHER face — which is what separates it from `type`. chartConfigPresentation's docblock names it 'the one declared key with no reader at all': the chart implementation declares no aria prop and the ARIA injection reads the FLAT ariaLabel/ariaDescribedBy/role. react-blocks.ts lists thirteen dataProps for ObjectChart and aria is the one ChartConfigSchema key missing. Pinned as a negative from both spellings: 'ignores chartConfig.aria — no accessible name appears anywhere', 'ignores aria — nested and flattened'.\"\n    },\n    \"_not_acted_on\": \"Neither dead verdict is executed here. Recording a verdict feeds the ADR-0049 enforce-or-remove worklist; executing one moves a published accept set. `aria` has its own card (#17751, named by the 2026-09-12 ruling); `type` is inside half 2.\"\n  },\n  \"half_2\": {\n    \"measured\": \"STOPPED at the dispatch fence, as instructed.\",\n    \"reading\": \"Half 2 is already RULED — comment 5644017639, decision batch #121, 2026-09-12, maintainer verbatim 「同意」 to 1C+D. What it rules: the protocol states the ownership split AND refuses chartConfig.xAxis / yAxis / series and chartConfig.type BY NAME on a dataset-bound widget, with a `major` changeset and an ADR-0087 semantic migration that removes the keys from stored dataset widgets. That is a published accept set moving and a published contract moving — exactly the condition this dispatch names for stopping. ⇒ the seat's to re-declare, not this round's to land.\",\n    \"note_on_the_ruling_text\": \"Ruling item 5 says `Clause-②: no` (narrowing). Recorded as sent; not resolved here. A narrowing that removes three keys from a published authorable shape reads to me like the contract-review tier rather than below it, but that is the seat's declaration to make and I did not act on either reading — this PR moves no key in either direction and declares `Clause-②: no` on that basis alone.\"\n  },\n  \"ablation\": {\n    \"where\": \"on the COMMITTED tree; trap on a restore function, EXIT INT TERM with absolute paths seeded from git rev-parse --show-toplevel\",\n    \"dist_leg\": \"NONE — check-liveness.mts imports ../../src/kernel/metadata-type-schemas and reads liveness/*.json from source, so there is no dist to preflight. Verified by reading its import block, not assumed.\",\n    \"HEAD_blob\": \"a5fdd41fff249a2206773d274573bbd7365b68d4 (packages/spec/liveness/dashboard.json)\",\n    \"leg_A\": \"delete the drilled child `showDataLabels`. ON-DISK PROOF READ FIRST (occurrences, grep -o | wc -l, not grep -c): 1 -> 0; blob a1376c8c498045b91dc8abbb98b7f98333182db9 != HEAD blob. Gate exit 1: '✗ 1 UNCLASSIFIED — dashboard/widgets.chartConfig.showDataLabels'. THIS is the load-bearing leg: it proves the new rows are READ at depth two and are data, not prose wearing the shape of data.\",\n    \"leg_B\": \"strip the `objectui` realm marker off the `height` child's evidence. Blob 1e5d2786f1f61e12fc5674f914971ea633954854 != HEAD. Gate exit 1: 4 entries cite a file missing from THIS repo. Proves the evidence strings are really read and realm-attributed at this depth.\",\n    \"restore\": \"git checkout HEAD double-dash ABSOLUTE_PATH (never a bare checkout), then blob compared against the HEAD blob and an empty-hash guard that exits non-zero. All three legs: restored blob == a5fdd41f, and git diff HEAD named 0 differing paths. Never inferred from an exit code.\",\n    \"control\": \"unmutated committed tree, gate exit 0.\",\n    \"direction\": \"predicted 'turns red' both legs; observed 'turns red' both legs.\"\n  },\n  \"changeset_decision\": {\n    \"verdict\": \"REQUIRED — .changeset/17385-chartconfig-liveness-drill.md, @objectstack/spec patch\",\n    \"method\": \"built packages/spec FIRST (os-verify-lock VERDICT command-exit 0, 171s), confirmed both build passes finished, then npm pack --dry-run --json on the package (2012 files in the tarball)\",\n    \"positive_control\": \"dist/index.js SHIPS\",\n    \"negative_control\": \"scripts/liveness/check-liveness.mts does NOT ship; 0 files under scripts/\",\n    \"result\": \"3 of the 4 originally-changed paths ship: liveness/dashboard.json, liveness/README.md, liveness/state-counts.md (38 files under liveness/ — packages/spec files[] lists `liveness` whole). The undrilled-containers baseline does not. A second positive fell out of the same read: files[] also carries src/**/*.zod.ts, so spec ships its Zod sources too.\",\n    \"not_assumed\": \"skip-changeset was NOT available: the ledger text reaches a published tarball.\"\n  },\n  \"verification\": {\n    \"check_liveness\": \"BEFORE 0, AFTER 0. Verdict line quoted, not $?: '✓ every governed-type property, at every depth the ledger drills, is classified ... every container inheritance is declared ...'. Container coverage moved 109 containers / 555 child keys -> 114 / 580.\",\n    \"spec_test_local\": \"pnpm --filter @objectstack/spec test (= vitest run --project local) — os-verify-lock VERDICT command-exit 0. 472 test files, 13434 tests, all pass.\",\n    \"spec_test_repo\": \"pnpm --filter @objectstack/spec test:repo (= vitest run --project repo) — SEPARATE project, RUN, exit 0. 30 test files, 520 tests, all pass. Both ran in one lock acquisition (VERDICT command-exit 0, held 811s, waited 66s).\",\n    \"skip_vs_pass\": \"packages/spec/dist was BUILT before any of these numbers were taken, so nothing skipped itself on a missing dist. The numbers reported above are the post-build run.\",\n    \"spec_typecheck\": \"exit 0, including check:scripts-typecheck and check:test-typecheck (spec's test layer compiles under tsconfig.test.json; 54 files / 259 errors / 144 pinned signatures held, shrink-only ledger unchanged).\",\n    \"check_generated\": \"pnpm --filter @objectstack/spec check:generated — exit 0, all 15 generated artifacts up to date (state-counts.md regenerated with gen:liveness-counts, never hand-patched).\",\n    \"react_declaration_parity\": \"TRIED rather than recorded as unavailable, per the claim comment's correction — and the claim comment is RIGHT: sdui.manifest.json IS tracked at the repo root (git ls-files --error-unmatch exit 0). First attempt with a RELATIVE MANIFEST failed (the filter runs with cwd=packages/spec); MANIFEST=\\\"$PWD/sdui.manifest.json\\\" ... --strict exits 0, no new declaration divergence vs baseline. ⚠️ check:generated's own prose still says nothing in this repo can produce the manifest — stale, and already filed as #17405.\",\n    \"derived_gates\": \"node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack. Derived TWICE: 4 paths / 53 commands before the changeset existed, 6 paths / 67 after (the tool said so itself: '9 families apply once this card's changeset exists'). All 67 run, each exit captured BEFORE any pipe (cmd > log 2>&1; E=$?). 64 exit 0.\",\n    \"NOT_MEASURED\": \"3 of 67, all exit 3 = PREREQUISITE NOT MET, reported as NOT MEASURED and NOT green: check:dual-build-cjs-loads (87 packages have no dist), check:lean-entry-closure (@objectstack/objectql/core entry absent), check:type-check-debt --re-measure (31 workspace deps unbuilt). DECLARED NARROWING, with the argument: all three read BUILT output, the `...` closure of @objectstack/spec is EMPTY (pnpm --filter '@objectstack/spec^...' build printed 'No projects matched the filters' — a vacuous run, reported as vacuous), and satisfying them needs a WHOLE-REPO build, not this package's closure. This diff contains zero compiled input — five data/doc files plus one test file that no dist carries. The one half of check:type-check-debt my diff COULD move is spec's own test layer, and that was measured green by check:test-typecheck above with a correctly-built closure. CI builds the farm and measures all three.\",\n    \"eslint\": \"Ran over the WHOLE repo rather than arguing a narrowing: eslint --no-inline-config --format json . reports 6655 files in eslint's own scope. Of the six changed paths, only check-liveness.test.ts is in eslint's configuration at all (0 messages); the five data/doc files each report 'File ignored because no matching configuration was supplied.' eslint.config.mjs records no parserOptions.project and no typed rules (line 328 states it), so this diff cannot move any untouched file's verdict either way.\",\n    \"control_bytes\": \"node scripts/check-nul-bytes.mjs exit 0; plus a self-sweep beyond the gate — grep -naP over all changed files for [\\\\x00-\\\\x08\\\\x0b\\\\x0c\\\\x0e-\\\\x1f\\\\x7f]: 0 hits.\",\n    \"commit_hygiene\": \"Both commit messages swept SEPARATELY for the bare stems (fix/fixes/fixed/close/closes/closed/resolve/resolves/resolved/part of/refs, word-boundary, case-insensitive) and for #digits, each sweep run against a LIT CONTROL file that contains the pattern (stems lit=1 each, #digits lit=2) to prove the sweep reaches. Both messages: 0 for every stem, 0 for #digits, no model identifier. Trailers exactly the two model-free lines, confirmed by reading them back out of git log.\",\n    \"pr_body_readback\": \"Sent 11171 bytes, stored 11170 — the ONLY difference is the stripped trailing newline; every line byte-identical. Footer count in the stored body: 1 (the session-URL form survived verbatim; nothing was appended). Card relations in the stored body: exactly ['Part of #17385']. Tag-shaped fragments in the stored body: none — the git-show PIN colon PATH spelling was rewritten to a placeholder spelling BEFORE sending, precisely so the sanitizer had nothing to eat. Clause line stored intact and starts with '**'.\",\n    \"queue_posture\": \"Draft. Not flipped ready, not enqueued, no auto-merge. Clause-② no ⇒ no needs:contract-review label; no other label is the dev's to apply here (documentation/size/skip-changeset are the labeler's).\"\n  },\n  \"mcp_calls\": \"0 — zero MCP GitHub calls for the whole run. The card and all 5 comments, the open-PR enumeration, the PR creation and the body read-back went through container REST; every repo and objectui read went through git.\",\n  \"out_of_scope_findings\": [\n    \"noted, not filed: the drill surfaced SIX containers one level further down — dashboard/widgets.chartConfig.{xAxis,yAxis,series,annotations,interaction,aria}, 39 child keys. RECORDED in the shrink-only baseline rather than drilled, because drilling is evidence work and fanning this row's verdicts down over them is the one move the drill rule forbids by name. Three of the six are the sharp ones: xAxis/yAxis/series are `live` as a whole while one of their OWN sub-keys is inert by design, and axisPresentation's docblock says normalizeChartSchema then narrows further ('keeps format/title/showGridLines on the x-axis and the full set on the y-axes'), so the x-axis sub-keys almost certainly diverge from the y-axis ones. Carrier: the baseline file itself — the row is the worklist entry, greppable and counted — plus #17385's own residue, which is where the next drill naturally attaches.\",\n    \"noted, not filed: the liveness README's dashboard Notes cell had ALREADY drifted one `dead` behind the generated artifact before this change — the cell's last stated position is '34/7' (#6774) and the artifact read 34/8 at origin/main. Recorded inside the cell rather than silently corrected, since the count columns are generated and are the authority. Carrier: that cell, which this PR extends.\",\n    \"noted, not filed: #17424's two pins named a CARD's coordinate (dashboard/widgets.chartConfig) as their worked example, so they went stale the moment the card they were written for landed — and one of them went stale QUIETLY in the worse direction: the counts assertion did not merely fail, it had started grading the fixture instead of the walk. Re-pointed at the SHAPE here. Carrier: the two pins in check-liveness.test.ts, now carrying a comment that says why the subject must be a container the shipped ledger has not drilled.\",\n    \"noted, not filed: chartConfig.type is the ONE REQUIRED key of ChartConfigSchema, so an author cannot write a dashboard chartConfig at all without writing a key this face silently ignores. That is the (c) metadata-authoring-trap shape, and it is ALREADY carried — the 2026-09-12 ruling refuses `type` on a dataset-bound widget by name with an ADR-0087 migration. Carrier: #17385's protocol half. Filed nowhere new, deliberately: a second card would duplicate a ruled one.\",\n    \"noted, not filed: check:generated's prose for check:react-declaration-parity still says the manifest 'exists only after objectui is built at .objectui-sha and enumerated in a real browser — nothing in this repo can hand it one', while sdui.manifest.json is tracked at the repo root and the gate's own failure text says the opposite and prints the working invocation. Two prose sites in one repo disagreeing about the same gate. Carrier: #17405, which the claim comment names as already filed for exactly this.\"\n  ],\n  \"open_questions\": [\n    {\n      \"question\": \"The claim comment and this dispatch disagree with the standing agent contract about PR-body attribution. The claim comment says 'session id in body PROSE, not a footer — both MCP and raw REST append their own'; the agent contract says the PR body ends with the session-URL footer form. I followed the agent contract (conflict resolution: the contract file wins) and MEASURED the outcome: sent via container REST, stored byte-identical, exactly ONE footer, nothing appended. So on THIS channel the claim comment's stated reason did not hold.\",\n      \"options\": [\"A — keep the footer for REST-created PR bodies (measured safe today)\", \"B — prose-only, per the claim comment\"],\n      \"recommendation\": \"A for this channel, with the reading recorded rather than generalised: AGENTS.md is explicit that the cells disagree per channel and per action, so one green create through REST says nothing about an MCP create or about an edit.\"\n    },\n    {\n      \"question\": \"Should the six newly recorded depth-3 containers be drilled next, and where does that card hang? They are the direct continuation of this one and the evidence is partly already in hand (axisPresentation and seriesPresentation are literal whitelists), but the x-axis / y-axis divergence needs a read of normalizeChartSchema in the renderer, which is a real measurement rather than bookkeeping.\",\n      \"options\": [\"A — a follow-on drill card, Blocked-by nothing (the gate already recurses)\", \"B — fold into the half-2 PR, since it refuses xAxis/yAxis/series anyway\", \"C — leave recorded; the baseline is the worklist\"],\n      \"recommendation\": \"A, but AFTER half 2 lands. B is wrong for a mechanical reason: if half 2 removes xAxis/yAxis/series from dataset widgets, three of the six containers may leave the walked shape entirely (or become retiredKey tombstones with rows that must STAY), so drilling them first would produce rows whose disposition the very next PR rewrites. C alone understates it — the recorded row is honest but it is debt, not an answer.\"\n    }\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5646798571,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-12T15:23:19Z","updated_at":"2026-09-12T15:23:19Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17385#issuecomment-5646798571","body":"## Half 2 is ruled, and it is **held** — on the same unanswered question that parked PR #17835, ⛔ not on anything this seat can clear\n\n`domain:spec` execution seat, 2026-09-12T15:25Z. ⛔ No label change; recorded so the next dispatcher does not re-derive it, and so the delay is not mistaken for a grading doubt.\n\nThe #17861 round stopped at the fence this seat set, and in doing so found what the dispatch had missed: **half 2 is already ruled** — comment `5644017639`, decision batch #121, 2026-09-12, carrying the maintainer's verbatim 「同意」 to `1C+D`.\n\nI read that ruling myself rather than taking the round's summary. Its item 3, verbatim:\n\n> 3. `major` changeset with an ADR-0087 semantic migration entry: the three keys are removed from stored dataset widgets …\n\n### ⇒ dispatching it today produces a second PR parked on the same wall\n\n`scripts/check-changeset-no-major.mjs` is the launch-window guard, and a `major` changeset makes it **exit 1**. That is exactly what happened to **PR #17835** (card #16929) this morning: its ruling also fixed the grade at `major`, the guard refused it, and the card went to the decision box because the escape the guard names (`allow-major`) would make that PR **the one that cuts 18.0.0** — roughly 70 packages to `18.0.0`, `PROTOCOL_VERSION` to 18, and 24 other pending protocol-18 conversions activated.\n\n⇒ half 2 hits the identical guard for the identical reason. Dispatching it now would spend a round to produce a second draft PR sitting behind **the same A/B** the maintainer has not answered. ⭐ This is the one shape where holding a card is right: not \"I have not looked\", but **measured, named, and pointing at a specific open question**.\n\n⇒ half 2 waits on **#16929's A/B** (「要不要现在切 18.0.0」). When that is answered, both move together.\n\n### ⚠️ One thing for whoever dispatches half 2, recorded not resolved\n\nRuling item 5 declares `Clause-②: no` (narrowing). The #17861 round flagged — and I agree it is worth a second look — that **removing three keys from a published authorable shape** is the kind of change this repo has been sending to the contract-review tier. ⛔ Not this seat's to overturn: a ruling declared the value in advance, and today's other correction (PR #17811) went the *other* way for exactly that reason. ⇒ the dispatching seat should re-read item 5 against the diff that actually lands and re-declare **on the card** if the measurement disagrees — ⛔ the dev never does.\n\n### What half 1 left standing\n\nPR #17861 carries `Part of #17385`, ⛔ not a closing keyword, so this card stays open for half 2. The drill itself is done: 14 per-key verdicts (12 live, 2 dead — `type` and `aria`), neither dead verdict executed, because recording a verdict feeds the ADR-0049 worklist while executing one moves a published accept set.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5647268405,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-12T16:44:43Z","updated_at":"2026-09-12T16:44:43Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17385#issuecomment-5647268405","body":"## 半 1 已落地并按内容核实;半 2 转 `pm:blocked`\n\n`domain:spec` 执行席,session `session_01MkQhmuuJAVDjmeWNixwDDH`,核实取数 2026-09-12T16:45Z。\n\n**PR #17861 已合并**(`Part of #17385`,⛔ 故意不用关闭关键字)。核实读的是 `origin/main`,先 `git fetch`,⛔ 不是共享检出的工作树:\n\n- `packages/spec/liveness/dashboard.json` 的 `/props/widgets/children/chartConfig` 现为 `status: live`,`verifiedAt: 2026-09-12`,`evidenceScope: cross-repo`,证据点名 objectui `@53ded82bf` 的 `packages/core/src/utils/chart-presentation.ts#chartConfigPresentation` 把**九个 chrome 键**降下去。\n- 点亮对照:同一份 JSON 里 `showLegend` 读到 4 次(确实存在),伪 token `chartConfigZZZ` 读到 0 次(对照是暗的,所以上面的非零是读数)。\n- `.changeset/17385-chartconfig-liveness-drill.md` 已在 `origin/main` 上。\n\n⭐ **本卡的前提被这一轮改掉了,记录在案**:立卡与本席派发令都写着「objectui 里 `chartConfig` 读 0 次,只有 `showLegend` 被读」。实测不是 —— 它是 `live`,九个 chrome 键都被降。⇒ **这一半的产出是把一个错的台账行改对,而不是把一个死行清掉。**\n\n**剩下的半 2(协议要不要声明这一层)转 `pm:blocked`**,上游是 **#16929**(`major` 撞发布窗口守卫的 A/B,仍在维护者决策箱)。同一笔写入:`pm:dispatched` → `pm:blocked`,正文加机器可读的 `Blocked-by: #16929` 行,并**释放 assignee** —— ⛔ 不静默摘:释放的理由是在飞轮次已完成并交付,剩余半没有在任持有者,#16929 关闭时由解锁扫描把本卡放回队列,那时按全套认领协议重新认领。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
