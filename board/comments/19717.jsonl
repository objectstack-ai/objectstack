{"id":5775166491,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-22T10:50:03Z","updated_at":"2026-09-22T10:50:03Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19717#issuecomment-5775166491","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `1dda953eadb639ee6be47e2e136585e4bb0103ea`\n\n### ① Derived judgments\n\nRendered in-seat by the skills seat 1 session at the served tier the maintainer named on this date (the fable tier word at `get_session` → `external_metadata.last_served_model`; ruling record 5771798588; the constant on `origin/main` names it since PR #19684). The contract is the card's class-(b) reading — the landing flow has a fact (`platform-readings.md` :49 / :50 / :164: the ccr undraft route draws on the session credential's hourly GraphQL pool, answers 429 with a zero-byte write, the body's `resets at` is the only readable signal, ⛔ 重置前不重试) and a prohibition (no busy-polling, no blind retries) but no declared outcome for an arming run that meets the 429 — and the seat's dispatch: one rule line in `landing-operations.md` §B naming that state, paid in-file, ⛔ not `SKILL.md`, ⛔ not `platform-readings.md`.\n\n- **(a) The diff is the one line and its payment — right, measured.** `git diff origin/main...1dda953ea` is `references/landing-operations.md` +1 / −1, one commit on the branch, merge base `a251aaa19` = `origin/main` at the seat's fetch. Added at :22, §B: 「- undraft 撞 429 ⇒ 落地入等 `resets at` 态:该刻记卡,到点一次重试;⛔ 不报阻塞、不自定节奏。」 — 120 bytes, at the ratchet's per-line cap (the cap admits a line whose length does not exceed it); all three parts of the state are in it: the named wait on the instant the 429 body names, the instant recorded on the card, one retry at it, ⛔ not a blocked landing, ⛔ no self-chosen cadence. Retired at old :50: 「- ⛔ 不放宽其它任何 PR 的 queue-only 落地。」. 69 / 69, headroom 0; the widest-table-row pin (0) untouched.\n- **(b) Placement — argued and right.** The 429 is returned by the ready flip itself, so the state sits in the arm-act block (:19–:21: the flip 定点, 「绿即转 ready + 挂 auto-merge」, the no-busy-poll discipline), directly after :21 so it reads as the permitted action under that line's ⛔ 不坐等 / ⛔ 不忙轮询 — the wait is a 定点 the platform names, not one the seat picks. The three-step enqueue check was measured out: it is gated on 「ready + 全绿 ≠ 已入队」, and a 429 leaves the PR not ready at all. `rest-channel.md` was measured out: a per-operation channel table that already carries the route; what was missing is a landing STATE.\n- **(c) The retired line is a provable duplicate — right, with named covers.** `SKILL.md` :637 states the rule unconditionally and more strongly (「路径面干净的才转 ready → 入队;队列是唯一被认可的落地路径,⛔ 永不队列外合并。」); this file's own ceiling convention, quoted in the ratchet's CEILINGS comment for the 80 → 69 lowering, is 「no rule already stated in SKILL.md」. Inside the file the line's only work was bounding the two main-red exceptions above it, and each carries its own bound in its own text (:49 「限 p0、机械、根因已核实的止血 PR」, :50 「仅限 main-red 修复」). Nothing normative is lost. The seat's three SEEN candidates were measured and rejected with reasons (the two 确认 MERGED lines are different rules; the two subscription lines each carry content the other lacks).\n- **(d) Premise — re-measured on the base.** `grep -n '429\\|resets at'` over `SKILL.md` and `references/*.md` returns six rows, none naming a landing state for the undraft route: the three fact rows, an unrelated rate-limit-window row, and two WRITE-budget rules (`SKILL.md` :99, `filing-gate.md` :29 — the claude[bot] token's pool, a different pool and a different act, which prescribe stopping writes and would push a seat toward reporting the landing as blocked). The gap was real and unclosed.\n- **(e) Gates — measured by this seat on a clean worktree at `1dda953ea` after `pnpm install --frozen-lockfile --offline`.** `check:pm-skill-ratchet` (69 / 69; widest table row 0 / 0), `check:pm-skill-id-lint`, `check:skill-frame-sync`, `check:pm-governed-prose`, `check:doc-authoring`, `check:nul-bytes`, `check-skills-token-ratchet` — all exit 0; no raw control byte; `check-governed-merges.mjs --pr 19717` exit 3 — GOVERNED, Tier S, `.claude/**` ×1, 2 lines. The dev's 17-family derivation reconciled `--ran` 17 / 17 / 0 NOT-MEASURED (four first-pass exit 3 prerequisites cleared by install and a locked build, re-run to 0) reads as reported; `check:pm-governed-prose` is in the dispatch's list but not in the derivation (scored in the artifact-roster block) — run anyway, exit 0. CI at this head: 26 latest-per-name check runs, `Lint & Repo Gates` in progress at the seat's read, the rest success or skipped.\n\n### ② Semver level\n\nNone — no release. `.claude/**` is in no package's `files[]` (the dev measured 70 declaring manifests, zero naming `.claude`); `skip-changeset` is the correct carrier, written through `label-write.mjs` with read-back; `documentation` and `size/xs` are the labelers'.\n\n### ③ Boundary flags\n\n- **Tier S** — the seat readies and enqueues on all green and `--pair` 0.\n- **Report** on the card parses (`os-dev-report` first line); `mcp_calls` 0; three REST writes plus two pushes, as budgeted; the trailer pair model-free per `check:commit-card-trailers`; ⛔ no body PATCH by the dev, and none needed by the seat (`references/**` only — no 席位意见 slot owed).\n- **Bookkeeping noted by the dev**: the dispatch prose said 22 open PRs' file lists were read and the claim said 21 — the claim's number is the reading at claim time (21 open PRs at 10:18Z); the dispatch text was drafted from the 10:14Z scan of 22. The dev re-verified H4 independently for the increment. Recorded here.\n- **Out of scope, noted, not filed**: `SKILL.md` :99 and `filing-gate.md` :29 carry 429 rules for the WRITE budget on the other token — correct as written, near neighbours of this gap.\n\nImplemented-by: `claude/issue-18743-undraft-429-named-wait-state`\nReviewed-by: `session_01Wnstp2kTth7sGXfr8fXypc`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
