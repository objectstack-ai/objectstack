{"id":5132431831,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-07-30T14:55:02Z","updated_at":"2026-07-30T14:55:02Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/4181#issuecomment-5132431831","body":"补一条会显著改变这个 issue 工作量的发现:**处方已经存在,就在同一个文件的兄弟路由里。**\n\n`GET /data/:object/export` 对同一个畸形输入早就是大声拒绝的 —— `packages/rest/src/rest-server.ts:4730`:\n\n```ts\nlet filter: any = undefined;\nif (typeof q.filter === 'string' && q.filter.length > 0) {\n    try { filter = JSON.parse(q.filter); }\n    catch {\n        res.status(400).json({ code: 'INVALID_REQUEST', error: 'filter must be JSON' });\n        return;\n    }\n}\n```\n\n所以这不是一个「要定语义」的问题,而是**同一个端点家族里两条路由对同一份畸形输入给出相反答案**:\n\n| 路由 | `?filter={status:done`(垃圾 JSON) |\n|:---|:---|\n| `GET /data/:object/export` | `400 INVALID_REQUEST` — \"filter must be JSON\" ✅ |\n| `GET /data/:object`(list) | `200` + **未过滤整页** ❌ |\n\n而且方向是反直觉的:被硬化的是 export(一次导出,调用方看得见结果),没硬化的是 list(常被程序化消费,多返回最不容易被发现)。\n\n实现建议就是把这段守卫搬到归一化层(`protocol.ts` 的 filter alias 链),让两条路由共用一个答案。错误码直接复用 `INVALID_REQUEST` —— 它已在 `@objectstack/rest` 的 ledger 里注册,不需要新码。搬到 protocol 层而不是 list 路由,顺带覆盖 `POST /data/:object/query` 与 runtime dispatcher 那条路径。\n\n## 同一段代码里的小弟(同病,低危)\n\n紧邻的 `orderby` 解析(`rest-server.ts:4745`)在 JSON.parse 失败时静默 `undefined`:\n\n```ts\ntry { orderby = JSON.parse(q.orderby); } catch { /* leave undefined */ }\n```\n\n排序被丢弃不改变结果集(仍是诚实的全集,只是顺序不是要的那个),所以危害远低于 filter;但如果调用方靠 `orderby` + `top` 取「最新 N 条」,拿到的就是任意 N 条。修 filter 时顺手一起处理比较合算。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
