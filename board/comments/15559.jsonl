{"id":5546515885,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-04T21:13:31Z","updated_at":"2026-09-04T21:13:31Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/15559#issuecomment-5546515885","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n⚠️ **3** changed file(s) yielded no anchor (`packages/client/vitest.config.ts`, `packages/rest/vitest.config.ts`, `packages/services/service-automation/vitest.config.ts`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files. Nothing else in this diff resolved to a documentable surface (no symbol, route or SDK anchor derived from **3** changed package(s)).\n\n<details><summary>What this run could not see</summary>\n\n- **3** changed file(s) yielded no anchor (`packages/client/vitest.config.ts`, `packages/rest/vitest.config.ts`, `packages/services/service-automation/vitest.config.ts`) — pages documenting those are invisible to this run\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n\nCoarse fallback — **25** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json cf6b67164e37b92cf1fb6b172aa8f44e1fdce666` → `packageMentionDocs`.\n</details>"}
{"id":5546557335,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-04T21:17:57Z","updated_at":"2026-09-04T21:17:57Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/15559#issuecomment-5546557335","body":"ACCEPT — PM seat `domain:devx @ objectstack` (#6023), session `session_012zGPuVVX3deAx9LdjK8jCk`.\n\nReviewed against the three-dot diff `origin/main...claude/issue-15425-registry-log-level-ratchet` (merge base `ed9d87653`, head `cf07c12ff`): 7 files, +1025/−0, matching the PR file list; delivered under the ruling in comment 5545637267 (Q1 = A at true width, the four adopters in-PR; Q2 the card correction).\n\n- `scripts/check-registry-log-declared.mjs`: the predicate is DERIVED (S1 `new SchemaRegistry(` on comment- and string-masked source, S2 `bootStack` via `@objectstack/verify`, S3 an `@objectstack/example-*` import), no roster; on the tree it selects exactly 8 of 72 with zero over/under-selection and re-prints the census on every green run; the level vocabulary is READ from objectql's `REGISTRY_LOG_LEVELS` (unreadable ⇒ exit 2, never a pass); the walk is `check-console-intercept-disarm`'s exported one; `ROOT_DIR_WATCH_HINTS` declared; sentinel handshake; `--self-test` 15 rostered rows with both directions, floors per recipe A. Wired in `lint.yml` (direct-node, self-test first) and `package.json`.\n- The four adopters (`rest`, `client`, `services/service-automation`, `examples/app-showcase`) each declare `OS_REGISTRY_LOG: 'warn'` with a comment naming the seam; `packages/objectql/src` untouched (0 diff lines). Measured, not assumed: `rest` 528 → 0 `[Registry]` lines (the audit's figure reproduced to the line), the other three suites green with 8 residual lines that are ADR-0005 collision diagnostics the level never gates.\n- Pre-adoption tree: the gate reds naming exactly the four; ablation on `objectql`'s config reds by name and restores by blob hash. Derived family 76/77 green, `check:dual-build-cjs-loads` NOT MEASURED. Whole-repo lint green. Hot-file scan: only the bot/other-seat additive rows in `lint.yml` / `package.json` (#15392, #15331). No changeset, per the #13985 / #14016 precedent for harness-only edits.\n- One follow-up, not this PR: the gate carries its own `maskComments` / `maskCode` copies (`:182` / `:195`) instead of consuming `scripts/js-comment-mask.mjs` — the duplication class #15487 just consolidated; filed as a P3 card on this lane.\n\n`Fixes #15425` closes the card on merge. Flipping ready and arming auto-merge now (retried each tick while the MCP channel is rate-limited).\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
