{"id":5747858195,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-20T05:25:15Z","updated_at":"2026-09-20T05:54:15Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19253#issuecomment-5747858195","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**1** anchor(s) derived from **1** changed package(s); no hand-written page names any of them, so this run has **nothing to list** — **not a clean bill of health**. This check sees only pages that NAME a derived anchor: one that documents this change in prose, or enumerates it in an authoring dialect, names none and stays invisible to it on every run.\n\n<details><summary>What this run could not see</summary>\n\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **136** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 7056ca50d1d94c596fc7d56a58e758b5339062e5` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `ed16912ba16318c966e5ce69b6f436aa536c63ce` — the merge of head `6a34e6f67a22478036bbe674a7089a5b981192bf` into base `7056ca50d1d94c596fc7d56a58e758b5339062e5`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin ed16912ba16318c966e5ce69b6f436aa536c63ce && git checkout ed16912ba16318c966e5ce69b6f436aa536c63ce\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 7056ca50d1d94c596fc7d56a58e758b5339062e5 6a34e6f67a22478036bbe674a7089a5b981192bf && git checkout -B drift-repro 7056ca50d1d94c596fc7d56a58e758b5339062e5 && git merge --no-ff 6a34e6f67a22478036bbe674a7089a5b981192bf\n\nnode scripts/docs-audit/affected-docs.mjs --json 7056ca50d1d94c596fc7d56a58e758b5339062e5\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5747932013,"author":{"login":"os-steve","id":317605050,"type":"User"},"created_at":"2026-09-20T05:43:15Z","updated_at":"2026-09-20T05:43:15Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19253#issuecomment-5747932013","body":"## Contract review\n\nReviewed at 2026-09-20T05:43Z by the isolated at-tier review subagent for the `domain:spec` lane, against primary sources only: the PR diff at its head, `../objectui` at the `.objectui-sha` pin, and the repo's own gates. Nothing below is taken from the PR body or the implementer's report; where their numbers are mentioned it is to say whether mine reproduce them.\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `cee5969dee6f732c910099282107d981f8e1f8a1`\n\nTrees the readings were taken against: objectstack head `cee5969dee6f732c910099282107d981f8e1f8a1` (base `1b82c519df9db93891514b814b4d8e16e42d5d5c` = `origin/main`), detached worktree; objectui pin `53ded82bf7a494f54e344e19099dbf00854b8694` (the exact content of `.objectui-sha` at the head), detached worktree. Every exit code below was captured before any pipe.\n\n### ① Derived judgments\n\n**J1 — BLOCKING. The new `'auto'` clause is false in the other direction at the pin: `repeater` also resolves to the full column count.**\n\nThe sentence under review asserts that at the pin \"**only** the long-form types (textarea, markdown, html, richtext) resolve to the full column count\". I re-derived the wide set by **executing** the pin's own code over every member of the spec's `FieldType` enum, along the path the runtime form actually takes:\n\n- Population: `FieldType.options` executed from `packages/spec/src/data/field.zod.ts` at the head = **49** members (second instrument, token count of the enum literal in source text = 49, 49 unique). Membership: `json` yes, `file` yes, `repeater` yes, `subform` no, `grid` no.\n- Instruments, all read out of the objectui worktree at the pin and executed unmodified: `plugin-form/src/autoLayout.ts` (`isWideFieldType`, `resolveColSpan`, `CONTAINER_GRID_COLS`), `plugin-detail/src/autoLayout.ts` (`isWideFieldType`), `fields/src/field-type-alias.ts` (`mapFieldTypeToFormType`, with only its `@object-ui/core` tombstone-reporter import stubbed to `false`), and `components/src/renderers/form/form.tsx:2472-2521` (`pickSpanClass`, extracted verbatim by line range, sha256 `917a686a09818e3d…`).\n- **Bare path** — `isWideFieldType(specName)` — wide set at grid 3: `{html, markdown, richtext, textarea}`. This is what objectui's own pin test (`autoLayout.wideSpelling.test.ts`, \"its spec-facing surface is EXACTLY the long-form family\") measures, and it is what the implementer measured. It reproduces.\n- **Runtime path** — `isWideFieldType(mapFieldTypeToFormType(specName))` — wide set at grid 3: `{html, markdown, repeater, richtext, textarea}`. `repeater` maps to `field:grid` (`field-type-alias.ts:102` at the pin), `field:grid` is a member of `WIDE_FIELD_TYPES` (`autoLayout.ts:61`), so `resolveColSpan({type: 'field:grid'}, 3)` = **3**, at grid 4 = **4**, class emitted `@2xl:col-span-3`. The detail twin agrees (`plugin-detail` `isWideFieldType('field:grid')` = true). With `multiple: true` the set is unchanged (the only arity move is `select` to `field:multiselect`).\n- The runtime path is the one the form walks, not a hypothetical: every non-test site that assigns `FormField.type` at the pin goes through the mapper first — `plugin-form/src/ObjectForm.tsx:732`, `plugin-form/src/sectionFields.ts:122` (`fromObjectSchema`) and `sectionFields.ts:207` (an authored form-row `type` override, `rawType`, is mapped too) — and the mapped list is what `applyAutoColSpan` receives (`ObjectForm.tsx:1330`, `ModalForm.tsx:670/765`, `DrawerForm.tsx:665`, `SplitForm.tsx:398`, `TabbedForm.tsx:475`, `WizardForm.tsx:1045`). `field:grid` is a registered, rendered form widget (`fields/src/index.tsx:2794`, `GridField`), and nothing in `plugin-form` filters it out of a form: the only form-side exclusions are `formula`/`summary`/`autonumber` and the system field names.\n- `repeater` is authorable on this key: `FormFieldSchema.safeParse({ field: 'a', type: 'repeater' })` at the head is **ACCEPTED** (span defaults to `auto`). So an author who reads the new sentence and lays out `{ field: 'items', type: 'repeater' }` expecting one cell gets the whole column count at the pin — the card's defect class with the sign flipped, which is exactly what this card exists to end.\n- The pin says this itself in prose, in a file the implementer counted: `app-shell/src/views/studio-design/formFieldType.ts:36-40` at the pin — \"a `repeater` (a spec `FieldType` that resolves to the WIDE `field:grid` widget) matched nothing: the canvas laid a repeater out at normal width while the runtime form spanned it full-row. Any future wide widget whose spec name differs from its widget id falls in the same hole.\" The PR's reasoning that `grid` \"is unwritable on this key\" is right for the literal `grid` (`type: 'grid'` is REFUSED at the head) and does not carry to `repeater`, which is the spec spelling that reaches the same widget.\n\nControls for the zeros in this section: `json` maps to `field:code` and `file` to `field:file`, both `resolveColSpan` = 1 at grid 3 and 4, no class emitted; `subform` is not a `FieldType`, maps through the `field:text` tail, = 1; `text` = 1; firing control `textarea` = 3. Re-taking the implementer's three-site literal counts at the pin: `json`/`file`/`subform` = 0/0/0 in `plugin-form/src/autoLayout.ts`, `plugin-detail/src/autoLayout.ts`, `app-shell/.../formFieldType.ts`; `textarea` = 2/2/0 in the same files — that shape reproduces — and `repeater` = 0/0/**2**, `grid` = 2/2/0 in the same three files, which is the hole: the third file names the type that the first two widen under another spelling. `subform` as a whole word across `packages/*/src` (ts/tsx, tests excluded) at the pin = 4 hits, all comments (`MetadataProvider.tsx:489`, `data-objectstack/src/index.ts:3420`, `GridField.tsx:71`, `:1212`); firing control `richtext` in the same radius = 35 files.\n\nWhat this does and does not say about the fix: the sentence needs to name `repeater` beside the four long-form types (or to stop saying \"only\"), and the sweep behind it must run along the runtime path (`mapFieldTypeToFormType` first). It needs **no** change to `WIDE_FIELD_TYPES` or any renderer file — triage's fence holds either way. I did not repair it.\n\n**J2 — PASS. The tier sentence is true at the pin and does not over-claim.** Executed: `CONTAINER_GRID_COLS[3]` = `grid gap-4 grid-cols-1 @md:grid-cols-2 @2xl:grid-cols-3`; `pickSpanClass(3)` against it = `@2xl:col-span-3` and nothing else; so at the `@md` tier the grid is two columns and a wide field carries no span class — one cell of two. Grid 2 emits `@md:col-span-2`, grid 4 emits `@4xl:col-span-4` only (one of two at `@md`, one of three at `@2xl`), so \"only the widest tier's class is emitted\" is the general rule, not just the 3-column example. `span: 'full'` on a `field:text` at grid 3 = 3, `@2xl:col-span-3` — same emission. The viewport numbers (390/720/1700, \"one of two at the 720px modal width\") are inherited text unchanged by this PR; I verified them against the code rather than a browser: the default modal is `sm:max-w-lg` (32rem = 512px, `ModalForm.tsx:205`), Tailwind is v4 (`^4.2.1`/`^4.3.3`) with no container-breakpoint override in the theme css, so the `@2xl` container tier (42rem = 672px) cannot engage inside the default modal and the `@md` tier (28rem = 448px) governs — one of two. This is the understatement direction at worst; nothing here over-claims. The forward-looking claim in the changeset about objectui `bd09957380` matches that commit's own title at objectui: \"fix(form): emit one col-span class per breakpoint tier, not one for the widest (#9253)\".\n\n**J3 — PASS. The pin guard guards; the control leg proves the conversion bought something.** `check-objectui-pin-citations.ts --self-test` exit 0. Baseline at the head: exit 0, \"27 asserting objectui pin citation(s) match .objectui-sha (53ded82bf), 45 historical citation(s) recorded and not checked, across 1483 spec source(s)\". Guard leg: the describe's asserting sha mutated to `1a2b3c4d5e6` (1 occurrence replaced, blob `44e35eb24abc…`) — exit **1**, \"packages/spec/src/ui/view.zod.ts:2925 cites `1a2b3c4d5e6`\". Control leg: the same wrong sha in the historical spelling (blob `2f032b56dba4…`) — exit **0**, 26 asserting / 46 historical. Base-tree leg: the file as it is on `main` (`65853f52f3…`) — exit 0, 26 / 46. Each restore proven by blob hash `cd5a238e193036044a5e07c7859ce09ff5fe3cce` == `HEAD:packages/spec/src/ui/view.zod.ts`, `git status --porcelain` empty. The implementer's 27/45 (was 26/46) and both ablation readings reproduce exactly.\n\n**J4 — PASS. Scope held; `Clause-②: no` holds; the changeset is right.** The diff touches three files and no objectui file (GitHub `get_files`: `.changeset/18516-span-auto-wide-types.md` +22, `content/docs/references/ui/view.mdx` +2/−2, `packages/spec/src/ui/view.zod.ts` +1/−1). `view.zod.ts` with every `.describe(…)` stripped is byte-identical between base and head (312871 bytes each), so the runtime schema cannot have moved. Executed on the same eleven authored rows before (base file swapped in, blob `65853f52f3…`) and after (head): span omitted → accepted, `auto`; `'auto'`/`'full'` → accepted; `'wide'`/`1` → refused `invalid_value`; `type: 'repeater'`/`'textarea'`/`'json'` → accepted; `type: 'subform'`/`'grid'` → refused; `colSpan: 5` → refused `too_big` — the two tables are identical. `check-clause2-carriers.mjs --pair 19253` reads the declaration `no` on both carriers and reports **C6 only** (no review of record on this head; exit 4 before this comment); the C5 widening tells are silent. On the changeset: `@objectstack/spec` `files[]` at the head is `dist`, `json-schema`, `liveness`, `prompts`, `llms.txt`, `README.md`, `src/**/*.zod.ts`, `CHANGELOG.md`, `api-surface`, `api-surface-declarations`, `spec-changes.json` — so the one string moves published bytes on three of them (measured after a fresh build: `src` 1, `dist` 30 occurrences in 30 lines, `json-schema` see note N1, docs 2). A `patch` changeset is owed; naming it as a third path beyond the claim's literal file surface, rather than treating it as a breach, is the right call. `check-adr-0087-registration` and `check-empty-changeset` readings are in N3.\n\n**J5 — PASS. The generated page matches its source.** Both `span` cells in `content/docs/references/ui/view.mdx` (lines 202 and 367) are byte-equal to the describe string in `view.zod.ts:2925` (709 characters, compared after unescaping the table's `\\|`). Independently of that text comparison: `pnpm --filter @objectstack/spec build` (exit 0) and then `pnpm --filter @objectstack/spec check:generated` (exit 0) in the detached worktree — \"All 16 generated artifacts are up to date\", `check:docs` (`content/docs/references/**`), `check:authorable-surface`, `check:api-surface` and `check:api-surface-declarations` among them. Retired wording at the head tree, `grep -rnF` excluding `.git`, `node_modules`, `dist`, `.turbo`: `json/file/subform` = 1 line, `take the whole row` = 1 line, both the changeset's own quotation of the old sentence (`.changeset/18516-span-auto-wide-types.md:9`); firing control, the new wording = 3 hits (source + the two mdx cells); the same sweep on the base tree finds the retired wording in `view.zod.ts:2925` and `view.mdx:202/367`. In the freshly built `dist` and `json-schema` the retired wording = 0 with the new wording as the firing control (30 and 22 lines respectively).\n\n### ② Semver level\n\n`patch` on `@objectstack/spec`, `Clause-②: no` — consistent with the diff (one describe string; no key, default, enum member or export moves) and with the accept/refuse tables above. No ADR-0087 marker is owed because nothing breaks. The level would be unchanged by the J1 fix, which is also prose only.\n\n### ③ Boundary flags\n\n- Implementer flag \"changeset is a third path beyond the claim's literal surface\" — answered in J4: right, and owed.\n- Implementer note \"docblock above `span` keeps the historical spelling\" — agreed, not a filing class; the asserting citation on the adjacent line now reds at the bump.\n- Implementer note \"objectui's two `WIDE_FIELD_TYPES` twins are hand-kept\" — an objectui observation outside this repo; nothing owed here.\n\nNoted (not blocking):\n\n- **N1** — the implementer reports the `FieldType` population as 55; it is 49 at this head by two instruments (executed `.options`; source token count), and `field.zod.ts` is identical between the head and the base. The bare-path set they derived is still right; the population number is not. The `json-schema` population reads 22 lines / 22 occurrences in 12 files here versus the reported 12 — a per-file versus per-line instrument difference; the retired wording is 0 there either way.\n- **N2** — \"zero occurrences of the retired wording remain anywhere outside `.git`\" is over-claimed by the changeset's own quotation of it (line 9). A changeset is a historical record by construction (the pin-citations gate's header classifies `.changeset/*.md` that way), so this is not a live claim; but the card's enumeration instrument is a grep for that literal, and objectui's own pin test deliberately declines to quote it for that reason.\n- **N3** — changeset gates, for the record: `node scripts/check-adr-0087-registration.mjs` exit 0 (\"this PR adds no declared-breaking changeset (1 non-breaking changeset(s) seen)\"); `node scripts/check-empty-changeset.mjs` exit 0 (\"1 declaring changeset(s) added\", none from the merge base modified or deleted). Nothing owed.\n\nImplemented-by: `claude/issue-18516-span-auto-describe`\nReviewed-by: `session_01AmH9bKvGoLjiY86Q4Z3og2`\n\n**VERDICT: FAIL**\n\nOne BLOCKING finding (J1); J2 through J5 pass and are stated plainly as such. What unblocks: the `'auto'` clause re-derived along the runtime path (`mapFieldTypeToFormType` then `isWideFieldType`) at the pin, naming `repeater` beside the four long-form types or dropping \"only\", with the generated page regenerated and the same asserting citation kept — no renderer file and no accept set touched. A fresh review of record is owed on the new head.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5748124338,"author":{"login":"os-steve","id":317605050,"type":"User"},"created_at":"2026-09-20T06:27:30Z","updated_at":"2026-09-20T06:27:30Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19253#issuecomment-5748124338","body":"## Contract review\n\nReviewed at 2026-09-20T06:27Z — round 2, the narrow re-review of the rework — by the isolated at-tier review subagent for the `domain:spec` lane, against primary sources only: the PR diff at its head, `../objectui` read by `git show` at the `.objectui-sha` pin, and the repo's own gates. Nothing below is taken from the PR body or from either implementer report; where their numbers appear it is to say whether mine reproduce them. Items 2–5 of round 1 are closed by the seat and were not re-litigated; what follows is the delta plus a regression check on the new head.\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `6a34e6f67a22478036bbe674a7089a5b981192bf`\n\nTrees the readings were taken against: objectstack head `6a34e6f67a22478036bbe674a7089a5b981192bf` (base `1b82c519df9db93891514b814b4d8e16e42d5d5c` = merge-base with `origin/main`), detached checkout; objectui pin `53ded82bf7a494f54e344e19099dbf00854b8694` — the exact content of `.objectui-sha` at both base and head — read file-by-file with `git show` at that sha and every extracted file proven by blob id, never a checkout of a moving ref. Every exit code below was captured before any pipe.\n\n### ① Derived judgments\n\n**J1 — PASS. The five-member set is complete and correct along the runtime path at the pin.** The round-1 defect was one missed member under a closed-set framing; the rework kept the framing, so I re-derived the set over the whole population rather than checking the one name.\n\n- Population swept: all **49** `FieldType` members at the head (J3 says how the number is known). `json`, `file`, `repeater` are members; `subform`, `grid`, `owner` are not.\n- Instruments, each extracted verbatim from objectui at the pin and its blob id checked against `git rev-parse PIN:path`: `packages/fields/src/field-type-alias.ts` (`3f413a8af929`), `packages/core/src/utils/retired-field-types.ts` (`bb80173b0807`, the REAL `@object-ui/core` module — see J2), `packages/plugin-form/src/autoLayout.ts` (`d5c39cadd420`), `packages/plugin-detail/src/autoLayout.ts` (`dcadff3bfbf4`), and `packages/components/src/renderers/form/form.tsx:2472-2521` (`pickSpanClass`, 50 lines, sha256 `917a686a09818e3d…`, wrapped only to supply `containerClass`). Executed with `tsx`, unmodified except the one import path.\n- Runtime path — `mapFieldTypeToFormType(t)` then `isWideFieldType`, then `resolveColSpan`, then `pickSpanClass` — over the 49: the wide set is **exactly `{html, markdown, repeater, richtext, textarea}`**, five, and it is the same five on every variant I could think of: the `plugin-form` set and the `plugin-detail` twin (10 entries each, equal as sets); `multiple: true` on every member (the only arity move is `select` → `field:multiselect`, narrow); `resolveColSpan` equal to the grid at grid 3 **and** at grid 4; and the prefix-stripped widget key (the `resolveFormWidgetType` shape the action-param dialogs consume — `repeater` → `grid`, which the set also holds). The other 44 members resolve to 1 at both grids and emit no class. Bare path (`isWideFieldType(specName)`) is the four long-form types; the runtime-minus-bare delta is exactly `[repeater]`.\n- The parenthetical the clause added is true as written: `repeater` → `field:grid` (`field-type-alias.ts` typeMap at the pin), and `field:grid` is a member of both `WIDE_FIELD_TYPES` sets; `resolveColSpan({type: 'field:grid'}, 3)` = 3, at 4 = 4, class `@2xl:col-span-3`.\n- The runtime path is the one a spec `FormField` walks, re-read at the pin: `sectionFields.ts` branch (2) \"spec FormFieldSchema object\" builds from `fromObjectSchema` (`:122`, mapped) and re-maps an authored `type` override (`:207`, `rawType`); a string shorthand goes through `ObjectForm.tsx:732` (mapped); the designer goes through `toFormFieldType` → `mapFieldTypeToFormType`. Inline `customFields` (`ObjectForm.tsx:719`) carry a runtime `FormField` in widget-id vocabulary, not a spec `FieldType`, so they are outside the population by construction. The `widget` override cannot add a member either: `resolveColSpan` reads `span`, `colSpan` and `type` only, and nothing in `plugin-form` derives `type` from `widget`. So the word 「only」 holds at the pin — no sixth member.\n- Controls, same instrument: `json` → `field:code` = 1 / `file` → `field:file` = 1 / `text` = 1 / `subform` (not a member) → `field:text` = 1 / `grid` (not a member) reads wide on the bare path and narrow on the mapped path (`field:text`); firing control `textarea` → 3, `@2xl:col-span-3`. Tier regression, unchanged text: `CONTAINER_GRID_COLS[3]` = `grid gap-4 grid-cols-1 @md:grid-cols-2 @2xl:grid-cols-3`, `pickSpanClass(3)` = `@2xl:col-span-3` only; grid 2 → `@md:col-span-2`; grid 4 → `@4xl:col-span-4`.\n\n**J2 — PASS, one imprecision noted. The stub is answer-preserving over the population — shown by execution, not by the argument.**\n\n- I ran the mapper twice from the same blob: REAL, with the `@object-ui/core` re-export block and import (lines 81–87 — I confirmed those are exactly the seven lines the implementer named) rewired to the pin's own `retired-field-types.ts`; and STUB, with those lines replaced by a `reportRetiredFieldType` that returns `false` unconditionally. Over all 49 members the two mappers disagree on **0** rows. The only divergence sits outside the population and is the firing control for the comparison: `owner` → real `field:owner` (the tombstone; one console line, the retired-branch path really executing) vs stub `field:text`. `RETIRED_FIELD_TYPES` at the pin is exactly `{owner}`, and `owner` is not a `FieldType` member, so no live member can reach the retired branch at all — never mind arrive at a wide id.\n- The argument as the implementer wrote it is imprecise: \"every widget id in `WIDE_FIELD_TYPES` has its spec spelling in `typeMap`\" is false for `field:grid` — `grid` is not a `typeMap` key (`mapFieldTypeToFormType('grid')` with the stub is `field:text`, the tail, not a table hit). The conclusion survives on the correct premise: the retired branch returns `field:` + the name, which is wide only for a name in `{textarea, markdown, html, grid, richtext}`; the first four are live `typeMap` keys and never reach the branch, `grid` is not in the population, and the sole retired spelling is `owner`. Noted, not blocking: the instrument's answer is sound; its stated justification has a hole that happens to be empty.\n\n**J3 — PASS. The population is 49, by two instruments of mine, and the round-1 55 is explained.**\n\n- Executed `FieldType.options` from `packages/spec/src/data/field.zod.ts` at the head: 49, 49 unique.\n- The enum literal read out of the source text with `/* */` and `//` comments stripped: 49 (49 unique) at the head and at the base — `field.zod.ts` is blob `e16ed2b9eff3` on both sides, untouched by the PR. The same instrument with the comments left in reads **55**: the enum's own comments quote `'password'`, `'secret'`, `'lookup'` and friends, which is exactly the round-1 number, so the earlier reading is a known bad instrument rather than an unexplained discrepancy. Firing control: a scratch copy with one member appended reads 50.\n\n**J4 — PASS. The delta is the clause, the changeset and the regenerated page; the guard survived; `Clause-②: no` holds.**\n\n- `git diff --stat cee5969dee..6a34e6f67a`: 3 files, +8/−8 (`view.zod.ts` +1/−1, the changeset +5/−5, `view.mdx` +2/−2), in two commits (`1fa83ebf03` clause + changeset, `6a34e6f67a` page); base..head: the same 3 files, +25/−3. No objectui file: the PR is one repository, `.objectui-sha` is byte-identical base vs head, and objectui's remote carries no `issue-18516` head (177 `issue-*` heads read as the control). `WIDE_FIELD_TYPES` is pinned by the sha — 10 entries in each twin, equal as sets, read fresh above.\n- The asserting citation is byte-untouched: word-diff of the round-1 and round-2 describe strings moves only the middle clause (`the long-form types (textarea,` → `textarea,`; `richtext)` → `richtext and repeater`; `count.` → `count (repeater reaches it …)`), and the substring `` (`.objectui-sha` = `53ded82bf7`) `` occurs once, byte-equal, in both. Gate at the head: `check-objectui-pin-citations.ts --self-test` exit **0**; the gate exit **0** — \"27 asserting objectui pin citation(s) match .objectui-sha (53ded82bf), 45 historical citation(s) recorded and not checked, across 1483 spec source(s)\"; `--list` classifies `view.zod.ts:2925` `asserting … current`. The round-1 ablation pair was not re-opened; the seat closed it.\n- `Clause-②: no`, verified in shape on this head: `view.zod.ts` with every `.describe(…)` call removed by my own string-aware stripper (344 calls on each side, 0 residual) is byte-identical base vs head and round-1 vs head (300041 bytes each), while the raw files differ; firing control — a scratch copy of the head with `.default('auto')` → `.default('full')` on the `span` line differs after stripping at exactly that line. A 13-row accept/refuse table executed against `FormFieldSchema` from the base tree (`packages/spec/src` archived at `1b82c519df`, `view.zod.ts` blob `65853f52f3b7`) and from the head: **13/13 identical** — `span` omitted / `'auto'` / `'full'` accepted (default `auto`), `'wide'` and `1` refused `invalid_value`, `type: 'repeater'` / `'textarea'` / `'json'` / `'file'` accepted, `'subform'` and `'grid'` refused `invalid_value`, `colSpan: 5` refused `too_big`, `colSpan: 2` accepted.\n- No spec test pins the describe text: all three generations of the sentence grep to 0 across `packages/spec/src/**/*.test.ts` and `packages/spec/scripts/**`; the radius holds tests that do pin describe text off a schema shape (`api/apis-publish-gates.test.ts:232` reads `ApiEndpointSchema.shape.path.description`, `automation/approval.test.ts:339` reads `ApprovalNodeConfigSchema.shape.minApprovals.description`), and `ui/view.test.ts`, the one ui test touching `span`/`colSpan`, asserts `colSpan` behaviour only.\n- The regenerated page: both `span` cells (`content/docs/references/ui/view.mdx:202` and `:367`) are byte-equal to the describe string (776 bytes, after unescaping the table's pipes), and those two lines are the only ones that moved in the file. Independently: `pnpm --filter @objectstack/spec build` exit **0**, then `check:generated` exit **0** — \"All 16 generated artifacts are up to date\", `check:docs` (`content/docs/references/**`) among them. The tree read clean afterwards.\n\n**J5 — PASS. The PR body and the changeset tell the truth, with one stale sentence in the body's main text that its own footnote withdraws.**\n\n- Body, read back through REST at head `6a34e6f67a`: the sweep table is labelled as the RUNTIME path and carries a `repeater` row (`field:grid`, 3, `@2xl:col-span-3`); the closing sentence is the five-member set over **49** members; the After: quotation is **byte-equal** (776 bytes) to the shipped describe string, and the Before: quotation carries the original retired wording; the seat's footnote names 49-not-55 and 12 files / 22 occurrences and withdraws the \"zero occurrences\" claim. Against the freshly built tree: `dist` **30** files / 30 occurrences, `json-schema` **12** files / **22** occurrences (`ui/FormField.json`, `ui/FormSection.json`, `ui/FormView.json`, `ui/View.json`, `ui/ViewItem.json`, `ui/ViewItemWire.json`, `objectstack.json`, five `api/*.json`); the retired wording and the round-1 wording are **0** in both, the shipped wording being the firing control. In tracked files the retired wording survives on exactly one line, the changeset's own quotation (`.changeset/18516-span-auto-wide-types.md:9`); the round-1 wording is gone (`git grep -qF` exit 1; control on the shipped wording exit 0).\n- **N1 — noted, not blocking.** Line 92 of the body's main text still reads `json-schema` **12** with no unit and \"Zero occurrences of the retired wording remain anywhere outside `.git`.\" The footnote corrects the first and withdraws the second, so the document as a whole is truthful, but a reader who stops at line 92 leaves with the withdrawn claim. A PR body is not a shipped artefact and the correction lives in the same document; an in-place edit of that one line is the tidier form.\n- Changeset at the head: every checkable sentence reproduces — 49 by two instruments; ten entries in both twins; `json` → `field:code`, `file` → `field:file`; `subform` not a member; the five-member set with `repeater` via `field:grid`; the bare-path four being what objectui's own pin test asserts (`autoLayout.wideSpelling.test.ts`, \"its spec-facing surface is EXACTLY the long-form family\", read at the pin); `type: 'grid'` refused and `repeater` accepted (table above); `@2xl:col-span-3` at grid 3 with the container class quoted correctly; `'@objectstack/spec': patch`; `Clause-②: no`.\n\n### ② Semver level\n\n`patch` on `@objectstack/spec`, `Clause-②: no` — unchanged from round 1 and consistent with J4: the delta moves one describe string and the artefacts generated from it; no key, default, enum member or export moves.\n\n### ③ Boundary flags\n\n- Round-1 items 2–5 are closed by the seat; the pin-guard, accept-set and page readings above are regression checks on the new head, not re-openings of those items.\n- Implementer flags carried forward unchanged (docblock above `span` in the historical spelling; the two hand-kept `WIDE_FIELD_TYPES` twins; nothing in this repo pins the runtime-path set): agreed, none is a filing class, nothing owed here.\n- Out of scope, noted, not filed (objectui-side, outside the card's fence): `mapFieldTypeToFormType` reads its `typeMap` by plain index, so a prototype key such as `constructor` returns a function rather than a widget id. It fired only in my controls; no `FieldType` member reaches it.\n\nNothing is blocking. The one BLOCKING finding of round 1 is closed by the measurement in J1, and no second missed member exists.\n\nImplemented-by: `claude/issue-18516-span-auto-describe`\nReviewed-by: `session_01AmH9bKvGoLjiY86Q4Z3og2`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
