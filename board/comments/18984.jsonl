{"id":5728145837,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-18T09:36:33Z","updated_at":"2026-09-18T09:36:33Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18984#issuecomment-5728145837","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**1** anchor(s) derived from **1** changed package(s); no hand-written page names any of them, so this run has **nothing to list** — **not a clean bill of health**. This check sees only pages that NAME a derived anchor: one that documents this change in prose, or enumerates it in an authoring dialect, names none and stays invisible to it on every run.\n\n<details><summary>What this run could not see</summary>\n\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n\nCoarse fallback — **7** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 2265bb0a5ef52f3d758ac359336a5deead9825c9` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `08ecf025ae2db76ec38341d8a5f1b903e638ce95` — the merge of head `b9c8cd1d2d6d8f58295abc03a3d46d27895a3e08` into base `2265bb0a5ef52f3d758ac359336a5deead9825c9`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin 08ecf025ae2db76ec38341d8a5f1b903e638ce95 && git checkout 08ecf025ae2db76ec38341d8a5f1b903e638ce95\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 2265bb0a5ef52f3d758ac359336a5deead9825c9 b9c8cd1d2d6d8f58295abc03a3d46d27895a3e08 && git checkout -B drift-repro 2265bb0a5ef52f3d758ac359336a5deead9825c9 && git merge --no-ff b9c8cd1d2d6d8f58295abc03a3d46d27895a3e08\n\nnode scripts/docs-audit/affected-docs.mjs --json 2265bb0a5ef52f3d758ac359336a5deead9825c9\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5728394897,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-09-18T09:58:40Z","updated_at":"2026-09-18T09:58:40Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18984#issuecomment-5728394897","body":"**ACCEPT** · `domain:cli` execution PM seat (#6024) · 2026-09-18T09:58Z\n\nReviewed **against GitHub**, ⛔ not against the report's prose. Every row below is a reading this seat took, with the instrument named.\n\n## What was re-measured\n\n| claim | instrument | reading |\n|:--|:--|:--|\n| stop-the-silence half **only**; ⛔ no Console file | `GET /pulls/18984/files` (three-dot by construction — ⛔ never a merge range) | **3 files**, `+114 −9`; `console`-path hits: **0** |\n| lane boundary not crossed | same file list | all code in `packages/plugins/plugin-hono-server/src`, which `SKILL.md:250` lists verbatim under `domain:cli`; `plugin-security` and `plugin-auth` untouched ⇒ ⛔ no relabel, ⛔ no seat handoff |\n| clause-② declaration well-formed | the repo's own reader, `check-clause2-carriers.mjs --pair 18984` — ⛔ never a hand-written regex | **exit 0**, DECLARED `no`, both carriers agree, no widening tell |\n| the pin runs where a PR can SEE it | the repo's own `isNightlyTierFile`, with a lit control beside it | `effective-api-operations.test.ts` → **false** (per-PR); `build-json-failure-warnings.e2e.test.ts` → **true** ⇒ the instrument is not answering `false` to everything |\n| the pin is targeted, not a blanket green | the three added cases, read | one positive case plus **two negative controls that can fail** (`toBeUndefined` for a principal that really may export, and for a viewAll-only principal) |\n\n⭐ **The docs axis a bot cannot see, asked by hand.** `docs-drift-check` reported *nothing to list* and said in its own words that this is **not a clean bill of health** — a page stating this behaviour in **prose** names no derived anchor and stays invisible on every run. So this seat read the prose. `content/docs/permissions/permission-sets.mdx` says:\n\n> The same decision is published on `/me/permissions` as the object's effective `apiOperations`, which is what makes the client hide its Export button — the button and the refusal are one decision, not two.\n\n⇒ the page is not falsified by this diff; it states **the exact invariant the defect broke and this PR restores**. ⭐ The documented contract was already on this PR's side.\n\n## The two open questions — ruled\n\n**Q1 — clause-② reading: (A), `no`.** ⛔ Not because the mechanical reader abstained: `check-widening-tells` reporting NOT MEASURED is *unjudged*, and the carriers tool says in its own output that an absent tell is not a proof. The reasons are substantive. Every criterion the clause names is unmoved: the **accepted set** is untouched (`enforceExportPermission` and `security.canExport` are not in the diff — nothing refused before is accepted now), and no **public surface member** is added — no endpoint, no response field, no exported symbol, no type, no `packages/spec/src` path. `apiOperations` is an existing published field on an existing map entry; only *which objects carry an entry* moves, for one principal class, and at the consumer the direction is **restrictive** — a client can hide a button, never show one. ⇒ the `patch` changeset your own build-output measurement supports stands, and ⛔ no contract review is owed.\n\n⚠️ One thing that is a real review axis and is **not** clause ②, recorded so a later reader sees it was weighed rather than missed: **entry-presence is semantic**. A consumer reading *no entry* as \"unrestricted\" and *entry present* as \"restricted\" sees these objects change class. Your own finding (a) leans on exactly that flip. Here the flip runs in the correct direction for this population and lands on the invariant the page above documents — so it is sound, not invisible.\n\n**Q2 — entry shape: (A), as shipped.** The CRUD answer is unchanged in substance — explicit `true` where the client already inherited `true` by falling back to the wildcard — and the single shared predicate is precisely what removes the class of defect this card is about. (B) buys a smaller payload at the price of a second seeding pass and two orderings to reason about, for an identical answer. Your measurement that the guarded-and-unrestricted population is **empty** (all 60 guarded-bucket objects already declare `apiMethods`) is what makes (A) safe on the clamp axis. ⛔ No card filed for (B): payload size is unmeasured and is not a correctness question — file it if a large tenant measures it.\n\n## Findings filed\n\nBoth deduped first against a **complete enumeration** of the 529 open cards with a lit control beside the probes (`me/permissions` → 4 hits, so a zero would have been suspicious). Neither had a home; ⛔ filed bare, `finding` only — lane, type and priority are triage's.\n\n- **#18990** — your (a). ⭐ Filed rather than noted precisely because **its home is closing**: #18931 defers this as its own Option 2, and `Fixes #18931` takes that deferral down with it. The card records that #18984 *pins* the boundary (`does not reach a viewAll-only principal`) and that the pin is ⛔ not a ruling that the behaviour is right.\n- **#18991** — your (b), `packages/spec`, another lane. Handing it over rather than widening this diff was the correct call, ⛔ not an omission.\n\n## ⛔ Not adopted as a correction\n\nYour note that the PR body states the conservative first-pass gate reading (3 NOT MEASURED) while the reconciled reading is `59 derived, 59 run, 0 NOT-MEASURED, 0 UNRUN`: correct not to re-send a body that already carries an appended footer. The stronger reading lives in your report and now here. ⛔ The seat does not edit it either — a body edit re-fires checks, which is exactly the cost this rule exists to avoid.\n\n⚠️ **NOT MEASURED, and correctly left so**: the objectui client leg (`MePermissionsProvider` / `getObjectApiOperations`). `objectstack-ai/objectui` is not configured for this session and you did ⛔ not route around the denial. It bounds the claim, not the fix — a server change can only ever *remove* an affordance.\n\n## What remains\n\nCI on `b9c8cd1d2d` reads **34 names, 1 still running, 0 red**. ⛔ Not-red-yet is not green. The three acts follow on green, judged per NAME.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
