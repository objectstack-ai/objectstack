{"id":5749318271,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-20T10:47:04Z","updated_at":"2026-09-20T10:47:04Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19308#issuecomment-5749318271","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `cf2c44b42877a9495ff9cc55a18d3ef4974a2e04`\n\nIn-seat review by the `domain:skills` seat at the contract-review tier (served tier read at seating, #7623 marker 5738863635) — the lane's record for a `scripts/pm/**` landing (Tier S: the seat lands through the queue on this record + `--pair` 0 + every check green; `check-governed-merges --pr 19308`: 「NOT governed — ordinary queue landing」, so the record is the lane's own discipline). `Clause-②: no` on the claim (5748809084) and at line start in the PR body. Verified against GitHub (`GET /pulls/19308/files`: one file, `scripts/pm/dispatch-gates.mjs` +64 / −38; three commits — `6cd6c136` the fix, `effedcc4` a merge of `origin/main` (PR #19162 landed mid-task, region-disjoint), `cf2c44b4` the census tree ref — trailer pairs model-free) and the diff read in full, ⛔ not against the report (5749305232).\n\n### ① Derived judgments\n\n- **One branch, the card's fix**: `EXCLUSION_DECL_NAME` gains `DEFERRED` between `SKIPPED` and `EXCLUDE`; the regex's anchors (`(?:^|_)…(?:_|$)`) are unchanged, so the arm matches the segment `DEFERRED` and nothing looser. On the tree it matches exactly two declarations — `DEFERRED_SURFACES` / `DEFERRED_GLOBS` in `check-issue-citations.mjs`, that gate's own exclusion table, which its `surfaceFor` applies first and returns `null` through — so an exclusion table stops deriving as a scan surface. Nothing in `check-issue-citations.mjs` is renamed (its exports stay pinned by its own self-test).\n- **The census is re-measured, not carried**: the docblock now states its METHOD (enumerate `topLevelDecls` over tracked JS/TS under `scripts/`, keep non-callable declarations, test each name, price the arm by diffing `extractWatchHints` against a predicate that matches nothing) and its tree (`e6a03e6491`), and every number is rewritten from the live reading (287 files, 4,697 declarations, 75 matches, 24 hints moved across 8 files, 13 free / 11 real; tally `SKIP 54, EXCLUDED 10, EXCLUSION 4, NOISE 2, SKIPPED 2, DEFERRED 2, EXCLUSIONS 1`). The dev also re-ran the OLD predicate on the same tree and found every old number already stale (230 → 287 files etc.) — recorded, so the drift is attributed to the tree, not to the arm. The auxiliary sentences (the one surviving false positive, the one function-local constant, the camelCase near-misses 4 → 7 across 3 files) were re-checked and rewritten where the tree disagreed.\n- **Firing pair, both directions, on PR #19259's head** (the brief's `cwd` recipe cannot work — `dispatch-gates` derives from the tree it lives in via `import.meta.url` — so the fixed file was copied INTO a detached worktree of `6d1272b3`, run, and restored; blob `5d78406d6` → mutated → `5d78406d6`, `git diff HEAD` empty): subject `.changeset/test-abc.md` 19 → 18 lines, the one deleted line is `pnpm check:issue-citations`; control `packages/types/src/index.ts` 44 → 44, command lists byte-identical. The arm also retires two fabricated leads the card did not name (`scripts/**`, `docs/adr/**` off the same table) and five cost-free ones under the gate's own `packages/**` inclusion — recorded in the census.\n- **The self-test case sits where the claim said**: the new `DEFERRED` entry joins the named-spelling loop (`for (const word of [...])`, :17171 on the head), beside the existing vocabulary cases and clear of the `selfTest()` tail PRs #19162 / #19024 touch; 1,866 → 1,867 cases. `pnpm check:pm-dispatch-gates` (detached, 1,012 s): exit 0, and the case that reds on PR #19259's head — 「a changeset path alone reaches NO value-bearing family any more」 — reads green with this fix in place. Correct per the card and the grading; #18224 / PR #19259 unblock once this lands.\n\n### ② Semver level\n\nNone — `scripts/pm/**` publishes nothing; `skip-changeset` applied by the seat (precedent PRs #19173 / #19280 / #19288) so this repo's `Check Changeset` exempts.\n\n### ③ Boundary flags\n\n- Net +26 lines against the +40 budget (28,345 → 28,371 on the merged base; +34 against the 28,337 the brief quoted, of which +8 are PR #19162's own).\n- `pnpm lint` repo-wide exit 0 (no narrowing); 27 further derived families exit 0; `--ran` reads 28 run, 0 NOT-MEASURED, derived from recorded exit codes.\n- A first self-test run taken while the dev's throwaway export-patched probe copies were still on disk reported 2 probe-artefact failures; it was DISCARDED, the copies removed (untracked, never in the `git ls-files` corpus), and both self-tests re-run clean — reported, not hidden.\n- The seat's mid-task status probe could not reach the dev in-channel; the report answered it. `open_questions`: none. `out_of_scope_findings` carried: the census prose goes stale with nothing to red it (the sibling gate keeps its census as code with a `measuredOn` ref — the standing queue on this file, #19070 → #19104 → #19105 → #19106 → #19172, now under seat 2, reads this block; mitigated here by the method + tree ref); the two extra fabricated leads retired by the same arm.\n- CI on the head at this record: 18 `success` · 7 `skipped` · 3 in progress (`Test Core (1/6)`, `Lint & Repo Gates`, `Type Check · consumer gates`) · 1 `failure` (`Check Changeset`, exempted by the label above); the seat re-reads every check before the ready flip.\n\nImplemented-by: `claude/issue-19260-dispatch-gates-deferred-exclusion`\nReviewed-by: `session_01W5y9kRg1YtYaMQYExVLRc2`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
