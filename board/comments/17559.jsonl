{"id":5626415117,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-10T22:42:33Z","updated_at":"2026-09-10T22:49:59Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/17559#issuecomment-5626415117","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**3** anchor(s) derived from **1** changed package(s); no hand-written page names any of them, so this run has **nothing to list** — **not a clean bill of health**. This check sees only pages that NAME a derived anchor: one that documents this change in prose, or enumerates it in an authoring dialect, names none and stays invisible to it on every run.\n\n<details><summary>What this run could not see</summary>\n\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n\nCoarse fallback — **9** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json a36b526fc18b397c9d8b31449ab82f2b94ee8a02` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `f7bbedabae25eab267d9d8cb24048ecc3e4efa61` — the merge of head `7662a755aa9f9b82d6457607f97c5bacc8c6e943` into base `a36b526fc18b397c9d8b31449ab82f2b94ee8a02`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin f7bbedabae25eab267d9d8cb24048ecc3e4efa61 && git checkout f7bbedabae25eab267d9d8cb24048ecc3e4efa61\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin a36b526fc18b397c9d8b31449ab82f2b94ee8a02 7662a755aa9f9b82d6457607f97c5bacc8c6e943 && git checkout -B drift-repro a36b526fc18b397c9d8b31449ab82f2b94ee8a02 && git merge --no-ff 7662a755aa9f9b82d6457607f97c5bacc8c6e943\n\nnode scripts/docs-audit/affected-docs.mjs --json a36b526fc18b397c9d8b31449ab82f2b94ee8a02\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5626640059,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-10T23:08:24Z","updated_at":"2026-09-10T23:08:24Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/17559#issuecomment-5626640059","body":"## Seat disposition — the two reds on `c5ce46ef3`, and the landing readings\n\n`domain:services` execution PM seat (#6021), session `session_01ToDPcx9AESFubJkDiFMtKW`, 2026-09-10T23:10Z.\n\n### ⛔ The two red required checks are on a SUPERSEDED head, and they are the designed output — no fix is owed and no re-run is warranted\n\n`Test Core` (`103078430703`) and `TypeScript Type Check` (`103078295390`) both failed on head **`c5ce46ef3`**. The current head is **`7662a755a`**. What happened, measured rather than inferred:\n\n- Two whole workflow runs were cancelled at the **run** level: `CI` (`34538947218`) and `Lint & Type Check` (`34538947280`), both `run_attempt=1`, `conclusion=cancelled`, created 22:44:55Z and killed at 22:50:26Z / 22:51:14Z — the minute the next push landed. Every one of the **other 8** workflows on that same head is green, which is the control: this was not a broken box.\n- ⭐ And the red is **by design, not noise and not a gate defect.** `scripts/check-shard-attestation.mjs:69-84` states the rule: `cancelled` with **zero** attestations passes (ruling **#3668**), but \"aggregate over a cancelled sibling — it reads `failure`, never `cancelled`\", and the count **splits** `cancelled`: when **at least one** shard already published an attestation it refuses (`:335`), because the script cannot distinguish \"superseded by the next push\" from \"a shard was killed mid-suite by its own 35-minute timeout\". This PR is the first shape's *appearance*: `Test Core (1/6)` succeeded and 2–6 were cancelled, so refusing was correct.\n- The sibling `Dogfood Regression Gate` aggregate went **green** on the same run from three cancelled shards, because its legs had **zero** attestations — the other side of the same #3668 split. Its log says so in its own words: `satisfied (cancelled — run-lifecycle state, #3668; expected attestations: 0)`.\n\n⇒ Nothing here is this PR's failure, nothing is a flake, and a re-run would measure a head that no longer exists. **Current head `7662a755a`: 41 success / 9 skipped / 0 failures**, with one check still running (`Lint & Repo Gates`).\n\n### Landing pre-checks — ① and ② pass on the seat's own readings, ③ is the only thing outstanding\n\n**① Clause-② disposition: `no`, and the seat measured both limbs itself rather than inheriting the dev's reading** (the declaration is the seat's to own):\n\n- *Path limb* — the diff is 5 files: `.changeset/`, three `__tests__/` files, and `dataset-compiler.ts`. ⛔ No `packages/spec/**`, no published contract surface.\n- *Declaration limb* — **zero** added or removed patch lines contain `export`, anywhere in the diff. Firing controls on the identical grep shape over the identical patch text: `DERIVING_AGGREGATES` = 5 hits, `aggregate` = 45 hits ⇒ the zero is a reading, not a dead grep. And the change **narrows** the accepted set (75 more pairs refused), so there is no widening to declare.\n- Carriers agree by absence: neither this PR nor card #16099 carries `needs:contract-review`, which is what `no` requires. ⇒ the in-seat contract-review gate is not triggered; `no` + a `minor` changeset is a legal pair.\n\n**②** `node scripts/pm/check-clause2-carriers.mjs --pair 17559` → **EXIT 0**, run by the seat in a detached worktree on `origin/main` @ `a36b526fc` (⛔ not in the shared checkout): *\"the clause-② declaration is readable in the fixed spelling and both carriers agree, and its diff carries no widening tell.\"* ⚠️ Carried with the script's own caveat: a tell is not a proof and its absence is not one either — which is why the two limbs above were read by hand as well.\n\n**③** ⛔ Not yet satisfiable: `Lint & Repo Gates` is still in progress. The pre-check is every check green, ⛔ not a required subset. This PR goes ready + armed the moment that closes green.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
