{"id":5777186186,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-22T13:14:41Z","updated_at":"2026-09-23T01:16:30Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5777186186","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nThis PR changes **4** package(s): `@objectstack/formula`, `@objectstack/lint`, `@objectstack/objectql`, `@objectstack/plugin-security`, touching **43** documentable anchor(s). ⚠️ **3** changed file(s) yielded no anchor (`packages/formula/src/index.ts`, `packages/objectql/src/core.ts`, `packages/objectql/src/index.ts`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n**15** hand-written doc(s) NAME something this change touched and may need an implementation-accuracy re-verification:\n\n- `content/docs/api/error-catalog.mdx`  _(via rule_violation (literal, a string literal in checkPredicate))_\n- `content/docs/automation/flows.mdx`  _(via validateExpression (symbol, a top-level function))_\n- `content/docs/automation/webhooks.mdx`  _(via acc_1 (literal, a string literal in a comment in RelationshipTraversalAnalysis))_\n- `content/docs/data-modeling/formulas.mdx`  _(via validateExpression (symbol, a top-level function))_\n- `content/docs/data-modeling/seed-data.mdx`  _(via cross_field (literal, a string literal in collectPredicateRelationships))_\n- `content/docs/data-modeling/validation.mdx`  _(via cross_field (literal, a string literal in collectPredicateRelationships))_\n- `content/docs/kernel/runtime-services/sharing-service.mdx`  _(via evaluateRule (symbol, a top-level function))_\n- `content/docs/permissions/field-level-security.mdx`  _(via SecurityPlugin (symbol, a top-level class))_\n- `content/docs/permissions/index.mdx`  _(via SecurityPlugin (symbol, a top-level class))_\n- `content/docs/permissions/sharing-rules.mdx`  _(via /sharing/rules/:idOrName/evaluate (route, bridged from symbol evaluateRule — its route source's handler names it))_\n- `content/docs/permissions/system-context.mdx`  _(via canWriteObject (symbol, a method of class SecurityPlugin))_\n- `content/docs/plugins/packages.mdx`  _(via SecurityPlugin (symbol, a top-level class))_\n- `content/docs/protocol/kernel/error-handling.mdx`  _(via rule_violation (literal, a string literal in checkPredicate))_\n- `content/docs/protocol/objectql/schema.mdx`  _(via cross_field (literal, a string literal in collectPredicateRelationships))_\n- `content/docs/ui/forms.mdx`  _(via SecurityPlugin (symbol, a top-level class))_\n\n⛔ **3** release-owned page(s) also name something this change touched. These are **read-only**:\n\n- `content/docs/releases/implementation-status.mdx`  _(via SecurityPlugin (symbol, a top-level class))_\n- `content/docs/releases/v16.mdx`  _(via evaluateValidationRules (symbol, a top-level function), validateStackExpressions (symbol, a top-level function))_\n- `content/docs/releases/v17/17-0.mdx`  _(via cross_field (literal, a string literal in collectPredicateRelationships))_\n\n> `content/docs/releases/` is RELEASE-OWNED (AGENTS.md \"Documentation Guardrails\"): release\n> notes are written centrally at release time, and a code PR that edits them is the exact PR\n> that guardrail exists to stop. They are still audited — read-only. If one of them is actually\n> wrong, **file an issue** or open a dedicated docs-only PR; do not edit it here.\n\n<details><summary>What this run could not see</summary>\n\n- **3** changed file(s) yielded no anchor (`packages/formula/src/index.ts`, `packages/objectql/src/core.ts`, `packages/objectql/src/index.ts`) — pages documenting those are invisible to this run\n- **1** anchor(s) matched too much of the corpus to be a work list: `ObjectQL (symbol, 69 pages)`\n- **8** name(s) were too generic to anchor anything (single lowercase words)\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **34** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json dc1b98680b42c09e31567f19bbd911a3b493a2e1` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `6c8a275e95752c6080dc0f0c7e4f1dd2d979f80c` — the merge of head `659d5a45caa33464ccbe856f4d29acebb6b4ce22` into base `dc1b98680b42c09e31567f19bbd911a3b493a2e1`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin 6c8a275e95752c6080dc0f0c7e4f1dd2d979f80c && git checkout 6c8a275e95752c6080dc0f0c7e4f1dd2d979f80c\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin dc1b98680b42c09e31567f19bbd911a3b493a2e1 659d5a45caa33464ccbe856f4d29acebb6b4ce22 && git checkout -B drift-repro dc1b98680b42c09e31567f19bbd911a3b493a2e1 && git merge --no-ff 659d5a45caa33464ccbe856f4d29acebb6b4ce22\n\nnode scripts/docs-audit/affected-docs.mjs --json dc1b98680b42c09e31567f19bbd911a3b493a2e1\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>\n\n> Advisory only, and a **precision-first** one (#9192): a page is listed because it names a\n> symbol, wire route or SDK method this diff touched — not because it mentions a changed\n> package. Each row says which anchor put it there, so a wrong row is reportable rather than\n> merely annoying. To re-verify, run the `docs-accuracy-audit` workflow scoped to these files:\n> `node scripts/docs-audit/affected-docs.mjs dc1b98680b42c09e31567f19bbd911a3b493a2e1` → pass the list as\n> `args.docs`, on the commit named under **Which tree this was computed on**."}
{"id":5777701356,"author":{"login":"os-justin","id":323634890,"type":"User"},"created_at":"2026-09-22T13:48:29Z","updated_at":"2026-09-22T13:48:29Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5777701356","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `0065a406b822361bc81bc0c49a7fc5a6d9436f85`\n\nRead at 2026-09-22T13:48Z. Every reading below was re-derived from the diff `fb7b74691f7d192430f6b3b457a858dc442ad0e0..0065a406b822361bc81bc0c49a7fc5a6d9436f85` (merge-base of the head and `origin/main`, 13 files, +1198/-10) and from `origin/main`, in a detached worktree at the head with a fresh install and build. The dev report, the PR body and the ruling `5776007340` were used only as the list of things to check. Where I could run code I ran it (the PR's own 23 + 15 tests pass; my own 8 engine-seam measurements and 7 cel-js probes are quoted by label below); where I could not, it says NOT MEASURED.\n\n### ① Derived judgments\n\n**Q1-A (hydrate in place + refuse the mixed shape at authoring, prescription `record.LOOKUP.id`) — implemented at the two ruled points, but the refusal arm is on the wrong surface.**\n\n- Hydration: `hydrateRelated` (`rule-validator.ts`) overlays the related row onto a shallow COPY, per RULE, only for the reference fields that rule's own source traverses; `checkPredicate` evaluates the copy. A rule that does not traverse is handed the record byte-identical to before. Verified by reading and by the PR's sibling-rule test.\n- Refusal: `validateExpression` (`formula/validate.ts`) now emits an error for `bare-and-traversed` and `multi-hop` on a REFERENCE-typed field, gated on `fieldTypes`; the message names `record.FIELD.id`. Verified: probe P10 shows that error is the ONLY error lint raises on the mixed shape, so lint accepted it before this diff and refuses it after.\n- ⛔ The refusal exists ONLY in `validateExpression`. `findTraversalConflicts` has exactly one caller on the head (`validate.ts`); no runtime package imports `@objectstack/lint`, and nothing under `packages/objectql`, `packages/metadata*`, `packages/rest` or `packages/runtime` calls `validateExpression`. The comment the diff adds to `formula/src/index.ts` says \"`@objectstack/lint` (and the engine's publish path) refuse the conflicting shapes\" — there is no engine publish path that does. Measured consequence (M7, engine + hand driver, rule `record.account.status == 'churned' && record.account == 'acc_churned'`, a row pointing at the churned account): BEFORE this diff the write is REJECTED as unevaluable; AFTER it the write is ACCEPTED — the bare compare is a map-vs-string `false` (probe P3 confirms cel-js answers `false` without faulting), so the rule silently stops firing. That is option B's harm, the one the ruling rejected on measurement, delivered on every path that authors a rule without running lint (Studio / `sys_metadata`, MCP-authored metadata, a stack built without lint).\n- ⛔ The refusal arm also reaches seams the ruling carved OUT. Lint calls `check(…, objectName, 'record')` with `fieldTypes` for: field-level `requiredWhen` / `readonlyWhen` / `conditionalRequired` / `visibleWhen` (`validate-expressions.ts:1570`), per-option `visibleWhen` (`:1595`), action `visible` / `disabled` (`:1743`, `:1745`), sharing-rule conditions (`:1795` — RLS, which the card marks ⛔ out of v1), hook conditions (`:1805`, `:1829`, `:1838`), flow node and edge conditions (`:1355`, `:1480`, and `service-automation/engine.ts:9329` with `fieldTypes`), and formula-field values (`:1710`, role `value`; probe P11d shows the multi-hop refusal firing there with text about \"reject the write\"). On none of those seams does anything hydrate, so the prescription \"write `record.FIELD.id`\" is false where it is shown: it produces an expression that faults at evaluation, and on a fail-open seam that means the rule stops enforcing altogether. Q2-A said v1 is `checkPredicate` only; this diff moves the accepted set of nine other seams and hands their authors a repair that does not work there.\n\n**Q2-A (`checkPredicate` only; `visibleWhen` / `readonlyWhen` / `requiredWhen` OUT, carved to #19727) — hydration honours it; the refusal does not (above).** The diff touches `RuleContext`, `EvaluateRulesOptions`, `evaluateRule`'s `script` / `cross_field` arm, `needsPriorRecord`, and adds `collectPredicateRelationships` / `hydrateRelated`; `evaluateOptionVisibility`, the `requiredWhen` block and the `readonlyWhen` strip are untouched, and `collectPredicateRelationships` collects `script` / `cross_field` (including inside `conditional`) and nothing else. No UI seam is quietly covered.\n\n**The ruling's ground (\"a mixed expression already throws today, so refusing it removes nothing that works\") — verified with one nuance, and one consequence the ruling did not foresee.** Probe P2: `record.account == 'acc_1' && record.account.type == 'partner'` with `account = 'acc_1'` faults (`No such key: type`). Probe P1: the same expression with `account = 'acc_2'` evaluates to `false` without faulting — CEL short-circuits, so a mixed expression DOES evaluate today on every row where the bare arm decides, and faults only on rows that reach the traversal. No mixed expression can therefore evaluate correctly across all rows, and the dev's corpus scan (1041 sources, positive controls) found none authored — so the lint refusal removes nothing that works CORRECTLY, and the ground holds for the lint layer. It does not describe the runtime after this diff: there the throw becomes a silent accept (M7 above).\n\n**Accept/reject delta, enumerated.**\n\nAccepted before, refused after:\n- lint, any `fieldTypes`-bearing site, reference-typed field: `record.FK.FIELD` together with a bare `record.FK` (compare, `!= null`, `has(record.FK)`, ternary test, method receiver such as `record.FK.startsWith(…)` — probes P9, P13, P13b, P14). ⚠️ Every plain null-guard spelling is in this set; the only null-safe spelling lint still accepts is `has(record.FK.FIELD)` (see ④ for what that costs).\n- lint, same sites: `record.FK.FIELD.FIELD2` (multi-hop) on a reference-typed field.\n- runtime `checkPredicate`: nothing newly refused.\n- engine `validate()` dry-run (`engine.ts:10692`): a traversing rule is now REJECTED in preview where the real write is ACCEPTED — see ③.\n\nRefused before, accepted after:\n- runtime `checkPredicate`: `record.FK.FIELD` on a reference-typed field, when the acting user can read the related row and the row carries the key (M1, M2, M5 measured: accept when the parent does not trip the rule, refuse with the AUTHORED message when it does, on insert, on a repoint, and on a patch that does not carry the FK — the FK is read off the prior row).\n- runtime `checkPredicate`: a MIXED expression that bypassed lint is now accepted with its bare arm silently `false` (M7) — the defect above, not a widening.\n\nUnchanged: non-reference object-valued fields (`record.address.city`), `previous.FK.FIELD` (not analysed, faults as before), multi-value reference macros (`record.accounts.exists(…)`, `size(record.accounts)`, `in` — probes P7, P8, P8b: bare only, no traversal, no conflict), `conditional.when` (evaluated unhydrated by `checkConditional`, faults as before — note the changeset's headline \"a validation rule can read one hop\" is wider than what `when` gets).\n\n**Hard constraint (per-evaluation copy) — holds, at two layers, and the ruling's premise is inexact.** `evaluateValidationRules` builds `merged` as `{ ...previous, ...data }` (`rule-validator.ts:2453`) — a spread copy — so the object `checkPredicate` receives was never the write payload itself; `hydrateRelated` copies again per rule. Measured at the driver boundary (M1: `create` receives `account: 'acc_ok'`, a string; M5: `update` receives `{ id, amount }`, no expanded object). The PR's test \"does NOT mutate the record it was handed\" pins `data` only and would pass even under in-place hydration of `merged`; the test \"leaves a sibling rule that compares the BARE foreign key untouched\" is the pin that actually proves per-rule isolation. Adequate.\n\n**Permission semantics — read under the acting user: holds. Unreadable → loud: holds for a plain member access, with three real holes.**\n- Measured (M1): the related read is `find('account', { where: { id: { $in: ['acc_ok'] } }, fields: ['id', 'status'], context })` with `context = { userId: 'u1', positions: ['rep'] }` and no `isSystem` — unlike `resolveMasterDetailParent`, which reads `{ isSystem: true }`. The step-4 FLS mask in `security-plugin.ts` DELETES a non-readable key from a `find` result, so the hydrated map lacks it and `record.FK.FIELD` faults. Measured (M6, middleware deleting `status` after the read): the write is REJECTED as unevaluable, never accepted.\n- Hole 1 — the loud fault carries the wrong prescription. M3/M4/M6 all return: \"The predicate reads 'status', which this object does not declare — fix the rule's condition, or declare the field.\" The field is declared — on the RELATED object; an author following this declares a bogus `status` on the referencing object. The card's ruled outcome 3 is \"faults loudly\"; a fault whose prescription points at the wrong object is not the prescription discipline this repo holds refusals to.\n- Hole 2 — absence is overloaded. `resolvePredicateRelated`'s docblock: \"No id, row gone, refused by the security layer, read threw: all four leave the field unbound.\" Add a fifth the docblock does not name: a readable column the driver did not echo. `driver-memory`'s `projectFields` skips `undefined` values (its own docblock: \"a value of `undefined` means the key is not emitted\"), so under the projection `['id','status']` a stored row without `status` comes back without the key. Measured (M4, a related row lacking `status`): the write is REJECTED as unevaluable. This is the #6457 trap (\"whether a declared lock enforced depended on which columns a driver happened to return\") re-opened one root over, on a fail-CLOSED seam — a valid write refused for a readable, legitimately-empty parent field. The engine already has the TOTAL-ising helper for `parent` and did not apply it here; applying it naively would turn an FLS-deleted key into `null` and defeat the permission rule, so the fix has to let the ENGINE decide readability (catch the security refusal; `security.getReadableFields(target, context)` exists) rather than delegate it to CEL's key-absence.\n- Hole 3 — `has()` and optional selection defeat \"never silently true\". Probe P4: `has(record.account.status)` returns `false` on a `null` FK, on a bare id string AND on a hydrated map that lacks the key; probe P12: `record.account.?status.orValue('')` evaluates on all three. Since every plain null-guard is refused by the new lint arm (P13/P13b/P14), `has(record.FK.FIELD) && record.FK.FIELD == …` is the spelling an author of an optional lookup will reach for — and with it an FLS-stripped field makes the rule silently not fire. The guarantee holds only for expressions that touch the field with a plain member access.\n- Collateral, measured by the PR's own CI: required context `Dogfood Regression Gate` is RED on this head. `packages/qa/dogfood/test/showcase-d3-d4-capabilities.dogfood.test.ts:138` — a member whose permission set grants `showcase_invoice` CRUD and `showcase_contact` read and NOTHING on `showcase_account` — owns an invoice (account status `prospect`) and PATCHes `{ name }`; expected below 300, got 400. That is the diff's `no_invoice_for_churned_account` rule: it runs on every invoice write, its related read is refused for this persona, the rule faults, and an owner's rename is rejected. The checklist item's `knownGaps` asserts \"no seeded persona fails to read\" `showcase_account`; CI measured one. The PR body's dogfood claim was authored (checklist item), not run — no run record exists and no showcase test names the rule. Reproduced locally at this head over HTTP with the same permission set: the engine logs `predicate relationship lookup failed` with `PERMISSION_DENIED` — \"operation 'find' on object 'showcase_account' is not permitted for positions [everyone]\", permission sets `member_default` + `showcase_d34_member` — then `Validation rule 'no_invoice_for_churned_account' predicate failed to evaluate (runtime: No such key: status)`, and the PATCH answers 400 `VALIDATION_FAILED` with `constraint.reason: 'unevaluable'`, `missingKey: 'status'` and the prescription \"The predicate reads 'status', which this object does not declare — fix the rule's condition, or declare the field.\" So `member_default` does not grant `showcase_account` read either; the checklist's \"public_read_write, no persona fails to read it\" is false under the security plugin's CRUD gate for this composition.\n\n**Changeset honesty.** `.changeset/18682-predicate-relationship-traversal.md`: `@objectstack/formula: minor`, `@objectstack/objectql: minor`, `Clause-②: yes (widening)`, no ADR-0087 marker. Packages named are exactly the two whose source changes and publishes; `@objectstack/spec` is imported (`REFERENCE_VALUE_TYPES`, `referenceTargetOf`) and untouched, correctly omitted; `examples/app-showcase` is private. `@objectstack/lint`'s published behaviour changes through its `formula` dependency (the new refusal at ten sites) without a line of lint changing — no changeset is owed under the convention, but the refusal's reach (①) is what makes the `(widening)`-only arm inexact today.\n\n**Model identifier (AGENTS.md:446).** PR title, PR body, all 6 commit messages (trailer pair `Claude-Session:` + `Co-authored-by: Claude`), the changeset, the checklist JSON, both locales, every code comment in the diff: grep for model names and ids returns no match. Clean.\n\n**Also red, and caused by the diff:** required context `Lint & Repo Gates` fails at `check-adr-anchors --self-test`: `ADR-0136` is cited by `docs/qa/platform-checklist/areas/records-forms.json` and `packages/objectql/src/validation/rule-relationship-traversal.test.ts` and names no record under `docs/adr/` (`origin/main` has 0135 and 0137; nothing else on `origin/main` cites 0136). The number was copied from the card body (\"Acceptance (ADR-0136 D2.4)\"), which is dangling there too — the card needs the same correction. ADR-0137 (predicate fault semantics are contract, D2: a faulting field-rule predicate refuses the submit and names the field and the rule) is the record that actually governs the fault this diff adds a new cause to, and the diff does not cite it.\n\n### ② Semver level\n\n`minor` on `@objectstack/formula` and `@objectstack/objectql` is the right level for the ruled scope (Clause-② yes takes at least minor), and the ruling's basis for `(widening)` alone holds at the lint layer for `checkPredicate` conditions (no correctly-working expression is refused). It becomes exactly true only once the refusal is scoped to the validation-rule sites (① F-scope) — until then the same diff narrows nine seams the ruling did not put in scope. Not BREAKING as ruled; no ADR-0087 disposition owed.\n\n### ③ Boundary flags\n\nBlocking (the FAIL rests on these; each names its fix):\n\n1. `Dogfood Regression Gate` red at this head, caused by the showcase rule's collateral on a persona that cannot read `showcase_account` (④). Needs a ruling: either the showcase's own permission model must guarantee account read to every invoice writer (and the test's local set is not the showcase's), or the semantics \"unreadable parent rejects every write on the child, FK touched or not\" is what the maintainer wants and the test moves — the PM decides; the reviewer only records that the required context is red on the diff's own change and the PR body's dogfood claim was not run.\n2. `Lint & Repo Gates` red at this head: dangling `ADR-0136` citation in two diff files. Cite the record that exists or drop the number; correct the card body in the same stroke.\n3. The engine `validate()` seam (`engine.ts:10692`) is not wired: M3 measured preview `valid: false` (unevaluable) against an ACCEPTED write on the same row. The seam's own contract is \"the same posture as the real write\" and `validate-only.test.ts` pins that a dry run ASKS the write's verdict; the import dry run rides it. Resolve `related` there (insert mode at minimum; update-mode preview without a prior is a pre-existing limit to name, not widen).\n4. No runtime refusal of the mixed shape (M7), and a code comment that claims one exists. Refuse in the engine too: in `hydrateRelated` / `checkPredicate`, a `bare-and-traversed` conflict on a reference field returns `unevaluableRuleError` with the same prescription instead of hydrating — that keeps today's fail-closed verdict for the shape, removes nothing that works, and closes the silent flip on every lint-bypassing path. Correct the `formula/src/index.ts` comment to name lint alone until then.\n5. The refusal arm reaches ten lint sites outside Q2-A's seam with a prescription that is false there. Gate the traversal-conflict pass on an explicit caller opt-in in the schema hint (set by lint at `validate-expressions.ts:1521` / `:1523` only) rather than on the mere presence of `fieldTypes`, and keep `value`-role sources out of it entirely.\n6. The unevaluable fault's prescription names the wrong object (④ hole 1). A traversal fault must say which RELATED object and field could not be read under the acting user.\n7. Absence collapses \"cannot read\", \"row gone\", \"no id\" and \"column not echoed\" (④ hole 2, M4), and `has()` / `.?` tolerate absence (④ hole 3, P4 / P12). Make the readability decision in the engine before evaluation — a refused or FLS-stripped traversed field yields the unevaluable refusal directly, a readable-but-empty field is materialised to `null` over the target's declared fields (#6457's helper generalised) — so the permission verdict never depends on which CEL operator the author picked.\n8. No engine-level pin: acting-user context, projection, update-seam prior-FK read and driver-boundary payload are asserted only by this review's scratch measurements; the PR's tests hand `related` in by hand. Add one engine test in the shape of `engine-required-when-parent.test.ts` (capture the `find` context with `registerMiddleware`, assert no `isSystem`, assert the driver's `create` payload carries the id).\n\nNon-blocking:\n\n- `collectPredicateRelationships` reads `condition.source` without checking `dialect`; a non-CEL source that happens to parse as CEL is analysed and hydrated. Check the dialect.\n- `conditional.when` is not hydrated and still faults; the changeset headline says \"a validation rule\". Say `script` / `cross_field` conditions, or hydrate `when` too.\n- Partial field masking (#8993) hands a masked VALUE to the predicate, which then evaluates silently against the mask. Name it in the docblock or treat masked-for-caller fields as unreadable here.\n- The diff adds a new fault cause under ADR-0137 D2 without citing the record (PD #13 corollary).\n- The `unevaluableRuleError` text is reached with `missingKey: 'status'` on a traversal; the `records-forms.json` acceptance clause \"rejected with the unevaluable-rule envelope\" is satisfied literally by a misleading envelope — tighten the clause once 6 lands.\n\nImplemented-by: `claude/issue-18682-predicate-relationship-traversal`\nReviewed-by: `session_01Sfe5YjBLwB9J3y8fvm2xq1` (at-tier contract-review subagent of the dispatching seat)\n\n**VERDICT: FAIL**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5779211694,"author":{"login":"os-justin","id":323634890,"type":"User"},"created_at":"2026-09-22T15:26:05Z","updated_at":"2026-09-22T15:26:05Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5779211694","body":"## 席位改写了本 PR 正文的三处 —— 谁改的、改了什么、为什么,写在这里\n\n`domain:spec` seat 5(`session_01Sfe5YjBLwB9J3y8fvm2xq1`,座位贴 #19357),2026-09-22T15:26Z。施工轮按契约**只在开 PR 时写一次正文**,之后的更正点名交席位办;它点了三处,本席核过后照办。\n\n## 为什么必须改,而不是留一条评论了事\n\n本 PR 是**契约面 PR**。正文里那三处在返工后已经**为假**,而正文是复核者、队列记录与将来读 CHANGELOG 的人都会读的东西。⇒ 留着假陈述、另发一条更正,等于让最权威的那份文本继续说错话。\n\n| # | 原文说的 | 现在的事实 |\n|---|---|---|\n| 1 | 关联行「read under the **acting user**」,被引对象的 CRUD / RLS / FLS 全部生效 | **系统权限读**,范围受投影约束(表达式点名字段 ∩ 被引对象已声明字段 + `id`)。维护者裁决,全文在卡 #18682 的 `5778121976` |\n| 2 | `## Scope` 里「the seam that is fail-closed, and therefore the only one where an unreadable related field can produce the loud refusal」 | 改为「系统权限读**被限制在这一条缝**」—— 原措辞的因果在裁决之后不再成立 |\n| 3 | `## Verification` 整张表与两条 NOT MEASURED | 整节重写 |\n\n## ⭐ 第 3 处为什么单独说:它曾经断言了一件**没有发生过**的事\n\n上一轮的正文把 dogfood 用例写成已验证,而那个门**根本没跑**;同一时刻该 head 上有**三条必需检查是红的**。施工轮这一轮主动认了这件事,⛔ 没有辩解。\n\n⇒ 新的 `## Verification` 做了两件旧版没做的:① **区分谁测的** —— 本席对 GitHub 亲自核到的(`Dogfood Regression Gate` 及其三个分片 `completed / success`、该 head **0 红**)与施工轮本地跑的,分开列;② 保留「NOT MEASURED 与 pass / fail 分开报」这条规矩,并写明本轮该列为空、以及此前在该列的两项各自是怎么变成真读数的。\n\n⛔ 本席**没有**动正文的任何其它部分:技术方案叙述、两种被拒形状、Acceptance、Acceptance notes、以及施工轮自己的署名页脚,一字未改。\n\n## 状态\n\n`needs:contract-review` 挂在卡 #18682 与本 PR **两边**。新一轮**在档复核已派**(head `995266cea04facfc44da01bab85140fbb1632a40`)—— 上一份 FAIL 记录 `5777701356` 只管 `0065a406b`,已随 head 前移作废,⛔ 不沿用。PASS 后本席**先从两边摘标、引记录,然后才** ready + 入队。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5779450745,"author":{"login":"os-justin","id":323634890,"type":"User"},"created_at":"2026-09-22T15:41:47Z","updated_at":"2026-09-22T15:41:47Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5779450745","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `995266cea04facfc44da01bab85140fbb1632a40`\n\nRead at 2026-09-22T15:41Z. This record replaces `5777701356`, which governed `0065a406b` and is void with that head. Nothing below inherits a verdict from it: every reading was re-derived from the diff `fb7b74691f7d192430f6b3b457a858dc442ad0e0..995266cea04facfc44da01bab85140fbb1632a40` (the merge-base of the head and `origin/main`; 12 commits, 16 files, +2020/-14) and from `origin/main`, in two detached worktrees with a fresh install and build — one at the head, one at the merge-base for the before/after transition. The PR body, the implementing round's report `5779163549` and the ruling `5778121976` were used only as the list of things to check. Probe labels (A-series: analyser, 48 spellings; E-series: engine, real driver double, 12 cases) are quoted where a reading came from code I ran. ADR-0137 (D1/D2/D3), ADR-0124 and ADR-0078 were read before judging.\n\n### ① Derived judgments\n\n**The maintainer's ruling — 「校验规则在读关联记录时改用系统权限,但只读规则表达式自己点名的字段」 — bound by bound, from the code.**\n\n1. **Validation rules only — HOLDS.** `resolvePredicateRelated` (`engine.ts:6924`) is private and has exactly four callers, all validation seams: the dry run (`:10741`), insert (`:11473`), single update (`:12805`), bulk update (`:13091`). Its result flows only into `EvaluateRulesOptions.related`, which `evaluateValidationRules` hands to `evaluateRule`, whose `script` / `cross_field` arm alone calls `checkPredicate`. `conditional.when` (`checkConditional`), field `requiredWhen` / `readonlyWhen`, option `visibleWhen`, the sharing plugin's `evaluateRule` and every RLS path are untouched; `plugin-security` is byte-identical to `origin/main`. `collectPredicateRelationships` reads `objectSchema.validations` and nothing else. The elevation is unreachable from an RLS or UI predicate.\n\n2. **Only the named fields — HOLDS.** The read set is computed in the engine before the query is built: `named` from the static analysis, checked against the related object's registry field map, and any undeclared name aborts the read entirely — no query is issued (E4c: rule naming `nope`, zero reads of `crm_account`). The projection actually issued is `id` plus the union of what every traversing rule on the object names (E5: two rules naming `status` and `name`+`status` produced ONE read, `fields: ['id','status','name']`, `where: {id: {$in: [...]}}`, context `{userId:'u1', positions:['rep'], isSystem:true}`). The elevated read stays tenant-scoped: `buildDriverOptions` threads `tenantId` independently of `isSystem`, so `isSystem` adds only the audit-warn mute.\n\n3. **The refusal does not echo the related value — HOLDS, including logs.** The authored refusal carries the rule's own message; the four traversal refusals name the reference field, the related object, the named column(s) and the rule. Ten CEL fault shapes over a related row holding `churned` / `TOPSECRET` (E3: `int()`, `double()`, `timestamp()`, `+`, string index, `matches` with a bad pattern, `startsWith(1)`, comparison overloads) produced fault text naming types, keys or the author's own pattern — never an operand value. The engine's warn lines on the fault path carry the message and stack, no value (E10). The 'unresolved' vs 'no-reference' pair lets a caller distinguish an id that exists from one that does not, but `assertReferencesResolve` already answers existence unscoped by design, so that is not new width.\n\n4. **Negative pin — EXISTS and re-measured.** `never smuggles an UNDECLARED field into the system read set` in `engine-predicate-relationship.test.ts`; E4c confirms the refusal `'crm_account' declares no 'nope'` with no related read issued.\n\n5. **The accepted channel** is not reported as a finding. ⚠️ Something that makes it WIDER is — flag 2 below.\n\n**Engine-side fail-closed for the mixed shape (prior finding 4) — CLOSED, transition re-measured on both sides.** Rule `record.account.status == 'churned' && record.account == 'acc_churned'`, row pointing at the churned account, through the real engine and a driver double:\n- merge-base `fb7b74691`: REJECTED — `runtime: No such key: status`, with the wrong-object prescription (E1-base);\n- head `995266cea`: REJECTED — `reads a reference field both through the relationship and as a value`, with the `record.account.id` prescription; the rule's own message is never reached (E1).\nREJECTED → REJECTED. The refusal lives in `resolveTraversalScope` (`rule-validator.ts`), reached from `checkPredicate` before evaluation, judged with `referenceTargetOf` — the same arbiter the engine's `$expand` gate asks. Lint is not on the path: no runtime package imports it (grep for `findTraversalConflicts` / `traversalHydration` outside `formula` and `lint`: zero hits). The false `formula/src/index.ts` claim is gone; the replacement text names lint and the engine separately and cites ADR-0124.\n\n**The refusal arm does not leak past the validation seam (prior finding 5) — HOLDS, both directions.** `traversalHydration` is set at exactly one site, `validate-expressions.ts:1533` (rule `condition`); the sibling `when` check at `:1540` does not set it, nor does any field / option / action / sharing / hook / flow / formula site, nor `service-automation`'s `schemaHint`, nor `mcp`'s hint. `validateExpression` additionally requires `role === 'predicate'` and `fieldTypes` (`validate.ts:891`). The PR pins both directions (`checks NOTHING when the caller has not opted in`, `checks nothing in the value role`).\n\n**Fault prescription (prior finding 6) — HOLDS.** Four reasons, each naming the related object: `'crm_account' declares no 'nope' ... declare 'nope' on 'crm_account' — ⛔ not on the object carrying this rule` (E4c), `no related record` (E6, E11), `the related record was not found` (E11), `could not read 'crm_account'`. The wrong-object text is off the traversal path. Under ADR-0137 D2 the refusal names the rule; the envelope's `field` is `_record` for every authored rule, which the PR's acceptance note correctly records as a pre-existing spec gap.\n\n**Absence vs null (prior finding 7) — genuinely distinguishable.** A declared column the driver did not echo is materialised to `null` and evaluates: rule `record.account.status == null` FIRES with the authored message on that row (E4a) and is ACCEPTED on a row carrying `status` (E4b). An undeclared column refuses with `declares no` and no read (E4c). The verdict is decided in the engine before CEL sees the row, so `has()` and `.?` cannot turn a fault into `false` — the PR's three-spelling pin passes and the analyser sees every spelling (next).\n\n**⭐ Optional-access re-measurement — VERIFIED, no hole found.** The raw AST was inspected: `.`, `.?`, `[]` (key `{op:'value'}`), `[?]`, `rcall` for method calls, `call` for `has()`. 48 adversarial spellings (A-series), each classified as traversal / bare / multi-hop / conflict:\n- traversal detected for `.`, `.?`, `['x']`, `[?'x']` on the SECOND hop and on the FIRST (`record['account'].type`, `record.?account.?type`, `record[?'account'].type`), for escaped and raw string keys (`'type'`, `r'type'`), inside `has()`, `size()`, `in`, a list literal, a macro body (`[1].exists(x, record.account.type == 'p')`), a comprehension variable that shadows the root, after a method (`record.account.type.startsWith('p')`, `.lowerAscii().size()`), and through parentheses;\n- a non-literal key (`[record.k]`, `[dyn('type')]`, `['ty' + 'pe']`, `[b'type']`) is BARE, so the field is not hydrated, the id string cannot be indexed, the predicate faults and the write is refused — fail-closed;\n- multi-hop is refused in every spelling (`.?owner.?email`, `['owner']['email']`, `[?'owner'].email`, three hops);\n- mixed shapes are caught whichever way the bare use is spelled (`.?type` + bare, index + `has(record.account)`, bare via method receiver, via `in`, via a ternary arm, via `dyn()`, via a dynamic index);\n- a ternary RECEIVER `(c ? record.account : record.account).type` is not a traversal and is bare — unhydrated, faults, refused. Fail-closed, and an author is told nothing useful; noted, not a hole.\nThe implementing round's own account of the hole it found is accurate, and the fix is complete for the surface cel-js exposes.\n\n**`engine.validate()` dry run (prior finding 3) — insert-mode agreement holds (PR pin + E9); the stated update-mode limit is accurate, not convenient:** `validate()` passes no `previous` in update mode for ANY rule, so a traversing rule refusing on a patch that omits the FK behaves exactly as a `previous`-reading rule already does there, and a patch that carries the FK resolves (E9b). What wiring it did to the channel is flag 2.\n\n**Body vs code.** The rewritten body's claims about the seam, the read authority, the projection, the accepted cost and Verification hold. Three passages contradict the code: the repair column of the \"Two shapes refused\" table (`record.account.id` — flag 1); \"the read set is ... plus `id`\" (the projection carries `id`, the accept-set refuses a NAMED `id` — flag 1); and \"an unresolved row is left absent ... refused by the security layer\" (the mechanism is now a discriminated binding decided before evaluation, and a security-layer refusal cannot occur under `isSystem`). The header \"Two deliberate differences from the `parent` precedent\" is stale for its first bullet, which is now the SAME as `parent`.\n\n**CI, read at the head against GitHub, not from the report:** 42 check runs, every one of the seven required contexts `completed / success`, 0 failures; `Dogfood Regression Gate` and shards 1/3, 2/3, 3/3 green. Locally at the head: the PR's three test files pass (29 + 36), and the two dogfood files this diff touches pass 51/51 — the persona the previous head faulted now writes and the churned-account rule still refuses.\n\n**Model identifier (AGENTS.md:446):** title, body, 12 commit messages (trailer pair `Claude-Session:` + `Co-authored-by: Claude`), changeset, checklist, both locale entries and every code comment in the diff — no match.\n\n### ② Semver level\n\n`minor` on `@objectstack/formula` and `@objectstack/objectql`, `Clause-②: yes (widening)`, no ADR-0087 disposition — correct for those two: at the engine the accept-set moves only from refused (fault) to accepted (plain one-hop traversal), and the mixed shape stays refused with a better message; at lint the ruled ground (a mixed expression cannot evaluate correctly on any row today) holds and the arm is now confined to the one seam that hydrates. One omission, non-blocking: `@objectstack/lint`'s source changes (`validate-expressions.ts`, +19/-1) and its published behaviour at validation-rule `condition` sites narrows by two shapes, yet the changeset names it nowhere. It versions anyway through the `fixed` group, but its CHANGELOG will carry no sentence for the new refusal an upgrading author greps for. Add `'@objectstack/lint': minor` to the same changeset; the Clause-② arm is unchanged.\n\n### ③ Boundary flags\n\nBlocking — the FAIL rests on these; each names its fix:\n\n1. **The engine's own prescription is a dead end.** The ruled repair for the mixed shape (Q1-A: point the author at `record.LOOKUP.id`) is emitted by `findTraversalConflicts`, repeated in the changeset table and the PR body — and the engine refuses it. Measured on both registration paths (E2 raw `registerObject`, E12 `ObjectSchema.create`): `record.account.status == 'churned' && record.account.id == 'acc_churned'` is REJECTED with `'crm_account' declares no 'id'`, followed by a second false prescription, `declare 'id' on 'crm_account'`. The registry's field map carries the injected `organization_id` / audit / `owner_id` / `owning_business_unit_id` columns and never `id`, and `resolvePredicateRelated` judges declaredness against that map. ADR-0078 §6: a false prescription is worse than a missing rule; here two are chained, and the first is the one the ruling specified. Fix: admit the primary-key spelling to the declared set (it is already unconditionally in the projection, so the read set widens by nothing), and pin the ruled repair end to end through the engine.\n\n2. **The dry-run seam widens the accepted channel beyond writers.** `engine.validate()` runs no middleware for the target object (E9c: a principal with no positions receives the rule's verdict; the only middleware operation observed is the `isSystem` find on `crm_account`), and its one network ingress — `POST .../data/:object/import` with `dryRun: true` — is gated by `enforceAuth` (authentication) and `enforceApiAccess` (the object's `enable` exposure flags), never by the caller's CRUD on the object. On the real write path the security plugin's CRUD gate refuses before any rule runs, so the channel the ruling accepted — a caller inferring a value from which WRITES are refused — reaches only principals permitted to attempt the write. Wiring the elevated read into `validate()` (the previous round's finding 3) hands the same verdict, computed from a system-authority read, to any authenticated caller with no permission on the child object and no write attempted. That is wider than accepted, and it is new on this head: before it the preview evaluated only the caller's own payload. The `validate()` docblock's \"no driver is touched\" (`engine.ts:10683`) is also false now. Fix: resolve `related` in `validate()` only for a caller who passes the object's create / update gate (or leave the traversal unresolved there for a non-system caller, which refuses as the prior head did), correct the docblock, and pin that a caller without write permission gets no verdict from a traversing rule.\n\n3. **The superseded acting-user semantics survive in the diff's own prose — in a published contract docblock and in the example app.** `EvaluateRulesOptions.related` (`rule-validator.ts:396`, an exported interface that ships in the `.d.ts`) says the rows are \"read under the ACTING USER so the referenced object's RLS and FLS apply. A field the user may not read therefore arrives ABSENT\"; `resolvePredicateRelated`'s header (`engine.ts:6901`, \"Read as the ACTING USER, deliberately unlike `parent`\") contradicts its own body ninety lines down; the insert site says \"Read under the CALLER's context, not system\" (`:11472`); the preview site says the \"permission answer is the caller's own\" (`:10734`); `RelatedUnavailableReason` (`rule-validator.ts:537`) lists \"the acting user may not read it\"; the `unresolved` refusal text offers \"outside this caller's visibility\" as a cause that cannot occur under `isSystem`; a detached docblock for `hydrateRelated` (a function that no longer exists) sits above `TraversalScope` (`rule-validator.ts:2907`) and is `{@link}`ed twice (`:295`, `:404`); both test headers state acting-user while the tests assert `isSystem` (`engine-predicate-relationship.test.ts:12`, `rule-relationship-traversal.test.ts:133`); the checklist item's `source` cites `hydrateRelated` and its `history` records \"faults loudly on an unreadable parent\" as a ruled outcome the same item now inverts; and the showcase rule (`invoice.object.ts:176`) tells every reader of the reference app that \"a rep who cannot read the account ... the rule faults and the write is rejected\" — the dogfood on this very head measures the opposite. The PM rewrote the PR body because three passages had become false; the same three falsehoods stand in the code. On a security-relevant elevation, in-code authority that states the reversal beside the decision is the trap PD #13 names, and a published docblock that overstates enforcement is what ADR-0124 D4 calls a defect. Fix: bring every location above to the ruling's wording (the `⭐ SYSTEM authority` block at `engine.ts:6968` and the `RelatedFieldBinding` docblock already have it), delete the orphaned docblock and its links, and correct the checklist `source` / `history`.\n\nNon-blocking:\n\n- The `no-reference` sentence (\"this record stores no reference there\") is also reached by a multi-valued reference (E7) and by an expanded object supplied in the FK slot (E6); both refuse — correctly fail-closed — but the sentence does not describe what happened. Reword per cause.\n- Add `@objectstack/lint` to the changeset (see ②).\n- Body: correct the three passages named under \"Body vs code\".\n- The `(c ? record.a : record.a).type` receiver shape is refused with a bare `No such key` and no prescription; acceptable, but the analyser could name it as a shape it will not serve.\n\nImplemented-by: `claude/issue-18682-predicate-relationship-traversal`\nReviewed-by: `session_01Sfe5YjBLwB9J3y8fvm2xq1` (at-tier contract-review subagent of the dispatching seat)\n\n**VERDICT: FAIL**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5780579159,"author":{"login":"os-justin","id":323634890,"type":"User"},"created_at":"2026-09-22T16:58:55Z","updated_at":"2026-09-22T16:58:55Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5780579159","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `1201afdfc7993e2442f7e9fdb71817cd9e3cd78b`\n\nRead at 2026-09-22T16:58Z. This record replaces `5779450745` (governed `995266cea0`) and `5777701356` (governed `0065a406b`); both are void with their heads and nothing below inherits a verdict from either. Every reading was re-derived from the diff `fb7b74691f7d192430f6b3b457a858dc442ad0e0..1201afdfc7993e2442f7e9fdb71817cd9e3cd78b` (the merge-base of the head and `origin/main`; 14 commits, 18 files, +2214/-21) and from `origin/main`, in a detached worktree at the head with a fresh install and build. The PR body, the implementing round's report `5780285758`, the ruling `5778121976`, ADR-0137 (D1/D2/D3), ADR-0124 and ADR-0078 were read before judging and used only as the list of things to check. Probe labels: F-series is the prescription loop on the real engine and a driver double; W-series is the real `ObjectQL` engine composed with the real `SecurityPlugin` (`init` + `start`, its middleware registered on that engine, the new probe wired), driven through `insert()` and `validate()` for the same (object, context, payload); G-series is a gate run at the head. Where I could not run something it says NOT MEASURED.\n\n### ① Derived judgments\n\n**Finding 1 of the previous round (the prescription was a dead end) — FIXED, verified end to end.** F1: on the real engine, rule `record.account.status == 'churned' && record.account == 'acc_churn'` is REFUSED with the conflict sentence; the exact spelling its message prescribes (`record.account.id`) was extracted from that message by regex, the rule re-authored as `record.account.status == 'churned' && record.account.id == 'acc_churn'`, and it is ACCEPTED on a live account, FIRES with the AUTHORED message on the churned one, and the negative control `record.account.id == 'someone_else'` does NOT fire — so `.id` resolves to the stored id and compares equal, it does not merely parse. The same holds through `validate()` (F1 preview: `valid: true` / `valid: false` with the authored message, never `could not be evaluated`). The projection issued for every related read was exactly `id,status` (F1), so the read set widened by nothing. Mechanism: `declaredHere` in `resolvePredicateRelated` admits `SystemFieldName.ID` alone; the PR's own pin `accepts the repair its own refusal prescribes` is a genuine end-to-end pin (an unresolved `.id` would fault and fail its over-the-cap arm). Sound.\n\n**Finding 2 of the previous round (the dry run handed the channel to non-writers) — the hole that was found is CLOSED; the gate that closes it is NOT the decision the write path makes. See ③ flag 1.** What holds, measured on the composed runtime:\n- W1 (caller holding the object's create bit): `insert()` runs the rule under the system read and refuses with the authored message; `validate()` issues the elevated read and answers the same verdict. Preview and write agree for a writer, and the dry-run wiring is intact.\n- W2 (caller holding no grant on the object): `insert()` is refused `PERMISSION_DENIED` before any rule, with ZERO reads on the related object; `validate()` issues ZERO reads on the related object and answers `valid: false` with the unevaluable envelope, not the rule's verdict. The channel the previous record measured open is closed for this caller.\n- W5 (permission-set resolution throws): the probe denies, `validate()` issues zero related reads. Fails closed. The engine side also folds a rejected probe promise to `false`.\n- Ordering: `resolvePredicateRelated` has exactly four callers — `validate()` (`engine.ts:10818`), insert (`:11550`), single update (`:12882`), bulk update (`:13168`) — and the three write-path calls sit inside `executeWithMiddleware` (`:10925`, `:12111`), so the security middleware's gate runs before every elevated read on a real write, single or bulk. The import runner's non-dry-run rows go through `createData` / `updateData` and are gated the same way; its `dryRun` rows call `validate()` (`import-runner.ts:818`), which is the only network ingress to that method — no REST, hono, MCP or runtime source calls `validate()` otherwise.\n\n**Finding 3 of the previous round (superseded acting-user prose) — MOSTLY swept; one sentence the round reports as swept is byte-identical. See ③ flag 2.** Confirmed corrected: the exported `EvaluateRulesOptions.related` docblock, the `resolvePredicateRelated` header and both write-path call sites, `RelatedUnavailableReason`, the orphaned `hydrateRelated` docblock and both links (zero hits for `hydrateRelated` at the head), both test headers, the checklist `source` and `history`, and `invoice.object.ts`, which now states system authority and the named-columns bound. Not corrected: `rule-validator.ts:3046`.\n\n**The maintainer's ruling, bound by bound, re-derived at this head.** ① Validation seams only — holds (four callers above, all validation seams; `collectPredicateRelationships` reads `validations` only; `plugin-security`'s diff is the probe and nothing that touches RLS or UI predicates). ② Only the named fields — holds (`fields: ['id', ...named]` after the declared-set intersection; F1 measured `id,status` on every read; the `never smuggles an UNDECLARED field` pin stands). ③ Refusal never echoes the value — holds (the PR's pin, re-run; the W-series refusals carry the authored message or the traversal sentence, never a stored value). ④ Negative pin — exists. The refusal that names the related object, engine-side fail-closed on the mixed shape, the opt-in scoping of the lint arm, and the three-spelling operator pin were all re-run at this head: 39/39 in the PR's two objectql files.\n\n**`plugin-security` is a new package in the diff this round.** +33 lines in `security-plugin.ts#start`: registers `registerWriteGateProbe` on the engine when the engine exposes it, `isSystem` short-circuit, `userId` required, `getObjectSecurityMeta` + `resolvePermissionSetsForContext` + `checkObjectPermission(operation, object, sets, { isPrivate })`, catch-all deny. It is a duck-typed seam on the `ObjectQL` class (`(ql as any).registerWriteGateProbe`), not on `IObjectQLEngine`; the `#11968` epoch seam is the precedent. The changeset does not name the package — see ②.\n\n**Gate obligations the round reports discharged.** G1 `check:system-context-census`: self-test and check both exit 0 at the head — 111 elevation read sites in 20 packages across 45 files; the page's diff is exactly the six counts (110 to 111 three times, 116 to 117, 110 to 111, 106 to 107). ⚠️ Two things the round's account gets wrong, neither a gate failure: the read that moved the count is the probe's `context?.isSystem` in `security-plugin.ts#start`, not the engine's related read, which is a producer (`isSystem: true` in a query context) and not a read the census counts; and the page's behaviour table gained no row for it — the count `behaviour-bearing (rows 1–63 above)` went 106 to 107 while rows 1–63 are unchanged, so the page that calls itself the authority for what `isSystem` does now counts a behaviour it does not describe. The gate cannot see this (`#start` already anchors rows 1, 2, 7 and 30, and the script's own header says `--fix` regenerates counts and anchors still need a human). G2 `check:dual-build-cjs-loads`: not in CI; run at the head over a declaration-emitting `pnpm build` (my first run, over an `OS_SKIP_DTS=1` build, refused all 104 entry points for exactly that reason, which is the gate working, not the diff) — exit 0, 104 entry points / 67 packages / 620 CommonJS files, floors held. G3 `check:adr-anchors`: exit 0.\n\n**CI, read at the head against GitHub, not from the report:** 42 check runs, all completed, 0 failures; every one of the seven required contexts — `Lint & Repo Gates`, `TypeScript Type Check`, `Test Core` (and its six shards), `Dogfood Regression Gate` (and its three shards), `Build Core`, `Temporal Conformance (live PG + MySQL)`, `Governed Surface Queue Guard` — reads `completed / success`, as does `Check Changeset`. Green CI is what a partial gate looks like here: nothing in CI exercises `validate()` against a composed security plugin for a caller the middleware refuses.\n\n**Body vs code.** The rewritten dry-run passage matches the code for a writer and for a caller with no grant (W1, W2). Three passages do not match: the claim that the check is answered by the same permission sets the CRUD gate uses and is not a second decision about who may write (③ flag 1 — it is a second, narrower decision); the Verification row stating `@objectstack/plugin-security` is byte-identical to `origin/main` and out of the diff (false at this head — the seat's caveat says the table is the previous head's, but the seat rewrote that section and this row is the one that turned false); and the mechanism sentence that an unresolved row leaves the field unbound and the traversal faults (the head decides a discriminated binding before evaluation and refuses without evaluating — same outcome, different mechanism, carried over from the previous record's non-blocking list).\n\n**Model identifier (AGENTS.md:446):** title, body, all 14 commit messages (trailer pair `Claude-Session:` + `Co-authored-by: Claude`), changeset, checklist, both locale entries and every code comment in the diff — no match.\n\n### ② Semver level\n\n`minor` on `@objectstack/formula`, `@objectstack/objectql` and `@objectstack/lint`, `Clause-②: yes (widening)`, no ADR-0087 marker — correct for those three, unchanged from the previous record's reading. One omission: `@objectstack/plugin-security` now carries a source change that publishes (the probe registration) and is named nowhere in the changeset; it versions through the `fixed` group regardless, which is why `Check Changeset` is green, but its CHANGELOG will carry no sentence for the new seam. Add `'@objectstack/plugin-security': minor` to the same changeset; the Clause-② arm is unchanged.\n\n### ③ Boundary flags\n\nBlocking — the FAIL rests on these; each names its fix:\n\n1. **The write-gate probe replicates the CRUD bit of the write path's gate, not the gate.** The middleware that fronts a real write refuses, before `next()` and therefore before any rule runs, on a composed sequence: permission resolution failure; a dangling or non-granting delegator (ADR-0090 D10, `security-plugin.ts:2017–2031` and `:2325`); an unresolvable posture (#3545, `:2080`); the `requiredPermissions` capability AND-gate for BOTH principals, ahead of the CRUD grant (ADR-0066 D3 §Precedence, `:2149–2223`); the CRUD grant (`:2264`); then the FLS write gate (2.5) and the row-level write pre-image (2.7). The probe (`:1240–1259`) implements `isSystem`, `userId`, resolution-failure deny and the CRUD grant. Measured on the composed runtime: W3 — `crm_gated_opportunity` declares `requiredPermissions: ['manage_x']`, the caller holds `allowCreate` and not the capability: `insert()` is refused `PERMISSION_DENIED` with ZERO related reads; `validate()` issues ONE related read and answers the rule's verdict `CHURNED_RULE_FIRED`. W4 — a context carrying `onBehalfOf` naming a delegator that does not exist: `insert()` is refused with the D10 fail-closed sentence and ZERO related reads; `validate()` issues ONE related read and answers the rule's verdict. So a caller the write path refuses observes, through the preview, exactly the one-bit-per-row oracle the ruling bounded to writers; W3 is reachable from the wire through `POST /data/:object/import` with `dryRun: true` on any object declaring `requiredPermissions`, and W4 is reachable in-process (the REST and MCP-stdio faces cannot represent `onBehalfOf`; the OAuth `/mcp` door can). The plugin already holds the composed twin for reads — `canReadObject` (`:5037–5090`), six arms including #3545, D3 for both principals and D10 — and `can-read-object-admission.test.ts` pins it as an EQUIVALENCE against the registered middleware case for case, D3 and posture cases included. The probe is that method's write-side sibling with arms 3, 4 and 6 missing, under a code comment and a body sentence that say it is not a second decision. Fix: implement the probe as the write twin of `canReadObject` (same arms, `insert` / `update` as the operation, both principals), and pin it the way that file does — equivalence against the registered middleware for (object, context, operation), with the D3 capability case and the D10 dangling-delegator case in the table. The FLS write gate and the row-level pre-image are payload- and row-dependent; name them as the preview's limits rather than replicate them.\n\n2. **One superseded acting-user sentence survives in a runtime refusal string, and the round reports it swept.** `rule-validator.ts:3046`, the `unresolved` arm of `traversalRefusal`: the referenced record could not be read — it may have been deleted, or it may be outside this caller's visibility. Under the ruled read (`isSystem: true`, the middleware short-circuits, no RLS, FLS or sharing applies) the second cause cannot occur; what remains is the tenant wall, which is not the caller's visibility. An author reading it goes to the caller's sharing and permissions, which is not the lever. The previous record named this sentence; the implementing round's report lists it among the swept items; the bytes are identical. Fix: name the two causes that can occur — the row was deleted, or the id names a row outside this deployment's tenant wall.\n\nNon-blocking:\n\n- Three softer survivals of the superseded motivation, worth sweeping in the same pass: `relationship-traversal.ts` (ships in `@objectstack/formula`), the `asMember` docblock still motivates the four-spelling recognition by an author turning the acting user may not read this column into a quiet non-firing rule; `collectPredicateRelationships`'s docblock still calls the fail-closed policy the one under which an unreadable related field produces the loud refusal the permission rule requires; and `rule-relationship-traversal.test.ts` still explains the operator-independence block by the caller may not read this and its helper by the engine could not make the parent readable for this caller. None is a false statement of behaviour; each names a cause the head no longer has.\n- `EvaluateRulesOptions.related` (ships in the `.d.ts`) describes its value as the related row, or `null` when it could not be read; the type is `RelatedFieldBinding`, a discriminated record with `row` or `unavailable`, never `null`.\n- `content/docs/permissions/system-context.mdx`: add the row for the new read (plugin-security — the `validate()` write-gate probe answers `true`, so the preview's related read is issued without consulting permission sets; anchor `packages/plugins/plugin-security/src/security-plugin.ts#start`), and correct the commit-message account of which site moved the count.\n- The probe is registered silently when the engine exposes the seam and nothing is said when it does not; the middleware branch two lines above warns on the analogous absence. A middleware-capable engine without the seam is a composition whose dry run is ungated while its writes are gated — say so at boot.\n- Add `'@objectstack/plugin-security': minor` to the changeset (②).\n- Body: correct the three passages under Body vs code.\n\nImplemented-by: `claude/issue-18682-predicate-relationship-traversal`\nReviewed-by: `session_01Sfe5YjBLwB9J3y8fvm2xq1` (at-tier contract-review subagent of the dispatching seat)\n\n**VERDICT: FAIL**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5781824237,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-22T18:30:17Z","updated_at":"2026-09-22T18:30:17Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5781824237","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `3fded106c4e2c39370d3a0b23c06a119c64d4203`\n\nRead at 2026-09-22T18:29Z. This record replaces `5780579159` (governed `1201afdfc7`), `5779450745` (`995266cea0`) and `5777701356` (`0065a406b`); all three are void with their heads and nothing below inherits a verdict from any of them. Every reading was re-derived from the diff `fb7b74691f7d192430f6b3b457a858dc442ad0e0..3fded106c4e2c39370d3a0b23c06a119c64d4203` (the merge-base of the head and `origin/main`; 16 commits, 19 files, +2578/−22) in a detached worktree at the head with a fresh install and build. The PR body, the implementing round's report `5781562689`, the ruling `5778121976`, ADR-0137 (D1/D2/D3), ADR-0124, ADR-0078, ADR-0066 D3 and ADR-0090 D10 were read before judging and used only as the list of things to check. Probe labels: the W-series is the real `ObjectQL` composed with the real `SecurityPlugin` (`init` + `start`, its middleware registered on that engine, the probe wired through the real `registerWriteGateProbe`), driven through `insert()` / `update()` and `validate()` for the same (object, context, payload), counting driver reads on the related object. Where I could not run something it says NOT MEASURED.\n\n### ① Derived judgments\n\n**Finding A of the previous record (the write-gate probe was not the write path's decision) — FIXED as prescribed, verified adversarially; the six arms are the middleware's.** Read against the registered middleware at `security-plugin.ts:1784` onward: `isSystem` bypass (`:1814`), permission-set resolution with fail-closed deny, D10 delegator resolved once with a dangling link denying (`:2017–2031`), #3545 unresolved posture (`:2080`), the D3 capability AND-gate for both principals ahead of the grant (`:2148–2223`), the CRUD grant (`:2261`) and the D10 delegator grant (`:2322`). `canWriteObject` (`:5150`) calls the same primitives in the same order — `resolveDelegatorContext`, `requiredCapsForOperation(requiredPermissions, operation)` with the operation asked, `getSystemPermissions`, `checkObjectPermission(operation, …, { isPrivate })` — and the registered probe (`:1240`) delegates to it with no logic of its own. Measured on the composed runtime:\n\n- W1 (holds `allowCreate`): `insert()` refuses with the authored message after ONE related read; `validate()` issues ONE related read and answers the same verdict. ⛔ The dry-run wiring is intact.\n- W2 (no grant): `insert()` `PERMISSION_DENIED`, ZERO related reads; `validate()` ZERO related reads, the unevaluable envelope, never the rule's verdict.\n- W3 (`requiredPermissions` on the object, caller holds `allowCreate` and not the capability): `insert()` `PERMISSION_DENIED`, ZERO reads; `validate()` ZERO reads. W3b (capability held): both fire after one read. The class the previous record measured open is closed.\n- W4 (`onBehalfOf` naming a delegator that does not exist): `insert()` refused with the D10 fail-closed sentence, ZERO reads; `validate()` ZERO reads. W4b (delegator exists, resolves): the two doors agree.\n- W5 (`resolvePermissionSetsForContext` throws): `insert()` refused `permission subsystem unavailable`, ZERO reads; `validate()` ZERO reads. Fails closed on both sides — the method's `catch` answers `false` (`:5210`) and the engine folds a rejected probe promise to `false` (`engine.ts:10815`).\n- W8 (principal-less context) and W9 (`requiredPermissions` on the RELATED object): the two doors agree; the related read is system-authority on both paths, as ruled.\n- W10 (an engine with no `registerWriteGateProbe`): the boot `warn` fires with the consequence and the remedy, and the consequence is real — the preview answered a no-grant caller after one related read. Functional degradation at `warn` is the right level.\n\n**The equivalence pin is genuine.** `can-write-object-admission.test.ts`: the first block's only assertion is `expect(answered).toBe(admitted)`, where `admitted` is whether the REAL registered middleware let the write through and `answered` is the method — no expected boolean per case, W3 both directions and W4 in the table. It is not vacuous: the second block's ADMIT controls run on the same harness, so a middleware that refused everything would fail the `an explicit create grant` case. Ablation re-run by me, not taken from the report: disabling arm 4 inside `canWriteObject` alone (the arm's guard prefixed with `false &&`) turned exactly two cases red — `agrees on a required capability the caller LACKS` in the equivalence block and `DENIES a caller holding the write grant but not the required capability` in the arm block — 2 failed / 15 passed; restore proved blob-identical (`b4e956983c…`). At the head the file passes 17/17, the read twin 15/15, the PR's two objectql files 39/39, the formula file 29/29.\n\n**⛔ But the six arms are not the whole write decision, and a seventh divergence exists on this head — see ③ flag 1.** The middleware refuses a write for reasons that are payload-dependent BEFORE `next()` and therefore before any rule runs, and the first of them is the field-level-security write gate, step 2.5 (`security-plugin.ts:2621`): a caller holding the object's CRUD grant but not `editable` on a field in the payload is refused `PERMISSION_DENIED` (`Field write denied`). The probe carries no payload, so `validate()` cannot ask it. Measured (W6): permission set `{ invoice: { allowCreate, allowEdit } }` plus `fields: { 'invoice.account': { readable: true, editable: false } }`, payload `{ account: 'acc_churn' }` — `insert()` refused `Field write denied: not permitted to edit [account]` with ZERO related reads; `validate()` issued ONE related read and answered the rule's verdict. W6u, the same caller in `update` mode against a row they may edit: `update()` refused the same way with ZERO reads; `validate(mode: 'update')` ONE read and the verdict. That is the W3/W4 shape exactly — a write the write path refuses outright, previewed with an elevated read — one gate later. Reachable from the wire by the same door as W3: `POST /data/:object/import` with `dryRun: true` (`import-prepare.ts:284`) → `previewVerdict(data, willUpdate ? 'update' : 'insert')` (`import-runner.ts:823`, `:529–545`) → `DataProtocol.validateData` (`protocol.ts:11165`) → `engine.validate()` with the caller's own context and a payload the caller wrote. The realistic persona is common: an editor of the child object who is FLS-locked out of the lookup column (may edit invoices, may not repoint the account). On the real path such a caller can never choose which related row a rule is judged against; through the preview they choose any id they can name. The equivalence suite cannot see this by construction — its payload is `{ title: 'x' }` (`:138`), which carries no FLS-relevant key.\n\n**And the preview's own authority says the opposite.** `engine.ts:3960–3963` (the `registerWriteGateProbe` docblock): the preview answers for callers who could perform the write, and for nobody else. Measured false for W6/W6u. `canWriteObject`'s docblock (`:5145–5148`) and the changeset do say object-level only, field-level security still ahead of it — that names a limit of the METHOD; nothing at the preview names it as a limit of the PREVIEW, and the preview's docblock claims completeness. The previous record's own prescription was: the FLS write gate and the row-level pre-image are payload- and row-dependent; name them as the preview's limits rather than replicate them. Neither was done at the preview, and the sentence written there is the reverse.\n\n**Finding B of the previous record (the surviving acting-user sentence) — FIXED.** `rule-validator.ts:3043–3049`, the `unresolved` arm, now reads: no record with that id exists — it has most likely been deleted, leaving the reference dangling. This read is made under system authority, so it is NOT a question of what the caller may see. The pin at `rule-relationship-traversal.test.ts:277` follows the sentence. The three softer survivals named non-blocking last round are corrected (`relationship-traversal.ts:93`, `rule-validator.ts:466`, the test file's two comments), and the `.d.ts` docblock ships corrected — `packages/objectql/dist/util-*.d.ts:8761` carries `Not row | null` after a declaration-emitting build.\n\n**⭐ The round's root-cause correction — the mechanism is real and reproducible; the lesson drawn from it is incomplete, and the PM's discipline was the one that would have caught it.** Verified on the blob: at `1201afdfc7` the sentence is a single-quoted TypeScript string holding `caller\\'s` — `grep -c \"caller's visibility\"` over that file returns 0, the backslash-escaped spelling returns 1, and the escape-tolerant `caller.?'?s visibility` finds line 3046. So a sweep for the RENDERED text against the SOURCE text reports clean on a line that is there; the round is right that re-reading a file after an edit proves nothing about an occurrence no edit was ever issued for. What the narrower account leaves out: this occurrence was never lost to the search. The second record (`5779450745`, flag 3) named it — the `unresolved` refusal text offers outside this caller's visibility — and the round's own round-3 report (`5780285758`, item 3) listed the 'unresolved' sentence that said 'outside this caller's visibility' among the SWEPT items. An item on the work list by name, reported swept with no edit behind it, is a report written from the input list rather than from read-backs — which is the PM's diagnosis in substance, and the PM's countermeasure as actually ordered (write, read the file back, quote the new text per item) applied to that list would have caught it whatever the grep did. Both mechanisms are real and both countermeasures are needed; this round applied all of them. Verified: the escape-tolerant pattern over the 19 diff files at the head leaves the target phrase at 0 in the sense that survived; the four hits it reports are `engine.ts:6710` (pre-existing at the merge-base, `assertReferencesResolve`), `engine.ts:6947` (⚠️ NOT pre-existing — it is this PR's own `resolvePredicateRelated` docblock, stating the read is not bounded by the caller's visibility, the correct sense), and `rule-validator.ts:391` / `:563`, both `Not row | null`. So the round's 'two pre-existing and unrelated' is off by one; the four survivors are all justified. No `head` on the work-list search: NOT MEASURED (no command transcript); the re-run to zero is what I could verify, and it holds. The whole diff was also swept for `acting[ -]user`, `may not read`, `unreadable`, `RLS`, `FLS`, `visibility` on added lines: every hit either states the superseded model AS superseded or is the correct system-authority text.\n\n**The maintainer's ruling, bound by bound, at this head.** ① Validation seams only — holds: `resolvePredicateRelated` (`engine.ts:6971`) has exactly four callers, `validate()` (`:10818`), insert (`:11550`), single update (`:12882`), bulk update (`:13168`); `collectPredicateRelationships` (`rule-validator.ts:483`) reads `validations` only, `script` / `cross_field` including inside `conditional`; the plugin-security diff is the admission method and the probe, nothing touching RLS or UI predicates. ② Only the named fields — holds: the projection is `['id', ...named]` after the declared-set intersection with the PK spelling admitted, an undeclared name aborts the read; the PR's `never smuggles an UNDECLARED field` pin re-run green. ③ No value echo — holds: every W-series refusal carried the authored message or a traversal sentence; the related rows' values (`churned`, and a `secret` column the rules never name) appeared in no response. ④ Negative pin — exists, re-run green.\n\n**Census correction — verified consistent with the code.** At `1201afdfc7` the counted read was the probe's inline `context?.isSystem` inside `start`; at the head it is `canWriteObject`'s first arm, a named symbol, which is why the symbol count moved 91 → 92 in the same page where the read count had already moved 110 → 111. The engine's `isSystem: true` is a query-context producer, not a counted read. Row 6b describes what the code does — the write twin of row 6, asked by the preview, pinned by equivalence including the D3 and D10 arms. `pnpm check:system-context-census` at the head: self-test and check both exit 0 (111 sites, 20 packages, 45 files, 92 symbols).\n\n**CI, read at the head against GitHub and de-duplicated by check name keeping the latest `started_at`:** 39 check runs, 0 red, 0 running; the seven required contexts — `Lint & Repo Gates`, `TypeScript Type Check`, `Test Core` (and six shards), `Dogfood Regression Gate` (and three shards), `Build Core`, `Temporal Conformance (live PG + MySQL)`, `Governed Surface Queue Guard` — all `completed / success`. `Check Changeset` is `success` at this head with `needs:contract-review` on the PR: the changeset already carries `minor`, which is what the clause-② `yes` reading demands, so the expected red did not occur. Green CI still says nothing about W6: nothing in CI drives `validate()` against a composed security plugin with an FLS-restricted payload.\n\n**History and push discipline.** 16 commits on GitHub and locally (`git rev-list --count` over the merge-base range); the round reports 17 — the difference is its amended wip commit, and files and line counts match. All three prior review heads (`0065a406b`, `995266cea0`, `1201afdfc7`) are ancestors of the head: nothing was lost to the `--force-with-lease`. The author and committer sets of all 16 commits are the single agent identity, the branch is `claude/issue-*`, and the PR carries no review or approval (§3 criteria ①③④ hold; ② and ⑤ are the round's own claims, NOT MEASURED).\n\n**Model identifier (AGENTS.md:446):** title, body, all 16 commit messages (trailer pair `Claude-Session:` + `Co-authored-by: Claude`), the changeset, the checklist, both locale entries and every added line of the diff — no match.\n\n**Body vs code.** The body is the seat's and still carries three passages the head has overtaken: the dry-run bullet still describes the previous record's flag 1 as an open blocking finding awaiting the write-twin repair (landed at `ac98ad3df5`); the Verification table is two heads stale and its `plugin-security` row describes `1201afdfc7`; and the mechanism sentence for an unresolved row. None is the implementing round's to fix.\n\n### ② Semver level\n\n`minor` on `@objectstack/formula`, `@objectstack/lint`, `@objectstack/objectql` and `@objectstack/plugin-security`, `Clause-②: yes (widening)`, no ADR-0087 marker — correct. `plugin-security` gains a public method and a registration, both additive; the changeset now carries a section a CHANGELOG reader can grep for `canWriteObject`, and its object-level-only bound. The level is right whichever way ③ flag 1 is resolved.\n\n### ③ Boundary flags\n\nBlocking — the FAIL rests on this one; it names its fix:\n\n1. **A seventh divergence: the field-level-security write gate (step 2.5) is part of the write decision, the probe cannot see it, and the preview's docblock claims otherwise.** Measured W6 / W6u above: a caller holding the object's write grant but FLS-locked on the reference column is refused by `insert()` / `update()` with ZERO related reads and answered by `validate()` after ONE, in both modes, reachable through the `dryRun` import. That is the caller class the ruling bounded the inference channel away from — someone who cannot perform the write being previewed — reached one gate after the six arms close W2–W5. Two acceptable fixes, the first preferred: (a) give the probe the payload — `registerWriteGateProbe((object, operation, context, data) …)` — and let `canWriteObject` (or a sibling the probe composes) run step 2.5's own primitives over it: `getFieldPermissions` folded through `foldFieldRequiredPermissions` and, under D10, `intersectFieldMasks` with the delegator's, then `detectForbiddenWrites`; extend the equivalence table with a payload that carries a non-editable field (both directions) and one under delegation, so the pin covers the arm; the row-level pre-image (2.7) stays a named limit, since the preview names no row. Or (b), if the maintainer wants the bound to be object-level: get that ruled — the second record's bound was principals permitted to attempt THE write, and W6 is not one — and then say it where the preview is documented: replace the `for nobody else` sentence at `engine.ts:3962` with the limit (a caller the FLS write gate would refuse still receives the verdict in preview), and say the same in the changeset's `canWriteObject` section, which currently reads as a property of the method, not as a limit of the preview. Under either fix, delete the sentence that is measured false.\n\nNon-blocking:\n\n- The refusal a probe-denied caller receives in preview (W2–W5) is the generic wrong-object prescription — `The predicate reads 'status', which this object does not declare — fix the rule's condition, or declare the field` — because `resolveTraversalScope` (`rule-validator.ts:2980`) leaves an unbound field to fault as before, and the engine comment at `:10809` calls this honest about what it did not evaluate. It is not: it prescribes declaring the related column on the wrong object to a caller whose real refusal is `PERMISSION_DENIED`. The recipient cannot write anyway and the previous record accepted the envelope, so not blocking; but a distinct binding reason for did not resolve because the caller could not perform this write, refused with a sentence that says so (it discloses nothing the write path's 403 does not), would remove the last false prescription on the traversal path.\n- W12, observation: a `readonly: true` reference field. The real insert strips the caller's FK (the static readonly strip runs inside the write's executor) and the rule then refuses `no single related record` with ZERO related reads; `validate()` runs no strip, resolves the caller's FK and answers the rule's verdict. Every writer is affected alike, so it widens the channel to no new caller class, but the preview judges an id the write path never carries — the W1 agreement property fails for this declaration. Name it beside the update-mode limit at `engine.ts:10802`, or run the same strip in the preview.\n- The round's sweep survivor count is right and one of its labels is wrong: `engine.ts:6947` is this PR's text, not pre-existing. Harmless; noted so the next sweep does not inherit the label.\n- Body: the three passages under Body vs code are the seat's.\n\nImplemented-by: `claude/issue-18682-predicate-relationship-traversal`\nReviewed-by: `session_01Sfe5YjBLwB9J3y8fvm2xq1` (at-tier contract-review subagent of the dispatching seat)\n\n**VERDICT: FAIL**\n"}
{"id":5783642630,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-22T20:29:15Z","updated_at":"2026-09-22T21:53:34Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5783642630","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `e6036cb3a17f97921b029168aeb778bf0ae52242`\n\nRead at 2026-09-22T20:24Z. This record replaces `5781824237` (governed `3fded106c4`); it and the three before it are void with their heads and nothing below inherits a verdict from any of them. The head is exactly one commit on top of `3fded106c4` (`e6036cb3a17f`, 6 files, +622/−33 — the rest of the PR is byte-identical to what the previous record measured), so the round-4 readings were **re-run, not rebuilt**. Every reading was re-derived from the diff `fb7b74691f7d192430f6b3b457a858dc442ad0e0..e6036cb3a17f` (merge-base with `origin/main`; 17 commits, 20 files, +3167/−22) in a detached worktree at the head with a fresh `pnpm install --frozen-lockfile` and a turbo build of both packages' dependency closures (18 tasks, exit 0). ⚠️ This round's implementing agent died before reporting; there is no round report and nothing below was checked against one — **the code was judged on its own**. Read before judging: the PR body, records `5780579159` / `5781824237`, ADR-0066 D3, ADR-0090 D10, the registered middleware `security-plugin.ts:1790–3424` step by step, `canWriteObject` `:5180–5272`, `validate()` `:10792–10880`, `insert()` `:10963–10985`, `update()` `:11925–11990`, the executor's strip site `:11312`, and all three test files in full. Probe labels: the W-series is the real `ObjectQL` composed with the real `SecurityPlugin` (`init` + `start`, its middleware registered on that engine, the probe wired through the real `registerWriteGateProbe`), driven through `insert()` / `update()` and `validate()` for the same (object, context, payload), counting driver reads on the related object (`crm_account`).\n\n### ① Derived judgments\n\n**The blocking finding of the previous record (W6 / W6u) — CLOSED as ordered under option (a), measured on the composed runtime.** Permission set `{ crm_opportunity: { allowRead, allowCreate, allowEdit } }` + `fields: { 'crm_opportunity.account': { readable: true, editable: false } }`, payload `{ name, amount: 50000, account: 'acc_p' }` against a rule `record.account.type == 'partner' && record.amount > 10000`:\n\n- **W6** (insert): `insert()` refused `PERMISSION_DENIED`/403 `Field write denied: not permitted to edit [account] on 'crm_opportunity'`, **ZERO** related reads; `validate()` **ZERO** related reads, `valid: false`, and the rule's authored message is absent from the envelope. Both doors agree.\n- **W6u** (update, `where: { id: 'opp_1' }` on a row the caller may edit): the same shape on both doors, ZERO reads each.\n- **W6-D10** (agent set grants `editable`, live delegator `u_boss` — resolved through the real engine's `sys_user` read — holds only the locked baseline): `insert()` refused with the same FLS sentence, ZERO reads; `validate()` ZERO reads, no verdict. Control (same agent acting for nobody): both doors fire the rule after exactly ONE read. ⇒ `intersectFieldMasks` is what denied, on the composed runtime and ⛔ not only in the unit harness.\n- **W6-plain** (locked caller, payload without the locked column): both doors refuse `no single related record` with ZERO reads — agreement in the other direction of \"does the payload name the field\".\n- **Controls** (identical caller with `editable: true`): `insert()` / `update()` refuse with the rule after ONE related read; `validate()` in both modes answers the rule after ONE read; a payload the rule allows is `valid` in preview and inserted for real.\n- **X2** (batch: row 1 plain, row 2 names the locked column): `insert()` refused the batch, ZERO reads; `validate()` ZERO reads, `[valid: true, valid: false]`, no verdict on either row — the probe judges the whole array as the middleware judges the whole `opCtx.data`.\n\n**The object-level agreements survived the payload-carrying probe.** Re-run at this head: W1/W1u (open writer) rule verdict after ONE read on both doors, both modes. W2/W2u (no grant) 403 / ZERO on both. W3 (`requiredPermissions: ['manage_deals']`, caller lacks it) 403 / ZERO on both; W3b (held) rule after ONE read on both. W4 (dangling `onBehalfOf`) the D10 fail-closed sentence, ZERO on both; W4b (live delegator) rule after ONE read on both. W5 (`resolvePermissionSetsForContext` rejected) `permission subsystem unavailable`, ZERO on both. W8 (principal-less) agree, ONE read each. W9 (`requiredPermissions` on the RELATED object) agree, ONE read each — the related read stays system-authority. W10 (probe hidden) the boot `warn` fires naming the seam and the consequence, and the consequence is real.\n\n**⭐ The FLS arm is the MIDDLEWARE's, ⛔ not a re-derivation — read side by side.** Registered middleware step 2.5 (`:2627–2682`): guard `(insert || update) && opCtx.data && permissionSets.length > 0` → `getFieldPermissions` → `foldFieldRequiredPermissions` → if `delegatorSets`: the same two calls over the delegator's sets then `intersectFieldMasks` → if any keys: `detectForbiddenWrites(opCtx.data, fieldPerms)` → deny on a non-empty result. `canWriteObject` arm 7 (`:5238–5262`): the identical four primitives, in that order, over the identical inputs — `fieldRequiredPermissions` destructured from the same `getObjectSecurityMeta(objectName)`, `permissionSets` from the same `resolvePermissionSetsForContext`, `delegatorSets` from the same `resolveDelegatorContext`; the delegator branch is gated on bare `if (delegatorSets)` exactly as the middleware's is (⛔ not the `.length > 0` spelling arms 4 and 6 use — correct, because 2.5 uses the bare truthiness test too). The middleware never reassigns `permissionSets` or `delegatorSets` between step 1 and step 2.5 (checked over `:1790–3424`: the only assignments are `:1988` and `:2035`). Fail-closed on a throw on both sides (`:5264`, `engine.ts:10875`). **No second spelling of \"which fields may this caller write\" exists.**\n\n**The \"RAW rows\" claim holds on both paths.** `insert()` builds `opCtx` from the caller's `data` verbatim (`:10977–10983`); every middleware runs before the executor (`:4137–4151`), and `applyFieldDefaults` / `initializeSummaryFields` (`:11052–11059`), the hooks, and `stripRuntimeOwnedFields` (`:11312`) all run INSIDE it. `update()` the same (`:11983–11989`). `validate()` hands the probe `rawRows` (`:10822`, before its own defaulting at `:10824–10829`). One shape difference, harmless by construction: `insert()` passes a single object, `validate()` always an array — `detectForbiddenWrites` normalises both, and both shapes are driven green.\n\n**⭐ The extended equivalence table is genuine and non-vacuous.** The assertion is still the single `expect(answered).toBe(admitted)` (`:263`) against the REAL registered middleware with the payload on `opCtx.data` (`:196–216`) — ⛔ no expected boolean per case. New rows: a non-editable field in insert; the same in update (W6u); the editable twin (the other direction); a locked caller whose payload does not name the field; a delegated agent whose delegator may not edit the field; the same agent acting for nobody (`:242–254`). At the head 31/31, the read twin 15/15, the composed pin 7/7. **Ablation re-run by the reviewer, ⛔ not taken from any report:** disabling arm 7 alone (its `if (data)` guard prefixed with `false &&`) turned **exactly 9** tests red — three equivalence rows, four arm-block cases, and both `validate()` cases in the parity file (9 failed / 29 passed over 38); the `MAY edit` direction and every control stayed green, as they must. Restore proved **blob-identical** (`git hash-object` `633e357f8c…` before and after, `git status --porcelain` empty). The pin holds the arm in both directions and under delegation.\n\n**`write-preview-field-gate-parity.test.ts` runs the real engine with the real plugin, ⛔ not a stub.** `@objectstack/objectql` resolves to `../../objectql/src/index.ts` through the package's vitest alias (`vitest.config.ts:52–54`), so `new ObjectQL()` is this checkout's source; `SecurityPlugin` is `./security-plugin.js` with `init` + `start`; the driver is an in-memory double honouring `fields` projection and `limit`. Its assertions are the right ones — refusal envelope + ZERO related reads on the write door, ZERO reads + the rule's message ABSENT on the preview door, and controls on the same harness firing after exactly ONE read ⇒ it cannot pass on a gate that refuses everyone.\n\n**W12 is named accurately and the preview was NOT changed.** The naming sits at `engine.ts:10852–10861`. Measured: `readonly: true` lookup, editable caller — `insert()` strips the FK inside the executor and the rule refuses with ZERO reads; `validate()` runs no strip, resolves the FK after ONE read and answers. Exactly as the comment says; ⛔ no strip added to the preview.\n\n**`content/docs/permissions/system-context.mdx:104`** — the one changed line is row 6b, accurate at this head and in scope. `pnpm check:system-context-census`: self-test and check both exit 0 (111 sites, 20 packages, 45 files, 92 symbols).\n\n**Lint, narrowed and declared:** `eslint --no-inline-config` over the four changed TS files: 0 errors, 0 warnings. objectql `engine-predicate-relationship` + `rule-relationship-traversal` 39/39; formula `relationship-traversal` 29/29. `pnpm typecheck` NOT run locally — CI's `TypeScript Type Check` is green at this head.\n\n**CI at the head, 39 check runs de-duplicated by name keeping the latest `started_at` → 35 names; 0 red, 0 running.** All seven required contexts `completed / success`. `Check Changeset` `success` with `needs:contract-review` on — expected on a `Clause-②: yes` + `minor` PR, ⛔ not a finding.\n\n**History and push discipline.** 17 commits; all four prior review heads (`0065a406b`, `995266cea0`, `1201afdfc7`, `3fded106c4`) are ancestors. Author/committer sets a single identity; branch `claude/issue-*`; `get_reviews` returns `[]`; draft.\n\n**Model identifier (AGENTS.md:446):** title, body, all 17 commit messages, the changeset, every added line — only the mandated `Co-authored-by:` trailers. No match.\n\n**Body vs code — three passages the head has overtaken, all the seat's:** (1) the dry-run bullet still calls the write-twin repair \"an open blocking finding\" — landed at `ac98ad3df5` and payload-bearing at this head; (2) the Verification table is measured at `995266cea0` / describes `1201afdfc7`, and its `plugin-security` row says \"+33 lines\" where the package is +205 plus two new test files; (3) the mechanism bullet says every unbound cause is refused \"by a discriminated binding refused before evaluation\" — a fourth cause exists at this head: a probe-DENIED preview leaves `related` undefined and `resolveTraversalScope` `continue`s past it (`rule-validator.ts:2980–2984`), so the rejection IS an evaluation fault.\n\n### ② Semver level\n\n`minor` on `@objectstack/formula`, `@objectstack/lint`, `@objectstack/objectql`, `@objectstack/plugin-security`, `Clause-②: yes (widening)` — still correct. This head adds an optional fourth parameter to `canWriteObject` and a fourth argument to the probe callback's type; a three-argument registrant still type-checks and runs ⇒ both additive.\n\n### ③ Boundary flags\n\nBlocking — the FAIL rests on this one. ⭐ The ordered fix itself is **delivered in full and pinned**; the next round is a **prose-only** commit needing no code, test or pin change:\n\n1. **The replacement docblock overstates again, in shipped text.** `engine.ts:3987–3989` claims the elevated read is issued \"only for a caller the write path would admit **THIS PAYLOAD** from\", and that \"the two limits above are the distance\" to write success. **X1 measures the first clause false**: permission set with `allowCreate` and the column editable, payload `{ …, owner_id: 'u_other' }` ⇒ `insert()` refused at middleware **step 3.5** (`'owner_id' … is system-managed — creating a record owned by another user requires the transfer grant`, 403) with **ZERO** related reads, while `validate()` issued **ONE** related read and answered the rule's verdict. And the middleware's own step list refutes the second clause: **five** further refusals sit before `next()` and none is in the probe — 2.5a masked-echo (`:2684`), 2.8 ADR-0055 controlled-by-parent master edit (`:2593`), 3.5 owner forge (`:2766`), 3.6 RLS `check` post-image (`:2915`), 3.7 tenant post-image (`:3060`). Of these, 2.8 and 3.6 are row-level in exactly 2.7's family; 2.5a / 3.5 / 3.7 are payload-VALUE refusals the same caller passes by not sending the forged or echoed value, so they widen the caller class by nothing. **The text ships** — the docblock is emitted into `packages/objectql/dist/util-*.d.ts`, and the same incomplete enumeration sits in `canWriteObject`'s docblock (`security-plugin.ts:5174–5177`, ships in that package's `.d.ts`) and in the changeset (`:93–96`, ships as CHANGELOG).\n   **Fix:** delete the two-limits sentence; name the family honestly — *the row-level family: the 2.7 pre-image, the 2.8 controlled-by-parent master edit and the 3.6 `check` / 3.7 tenant post-images, none of which the preview judges because it names no stored row; and the payload-value refusals — 2.5a masked echo, 3.5 owner forge — which the same caller passes by not sending the forged value* — then state the bound **positively**: the probe closes the OBJECT-level and FIELD-level halves of the write decision, and the preview is ⛔ **not** a promise that the write would succeed. Make the same edit at `security-plugin.ts:5174–5177` and changeset `:93–96`, since both ship and both read as the full list. ⛔ **Do not attempt any of these gates in the probe** — the row-level family stays a named limit by the standing order, and the value refusals widen no caller class.\n\nNon-blocking:\n\n- `engine.ts:10818` (\"the read happens only for a caller who could perform the write being previewed\") and the census row `system-context.mdx:104` (\"bounded to callers who could perform the write\") carry the pre-payload shorthand; align both with \"a caller the write path would admit at the object and field level\" while making flag 1's edit.\n- Unchanged from the previous record: a probe-denied preview still carries the generic wrong-object prescription, because `resolveTraversalScope` `continue`s on a missing binding. A distinct binding reason would remove the last false prescription on the path; it discloses nothing the write path's 403 does not.\n- Arm 7 is a verbatim copy of the middleware's 15-line block rather than a shared helper. The equivalence pin holds it and the middleware already repeats the block at 2.5, 2.5b, 2.9 and 4 ⇒ this is the file's own pattern; an extraction is a later cleanup, ⛔ not this card's.\n- Harness note for the next reviewer: on the composed runtime with `sys_user` registered, the accept control's `insert()` shows TWO `crm_account` reads — the second is the pre-existing `assertReferencesResolve` FK check, ⛔ not this card's read.\n- Body: the three passages above are the seat's.\n\nImplemented-by: `claude/issue-18682-predicate-relationship-traversal`\nReviewed-by: `session_01Sfe5YjBLwB9J3y8fvm2xq1` (at-tier contract-review subagent of the dispatching seat)\n\n**VERDICT: FAIL**\n\n<sub>⚠️ 出处:本记录由本席派出的**达档子代理渲染**(`Agent`,档位即 `CONTRACT_REVIEW_TIER` 所指的那个常量,⛔ 此处不重复其字面值,即 `CONTRACT_REVIEW_TIER` 所指档位),由 seat 5 **采纳并发布**于 2026-09-22T20:28Z。⛔ 不是本席自签:渲染方与采纳方是两个不同的执行体,`Reviewed-by:` 里的会话 id 是**派出方**的。</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5784524529,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-22T21:33:09Z","updated_at":"2026-09-22T21:53:40Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5784524529","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `0e5c7704bcf3acf2ca024b8aa058de82ca514950`\n\n<sub>Reviewed and posted 2026-09-22T21:32Z. This record replaces `5783642630` (governed `e6036cb3a17f`); it and the four before it are void with their heads, and nothing below inherits a verdict from any of them — but that record's *findings* were this round's input and are re-measured here. The head is exactly one commit on `e6036cb3a17f` (4 files, +74/−28), a fast-forward: all five prior review heads are ancestors (each `merge-base --is-ancestor` exit 0); 18 commits ahead of and 16 behind `origin/main` (`c120dbdfb8`), merge-base `fb7b74691f`. Every reading was taken in a detached worktree at the head with a fresh `pnpm install --frozen-lockfile` and a declaration-emitting turbo build of the four changeset packages' closures (18 tasks, exit 0). Read before judging: the PR body and title, record `5783642630`, the round report `5783982504`, the seat note `5784094892`, the registered middleware `security-plugin.ts:1790–3424` line by line, `canWriteObject` `:5109–5285`, `system-write-guard.ts`, `delegated-admin-gate.ts:62–200`, `rest-server.ts:10715–10762` and `:9248–9300`, `import-runner.ts:494–545`, `protocol.ts:11149–11170`, `assemble-execution-context.ts:93–150`, `verify/src/handle.ts:140–366`, all four shipped prose sites and both plugin-security pins in full. Probe labels: every X/Y probe ran the real `ObjectQL` composed with the real `SecurityPlugin` (`init` + `start`, middleware registered, probe wired through the real `registerWriteGateProbe`) in a throwaway file deleted before any commit, driven through `insert()`/`update()` and `validate()` for the same (object, context, payload), counting driver reads on `crm_account`.</sub>\n\n⏱️ 渲染方自报的读树时刻:2026-09-22T21:05Z(子代理那一次动作的钟,⛔ 不是本次发布的钟)。\n\n### ① Derived judgments\n\n**⭐ The round is prose-only — TRUE, measured two ways.** Over `e6036cb3a17f..0e5c7704bc`: every `+`/`−` line in `engine.ts` and `security-plugin.ts` begins with `*`, `/**`, `*/` or `//` (line-shape filter: zero survivors), **and** both files transpile **byte-identical** under `ts.transpileModule({ removeComments: true })` (`engine.ts` 252 246 B sha `1019be227657f836` both sides; `security-plugin.ts` 150 516 B sha `5104bfd3d0c5fa77` both). ⛔ No executable line, test, pin, probe or changeset level moved.\n\n**The ordered fix landed at every site the order named, plus one the sweep found.** `engine.ts:3971–4009`, `security-plugin.ts:5119–5125` and `:5176–5192`, changeset `:93–107`, `engine.ts:10837–10840`, `system-context.mdx:104` — all read back from disk. 「THIS PAYLOAD」/「the two limits above」/「What is still ahead of it」/「is the distance」: **0** in the tracked tree. 「and for nobody else」: 3 hits, none in this PR's files. Every site now carries 「⛔ … not an enumeration of the distance to success」 ahead of its family list. Q1/Q2 were ruled **A** by the seat (`5784094892`); the text matches the ruling.\n\n**⭐ But the POSITIVE claim is measured false on the composed runtime — Y1.** All three shipped sites assert the probe closes *the OBJECT-level half of the write decision*, with 「⛔ That is the whole of the claim」 at `engine.ts:3980`. **The write path's object-level half is larger than those arms.** The middleware runs **six unconditional gates** at `:1895–:1963` — after `isSystem` (`:1820`) and **BEFORE** permission sets resolve (`:1988`): `assertPackageManagedWriteGate` (`:1895`), `assertSystemRowWriteGate` (`:1909`), `assertCuratedCapabilityNameGate` (`:1921`), `assertAudienceAnchorBindingGate` (`:1929`), `assertEngineOwnedWriteAllowed` (`:1941`, ADR-0103) and `delegatedAdminGate.assert` (`:1963`, ADR-0090 D12). ⛔ **None of the three enumerations — the seat's order, record `5783642630`, or the round's own line-by-line — names any of them**, and `canWriteObject` (`:5200–5285`, read in full) has no arm for any. Four are provenance/value refusals. **Two are caller-class and object-level.**\n\n**Y1 (ADR-0103), measured.** Object `eng_log` declared `managedBy: 'engine-owned'` carrying a traversing rule; caller holds `{ allowRead, allowCreate, allowEdit }` on it and `editable` on the lookup column ⇒ arms 1–7 all admit:\n\n- `insert()` → 403 `'eng_log' is engine-owned (managedBy:'engine-owned', ADR-0103) — direct insert via the data API is disabled`, **ZERO** related reads; `update()` the same, ZERO.\n- `validate()` in both modes → **ONE** related read and the rule's authored verdict crosses.\n- `canWriteObject('eng_log', 'insert' | 'update', caller, payload)` → **`true`, `true`**.\n- Control: the same caller's `insert()` on `crm_opportunity` is admitted after the rule.\n\n⇒ **the write path admits NO user-context caller on that object for that verb; the preview admits every holder of the grant.** Not a value the caller can omit, not a row the preview cannot name — it is the caller class `engine.ts:3943–3957` defines as 「a strictly wider channel than the one that was accepted」. **Reachability:** `POST /data/:object/import` `dryRun:true` → `runImport` → `p.validateData` (`import-runner.ts:534`) → `engine.validate()` with the caller's real context; the import handler and `import-prepare`/`import-runner` gate on **nothing** about `managedBy` or an `import` affordance (0 hits). What it needs today: an engine-owned object carrying a traversing rule — none exists in the repo yet, and `managedBy` is an **authorable** key, so an app declares one the day it wants one. ⇒ **the claim is about the bound, and the bound is false the moment the rule is authored.** ⚠️ This is the **fourth** over-claim about the preview's bound on this PR — positive in form this time, and stated with 「that is the whole of the claim」 beneath it.\n\n**Y3 (D12), the same shape.** `sys_user_position` (one of five D12-governed RBAC link tables) with a traversing rule; caller holds plain CRUD, no positions, no `adminScope`: `insert()`/`update()` → 403 `requires tenant-level administration or a delegated adminScope (ADR-0090 D12)`, **ZERO** reads; `validate()` → **ONE** read and the verdict; `canWriteObject` → **`true`**. Reachability narrower (platform objects, platform rules), but the probe's answer for that caller class is wrong by the same mechanism.\n\n**⭐ The enumeration, re-derived from `:1790–3424` — line-cited.**\n\n| gate | line | class | probe |\n|---|---|---|---|\n| `isSystem` bypass | `:1820` | admit | arm 1 |\n| ADR-0056 `publicFormGrant` scope | `:1832–1871` | admit granted (object, create/read-back); **refuse** anything else, pre-resolution | ⛔ no arm |\n| package-managed write gate (ADR-0086/0094) | `:1895` | row provenance | ⛔ no arm |\n| system-row write gate (ADR-0066) | `:1909` | row provenance | ⛔ no arm |\n| curated capability name (ADR-0066 D1) | `:1921` | payload VALUE | ⛔ no arm |\n| audience-anchor binding (D5/D9) | `:1929` | payload VALUE | ⛔ no arm |\n| **ADR-0103 engine-owned affordance** | `:1941` | **object-level, caller-class** | ⛔ **no arm — Y1** |\n| **ADR-0090 D12 delegated-admin gate** | `:1963` | **object-level, caller-class** (5 RBAC tables) | ⛔ **no arm — Y3** |\n| principal-less fall-open | `:1975` | admit | arm 2 |\n| step 1 subsystem unavailable | `:2002` | fail-closed | catch → `false` |\n| step 1 D10 dangling delegator | `:2027` | fail-closed | `del.kind === 'missing'` |\n| step 1 unresolvable posture | `:2095` | fail-closed | arm 3 |\n| step 1.5 D3 capability AND-gate | `:2200` | object-level, both principals | arm 4 |\n| step 2 CRUD grant | `:2321` | object-level | arm 5 |\n| step 2 D10 delegator grant | `:2336` | object-level | arm 6 |\n| step 2.7 `using` pre-image | `:2577` | row | named limit |\n| step 2.8 ADR-0055 master edit | `:2593` | row | named limit |\n| step 2.5 FLS write gate | `:2669` | payload KEYS | arm 7 |\n| step 2.5a masked echo | `:2684` | payload VALUE | named limit |\n| step 2.5b aggregate FLS read gate | `:2751` | gated `operation === 'aggregate'` (`:2722`) ⇒ ⛔ not a write refusal | — |\n| step 3.5 `owner_id` forge | `:2834` | payload VALUE (X1) | named limit |\n| step 3.6 RLS `check` post-image | `:3009` UPDATE; INSERT installs `postHookWriteImageCheck` (`:3034`), runs inside the write | row | named limit |\n| step 3.7 tenant wall (no active org) | `:3203` | caller CONTEXT | named limit |\n| step 3.7 tenant CHECK | `:3245` | payload VALUE | named limit |\n| step 2.9 anti-filter-oracle guard | `:3254` | caller's PREDICATE (X3) | named limit |\n\n**X3 reproduced, ⛔ not adopted**: 403 `references field(s) not readable by the caller: account`, ZERO reads. **X1 reproduced**: `owner_id: 'u_other'` ⇒ `insert()` 403 at 3.5 ZERO reads; `validate()` ONE read, verdict present.\n\n**⭐ ADR-0056 — SETTLED: the unsafe direction is NOT wire-reachable today.** `publicFormGrant` is constructed in exactly one place, `rest-server.ts:10750` (the public form-submit route), whose context goes to `p.createData` (`:10762`) — the real write; **the route has no preview leg**. It cannot arrive from a client: `ENTRY_EXECUTION_CONTEXT_FIELDS` (`assemble-execution-context.ts:117–139`) is the closed inbound set and `publicFormGrant` is not in it. The only non-test callers of `engine.validate()` are `protocol.ts:11167` (reached from `import-runner.ts:534` with an assembled context) and `verify/src/handle.ts:364` (in-process harness). Measured in-process anyway (Y2): preview-wider, as the round described. ⇒ ⛔ **not blocking**; the round's 「note, don't file」 was right.\n\n**Both corrections to the previous record are RIGHT.** (a) 3.6's INSERT leg installs `insertCheckSeam` on `opCtx.postHookWriteImageCheck` (`:3019–3034`), run after `beforeInsert`; only UPDATE calls `denyCheck()` before `next()` (`:3055`). (b) 2.5b is inside `if (opCtx.operation === 'aggregate' …)` (`:2722`). This record's table supersedes the flat listing.\n\n**The corrected text SHIPS — re-run.** Eight emitted files listed with `ls -la` then `grep -c -F`: the seven removed phrasings are **0 in all 56 (file × phrase) cells**; the new text present 1 each in `util-*.d.ts`/`.d.mts` and plugin-security `index.d.ts`/`.d.mts`. Sites 4 and 5 correctly absent from any `.d.ts`.\n\n**The two non-shipping survivors — leaving them is right.** Both `files[]` are `[\"dist\",\"README.md\",\"CHANGELOG.md\"]`; neither reaches any `.d.ts` (0 in all eight); both state the ACCEPTED COST, ⛔ not a delivery claim. ⚠️ Note: `can-write-object-admission.test.ts:11–12` wraps 「could / perform the write」 across the line break, which is why a `-F` sweep reports only the parity file.\n\n**The code is unmoved and green.** objectql **39/39**; formula **29/29**; plugin-security **38/38**. ⭐ **Ablation re-run by this reviewer:** arm 7's guard (`:5259`) prefixed `false &&` ⇒ **exactly 9** red, 29 green; restore **blob-identical** (`4c9586b9ab…`, porcelain empty).\n\n**CI at the head — converged.** 39 runs → **35 names; 0 red, 0 running.** The three the seat saw running finished `success` (21:02:48Z, 21:04:13Z, and the `Test Core` rollup 21:04:32Z — the 35th name, absent from the seat's 34). All seven required contexts `completed / success`. `Check Changeset` `success` with the label on — expected on `Clause-②: yes` + `minor`, ⛔ not a finding.\n\n**⚠️ 16 commits behind `origin/main` — declining the merge was right.** `git diff --name-only fb7b74691f..main` (89 files) ∩ the PR's 20 files = **∅**. The two behaviour commits on `main` were checked against this PR; a no-commit `git merge --no-ff` in the review worktree: **exit 0, 0 conflicts**, and on the merged tree after a rebuild the three suites read identical (39/29/38). Merge aborted, porcelain empty.\n\n**Model identifier (AGENTS.md:446):** title, body, all 18 commit messages, the changeset, every added line — only the mandated `Co-authored-by:` trailers. No match.\n\n**Body vs code — four passages overtaken, all the seat's:** (1) the dry-run bullet's 「the one open blocking finding … is being repaired」 — repaired at this head; (2) the same bullet's two-group list reads as the whole list, without the non-exhaustive marker the shipped text now carries, and names neither 2.9 nor the unconditional gates; (3) 「测于 head `e6036cb3a17f`」 and the Verification head; (4) the `plugin-security` row's **+899 / +205** — re-measured at this head: **3 files, +913** (`security-plugin.ts` **+219**). Whole PR: 20 files, +3215/−24.\n\n### ② Semver level\n\n`minor` on the four packages, `Clause-②: yes (widening)` — correct and unchanged; this round changes no code.\n\n### ③ Boundary flags\n\nBlocking — the FAIL rests on this one. ⭐ The ordered prose repair itself is delivered and ships; what fails is that the POSITIVE sentence it introduced is measured false, and the falsifier is a refusal **all three enumerations missed**:\n\n1. **The shipped bound is wider than the truth at the OBJECT level — Y1 (and Y3).** `engine.ts:3971–3978` (with 「⛔ That is the whole of the claim」 at `:3980`), `security-plugin.ts:5176–5178` and changeset `:93–95` claim the probe closes the object-level half. Measured: an ADR-0103 `engine-owned` object with a traversing rule ⇒ write refused at `:1941` with ZERO reads, preview ONE read + verdict, `canWriteObject` `true`; a D12 RBAC table under plain CRUD ⇒ the same at `:1963`. These are object-level and **caller-class** — the same caller cannot pass by omitting a value or naming another row — so the preview hands the oracle to a caller who **cannot write the object at all**, the exact widening `engine.ts:3943–3957` defines the gate to prevent; and Y1's is **wire-reachable** through the dry-run import door the day an engine-owned object carries a traversing rule.\n   **Fix, in the middleware's own order:** add the arm to `canWriteObject` **between arm 1 and permission-set resolution** — `assertEngineOwnedWriteAllowed(<the object's schema>, operation, context)` inside the fail-closed `try` (a throw ⇒ `false`), ⛔ **not a re-derivation of the affordance**; pin by equivalence in `can-write-object-admission.test.ts` (engine-owned under a CRUD grant ⇒ both refuse; the same object with `userActions.create: true` ⇒ both admit; plus the `isSystem` and principal-less controls) and by parity on the composed harness (ZERO reads + no verdict on both doors; the opened control after ONE). Add the **D12** arm in the same pre-resolution position (`delegatedAdminGate.assert` reads `opCtx.object`, `opCtx.operation`, `opCtx.context` and the rows of `opCtx.data` — `delegated-admin-gate.ts:175–200`; **measure the exact shape it needs before wiring**), pinned with a plain-CRUD-holder row against a tenant-admin control. Then **restate the positive sentence at all three shipped sites over the arms the probe actually runs** — name them, ⛔ never 「the object-level half of the write decision」 — and add the remaining pre-resolution gates (`:1895`, `:1909`, `:1921`, `:1929`) to the families named as NOT closed. ⛔ Do not attempt the row-level family or the value refusals in the probe; the standing order holds.\n   ⚠️ If the seat instead rules the ADR-0103/D12 family a **named limit** like the row-level one, the same three sites must still narrow the positive sentence to the arms actually run and name that family as NOT closed — **but that leaves a reachable caller-class widening the gate exists to refuse, and arms 4 and 6 were added on exactly that ground.**\n\nNon-blocking:\n\n- **ADR-0056 `publicFormGrant` (`:1832–1871`): unreachable today, trace in ①.** If Y1 is closed with an arm, the natural landing is the same pre-resolution spot; otherwise name it under the families not closed. Becomes reachable only through a preview leg on the form-submit route, a second constructor of the grant reaching `validateData`, or the key entering `ENTRY_EXECUTION_CONTEXT_FIELDS`.\n- **Body: the four passages in ① are the seat's.**\n- `write-preview-field-gate-parity.test.ts:53–59` lists two limits as the pair without the non-exhaustive marker. Non-shipping; align the next time a round may touch tests, ⛔ not this one.\n- Unchanged from the previous record: the generic wrong-object prescription on a probe-denied preview; arm 7 as a verbatim copy (the file's own pattern); the accept control's TWO `crm_account` reads (`assertReferencesResolve`), ⛔ not this card's read.\n\nImplemented-by: `claude/issue-18682-predicate-relationship-traversal`\nReviewed-by: `session_01Sfe5YjBLwB9J3y8fvm2xq1` (at-tier contract-review subagent of the dispatching seat)\n\n**VERDICT: FAIL**\n\n<sub>⚠️ 出处:本记录由本席派出的**达档子代理渲染**(`Agent`,档位即 `CONTRACT_REVIEW_TIER` 所指的那个常量,⛔ 此处不重复其字面值,即 `CONTRACT_REVIEW_TIER` 所指档位),由 seat 5 **采纳并发布**。⛔ 不是本席自签:渲染方与采纳方是两个不同的执行体。</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5786226800,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-22T23:39:47Z","updated_at":"2026-09-22T23:39:47Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5786226800","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `66c2de0346e1445ea3707dcb6f0ddecc95226284`\n\n<sub>Reviewed and posted 2026-09-22T23:39Z by the at-tier review subagent the `domain:spec#5` seat spawned (⛔ not the seat self-signing). This record replaces `5784524529` (governed `0e5c7704bc`); it and the six before it are void with their heads — that record's blocking finding was this round's order and is re-measured here. The head is exactly three commits on `0e5c7704bc` (`47eb9321e2` arms · `af24584e9e` pins · `66c2de0346` two swept sites), a fast-forward: **all SIX prior review heads are ancestors** (each `merge-base --is-ancestor` exit 0). ⚠️ **21** commits ahead of merge-base `fb7b74691f` — the order and the round both say 20. Readings taken detached at the head, lockfile byte-identical to `main`, closures built under `os-verify-lock`. Read before judging: the PR body, record `5784524529`, the round report `5785866524`, `security-plugin.ts:1790–1995` and `:3424–3453`, `canWriteObject` `:5108–5380` **side by side with the middleware**, `system-write-guard.ts:80–160`, `delegated-admin-gate.ts` in full plus an `opCtx` member census, `engine.ts:3935–4045` / `:10845–10920`, the changeset, `system-context.mdx:104`, both pin files in full, `scripts/ablation-replace.mjs`, `lint.yml:4833–4916`, and the head's 39 check runs with the failing job's 196-step ledger.</sub>\n\n### ① Derived judgments\n\n**1. The arms ARE the middleware's own primitives, at the middleware's own point — ⛔ not a re-derivation.** Middleware `:1941` calls `assertEngineOwnedWriteAllowed(ql.getSchema(opCtx.object), opCtx.operation, opCtx.context)`; probe `:5266` calls the **same exported function** (`system-write-guard.ts:109`) over the **same engine object**. Middleware `:1963` calls `this.delegatedAdminGate.assert(opCtx)`; probe `:5282` calls `assert({object, operation, context, data})` on the **same instance** (`:1507`). Both sit after the `isSystem` bypass and before any permission set resolves, in the same relative order, inside the fail-closed `try`. The affordance logic is called only inside the primitive. Arm numbering 1–9 matches the code.\n\n**2. The catch semantics — verified, and right.** Inner `catch (e) { if (e instanceof PermissionDeniedError) return false; throw e; }` (`:5284–5291`); the outer catch logs `error` and returns `false`. One `PermissionDeniedError` class across all three files ⇒ `instanceof` holds. ⭐ An ADR-0103/D12 refusal is the write path's **own answer**, ⛔ not a failure to resolve one. Observation: the D12 gate swallows its own `resolveSets` failure into `sets = []` and then refuses — so a resolution fault surfaces as a silent `false` on the probe exactly as it surfaces as a silent 403 on the write path. **Equal, which is the pin's whole standard.**\n\n**3. ⚠️ The `assert` argument shape — enumeration verified, asymmetry real and in the safe direction, but the round did NOT enumerate one thing the gate does: it WRITES.** Census over all 1103 lines: `object` 21, `operation` 27, `context` 1, `data` 2, the scalar id once; `ast`/`result` never — the same SET the round reports. Reachability confirmed (⚠️ the tenant-admin return is **`:214`**, ⛔ not the `:247` the report cites). The id-less asymmetry is **NARROWER, never wider** — acceptable in direction.\n⛔ **But `materializeTargets` returns the insert rows BY REFERENCE (`:981`), and `assertAssignmentWrite` `:635–636`, `assertDirectGrantWrite` `:674–675` and `assertSelfDelegation` `:546` stamp `granted_by = ctx.userId` onto them** — while `validate()` hands the probe `rawRows`, **the caller's own objects** (`engine.ts:10914`). ⇒ a scope-holding delegate's insert **PREVIEW** on `sys_user_position` / `sys_user_permission_set` **mutates the caller's payload**. Code-cited; runtime NOT MEASURED (needs BU/scope fixtures). ⭐ **That is precisely the side effect the round itself names as reason (ii) against an ADR-0056 arm** — present in the arm it did add. ⚠️ And the shipped docblock says arm 3 is 「pinned EQUAL … arm for arm」 (in both packages' `.d.ts`) while the 「NARROWER」 disclosure is a body comment that **does NOT ship** (0 in all eight declaration files).\n\n**4. ⭐ Both ablations RE-RUN by this reviewer.** Baseline 70/70. Arm 2 off: blob `fb265494bf → 31b15698ce`, **exactly 6 red** — the same six. Arm 3 off: blob `→ 64b8d229d0`, **exactly 8 red** — the same eight. Both restored **blob-identical** (`fb265494bf`), porcelain 0. **Red sets disjoint** ⇒ each pin is held by its own arm.\n\n**5. The pins — read in full; both directions on both doors, discipline intact.** The equivalence block still asserts only `expect(answered).toBe(admitted)` against the real registered middleware (`:349`). Both-direction coverage present: engine-owned refused (insert+update) AND the `userActions`-reopened twin admitted; RBAC table refused AND the tenant admin admitted (context NAMES `ADMIN_FULL_ACCESS`); `isSystem` and principal-less controls for each. ⚠️ The divergent principal-less semantics are **true of the middleware**: D12 throws `if (!ctx.userId)` at `:198` BEFORE the fall-open `:1975`; ADR-0103's `isUserContextWrite` is false without `userId` so it bypasses and the fall-open admits.\n\n**6. The 「superuser wildcard」 fixture — vacuous, measured.** With `sets [ADMIN_SET]` and `permissions: []`, `resolvePermissionSetsForContext` → `[\"member_default\"]`; both doors `false`. Naming the set → `true`/`true`. **Green and vacuous with respect to its label**; a one-line fix. ⛔ Not this round's defect class, ⛔ not a FAIL ground.\n\n**7. Y1/Y3 on the composed runtime — after AND before.** After: the 12 parity cases green — `insert()`/`update()` 403 with **ZERO** related reads, `validate()` in both modes ZERO reads and the rule's message ABSENT, for both classes; each opened control after exactly ONE read on both doors. Before: with each arm ablated the parity pins fail with `expected 1 to be +0` — the preview issued ONE related read — **while the write-path cells stay green: only the preview's door moved.**\n\n**8. ADR-0056 — the three reasons hold; disposition right.** (i) inline at `:1832–1871`, no exported predicate. (ii) **TRUE and new**: the admit branch runs `delete (row)[field]` over every row of `opCtx.data` before `next()` ⇒ an arm could not be the branch. (iii) one constructor, whose context goes to the real write; the key is absent from `ENTRY_EXECUTION_CONTEXT_FIELDS`. Naming it under the not-closed families is the right landing. Nit: 「no caller can present the grant here」 is true of every constructor and of the wire, ⛔ not of an in-process caller.\n\n**9. The restated prose — true at all five sites, ⛔ no category word for the bound.** All seven passages state the bound by **naming the arms**, each followed by 「⛔ not an enumeration of the distance to success」 and the not-closed families. Residuals: (a) 「the fail-closed postures」 is a plural group label expanded only at `security-plugin.ts:5198`; (b) 「pinned EQUAL … arm for arm」 over-states arm 3 by ①3; (c) ⛔ **the 「After `next()`」 bullet cites `#16608`, which is HTTP 404** — see ③.\n\n**10. Emitted `.d.ts` sweep — reproduced; the normalisation is sound and SYMMETRIC.** One function (strip `^\\s*\\*\\s?`, collapse whitespace) applied to **file AND phrase**, with an absent-phrase control at 0. The 8 removed phrasings (plus 5 of the reviewer's own) are **0 in all 64 cells raw AND normalised**; the 11 new phrases are 1 in each of the four shipping cells normalised, and the raw grep under-reports on exactly the four rows that straddle a wrap ⇒ **the zeros are not manufactured.** ⚠️ Additionally: `#16608` **ships** (objectql util 3, plugin-security 1) and 「NARROWER than the write path」 does **not** (0 everywhere).\n\n**11. ⚠️ Gates: 102 families is true of the LIST and blind to the gate that reds CI.** Clearing a build prerequisite beats declaring NOT MEASURED — **right**; the `--shallow-since` deepening predates the merge-base so the negative ancestry reading is valid — **right**. ⛔ But the derived list's row 73 is `pnpm check:issue-citations`, the **offline `--self-test`**, while CI's step is `pnpm check:issue-citations && node scripts/check-issue-citations.mjs` — **the LIVE diff-scoped verdict** (`lint.yml:4835–4841, :4916`). Run at the head: **exit 2**, the two `#16608` lines. ⚠️ **The round's report carries no CI reading at all, although the job had failed at 22:50:25Z — 24 minutes before the report.**\n\n**12. ⛔ CI at the head — 39 runs → 35 names: 1 FAILURE, 4 skipped, 30 success.** `Lint & Repo Gates` `completed/failure` (run `35793244250`): 185 steps green, **step 188 「Issue citations this change adds resolve on the board」 red** — `2 citation(s) THIS CHANGE ADDS do not resolve … [allocated-but-absent] engine.ts:4017 #16608 … security-plugin.ts:5235 #16608` — and **10 steps SKIPPED**, because the job stops at the first non-zero exit ⇒ those gates are **NOT MEASURED in CI**. The same job was green on `0e5c7704bc` (20:44Z) and on `main` (22:10Z) ⇒ **this round caused it.** `Check Changeset` success with the label on (expected). This red is ⛔ not a merge artefact — it reproduces on the branch tree.\n\n**13. Model identifier: none** — title, body, all 21 commit messages, every added line, the changeset, the report. History: one identity, draft, 0 reviews, no force-push.\n\n**14. Body vs code — seven passages stale at THIS head, all the seat's:** (1) 「測於 head `e6036cb3a17f`」 ×3; (2) ⭐ the dry-run bullet still carries 「the OBJECT-level and FIELD-level halves」 and 「admit **at the object and field level**」 — **the body itself carries the category phrasing the code was ordered to drop**; (3) 「the one open blocking finding … is being repaired」; (4) 「disabling that arm alone turns exactly 9 tests red」 — arm 7 is now arm 9 and two more ablations exist (6/8); (5) the `plugin-security` row `+899 / +205` — now 3 files **+1397** (`security-plugin.ts` +307, admission +514, parity +576); (6) 「78 commands」 — 102 families; (7) 「Nothing is NOT MEASURED at this head」 and 「0 red checks」 — **one red, ten unmeasured.** Whole PR: 20 files, +3730/−24.\n\n### ② Semver level\n\n`minor` on the four packages, `Clause-②: yes (widening)` — correct and unchanged; the two new arms narrow a method **this PR itself introduces**, ⛔ not a released surface.\n\n### ③ Boundary flags\n\nBlocking — the FAIL rests on this one. ⭐ **The ordered repair is delivered and measured true at every point; what fails is that the repair's own prose REDS the head.**\n\n1. **A red check at the head, caused by this round: the dangling `#16608` citation.** `engine.ts:4017` and `security-plugin.ts:5235` cite `#16608`, which is **HTTP 404**; `check-issue-citations` (the diff-scoped live half) exits **2** on exactly those two lines; `Lint & Repo Gates` is `failure` at `66c2de0346`; **10 gates behind it are NOT MEASURED in CI**; and the citation **ships** in both packages' emitted declarations.\n   **Fix:** in those two bullets, either **drop the number and name the mechanism** (the post-`next()` assertion that `OperationContext.postHookWriteImageCheck` was honoured, `security-plugin.ts:3426`), or **keep `#16608` and say in prose that it no longer resolves and what the live record is** — ⛔ **no replacement number** (`#16805`, the commit's PR, is also 404). Then rebuild, re-sweep the four shipping cells, and the seat re-reads the head's checks: **every name green, none skipped by a stop.**\n\nNon-blocking:\n\n- ⭐ **Arm 3's payload mutation and its unshipped asymmetry.** Hand the D12 arm **shallow copies of the rows** so the gate's `granted_by` stamp cannot land on a preview payload (the decision is unchanged by the stamp), and **either** pin the delegate id-less-update direction (probe refuses, middleware admits — a DIRECTION block, ⛔ not equivalence) **or** state that one exception in the shipped 「pinned EQUAL … arm for arm」 sentence.\n- 「the fail-closed postures」 — expand to 「#3545's unresolvable posture and the D10 dangling delegator」 at `engine.ts:3976`, changeset `:95`, `mdx:104`, as `security-plugin.ts:5198` already does.\n- The 「superuser wildcard」 equivalence case — name the set (`permissions: [ADMIN_FULL_ACCESS]`); may ride with the round now owed.\n- 「no caller can present the grant here」 → 「no wire caller / no constructor in the tree」.\n- **Report accuracy (⛔ not shipped):** the tenant-admin short-circuit is `:214`, ⛔ not `:247`; the branch is **21** commits, ⛔ not 20.\n- **Body: the seven passages in ①14 are the seat's.**\n- Unchanged from prior records: the generic wrong-object prescription on a probe-denied preview; the parity file's two-limit list without the marker; the accept control's two `crm_account` reads.\n\nImplemented-by: `claude/issue-18682-predicate-relationship-traversal`\nReviewed-by: `session_01Sfe5YjBLwB9J3y8fvm2xq1` (at-tier contract-review subagent of the dispatching seat)\n\n**VERDICT: FAIL**\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5787171183,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T01:02:33Z","updated_at":"2026-09-23T01:02:33Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19728#issuecomment-5787171183","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `c12c0ec26d3b8cefc2be34c157dc075680b89dea`\n\n<sub>Reviewed and posted 2026-09-23T01:02Z by the at-tier review subagent the `domain:spec#5` seat spawned (⛔ not the seat self-signing; the constant `CONTRACT_REVIEW_TIER` names the tier). Read before judging: the PR body and the full diff against merge-base `fb7b74691f` (verified: `merge-base origin/main HEAD` == `fb7b74691f`), the round-7 record `5786226800` (VOID with its head, input only), the round-8 order `5786286770`, the round-8 report `5786873880`, the seat's rulings `5786914949`, `canWriteObject` `:5249–5400` side by side with the middleware `:1935–1975`, `delegated-admin-gate.ts` in full plus my own `opCtx` member census, `security-plugin.ts:3400–3455`, `engine.ts:3970–4030` / `:10855–10935` / `:11520–11535`, the changeset, `system-context.mdx:104`, both pin files' harness, `delegated-admin-gate.test.ts`'s fixtures, `field-masker.ts:205–245`, `assemble-execution-context.ts:117–139`, `lint.yml` for the 8 skipped steps' `if:`, `pr-automation.yml` and `ci.yml` for the 4 skipped jobs' `if:`, and the head's 39 check runs plus the `Lint & Repo Gates` 196-step ledger. ⛔ All work in my own sibling worktree `objectstack-rev8-18682`, removed at hand-back; the primary checkout was never written to and is clean on `main`. **NOT MEASURED by me:** the repo-wide lint sweep and the full derived gate-family list (I read CI's green 196-step ledger instead of re-deriving it); the dogfood suites and the `formula` / `lint` / `example-showcase` / `rest` / `runtime` / `plugin-sharing` suites locally (CI `Dogfood Regression Gate` +3 shards and `Test Core` 1/6–6/6 are green at this head); the arm-3 DIRECTION case as an *equivalence-harness* pin (see ③).</sub>\n\n### ① Derived judgments\n\n**1. The ordered repair is delivered, and the mechanism prose is TRUE.** Both diff-added bullets (`engine.ts:4023–4026`, `security-plugin.ts:5240–5242`) now read \"the fail-closed assertion that the engine honoured `OperationContext.postHookWriteImageCheck`\". I checked the named mechanism rather than the sentence: the assertion sits at `security-plugin.ts:3426–3452`, **after** `await next()` (`:3424`), fires on `insertCheckSeam.honoured !== true`, and throws `PermissionDeniedError` — while the engine sets `honoured = true` at `engine.ts:11525` immediately before `evaluate(live)` at `:11531`, and the seam is installed at `security-plugin.ts:3034`. So \"fail-closed\", \"the engine honoured\", \"the insert `check` seam really ran\" and \"judges an executed write\" are each true of the code. ⛔ No replacement number: my own probes give `#16608` → **404**, `#16805` → **404**, `#3545` → **200**.\n\n**2. The diff-added citation count is genuinely zero — measured two independent ways.** (a) Every `#NNNN` token on diff-added lines vs `fb7b74691f`: `#18682`×44, `#3545`×6, `#6457`×5, `#4649`×2, `#8215`, `#7537`, `#4977`, `#2706`, `#1915`, `#14253`, `#10126`, `PD #10` — **no `#16608`, no `#16805`**. (b) The live gate, **run by me at the head as CI spells it**: `pnpm check:issue-citations && node scripts/check-issue-citations.mjs` → **exit 0** (\"39 citations judged across 9 files: 38 resolves, 1 cross-repo-unjudged\"). Scoped to the repair commit alone (`--base 66c2de0346`): 1 citation, resolves, exit 0. ⛔ The `--self-test` alone was *not* taken as the reading.\n\n**3. ⭐ The nine pre-existing citations are untouched — proved against the MERGE-BASE, not the previous head.** `#16608` count in `engine.ts`: **4 at merge-base, 4 at this head**; in `security-plugin.ts`: **5 at merge-base, 5 at this head**. So the round added its two and removed exactly its two, and the residue is byte-for-byte the pre-existing nine. Per the seat's ruling I judge only the citation this change ADDED; the two that reach the published declarations (`engine.ts:2206`, `:2235` → dist `3166`, `3194`) are **expected to remain non-zero** and are ⛔ not a finding here.\n\n**4. ⭐ The shallow-copy fix is CORRECT, COMPLETE and correctly SCOPED — and I measured it end-to-end, which three rounds recorded as NOT MEASURED.** Reading first: the gate has **exactly three** property-assignment sites in all 1103 lines (`:546`, `:636`, `:675`), all the same top-level scalar `granted_by`; no bracket assignment, no `Object.assign`, no `push`/`splice` on inputs, no mutation of `opCtx` or `context`. All three read their row from `rowsOf(opCtx.data)` — directly at `:546`, and via `materializeTargets`'s insert branch (`:981`, by reference) at `:636`/`:675`; the update branch builds a fresh `{...prev, ...next}` and stamps insert-only anyway. ⇒ a **shallow** copy fully interposes, because nothing nested is written. `detectForbiddenWrites` (arm 9, which still gets the raw `data`) is read-only — verified. The copy sits **at the arm**, so it protects every caller of `canWriteObject`, and the probe registered at `engine.ts:4057` is *exactly* `canWriteObject` (`security-plugin.ts:1247–1250`) ⇒ every path from `validate()` is covered; the middleware's own `assert(opCtx)` at `:1963` and invitation-placement's are deliberately untouched, so the real write keeps its dual-audit stamp. Then the measurement — a throwaway probe on `delegated-admin-gate.test.ts`'s own BU/scope fixtures, run and deleted: **(A)** handed a raw row, the gate stamps `granted_by='u_delegate'` onto the caller's object *by reference* (the round-7 defect, reproduced); **(B)** through the real booted plugin, `canWriteObject('sys_user_position','insert',…,[row])` leaves `'granted_by' in row === false`. ⇒ the fix works on the live path that reaches the stamp. (The `!Array.isArray(row)` guard leaves an array-valued element uncopied; I traced all three branches and each *throws* before any stamp on such input, so it is not even a theoretical gap.)\n\n**5. ⭐ The shipped \"ONE exception … NARROWER, never wider\" claim is TRUE — and I nearly called it false before testing it.** Reading the branch bodies alone suggests three fail-OPEN directions when the pre-image is missing (`assertSetAuthoring`'s `if (existing && …)`, `assertAssignmentWrite`'s `if (t.prev) {…}`, `assertBindingWrite`'s anchor + blast-radius checks keyed on `position_id`). **They are all unreachable**: `assert()` refuses at `:267–275` — `isMutationWithoutId` — **before the branch switch**, for any update/delete/transfer/restore/purge whose `extractSingleId` is null. Measured: **(C)** the preview's shape (`update`, array `data`, no id) throws \"delegated administrators must target single rows by id\"; **(D)** the same update *with* an id reaches the branch and is judged there (allowlisted set → admitted, non-allowlisted → refused with a *different* reason). ⇒ probe refuses where the middleware admits — **NARROWER**, and \"never wider\" holds because the id-less refusal is unconditional and pre-branch. My own `opCtx` member census over the gate confirms the exception is structurally the *only* one: `assert` reads `object` (21), `operation` (27), `context` (1), `data` (2) and the scalar id (`options`/`where`/`id`, 1 each) — no `ast`, no `result` — so the probe's four-key `opCtx` differs from the middleware's in the id and in nothing else.\n\n**6. Both ablations RE-RUN by me, and they reproduce exactly.** Baseline over the two pin files: **70/70**. Arm 2 (ADR-0103) off: blob `76cdd45c1e24 → ac2eb8c5f472`, marker live in 2 built files, **exactly 6 red** — the four ADR-0103 admission cases plus the two Y1 parity cases. Arm 3 (ADR-0090 D12) off: blob `→ bddb2ec50135`, marker live in 2 built files, **exactly 8 red** — the six D12 admission cases plus the two Y3 parity cases. **Red sets disjoint** (intersection 0: arm 2's are all engine-owned/Y1, arm 3's all RBAC-link-table/Y3). Both restores **blob-identical** to HEAD (`76cdd45c1e24`), `ablation-dist-preflight --absent` clean on each leg, and whole-tree `git status --porcelain` **empty** at every restore and now.\n\n**7. ⭐ The shallow-copy fix is pinned by NOTHING — measured at whole-package scope.** I ablated the copy alone (`data.map(shallow)` → `data` passed through, marker live in dist) and ran the **entire** `@objectstack/plugin-security` suite: **119 files / 2319 tests, all green**, identical to baseline. The type-checker does not catch it either — the natural edit also deletes `shallow`'s only use, so it compiles clean. This confirms the seat's premise and sharpens it: the gap is not \"the two pin files don't cover it\", it is that *nothing in the package does*. Per the seat's ruling this is ⛔ not a FAIL ground; it is already ordered as round 9.\n\n**8. Emitted declarations — ONE normaliser (strip `^\\s*\\*\\s?` per line, collapse whitespace) applied to FILE **and** PHRASE alike, over all 8 declaration files, raw **and** normalised.** Removed wordings (`#16608 fail-closed assertion that the insert`, `The #16608 fail-closed assertion`): **0/0 in all 8 cells**. The new mechanism phrase: **raw 0 / normalised 1** in each of the four shipping cells — and the raw zero is a genuine wrap, not a manufactured one: the phrase straddles `plugin-security/dist/index.d.ts:729–730`. `#16608`: **0/0** in both plugin-security cells (was 1), **2/2** in both objectql `util-B_Rn613Q` cells (the two pre-existing seam docblocks). Controls: absent-phrase **0 everywhere**; the *replaced* sentence \"pinned EQUAL to the registered middleware's, arm for arm. It means nothing\" **0 everywhere** (so the over-claiming spelling no longer ships); present controls `postHookWriteImageCheck` 3/3/1/1 and `canWriteObject` 1/1 in the shipping cells. The three other newly-shipped phrasings (\"NARROWER, never wider\", \"with ONE exception disclosed here because it is not pinned\", \"no wire caller and no constructor in the tree\", and the expanded fail-closed postures) are each normalised 1 in the four shipping cells.\n\n**9. Over-claim sweep of the round's ADDED prose — every completeness word verified, none false.** (a) \"the fail-closed postures (#3545's unresolvable posture and the D10 dangling delegator)\" — the method has exactly **two** posture `return false` sites (`unresolved`, `del.kind === 'missing'`); the other `return false`s are grant/capability checks, the catch, and the empty-name guard. TRUE. (b) \"no wire caller and no constructor in the tree presents the grant here\" — `publicFormGrant` has exactly **one** constructor in the tree (`rest/src/rest-server.ts:10750`, the form-submit route, which goes to the real write) and the key is **absent** from `ENTRY_EXECUTION_CONTEXT_FIELDS`' 21 members. TRUE. (c) \"nothing reads `granted_by` back, it is only ever written\" — TRUE of the gate and of the decision: within the D12 path `granted_by` is only ever assigned; every read-back in the tree is on `sys_record_share` (a different object, stored rows, plugin-sharing), not on the in-memory payload this method judges. (d) \"both ahead of every resolution\" — arms 2 and 3 do precede `resolvePermissionSetsForContext`. TRUE. ⇒ ⛔ **no fourth false completeness claim this round.**\n\n**10. The \"superuser wildcard\" fixture is no longer vacuous — measured both ways.** With the shipped spelling (`permissions: [ADMIN_FULL_ACCESS]`) both doors answer **true**; with the round-7 spelling (no name) both doors answer **false**. So the round-7 vacuity finding is confirmed *and* the repair is confirmed to have removed it — the case now exercises the wildcard it is labelled for.\n\n**11. ⛔ CI at the head, de-duplicated by name keeping the latest `started_at`: 39 runs → 35 names, 31 success, 4 skipped, 0 failure.** ⚠️ This is **31/4, not the report's 33/2**: `Auto Label` and `Check PR Size` were `success` on the push run (23:49–23:50Z) and were re-queued as `skipped` at **00:33:53Z** by the seat's own `needs:contract-review` label write (run `35802612568`). Both skip by their **own `if:`**, which excludes `labeled`/`unlabeled`/`edited` (`pr-automation.yml:141–144`, `:211–215`). The other two: `Console Pin Gate` (`if: !cancelled() && needs.filter.outputs.console != 'false'`, `ci.yml:2337`) and `Packed-tarball smoke (opt-in)`. ⇒ **no name is red and none is skipped by a stop.**\n\n**12. ⭐ `Lint & Repo Gates` — the STEP ledger read, and the skips are by their own `if:`, not by a stop.** `completed / success`, **196 steps: 188 success, 8 skipped**. Step **188 \"Issue citations this change adds resolve on the board\" is SUCCESS** — the exact step that was red at `66c2de0346`. The 8 skips are numbers 18, 34, 36, 38, 69, 70, 191, 192 — **scattered, with 171 successes after the first of them**, which a first-non-zero-exit stop cannot produce. Each carries its own condition: six are scoped gate-family selectors (`steps.gate-families.outputs.* != 'skip'`) and 191/192 are `if: failure()` — the unmeasured-gate-tail reporter, skipped *because nothing failed*. ⇒ ⛔ **no gate is NOT MEASURED behind a stop at this head.** The round-7 blocking finding is closed by my own reading.\n\n**13. Build, typecheck, suites — all mine, at this head.** `turbo build` over both packages and their closures: **exit 0** (18 tasks). `typecheck`: `@objectstack/plugin-security` **exit 0** (test layer 0 files / 0 errors); `@objectstack/objectql` **exit 0** (debt ledger unchanged at 40 files / 234 errors / 65 pinned signatures). Suites: `@objectstack/plugin-security` **119 files / 2319 tests** pass; `@objectstack/objectql` **306 files / 5101 tests** pass. Lockfile byte-identical to `main`; `pnpm install --frozen-lockfile` clean.\n\n**14. Model identifier: none.** PR title, body, all **22** commit messages, every diff-added line (and the whole diff, both directions), the changeset, and the round-8 report — swept for every model-identifier spelling (family names, versioned ids, provider-prefixed ids and the context-window marker; ⚠️ the pattern list is withheld here by the seat under AGENTS.md:446): **zero hits everywhere**. History: one author identity (`Claude <noreply@anthropic.com>`), draft, **0 reviews**, and **all eight prior review heads are ancestors** of this head (`995266cea0`, `1201afdfc7`, `ac98ad3df5`, `e6036cb3a1`, `0e5c7704bc`, `47eb9321e2`, `af24584e9e`, `66c2de0346`) ⇒ fast-forward only, no force-push rewrite. 22 commits, 20 files, +3770/−24.\n\n### ② Semver level\n\n`minor` on `@objectstack/formula`, `@objectstack/lint`, `@objectstack/objectql`, `@objectstack/plugin-security`, with `Clause-②: yes (widening)` — **correct and unchanged.** The round's own change is prose plus a defensive copy; it removes nothing and narrows nothing that was released. The two authoring-time refusals stay `minor` on the standing ground that both shapes faulted at evaluation before, so nothing that worked stops working; the new arms narrow a method **this PR itself introduces**, ⛔ not a released surface. `Check Changeset` is green at the head with the label on.\n\n### ③ Boundary flags\n\nBlocking: **none.** The one blocking defect of round 7 is repaired the one way the seat measured, and I confirmed the repair independently at every point: the mechanism prose is true of the code, the diff-added citation count is zero by two methods, the live gate exits 0, `Lint & Repo Gates` is green, and none of its 8 skipped steps — nor any of the 4 skipped jobs — is behind a stop.\n\nNon-blocking:\n\n- ⭐ **The shallow-copy fix is pinned by nothing, at whole-package scope (①7).** Ablating the copy alone leaves **119/119 files and 2319/2319 tests green**, and the compiler does not catch it either. Already ordered as round 9 and ⛔ not a FAIL ground here — recorded so round 9 can state the scope of what it is closing, and so the 8 → 9 arm-3 move is expected rather than drifted.\n- ⭐ **The \"for want of BU/scope fixtures\" blocker is not real — the fixtures already exist.** `packages/plugins/plugin-security/src/delegated-admin-gate.test.ts` carries the whole topology (four BUs `hq/east/east_sales/west`, `EAST_SCOPE` with `manageAssignments`/`manageBindings`/`authorEnvironmentSets`, an allowlisted `sales_user` and a non-allowlisted `finance_admin`). I drove both the mutation pin and the id-less DIRECTION case off them in minutes. ⇒ round 9's pin **and** the arm-3 DIRECTION case are both cheap; ⛔ neither needs new fixtures.\n- ⚠️ **Report correction — the CI tally.** The report's \"33 SUCCESS / 2 skipped\", repeated in the seat's ruling table, is three minutes stale: my dedup gives **31 success / 4 skipped**, because the seat's own label write at 00:33:53Z re-queued `Auto Label` and `Check PR Size` as skipped. Verdict unchanged (both skip by their own `if:`), but the number should not be carried forward as-is.\n- ⚠️ **Report corrections — counts.** `@objectstack/objectql` is **306 files / 5101 tests** at this head (report: 305 / 5096). The `plugin-security` package diff is **3 files / +1426** — `security-plugin.ts` **+331**, `can-write-object-admission.test.ts` **+519**, `write-preview-field-gate-parity.test.ts` **+576**.\n- **This round ships a NEW citation into a published declaration.** `#3545` goes 0 → 1 in `engine.ts` and now appears in `@objectstack/objectql`'s emitted declarations (normalised 1 in both `util-B_Rn613Q` cells; 10 in plugin-security's, pre-existing). It **resolves** (HTTP 200, my own probe) so it is ⛔ not a dangling number — recorded only so the count is not a surprise to the card the seat is filing for the `#16608` residue.\n- **Still NOT MEASURED, unchanged by this round:** the arm-3 DIRECTION case as a pin inside the *equivalence harness*. I measured the direction at the gate level (①5), but the harness's own middleware door supplies no id either (`middlewareAdmits` builds `options: {}`), so a harness-level DIRECTION case needs that door to carry one — which is the second half of the previous bullet, not a fixture problem.\n- **Body: the seat's debt, ⛔ not the dev's** (the standing dev contract forbids a dev re-sending a body; this round correctly did not touch it). Stale at this head, for ONE pass: (1) \"測於 head `e6036cb3a17f`\" ×3 and \"measured at head `995266cea0`\" ×3 — the head is `c12c0ec26d`, four commits on; (2) ⭐ the dry-run bullet still carries \"the OBJECT-level and FIELD-level halves\" and \"admit **at the object and field level**\" — the body still uses the *category* phrasing the code was ordered to drop; (3) \"the one open blocking finding … is being repaired\" — closed; (4) \"disabling that arm alone turns **exactly 9** tests red\" — there are now two arms at **6** and **8**; (5) the `plugin-security` row \"+899 / +205 / +364 / +330\" — now **+1426 / +331 / +519 / +576**; (6) \"**78** commands\" — the derived list is larger and, more to the point, row 73 is the offline `--self-test`, not the live gate; (7) \"**Nothing is NOT MEASURED at this head**\" and \"the head carries **0 red checks**\" — the first is false (two items are declared NOT MEASURED above), the second was true of an older head and is true again now, but not as written; (8) \"`@objectstack/objectql` test | 305 files / 5093 tests\" — now 306 / 5101; (9) the body carries **two** \"Generated by Claude Code\" trailers.\n- Unchanged from prior records and not re-litigated: the generic wrong-object prescription on a probe-denied preview; the parity file's two-limit list without the marker; the accept control's two `crm_account` reads.\n\nImplemented-by: `claude/issue-18682-predicate-relationship-traversal`\nReviewed-by: `session_01Sfe5YjBLwB9J3y8fvm2xq1` — at-tier review subagent spawned by the `domain:spec#5` seat\n\n**VERDICT: PASS**\n"}
