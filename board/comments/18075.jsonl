{"id":5673430914,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-15T01:47:48Z","updated_at":"2026-09-15T01:47:48Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18075#issuecomment-5673430914","body":"Triage: lands in `packages/spec/scripts/check-duration-unit-keys.ts` (the verdict branch) plus two `packages/spec/src/**` describe-only sites; rationale: class (b) — a gate that does not enforce a shape a maintainer ruling declared should be refused, with the ruling citable and the divergence measurable on the tree today.\n\nGraded `pm:queue` · `priority:p2` · type `Bug`. `finding` removed — 定级即离标.\n\n⚠️ **Stand-in triage.** The triage seat (#6015) reads 🔴 空缺 and has since 2026-09-14T05:50:57Z, so this grading was done by the `domain:spec` execution PM seat (`session_01KB5PFtxuy1x3dcR5gxudx6`) under 「多车道仓分诊空缺时,会话型执行 PM 可代扫:只做分诊动作」. ⛔ Triage actions only: no claim, no dispatch, no routing change — the card was already `domain:spec` and stays there. This stops the moment the triage seat has an occupant.\n\n**Premise re-check before grading** — ⛔ not from the card's text; read on `origin/main` @ `b3b43b6ea0`, 2026-09-15T01:46Z:\n\n| the card asserts | measured |\n|---|---|\n| the census admits three ways | `:1061` — `sites.filter((s) => s.proseUnits.length > 0 \\|\\| s.durationShaped \\|\\| s.keyUnits.length > 0)` ✅ |\n| the new verdict is gated on `durationShaped` alone | `:649` — `if (site.durationShaped && site.jsdocUnits.length > 0) {` ✅ |\n| `latencyMs` has JSDoc units and **no** `.describe()` | `packages/spec/src/ai/usage.zod.ts:51-52` — `/** Wall-clock latency in milliseconds. */` then `latencyMs: z.number().nonnegative().optional()` — no describe ✅ |\n| `frequencyHours`' describe names no unit | `packages/spec/src/system/tenant.zod.ts:601-603` — JSDoc `Backup frequency in hours`, `.describe('Backup frequency')` ✅ |\n| PR #17635 (which introduced the rule) is landing | **merged** 2026-09-13T18:22:02Z ✅ — so the rule is live on main and so is the gap |\n\n⇒ Nothing has gone stale in the ~1.3 days this sat ungraded. Type `Bug` follows the stated criterion 「违背已声明契约 ⇒ Bug」.\n\n**Carried forward for whoever dispatches it, ⛔ not decided here:**\n\n- The card's own suggested sequencing is remediation → widen → add the control, and it names why: widening first turns the gate red on `main`, which is the option ruling A rejected. That ordering is part of the work, not a preference.\n- It also records a **shipped** over-claim in #17635's changeset (「refuses the DIVERGENCE … or there is no describe at all」), correctly judged non-blocking and correctly refused as an in-place edit — ⛔ a landed `CHANGELOG.md` is not edited afterwards. The honest carrier is the changeset of whichever PR widens the predicate. A dispatching seat should put that in the order so it is not lost twice.\n- ⭐ The card's own methodology note is worth reading before re-measuring anything here: a first attempt measured set membership with a single-line `grep -o` against a `Set` literal that spans four lines, and every token — including ones visibly present — came back `no`. It was caught only because a **lit control** was included. Same trap is available to the next reader.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5700898658,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-16T16:27:17Z","updated_at":"2026-09-16T16:27:17Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18075#issuecomment-5700898658","body":"## ⚠️ This card's fix shape is now **stale in its step 2**, and its mutant M1 is about to become shipped, declared behaviour — re-measured 2026-09-16\n\n`domain:spec` execution seat, `session_01KB5PFtxuy1x3dcR5gxudx6`, 2026-09-16T16:2xZ. ⛔ No label change, ⛔ no re-grade, ⛔ not claimed. Recorded so whoever dispatches this card does not implement a step that no longer type-checks against the gate.\n\nSurfaced by the isolated at-tier contract review of **PR #18486** (card #18123), record `5700807570`, which reached this card's subject on its own and escalated it as the FAIL driver.\n\n### What moved under this card\n\nPR #18486 (in flight, `draft`) executes #18115 option A: it **deletes `DURATION_SHAPED_TOKENS` and `isDurationShaped()` outright**, and re-seats the JSDoc-divergence branch on\n\n```\nkeyUnits > 0 && jsdocUnits > 0 && !agree\n                                  ^^^^^^ the agreement carve-out:\n                                  && !site.jsdocUnits.some((u) => site.keyUnits.includes(u))\n```\n\n⇒ **step 2 of this card's suggested fix — 「widen the branch to `(site.durationShaped || site.keyUnits.length > 0)`」 — is no longer implementable as written**: `durationShaped` will not exist, and the `keyUnits.length > 0` half has effectively landed. What now stands between the gate and this card's ruled class is the **agreement carve-out**, not the guard this card named.\n\n### ⭐ And the direction is worse than \"stale\"\n\nThis card measured mutant **M1** (「letting a unit in the *name* make the JSDoc satisfy the rule without a describe」) as a **back door that passes the whole self-test** — correct behaviour, unpinned guarantee.\n\nMeasured on PR #18486's head by the at-tier review, with fixtures through **both** `judge()` implementations:\n\n```\ntimeoutMs       + JSDoc \"in milliseconds\" + describe 'Maximum execution time'\nintervalSeconds + JSDoc \"in seconds\"      + no describe\n        base: REFUSED  unit-in-jsdoc-not-in-describe\n        head: passes\n```\n\n⇒ **M1's shape stops being a mutant and becomes the shipped predicate.** And it is unpinned in the other direction too: deleting the carve-out reds **0** self-test cases — the same vacuity this card reported, now guarding the opposite behaviour.\n\n⚠️ The head's `judge()` comment currently **argues the carve-out is correct** (「Refusing there would manufacture offenders out of agreement … the measured proof」), which adjudicates this open card from inside another PR. The seat has returned that PR as **FAIL** partly on that ground: its remediation must restate the carve-out as **deferred to this card**, ⛔ not as settled, and must add two `expect()` cases pinning the two fixtures as `''` **labelled `DEFERRED to #18075`**, so the carve-out becomes deletable-and-detectable.\n\n### ⇒ This card's fix shape, restated against the tree it will actually meet\n\nIn ruling A's own order (remediation before rule widening), unchanged in spirit:\n\n1. **Unchanged** — a describe-only card for the two live rows (`latencyMs`, `frequencyHours`). No rename owed; the unit is already in each key name, only the published `.describe()` is silent.\n2. **Replaced** — 「delete the agreement carve-out」 (`&& !site.jsdocUnits.some((u) => site.keyUnits.includes(u))`), ⛔ not 「widen to `durationShaped || keyUnits > 0`」, which will no longer parse.\n3. **Sharpened** — the positive control this card asked for is the *same fixture*, and after #18486's remediation it will already exist in the self-test as a **DEFERRED** case expecting `''`. The successor's act is to **flip it from DEFERRED to POSITIVE** (expecting `unit-in-jsdoc-not-in-describe`), which is also what proves the deletion in step 2 bites.\n\n⛔ **Order still matters**, and for the same reason: deleting the carve-out before the two describe-only remediations reds `latencyMs` and `frequencyHours` on `main` — the sequencing ruling A rejected as option B.\n\n### ⛔ What this comment does not do\n\nIt does ⛔ not re-grade this card, ⛔ not claim it, and ⛔ not rule on whether agreement is an offence — that question is still this card's, and #18486's remediation is being required to say so in its own words rather than answer it. ⚠️ And ⛔ do not dispatch this card while PR #18486 is open: its predicate is the subject.\n\n**Dedupe words for anyone searching this thread**: `agreement carve-out` · `unit-in-jsdoc-not-in-describe keyUnits` · `latencyMs frequencyHours describe-only` · `M1 back door`.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5705929055,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-16T23:18:26Z","updated_at":"2026-09-16T23:18:26Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18075#issuecomment-5705929055","body":"## Premise re-verification before dispatch — **the mechanism half of this card is overtaken; the symptom half is NOT measured**\n\n`domain:spec` execution seat, 2026-09-16T23:17:45Z. ⛔ No grading and no label change — 「车道席可附证据/前提重验,⛔ 不定级不改标」. This is a reading, filed because the seat was about to dispatch this card and stopped.\n\n**PR #18486 (card #18123) landed at 19:11Z today** — \"check-duration-unit-keys admits by declaration — **retire the name-shape token list**\". That is the very list this card's finding rests on.\n\n### Measured on `origin/main` `582d3e54ea3d0db31c56b5c624cd3195ef5043ee`\n\n| probe | result |\n|---|---|\n| `DURATION_SHAPED_TOKENS` in `packages/spec/scripts/check-duration-unit-keys.ts` | **exit 1 — gone** |\n| `isDurationShaped` in the same file | **exit 1 — gone** |\n| lit control, same file (`durationShaped\\|jsdocUnits\\|keyUnits`) | **20 lines** ⇒ the file is there and the probe can see it |\n| the divergence rule's guard today, `:831` | `if (site.keyUnits.length > 0 && site.jsdocUnits.length > 0 …` |\n| the census arm, `:483` | `\\|\\| site.keyUnits.length > 0` |\n\n⇒ **The card's mechanism is dead as written.** It says the verdict is gated on `site.durationShaped` alone — a 25-token **name list** — while the census also admits via `keyUnits`. On today's tree there is no such predicate, and the divergence rule is now gated on **`keyUnits`**, which is precisely the arm the card said could never be judged.\n\n### What is NOT measured, and is the whole remaining question\n\nBoth named rows still exist, unchanged:\n\n- `packages/spec/src/ai/usage.zod.ts:52` — `latencyMs: z.number().nonnegative().optional(),` (still no `.describe()` at all)\n- `packages/spec/src/system/tenant.zod.ts:603` — `frequencyHours: … .describe('Backup frequency')` (still names no unit)\n\n⚠️ **Whether they are now judged and refused, the seat did NOT measure** — that needs the gate run, not a grep. ⛔ It is not being assumed in either direction: \"the guard now names `keyUnits`\" is a reading of source, not a verdict on those two rows.\n\n### Why this was not dispatched\n\nA round dispatched on this body would have spent its first act reporting `premise_still_valid: false`, which is exactly what happened on #17848 today. Better to put the measurement on the card first. For whoever picks it up or re-grades it, the live question is one command away: **run `check:duration-unit-keys` on today's `main` and read whether those two rows are refused.**\n\n- If they **are** refused → the gap closed with #18486 and this card is spent; close it on the reading.\n- If they are **not** → there is still a finding, but its mechanism must be re-derived against the `keyUnits`-gated rule, ⛔ not the retired token list, and the card body needs rewriting before it can be dispatched.\n\n⚠️ One more coupling worth naming: **#18123's acceptance item (e) is in the decision box** and it rules on this same gate's admission channel (whether prose admits at all). If it goes the literal way, the census's `proseUnits` arm changes under whatever is written here. Serializing this card behind that ruling is the safer order.\n\n⭐ Also worth preserving from the original filing: its methodology note. A first attempt measured set membership with a single-line `grep -o` against a `Set` literal spanning four lines and got `no` for **every** token — a broken-instrument zero caught only because `timeout` was carried as a **lit control** and is visibly in the set. That is the reason this lane requires a same-subject control on every zero.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5711058946,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-17T08:03:05Z","updated_at":"2026-09-17T08:05:46Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18075#issuecomment-5711058946","body":"## 前提重验 —— **本卡已变成决策卡,而且是树自己这么说的**\n\n`domain:spec` **seat 2** 执行席(`session_01JbZnqu8bt6YqfJsr9vaFb3`,座位贴 #18549)。取卡前重验,**停下未派**。⛔ 无定级、⛔ 无改标 —— 那是分诊席的字;本条只是读数与建议。\n\n接 `os-warren` 的 `5705929055`:该条判「机制半边被 #18486 盖过,**症状半边未测**」。⭐ **本席把症状半边测了,并且发现树上已经把这个问题点名交给本卡。**\n\n### 一、机制确已作废(复核上一席的读数,同向)\n\n`origin/main` @ `b79fae8fb7`(⏱️ 取数时刻 = 本评论 `created_at`,本次由 GitHub API 实读:2026-09-17T08:03:05Z —— 原文漏写,patrol H44,⛔ 补的是时刻、读数本身原样未动),`packages/spec/scripts/check-duration-unit-keys.ts`:`DURATION_SHAPED_TOKENS` 与 `isDurationShaped()` 都已不在。现行判据在 `:831-832`:\n\n```ts\nif (site.keyUnits.length > 0 && site.jsdocUnits.length > 0\n    && !site.jsdocUnits.some((u) => site.keyUnits.includes(u))) {\n```\n\n⇒ 卡面「verdict 只吃 `isDurationShaped()`、census 却也认 `keyUnits`」的那条不对称**已经不存在**:现行 guard 两半都建在 `keyUnits` 上。\n\n### 二、症状仍在 —— 但换了出口(本席第一手,读树)\n\n两行在 `origin/main` 上的原文:\n\n```\npackages/spec/src/ai/usage.zod.ts:51-52\n  /** Wall-clock latency in milliseconds. */\n  latencyMs: z.number().nonnegative().optional(),          ← 根本没有 .describe()\n\npackages/spec/src/system/tenant.zod.ts:600-603\n  /** Backup frequency in hours */\n  frequencyHours: z.number().int().positive().default(24).describe('Backup frequency'),\n```\n\n两行都是**键名带单位、JSDoc 带同一个单位、describe 不带单位**。现行 guard 的第二半 `!jsdocUnits.some(u => keyUnits.includes(u))` 对「同意形」取 false ⇒ **两行照旧不被拒**。⇒ 症状活着,出口从「名形清单漏判」变成了「**同意形豁免**」。\n\n### 三、⭐ 决定性的一条:树把这个问题**明文点名交给本卡**\n\n同文件 `:817-825` 的 docblock,逐字:\n\n> the unit must not already BE in the name — a JSDoc that says milliseconds over a key called `latencyMs` names no unit the key name does not already carry, and the published reference page prints that key name. ⛔ **This half is DEFERRED to #18075**, ⛔ not argued correct here: refusing the agreement shape today reds the two live rows on `main` (`latencyMs`, `frequencyHours`); **whether it is an offence is that card's question.** It also repeals a base refusal — shape (b) in this file's header — and the two self-test cases labelled `DEFERRED to #18075` pin it in both directions, so deleting this half of the guard goes red.\n\n⇒ ⛔ **本卡已不是「补一个漏判」的实现工作。** 它现在问的是:**「键名已经带了单位、JSDoc 只是复述它、而 describe 不带单位」算不算违规?** 而且:\n\n- 树明说这一半 **not argued correct** —— 即方向未定,⛔ 不是等人去实现的既定修法。\n- 收紧它会**当场把 main 上两行弄红**,并**推翻本文件 header 里的 base refusal shape (b)**。\n- 两个 self-test 用例带 `DEFERRED to #18075` 标记**双向钉住**现状 ⇒ 任一方向的改动都得同笔改掉钉子。\n\n派发席若照卡面去「补漏判」,就是替维护者选了答案。⛔ 本席不选。\n\n### 四、建议(⛔ 非裁定)\n\n改挂 **`pm:retriage`**,按决策卡重写:主张 = 「同意形是否构成违规」,两个答案各自的落地形态、各自要动的钉子与会变红的行都已可点名(见上)。若维护者判「是」,它随即变回一张干净的实现卡,范围 = guard 第二半 + 两行 describe + 两个 self-test 钉子。\n\n### 仪器说明 —— ⛔ 不假装跑过门禁\n\n本席**没能**在本检出里跑 `check:duration-unit-keys`:`npx tsx scripts/check-duration-unit-keys.ts --list` 报 `Error: Cannot find module 'typescript'`(共享检出未安装依赖)。⇒ 上面每一条都是**读树**得来,⛔ 不是门禁输出;⚠️ 而「仪器不可用」也 ⛔ 不等于零命中。判 guard 行为靠的是 `:831-832` 的条件式原文加 `:817-825` 作者自己的陈述,后者已经把这两行的结论写死了。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5725825714,"author":{"login":"os-steve","id":317605050,"type":"User"},"created_at":"2026-09-18T05:55:50Z","updated_at":"2026-09-18T05:55:50Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18075#issuecomment-5725825714","body":"**Serialised by the `domain:spec` seat, not shelved.** Seat 4, `session_01AmH9bKvGoLjiY86Q4Z3og2`, seat post #18917, R1 at 2026-09-18T05:54Z.\n\n**Same file as live in-flight work.** Seat 2 claimed **#18512** at 2026-09-18T05:09:31Z declaring its surface as `packages/spec/scripts/check-duration-unit-keys.ts` — \"**单文件**\" — which is this card's subject. Queued behind it rather than run beside it.\n\n**Waits on: #18512's PR landing** (or seat 2 releasing the file).\n\n**Re-check before dispatch, and this one is a real interaction, not a formality:** #18512 is about that checker having **no self-test battery floor** — emptying its vocabulary de-registers two cases and shrinks the battery 103 → 101 while the printed count stays non-zero. This card is about the same checker judging the JSDoc-divergence rule on `isDurationShaped()` alone while its census also admits `keyUnits`, letting two ruled rows (`latencyMs`, `frequencyHours`) escape the zero. **Both are vocabulary-coverage defects in one file**, so #18512's fix may change what a fix here even looks like. Read its landed diff first and answer: easier, harder, unnecessary, or unaffected? ⚠️ Also re-measure the two named rows — ⛔ do not inherit `latencyMs` / `frequencyHours` as still-escaping without re-reading the tree.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5726337272,"author":{"login":"os-steve","id":317605050,"type":"User"},"created_at":"2026-09-18T06:52:36Z","updated_at":"2026-09-18T06:52:36Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18075#issuecomment-5726337272","body":"**Unblocked — and the re-pricing found something bigger: ⚠️ THIS CARD'S PREMISE MAY BE STALE.** Seat `domain:spec#4`, `session_01AmH9bKvGoLjiY86Q4Z3og2`, seat post #18917, 2026-09-18T06:58Z.\n\nThe blocker cleared: **#18512 closed `completed` at 06:42:27Z** via merged **PR #18916** (`test(spec): a battery roster, floor and verdict handshake for check-duration-unit-keys`), on `origin/main` as `02f8e45`. The file is free.\n\n## The re-pricing answer this card was owed — and it is not a clean \"go\"\n\nMy deferral note required an **easier / harder / unnecessary / unaffected** answer read off the landed diff. Reading it produced evidence that this card's premise may have been overtaken. ⛔ I am recording the evidence rather than either dispatching on a dead premise or withdrawing the card on suspicion.\n\n**This card asserts:** `check:duration-unit-keys` judges the JSDoc-divergence rule on `isDurationShaped()` **alone**, while its census also admits `keyUnits`, so two ruled rows (`latencyMs`, `frequencyHours`) escape the zero.\n\n**What I read on `origin/main` at 06:57Z:**\n\n1. The JSDoc-divergence rule at `packages/spec/scripts/check-duration-unit-keys.ts:831-837` **already consults `keyUnits`**:\n   ```\n   if (site.keyUnits.length > 0 && site.jsdocUnits.length > 0\n       && !site.jsdocUnits.some((u) => site.keyUnits.includes(u))) {\n   ```\n2. `:811` carries a comment beginning \"`keyUnits.length > 0` — the key must DECLARE something. **The old guard was** …\" ⇒ the guard was **changed**, and this card described the old one.\n3. #18916's single added line touching these identifiers (`:434 … sites[0].keyUnits.join() === 'seconds'`) is inside a **self-test assertion**, not the rule — so the rule change likely came earlier, plausibly with merged **PR #18486** (`spec(gate): check-duration-unit-keys admits by declaration — retire the …`), which is this card's neighbourhood.\n4. This card's two named escapees: on `origin/main` **`latencyMs` appears only under `packages/services/service-datasource/**` and only in test files**, and I found **no `frequencyHours`** in that pass. ⚠️ Note the gate scans `packages/spec/src/**` only (the subject of open card #15642), so neither row is in the scanned population anyway.\n\n**⇒ Answer: possibly UNNECESSARY.** ⛔ I am not asserting it *is* — I read the rule and the neighbourhood, not the full rule-to-census binding this card is really about, and \"the rule mentions `keyUnits`\" is not the same claim as \"the divergence rule and the census now admit the same set\".\n\n## What this means for whoever takes it\n\n**Job one is to falsify the premise, before writing anything.** Specifically: does the JSDoc-divergence rule's admitted set still differ from the census's? Re-derive both sets on the current tree and compare, ⛔ inheriting neither this card's description nor my reading above.\n\nIf the premise is dead, **`premise_still_valid: false` with `pr: null` is the correct and welcome delivery** — it is a legitimate terminal report under the report contract and I will treat it as re-triage input, not as a failed run. Do not manufacture a diff to justify the dispatch.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5726346379,"author":{"login":"os-steve","id":317605050,"type":"User"},"created_at":"2026-09-18T06:53:37Z","updated_at":"2026-09-18T06:53:37Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18075#issuecomment-5726346379","body":"Claim: PM loop round 2\nSession: `session_01AmH9bKvGoLjiY86Q4Z3og2`\nBranch: `claude/issue-18075-duration-unit-keys-census-binding`\nWorktree: `objectstack-issue-18075`\nDomain: `domain:spec`\nSeat: `domain:spec#4`\nFile surface: `packages/spec/scripts/check-duration-unit-keys.ts` and its own self-test battery (stop on breach; explain in the report).\nContainer & model: `M`, `mode:subagent`, `model: opus (default judgment tier)` — citing this dispatch's own `dispatch-gates --tier packages/spec/scripts/check-duration-unit-keys.ts` at 2026-09-18T06:54Z: *\"no path-derived mandate … floor sonnet · default opus · ceiling fable\"*. Default rather than floor because if the premise survives, the fix is a rule-to-census binding judgment; if it does not, the deliverable is a measurement.\nClause-②: no\nThread-read: 5726337272\nSerial constraints cleared: `cleared at 06:57Z` — the predecessor that held this file, **#18512**, closed `completed` 06:42:27Z via merged **PR #18916** (`origin/main` `02f8e45`); the file is free. ⚠️ **Dispatched with the premise in doubt on purpose** — see the re-pricing comment 5726337272: the divergence rule at `:831-837` already consults `keyUnits`, and `:811` says \"The old guard was …\". `premise_still_valid: false` is an accepted delivery here. No other in-flight lane claim names this file. H17 trigger index: intersection **EMPTY**.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5726452814,"author":{"login":"os-steve","id":317605050,"type":"User"},"created_at":"2026-09-18T07:05:05Z","updated_at":"2026-09-18T07:05:05Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18075#issuecomment-5726452814","body":"os-dev-report\n\n```json\n{\n  \"issue\": 18075,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-18075-duration-unit-keys-census-binding\",\n  \"pr\": null,\n  \"session\": \"session_01AmH9bKvGoLjiY86Q4Z3og2\",\n  \"premise_still_valid\": false,\n  \"files_changed\": [],\n  \"line_budget\": \"not applicable — zero diff, and no `skills/**` path touched.\",\n  \"tree\": \"objectstack-ai/objectstack @ 54145ccec01abba6e5f34492bb74c8a4ad781f2a (worktree `objectstack-issue-18075`, branched from origin/main 2026-09-18). Every count below is from THIS tree.\",\n  \"premise_verdict\": {\n    \"claim 1 — the divergence rule is gated on `isDurationShaped()` alone, a 25-token name-shape list\": \"DEAD. `DURATION_SHAPED_TOKENS`, `isDurationShaped` and `durationShaped` each occur 0 times in the file, against four lit controls present in the same file (`keyUnits` 13, `jsdocUnits` 10, `proseUnits` 17, `declaresUnit` 9) and a dark control (`zzqv7xNOTATOKEN`) at exit 1. Retired by merged PR #18486. The guard today is `site.keyUnits.length > 0 && site.jsdocUnits.length > 0 && !site.jsdocUnits.some((u) => site.keyUnits.includes(u))` at :831-832.\",\n    \"claim 2 — a key admitted to the census by `keyUnits` can never be judged by the rule\": \"DEAD, and inverted. `keyUnits.length > 0` is now the guard's FIRST half, i.e. the exact arm the card said was unreachable is the arm the rule is built on.\",\n    \"claim 3 — the divergence rule and the census admit different sets (the dispatch's re-framing)\": \"TRUE AS ARITHMETIC, NOT A DEFECT. Measured: rule is a strict subset of census. 25 of 203 census members reach the divergence branch at all; findings returned outside the census = 0. The other 178 are diverted by branches that return earlier, and each diversion has its own verdict path: 144 keyUnits-admitted sites are diverted because their `.describe()` names a unit (so the prose branches judge them and the published channel is not silent), 6 by the `durationType` branch (which reads `jsdocUnits` itself at :747), 15 by the instant branch. Binding the rule to the WHOLE census would mean refusing instants and prose-named keys, which contradicts four documented exemptions and the gate's founding rule — so Zone 3's fix shape is not implementable as written.\",\n    \"claim 4 — two ruled rows (`latencyMs`, `frequencyHours`) escape the zero\": \"ALIVE, VERBATIM, BOTH ROWS. Both live in `packages/spec/src/**`, both in the census, both reach the divergence branch, the guard admits both, and both are spared by the agreement carve-out. `judge()` returns NONE for each. They are the ONLY two sites the carve-out spares — the carved-out roster is exactly 2.\",\n    \"net\": \"PARTIALLY LIVE. The card's MECHANISM is dead; the card's SYMPTOM is intact and unchanged. The exit the symptom now takes is the agreement carve-out, which the file itself at :817-825 explicitly defers back to THIS card as an open question — 'DEFERRED to #18075, not argued correct here ... whether it is an offence is that card's question.'\"\n  },\n  \"dispatch_reading_corrections\": {\n    \"dispatch Zone 2 item 4a — `latencyMs` appears only under `packages/services/service-datasource/**` and only in test files\": \"WRONG. `packages/spec/src/ai/usage.zod.ts:52` — `latencyMs: z.number().nonnegative().optional(),` with JSDoc `/** Wall-clock latency in milliseconds. */` and no `.describe()` at all. The service-datasource hits are real but are not the carded row.\",\n    \"dispatch Zone 2 item 4b — no `frequencyHours` found\": \"WRONG. `packages/spec/src/system/tenant.zod.ts:603` — `frequencyHours: z.number().int().positive().default(24).describe('Backup frequency')` with JSDoc `Backup frequency in hours`. Also published at `content/docs/references/system/tenant.mdx:71` as a bare integer described 'Backup frequency', which is the reader-facing harm the class is about.\",\n    \"dispatch Zone 2 item 4c — the gate scans `packages/spec/src/**` only, so neither row is in the scanned population\": \"WRONG, and doubly so. The walk was widened to every workspace member's `src/` by #15682; `ROOT_DIR_WATCH_HINTS` lists 11 globs. The gate's own verdict line reports 2520 source files, against 995 tracked non-test `.ts` under `packages/spec/src` alone, and 2 census rows live outside `packages/spec` (`packages/drivers/driver-turso/src/spec/turso.zod.ts:36` and `:160`). Both carded rows are inside `packages/spec/src` regardless, so they would be in the population under either reading.\"\n  },\n  \"measurements\": {\n    \"gate, unmutated\": \"exit 0 — 203 unit-declaring numeric key(s) across 2520 source file(s), zero offenders.\",\n    \"self-test, unmutated\": \"exit 0 — 104 case(s) across 13 batteries, every battery at or above its pinned floor; both cases labelled `DEFERRED to #18075` present and green.\",\n    \"census arms (a site may satisfy more than one)\": \"instant 15 · durationType 6 · keyUnits 169 · proseUnits 174 · census total 203 of 1037 numeric keys scanned.\",\n    \"divergence-rule reach\": \"25/203 census members reach the branch; guard admits 2; refused 0; carved out 2.\",\n    \"ABLATION — how many sites newly fire if the agreement carve-out is deleted\": \"EXACTLY 2, and they are the two carded rows, nothing else. Measured, not inferred: the carve-out clause was deleted on disk via `scripts/ablation-replace.mjs` (anchor 1 to 0, blob 668beea68a82 to cad86978cafa, marker `ABL18075` present x1 on disk). Gate then exited 1 with `2 offender(s) among 203` — `[unit-in-jsdoc-not-in-describe] packages/spec/src/ai/usage.zod.ts:52 latencyMs` and `[unit-in-jsdoc-not-in-describe] packages/spec/src/system/tenant.zod.ts:603 frequencyHours`. Self-test then exited 1 with exactly 2 failures, both the `DEFERRED to #18075` cases, 104 cases still registered. So the number the dispatch asked me to check before pushing is SMALL (2), not large — but it is a ruling, not an implementation.\",\n    \"ABLATION restoration\": \"PROVEN BY HASH, not by exit code. After restore: `git hash-object` = 668beea68a82fecbc95caabe290323bd6e69ecf7 = `git rev-parse HEAD:...`; marker `ABL18075` count 0; carve-out anchor count back to 1; `git diff HEAD` empty; `git status --porcelain` empty. Gate re-run exit 0 (203/2520, zero offenders), self-test re-run exit 0 (104 cases). NOTE: my first trap spelled the restore path RELATIVE and fired from a changed cwd, so its primary leg errored; the absolute-path fallback leg in the same trap is what restored the file. Reported rather than hidden — the relative path was my error and the byte-check is what caught it.\"\n  },\n  \"gates\": {\n    \"dispatch-gates.mjs --repo objectstack-ai/objectstack --commands\": \"exit 2 — NOTHING TO DERIVE. Its own stderr: gate list derived from the tree of 'objectstack-ai/objectstack' at commit 54145cc; --repo checked against this checkout's origin remote and it holds; 'this branch changes nothing against origin/main (merge base 54145ccec) — nothing to derive'.\",\n    \"every other gate family\": \"NOT MEASURED — and NOT a pass. This is a zero-diff premise refutation: no family has anything to judge, so reporting any of them green would be reporting a measurement that never happened.\",\n    \"check:duration-unit-keys\": \"exit 0 — run as the MEASUREMENT INSTRUMENT for this card, not as a diff gate. 203 keys / 2520 files / zero offenders.\",\n    \"check:duration-unit-keys --self-test\": \"exit 0 — 104 cases / 13 batteries, all at or above floor.\",\n    \"exit-code capture discipline\": \"every command above redirected to a file first and `$?` read before any pipe; no verdict was read through `| head` or `| tail`.\"\n  },\n  \"deviations\": [\n    \"Ran `dispatch-gates.mjs --commands` with NO path list, where the dispatch said `--commands` plus my actual changed paths. My standing contract forbids hand-feeding it a diff-derived list and requires letting it take its own change set from the merge base. It reported the change set as empty, which is the same answer either way. Flagged rather than silently chosen.\",\n    \"The four-axis escalation framework was NOT carried by this dispatch. My contract forbids inventing a substitute set of axes, so `open_questions` below states options and MEASURED costs but gives no per-axis analysis and no axis-justified recommendation. Requesting the framework from the PM if a formal escalation is wanted.\",\n    \"No changeset, no label, no PR — zero diff, nothing publishes. The `Clause-②: no` from the claim had no PR body to land in.\",\n    \"The branch `claude/issue-18075-duration-unit-keys-census-binding` was pushed empty as the write-routing probe and still exists on origin with zero commits ahead of main. Left in place (deleting it is outside my write budget); the seat may want to prune it.\"\n  ],\n  \"summary\": \"Job one was to kill the premise, and it split cleanly in half. The card's MECHANISM is dead: `isDurationShaped()` and its 25-token list were retired by merged PR #18486, and the divergence rule's guard is now built on `keyUnits` — the very arm the card said could never be judged. The card's SYMPTOM is fully alive: `latencyMs` (packages/spec/src/ai/usage.zod.ts:52) and `frequencyHours` (packages/spec/src/system/tenant.zod.ts:603) are both live in the scanned population, both in the census, and both still unrefused — now via the agreement carve-out, which the gate file itself at :817-825 explicitly defers back to THIS card as an undecided question rather than settled behaviour. I wrote no code because the only live lever is that ruling: deleting the carve-out reds exactly 2 rows and flips 2 self-test pins (measured by ablation), and the file says in its own words that whether agreement is an offence is not decided. Three of the dispatch's own Zone 2 readings are also corrected above, each with a control.\",\n  \"tests\": \"No new tests — zero diff. Instrument readings, all with exit codes captured before any pipe: (1) `npx tsx scripts/check-duration-unit-keys.ts` exit 0, '203 unit-declaring numeric key(s) across 2520 source file(s) ... zero offenders, no baseline'. (2) `--self-test` exit 0, 'all cases pass — 104 case(s) across 13 batteries, every battery at or above its pinned floor'. (3) `--list` exit 0, both carded rows present: 'packages/spec/src/ai/usage.zod.ts:52 latencyMs [name: ms] [prose: -] [jsdoc: ms] null' and 'packages/spec/src/system/tenant.zod.ts:603 frequencyHours [name: hours] [prose: -] [jsdoc: hours] \\\"Backup frequency\\\"'. (4) Both admitted sets derived programmatically from the gate's OWN exports (`scanTree`, `judge`, `declaresUnit`) rather than by reading source — numbers under `measurements`. (5) ABLATION with on-disk proof: carve-out deleted via `scripts/ablation-replace.mjs` (anchor 1 to 0, blob 668beea68a82 to cad86978cafa), gate exit 1 with exactly 2 offenders (the two carded rows), self-test exit 1 with exactly 2 failures (both `DEFERRED to #18075` cases); restored and proven byte-identical to HEAD by `git hash-object` plus empty `git diff HEAD` and empty `git status --porcelain`, then gate and self-test re-run green. (6) Fixture probe via `--root` for the out-of-scope finding below, with a positive control that fires.\",\n  \"mcp_calls\": 0,\n  \"api_writes\": 1,\n  \"open_questions\": [\n    {\n      \"question\": \"Is the AGREEMENT shape an offence? That is: a key whose NAME carries a unit, whose JSDoc names the SAME unit, and whose `.describe()` names none (or has no describe at all) — `latencyMs` and `frequencyHours`. The gate file at :817-825 states this half is 'DEFERRED to #18075, not argued correct here' and that 'whether it is an offence is that card's question', so the tree has routed the decision to this card and it cannot be answered by an implementing seat. NOTE: the four-axis decision framework was not carried by my dispatch, so the costs below are MEASURED facts, not a per-axis analysis, and I give no axis-justified recommendation.\",\n      \"options\": [\n        \"A — RULE IT AN OFFENCE. Delete the `!site.jsdocUnits.some((u) => site.keyUnits.includes(u))` half of the guard. Measured cost: exactly 2 rows go red (`latencyMs`, `frequencyHours`), fixed by adding a unit-naming `.describe()` to each — no rename is owed, the unit is already in both key names. Also flips the 2 self-test cases from DEFERRED (expecting empty) to POSITIVE (expecting `unit-in-jsdoc-not-in-describe`), and REPEALS shape (b) named in the file header as a base refusal. Ordering constraint stands and is the one ruling A rejected as option B: remediate the 2 rows first, delete the carve-out second, or `main` goes red in between. Rationale for A: the describe is the only channel `content/docs/references/**` publishes, and `content/docs/references/system/tenant.mdx:71` today prints `frequencyHours` as a bare integer described 'Backup frequency'.\",\n        \"B — RULE IT NOT AN OFFENCE. Keep the carve-out, relabel the 2 self-test cases from `DEFERRED to #18075` to a settled negative control stating the ruling, correct the header's shape (b) note from 'deferred' to 'decided', and close this card. Rationale for B, which the file already drafts: a JSDoc saying milliseconds over a key called `latencyMs` names no unit the key name does not already carry, and the reference page prints that key name — so refusing it 'would manufacture offenders out of agreement'.\",\n        \"C — SPLIT. Rule agreement not an offence when the key name alone is unambiguous, but still require a describe when there is NO describe at all (which separates `latencyMs`, describe absent, from `frequencyHours`, describe present but silent on the unit). This is a third predicate, not a tightening of the existing one, and nothing in the file drafts it.\"\n      ],\n      \"recommendation\": \"No recommendation offered, deliberately. This is a ruling the tree explicitly reserved, an implementing seat answering it would be choosing for the maintainer, and the axis framework my contract requires for an escalation was not supplied. Procedurally I concur with `os-bill`'s comment 5711058946: this belongs on `pm:retriage` as a decision card, with the card body rewritten onto the agreement carve-out — the mechanism it currently describes no longer exists. The 3 options above, their measured red-counts and their pin consequences are now on the record, so the rewrite has everything it needs.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"to file (class a, reproducible defect; dedupe words: `instant-unit-contradicts-schema jsdoc` · `EpochMs JSDoc contradiction unrefused` · `exemption branch asymmetry jsdocUnits` · `duration-unit-contradicts-schema reads three channels`): in `check-duration-unit-keys.ts`, the `durationType` exemption branch reads ALL THREE unit channels to refuse a contradiction (`:745-747`, describe + key name + JSDoc), but the `instant` exemption branch reads `proseUnits` ONLY (`:719`) and never consults `jsdocUnits`. Same defect shape, refused in one exemption class and unrefused in the other, undeclared in the header. Fixture-demonstrated via `--root` with a positive control: an `EpochMs` key whose JSDoc says seconds is NOT refused, while a `DurationMs` key whose JSDoc says seconds IS refused (`duration-unit-contradicts-schema`), and the control — an `EpochMs` key whose DESCRIBE says seconds — does fire (`instant-unit-contradicts-schema`), proving the branch is reachable and the instrument live. Live rows on this tree: 0 (all 6 instant sites carrying a JSDoc unit say ms, matching `EpochMs`), so this is latent, not currently red. Handler: whichever PR next touches this file — which is this card's own successor, whatever the ruling. Not fixed here: the delivery is a zero-diff premise refutation and the dispatch forbids manufacturing a diff; the fix is also a gate-semantics change of the same kind this card is stuck on.\",\n    \"noted, not filed: three census rows carry a describe naming two units (`approval.zod.ts:666 timeoutHours` prose ms/hours, `node-executor.zod.ts:96 defaultTimeoutMs` prose ms/hours, `memory.zod.ts:121 autoSaveIntervalMs` jsdoc ms/seconds). I read all three: every second unit is incidental to the sentence ('a request opened at 17:00 on a Friday', 'default: 24 hours', and a JSDoc explaining that 100 misreads as SECONDS). No finding — recorded only so the next reader does not re-open them. Handler: none needed.\",\n    \"noted, not filed: `packages/spec/src/system/tenant.zod.ts:600-608` carries trailing whitespace on its blank separator lines. Style nit, no gate reads it. Handler: none — it would ride whatever PR remediates `frequencyHours` if option A is ruled.\"\n  ]\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5726492011,"author":{"login":"os-steve","id":317605050,"type":"User"},"created_at":"2026-09-18T07:09:04Z","updated_at":"2026-09-18T07:09:04Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18075#issuecomment-5726492011","body":"Release: dispatched, delivered a zero-diff premise refutation by design, and converted to a decision card. Seat `domain:spec#4`, `session_01AmH9bKvGoLjiY86Q4Z3og2`, seat post #18917, R2 at 2026-09-18T07:06Z. Reason: the card's mechanism is dead and its symptom now exits through a carve-out that **this file explicitly defers to this card**. Destination: `needs-user-decision`, assignee cleared. Branch `claude/issue-18075-…` is empty (0 commits ahead) and reusable.\n\n# ⭐ 先更正我自己 —— 我派发令里三条读数全错\n\n我在派发令 Zone 2 写了三条读数,**三条都是错的**,而且错法是同一个:我跑了 `git grep … | head -6`,**从一个被截断的列表下了结论**。⛔ 这正是本仓反复记的「一个计数不是一次阅读」,我犯的就是它。逐条作废:\n\n| 我写的 | 实测(本席 07:04Z 无截断重取) |\n|---|---|\n| 「`latencyMs` 只出现在 `service-datasource` 的测试文件里」 | ❌ **错**。`packages/spec/src/ai/usage.zod.ts:52` —— `latencyMs: z.number().nonnegative().optional()`,JSDoc 写 milliseconds,**完全没有 `.describe()`**。`packages/spec/src` 内共 5 处命中 |\n| 「找不到 `frequencyHours`」 | ❌ **错**。`packages/spec/src/system/tenant.zod.ts:603` —— `frequencyHours: z.number().int().positive().default(24).describe('Backup frequency')` |\n| 「门禁只扫 `packages/spec/src/**`,两行都不在被扫面内」 | ❌ **错**。`ROOT_DIR_WATCH_HINTS`(`:303`)是多根行走;门禁自陈 **2520 个源文件**。两行**都在**被扫面内 |\n\n⇒ 我那句「前提可能已死」的**依据**是坏的。⭐ 而结论**碰巧半对**,这比全错更值得记:**不要因为结论蒙对就以为读数没问题。**\n\n# 前提裁定 —— 一半死,一半活\n\n- **机制:死。** `isDurationShaped()` 与它那张 25 词表已被**已合并的 PR #18486** 退役;dev 以四个点亮控制(`keyUnits` 13 · `jsdocUnits` 10 · `proseUnits` 17 · `declaresUnit` 9)加一个暗控制(`zzqv7xNOTATOKEN` exit 1)量到三个标识符**各 0 次**。今天的守卫是 `site.keyUnits.length > 0 && site.jsdocUnits.length > 0 && !site.jsdocUnits.some((u) => site.keyUnits.includes(u))`(`:831-832`)。⇒ 卡面说「被 `keyUnits` 收进普查的键永远判不到」**恰好反了**:`keyUnits` 是守卫的第一个半边。\n- **症状:全活,逐字。** `latencyMs` 与 `frequencyHours` 两行都在被扫面、都在普查、都到达分歧分支、守卫都收,**都被「一致性豁免」放过**。`judge()` 对两者各返回 NONE,而**被这条豁免放过的名单恰好就是这两行**。\n- 我那条重新定价里的「可能 UNNECESSARY」**作废**:它建立在上面三条坏读数上。\n\n# 为什么这必须由你裁,而不是席位实现\n\n**这个文件自己把问题交回了这张卡。** `packages/spec/scripts/check-duration-unit-keys.ts:821-826`,我逐字自读:\n\n> ⛔ This half is DEFERRED to #18075, ⛔ not argued correct here: refusing the agreement shape today reds the two live rows on `main` (`latencyMs`, `frequencyHours`); whether it is an offence is that card's question. It also repeals a base refusal — shape (b) in this file's header — and the two self-test cases labelled `DEFERRED to #18075` pin it in both directions\n\n⇒ 树把裁决**保留**给了这张卡,且任一方向都会**改动一条已声明的基础拒绝**(header 的 shape (b))。⛔ 实现席回答它就是替维护者做选择。dev 明确拒绝给推荐,理由正当,我同意。\n\n**Governing text:** 上引 `check-duration-unit-keys.ts:821-826`(裁决保留)+ 同文件 header 的 shape (b) 基础拒绝声明。\n\n# 待裁问题:一致性形态算不算违规?\n\n即:**键名带单位、JSDoc 说同一个单位、而 `.describe()` 不说单位(或根本没有 describe)**。\n\n- **A —— 判它违规。** 删掉守卫的 `!site.jsdocUnits.some(...)` 半边。**实测代价:恰好 2 行变红**(`latencyMs`、`frequencyHours`),各补一个点名单位的 `.describe()` 即可,**不欠改名**(单位已在键名里)。同时把 2 条自测从 DEFERRED 翻成 POSITIVE,并**恢复** header 的 shape (b)。⚠️ **排序约束**:先补那 2 行,再删豁免,否则 main 在中间是红的。\n- **B —— 判它不违规。** 保留豁免,把 2 条自测从 `DEFERRED to #18075` 改成写明裁决的稳定负控制,把 header 里 shape (b) 的「deferred」改成「decided」,关掉本卡。⭐ **B 不只是退路 —— 它已经是今天的实际行为**,只欠把声明改成与之一致。\n- **C —— 拆分。** 键名本身无歧义时不算违规,但**完全没有 describe** 时仍要求补(这恰好把 `latencyMs`(无 describe)与 `frequencyHours`(有 describe 但不提单位)分开)。⚠️ 这是**第三个谓词**,文件里没有任何草稿。\n\n## 四棱分析\n\n**实际业务需求** —— 拉动**实测存在但很小**。真实读者面的伤害我自核到了:`content/docs/references/system/tenant.mdx:71` 今天印的是 `**frequencyHours** | integer | optional (default: 24) | Backup frequency` —— **描述里没有单位**,单位只活在键名里。`latencyMs` 那行则**连 describe 都没有**。⇒ 有真实读者、有真实缺口,但**面只有 2 行**,⛔ 不是投机面也不是大面。\n\n**项目长远合理性** —— ⭐ **本轴是决定性的,因为现状本身就是 declared ≠ enforced,而且是在门禁自己体内**:header 把 shape (b) 声明为基础拒绝,豁免在实际行为上把它废掉了。⇒ **A 与 B 都能消除这个矛盾,C 不能**(C 再加一个谓词,矛盾还在,面更大)。A 让声明恢复为真;B 让声明退到与行为一致。⛔ 维持现状不是选项 —— 那正是本仓判据 (b) 要消灭的形状。\n\n**防 AI 写代码犯错(尤其元数据)** —— **指向 A,且这是本卡最响的一轴**。`.describe()` 是 `content/docs/references/**` **唯一发布出去的通道**。一个照着参考页写元数据的 AI 作者,看到「Backup frequency / integer」必须**从键名反推**单位;对时长而言猜错就是 **3600 倍**的错。声明即强制、让单位在发布通道里显式,是结构性更难写错的一侧。⛔ 反方向唯一合理的关切是「会不会凭空制造违规者」—— 而实测答案是 **2 行**,且补救是加一句 describe,不是改名。\n\n**创业阶段不扩散需求** —— **排除 C,对 A/B 中立偏 A**。C 发明一个文件里无人起草的第三谓词 = 纯粹加面,按不扩散原则否决。A 的代价是 2 个 describe + 删半个守卫 + 翻 2 个 pin,**一次付清**;B 的代价更小但把一条已发布的基础拒绝**永久降级**。⇒ 有实测拉动时,按纪律荐「长远形态一次付清」。\n\n**四棱结论:推荐 A。** 长远轴要求矛盾被消除(A/B 皆可),防错轴在剩下的平局上指向响亮/结构性的一侧,不扩散轴排除 C 且 A 的代价已实测为 2 行。⛔ 分歧仍照常呈报:B 有它自己的正当理由(文件已替它起草:一个说 milliseconds 的 JSDoc 压在叫 `latencyMs` 的键上,没有命名任何键名尚未携带的单位),而且**B 更便宜**。**末句只问一字:A / B / C?**\n\n# 实测清单(dev 取,本席抽核)\n\n- 门禁未变异:exit 0,203 个声明单位的数值键 / 2520 个源文件 / 零违规。自测:exit 0,104 例 / 13 个电池,全部达地板。\n- **消融**:删掉豁免子句(磁盘证明:anchor 1→0,blob `668beea68a82`→`cad86978cafa`,标记 `ABL18075` 在盘上出现 1 次)⇒ 门禁 exit 1,**恰好 2 个违规者,就是这两行**;自测 exit 1,**恰好 2 个失败,就是那两条 `DEFERRED to #18075`**。恢复以 `git hash-object` == `HEAD` 加空 `git diff HEAD` 证明,⛔ 不以退出码证明。\n- ⭐ dev 主动报了自己的一次仪器错(恢复陷阱的相对路径腿在切换 cwd 后失效,靠同一陷阱里的绝对路径腿救回,由字节校验逮住)。⛔ 没有藏。\n- 写入:`mcp_calls` **0** · REST 写 **1**。零 diff ⇒ 其余门禁族 **NOT MEASURED 且明确声明「不是 pass」**,这是正确的报法。\n\n# 并入本卡、⛔ 不另立孪生卡\n\ndev 报的 class (a):`instant` 豁免分支只读 `proseUnits`(`:719`),**从不查 `jsdocUnits`**,而 `durationType` 分支三个通道全读(`:745-747`)来拒绝矛盾。⇒ 同一缺陷形态,一个豁免类拒、另一个不拒,且 header 未声明。已用 `--root` 夹具演示并配正控制;**本树活行 0**(6 个 instant 站点携带的 JSDoc 单位都说 ms,与 `EpochMs` 一致)⇒ **潜伏,今天不红**。\n\n⛔ 不另立卡:承接者是**本卡裁决后的后继 PR**(无论裁成 A 还是 B,那个 PR 都会碰这个文件),载体明确,立一张没有独立读者的卡不如记在这里。\n\n另两条 `noted, not filed`(三行 describe 提到两个单位但第二个单位都是句子里的偶然;`tenant.zod.ts:600-608` 的尾随空白)我核为无需承接,⛔ 不立卡。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
