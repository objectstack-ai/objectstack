{"id":5367274804,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-21T08:17:38Z","updated_at":"2026-08-21T08:17:38Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/10653#issuecomment-5367274804","body":"Claim: `/pm-dispatch` **domain:devx** execution seat.\nSession: `session_01DdCnBGcHeufjrq7drTD3wt`\nBranch: `claude/issue-10653-lint-parse-diagnostics`\nWorktree: `objectstack-issue-10653`\nDomain: `domain:devx`\nFile surface: `packages/lint/src/validate-react-page-props.ts`, `lint-startup-registry-verdict.ts`, `validate-hook-body-writes.ts` (the three validators) — ⛔ **not** `packages/cli/src/utils/detect-free-identifiers.ts`, which is CLI runtime and answers separately\n\n⭐ **The ruling is already in the card body: option A**, and ⛔ **not B** (a shared published package for four call sites is the shape `invoked-as.mjs` already argued down). C — leave them — is what PR #10651 left standing and is a legitimate cheaper answer, so nothing here is urgent or red.\n\n⭐ **The correction that decides what a fix must address**, measured in PR #10651: the defect is the **unread `parseDiagnostics`**, not the `try/catch`. `createSourceFile` cannot throw, so a recovered partial tree is walked and scored clean **with no `catch` involved**. ⛔ A fix that only deletes the catch blocks closes nothing.\n\n⚠️ This touches **published package source**, unlike most of this round — the changeset judgement is genuinely open and you are asked to make it explicitly rather than default to `skip-changeset`.\n\n⛔ Governed surface out of scope (`docs/adr/**`, `.claude/**`, `skills/**`, `AGENTS.md`, `CLAUDE.md`, `content/docs/releases/**`) and the #9465 epic fence.\n⚠️ **No formatter of record** — ⛔ do not run `prettier --write` (#10622).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5367857608,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-21T09:06:11Z","updated_at":"2026-08-21T09:06:11Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/10653#issuecomment-5367857608","body":"<!-- os-dev-report -->\n\n```json\n{\n  \"issue\": 10653,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-10653-lint-parse-diagnostics\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/10697\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Option A, implemented: a packages/lint-local checked parse (checked-parse.ts) that READS parseDiagnostics and returns the verdict, with the two dead catch blocks deleted. Wired into the three carded validators plus validate-action-body-writes.ts (declared scope extension below), each reporting an advisory 'warning' finding: react-page-source-unparseable, startup-source-unparseable, hook-body-source-unparseable, action-body-source-unparseable. The change is ADDITIVE by design and by test: the recovered tree is still walked, so every finding these rules produce today they still produce (measured: a source containing 0755 keeps its react-prop-missing-required and gains the parse finding beside it); ablation A4 is the guard on that property. Premise re-verified before building and CONFIRMED (see tests). Population re-derived and unchanged at 4 shipped-source rows; after the change that census tier reads 2 in 2 files, because the three validators no longer call ts.createSourceFile directly and one checked-parse.ts call site replaces them. SCOPE EXTENSION, declared: packages/lint/src/validate-action-body-writes.ts is one file beyond the card's three. It consumes the SAME extractHookBodyWriteSet parse, so wiring only the hook rule would have left the identical blind spot at the call site next door, which that rule's own header forbids ('an action body runs through the same HookBodySchema and the same sandbox, so it gets the same treatment'). Same defect class, mechanical shape fixed by the sibling, same gate family, no other claim on the file. PROCESS INCIDENT worth the PM's attention: my first ablation pass restored with 'git checkout --' against files that were still UNTRACKED; it silently reverted two source files to HEAD and left A1's mutation live under A2-A4. The tell was four runs reporting an identical 21 failures against very different predictions. Work reconstructed, committed, all four ablations re-run from a committed tree; the reported numbers are from that second pass.\",\n  \"tests\": \"All at final commit 2dec2dfaf5. PREMISE (verified BEFORE building, TypeScript 6.0.3): createSourceFile threw on 0 of 10 wrecks (merge markers, truncated body, unterminated string/comment/template, JSX-under-TS; both ScriptKinds) and parked 1-3 diagnostics on parseDiagnostics each -- so the catch blocks were dead and the live path was the unread diagnostics, exactly as the card states. Pinned in checked-parse.test.ts so it cannot rot. REPRODUCTION (before the fix, each with a repaired positive control): unterminated comment/template gave parseDiagnostics>=1 and findings=0 where the repaired twin gave 1-2 findings, at all three validators. FALSE-POSITIVE CONTROL: per-rule parseable corpora (incl. TSX generics, type annotations, fragments, spreads, enums, non-null assertions) plus every HOOK_BODY_WRITE_PATTERNS / ACTION_*_PATTERNS ledger example asserted to gain no parse finding; the corpus sweep lint-startup-registry-verdict.corpus.test.ts was measured first -- 10 of 1865 files actually reach the parse (rest cut by the raw-text pre-filter) and 0 carry a diagnostic, so that ratchet cannot redden today. WRAPPER ATTRIBUTION (the delicate site): a test pins that the synthesised 'async function __body(ctx) {...}' wrapper parses clean around a trivial body AND around every ledger example, so a failure is provably the body's; positions are remapped into the body's own coordinates (synthesizedLinesBefore: 1, clamped) and pinned. FIXTURE RE-JUDGED, not edited: lint-startup-registry-verdict.test.ts asserted toEqual([]) for an unparseable source -- it passed BECAUSE nothing was produced; split into the half that still holds (no startup verdict from wreckage) and the half that does not. ABLATIONS -- each mutation confirmed ON DISK by anchor count (old:1->0 new:0->1), each restore leg verified anchor-gone + tree-clean, predictions written first: A1 checked-parse never sees parseDiagnostics -> predicted 21, measured 21; A2 drop the synthesised-line offset -> predicted 2, measured 2 (both position tests); A3 hook rule stops surfacing the verdict -> predicted 1, measured 1; A4 react gate skips the walk on failure -> predicted 1, measured 1 ('is ADDITIVE'). Under A1 the two independent controls stayed GREEN by design ('cannot throw', which calls createSourceFile raw, and 'the property is really there', which asserts on the compiler) -- that is the non-vacuity proof for the internal-API read. Restore leg re-run: 290 passed, tree clean. FINAL: 'pnpm --filter @objectstack/lint typecheck' TYPECHECK_EXIT=0; 'pnpm --filter @objectstack/lint test' -> Test Files 80 passed (80), Tests 2235 passed | 5 skipped (2240), TEST_EXIT=0; os-verify-lock VERDICT command-exit 0. GATE UNION via 'node scripts/pm/dispatch-gates.mjs' (no paths), all 16 EXIT=0, each quoting its own verdict line: nul-bytes ('OK, scanned 6218 text file(s)'), parse-guard ('122 scripts/ file(s)'), cross-package-test-inputs, slot-lookup, test-source-alias, type-source-resolution, changeset-gate-self-tests, objectui-changeset, empty-changeset, adr-0087-registration, changeset-no-major, affected-docs, query-options-erasure, type-check-coverage, engine-double-contract, where-matcher. HAND CROSS-CHECK (the brief's warning paid off): check:parse-guard -- the family this card is ABOUT -- sits in dispatch-gates' unreachable-by-construction bucket because its population literal 'scripts' is refused as too generic, so no path derivation could name it; run explicitly. NARROWING DECLARED: check:type-check-debt (--re-measure) needs the whole workspace closure built and was not run. @objectstack/lint is a TEST_DEBT entry (19), measured through a generated test-inclusive project that 'pnpm --filter ... typecheck' does not see, so substitute measurement taken: a test-inclusive tsc program over packages/lint/src/**/*.ts reports 0 errors, none in any file this PR touches, so the count cannot drift up from this change. (First attempt at that measurement returned TS2688 -- a setup failure, i.e. INVALID rather than zero -- and was re-run with typeRoots resolved before being read.)\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"filed as #10696: the react props gate (TypeScript) and the react syntax gate (Sucrase) accept different sources -- measured both directions: 0755, \\\\012, 0b2 and 1__0 pass Sucrase and are rejected by TypeScript; with(o){} goes the other way. The card recorded this as deliberately unmeasured; filed only after measuring, and pinned as a regression test in the PR.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
