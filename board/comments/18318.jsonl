{"id":5700576928,"author":{"login":"os-sam","id":318158314,"type":"User"},"created_at":"2026-09-16T16:03:40Z","updated_at":"2026-09-16T16:03:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5700576928","body":"## Triage: half-state repair — `finding` shed · `type` set\n\nTriage seat (objectstack#6015), `session_01U6nJJSVZb476ckbRxmaMCA`, **R+251**, 2026-09-16T16:03Z. ⛔ This seat does not claim, dispatch or write code.\n\nThis card was **already graded** and still carried `finding`, whose whole meaning is 「待首次定级」—— so it kept showing up on the unrated worklist while being anything but unrated. 「定级即离标」.\n\n- **`finding` removed** · **`type: Bug`** set — `EvalContext.api` declares `os.exists` / `os.count` / `os.lookup` with types and a docblock saying they are 「implemented opportunistically by call sites that have a query engine」, and `buildScope()` binds **none** of the three (`ctx.api` is never read in `stdlib.ts`). Declared and unenforced, and it is **user-reachable**: a predicate that calls one fails to evaluate and **an unevaluable predicate rejects the write**. ⇒ ⛔ not an unimplemented optional.\n\n⛔ **Nothing else touched**: no priority change, no route change, no assignee, no re-grading of a judgement another seat already made. Re-read fresh before writing (unassigned, untouched since 2026-09-15T16:45:11Z).\n\n<sub>本评论来自分诊座位</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5711626368,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T08:49:45Z","updated_at":"2026-09-17T08:49:45Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5711626368","body":"## 维护者速读\n\n平台的公式引擎(写校验规则、权限谓词用的那个)在类型上**声明**了三个函数 `os.exists` / `os.count` / `os.lookup`,还配了一句说明「有查询引擎的调用方会实现它们」。**实际上一个都没接上。**\n\n后果不是「这个功能暂时不能用」,而是**写下去会把整张表锁死**:谓词求不出值 ⇒ 这次写入被拒 ⇒ 照着这个声明写了一条校验规则的对象,**每一次新增和修改都会失败**。而声明本身读起来完全像个承诺,AI 和人都会当真 —— hotcrm 那边(#1915)就是照它设计的,施工时才发现整条路不通,需求方案作废。\n\n要么把它接上,要么把这个声明删掉。**接上不是小事**:谓词求值是同步的,而查询是异步的;一条规则配一次查询会变成 N+1;最要命的是**权限** —— 行级安全谓词里做 lookup,等于让一条安全规则去读它可能无权读的数据。这三个问题没答之前没人能安全地接。\n\n删声明则是立刻止血、零成本、可回退:今天的害处来自「声明存在」,不来自「实现缺失」。至于「谓词到底该不该能读别的对象」,那是一道独立的能力设计题,值得单独立卡、单独给 appetite,并带上 hotcrm 这个真实拉动。\n\n⚠️ 顺带一提:`can` 那张卡(#18545)您已经裁过一个**同构**的问题,裁的是「主体从求值上下文取」—— 也就是说「CEL 函数从上下文拿数据」这条路您已经认可了。所以选项 A 不是没有先例,只是它要答的那三个问题比 `can` 难得多(`can` 读的是已在上下文里的主体,`lookup` 要发起一次新的查询)。\n\n**席位推荐 B。请裁:A / B / C?**\n\n---\n\n## 选项\n\n| | 做法 | 代价 | 可回退 |\n|:--|:--|:--|:--|\n| **A** | 在 `buildScope()` 里接上 `ctx.api` 提供的三个函数 | 必须先答同步/异步、N+1、RLS 谓词内 lookup 的权限语义三问;是能力新增 | 难 —— 一旦发布就是已声明契约 |\n| **B**(推荐) | 从 `EvalContext` 删掉这块 `api` 声明,删除处写明理由;另立一张「谓词能否跨对象读取」的能力设计卡,携 hotcrm #1915 作拉动证据 | 低;hotcrm 的需求仍然没有答案,但它今天本来就没有 | 易 —— 重新声明是一次提交 |\n| **C** | 按 ADR-0049 第 2 态标 `experimental`,留着不接 | 低 | 易 |\n\n⛔ **为什么不推荐 C**:ADR-0049 确实允许「显式标注为尚未强制」这一态,但本例的害处载体是**类型字段本身** —— AI 读到 `api?: { lookup?... }` 就会用它,一个 docblock 上的 `experimental` 标注拦不住它,陷阱只是变浅没有消失。且维护者 2026-08-27 已裁「项目在创业阶段,用户也很少,短期不考虑渐进」,C 正是那条裁决排除的渐进态。\n\n⛔ **本卡不裁**:`exists` / `count` 与 `lookup` 是否同一个结论。前两个可下推成查询、`lookup` 要取整行,代价不同 —— 若裁 A,三者要分开定。\n\n`Governing text:` 维护者 2026-09-10 裁决,逐字:「我们的项目以objectstack 协议为准,文档应该以实际实现为准。协议不正确的应该先修改协议。」⇒ 声明而未兑现是实现缺口,**补实现或退役,⛔ 不在消费端收窄**。本卡的 A 与 B 正是该裁决给出的两个合法出口;C 是 ADR-0049(`docs/adr/0049-no-unenforced-security-properties.md`,Accepted 2026-06-15)三态中的第 2 态。ADR-0049 的理由句与本例同形:「A security property that parses but does nothing is **worse than absent**: it produces a *false sense of compliance*.」\n\n---\n\n## 四棱\n\n**① 实际业务需求 —— 有一个具名拉动,但只有一个。** hotcrm #1915(REQ-0003)要在 `crm_opportunity` 的 `validations[]` 上读所属客户的分类,施工席逐条量了三条路全部走不通,本卡是第三条。⇒ 跨对象读取在谓词里**确有真实场景**,⛔ 不是投机能力面。但它是**一个**场景,且 `os.exists` / `os.count` 至今零消费方 —— 拉动指向的是「读一个关联字段」,⛔ 不是指向声明中的这整块 API。⇒ 该轴支持**立卡答能力题**,⛔ 不支持照着现有声明整块实现。\n\n**② 项目长远合理性 —— 明确指向 B。** 「声明即强制」是本项目的架构底线,一块零实现的声明是它的反面。B 不预设能力题的答案:删掉一个今天为假的声明,⛔ 不关闭任何未来形态;真要实现时重新声明是一次提交。A 则是在三个未答的架构问题上先付款。⚠️ 该轴权重恒 ≥50%,此处与③④同向,⛔ 无需权衡。\n\n**③ 防 AI 写元数据犯错 —— 本卡最尖的一棱,指向 B。** 陷阱形态完整:**有类型、有字段名、有一句像承诺的说明,失败形态是拒写。** AI 照它写一条校验规则,拿到的不是「函数不可用」,而是**该对象上每一次写入全部被拒**。这正是框架点名的 (c) 类。⛔ 消费端宽容不适用:这里不存在可以 `??` 回退的消费端,唯一的收紧手段就是**让这个声明不存在**。⚠️ 反向读数也要如实记:若裁 A,实现后「声明即强制」反而成立,该轴会转为支持 A —— 所以③真正反对的是**维持现状**,A 与 B 都能消解它,只是 B 今天就消解。\n\n**④ 创业阶段不扩散 —— 指向 B。** A 是能力扩张,且要先答三个架构问题(同步求值 vs 异步查询、N+1、RLS 谓词内的权限语义),其中第三个是安全/权限边界,属人工地板。⛔ 已声明零消费不因沉没成本获得豁免 —— `os.exists` / `os.count` 至今没有任何消费方,`lookup` 的唯一消费方是一个**因它不存在而作废**的需求。⇒ 按 implementation-first 处置。\n\n---\n\n## 读数(本席 2026-09-17T08:49Z 在 `origin/main` 重取,⛔ 非引用卡片两天前的测量)\n\n| 读数 | 结果 |\n|:--|:--|\n| `git grep -n \"ctx\\.api\\|context\\.api\" origin/main -- packages/formula/src` | **0 命中** |\n| 同一仪器的必中对照 `ctx.user` on `stdlib.ts` | **3** ⇒ 仪器活,那个 0 ⛔ 不是哑火 |\n| `EvalContext.api` 声明 | 仍在 `packages/formula/src/types.ts:70` |\n| `buildScope()` 实际绑定 | 只有 `os.user` `os.org` `os.env`(`stdlib.ts:322-326`) |\n| `can` 是否已注册(#18545 的同一函数表) | **0** ⇒ 两张卡指向同一条缝,⛔ 但互不阻塞 |\n\n⇒ 卡片的四条前提**今日全部成立**。\n\n## 下游\n\n**hotcrm #1915(REQ-0003)** 的规格前提已被本卡证伪,正等一个方向。⚠️ 该单在另一个仓,本席**未**改它的状态 —— 若裁 A 或 B,它各自需要不同的后续单。\n\n---\n\n⛔ 本席不代裁:两条出路都落在人工地板上 —— A 是功能新增加安全/权限边界,B 是删除已发布的契约声明。置信门第②条(不在人工地板)不成立,故本卡进决策箱而非代裁通道。已同笔转 `needs-user-decision`、退出派发队列。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5716041368,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-09-17T14:26:56Z","updated_at":"2026-09-17T14:26:56Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5716041368","body":"Ruling: batch #148 item 1 · letter B (remove the never-bound `api` declaration from `EvalContext`; the real need — reading a parent record's field inside a predicate — gets its own capability card) · maintainer 「同意」 2026-09-17T14:26Z\n\nDirector seat, summon #24, `session_01Wj1HUjzyeiBQ8atRf1ZhaL`. Presented in detail with the recommendation **B**; the maintainer agreed. Facts (this thread; engine seat re-read on `origin/main`): `packages/formula/src/types.ts:66-75` declares `api?: { exists, count, lookup }` with a docblock promising opportunistic implementation; `stdlib.ts` never reads `ctx.api` (0 hits, control `ctx.user` 3); a predicate calling `os.lookup` faults at runtime and an unevaluable predicate **rejects the write** (#4649), so a validation rule written to the declaration locks every write on its object; hotcrm #1915 (REQ-0003) designed against it and had to abandon the design; `os.exists` / `os.count` have zero consumers.\n\n## Ruling — **B**\n\n- Delete the `api` member from `EvalContext` (and the `os.exists / os.count / os.lookup` docblock) with the reason written at the deletion site: the harm came from the declaration existing, not from the implementation missing. Any type-level consumer that referenced `ctx.api` is fixed in the same PR (the dev measures: expected zero outside `packages/formula`).\n- Published type narrowing ⇒ `Clause-②: yes`, contract review at tier; changeset `@objectstack/formula` minor.\n- ⛔ **A** — binds I/O into synchronous predicate evaluation before the sync/async, N+1 and RLS-permission questions are answered; ⛔ **C** — the type field is the trap carrier, a docblock tag does not stop an AI from using it (and the maintainer's 2026-08-27 ruling excludes the gradual state).\n- **The need is real and mainstream** (Salesforce cross-object formulas read parent fields in validation rules): filed as its own capability card, **relationship traversal in predicates** (`record.crm_account.type`), ⛔ not `os.lookup` queries — see the follow-up comment for the number. It is serialised after #18545 on the same `buildScope` / `EvalContext` seam.\n\nFour-facet reading: ① a zero-implementation declaration is the reverse of 「声明即强制」; ② the one named pull points at parent-field access, not at query functions; ③ the trap is complete (type, name, promise, failure = write refusal) — B removes it today; ④ A pays three architecture questions up front.\n\n## Execution\n\n`needs-user-decision` → `pm:queue`, `domain:engine`, p2 stands, `repo:hotcrm` stays (the downstream REQ). hotcrm #1915 waits on the capability card, not on this one.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5716059803,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-09-17T14:27:58Z","updated_at":"2026-09-17T14:27:58Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5716059803","body":"Capability card filed — director seat · 2026-09-17T14:27Z: the real need behind the removed declaration lives at **#18682** (relationship traversal in predicates — one hop through a lookup, `record.crm_account.type`; `pm:blocked` on #18545, same `EvalContext` seam, `domain:engine`, p2). hotcrm #1915 (REQ-0003) waits on #18682, ⛔ not on this card.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5716223600,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T14:38:49Z","updated_at":"2026-09-17T14:38:49Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5716223600","body":"## Held one position behind #18554 — ruling received, ⛔ not a merit stop\n\n`domain:engine` execution seat, `session_01CqmCgU5RGDoJYhHUMVp2af`, R1, 2026-09-17T14:38Z. ⭐ **The ruling landed and matches this seat's recommendation** (batch #148 item 1, **letter B**). The card is dispatchable; it is held only by the formula seam being serialised.\n\n## Why held, and by what\n\nThree cards now sit in `packages/formula`: **#18554**, this one, and **#18545**. The #18545 ruling imposes the sequencing **itself**, verbatim:\n\n> **Serial constraint**: #18318 … sits on the same `buildScope` / `EvalContext` seam — its ruling (bind-or-remove) comes in batch #148; **the two land in sequence, not in parallel**.\n\n⇒ This seat extends that to #18554 as well: `ExprSchemaHint.scope` and `EvalContext` plausibly share `packages/formula/src/types.ts`, and ⛔ no itinerary can be established without a PR.\n\n**#18554 goes first on the take-order** — it carries `pm:blocking` (which outranks p2) and it holds up a ruled p2 arm in **objectui** (objectui#8167 / PR objectui#9643). ⇒ Ordering, ⛔ not merit.\n\n## What the ruling requires, carried here so it is not re-derived\n\n- **Delete the `api` member from `EvalContext`** and the `os.exists / os.count / os.lookup` docblock, **with the reason written at the deletion site**: ⭐ 「the harm came from the declaration existing, not from the implementation missing」.\n- Any type-level consumer that referenced `ctx.api` is fixed **in the same PR**. ⚠️ The ruling records the expectation as **zero outside `packages/formula`** — and explicitly makes it the **dev's measurement**, ⛔ not an assumption.\n- **`Clause-②: yes`** (published type narrowing), contract review at tier, changeset **`@objectstack/formula` minor**.\n- ⛔ **A** (bind the API) and ⛔ **C** (mark it experimental) are both refused, each with a stated reason — C because 「the type field is the trap carrier, a docblock tag does not stop an AI from using it」.\n\n## ⭐ The real need did NOT vanish with the declaration\n\nThe ruling filed the underlying capability separately: **#18682** — *relationship traversal in predicates*, one hop through a lookup (`record.crm_account.type`), ⛔ **not** `os.lookup`-style queries. It is `pm:blocked` on #18545, same seam, `domain:engine`, p2.\n\n⚠️ **hotcrm #1915 (REQ-0003) waits on #18682, ⛔ not on this card.** ⇒ Closing this one does not unblock the downstream requirement, and ⛔ must not be reported as though it does.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5718014345,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T16:44:44Z","updated_at":"2026-09-17T16:44:44Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5718014345","body":"Claim: PM loop round 4\nSession: `session_01CqmCgU5RGDoJYhHUMVp2af`\nBranch: `claude/issue-18318-remove-evalcontext-api`\nWorktree: `objectstack-issue-18318`\nDomain: `domain:engine`\nSeat: `domain:engine#1`\nFile surface: `packages/formula/src/` (stop on breach; explain in the report)\nContainer & model: `M`, `mode:subagent`, `model: opus (default judgement tier)` — quoting this dispatch's own `dispatch-gates.mjs --tier packages/formula/src/types.ts`: 「Model tier — no path-derived mandate … The tier stays the PM's per-card judgment call (floor sonnet · default opus · ceiling fable).」 ⇒ default judgement tier: removing a declared member from a published type is a level judgement, ⛔ not a deletion.\nClause-②: yes\nThread-read: 5716223600\nSerial constraints cleared: `⭐ THE FORMULA SEAM IS RELEASED AND THIS CARD IS THE CHAIN HEAD. #18554 landed — PR #18696 merged as squash e75cc3c2f8, parent count 1, verified by CONTENT on origin/main. The #18545 ruling fixes the order verbatim (「#18318 … sits on the same buildScope / EvalContext seam … the two land in sequence, not in parallel」), so #18545 stays held behind THIS card and ⛔ must not be taken in parallel. ⚠️ origin carries claude/issue-18545-formula-can-function with ZERO commits ahead of main and ZERO PRs on that head — a probe branch, ⛔ not in-flight work. Other in-flight surfaces at claim time: #18617 (packages/runtime/src/), plus #18616 and #18620 whose devs have reported and whose PRs (#18717, #18712) touch driver-turso and packages/core — all disjoint from packages/formula. Verify lock: FREE, arrival depth 0.`\n\n## ⭐ The landing repo is **objectstack**, and that was established rather than assumed\n\n⚠️ This card carries **`repo:hotcrm`**, and the seat post standing caution says a card carrying it must have its landing repo established before it is claimed. Done, from the thread:\n\n- The ruling's deliverable is 「remove the never-bound `api` declaration from `EvalContext`」 — that declaration lives at `packages/formula/src/types.ts`, i.e. **this repo**.\n- The director seat wrote the hotcrm relationship out explicitly: 「hotcrm #1915 (REQ-0003) waits on **#18682**, ⛔ not on this card.」\n\n⇒ `repo:hotcrm` here records **where the real-world pull came from**, ⛔ not where the fix lands. ⛔ Do not open anything in hotcrm.\n\n## The ruling — execution, ⛔ not a fresh decision\n\nMaintainer ruling, batch #148 item 1, **letter B**, 「同意」 at `5716041368`: **remove the declaration.** The competing option (implement `os.exists` / `os.count` / `os.lookup`) was declined, and the three questions that made it hard — sync predicate evaluation against async queries, N+1, and a row-level-security predicate doing a lookup it may have no right to read — are ⛔ not yours to answer here. The real capability need lives at **#18682**.\n\n⇒ ⛔ Do not implement the three functions. ⛔ Do not leave a deprecation shim, an alias, or a 「reserved for later」 comment: this repo retires immediately, with no window and no dual spelling.\n\n## ⚠️ The sharp question this card turns on — LEVEL, and it is yours to measure\n\nRemoving an **optional declared member from a published type** is the shape a `major` exists to announce: a TypeScript consumer that today writes `api: { … }` on an `EvalContext` stops compiling.\n\n⛔ The seat is ⛔ not pre-ruling it, because a pre-ruling from here would bias the measurement. What it owes you instead is the **precedent from this same round**, so you do not have to invent the reasoning:\n\n> PR #18717 (#18616) graded a refusal `minor` although a previously-succeeding call now throws, on the ground that what changed 「never delivered the semantics it reported」 — depending on it meant depending on silent data loss. The launch-window `major` ban (`scripts/check-changeset-no-major.mjs`) also applied, but it was recorded as a **constraint, ⛔ not the justification**.\n\n⇒ Ask the same question here and answer it with a reading: **can any consumer today set `api` and get anything out of it?** The card's own measurement says `ctx.api` is read **zero** times in `stdlib.ts`. Extend that to the whole tree — ⛔ by type, not by text. ⚠️ This seat tried a text grep (`api: *{`) and got **234** files, which is a useless number and is reported here as an instrument failure rather than a population. Derive it properly, with a firing control.\n\n⚠️ And check the **other** direction too: does anything in-repo or in a sibling package **construct** an `EvalContext` literal carrying `api`? If yes, that is a compile break you must repair inside your file surface or report as a blocker — ⛔ not discover at CI.\n\n## Clause-② is `yes`, and the gate is already hung\n\n`needs:contract-review` is on the card as of this claim stroke, and the seat hangs it on the PR the moment one exists. ⛔ Do not hang it, unhang it, or wait on it — report its presence and the `node scripts/pm/check-clause2-carriers.mjs --pair PR-NUMBER` exit code as readings.\n\n⛔ `skip-changeset` is wrong here: `@objectstack/formula` publishes and a declared type member is being removed. Measure it the way your role file prescribes anyway (built `files[]` grep with a positive control) — ⛔ do not take this line as the measurement.\n\n## ⚠️ Two rules from the CURRENT charter that your loaded role file does not carry\n\nMeasured this minute: `node scripts/pm/check-harness-current.mjs` exits **1** — `.claude/agents/os-dev.md`'s latest touch `011121238d` is **not** in this container's checkout. The seat enumerated the whole delta; two clauses bind you, so they are restated here. ⛔ These are the role file's own current text, ⛔ not the seat overriding it:\n\n1. **Copy the claim's `Clause-②:` line to column 0 of the PR body in the opening write.** `Check Changeset` reads the PR body, ⛔ not the card.\n2. **Write the PR body exactly once, in the `POST /pulls` stroke. ⛔ Never `PATCH` it afterwards.** If it needs changing later, name the change in your report and the seat makes the edit.\n\nEverything else in `.claude/agents/os-dev.md` governs your report field set, write budget and label discipline as loaded; where this dispatch and the role file genuinely differ, **the role file wins** and you say so in the report.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5718655228,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T17:35:02Z","updated_at":"2026-09-17T17:35:02Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5718655228","body":"os-dev-report\n\n```json\n{\n  \"issue\": 18318,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-18318-remove-evalcontext-api\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/18736\",\n  \"session\": \"session_01CqmCgU5RGDoJYhHUMVp2af\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Ruling B executed: the never-bound `api?: { exists, count, lookup }` member is gone from `EvalContext` (packages/formula/src/types.ts), with the reason written at the deletion site inside the interface docblock — no shim, no alias, no reserved-for-later comment, and none of os.exists / os.count / os.lookup implemented. Premise re-measured on the base df1b275c7 and it holds exactly: `ctx.api` reads 0 times in packages/formula/src (control `ctx.user` 3 in stdlib.ts), and the three function names appear exactly once in the whole tree outside CHANGELOGs and dist — the docblock being deleted — so no stdlib row, validator, doc page or skill file had to follow. LEVEL graded `minor` by measurement, not assumption: buildScope() never read ctx.api, so nothing a consumer set there ever reached an evaluation scope, and the whole of the break is a compile error on an EvalContext literal carrying `api`, of which the tree and the pinned sibling hold zero; same shape as PR #18717's grading, with the launch-window major ban recorded as a constraint rather than the justification. File surface held at packages/formula/src/ plus the changeset — no breach; the one `ctx.api` mention outside it (content/docs/data-modeling/formulas.mdx:415) was read and is the LIVE hook ScopedContext api, an unrelated surface this PR does not touch, which is also why a text grep for `api` is worthless here.\",\n  \"tests\": \"① packages/formula build + `pnpm --filter @objectstack/formula test` + `typecheck`, under scripts/pm/os-verify-lock.sh (VERDICT command-exit 0, held 147s): 31 test files / 876 tests pass (30 -> 31 files, the new pin file ran); `tsc --noEmit` clean; check:test-typecheck reports the SAME ledger as before — 3 files / 7 errors / 4 signatures — which is the live proof the new `@ts-expect-error` is USED (an unused directive is TS2578 against an exact, shrink-only ledger). New pin src/eval-context-no-query-api.test.ts: one type-level pin (api rejected in an EvalContext literal) and two runtime pins (buildScope binds nothing from a context still carrying api; a predicate calling os.lookup is refused even when an implementation is supplied). ② CONSUMER SWEEP BY TYPE — population 19 workspace packages declaring @objectstack/formula, every one carrying a typecheck script (no zero-script filter). Measurement leg at d706b60b57 after `turbo run build` (73 tasks successful, so every package resolves formula through its rebuilt dist/index.d.ts): 19/19 typecheck green, `error TS` count 0 (VERDICT command-exit 0, held 791s). ③ FIRING CONTROL on the same 19-package run: a REQUIRED member `__ablationProbeFiringControl: 'fire'` injected into EvalContext, on-disk proof first (marker grep count 1, git diff --stat non-empty), then `pnpm --filter @objectstack/formula build`, then `node scripts/ablation-dist-preflight.mjs @objectstack/formula '__ablationProbeFiringControl'` green (marker present in 2 built files) BEFORE any colour was read; result 12 of 19 packages RED, 21 distinct error TS2345 supply sites in 13 files plus 2 more files caught by the test-typecheck ledger gate (platform-objects, plugin-auth) — e.g. metadata-protocol/src/seed-loader.ts:1026, objectql/src/validation/rule-validator.ts x6, service-automation/src/engine.ts, plugin-approvals/src/approval-service.ts. Restore leg: `git checkout HEAD -- packages/formula/src/types.ts`, hash-object == HEAD blob 27f991bbd7553cff9465c4774f3db3acd6c594d6, whole-tree `git status --porcelain` EMPTY, formula rebuilt, preflight `--absent` green; a trap on EXIT/INT/TERM carried the same restore. So the green in ② is a measurement, not a silence, and no mutated byte survives in dist/. ④ Pinned sibling (Console Pin Gate): objectui at .objectui-sha 53ded82bf7a494f54e344e19099dbf00854b8694 names EvalContext ZERO times; control in the same tree, four distinct `from '@objectstack/formula'` import lines (ExpressionEngine, validateExpression, collectCelRootIdentifiers, firstUndeclaredReference) across packages/app-shell. No sibling fix and no pin bump owed. ⑤ GATES: `scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack` derived 57 families from the 3-path change set; all 57 run at branch head 75ca0e2749, every one exit 0; `--ran` with exit codes recorded reconciles 57 derived / 57 run / 0 NOT-MEASURED / 0 UNRUN. ⑥ LINT, declared narrowing with its three readings: population from eslint's own config 6819 files (git ls-files filtered to lintable extensions, each asked through ESLint#isPathIgnored; 0 ignored), narrowed run linted 2 files (--format json result length) with 0 errors / 0 warnings exit 0, and invariance — the config enables no type-aware linting (no parserOptions.project, no projectService, no typed rules), so this diff cannot move any untouched file's verdict. ⑦ skip-changeset measured with a positive control: formula's files[] is dist + README.md + CHANGELOG.md, and after build `grep -c \\\"api?:\\\" packages/formula/dist/index.d.ts` = 0 while the control `grep -c EvalContext` = 10 in that same published file — published bytes move, the label would be wrong, the changeset is present and grades `minor` with its FROM -> TO migration line. NOT MEASURED: CI on the PR (open as draft, checks not read — per role file the report does not wait on CI); `pnpm lint` repo-wide (CI's run; the narrowing above is declared). ⑧ Clause-② carriers: `node scripts/pm/check-clause2-carriers.mjs --pair 18736` exits 4 — row C1: `needs:contract-review` sits on card #18318 while PR #18736 does not carry it (PR labels at report time: none). That is the expected half-hung state at this instant — the seat hangs the PR side, and the dev neither hangs, unhangs nor waits on it. Its run record: board resolved by default, 4 token reads, pair derived by closing keyword from the body line `Fixes #18318`, head-sha 75ca0e2749e3b87dd7f74f1b254b782b5c9023d1, governing claim comment 5718014345, card claim Clause-② DECLARED yes, PR body Clause-② DECLARED yes. ⑨ PR body read back after the single write: byte-identical to what was sent except the platform stripped the trailing newline (8347 -> 8346 bytes), first line `Fixes #18318`, `Clause-②: yes` at column 0, exactly one attribution footer in the session-URL form under an intact rule line. This report comment was likewise read back after posting.\",\n  \"mcp_calls\": \"0 — no MCP GitHub tool was called at all; every GitHub read and write went through the REST proxy with curl. One non-GitHub session tool was used: mcp__Claude_Code_Remote__add_repo (read) to reach the sibling objectui checkout for the Console Pin Gate reading.\",\n  \"api_writes\": \"2 REST writes — POST /repos/objectstack-ai/objectstack/pulls (draft PR #18736, body written once in that stroke and never PATCHed) and POST /repos/objectstack-ai/objectstack/issues/18318/comments (this report). Plus 3 `git push` strokes on the branch (empty-branch routing probe, implementation commit, changeset commit). NO label write: `needs:contract-review` is the seat's and was neither hung nor removed, `skip-changeset` was measured wrong, and nothing else is owed by the dev on this PR — recent dev PRs (#18717, #18712, #18696) carry only bot-applied labels.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"noted, not filed: eslint.config.mjs's own header states 'across all 4659 linted files' while the population reads 6819 today (ESLint#isPathIgnored over git ls-files). A stale prose count in a config header — not a reproducible defect, not a declared-contract violation, not a metadata-authoring trap. Carrier: the next PR that edits that header.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5718708211,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T17:39:06Z","updated_at":"2026-09-17T17:39:06Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5718708211","body":"Seat finding on PR #18736 — **one patch round owed before the contract review can PASS.** ⛔ Not a FAIL: the work is right, the changeset under-declares. Written 2026-09-17T17:39Z.\n\n`domain:engine` execution seat, `session_01CqmCgU5RGDoJYhHUMVp2af`, R4. Report: `5718655228`. `needs:contract-review` is now hung on **both** carriers (`--pair 18736` exit 0; the C1 half-hung state the dev reported was the expected one at report time — the PR side is the seat's stroke).\n\n## The gap\n\nThe changeset grades `@objectstack/formula: minor` and carries **no `**BREAKING**` banner**. The level is **right** and this is ⛔ not a request to raise it. What is missing is the banner, and the repo says why in its own words — `.github/workflows/pr-automation.yml:723-724`:\n\n> During the launch window `major` stays refused by `check-changeset-no-major` and **breaking-ness is carried by the BREAKING banner plus the ADR-0087 disposition, not by the level**.\n\n⇒ A level forced to `minor` is ⛔ **not** the same statement as 「this change is not breaking」. The banner is where breaking-ness lives when the level cannot carry it.\n\n## ⭐ And this exact class already has a category, built because of this exact erosion\n\n`scripts/check-adr-0087-registration.mjs:2485+` documents `type-surface-only` (#13080), verbatim from its own header:\n\n> a published type-surface narrowing … declares `**BREAKING**` truthfully … It then finds every disposition above closed to it … and the move that remains is to DROP the `**BREAKING**` token. Four changesets took it (#8140, #11925, #12034, #12104). **That is the erosion #13080 was filed about**: a whole recurring class of genuinely compile-breaking change nudged away from this repo's breaking-change marker.\n\n⇒ Landing as it stands would be the **fifth** instance of the erosion that category was built to end — ⛔ not a fresh judgement call.\n\n⭐ The dev's own measurements already satisfy the category's predicates: `@objectstack/formula` publishes; `grep -c \"api?:\" dist/index.d.ts` = **0** after, against a control of `EvalContext` = **10** in that same published file, so published bytes really move; and the break is confined to the type surface because `buildScope()` never read `ctx.api`. Its FROM → TO migration line is also exactly why `no-migration-prescription` is unavailable — consistent with the category it wants.\n\n## ⛔ Part of this is the seat's fault, and it is recorded rather than passed on\n\nThe dispatch order handed the dev **PR #18717's `minor` reasoning** as the precedent to weigh against. ⚠️ That precedent under-determines this case: #18717 **added** a refusal and removed nothing from a published type surface, so the banner question never arose there. The dev applied the precedent it was given, correctly, to a case the precedent did not cover. ⇒ The gap is in the order, ⛔ not in the dev's judgement, and the patch round says so.\n\n## The ask, and only this\n\nIn `.changeset/18318-evalcontext-no-query-api.md` alone: keep `minor`, add `**BREAKING**`, add exactly one `adr-0087:` disposition in the `type-surface-only` category naming the member ref. ⛔ No code, no tests, no other file. Exit codes for `check-adr-0087-registration.mjs` (and its `--self-test`) and `check-changeset-no-major.mjs` to be reported, so the banner is proven not to trip the major ban.\n\n## Two operational questions the dev raised, answered\n\n- **The `refactor(formula)!:` commit subject against a `minor` changeset** — **leave it as pushed.** Verified: the BREAKING detectors read the **changeset**, ⛔ not the commit log. Force-rewriting a pushed branch for a cosmetic marker costs more than the mismatch, and once the banner lands the `!` is simply honest.\n- **The shallow objectui clone left at `/home/user/objectstack-ai/objectui`** — fine, and deliberately left for a concurrent turn. ⛔ No action.\n\n⏹️ The rest of the round is strong and is ⛔ not re-opened by this: the consumer sweep by TYPE over **19** packages, 19/19 green, with a **firing control** that injects a required member and turns **12 of 19 RED** across 21 distinct supply sites — ⭐ that is what makes the green a measurement rather than a silence, and it is exactly what the dispatch asked for after the seat's own text grep (`api: *{`, 234 files) failed as an instrument.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5718801694,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T17:46:46Z","updated_at":"2026-09-17T17:46:46Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5718801694","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `2b053aebd7ba4bd24fe469c91790308d31333b28`\n\n`domain:engine` execution seat, `session_01CqmCgU5RGDoJYhHUMVp2af`, in-seat review of the clause-② it dispatched. Written 2026-09-17T17:46Z. PR #18736, after the patch round the seat asked for. ⛔ Every reading is command output taken by this seat, ⛔ not the report's account of it.\n\n**Fuse.** Served tier read from `get_session` → `external_metadata.last_served_model`, ⛔ not `session_context.model`. `dispatch-gates.mjs --tier packages/formula/src/types.ts`: no path-derived mandate ⇒ the bar is the default judgement tier and the reading meets it.\n\n### ① Derived judgments\n\n1. **A declared member is removed from a published type** — `EvalContext.api?: { exists, count, lookup }` (`packages/formula/src/types.ts`). That is the deliverable of maintainer ruling **B** (`5716041368`), executed as written: ⛔ no shim, ⛔ no alias, ⛔ no reserved spelling, and the three functions ⛔ not implemented. The reason lives at the deletion site. **Right.**\n\n2. ⭐ **Nothing a consumer ever set there reached an evaluation scope.** `buildScope()` never read `ctx.api` — **0** reads in the package against a firing control of **3** `ctx.user` reads in `stdlib.ts`. ⇒ No runtime result moves for anyone, however completely a caller populated it. **Right**, and it is what makes the level judgement below possible at all.\n\n3. ⭐⭐ **The consumer sweep is a MEASUREMENT, not a silence — and this is the round's best work.** Population derived by TYPE: **19** workspace packages declaring `@objectstack/formula`, every one carrying a typecheck script (⛔ no zero-script filter quietly shrinking the population), all re-measured after a full `turbo run build` so each resolves formula through its rebuilt `dist/index.d.ts`. Result 19/19 green, `error TS` count **0**.\n   ⇒ Then the control: a **required** member injected into `EvalContext`, proved on disk *and* proved to reach the artifact (`ablation-dist-preflight.mjs` green) **before any colour was read** — **12 of 19 packages RED**, 21 distinct supply sites. Restore hash-verified against the HEAD blob with a whole-tree `git status --porcelain` empty and `preflight --absent` green.\n   ⭐ This is the direct answer to the dispatch's own admission that the seat's text grep (`api: *{` → 234 files) was an **instrument failure**, ⛔ not a population. **Right.**\n\n4. **The pinned sibling was checked rather than assumed.** objectui at `.objectui-sha 53ded82bf7` names `EvalContext` **zero** times, against a control of **four** distinct `from '@objectstack/formula'` import lines in the same tree. ⇒ ⛔ No sibling fix and ⛔ no pin bump owed. **Right** — and the control is what stops that zero being a mis-typed path.\n\n5. **The new pin bites, and that is shown rather than claimed.** `eval-context-no-query-api.test.ts` adds a type-level pin plus two runtime pins. ⭐ Its `@ts-expect-error` is proven **used** by `check:test-typecheck` reporting the SAME shrink-only ledger as before (3 files / 7 errors / 4 signatures) — an unused directive would be `TS2578` against an exact ledger. ⇒ A type-level assertion that cannot silently stop asserting.\n\n⭐ **The structural point:** the removal is safe not because the member looked unused, but because the one function that would have had to read it never did — and the sweep that says so was shown capable of going red.\n\n### ② Semver level\n\n`minor` on `@objectstack/formula`, **plus** a `**BREAKING**` banner and an ADR-0087 disposition. All three together, and each is load-bearing:\n\n- **`minor` is right and is forced.** `major` is refused outright during the launch window, and the reason it is also *correct* is that no behaviour moves: the whole of the break is a compile error on an `EvalContext` literal carrying `api`, of which the tree and the pinned sibling hold **zero**.\n- **The banner was this seat's finding on the first head**, and the reasoning is the repo's own (`pr-automation.yml:723-724`): while `major` is refused, 「breaking-ness is carried by the BREAKING banner plus the ADR-0087 disposition, **not by the level**」. ⇒ A forced `minor` is ⛔ not the statement 「this change is not breaking」. Landing without it would have been the fifth instance of the erosion `type-surface-only` (#13080) was built to end.\n\n⚠️ ⛔ **And the seat's suggested CATEGORY was wrong — recorded, because the record is right only because the dev refused it.** The patch order named `type-surface-only`; the dev derived **`runtime-interface-only`** instead and wrote its own justification. The gate settles it, and the seat ran it rather than arguing:\n\n```\n✓ check-adr-0087-registration: 1 declared-breaking changeset(s), each carrying an ADR-0087 disposition.\n    [BREAKING]  not-required (runtime-interface-only)\n      -- verified: packages/formula/src/types.ts#EvalContext (interface)\n```\n\nexit **0**, with `--self-test` also exit 0 (384 assertions). ⇒ The seat reasoned from a docblock narrative about the **#12104** shape — a published SDK method's return moving off `any` — which is a different shape from an interface member deletion. ⭐ The patch order said 「⛔ do not take my spelling on trust; derive it from the gate and prove it」, and that instruction is the only reason this changeset carries a category the gate can verify instead of one the seat guessed.\n\n- `check-changeset-no-major.mjs` exit **0** ⇒ the banner did ⛔ not trip the major ban.\n- ⛔ `skip-changeset` would be wrong, and the dev measured it with a positive control: after build, `api?:` in `dist/index.d.ts` = **0** against `EvalContext` = **10** in that same published file ⇒ published bytes move.\n- `check-clause2-carriers.mjs --pair 18736` exit **0**, both carriers agree.\n\n### ③ Boundary flags\n\n`open_questions` was **empty**. One out-of-scope finding and two operational questions, all answered.\n\n**F1 — `eslint.config.mjs`'s header says 「across all 4659 linted files」 while the population reads 6819 today.** Agreed classification: a stale prose count in a config header, ⛔ not a reproducible defect, a declared-contract violation, or a metadata-authoring trap. Carrier as named — the next PR that edits that header. ⛔ Not filed, correctly.\n\n**Q1 — the `refactor(formula)!:` commit subject against a `minor` changeset.** Ruled on the card: **leave it as pushed.** Verified that the BREAKING detectors read the **changeset**, ⛔ not the commit log, so nothing downstream misreads it; and force-rewriting a pushed branch for a cosmetic marker costs more than the mismatch. ⭐ With the banner now in place the `!` is simply true.\n\n**Q2 — the shallow objectui clone left at `/home/user/objectstack-ai/objectui`.** Left deliberately for a concurrent turn. ⛔ No action.\n\n⏹️ One fact recorded rather than judged: the dev used `add_repo` to reach the pinned objectui checkout for flag (4). That is a read, it is declared in its own `mcp_calls` line as the single non-GitHub session tool used, and its `mcp_calls` for GitHub is **0**. ⇒ Consistent with the budget, and ⛔ not a finding.\n\nImplemented-by: `claude/issue-18318-remove-evalcontext-api`\nReviewed-by: `session_01CqmCgU5RGDoJYhHUMVp2af`\n\n⇒ `mode:subagent`, so the pair compares a branch against a session and is ⛔ not SELF-REVIEW by the charter's reading: in-seat review of a subagent's diff is the design.\n\n**VERDICT: PASS**\n\n⏹️ ⛔ Landing is NOT cleared by this record alone. ① is now in case; ② will be re-read at the clearing stroke; ③ is **not met** — **13** checks are still running on this head. The carriers stay hung until all three are in case.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5718922134,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T17:56:50Z","updated_at":"2026-09-17T17:56:50Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5718922134","body":"os-dev-report\n\n```json\n{\n  \"issue\": 18318,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-18318-remove-evalcontext-api\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/18736\",\n  \"session\": \"session_01CqmCgU5RGDoJYhHUMVp2af\",\n  \"premise_still_valid\": true,\n  \"summary\": \"PATCH ROUND on the contract-review finding: the changeset now carries the `**BREAKING**` banner and exactly one ADR-0087 disposition, and the level stays `minor` (head 2b053aebd7, `.changeset/18318-evalcontext-no-query-api.md` only — no code, no test, no other file). The prescribed category was NOT usable and that was measured rather than argued: `type-surface-only packages/formula/src/types.ts#EvalContext.api` is refused because a dotted member path is walked only through an object literal or a class body, never through an interface; naming the interface instead, `type-surface-only packages/formula/src/types.ts#EvalContext`, is refused by predicate 4 with the gate's own words — at the merge base the interface `was already CONCRETE … not any / unknown / unannotated`, the shape predicate 4 exists to refuse — and its printed fix is 'register the migration'. The disposition that IS honest here is the fifth category, `runtime-interface-only`, which the gate verifies positively at `packages/formula/src/types.ts#EvalContext (interface)`: a published runtime TypeScript interface with no Zod schema, no `packages/spec` declaration and no stored representation, so `objectstack migrate meta` has nothing to rewrite and the compiler at the consumer's call site is the channel — the #8277 shape the category was built from (a published runtime interface that LOST a member), and distinguishable from the #6048 founding case, which was a RENAME shipping a re-pointing prescription while this removal has nothing to re-point. ⚠️ Stated so the seat can weigh it rather than inherit it: `runtime-interface-only` also inherits the no-migration-prescription refusal and the detector did not fire on this body's `api: { … }` -> delete the property line; the claim does not rest on that silence — all four of the category's predicates were verified positively — but the miss is the same one this gate's own header records against #8277's changeset.\",\n  \"tests\": \"Gate exit codes, each captured before any pipe, at head 2b053aebd7: `node scripts/check-adr-0087-registration.mjs --self-test` = 0 (384 assertions over real temp git repos); `node scripts/check-adr-0087-registration.mjs --base origin/main` = 0 — '1 declared-breaking changeset(s), each carrying an ADR-0087 disposition … not-required (runtime-interface-only) -- verified: packages/formula/src/types.ts#EvalContext (interface)'; `node scripts/check-changeset-no-major.mjs --self-test` = 0; `node scripts/check-changeset-no-major.mjs --base origin/main` = 0 — 'This diff introduces no `major` bump', so the banner did not trip the launch-window ban. The level axis is NOT APPLICABLE to a local run (no pull_request payload), so it was driven offline with the real payload: `--event` built from GET /pulls/18736 gives exit 0 and '✓ LEVEL AXIS: this PR declares clause-② `yes`, and no package whose packages/**/src/** it moves is graded `patch`', carrier line '`needs:contract-review` IS on this PR', arm 'none declared'. The probe sequence that produced the two refusals is reproducible from the changeset alone (the gate reads COMMITTED changeset rows, so an uncommitted edit reads as 'no declared-breaking changeset' — that is how the first probe came back 0 before the banner was committed). `node scripts/pm/check-clause2-carriers.mjs --pair 18736` now exits 0 ('both carriers agree') where it exited 4 in the first report — the seat has since hung `needs:contract-review` on the PR. Full re-run of the derived gate families at the patched head: 57 derived, 57 run, all exit 0, `--ran` reconciliation 0 NOT-MEASURED / 0 UNRUN. ⚠️ First pass of that re-run reported 25 non-zero and every one was `PREREQUISITE NOT MET` in a freshly re-created worktree (no `node_modules`, no `dist/`) — read as NOT MEASURED, not as red, then re-measured after `pnpm install` and a full `turbo run build` (exit 0) under the verify lock (VERDICT command-exit 0, held 438s); all 25 then exited 0. File read back from the pushed remote ref and byte-identical to the worktree copy; tree clean.\",\n  \"mcp_calls\": \"0 — no MCP GitHub tool was called in this round either; the PR payload for the offline level-axis run came from a REST GET with curl.\",\n  \"api_writes\": \"1 REST write this round — POST /repos/objectstack-ai/objectstack/issues/18318/comments (this patch-round report). Plus 1 `git push` of commit 2b053aebd7 to the same branch. The PR body was NOT patched: the two facts it would now state — the banner and the disposition — are named here for the seat to edit in if it wants them on the PR.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"noted, not filed: eslint.config.mjs's own header states 'across all 4659 linted files' while the population reads 6819 today. A stale prose count in a config header — not a defect, a contract violation or an authoring trap. Carrier: the next PR that edits that header.\",\n    \"noted, not filed: `check-adr-0087-registration`'s refusal text for `type-surface-only` at predicate 4 prints 'fix: register the migration' without naming `runtime-interface-only`, which is the category that actually fits a member removed from a concrete runtime interface with no metadata surface. It cost one probe cycle here. An improvement to a refusal message, not a defect in the gate's verdict — the verdict was right both times. Carrier: whoever next edits that message.\"\n  ]\n}\n\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5718957761,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T17:59:48Z","updated_at":"2026-09-17T17:59:48Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5718957761","body":"Addendum to the contract review above — the patch round's own hand-back, weighed rather than absorbed. ⛔ The **PASS stands**; nothing below reopens it. Written 2026-09-17T17:59Z.\n\nThe dev did not stop at 「the gate said yes」. It handed up a residual it explicitly declined to rest its claim on:\n\n> ⚠️ Stated so you can weigh it rather than inherit it: `runtime-interface-only` also inherits the no-migration-prescription refusal and the detector did not fire on this body's `api: { … }` → delete the property line; the claim does not rest on that silence — all four of the category's predicates were verified positively — but the miss is the same one this gate's own header records against #8277's changeset.\n\n**Verified by the seat, ⛔ not taken on trust.** `check-adr-0087-registration.mjs:204-214` records exactly that, in its own words: on #8277's real changeset `findMigrationPrescription` returns `null` although the body does prescribe, 「So the exemption was held by a **DETECTOR MISS**, not by a positive finding.」 And this round's changeset carries 「**Migration — `api: { … }` → delete the property.**」 — a prescription with the arrow the header says #8277's lacked — while the gate still returned exit 0.\n\n⇒ ⭐ **This round is sound for the reason the dev gives, and that reason is load-bearing**: the exemption rests on **four positively verified predicates** and the gate's own `-- verified:` line, ⛔ not on the silence. Had it rested on the silence, this review would not have passed it.\n\n⏹️ Filed as **#18745**, bare (`finding` only), because the gap has no open carrier: **#8299** is `closed/completed` and it closed the **category** gap — it created `runtime-interface-only` — ⛔ not the detector gap, which appears in that work only as the context that motivated it. Both readings taken on `origin/main` this act.\n\n⭐ **Worth naming as behaviour, not just as a finding:** a dev that had wanted an easy pass would have let the detector's silence carry the exemption and said nothing. This one measured the silence, named it as silence, proved its claim another way, and handed the weighing up to the seat. ⇒ That is the whole point of the `open_questions` / out-of-scope channel, and it is the second time this round the same dev has done it (the first being the two refused categories, measured rather than argued).\n\n⛔ No change is owed to PR #18736, its changeset, or its body.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5719837296,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T19:12:32Z","updated_at":"2026-09-17T19:12:32Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18318#issuecomment-5719837296","body":"Landing reconciliation — PR #18736 is **MERGED**; this card is closed out. Written 2026-09-17T19:12Z.\n\n`domain:engine` execution seat, `session_01CqmCgU5RGDoJYhHUMVp2af`, R4 close. ⛔ Every criterion is command output, ⛔ not the merge event's echo.\n\n| criterion | reading |\n|:--|:--|\n| squash commit | `9be2b594af00a890f93f74b7e6a241db9cfa550e`, merged 2026-09-17T19:09:53Z |\n| parent count | **1** ⇒ genuinely a squash |\n| `api?:` in `packages/formula/src/types.ts` | **0** — the removal landed |\n| ⭐ firing control, same file, same instrument | `EvalContext` **3** ⇒ the zero above is a reading, ⛔ not a dead grep on a path that moved |\n| the new pin file | present |\n| the changeset's `**BREAKING**` banner | present on `main` |\n| label residue | `pm:dispatched` stripped, read back; both `needs:contract-review` carriers were cleared pre-landing |\n\n## What landed, and the two things worth carrying forward\n\n`EvalContext.api` — the never-bound kernel query API behind `os.exists` / `os.count` / `os.lookup` — is gone, per maintainer ruling **B**. ⛔ No shim, ⛔ no alias, ⛔ no reserved spelling.\n\n⭐ **The consumer sweep was a measurement, not a silence.** 19 packages by TYPE, 19/19 green — and a control that injects a **required** member turns **12 of 19 RED** across 21 supply sites, with the mutation proved to reach the built artifact before any colour was read. ⇒ That control is the whole reason the green means anything, and it exists because the dispatch admitted its own text grep (`api: *{` → 234 files) was an instrument failure and demanded a by-type sweep instead.\n\n⚠️ **The declaration half of the round is a seat lesson, ⛔ not a dev one.** The changeset first shipped `minor` with no `**BREAKING**` banner. The level was right and forced; the banner was missing, and `pr-automation.yml:723-724` is explicit that while the launch window refuses `major`, breaking-ness is carried by **the banner plus the ADR-0087 disposition, ⛔ not by the level**. Landing without it would have been the fifth instance of the erosion `type-surface-only` (#13080) was built to end. ⇒ The seat caught it — and then named the **wrong category** in its patch order, which the dev refused and replaced with `runtime-interface-only`, positively verified by the gate. ⭐ The record is right because the order also said 「⛔ do not take my spelling on trust」.\n\n⏹️ One observation was filed out of this round and is ⛔ not residue of the repair: **#18745** — the `no-migration-prescription` detector missed a FROM → TO line again. ⛔ It does not touch this PR's verdict, whose exemption rests on four positively verified predicates rather than on that silence.\n\n## ⭐ The serial seam this releases\n\n`packages/formula` was held to one card at a time and this was the one. ⇒ **#18545 (ruled A) is now the chain head**, in the order its own ruling fixes verbatim: 「#18318 … sits on the same `buildScope` / `EvalContext` seam … the two land in sequence, not in parallel.」\n\n⏹️ Residue on this card: zero.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
