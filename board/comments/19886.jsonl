{"id":5797738851,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T15:32:38Z","updated_at":"2026-09-23T15:32:38Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19886#issuecomment-5797738851","body":"## 进决策箱 `needs-user-decision` · `priority:p2` · `domain:spec` —— 「不等于 + 数组」四个后端四种答案;乙 的字面只管「等于」,延伸到 `$ne` 是再收窄一个已发布的接受集\n\nPath: api-backend\n\nTriage: lands in `packages/spec/src/data/filter-comparand-shape.ts` (the shared face; `FieldOperatorsSchema.$ne` is `z.any()` at `filter.zod.ts:269`) ⇒ `domain:spec`; rationale: `{ f: { $ne: [...] } }` is refused by driver-sql / driver-memory, answered by driver-mongodb under its own array rule, and matches EVERY row on `@objectstack/formula` (`matches-filter.ts:225`, `!looseEq(actual, array)`), while both shared faces pass it; ruling 乙 (`5793368540`) settled the equality slot only, and extending the refusal narrows a published accept set ⇒ the maintainer's call; p2, with a stated escalation criterion (below).\n\n分诊席(`session_01Tw7jnJinGHvoGSi8aFkhPJ`),2026-09-23T15:31Z。本席读完了卡面(本卡尚无评论),并读了 #19757 的裁决 `5793368540` 与实现报告 `5796984894`。\n\n## 维护者速读\n\n筛选条件里写「**不等于**」再给一个**数组**,今天四种后端四种答案:SQL 和内存驱动拒绝;MongoDB 按它自己的规则答;公式引擎**每一行都算匹配**;两道共享检查都放行。您上午裁的 乙(批 #217 item 3)只管「**等于**」这一格;「不等于」是它的反面,实现轮按裁决字面**有意没碰**。\n\n**要您定的一件事:「不等于」这一格要不要照 乙 一样,在共享层一律拒?**\n- **A**:照 乙 拒,提示改用列表的「不在其中」写法(具体拼写由开发从 `FieldOperatorsSchema` 读出,⛔ 不在这里发明);\n- **B**:维持现状,只写进文档。\n\n本席推荐 **A**。\n\n## 四棱\n\n- **实际业务需求**:⛔ 没测到有人写这个形状。托管产品跑在 SQL 家族上,那里今天就是 400;可能依赖它的只有自托管的 MongoDB 用户,这个人群与 乙 一样没有测。\n- **项目长远合理性**:与 乙 同一个原则,「一个声明的契约,一个答案」。`NE_DESCRIPTION` 本身写的就是「比较值是字面量或 `{ $field }` 引用」,数组两者都不是。视图筛选规则那一道门(PR #19514)已经在编辑时拒绝「不等于 + 数组」。\n- **防 AI 写错**:指向 A。今天 AI 写出来会被放行,然后换一个后端就换一种答案。\n- **创业阶段不扩散**:A 是收窄,阶段姿态默认从紧;⛔ 不设宽限期,与 乙 一致。\n\n## ⚠️ 升级判据(裁决前先测)\n\n安全插件的写入检查,用的正是公式引擎的 `matchesFilterCondition`(`plugin-security/src/security-plugin.ts:2981` 起)。⇒ **如果**一条权限策略或共享规则的条件能写成 `$ne: [数组]`,写入检查会对每一行都放行。\n- 这一点本席**没有测**:还不知道策略的书写入口收不收这个形状。\n- 能 ⇒ 这是权限失效,本卡升 `p1` + `security`,而且拒绝不必等裁决(安全底线);\n- 不能 ⇒ 维持 `p2`。\n- 执行时排在 PR #19882 之后(同一个 `filter-comparand-shape.ts`)。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5805254639,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T00:30:28Z","updated_at":"2026-09-24T00:30:28Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19886#issuecomment-5805254639","body":"Ruling: summon #28 (续) class-1 item 4 · letter A · maintainer 「—」 (class-1 self-adjudication under the charter's 一类自裁门; listed for 追认 in the summon #28 summary on objectstack-ai/objectstack#12708) 2026-09-24T00:30Z\n\nDirector seat, summon #28 (续) (`session_01GLdRPcbaCBQCTvVmU6YEUY`). Thread re-read to its last comment (5797738851) in the act that wrote this.\n\n## Why this card qualifies for class-1 (three criteria, each with its authority)\n\n1. **The direction is decided by standing text.** `NE_DESCRIPTION` (`packages/spec/src/data/filter.zod.ts:253-255`, a published describe): 「the comparand is a literal, or a { $field } reference to another column of the same table」 — an array is neither, so refusing it PULLS BACK to the declared contract, ⛔ not a new narrowing. Ruling 乙 (5793368540) settled the same one-contract-several-answers shape one slot over; `ViewFilterRuleSchema` already refuses `not_equals` with an array at authoring (PR #19514); 元判据 ① (a silent sibling re-uses the parent ruling).\n2. **The failure direction is loud, and one act reverses it.** The same shared-face refusal shape as 乙.\n3. **No floor moves.** The declared contract already excludes arrays. The security reading below can only RAISE the grade; it does not change the direction.\n\n## Ruled — A: `$ne` with an array comparand is refused at the shared face and at the schema door\n\n1. The shared comparand-shape face refuses an array under `$ne` for every driver, and `FieldOperatorsSchema.$ne` (`filter.zod.ts:269`) refuses it at parse — one remedy text, naming the declared list-negation operator by its spec spelling (the dev reads it off `FieldOperatorsSchema`; ⛔ not invented here). ⛔ No alias, ⛔ no window.\n2. **First act, measured before the fix:** whether a permission-policy or sharing-rule condition can carry `$ne: [array]` into `plugin-security`'s write check (`security-plugin.ts:2981` region, evaluated through `matchesFilterCondition`, where an array matches EVERY row). Reachable ⇒ the seat raises this card to `priority:p1` + `security` and the same refusal lands first, ⛔ waiting on nothing; not reachable ⇒ p2 as graded. Either way the fix is this ruling.\n3. ADR-0087 semantic entry; pins: `$ne` arrays refused at the compile face and the schema door, scalars and `{ $field }` still pass; live `mongod` unmeasured, mingo named as the proxy.\n\n## Execution\n\n`needs-user-decision` → `pm:queue` in this stroke; `domain:spec` · `priority:p2` (item 2 may raise it) · `Clause-②: no`; serial after PR #19882 (same file).\n"}
{"id":5805632547,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T01:09:04Z","updated_at":"2026-09-24T01:09:04Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19886#issuecomment-5805632547","body":"Claim: seat 5 serial dispatch, one dev at a time. Stage 1 only: the ruling's first act, the measurement.\nSession: `session_01Sfe5YjBLwB9J3y8fvm2xq1`\nBranch: `claude/issue-19886-ne-array-refused`\nDomain: `domain:spec`\nSeat: `domain:spec#5` (seat post #19357)\nRuling executed: `5805254639` (A), item 2 first.\nStage 1 (now): measure whether a permission-policy, sharing-rule or row-level CHECK condition can carry `$ne: [array]` into `plugin-security`'s write check (`security-plugin.ts:2981`, `matchesFilterCondition`). No file surface: scratch probes only, nothing committed. The dev returns the measurement to this seat, and the seat sets the grade on this card.\nStage 2 (the fix): the shared face `filter-comparand-shape.ts`, the enforced `FieldOperatorsSchema.$ne` (`filter.zod.ts:1420`; `:269` is the documentation copy `EqualityOperatorSchema`), the conformance table, one ADR-0087 entry with `registry.ts` regenerated, and a changeset.\n- Reachable ⇒ stage 2 starts at once, ahead of PR #19882, as item 2 says.\n- Not reachable ⇒ stage 2 waits for PR #19882 to merge, as the Execution line says.\nContainer & model: `mode:subagent`, default tier. `dispatch-gates.mjs --tier` on the three paths printed \"Model tier — no path-derived mandate\". The review is owed at tier: the change moves an accept set.\nClause-②: no\nThread-read: 5805254639\nSerial constraints cleared at 2026-09-24T01:08Z: stage 1 writes no file. Stage 2: PR #19882 (seat 1, draft) holds `filter-comparand-shape.ts`, its test and the conformance files. `filter.zod.ts` is in no open PR of the 37. `registry.ts` is regenerated, never hand-merged.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5805865119,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T01:33:59Z","updated_at":"2026-09-24T01:33:59Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19886#issuecomment-5805865119","body":"os-dev-report\n{\n  \"issue\": 19886,\n  \"status\": \"measured\",\n  \"stage\": \"Stage 1 only: the measurement in ruling 5805254639 (A), item 2. No branch was pushed, nothing was committed, no PR was opened and nothing was fixed.\",\n  \"branch\": null,\n  \"pr\": null,\n  \"session\": \"session_01Sfe5YjBLwB9J3y8fvm2xq1. This is the dispatching seat's session, because this run is its subagent.\",\n  \"tree\": \"objectstack-ai/objectstack @ fdeeea0cc9. That was origin/main at dispatch. It was re-fetched before posting and had not moved. Every reading below was taken on this tree.\",\n  \"premise_still_valid\": true,\n  \"verdict\": \"REACHABLE. An authored row-level CHECK carries $ne with an array into plugin-security's write check. The check then admits the insert and the update that its author wrote it to stop. The write lands and is stored.\",\n  \"summary\": \"Measured through the real SecurityPlugin and a real ObjectQL engine on three drivers: driver-sql (better-sqlite3), driver-sqlite-wasm and driver-memory. A permission set's rowLevelSecurity[].check written as `record.F != LIST` (a CEL literal list, a resolver-staged current_user array key, or the kernel-resolved current_user.org_user_ids) lowers to { F: { $ne: [array] } }. Nothing between authoring and checkParts refuses it or rewrites it. matchesFilterCondition answers true for every post-image, so on all three drivers the forbidden insert and the forbidden update were admitted and stored (18 of 18 probe writes). The scalar control refused the same writes with 403 PERMISSION_DENIED (18 of 18). The delegator leg (delCheckFilter, ADR-0090 D10) is reachable the same way. tenantParts is not reachable: its filter is platform-derived and has no authored input. The read side refuses the shape: driver-sql, driver-sqlite-wasm and driver-memory answer 400 INVALID_FILTER. When a policy carries the shape in both using and check, the read answers 400 while the INSERT still passes the write CHECK. Sharing-rule criteria never reach matchesFilterCondition: they go to a driver query, which refuses them, so the rule grants nothing. A tenant admin cannot author the reachable shape at runtime with the default sets. It is authored in package metadata or by a platform-tier admin, and every save door admits it. Stage-2 scope finding: the two refusal sites the ruling names (the shared comparand-shape face and FieldOperatorsSchema.$ne) are not on the reachable path. A simulated face refusal left the forbidden insert admitted (section S).\",\n  \"paths\": [\n    {\n      \"id\": \"P1 caller CHECK (checkParts[0])\",\n      \"entry\": \"permission set rowLevelSecurity[].check. The policy must declare check: using-only policies never enter checkParts.\",\n      \"authoring_schema\": \"PermissionSetSchema.rowLevelSecurity = z.array(RowLevelSecurityPolicySchema). check is z.string() (packages/spec/src/security/rls.zod.ts).\",\n      \"transforms\": \"The save door (Zod safeParse, then authoring gates: the packaged-permission-set lock only). Then sys_metadata. Then metadata list('permission') and PermissionEvaluator.resolvePermissionSets. Then collectRLSPolicies: platform tenant-policy strip, enabled, object match, positions domain, operation. Then policyDeclaresClause(p,'check') (security-plugin.ts:6493). Then stageRlsMembership (resolver keys). Then RLSCompiler.compileFilter(..,'check', fieldGuard): reserved-key screen, then compileExpressionOutcome = sqlPredicateToCel bridge + compileCelToFilter, whose emit() writes { F: { $ne: value } } with no array test (cel-to-filter.ts:429), then isEmptyMembershipFilter, then judgeCompiledFields (field existence), then an OR-combine. The result is checkParts. Then satisfiesCheck, then matchesFilterCondition: assertFilterShape refuses only zero-operator constraints, and evalOp $ne is !looseEq(actual, array) = true (matches-filter.ts:225). Insert runs it through the engine seam OperationContext.postHookWriteImageCheck after the beforeInsert hooks. Update runs it in the middleware over the merged pre-image.\",\n      \"section_A_transform_readings\": \"PermissionSetSchema admits 3 of 3 probe variants. isSupportedRlsExpression is true for 3 of 3. compileCelToFilter gives { status: { $ne: ['closed','archived'] } }, { account: { $ne: ['acc_blocked','acc_banned'] } } and { reviewer_id: { $ne: ['usr_admin_a','usr_peer'] } }. RLSCompiler.compileFilter with the field guard returns them unchanged. matchesFilterCondition on the forbidden row: probe true 3 of 3, control false 3 of 3.\",\n      \"verdict\": \"REACHABLE on insert and on update, on all three drivers. The check is evaluated in-process, so the verdict does not depend on the driver.\",\n      \"control\": \"check `record.status != 'closed'`, `record.account != 'acc_blocked'` and `record.reviewer_id != 'usr_admin_a'`. The forbidden insert got REFUSED 403 PERMISSION_DENIED `the insert would violate a row-level CHECK on 'qa_ticket'` 9 of 9. The update to the forbidden value got REFUSED 403 `the update would violate a row-level CHECK` 9 of 9. The allowed insert was admitted 9 of 9.\",\n      \"probe\": \"check `record.status != ['closed', 'archived']`, `record.account != current_user.blocked_accounts` (resolver-staged) and `record.reviewer_id != current_user.org_user_ids` (kernel key). The forbidden insert was ADMITTED 9 of 9 and stored (for example ins_bad status=closed). The update to the forbidden value was ADMITTED 9 of 9 and stored (for example upd_1 status=closed, account=acc_blocked, reviewer_id=usr_admin_a).\",\n      \"who_can_author\": \"(1) Package metadata: a stack's permissions[].rowLevelSecurity[].check, or a *.permission.ts file. PermissionSetSchema admits it at parse. The CLI authoring lint validateRlsPredicateEnforceability (gating; os validate / lint / build) reports nothing for the literal list, nothing for current_user.org_user_ids and nothing for current_user.positions. It reports the resolver-key variant as rls-predicate-unknown-user-variable (error), with a consequence text that is the wrong way round (see out_of_scope_findings). (2) The runtime metadata door saveMetaItem('permission') (Studio / REST /meta / MCP) admits it, measured with the lock gate wired, hatch closed and hatch open: 1 row landed and the check text was stored verbatim. That door runs no RLS predicate lint (authoring-rules.ts marks the rule surfaces: CLI_ONLY). By reading, not run: REST PUT /meta is gated on manage_metadata, which sits in PLATFORM_ADMIN_ONLY_CAPABILITIES. The default organization_admin set denies writes to sys_permission_set, and the Setup data door on sys_permission_set.row_level_security redirects into the same save. So at runtime the author is a platform-tier admin, or an app-defined set granting sys_permission_set edit. A default tenant admin cannot author it (reading; NOT MEASURED through REST or the data door).\",\n      \"schema_admits_at_save\": \"yes. Every door measured admits it: PermissionSetSchema parse, saveMetaItem with the hatch closed and with it open, and the CLI lint for 2 of the 3 variants.\"\n    },\n    {\n      \"id\": \"P2 delegator CHECK (checkParts[1], ADR-0090 D10 on-behalf-of)\",\n      \"entry\": \"the delegator's permission sets, rowLevelSecurity[].check\",\n      \"authoring_schema\": \"same as P1\",\n      \"transforms\": \"resolveDelegatorContext, then buildContextForUser, then resolvePermissionSetsForContext(delegator), then computeWriteCheckFilter. After that it is identical to P1 and is AND-ed into checkParts.\",\n      \"verdict\": \"REACHABLE (driver-sql). The agent's own leg had a null check filter, so the delegator leg alone decided.\",\n      \"control\": \"Agent (principalKind agent, own set qa_agent_set with no RLS) acting on behalf of usr_deleg, whose baseline set carries check `record.status != 'closed'`. The forbidden insert got REFUSED 403 `the insert would violate a row-level CHECK`.\",\n      \"probe\": \"The same set up with `record.status != ['closed', 'archived']`. The forbidden insert was ADMITTED and stored (d_bad status=closed).\",\n      \"who_can_author\": \"same as P1: these are the same permission-set artifacts\",\n      \"schema_admits_at_save\": \"yes (same doors as P1)\"\n    },\n    {\n      \"id\": \"P3 tenant post-image check (tenantParts, security-plugin.ts:3204)\",\n      \"entry\": \"none authored\",\n      \"authoring_schema\": \"none. computeWriteTenantCheckFilter returns computeLayeredRlsFilter().layer0, which is tenantLayer0FilterOf(verdict): null, { organization_id }, { organization_id: { $in } } or the deny sentinel. It is platform-derived.\",\n      \"transforms\": \"n/a\",\n      \"verdict\": \"NOT REACHABLE. There is no authored input, and the authored $ne array does not touch it.\",\n      \"control\": \"Org scoping isolated, caller in org_acme, authored check `record.organization_id != 'org_globex'`. layer0 = { organization_id: 'org_acme' }. The forged-org insert got REFUSED 403 `would place 'qa_ticket' in another tenant`.\",\n      \"probe\": \"Authored check `record.organization_id != ['org_globex', 'org_initech']` compiled to { organization_id: { $ne: [..] } }, yet layer0 was still { organization_id: 'org_acme' }. The forged-org insert still got REFUSED 403 by the tenant wall.\",\n      \"who_can_author\": \"n/a\",\n      \"schema_admits_at_save\": \"n/a\"\n    },\n    {\n      \"id\": \"P4 explain engine record attribution (explain-engine.ts:854, security service explain)\",\n      \"entry\": \"the same permission-set rowLevelSecurity[].using, via computeLayeredRlsFilter().layer1\",\n      \"authoring_schema\": \"same as P1 (using)\",\n      \"transforms\": \"compileFilter(using), then matchesFilterCondition(record, layer1) in applyRecordAttribution\",\n      \"verdict\": \"REACHABLE as a wrong diagnostic, not as a write. explain reports access that enforcement does not give.\",\n      \"control\": \"using `record.status != 'closed'` on r_closed: record.visible false, rls outcome excluded (3 of 3 drivers)\",\n      \"probe\": \"using `record.status != ['closed', 'archived']` on r_closed: record.visible TRUE, rls outcome admitted, matchesRecord true (3 of 3 drivers). The enforcing read of the same row answered 400 INVALID_FILTER.\",\n      \"who_can_author\": \"same as P1. The report is read by the caller for themself, and by manage_users or a delegated adminScope for others.\",\n      \"schema_admits_at_save\": \"yes\"\n    },\n    {\n      \"id\": \"P5 RLS using: the read and the update/delete pre-image gate (driver path, not matchesFilterCondition)\",\n      \"entry\": \"permission set rowLevelSecurity[].using\",\n      \"authoring_schema\": \"same as P1 (using: z.string())\",\n      \"transforms\": \"compileFilter(using) builds layer1, which is AND-ed into the query where and sent to the driver. The engine's comparand-shape seam does not re-walk the RLS-composed where (section S).\",\n      \"verdict\": \"REFUSED by the driver, and fail-closed for writes. It never reaches checkParts.\",\n      \"control\": \"The read returns [r_open]. The update of r_closed got 403 (row-level security).\",\n      \"probe\": \"read / findOne: 400 INVALID_FILTER on driver-sql and driver-sqlite-wasm (`A comparison in this filter requires a single comparable value ... for a list use $in/$nin`) and on driver-memory (`Operator \\\"$ne\\\" on field \\\"status\\\" requires a single comparable value, but received an array`). Update by id: REFUSED 403 PERMISSION_DENIED `not permitted to update this 'qa_ticket' record (row-level security)`, and harmless-field updates are refused too (availability loss).\",\n      \"who_can_author\": \"same as P1\",\n      \"schema_admits_at_save\": \"yes\"\n    },\n    {\n      \"id\": \"P6 sharing rule criteria (plugin-sharing; driver path, not matchesFilterCondition)\",\n      \"entry\": \"package sharingRules[].condition (CEL), or runtime POST /sharing/rules / SharingRuleService.defineRule criteria (a raw FilterCondition), or the Setup insert on sys_sharing_rule\",\n      \"authoring_schema\": \"SharingRuleSchema (CriteriaSharingRuleSchema.condition = EvaluatedExpressionInputSchema). defineRule runs no spec schema, and bindRuleCriteriaGuard refuses match-all criteria only.\",\n      \"transforms\": \"bootstrap-declared-sharing-rules compileCelToFilter(condition, { variables: {} }), then criteria_json, then SharingRuleService.matchRecord / evaluateRule, then engine.find(object, { filter: criteria }) against the driver\",\n      \"verdict\": \"It does not reach plugin-security's write check. On driver-sql, driver-sqlite-wasm and driver-memory the criteria query is REFUSED (400, swallowed): 0 matched, 0 grants. That is fail-closed, not a widening. driver-mongodb is NOT MEASURED.\",\n      \"control\": \"criteria { status: { $ne: 'closed' } } on a private object: evaluateRule gave 1 grant (r_open, usr_b, edit), 3 of 3 drivers\",\n      \"probe\": \"criteria { status: { $ne: ['closed','archived'] } }: defineRule was admitted 3 of 3. evaluateRule gave matchedRecords 0 and grants [] 3 of 3, with WARN `[sharing-rule] criteria query failed` carrying the driver's 400 text. SharingRuleSchema admits condition `record.status != ['closed', 'archived']`, and the sharing-rule lint reports nothing.\",\n      \"who_can_author\": \"manage_sharing or manage_platform_settings holders (defineRule), or package metadata\",\n      \"schema_admits_at_save\": \"yes\"\n    }\n  ],\n  \"read_side\": \"The read side refuses the shape: 400 INVALID_FILTER on driver-sql (better-sqlite3), on driver-sqlite-wasm and on driver-memory, for find and findOne. With using+check twins (section F, 3 drivers), the read answers 400, the update is refused 403 at the pre-image gate (harmless edits too), and the INSERT of status=closed is ADMITTED and stored. That is exactly the case the dispatch asked about: the SQL family 400s on the read while the write CHECK passes. driver-mongodb is NOT MEASURED for the read (no live mongod here; the card names mingo as the proxy).\",\n  \"stage2_scope\": \"Measured (section S): the refusal sites the ruling names are not on the reachable path. The engine's comparand-shape seam (packages/objectql/src/filter-comparand-shape.ts, which delegates to the spec face) was mutated through scripts/ablation-replace.mjs in WRAP mode to refuse any $ne array. The blob went from 3b09a0ee563a to c430115dca0f, and the anchor count went 1 to 0. The mutation was live: a direct engine where { status: { $ne: ['closed'] } } answered 400 INVALID_FILTER SIMULATED_STAGE2_NE_ARRAY_REFUSAL (pristine: the driver's 400). Under the same mutation, the check-probe forbidden insert was still ADMITTED and stored. The using-probe caller read was refused by the DRIVER, not by the seam, so the RLS-composed where bypasses that seam too. Restore was proven: blob == HEAD 3b09a0ee563a and `git diff HEAD` empty. FieldOperatorsSchema never parses a CEL-authored RLS predicate. The write CHECK filter comes from compileCelToFilter (cel-to-filter.ts emit) and is evaluated by matchesFilterCondition (matches-filter.ts); neither consults either named site. So closing the reachable path needs a refusal on that path (see open_questions).\",\n  \"seat_facts_rechecked\": \"All confirmed on fdeeea0cc9. security-plugin.ts:2980-2981 is satisfiesCheck over checkParts. :3204 is the tenantParts site. matches-filter.ts:225 is the $ne arm. filter.zod.ts:1414 is FieldOperatorsSchema, and its :1420 $ne is z.any(). :269 is the $ne of EqualityOperatorSchema (the documentation copy, starting at :264).\",\n  \"tests\": \"Scratch vitest files, uncommitted and now deleted, run through bash scripts/pm/os-verify-lock.sh (slot dev-19886): `pnpm --filter @objectstack/plugin-security exec vitest run --maxWorkers=2 src/zz-scratch-19886-ne-array.test.ts` gave VERDICT command-exit 0, 34 passed (sections A, B, C, D, E). `... src/zz-scratch-19886-doors.test.ts` gave command-exit 0, 18 passed (F, G, J0, J). `pnpm --filter @objectstack/lint exec vitest run --maxWorkers=2 src/zz-scratch-19886-lint.test.ts` gave 8 passed (H, H2). `... src/zz-scratch-19886-sim.test.ts` gave 2 passed pristine, then 2 passed under the wrapped mutation (S). `... src/zz-scratch-19886-eq.test.ts` gave command-exit 0, 4 passed (EQ). The probes record outcomes and do not assert them; the readings are quoted in paths[]. Before the runs: `pnpm turbo run build --filter='@objectstack/plugin-security^...' --concurrency=2` gave 17 of 17 tasks successful (VERDICT command-exit 0). objectql, driver-sql, driver-sqlite-wasm, types and metadata-protocol are vitest-aliased to src. formula and spec were read from that fresh dist. driver-memory was imported from src by relative path. First-run note: the first B run refused every insert with `VALIDATION_FAILED: Owner: no sys_user record` because the scratch declared sys_user without seeding the caller. It was re-run with the principals seeded, and the numbers above come from that re-run. The first mutation attempt was a NO-OP: ablation-replace refused because the replacement contained the anchor, then restored, with blob == HEAD. It was re-run with a corrected anchor.\",\n  \"mcp_calls\": \"0\",\n  \"api_writes\": \"1: this os-dev-report comment, POST /repos/objectstack-ai/objectstack/issues/19886/comments via scripts/pm/post-stamped.mjs on the fleet-write relay (transport dispatch). Reads were REST GET only (the issue, its comments, one prior report comment, rate_limit, and the relay route probe).\",\n  \"open_questions\": [\n    {\n      \"question\": \"Stage 2 scope. Ruling A item 1 names the shared comparand-shape face and FieldOperatorsSchema.$ne. Section S measured that neither is on the reachable path: the write CHECK compiles CEL through compileCelToFilter and evaluates it with matchesFilterCondition in-process. Which site or sites carry the refusal that closes P1, P2 and P4?\",\n      \"options\": [\n        \"A: refuse at the CEL lowering. compileCelToFilter emit() refuses `!=` whose resolved comparand is an array, with a CompileError. The policy is dropped, compileFilter returns RLS_DENY_FILTER, the write is refused (fail-closed) and a WARN is logged. The CLI lint, which calls the same compiler, turns the literal-list case red at authoring. Cost: a formula change plus pins. A resolved-variable array is only known at request time, so its authoring report depends on the lint's probe binding arrays.\",\n        \"B: refuse in matchesFilterCondition's up-front assertFilterShape, with $ne array giving INVALID_FILTER / 400 for every record. This closes every in-process consumer at once (write CHECK, explain, any future producer). Cost: the write gate would answer with a thrown 400 instead of the 403 CHECK envelope (the propagation path is NOT MEASURED), and the policy is not dropped by the RLS vocabulary.\",\n        \"C: both A and B, plus the ruling's two named sites for the query path.\"\n      ],\n      \"recommendation\": \"C, if the seat reads it as inside ruling A's letter. A is the authoring-visible refusal for CEL-authored predicates, and B is the evaluator floor for any other producer. Whether adding sites beyond the two the ruling names needs the director's word is the seat's call, not this run's.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"class: a · The equality slot reaches the write CHECK under negation. check `!(record.status == ['closed', 'archived'])` lowers to $not { status: [..] }. matchesFilterCondition evalField answers false for a bare array (matches-filter.ts:198), $not inverts that, and every write is ADMITTED. Measured on driver-sql at fdeeea0cc9: the insert of status=closed was admitted and stored. Controls: the positive `record.status == ['open', 'pending']` refused every insert (fail-closed), and the correct spelling `!(record.status in ['closed', 'archived'])` refused closed and admitted open. Section S shows the shared face PR #19882 extends is not on this path. Placement: the #19757 slot (ruling 乙). Name it as a sub-issue of #19757, or as a rider on this card's stage 2 at the same formula site; the seat decides. Dedupe words: `equality array comparand write check negation` · `not equals list rls check admits` · `matchesFilterCondition bare array $not`\",\n    \"class: b · The published RowLevelSecurityPolicySchema.check describe reads: 「Validation condition for INSERT/UPDATE (defaults to USING clause if not specified - enforced at application level)」. Its TSDoc says: 「If not specified, implementations should use the USING clause as the CHECK clause」. The runtime scopes the post-image check to policies that declare check (security-plugin.ts:6493), so a using-only policy never gates an insert. Measured on driver-sql at fdeeea0cc9: using-only `record.status != 'closed'`, and the insert of status=closed was ADMITTED and stored. Seam: spec:RowLevelSecurityPolicySchema.check (describe) → runtime:security-plugin.ts computeWriteCheckFilter (policyDeclaresClause filter). The comment in RLSCompiler.compileFilter that says 'defaulting to using when omitted' is unreachable from that caller. Dedupe words: `rls check defaults to using` · `using-only policy insert not checked` · `computeWriteCheckFilter policyDeclaresClause`\",\n    \"carrier: #19886 stage 2 · noted, not filed. The CLI lint rls-predicate-unknown-user-variable, for `record.account != current_user.blocked_accounts` (check), tells the author the check is 'a blanket refusal', and its hint says membership keys are arrays so they 'can only be tested with in'. The runtime measured in P1 ADMITS every write when a resolver stages that key. The lint also reports nothing for `!=` against the kernel array keys (org_user_ids, positions) or against a literal list. If stage 2 refuses at the lowering (option A), the text becomes true; otherwise it stays inverted.\",\n    \"carrier: #19886 stage 2 · noted, not filed. P4: explain answers visible true / admitted for a row that the using policy excludes and whose enforcing read answers 400. This is the same evaluator, and it is closed by option B.\",\n    \"carrier: none · noted, not filed. Observation: the sharing-rule per-record match (evaluateAllForRecord, non-field recipient) swallows the driver's 400 with no WARN. The whole-rule sweep does WARN. This is fail-closed by design.\"\n  ],\n  \"deviations\": [\n    \"The dev contract's worktree rule says to push the claimed branch empty as the write-route probe first. The dispatch says the branch is for stage 2 only and that stage 1 writes no file. I followed the dispatch: no branch was created or pushed, and the worktree was a detached checkout of fdeeea0cc9. The write route was read instead with scripts/pm/fleet-write/dispatch.mjs --route, which returned transport dispatch. Named here, not chosen silently.\",\n    \"One temporary source mutation was made (section S, packages/objectql/src/filter-comparand-shape.ts). It went through ablation-replace in WRAP mode and was restored to the HEAD blob, which I checked by hash and by an empty `git diff HEAD`. It was a measurement, not a fix, and it never left the working tree.\"\n  ],\n  \"cleanup\": \"Before this post, the 5 scratch test files (4 in plugin-security src and 1 in lint src) were deleted, and git status --porcelain in the worktree printed 0 lines at HEAD fdeeea0cc9. This comment is posted from that worktree. Right after it, the worktree's node_modules is removed and git worktree remove /home/user/objectstack-issue-19886 runs without --force; the outcome is reported in the final hand-off. No dev server was started and no background process or monitor was left. The scratch outputs remain only in the session scratchpad (issue-19886/).\"\n}\n"}
{"id":5805921577,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T01:40:33Z","updated_at":"2026-09-24T01:40:33Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19886#issuecomment-5805921577","body":"## 升 `priority:p1` + `security`(实测可达)· 第 2 阶段范围:本席裁定\n\n`domain:spec` seat 5(`session_01Sfe5YjBLwB9J3y8fvm2xq1`),2026-09-24T01:40Z。已读至本卡最新评论 `5805865119`。\n\n### 维护者速读\n\n权限策略的写入检查里,如果把「不在这几个值里」误写成「不等于 [列表]」,本该拦下的写入会**全部放行,并且落库**。\n- 开发在三个驱动上用真实的安全插件实测:18 次写入全部放行;对照组写成单个值,18 次全部拦下。\n- 能写出这种策略的人是包元数据作者或平台级管理员。按阅读判断,默认的租户管理员写不了。\n- 按裁决第 2 项,本卡已升 `p1` + `security`。\n\n### 读数\n\n**开发实测**(报告 `5805865119`,`fdeeea0cc9`,driver-sql / driver-sqlite-wasm / driver-memory):\n- 可达的路径有三条:调用者自己的行级 check(P1)、代办人一侧的 check(P2)、explain 诊断(P4,报「可见」,实际读取却是 400)。\n- 另外三条路径:租户墙(P3)不可达;行级 `using` 的读取(P5)被驱动拒绝,失败即关闭;共享规则(P6)不经过这个求值器。\n\n**本席在 `fdeeea0cc9` 复读的三处代码**:\n- `packages/formula/src/cel-to-filter.ts:429` 把 `!=` 降成 `{ F: { $ne: value } }`,不检查数组;\n- `packages/formula/src/matches-filter.ts:225` 的 `$ne` 分支是 `!looseEq(actual, v)`;\n- `security-plugin.ts:2981` 的写入检查调用的正是这个求值器。\n\n### 第 2 阶段范围(本席裁定,方向不变)\n\n开发的 S 段做了一次变异实验:让共享比较值形状层拒收 `$ne` 数组,违规的插入**仍被放行**。所以裁决第 1 项点名的两个位置(共享层、`FieldOperatorsSchema.$ne`)都不在这条可达路径上。\n\n写入检查走的是 `formula` 求值面,即 `compile-surfaces.md` 编译面清单第 5 面。裁决说的「对每个驱动都拒」本来就包括这一面。`execution-duties.md:137` 也要求改过滤语义时逐面申报。⇒ 本席裁定:\n\n- **2a(立即,安全底线,不等 PR #19882,与它不共用文件):**\n  - 在 `formula` 面拒收 `$ne` 带数组。P1、P2、P4 三条路径都要以失败即关闭收尾,用同样的探针在同样的三个驱动上实测。\n  - CEL 降级那一处要不要也拒(作者在编写时就能看到报错),由开发实测后申报。\n  - 跨车道申报:`packages/formula` 属 `domain:engine`(`SKILL.md:163`)。\n- **附带修复(同一面、同一种放行):** `!(record.F == [列表])` 会降成 `$not { F: [..] }`。`matches-filter.ts:198` 对裸数组答 false,`$not` 一取反,所有写入都放行(开发实测)。这一格属于 #19757 的裁决 乙,而 PR #19882 走不到 `formula` 面。2a 在同一处一并关闭,并在 #19757 上留言知会。\n- **2b(原样):** 裁决第 1 项点名的两个位置,等 PR #19882 合并后再做。\n\n在裁决点名位置之外新增的这一面,列请董事席追认。\n\n### 另立\n\n`RowLevelSecurityPolicySchema.check` 发布的说明是「不写时沿用 USING」,运行时却只检查声明了 `check` 的策略,开发实测只写 `using` 的策略拦不住插入 ⇒ **#19942**(未分诊)。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5805941422,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T01:42:27Z","updated_at":"2026-09-24T01:42:27Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19886#issuecomment-5805941422","body":"Claim addendum: stage 2a, per the seat ruling `5805921577`\nSession: `session_01Sfe5YjBLwB9J3y8fvm2xq1`\nBranch: `claude/issue-19886-formula-face-refusal` (stage 2a; its PR says `Refs #19886`, not `Fixes`). The claimed `claude/issue-19886-ne-array-refused` stays for stage 2b, which closes the card.\nSeat: `domain:spec#5` (seat post #19357)\nFile surface: `packages/formula/src/matches-filter.ts` and its tests; one write-check pin in `packages/plugins/plugin-security` (test only); the ADR-0087 entry with `registry.ts` regenerated, if the repo's rules require one for this refusal; `.changeset/`. **Cross-lane, declared:** `packages/formula` is `domain:engine` (`SKILL.md:163`). ⛔ Not the files of PR #19882. ⛔ Not the `packages/formula` files of PR #19728 (`index.ts`, `validate.ts`, `relationship-traversal.ts`). ⛔ No `cel-to-filter.ts` or lint change in 2a. Stop and report on a breach.\nContainer & model: `mode:subagent`, default tier: the same dev, resumed.\nClause-②: no\nThread-read: 5805921577\nSerial constraints cleared at 2026-09-24T01:42Z: the census of 40 open PRs finds no PR touching `packages/formula/src/matches-filter.ts` or `cel-to-filter.ts`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5806337850,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T02:24:27Z","updated_at":"2026-09-24T02:24:27Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19886#issuecomment-5806337850","body":"os-dev-report\n{\n  \"issue\": 19886,\n  \"status\": \"done\",\n  \"stage\": \"2a (seat ruling 5805921577, claim addendum 5805941422). The PR says Refs #19886, not Fixes: stage 2b closes the card.\",\n  \"branch\": \"claude/issue-19886-formula-face-refusal\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/19946 (draft, opened by objectstack-fleet[bot] through the relay; body read back byte-identical, 4195 bytes)\",\n  \"head_sha\": \"58ce4f1b16\",\n  \"session\": \"session_01Sfe5YjBLwB9J3y8fvm2xq1. This is the dispatching seat's session, because this run is its subagent.\",\n  \"premise_still_valid\": true,\n  \"summary\": \"matchesFilterCondition (packages/formula/src/matches-filter.ts, compile surface 5) now refuses two shapes up front in assertFilterShape: an array comparand under $ne, and an array in the equality slot (a bare-array field spec, or $eq). The refusal happens before any record is judged, at any depth under $and / $or / $not, empty arrays included, and throws INVALID_FILTER / 400. That is the envelope driver-sql (unbindable-comparand refusal) and driver-memory (arrayComparandError) give the same shapes, read off their source and measured below. The message names $in / $nin as the remedy and withholds the field, the operator and the value, following driver-sql's withheld-diagnostic posture, because the callers evaluate access policies and the comparand may be a resolved membership set. $in / $nin, scalars, null, Date and { $field } references are unchanged; the refusal reads the authored comparand, never a resolved one. Every reachable path from stage 1 (P1 insert and update, the P2 delegator leg, P4 explain, and the negation rider) now fails closed on all three drivers with nothing stored. The scalar !=, in and not-in controls are byte-identical. File surface: matches-filter.ts plus 3 formula test files, 1 plugin-security test file and 1 changeset. No cel-to-filter.ts, lint, spec or export change, and none of the files of PR #19882 or PR #19728.\",\n  \"fail_closed_table\": {\n    \"method\": \"The stage-1 probes re-run through the real SecurityPlugin + ObjectQL on driver-sql (better-sqlite3), driver-sqlite-wasm and driver-memory. BEFORE = the same probe file with only the refusal ablated (formula rebuilt, marker proved live in dist/); it reproduced stage 1 exactly. AFTER = head. Scratch probe, uncommitted, now deleted.\",\n    \"rows_before_to_after_identical_on_all_3_drivers\": [\n      \"P1 V-lit check `record.status != ['closed', 'archived']`: forbidden insert ADMITTED to REFUSED 400 INVALID_FILTER; update to forbidden ADMITTED to REFUSED 400 INVALID_FILTER; stored rows went from ins_bad:closed, ins_ok:open, upd_1:closed to upd_1:open (the system seed, unchanged).\",\n      \"P1 V-membership check `record.account != current_user.blocked_accounts`: the same, with stored going from ins_bad:acc_blocked, upd_1:acc_blocked to upd_1:acc_ok only.\",\n      \"P1 V-reserved check `record.reviewer_id != current_user.org_user_ids`: the same, with stored going from ins_bad:usr_admin_a, upd_1:usr_admin_a to upd_1:usr_external only.\",\n      \"Negation rider check `!(record.status == ['closed', 'archived'])`: insert and update ADMITTED to REFUSED 400 INVALID_FILTER, with stored going to upd_1:open only.\",\n      \"For every probe the ALLOWED insert is now refused 400 as well. The policy is refused for every record (fail-closed), as property 1 requires.\",\n      \"P4 explain (explainAccessForCaller, read, recordId r_closed) under `using` probe and rider: {visible: true, rls outcome: admitted} to REFUSED 400 INVALID_FILTER; it no longer answers visible or admitted. The enforcing read was 400 before and after.\",\n      \"P2 delegator (driver-sql; agent qa_agent_set with no RLS, agent's own check filter null; delegator baseline carries the check): probe and rider forbidden insert ADMITTED (d_bad:closed stored) to REFUSED 400 INVALID_FILTER, stored []. The control stayed REFUSED 403 PERMISSION_DENIED.\"\n    ],\n    \"controls_before_to_after_on_all_3_drivers\": [\n      \"scalar != (all three variants): the forbidden insert and update stay REFUSED 403 PERMISSION_DENIED and the allowed insert stays ADMITTED. Unchanged.\",\n      \"in list `record.status in ['open', 'pending']`: forbidden 403 and allowed admitted. Unchanged.\",\n      \"not in list `!(record.status in ['closed', 'archived'])`: forbidden 403 and allowed admitted. Unchanged.\",\n      \"positive `record.status == ['open', 'pending']`: every write REFUSED before and after, with nothing stored. The envelope moves from 403 PERMISSION_DENIED to 400 INVALID_FILTER, because this is the bare-array equality shape property 1 refuses. See open_questions[0].\",\n      \"explain control using `record.status != 'closed'`: {visible false, excluded} is unchanged.\"\n    ],\n    \"tenantParts_P3\": \"Not affected. Its filter is platform-derived ({organization_id}, $in, deny sentinel) and never carries either shape. Stage 1 E measured layer0 = { organization_id: 'org_acme' } with an authored array check present.\"\n  },\n  \"caller_enumeration\": {\n    \"method\": \"Enumerated repo-wide with git grep on matchesFilterCondition (including the `as m` aliases) at head; objectui read-only.\",\n    \"production\": [\n      \"plugin-security security-plugin.ts:2981 satisfiesCheck (the RLS write CHECK; for insert it runs through the objectql engine.ts:11461 postHookWriteImageCheck.evaluate seam). Before: $ne array true (admit), bare/$eq array false (403), $not{bare} true (admit). After: throws 400. No catch in the middleware (lines 2900-3240) or in the engine seam, so the write fails and nothing is stored (measured). Security decision: yes. Throw turned into a pass: no.\",\n      \"plugin-security security-plugin.ts:3204 tenantParts (tenant post-image). Its inputs never carry either shape, so before and after are identical. No catch.\",\n      \"plugin-security explain-engine.ts:854 matches() in applyRecordAttribution. Before: probe gives visible/admitted. After: explain throws 400. explainAccess has no catch around applyRecordAttribution; the catches at explain-engine.ts:1485-1497 wrap computeRlsFilter only. Throw turned into a pass: no.\",\n      \"formula index.ts:96 is the re-export only.\"\n    ],\n    \"test_callers\": [\n      \"plugin-security fake engines and oracles: controlled-by-parent-{chain,sharing,master-widener,detail-write-authority}, no-active-organization-write-refusal, rls-denial-observability, rls-empty-membership-polarity, rls-phantom-column-negation and security-denial-user-copy. They delegate, so their answer on the shapes equals the evaluator's, but their corpora carry neither shape. 121 files / 2294 tests green after, and none catches the throw.\",\n      \"driver-sql sql-driver-cross-field-conformance (117 passed, 2 skipped) and driver-sqlite-wasm sqlite-wasm-cross-field-conformance (117 passed), used as a reference oracle: green after.\",\n      \"platform-objects system-overview-tile-semantics: 18 passed after.\",\n      \"formula's own 34 files: 946 passed after. Two pins asserted the retired bare-array answer false: matches-filter.test.ts now asserts the refusal envelope, and matches-filter-empty-field-constraint.test.ts drops the case from its 'still returns false' group.\",\n      \"qa/dogfood expression-conformance.test.ts, expression-conformance.ledger.ts and authz-conformance.matrix.ts mention it in a regex or ledger text only, with no call. objectui packages/core/src/utils/filter-converter.ts:1157 is a comment only.\"\n    ],\n    \"fail_open_callers\": \"none\"\n  },\n  \"compile_face_table\": \"Declared in the PR body, measured at head, with locations re-verified with the compile-surfaces grep string. The table's line numbers had moved (sql-driver.ts:15311, remote-transport.ts:2632, read-scope-sql.ts:503, filter-normalizer.ts:1508, matches-filter.ts:212, having-filter.ts:292, memory-matcher.ts:361, mongodb-filter.ts:568). Answers on $ne array / bare array / $eq array / $not{bare}: face 1 driver-sql (and sqlite-wasm by inheritance) 400/400/400/400 INVALID_FILTER. Face 2 turso RemoteTransport 400/400/400/400. Face 3 read-scope-sql: answers with the array bound as the parameter / 500 READ_SCOPE_COMPILE_FAILED / answers with the array bound / 500; better-sqlite3 throws a TypeError binding it; PG NOT MEASURED. Face 4 lowerAnalyticsWhere passes all four through. Face 5 formula (this PR) 400/400/400/400, where before it answered true every row / false / false / true every row. The half face, objectql having matchesHaving, answers true every row / false / false / true every row. driver-memory, both the live mingo path and the reference matcher, gives 400 on all four. driver-mongodb translateFilter passes them through; the mingo proxy selects every row / none / none / every row ($not lowers to $nor). A live mongod was NOT MEASURED.\",\n  \"lint_after_fix\": \"No lint change. validate-rls-predicate-enforceability on the check variants at head: `record.status != ['closed', 'archived']` gives 0 findings. `record.account != current_user.blocked_accounts` gives 1 error, rls-predicate-unknown-user-variable (unchanged). `record.reviewer_id != current_user.org_user_ids` gives 0. `!(record.status == ['closed', 'archived'])` gives 0.\",\n  \"pins_and_ablation\": \"Pins: packages/formula/src/matches-filter-array-comparand.test.ts, 32 tests. They cover 3 shapes x 6 positions, empty arrays, every-record refusal (4 records), remedy naming, the withholding check, and 6 neighbour-unchanged pins. packages/plugins/plugin-security/src/rls-check-array-comparand-refusal.test.ts has 2 tests: the forbidden insert under `!= [list]` and under `!(== [list])`, each asserting code INVALID_FILTER, status 400 and nothing stored, through the real plugin, engine and driver-sql. Ablation: the refusal block was replaced via scripts/ablation-replace.mjs in WRAP mode (anchor 1 to 0, blob d6efb91bac0b to 6797eb9dd5f0), formula was rebuilt, and ablation-dist-preflight confirmed the marker in 2 built files. 27 formula tests went from passed to failed. All 8 tests of each of the 3 shapes flipped (6 positions, the empty array, and refused-for-every-record): 24 in all. The other 3 were 'the remedy names the list operators', 'the message withholds the field and the comparand', and matches-filter.test.ts 'bare array value to REFUSED'. The 6 'every neighbouring shape answers exactly as before' pins stayed green, as designed. plugin-security had 2 of 2 failed ('`!=` against a list' and 'a negated `==` against a list'). Restore: blob == HEAD d6efb91bac0b and git diff HEAD empty. formula was rebuilt, preflight --absent found the marker absent from all 6 built files, and the tree was clean. Everything was green again: 61 of 61 formula tests in the two files, and 2 of 2.\",\n  \"tests\": \"At head 58ce4f1b16 (after merging origin/main 24d1a2d945 and rebuilding the plugin-security, lint and formula closure: 17 of 17 tasks), all through os-verify-lock with VERDICT command-exit 0. formula vitest: 34 files, 946 passed. plugin-security vitest: 121 files, 2294 passed. lint vitest: 108 files, 4121 passed. formula typecheck and plugin-security typecheck passed, with test-typecheck ledgers unchanged (formula 3 files / 7 errors / 4 pinned; plugin-security 0). Also green at 6ce380ce92: driver-sql cross-field conformance (117 passed, 2 skipped), sqlite-wasm cross-field conformance (117) and platform-objects tile semantics (18). `node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack` derived 62 families at 58ce4f1b16. All were run with exit codes recorded, and `--ran` gave 62 derived, 59 run, 3 NOT MEASURED, 0 UNRUN. The 3 exited 3, PREREQUISITE NOT MET, because each needs the whole-repo build that CI performs: check:dual-build-cjs-loads, check:i18n and check:type-check-debt. The diff touches no translation, no root-program file and no DEBT-ledger package, which is declared as a narrowing. The live citation gate `node scripts/check-issue-citations.mjs --base origin/main` exited 0: 8 citations judged, 8 resolve.\",\n  \"adr_0087\": \"Not added. Rule read: scripts/check-adr-0087-registration.mjs header ('a changeset this PR newly ADDS that declares a breaking change must carry exactly one ADR-0087 disposition marker') and AGENTS.md Post-Task step 3 (only a declared-breaking changeset states a disposition; Clause-② no is not breaking). Gate verdict at head: 'this PR adds no declared-breaking changeset (1 non-breaking changeset(s) seen)'. The changeset is .changeset/19886-formula-array-comparand-refused.md, '@objectstack/formula': patch, with a Clause-② no line. plugin-security's shipped code is unchanged, and it picks up the new behaviour through the formula dependency.\",\n  \"ci\": \"Read at head 58ce4f1b16: 31 check runs listed; completed/success: 11, completed/skipped: 3, in_progress/None: 17. Failed: none. Required contexts: Lint & Repo Gates: in_progress; TypeScript Type Check: not yet listed; Test Core: not yet listed; Dogfood Regression Gate: not yet listed; Build Core: in_progress; Temporal Conformance (live PG + MySQL): in_progress; Governed Surface Queue Guard: completed/success. Reported as read; per the dev contract no wait for convergence (it belongs to the seat).\",\n  \"mcp_calls\": \"0\",\n  \"api_writes\": \"2 REST writes via the fleet-write relay: pr_create (POST /repos/objectstack-ai/objectstack/pulls, draft forced, run 35946848266), and this os-dev-report comment (POST .../issues/19886/comments via post-stamped). Plus git pushes of the branch, which are not REST. 0 label writes, and no ready, auto-merge or merge.\",\n  \"open_questions\": [\n    {\n      \"question\": \"Property 2 lists the positive `record.status == ['open', 'pending']` among the controls that must stay unchanged. Property 1 requires refusing exactly that shape (a bare-array field spec). The verdict is unchanged: every write is refused and nothing is stored. The envelope moves from 403 PERMISSION_DENIED to 400 INVALID_FILTER. I read 'unchanged' as the verdict and implemented property 1 as written. If the seat meant the envelope, the two properties conflict.\",\n      \"options\": [\n        \"A: accept it as implemented. The positive list is refused with the 400, like every other equality-slot array, which is also the direction of ruling 乙.\",\n        \"B: keep the 403 for the positive form. That needs a polarity-aware refusal that lets a non-negated bare array fall through to evalField's false. It contradicts property 1's 'whatever the record, at any depth', and it re-opens the shape that inverts under $not.\"\n      ],\n      \"recommendation\": \"A, because B cannot hold together with property 1 and adds a polarity rule to a shape walk that is currently polarity-free. The seat decides.\"\n    },\n    {\n      \"question\": \"An RLS `using` read through driver-mongodb is still open on the mingo proxy, and neither 2a nor 2b as scoped closes it. translateFilter passes { f: { $ne: [..] } } through, and mingo selects every scalar row (the rider lowers to $nor and also selects every row). So on a mongodb deployment, a `using` of `record.f != [list]` would widen the read to every row. Stage 1's section S measured that the RLS-composed where does not pass the engine's comparand-shape seam, which is 2b's site. The formula face (2a) is not on the read path. Where should the refusal go?\",\n      \"options\": [\n        \"A: refuse `!=` / `==` with an array comparand at the CEL lowering (cel-to-filter.ts emit). The policy is dropped and fails closed on every consumer, reads and writes, on every driver, and it is visible at authoring through the lint's compile.\",\n        \"B: refuse in driver-mongodb translateFilter (face-local, like driver-sql and driver-memory).\",\n        \"C: both.\"\n      ],\n      \"recommendation\": \"C, if the seat keeps the p1 scope at 'every reachable path'. A closes RLS at one point, and B closes non-RLS mongodb queries. A live mongod is NOT MEASURED. This is a stage-2b / seat question; 2a was fenced away from cel-to-filter.ts.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"class: a · The half face objectql having (matchesHaving, having-filter.ts:292) answers `$ne: [..]` true for every row and `$not { f: [..] }` true for every row, and a bare array / $eq array false. driver-sql / memory / turso refuse the same shapes 400. Measured at 58ce4f1b16 by a direct call. Placement: the card's own multi-face scope, carrier #19886 stage 2b; noted, not filed. Dedupe words: `having $ne array matches every row` · `matchesHaving array comparand` · `half face having equality array`\",\n    \"class: a · The formula ordering operators coerce an array comparand. JS relational comparison turns ['a'] into 'a', so `{ x: { $gt: ['a'] } }` answers as `$gt: 'a'` (true for x=b), and a check comparing record.priority greater-than ['low'] lowers to exactly that (compileCelToFilter ok). driver-sql refuses any array on a single-value comparison with 400. Measured on the formula dist at head. It was deliberately left out of the 2a refusal: the ruling names $ne and the equality slot only. Dedupe words: `ordering operator array comparand coerced` · `$gt array formula matches` · `rls check greater than list`\",\n    \"carrier: #19886 stage 2b · noted, not filed. Face 3 read-scope-sql (compileScopedFilterToSql, analytics RLS read scope) answers $ne / $eq arrays by emitting a comparison with the array bound as one parameter; better-sqlite3 throws a TypeError at bind, and PG is NOT MEASURED. A bare array gets 500 READ_SCOPE_COMPILE_FAILED rather than 400 INVALID_FILTER. Face 4 lowerAnalyticsWhere passes all four shapes through. Both are outside 2a's surface.\",\n    \"carrier: #19886 stage 2b · noted, not filed. The lint's rls-predicate-unknown-user-variable text for `!= current_user.KEY` predicts a blanket refusal with PermissionDeniedError. Since 2a the runtime refuses every write, but with INVALID_FILTER / 400, so the direction now agrees and the envelope does not. The literal list and the kernel array keys still get no lint finding.\"\n  ],\n  \"deviations\": [\n    \"open_questions[0]: I implemented property 1's letter (the bare-array spec refused everywhere) and read property 2's 'unchanged' for the positive list as its verdict. This is named, not chosen silently. Reverting it is one line (the bare-array arm in assertFilterShape).\",\n    \"The PR body's two SQL cells paraphrase the emitted not-equal and equality comparisons in words, to keep angle-bracket characters out of a GitHub body.\"\n  ],\n  \"cleanup\": \"Scratch probes (the property-2 probe file, the compile-face probe file and the lint probe file) were uncommitted and deleted before the gate derivation; copies stay in the session scratchpad (issue-19886/s2a/). git status --porcelain printed 0 lines at 58ce4f1b16. Both source mutations were wrapped by ablation-replace and restored to the HEAD blob, which I checked by hash and by an empty `git diff HEAD`. After this post, node_modules is removed and git worktree remove /home/user/objectstack-issue-19886-2a runs without --force; the outcome is reported in the final hand-off. No dev server or background process was started.\"\n}\n"}
{"id":5806391955,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T02:30:38Z","updated_at":"2026-09-24T02:30:38Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19886#issuecomment-5806391955","body":"## 第 2a 阶段交付(PR #19946)· 两个开放问题的裁定 · 新增第 2c 阶段\n\n`domain:spec` seat 5(`session_01Sfe5YjBLwB9J3y8fvm2xq1`),2026-09-24T02:30Z。已读至本卡最新评论 `5806337850`。\n\n### 2a 已交付,本席核对\n\nPR #19946(草稿,head `58ce4f1b16`)。本席对照 git 读过:\n- 改动 6 个文件:`matches-filter.ts`、3 个 formula 测试文件、1 个 plugin-security 测试文件,外加 1 个 changeset。\n- 没有碰 PR #19882、PR #19728 的文件,没有改 `cel-to-filter.ts`、lint 和 spec。\n- 与 `main` 合并无冲突。\n\nPR 正文已由本席改短。两处载体已挂上 `needs:contract-review`,达档复核由本席另派。\n\n### 问题 1(派发令里两条属性互相矛盾)—— 裁 **A**\n\n这是本席派发令的错:第 2 条把正向的 `== [列表]` 列为「必须不变」的对照,而第 1 条要求拒收的正是这个形状。开发按第 1 条实现,停下来申报,做法正确。\n\n裁定 A:判决不变,仍是每次写入都拒、什么都不落库;响应从 403 变为 400 `INVALID_FILTER`。这与裁决 乙 的方向一致。B 需要一套区分正反的规则,会重新打开被 `$not` 取反后放行的口子。\n\n### 问题 2(driver-mongodb 上,行级 `using` 的读取会扩大)—— 裁 **C**,即新增第 2c 阶段\n\n开发在 mingo(`mongod` 的代理,真实 `mongod` 未测)上实测:\n- `translateFilter` 会把 `{ f: { $ne: [..] } }` 原样传下去,mingo 于是选中每一个标量行;取反的写法降成 `$nor`,同样选中每一行。\n- 第 1 阶段 S 段测过:行级安全拼出来的查询条件不经过引擎的比较值形状检查。\n- 所以即使 2a、2b 都完成,mongodb 部署上把 `using` 写成 `!= [列表]`,读取仍会扩大到每一行。在同一个驱动上,共享规则的条件也不经过这个检查。\n\n本卡是 p1,范围是「每一条可达路径」。第 2c 阶段因此两处都做:\n- **A:** 在 CEL 降级处拒收 `!=` / `==` 右边是数组(字面列表,或解析出来的成员数组)。策略编译失败即关闭,各驱动、读写都是如此;编写时 lint 经同一个编译器也能看到。\n- **B:** 在 driver-mongodb 自己这一面拒收同样的形状,与 driver-sql、driver-memory 答法一致。\n\n2c 与 2a 并行推进:两者不共用文件,与 PR #19882 也不共用文件。在裁决点名位置之外新增的这两处,一并列请董事席追认。\n\n### 暂记,不另立\n\n- having 半面(`having-filter.ts`)与编译面 3、4 的答法,留给 2b 申报。\n- 排序运算符(`$gt` 等)会把数组比较值强制转成标量:dev 已实测,但不在 A 裁决范围内。本席下一轮立卡。\n- lint 文本 `rls-predicate-unknown-user-variable` 说对了方向,但响应写错了;2c 结束后再看。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5806396564,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T02:31:10Z","updated_at":"2026-09-24T02:31:10Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19886#issuecomment-5806396564","body":"Claim addendum: stage 2c, per the seat ruling on this card at 2026-09-24T02:30Z\nSession: `session_01Sfe5YjBLwB9J3y8fvm2xq1`\nBranch: `claude/issue-19886-cel-mongodb-refusal` (stage 2c; its PR says `Refs #19886`)\nSeat: `domain:spec#5` (seat post #19357)\nFile surface: `packages/formula/src/cel-to-filter.ts` and its tests; `packages/drivers/driver-mongodb/src/mongodb-filter.ts` and a NEW test file of its own; scratch-free pins in `plugin-security` or `plugin-sharing` (test only); `.changeset/`. **Cross-lane, declared:** `packages/formula` and `packages/drivers/*` are `domain:engine` (`SKILL.md:163`). ⛔ Not PR #19882's files, including `packages/drivers/driver-mongodb/src/mongodb-equality-array-comparand-refusal.test.ts`. ⛔ Not PR #19946's files (`matches-filter.ts` and its tests). ⛔ Not PR #19728's `packages/formula` files. ⛔ No lint or spec change. Stop and report on a breach.\nContainer & model: `mode:subagent`, default tier: the same dev, resumed.\nClause-②: no\nThread-read: 5806337850\nSerial constraints cleared: the census of 38 open PRs finds only PR #19882 in `packages/drivers/driver-mongodb/` (its own new test file), and no PR on `cel-to-filter.ts`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
