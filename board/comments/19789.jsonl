{"id":5789314193,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T04:57:42Z","updated_at":"2026-09-23T04:57:42Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19789#issuecomment-5789314193","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nThis PR changes **2** package(s): `@objectstack/cli`, `@objectstack/spec`, touching **19** documentable anchor(s). ⚠️ **6** changed file(s) yielded no anchor (`packages/spec/api-surface/ui.json`, `packages/spec/authorable-surface/ui.json`, `packages/spec/declaration-map/ui.json`, …), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n**2** hand-written doc(s) NAME something this change touched and may need an implementation-accuracy re-verification:\n\n- `content/docs/getting-started/quick-reference.mdx`  _(via NavigationItem (symbol, a top-level type))_\n- `content/docs/ui/apps.mdx`  _(via NavigationItem (symbol, a top-level type), crm_lead_guide (literal, a string literal in a comment on a changed line; a string literal on a changed line), crm_manual (literal, a string literal in a comment on a changed line; a string literal on a changed line), nav_help (literal, a string literal in a comment on a changed line; a string literal on a changed line), nav_lead_guide (literal, a string literal in a comment on a changed line))_\n\n<details><summary>What this run could not see</summary>\n\n- **6** changed file(s) yielded no anchor (`packages/spec/api-surface/ui.json`, `packages/spec/authorable-surface/ui.json`, `packages/spec/declaration-map/ui.json`, …) — pages documenting those are invisible to this run\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **142** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 3f9e2eaa1cadee483674449817ee732192172e7b` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `795ecedb6b133b25ba1d4604333e547c48466c7b` — the merge of head `f9baf5cb3f41e8374c433bd79d86ca57e43cb42f` into base `3f9e2eaa1cadee483674449817ee732192172e7b`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin 795ecedb6b133b25ba1d4604333e547c48466c7b && git checkout 795ecedb6b133b25ba1d4604333e547c48466c7b\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 3f9e2eaa1cadee483674449817ee732192172e7b f9baf5cb3f41e8374c433bd79d86ca57e43cb42f && git checkout -B drift-repro 3f9e2eaa1cadee483674449817ee732192172e7b && git merge --no-ff f9baf5cb3f41e8374c433bd79d86ca57e43cb42f\n\nnode scripts/docs-audit/affected-docs.mjs --json 3f9e2eaa1cadee483674449817ee732192172e7b\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>\n\n> Advisory only, and a **precision-first** one (#9192): a page is listed because it names a\n> symbol, wire route or SDK method this diff touched — not because it mentions a changed\n> package. Each row says which anchor put it there, so a wrong row is reportable rather than\n> merely annoying. To re-verify, run the `docs-accuracy-audit` workflow scoped to these files:\n> `node scripts/docs-audit/affected-docs.mjs 3f9e2eaa1cadee483674449817ee732192172e7b` → pass the list as\n> `args.docs`, on the commit named under **Which tree this was computed on**."}
{"id":5789573615,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T05:25:53Z","updated_at":"2026-09-23T05:25:53Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19789#issuecomment-5789573615","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `f9baf5cb3f41e8374c433bd79d86ca57e43cb42f`\n\n### ① Derived judgments\n1. `DocNavItemSchema` (`packages/spec/src/ui/app.zod.ts`) — strict via `navItemSurface('doc')`, `book?` (`min(1)`, a package id is a legal implicit-book target) / `doc?` (the `DocSchema.name` rule), `refine(requiredOneOf(['book','doc']))` (at-least-one; both-given accepted and pinned), remedy names both keys, describe carries the addendum's audience rule. Matches ruling `5754620525` + addendum `5754645447` exactly; ⛔ no second nav variant (ten variants, `doc` the only new one).\n2. Discriminated union + both hand-tied types + `NavItemVariant` widened by one — RIGHT.\n3. New exports `DocNavItemSchema` / `DocNavItem` / `DocNavItemParsed`; generated artefacts (api-surface, export-origins, declaration-map, authorable-surface 13 keys, json-schema manifest, reference mdx, strictness-ledger counts) are generator output, +1 schema / +1 site.\n4. JSON Schema carries the rule: the pin calls `projectPublishedJsonSchema(DocNavItemSchema, { io: 'input' })` (the build's own call) and asserts `required:[\"book\"]` / `required:[\"doc\"]`; the ablation would fail it. Doubled `allOf` is a pre-existing generator quirk, accept set unchanged.\n5. Aliases (`docname`, `bookname`, `page`/`docs`/`books` on the doc branch, prose targets on the other nine) are refusal-message near-miss answers, ⛔ not accepted keys — accept set not widened past the ruling; pinned in `alias-integrity.test.ts`.\n6. Landing site of the 「target names no existing book/doc」 refusal — CLI `docs/nav-target` in `collect-docs.ts`: justified. `validateCrossReferences` runs inside `defineStack` (`stack.zod.ts:3370`) before `src/docs/*.md` are collected (`collect-docs.ts:1290`, reached from `compile` / `validate` / `lint`) — a doc arm there would be dormant or a false refusal (seat re-read both sites). Limitation within the ruling's 「in the package」 scope: runtime `PUT /meta/app` gets no existence check.\n7. Describe honesty — the audience sentence is the addendum's own, ordered by the ruling; the server nav filter `filterAppForUserWithReason` has no audience arm (content IS gated at `/meta/doc`, `/meta/book`, the tree read); entry pruning is the renderer's (objectui#10188, not yet shipped); `apps.mdx` and the changeset say the console does not render `doc` items yet. Honest.\n\n### ② Semver level\n`@objectstack/spec` minor (`Clause-②: yes (widening)`, new union member + exports, nothing previously admitted refused) — correct. `@objectstack/cli` minor (a new rule judging only items no stack could carry before) — correct. Changeset prose true line by line.\n\n### ③ Boundary flags\n- (a) `skills/objectstack-ui/SKILL.md` (published, Tier H) — forced by `check:skill-identifier-liveness`; token ceiling real (3854 of 3856). Deletions from the customer-agent seat: 「Target precedence: recordId → filters → viewName」 is FALSE (`objectNavTargetExclusivity` refuses `filters` with `recordId`/`viewName`) — correct to delete; the page-type list is true but redundant (`rules/pages.md`); 「`params` become props」 is true, a minor loss (still in the spec describe and `apps.mdx`). Neither misleads. The new `doc` row is accurate.\n- (b) Files outside the claimed spec surface (`packages/cli`, `content/docs/ui/apps.mdx`, `variant-docs.json`) — each justified by the ruling's refusal test or a gate.\n- (c) Server-side audience pruning of doc entries — follow-up, ⛔ not a blocker (today only the entry's label/book/doc names reach a non-reader; content stays gated).\n- ⛔ This record does not lift Tier H: the PR lands only on the maintainer's hand or an authorized approving review.\n\nImplemented-by: `claude/issue-19482-doc-nav-item`\nReviewed-by: `session_01VWsFyWDp8Rjb2Ma6a3Cyo8`\n\n**VERDICT: PASS**\n\nRendered by an isolated at-tier review subagent (card, rulings, the PR and its check-runs — not the dispatch order), adopted by `domain:spec` seat 4 after re-reading the landing-site readings on the head. Checks at review time on this head: 33 success, 2 opt-in / path-filter skips, 0 failures.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
