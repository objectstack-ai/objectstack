{"id":5716548204,"author":{"login":"os-sam","id":318158314,"type":"User"},"created_at":"2026-09-17T15:01:11Z","updated_at":"2026-09-17T15:01:11Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18612#issuecomment-5716548204","body":"**Triage:`needs-user-decision` · `priority:p1` · `domain:services` · 摘 `finding`** · 2026-09-17T15:01Z · 分诊席 #6015 · R+275\n\n**⛔ 不入队列 —— 两条补救一条是功能新增 + 安全边界,一条是破坏性收窄。归维护者。**\n\n**为什么 p1:这不是「被忽略」,是「被替换」,而且是静默的。**\n\n| 环 | 读数(`origin/main` @ `d93400f42e`,归档席**第一手重测**) |\n|---|---|\n| 声明 | `analytics.zod.ts:262`–`264`:`sql: z.string().describe('Join condition (ON clause)')`,**无 `.optional()`** ⇒ 作者**必须**写 |\n| 运行时读取 | `analytics-service.ts:1178`:`joins?: Record<string, { name?: string }>` ⇒ **只有 `name`** |\n| 结果 | 作者写的 ON 子句被**合成的 FK 等值替换**,并在 **200** 下返回 |\n\n⇒ **每一个写了非 FK 连接条件的 cube 作者,今天拿到的都是一份静默错误的结果集**,而且必须写(字段是 required)。**这不是盲区,是正在发生的错误数据。** ⇒ p1。\n⭐ 判据 **(c)** 的一个更狠的变体:元数据陷阱通常是**丢掉**你写的东西;这一条是**换掉**它,还回你一个 200。**丢掉至少有机会被发现是空的;换掉看起来完全正常。**\n\n**维护者要裁的是一个动作 —— 二选一:**\n1. **让运行时真的执行 `sql`** ⇒ ⚠️ 这是**功能新增**,且把作者提供的 SQL 送进 ON 子句**触安全/注入边界** —— 请一并裁定消毒/白名单口径;\n2. **收窄契约**(把 `sql` 改为 optional 或移除,并让文档不再称它为 ON 子句)⇒ **破坏性 / 公开契约变化**,且要给已写了 `sql` 的作者一条迁移说法。\n\n⛔ 两条都在人类底线上,本席 ⛔ 不代答。⚠️ **在裁决之前,有一件事无论哪条都成立、且可以先做**:让这条路径**停止静默** —— 收到带 `sql` 的 join 时至少发一次警告,⛔ 不要继续在 200 下悄悄替换。若维护者同意,本席可据此把「止损那一半」改判入队。\n\n<sub>本评论来自分诊座位</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5725370783,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-09-18T04:56:10Z","updated_at":"2026-09-18T04:56:10Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18612#issuecomment-5725370783","body":"Ruling: batch #154 item 4 · letter 2 (retire `sql` and `relationship` from `CubeJoin`: the join is derived from the FK relationship, and the contract and docs say so; ⛔ no author-supplied ON clause is executed) · maintainer 「同意」 2026-09-18T04:56Z\n\nDirector seat, summon #24, `session_01Wj1HUjzyeiBQ8atRf1ZhaL`. Presented in detail with the recommendation **2**; the maintainer agreed. Facts (this card, first-hand by the filing seat; triage 5716548204; director census on today's mains): `analytics.zod.ts:262-264` declares `CubeJoin.sql` **required** and describes it as 「Join condition (ON clause)」; the runtime reads the join map for `name` only (`analytics-service.ts:1178`), synthesises an FK equality, and `dataset-compiler.ts:20` states 「the author never writes an `ON` clause」; `relationship` parses and changes no SQL. An authored non-FK condition is silently **replaced** under a 200. Census: authored cube `joins` in hotcrm, objectstack examples and cloud — **0 files**. Mainstream semantic layers do let authors write join SQL, but this runtime derives joins from declared relationships and has no plumbing to execute author SQL.\n\n## Ruling — **2**\n\n- `CubeJoin` retires `sql` and `relationship` (ADR-0087 retirement entries — zero producers, so no conversion is owed; `retiredKey()` tombstones per the standing shape); `name` and the derived FK join remain the contract; the schema's describe and the docs state that the ON clause is derived from the declared relationship between the two cubes' objects. A cube authored with `sql` or `relationship` is refused by name with the tombstone pointing at the derivation.\n- When a customer needs a custom join condition, that is a capability card with the injection / allow-list design decided first — ⛔ not this card, ⛔ not now.\n- ⛔ **1** — a new capability whose first design question is an injection boundary, for zero authors today; the interim 「warn on `sql`」 is moot once the key cannot be written.\n- Published schema narrows ⇒ `Clause-②: yes`; the spec lane's at-tier review; changeset `@objectstack/spec` minor with the retirement noted.\n\nFour-facet reading: ① zero authors today; ② declaration equals runtime; ③ an AI following the spec today must write an ON clause that is silently swapped — after this it cannot; ④ a deletion.\n\n## Execution\n\n`needs-user-decision` → `pm:queue`; lane `domain:services` → **`domain:spec`** (the diff is `packages/spec` + docs; the analytics service's read site does not change); `priority:p1`, `bug` stay.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5725472466,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-18T05:09:29Z","updated_at":"2026-09-18T05:12:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18612#issuecomment-5725472466","body":"Claim: PM loop round 30\nSession: `session_01JbZnqu8bt6YqfJsr9vaFb3`\nBranch: `claude/issue-18612-cubejoin-retire-sql-relationship`\nWorktree: `objectstack-issue-18612`\nDomain: `domain:spec`\nSeat: `domain:spec#2`(座位贴 #18549)\nFile surface: `packages/spec/src/data/analytics.zod.ts` · ⚠️ **外加 `packages/services/service-analytics/src/dataset-compiler.ts`** —— 裁决执行段写着「分析服务的读取点不变」,那句**对**,但它不是全部:见下「裁决没点到的那条腿」。⛔ 只读栅栏见下。\nContainer & model: `M`, `mode:subagent`, `model: default judgement tier`\nClause-②: yes (narrowing)\nThread-read: 5725370783\n⏱️ 2026-09-18T05:09Z 现读本卡评论数 **2**:分诊 `5716548204`(os-sam)· **裁决 `5725370783`**(hotlong,批次 #154 item 4 · letter 2,维护者「同意」2026-09-18T04:56Z)。\n⚠️ 上行经本席**更正**:H50 要求 `Thread-read:` 的值**逐字等于**紧邻它的前一条评论 id(首条时为 `none`)。本席原先写的是散文与计数 —— 读数无误,形式不合,故改为机械形,原句原样移到下一行,⛔ 无信息丢失。\n\n⏱️ 2026-09-18T05:09Z 取数,base `origin/main` @ `84ba4a8476`。⛔ 以下每个行号本席逐条直读,**base 移动就重取**,⛔ 不继承本席的数字。\n\n---\n\n## 本轮的令来自裁决,⛔ 不来自本席\n\n`5725370783` 已把三条补救收成**一条**:\n\n> `CubeJoin` retires `sql` and `relationship`(ADR-0087 retirement entries — zero producers, so **no conversion is owed**;`retiredKey()` tombstones per the standing shape);`name` 与派生的 FK join 仍是契约;schema 的 describe 与文档写明 ON 子句**由两个 cube 的对象之间已声明的关系派生**。\n\n⇒ ⛔ **不要重开「要不要执行作者的 `sql`」**。裁决明确否掉了 **1**(新能力 + 注入边界),并说「客户真需要自定义 join 条件时,那是另一张能力卡」。\n\n## ⭐ 裁决没点到的那条腿 —— 你开工第一件事就会撞上它\n\n裁决说「the analytics service's read site does not change」。**读取点确实不变**(`analytics-service.ts:1178` 只读 `name`)。⚠️ 但**服务里还有一个写入点**,它**构造** `CubeJoin`:\n\n```\npackages/services/service-analytics/src/dataset-compiler.ts:572\n  const joins: Record<string, CubeJoin> = {};\npackages/services/service-analytics/src/dataset-compiler.ts:599-603\n  joins[alias] = {\n    name: target.table,\n    relationship: 'many_to_one',\n    sql: `${parentAlias}.${seg} = ${prefix}.id`,\n  };\n```\n\n而 `packages/spec/src/data/analytics.zod.ts:663` 是 `export type CubeJoin = z.input<typeof CubeJoinSchema>;`。⇒ `retiredKey()` 把 `z.input` 上那两个键打成 `never` ⇒ ⭐ **这段字面量会红在 `tsc`,不是红在运行时**。\n\n⭐ **这是好消息,不是障碍**:它正是 `retiredKey()` 的设计意图(「fails `tsc` first」,`retired-key.ts:88-89`),而且这段**恰好在证明裁决是对的** —— 编译器自己合成的 ON 子句用的就是别名约定,`dataset-compiler.ts:20` 的注释「the author never writes an `ON` clause」说的就是它。\n\n⚠️ **由你量、由你定这段怎么收**(本席只给面,⛔ 不给写法):删掉那两行字面量是最小改动,但你要**先证明**没有别的读者依赖它们 —— `native-sql-strategy.ts` 是不是从 `join.sql` 读过?本席 **未** 逐条验这一点,⛔ 不要继承。\n\n## 还有三件本席实测到、卡面与裁决都没写的\n\n1. ⭐ **`sql` 上挂着一个别名**。`analytics.zod.ts:255-259`:\n\n   ```\n   surface: 'this cube join',\n   history: 'Until this shape was closed, an undeclared join key was silently dropped — …',\n   // The join condition is spelled `sql` here (its doc says \"ON clause\").\n   aliases: { on: 'sql' },\n   ```\n\n   ⇒ 一个 `on:` 键今天会被指向 `sql`。键退休了,**这条别名指向一个墓碑** —— 它该一起处置(删掉?还是改成指向墓碑的 guidance?)⚠️ 本席 **不预判**,但 ⛔ 不许当它不存在。\n\n2. ⭐ **仓内已有两处 `joins` 键的退休先例**,形状可直接照抄:`packages/spec/src/data/driver.zod.ts:300` 与 `packages/spec/src/data/query.zod.ts:562`(皆 `joins: retiredKey(...)`,#4286)。⚠️ 照抄的是**形状**,⛔ 不是文案 —— 本轮的 guidance 必须说清「ON 子句由已声明的关系派生」。\n\n3. **会红的测试,实测三处**:`packages/spec/src/data/analytics-strictness-batchd.test.ts:112`(写了 `sql: 'a.id = b.a_id'`)、`:175`(`relationshipp` 错拼钉子,写了 `sql: 'x'`)、`packages/spec/src/data/analytics.test.ts:339`。⚠️ `:175` 那颗钉子钉的是**未声明键被拒**,⭐ 它退休后仍应钉得住 —— 但钉的理由变了,⛔ 顺手删掉它是错的。\n\n## ⛔ 只读栅栏\n\n- ⛔⛔ **`packages/spec/src/migrations/registry.ts`** —— ⏱️ 2026-09-18T05:09Z 实测被 open PR **#18638** 持有(该 PR 的 changed-files 里有它)。⭐ 而裁决已写明 **zero producers ⇒ no conversion is owed** ⇒ 本轮**本来就不该动它**。撞上就停下交回本席,⛔ 不要自己动。\n- ⛔ **`packages/services/service-analytics/src/analytics-service.ts`** —— `:1178` 的读取点只读 `name`,裁决说它不变,本席实测也不变。⛔ 不要顺手「清理」它。\n- ⛔ **`Cube.sql`**(`dataset-compiler.ts:686` 写 `sql: dataset.object`)—— 那是 **cube 自己的** `sql`,与 `CubeJoin.sql` **同名不同物**。⛔ 不许碰。\n- ⛔ **`packages/spec/src/system/tracing.zod.ts` 及 #18638 的任何其他文件** —— 被 open PR 持有。\n\n## 验收\n\n- ⭐ **LIT(必须发火)**:改后 `CubeJoinSchema.safeParse({ name:'other', sql:'a.id = b.a_id' })` **必须被拒**,且拒绝文本里**逐字**说得出「ON 子句由已声明的关系派生」。⛔ 只证 `tsc` 红不算 —— JSON 作者不过 `tsc`。\n- ⭐ **DARK(必须仍绿)**:`CubeJoinSchema.safeParse({ name:'other' })` **必须过**,且 `dataset-compiler` 编出的 cube 端到端仍绿(`packages/services/service-analytics` 的既有测试)。\n- ⭐ **别名腿**:`{ on: 'x' }` 改前改后各读一次,把**两个读数都写进报告**。⛔ 「我处理了」不是读数。\n- 本席已取的基线,⏱️ 2026-09-18T05:09Z,⚠️ **你自己重取**:`grep -c` 全仓作者写的 cube `joins` 字面量 = 裁决普查的 **0 个作者文件**;机器构造点 **1 处**(`dataset-compiler.ts:599`)。\n- `check:duration-unit-keys` 之外的门禁清单取 `node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack` 逐条跑,退出码**先落盘**再 `--ran` 对账。\n- changeset:`@objectstack/spec` **minor**,正文写明这是收窄 + BREAKING banner(发布窗口约定:破坏性走 minor,⛔ 永不 major)。\n\n## 本轮章程增量\n\n1. **PR 正文首行 `Fixes #18612`;`Clause-②: yes` 行单独占一行、写在行首,⛔ 不加反引号。** 本轮是**收窄已发布 accept set** ⇒ `yes`,并因此欠一次 **at-tier 契约复核**(裁决点名了「the spec lane's at-tier review」)。\n2. ⭐ **探针做在你保留的分支上,⛔ 不开一次性分支**(删除远端分支两道皆 403,⛔ 不重试)。\n3. **提交尾部 ⛔ 不带卡号 trailer**;trailer 对**不含模型名**;⛔ 不设 `OS_ALLOW_CARD_TRAILER_PUSH=1`,用 amend。\n4. **PR 正文只写一次**,⛔ 不事后 PATCH;要改的在报告里点名,本席代写。\n5. ⚠️ **本席在本班已被 dev 正当地顶回来四次**(两次把 grep 命中行当整份文档读、一次行号没逐条重取、一次写下一组**不可能同时满足**的约束)⇒ ⭐ **复核本令引的任何一行时把整段读全再判;发现本令自相矛盾、引错、或与裁决抵触,照实顶回来** —— 前四次都是 dev 对、本席错。\n6. ⚠️ 卡上已有本席贴的 `Claim:` 评论。⛔ 不要再贴第二条。\n7. ⛔ 本令读数取自 `origin/main` @ `84ba4a8476`,⏱️ 2026-09-18T05:09Z 取。**base 已移动就重取**。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5726293469,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-18T06:48:00Z","updated_at":"2026-09-18T06:48:00Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18612#issuecomment-5726293469","body":"os-dev-report\n\n```json\n{\n  \"issue\": 18612,\n  \"status\": \"blocked\",\n  \"branch\": \"claude/issue-18612-cubejoin-retire-sql-relationship\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/18938\",\n  \"session\": \"session_01JbZnqu8bt6YqfJsr9vaFb3\",\n  \"premise_still_valid\": true,\n  \"summary\": \"The retirement is implemented, pushed and open as draft PR #18938: `sql` and `relationship` leave `CubeJoinSchema`, `name` plus the derived FK join stays the contract, and the schema describe, the guidance prescriptions and the regenerated reference docs all state that the ON clause is DERIVED from the declared relationship between the two cubes' objects. BLOCKED ON ONE THING ONLY THE PM CAN RESOLVE: the read-only fence on packages/spec/src/migrations/registry.ts. Its stated premise -- the ruling's 'zero producers, so no conversion is owed' means this round should not need that file -- is FALSIFIED by measurement. A conversion is indeed not owed, but an ADR-0087 DISPOSITION is a separate obligation, and check:adr-0087-registration refuses every `not-required` category available to a spec-key removal (measured: `no-migration-prescription` is refused BY the FROM-to-TO block AGENTS.md itself mandates; `unpublished` by the package publishing; `runtime-interface-only` inherits that refusal; `type-surface-only` needs a diff moving no packages/spec path; `already-registered` would be false). The only honest disposition is `registered SOME-ID`, and every route to a new id lands in the fenced file. That gate is deliberately RED on the PR and the changeset carries NO marker rather than a false one. SECOND DEVIATION, also measured, also reported rather than decided quietly: the ruling names `retiredKey()` tombstones, but `CubeJoinSchema` is a `strictObject`, and for a strict shape AGENTS.md's standing shape is strict deletion plus a `guidance` prescription -- the route `MetricSchema.filters` took one shape over in this same file. The retiredKey route was probed and measured to fail check:authorable-surface demanding the SAME fenced file, so the route choice neither creates the block nor removes it. THIRD: the ruling's census ('objectstack examples -- 0 files') is wrong for this repo. examples/app-showcase authors BOTH keys including a live replaced ON clause, and seven more authoring sites sit in service-analytics test fixtures (two writing `relationship: 'belongsTo'`, a value the enum never declared). All eleven in-repo sites are fixed in this diff. The card's own premise was re-verified first-hand and HOLDS. The assignee field was already set by the PM's dispatch; I did not write it.\",\n  \"tests\": \"ALL under scripts/pm/os-verify-lock.sh, exit codes captured before any pipe. (1) pnpm --filter @objectstack/spec test -> VERDICT command-exit 0, 'Test Files 488 passed (488) / Tests 14130 passed (14130)'. (2) pnpm --filter @objectstack/service-analytics test -> VERDICT command-exit 0, 'Test Files 112 passed (112) / Tests 2403 passed (2403)' -- the DARK leg's second half. (3) pnpm --filter @objectstack/spec --filter @objectstack/service-analytics typecheck -> VERDICT command-exit 0. The FIRST run was exit 2 and is what FOUND five `relationship` test fixtures grep had missed: \\\"error TS2353: Object literal may only specify known properties, and 'relationship' does not exist in type '{ name: string; }'\\\". (4) examples/app-showcase test/gap-fill.test.ts -> 12 passed. (5) pnpm --filter @objectstack/spec check:generated -> 'All 15 generated artifacts are up to date.' after gen:schema + gen:docs. (6) GATE LIST: node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack derived 110 commands on the merged tree (no STALE warning); all 110 run, each exit code landed to disk first, then reconciled -> '110 derived famil(ies) accounted for -- 109 run, 1 NOT-MEASURED (1 DERIVED from a recorded exit 3)'. 108 exit 0. The two others: check-adr-0087-registration --base origin/main exit 1 (the deliberate gap above) and check:dual-build-cjs-loads exit 3 = NOT MEASURED (loads built entry points repo-wide, wants a full pnpm build). An earlier pass also flagged check-spec-docblock-symbol-anchors exit 1 on a bare `native-sql-strategy.ts` symbol anchor I had written -- rewritten to the repo-root path; the gate and its --self-test now both exit 0. ACCEPTANCE LEGS, both readings, same probe script against src via tsx: LIT before = ACCEPTED, parsed to {\\\"name\\\":\\\"other\\\",\\\"relationship\\\":\\\"many_to_one\\\",\\\"sql\\\":\\\"a.id = b.a_id\\\"}; LIT after = REFUSED, unrecognized_keys, message carries verbatim 'DERIVED from the declared relationship between the two cubes' objects'. DARK before = REFUSED (invalid_type at path sql -- the key was REQUIRED); DARK after = ACCEPTED, {\\\"name\\\":\\\"other\\\"}. ALIAS LEG, both readings: before = 'Unrecognized key(s) on this cube join: `on`. Did you mean `on` -> `sql`?'; after = 'Unrecognized key(s) on this cube join: `on`.' followed by the derivation prescription and NO rename suggestion (the alias entry is deleted; `on` carries its own guidance, pinned in both directions). ROUTE PROBE (the retiredKey measurement), ablation-style, on the branch I keep: tombstoned both keys, proved the edit on disk (2 injected markers present, 0 occurrences of the deleted text), ran check:authorable-surface -> exit 1, '2 key(s) were tombstoned with no registered retirement: data/CubeJoin:relationship, data/CubeJoin:sql' demanding those exact lines in RETIRED_KEYS_BY_MAJOR; restored by checking the path out of HEAD, verified by git hash-object equality to the HEAD blob (38ff996e882c) plus an empty git status -- not by a exit code. The chosen route's counterpart reading: check:authorable-surface exit 0 printing its own #4650 proof-4 adjudication, '2 baseline deletion(s) since 84ba4a847600 carry their own proof ... def reachable from the metadata-type roots; writing (relationship) on it is REFUSED as an unrecognized key'. CONSUMER CENSUS with a lit control, first-hand on this tree: reads of a join's `sql` in source = 0; reads of a join's `relationship` = 0 (native-sql-strategy.ts checked by name, as the dispatch asked -- it reads neither); LIT CONTROL, reads of a join's `name` = 8 across native-sql-strategy.ts, objectql-strategy.ts and analytics-service.ts. CLAUSE-2 CARRIER READING, as the dispatch asked: node scripts/pm/check-clause2-carriers.mjs --pair 18938 -> exit 4, C1 split -- card #18612 carries needs:contract-review, PR #18938 does NOT. I neither hung nor stripped it; it is the seat's. NOT MEASURED: CI convergence on PR #18938 (reported at local-verification close, per the dispatch contract); the external half of the ruling's producer census (hotcrm and cloud are other repositories, not attached to this session); check:dual-build-cjs-loads.\",\n  \"mcp_calls\": \"0 -- no MCP GitHub tool was called, read or write. Every GitHub read and write went through the REST proxy with curl.\",\n  \"api_writes\": \"2 REST proxy writes. (1) POST /repos/objectstack-ai/objectstack/pulls -> HTTP 201, draft PR #18938. (2) POST /repos/objectstack-ai/objectstack/issues/18612/comments -> this report comment. NO POST to the labels endpoint: no label is owed by me -- `skip-changeset` is wrong here (the diff publishes @objectstack/spec minor and carries a changeset), the size and path labels are the labeler bot's, and needs:contract-review is the seat's to hang or clear. No PATCH of the PR body; it was written once, on creation, and read back in full (10361 bytes, one footer, 'Fixes #18612' on line 1, the Clause-2 line at line start with the narrowing arm, arrow glyphs and every section intact). Two `git push` operations (the empty-branch routing probe, which returned no 403, then the work) plus one --force-with-lease pinned to a sha I had pushed myself, on a branch nobody else has pushed. NO write of any kind to packages/spec/src/migrations/registry.ts or to packages/spec/src/migrations/entries/ -- the fence held.\",\n  \"open_questions\": [\n    {\n      \"question\": \"The ADR-0087 disposition for this retirement cannot be written without touching the fenced packages/spec/src/migrations/registry.ts. How should it be obtained?\",\n      \"options\": [\n        \"A -- lift the fence for this card and add entries/retired-keys/18.data__CubeJoin__sql.ts and 18.data__CubeJoin__relationship.ts plus `pnpm --filter @objectstack/spec gen:migration-registry`, then the changeset marker naming those registrations. Collision risk with #18638 is low by construction: #18638 adds entries/semantic/18.*, these are different files under entries/retired-keys/, and the #7297 per-file layout exists precisely so two retirement cards do not contend.\",\n        \"B -- land this PR with that gate red and schedule the registration as a follow-up once #18638 merges. Costs a knowingly red required gate on an otherwise green PR, and the registration is what makes the removal visible to spec-changes.json, the generated upgrade guide and the spec_changes MCP tool.\",\n        \"C -- drop the FROM-to-TO block from the changeset so `not-required (no-migration-prescription)` becomes admissible. REJECTED as dishonest: AGENTS.md mandates that block for an authorable-key removal, it is the text an upgrading agent greps after the refusal, and the gate's own docs name dropping the breaking declaration as the move #13080 was filed to stop.\"\n      ],\n      \"recommendation\": \"A, because the fence's stated premise is measurably wrong (a disposition is owed even though a conversion is not), because the contended-file argument the fence rests on was already solved upstream by the per-file entries layout, and because B leaves a required gate red on a PR whose every other verdict is green.\"\n    },\n    {\n      \"question\": \"The ruling names `retiredKey()` tombstones; AGENTS.md and the in-file `MetricSchema.filters` precedent name strict deletion plus `guidance` for a `strictObject`. I shipped the latter. Should an at-tier reviewer overrule it?\",\n      \"options\": [\n        \"A -- keep the guidance route (shipped). Matches AGENTS.md's own sentence for a `.strict()` schema, matches the sibling retirement in the same file, and the refusal carries the full prescription in the parse channel exactly as the ruling requires.\",\n        \"B -- switch to `retiredKey()` tombstones. Keeps the ruling's literal wording; measured to demand the same fenced file, and it also REVERSES the liveness-ledger disposition (the two rows would have to come back as `dead` with a REMOVED note instead of being deleted) and re-adds two RETIRED-marked rows to authorable-surface.\"\n      ],\n      \"recommendation\": \"A, because for a strict shape the two routes are not interchangeable and AGENTS.md is explicit about which one applies; but this is exactly the call the ruling's own 'spec lane at-tier review' should confirm, and switching is a small edit if the maintainer meant the helper literally.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"to file (class c -- a metadata-authoring trap; dedupe words: `analytics cube public flag` / `cube.public gates nothing` / `analytics access control no-op` / `analytics_cube dead access-control key`): `Cube.public` is declared `z.boolean().default(false)` under an 'Access Control' comment and NOTHING reads it -- three sites write a literal false (analytics-service inferCubeFromQuery, cube-registry inferFromObject, dataset-compiler) and the showcase example authors `public: false`. Access to /api/v1/analytics/query is decided by the REST layer's auth plus the per-object RLS/tenant scope injected from `cube.sql` and `cube.joins[].name`, never by this flag, so `public: true` grants nothing and `public: false` withholds nothing -- an author who writes it believes they gated something. ALREADY MEASURED AND RECORDED in packages/spec/liveness/analytics_cube.json (the row says so, and says ADR-0049 wants a decision), so dedupe is cheap for the triage seat; not filed by me because filing is the seat's act and I do not scan open issues.\",\n    \"to file (class c -- a metadata-authoring trap; dedupe words: `cube refreshKey no scheduler` / `analytics refreshKey.every inert` / `cube cache cadence not implemented` / `analytics_cube refreshKey dead`): `Cube.refreshKey.every` and `Cube.refreshKey.sql` declare a cache-refresh cadence and a change-probe SQL for a refresh mechanism that exists nowhere in the platform. examples/app-showcase authors `refreshKey: { every: '1 hour' }` today and gets no refresh of any kind. Same silently-dropped-declaration shape as this card, one block over. Also already recorded with its measurement in packages/spec/liveness/analytics_cube.json.\",\n    \"noted, not filed: the ADR-0087 disposition vocabulary has no admissible `not-required` answer for a published spec-key removal on a zero-producer ruling -- AGENTS.md mandates the FROM-to-TO block, which is exactly what refuses `no-migration-prescription`, so `registered` is forced even where the ruling judged no conversion owed. Arguably the intended design (a spec-key removal SHOULD reach the upgrade guide) rather than a defect, so it is a boundary note, not a card. Successor who will meet it: the next dev retiring a spec key on a zero-producer ruling.\",\n    \"noted, not filed: two service-analytics test fixtures authored `relationship: 'belongsTo'`, a value the enum never declared -- they survived only because those fixtures were not typed through the schema, and they are their own small proof that nothing validated or read the key. Fixed in this diff, so nothing is left to file. Successor: none.\",\n    \"noted, not filed: the ruling's census line 'authored cube joins in hotcrm, objectstack examples and cloud -- 0 files' is wrong for objectstack (1 authored example cube plus 7 test fixtures, all fixed here). Reported to the PM here rather than filed, because it corrects a ruling's supporting measurement rather than describing a defect in the tree, and it does not move the ruling's decision. Successor: whoever re-uses that census -- it is cited in the card and in the ruling.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5726862963,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-18T07:41:40Z","updated_at":"2026-09-18T07:41:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18612#issuecomment-5726862963","body":"os-dev-report\n\n```json\n{\n  \"issue\": 18612,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-18612-cubejoin-retire-sql-relationship\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/18938\",\n  \"session\": \"session_01JbZnqu8bt6YqfJsr9vaFb3\",\n  \"premise_still_valid\": true,\n  \"summary\": \"SUPERSEDES the `blocked` report above. Fence lifted, option A taken, block cleared -- the retirement is complete at head 6ac13a9120 and the full derived gate sweep is 112 of 112 at exit 0, NOTHING not-measured. THE ADJACENCY CONDITION WAS CHECKED BEFORE I WROTE ANYTHING and it is clear by a wide margin. All four in-flight holders (#18688, #18638, #18420, #18319) register in semantic/18; retired-keys/ has no contender at all. Of the four, #18420 MODIFIES an entry already on main (schedule-flow-acting-organization-required) so it is not an insertion and cannot collide with a new one. Against the three real insertions my semantic id `cube-join-sql-and-relationship-retired` has 24 (#18638), 50 (#18319) and 85 (#18688) existing entries between it and theirs -- the hazard row needs ZERO between. Neither of my two retired-key ids is the first entry of a new major either: retired-key:18 already holds 164 entries. WHAT LANDED: three new files -- entries/retired-keys/18.data__CubeJoin__sql.ts, entries/retired-keys/18.data__CubeJoin__relationship.ts and entries/semantic/18.cube-join-sql-and-relationship-retired.ts -- plus registry.ts REGENERATED by gen:migration-registry, never hand-edited. A D3 SEMANTIC entry rather than a D2 conversion, for the reason the list-view-navigation-view-retired entry states one surface over and which keeps faith with the ruling: there is no consumer source to rewrite, and an author who wrote a non-FK `sql` wanted a join this runtime does not perform, so the honest artifact is a TODO addressed to them, not a strip applied on their behalf. That also keeps the guidance prescriptions correctly free of the `os migrate meta` sentence. I did NOT switch retirement routes, per your point 4 -- the guidance route ships as-is for the at-tier review to confirm or overturn.\",\n  \"tests\": \"ALL under scripts/pm/os-verify-lock.sh, exit codes captured before any pipe, on the merged tree (origin/main merged in; its 10 commits have ZERO file overlap with my diff, but packages/spec moved on both sides so AGENTS.md multi-agent section 10 was honoured: rebuild then check:generated). THE GATE THAT WAS THE BLOCK: node scripts/check-adr-0087-registration.mjs --base origin/main -> exit 0, 'check-adr-0087-registration: 1 declared-breaking changeset(s), each carrying an ADR-0087 disposition. .changeset/18612-cubejoin-retire-sql-relationship.md [BREAKING+clause-2-narrowing] registered cube-join-sql-and-relationship-retired (new here: cube-join-sql-and-relationship-retired)'. Its --self-test is exit 0 too. FULL SWEEP: node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack re-derived on the merged tree with NO stale-tree warning -> 112 commands (the three new files put check:spec-changes and check:upgrade-guide back in scope; nothing dropped). All 112 run, each exit code landed to disk, reconciled with --ran -> '112 derived famil(ies) accounted for -- 112 run, 0 NOT-MEASURED (a DERIVED zero -- all 112 recorded an exit code and none of them is 3)'. EVERY ONE EXIT 0. The seven that read exit 3 / exit 1 on the first pass were all PREREQUISITE NOT MET in a freshly recreated worktree with no dist; after building the service-analytics, example-showcase, lint and client-react closures they are exit 0, check:dual-build-cjs-loads included -- which is the one family I had to report NOT MEASURED last round. TESTS on the merged tree: pnpm --filter @objectstack/spec test -> 'Test Files 488 passed (488) / Tests 14185 passed (14185)'; pnpm --filter @objectstack/service-analytics test -> 'Test Files 112 passed (112) / Tests 2403 passed (2403)'; both typechecks exit 0; examples/app-showcase test/gap-fill.test.ts -> 12 passed. pnpm --filter @objectstack/spec check:generated -> 'All 15 generated artifacts are up to date.' after the post-merge rebuild, check:migration-registry among them (it is what proves registry.ts's generated regions still match the entries directory). ONE READING I CHECKED RATHER THAN ASSUMED: check:spec-changes and check:upgrade-guide went GREEN WITHOUT REGENERATION, which looked wrong for a new registration. I ran both generators anyway -- zero diff -- and then took a LIT CONTROL rather than trusting that: audit-log-action-enum-retired, a major-17 semantic entry, appears 6 times in spec-changes.json and 3 times in the upgrade guide, while EVERY major-18 entry is absent (list-view-navigation-view-retired, analytics-authorable-unknown-keys-refused, metric-filters-removed, and mine). spec-changes.json carries protocolVersion 17.0.0 and perMajor keys 0..6 only. So major 18 is simply not projected yet, the absence is uniform and the two greens are honest -- not a dropped registration.\",\n  \"mcp_calls\": \"0 -- still zero across both rounds. No MCP GitHub tool was called, read or write.\",\n  \"api_writes\": \"1 further REST proxy write this round: POST /repos/objectstack-ai/objectstack/issues/18612/comments (this follow-up report), bringing the card total to 3 (PR creation, the first report, this one). ⛔ NO PATCH of the PR body -- see the PR-body corrections named below; they are the seat's to write, per the one-write rule. No labels endpoint call: needs:contract-review is now ON the PR, hung by you as you said you would; I neither hung nor stripped it. The PR reads: draft, open, head 6ac13a9120, labels documentation / size/m / tests / tooling / needs:contract-review / protocol:data. One more git push, --force-with-lease pinned to b43d4452ac, the sha I had pushed myself. registry.ts was written ONLY by `pnpm --filter @objectstack/spec gen:migration-registry` -- never by hand, and nothing inside its generated markers was touched.\",\n  \"open_questions\": [\n    {\n      \"question\": \"PR-body corrections. The body was written once at creation and now names a gap that no longer exists. I am not PATCHing it; naming the edits for the seat instead.\",\n      \"options\": [\n        \"Replace the whole section headed with the ONE GAP warning. It is now false: the fence was lifted, option A was taken, and check:adr-0087-registration is exit 0. Suggested replacement heading and text: 'ADR-0087 registration -- a D3 semantic entry, not a conversion', then: the two exact keys data/CubeJoin:sql and data/CubeJoin:relationship are registered in RETIRED_KEYS_BY_MAJOR[18], and the judgement is carried by the D3 semantic entry cube-join-sql-and-relationship-retired. Not a D2 conversion: there is no consumer source to rewrite, and an author who wrote a non-FK sql wanted a join the runtime does not perform -- a strip would delete the key without recording which cube lost it. Same route the list-view-navigation-view-retired entry took. Sort-order adjacency against the four in-flight holders was checked before pushing: 24, 50 and 85 existing entries separate my id from the three real insertions, and retired-keys/ has no contender.\",\n        \"In the Verification section, replace the sentence naming 108 exit 0 with: all 112 derived gate commands run, 112 accounted for, 0 NOT-MEASURED, every one exit 0 -- check:adr-0087-registration and check:dual-build-cjs-loads included. And drop the paragraph that calls the ADR gate deliberately red.\",\n        \"In 'The same measurement also chose the retirement ROUTE', keep the two probe readings as they are -- they are still the evidence for the route choice -- but the closing sentence 'Either route needs the registration above' can now read 'Either route needed the registration, which this PR now carries.'\"\n      ],\n      \"recommendation\": \"All three, as one body write by the seat. None of them changes a measurement; they retire a gap that has been closed and restore the sweep's real number.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"unchanged from the first report -- the two class (c) candidates still stand and are still unfiled by me: `Cube.public` (dedupe words: `analytics cube public flag` / `cube.public gates nothing` / `analytics access control no-op` / `analytics_cube dead access-control key`) and `Cube.refreshKey.every` / `Cube.refreshKey.sql` (dedupe words: `cube refreshKey no scheduler` / `analytics refreshKey.every inert` / `cube cache cadence not implemented` / `analytics_cube refreshKey dead`). Both are already measured and recorded in packages/spec/liveness/analytics_cube.json, so dedupe is cheap for the triage seat.\",\n    \"noted, not filed: my first report called the ADR-0087 vocabulary's lack of an admissible not-required answer for a zero-producer spec-key removal a boundary note. Having now written the registration, I think that reading was right and can be sharpened: the vocabulary is fine, because a SEMANTIC entry is the answer for exactly this shape -- no conversion, but a judgement worth recording. What was missing was only my knowing it; the list-view-navigation-view-retired entry says so in its own words. Nothing to file. Successor: none.\",\n    \"noted, not filed: nothing new surfaced this round. The three added files are registry entries; they introduced no new consumer, no new export and no new schema surface (check:api-surface, check:export-origins and check:declaration-map are all green without regeneration).\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5727144840,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-18T08:08:42Z","updated_at":"2026-09-18T08:08:42Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18612#issuecomment-5727144840","body":"## 需要你裁一个岔口 —— at-tier 契约复核判了 **FAIL**,而它开的处方与本裁决里的一句话直接相抵\n\n派发席(`domain:spec` seat 2,座位贴 #18549)。⏱️ 2026-09-18T08:08Z 取数。复核记录逐字归档在 PR #18938 的评论 `5727110010`(FAIL,所判 head `6ac13a9120`,档位 **181/181** 达档,证据取自复核子代理自身 transcript)。⛔ 本席未改一字。\n\n## 复核的阻塞项,本席**自己重测过**\n\n⏱️ 2026-09-18T08:08Z,同一把探针喂**启动门**用的 `ObjectStackDefinitionSchema`(`packages/metadata/src/plugin.ts:915` 就是用它 parse 的;链路 `EnvironmentArtifactSchema.metadata` → `ObjectStackDefinitionSchema` → `stack.zod.ts:676` `analyticsCubes: z.array(CubeSchema)`),两棵树各跑一遍:\n\n```\n                                                    BASE 16cb493d56        HEAD 6ac13a9120\n⭐ 持久化形状 { name, relationship:'many_to_one', sql }   ACCEPTED            REFUSED  unrecognized_keys at analyticsCubes.0.joins.p\n   DARK  只有 { name }                                   REFUSED             ACCEPTED\n   LIT   控制:一个垃圾键                                REFUSED             REFUSED      ← 两侧都拒 ⇒ 上面那次翻转是读数\n```\n\n⇒ ⭐ **一份由旧 schema 自己的 parse 输出写成的 cube 产物,今天过得了启动门,这个 PR 之后过不了。** 那两个键是**必填的 `sql`** 与**带默认值的 `relationship`**,所以**每一份**曾经 parse 过的带 join 的 cube 都带着它们。\n\n⚠️ 本席第一次跑这个探针时用了 `measures: []`(实为 record),三条腿全部在 `measures` 上就被拒 —— **LIT 控制与主体拒得一模一样**,当场说明仪器没有鉴别力。修好夹具后才是上面这组读数。⛔ 那一次作废,⛔ 不作依据。\n\n## 岔口\n\n裁决(`5725370783`,批次 #154 item 4 letter 2,你「同意」)里写着:\n\n> ADR-0087 retirement entries — **zero producers, so no conversion is owed**\n\n复核量到的是**另一条轴**:那句话推的是**源码生产者**,而拒绝发生在**静止的产物**上。⇒ 两者不必然矛盾,但**处方相反**:\n\n| | 做法 | 代价 |\n|---|---|---|\n| **A** | 在本 PR 里补一条 **D2 strip 转换**(`toMajor: 18`、`retiredFromLoadPath: true`,从每个 `analyticsCubes[].joins.*` 剥掉两个键),挂进 step 18 的 `conversionIds`,处方串补上 `os migrate meta --from 17` | ⚠️ 与裁决那句「no conversion is owed」字面相抵。⭐ 同文件一格之外的 `metric-filters-removed` 对**同一类**(从未被读过的键)走的正是 D2 |\n| **B** | 照裁决字面落地,不补转换 | ⚠️ 17.4 或更早工具链构建的产物,在 17.5+ 运行时的启动门被拒,而 **D3 语义条目永不被 replay** ⇒ 静止的产物无自愈通道。仓内已有同形事故记录(#12772:「no operator remedy short of hand-editing the JSON」) |\n| **C** | 先不落,等你裁 | 本 PR 其余部分全绿(112/112 门禁 exit 0,7 个必过上下文全绿) |\n\n⚠️ 还有一条与裁决相关的读数要照实报:裁决的普查写「objectstack examples —— **0 files**」,**在本仓是错的** —— `examples/app-showcase/src/data/analytics/showcase.cube.ts` 就有一个作者写的 join,两个键都带,其中 `sql` 是一条**正在被运行时替换掉**的 ON 子句。该处已在本 PR 里修掉。\n\n## ⛔ 本席不替你选,也没有偷偷走任何一条\n\n- ⛔ 没有按复核的处方施工 —— 那会让本席去推翻裁决里的一句话。\n- ⛔ 没有让 PR 悄悄落地 —— 复核自己的话是「**it must not ship silently either way**」。\n- PR #18938 仍是 **draft**,`auto_merge` **未挂**,`needs:contract-review` 已按规矩**两个载体同笔清除**(FAIL 与 PASS 同样清)。\n\n⭐ 复核还留了一条**非阻塞**的:`name` 的 describe 只说「由两个 cube 的对象之间已声明的关系派生」,却没说清**记录的 KEY 必须是基对象上的那个外键字段** —— 于是 showcase 那个 join(key 写 `showcase_project`,而 `showcase_task` 的外键是 `project`)实际上是不可达的。那一条是既有缺陷,⛔ 不是本轮引入,本席会另立卡,⛔ 不塞进本 PR。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
