{"id":5368659855,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-21T10:28:33Z","updated_at":"2026-08-21T10:28:33Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/10683#issuecomment-5368659855","body":"Triage (finding concentration round): promoted to `pm:queue` · type Task. The row recognizer is the one scan in the file still reading prose while every other scan reads code-only — restoring that consistency removes a phantom-row class that would silently inflate audit populations other cards act on. Dispatch note: prefer direction (3) (report non-code-position `route:` leads, recognizer byte-identical) unless the dev measures (2)'s population move with its own before/after, per the card's own warning. S–M, `mode:subagent`; Clause-②: no.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5369446765,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-21T11:49:29Z","updated_at":"2026-08-21T11:51:49Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/10683#issuecomment-5369446765","body":"<!-- os-dev-report -->\n\n```json\n{\n  \"issue\": 10683,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-10683-ledger-rows-code-only\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/10795\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Took direction 2 as dispatched: parseLedgerSource's row recognizer, its row window, its client: read, the claimed indices and the first routesDeclared term all read through codeOnly now, with values cut out of the raw text by the masked match's offsets (blank preserves length, so this is exact even for a value carrying an escaped quote — re-running the regex over raw text would not be). declinedIn keeps its RAW window deliberately: masking it would make it name `client: \\\"\\\"` for every entry it exists to quote. The dispatched premise HELD — the population move is zero on today's tree, not merely in counts but in the populations themselves: the sha256 of the full 259-row set and of the 176-row UNREACHABLE set are identical across the change, and the two --bridge-coverage runs differ by exactly one line, the new report reading 0. I also took the cheap half of direction 3, since the mask closes the hole by making a prose-quoted lead produce NOTHING and that is a new silence: parseLedgerSource now returns outsideCode, naming every lead the mask dropped, printed by --bridge-coverage on every run and entering NEITHER ratio and NO brokenScan — a comment quoting a retired path is legitimate prose, not a broken scan. Two residuals of the same asymmetry were measured and FILED rather than ridden along, each being a separate population move that wants its own before/after by this card's own standard: #10793 (typeDeclRegions still not applied to the recognizer — a literal-union route: type member still mints the identical silent phantom) and #10794 (declinedIn still reads raw — a comment-quoted route: in another spelling is billed as an unread row, raising a PARTIAL-read verdict and exit 1 on an accurate ledger; a false RED, the opposite direction, which is why it is triaged apart). parseRegistrarSource untouched per the fence. ⚠️ PR STATE CHANGED BY ANOTHER ACTOR, NOT REVERTED: I opened #10795 as a draft with auto-merge off, as dispatched (create response: draft=True). 40s later the timeline records `ready_for_review` by os-zhuang at 2026-08-21T11:48:16Z and `auto_merge_enabled` (merge_method 'merge') by os-zhuang at 11:48:23Z — my own writes on this PR are attributed to claude[bot] (the skip-changeset label at 11:47:37Z), so this was a different actor. The dispatch said explicitly not to enable auto-merge or mark it ready, so the PR is now on a path to land without the review the dispatch reserved. I did NOT revert either: reverting another actor's ready flip would silently destroy auto-merge and merge-queue membership, and correcting state I did not set is not mine to do under a shared identity. Flagging for your ruling — see open_questions.\",\n  \"tests\": \"All at HEAD 1146c948f2, clean tree; exit codes captured BEFORE any pipe (cmd > log 2>&1; ec=$?), and each gate quoted by ITS OWN verdict line.\\n\\nREPRODUCTION ON THE BASE (e072c97f76) — parseLedgerSource driven directly via a probe that is the real file with exactly two mechanical transforms (import path; the --self-test short-circuit body), diff shown to be 2 hunks. Card's exact fixture ⇒ 'rows 2 · routesDeclared 2 · clientsDeclared 1 · declined []' from a file declaring one row; rows: [{route:'GET /api/v1/gone',client:null},{route:'GET /api/v1/meta',client:'meta.getTypes'}]. Client-bound variant ⇒ 'rows 2 · clientsDeclared 2' with GET /api/v1/gone → meta.getGone. String-payload variant ⇒ rows 2. Same probe on this branch ⇒ rows 1 on all three.\\n\\nBEFORE/AFTER ON THE REAL TREE (both legs printed, not summarised). base: 'ledger rows read 259 of 259 declared / client-bound ledger rows 221 of 221 declared / reachable 45 / UNREACHABLE 176 / brokenScan []'. after: identical, plus 'prose-quoted leads (no row) . 0'. diff of the two --bridge-coverage outputs = exactly 1 added line. Per-ledger rows identical on all seven (56/96/79/11/3/4/10), declined 0 both legs. Population hashes, both legs: ALL rows count=259 sha256=1fd5cbc475a9a179ebfdaa8beaafeed784122d71f73f355565420cff87a8d48c; UNREACHABLE count=176 sha256=5c379536b41c5ca8acd6b91758f3858520fb62d2417065478181e4ee9375b799. DELTA ZERO.\\n\\nSELF-TEST: 312 → 323 cases, '✓ affected-docs self-test: 323 cases pass.' New cases pin BOTH directions (comment lead yields no row AND the code row keeps its value 'GET /api/v1/meta → meta.getTypes'; denominator drops it so no phantom gap; client-bound variant mints no phantom and the survivor keeps its own binding; string payload; and the coverage arm pins no brokenScan while still counting it).\\n\\nABLATION: `const code = codeOnly(text)` → `const code = text` (mask not applied). NO REBUILD LEG APPLIES and this is stated rather than skipped: it is a plain .mjs run from source by `node scripts/...`, nothing resolves through a package exports/dist, so there is no stale-artifact path for the mutation to hide in. MUTATION CONFIRMED ON DISK BY ANCHOR COUNT, never by an editor exit code: 'const code = codeOnly(text);' 2 → 1 (the second lives in declarationsIn) and the injected marker 0 → 1, file sha 983c6d50… → dca63717…. The FIRST attempt at this ablation failed its own 2-hit anchor assertion and wrote nothing (sha unchanged) — which is exactly why the count is the evidence. Predicted 11 red, written down before running; observed 11, the same cases in the same order. Notably 'a prose-quoted lead carries NO broken-scan verdict' stayed GREEN under ablation — the card's thesis reproduced: rows and routesDeclared move together, so the defect is silent. RESTORED: 'const code = codeOnly(text);' back to 2, zero ABLATION residue, file sha256 983c6d50828bfd9faf94aa2dd23d012e0616c739d28a004adb0a695b6378e9c2 — byte-identical to pre-ablation — and 323 cases pass again.\\n\\nGATES — derived by `node scripts/pm/dispatch-gates.mjs` with NO hand-supplied paths (it takes its own change set from the merge base: 'change set derived from git — 1 path(s) vs merge base e072c97f7'), re-derived after the final commit, set unchanged at 4 families. Union re-run at 1146c948f2:\\n  EXIT=0 pnpm check:docs-audit-scope → '✓ affected-docs self-test: 323 cases pass.' + '✓ check-audit-scope self-test: 24 cases pass.' + '✓ docs-accuracy-audit scope is in sync with content/docs/: 189 hand-written doc(s).'\\n  EXIT=0 pnpm check:cross-package-test-inputs → 'OK: 13 package(s) read outside themselves, all declared, and turbo.json hashes every declared glob.'\\n  EXIT=0 node scripts/check-cross-package-test-inputs.mjs → same verdict line\\n  EXIT=0 node scripts/docs-audit/check-affected-docs.mjs → '259 of 259 declared' / '221 of 221 declared' / 'prose-quoted leads (no row) . 0' / 'UNREACHABLE .. 176'\\n  EXIT=0 pnpm check:entry-guard → '✓ check:entry-guard: 127 scripts/ file(s) — every entry guard goes through invoked-as.mjs; 85 export bindings, 75 of them inert on import (10 known-unsafe, ⛔ SHRINK-ONLY).'  [run although the derivation did NOT name it, per the dispatch warning; this PR adds no entry point and no process.argv[1] guard]\\n  EXIT=0 pnpm check:nul-bytes → 'check-nul-bytes: OK (scanned 6238 text file(s) -- 6238 tracked, 0 untracked-not-ignored; skipped 5 binary; no raw ASCII control bytes).'\\nPlus a control-byte self-scan outside the gate over the changed file: grep -naP '[\\\\x00-\\\\x08\\\\x0b\\\\x0c\\\\x0e-\\\\x1f\\\\x7f]' → empty (exit 1).\\nNo changeset: gate tooling publishes nothing. skip-changeset applied additively by POST /issues/10795/labels; read back = ['size/m','skip-changeset'] (the bot's size label survived, which is the point of the additive write).\",\n  \"open_questions\": [\n    {\n      \"question\": \"#10795 was flipped ready_for_review and had auto-merge enabled by os-zhuang ~40s after I opened it as a draft, contrary to the dispatch's 'do not enable auto-merge and do not mark it ready; I will land it'. Do you want that undone, and is this automation you own?\",\n      \"options\": [\n        \"A. Leave it. If os-zhuang is your own PM automation or a repo workflow, the flip is intended and the PR lands when the gates go green.\",\n        \"B. You (not me) disable auto-merge and return it to draft, restoring the review you reserved. It has to be you or the actor that set it — reverting a ready flip is a one-step, silent destruction of auto-merge and merge-queue membership, and under a shared identity I cannot tell your write from anyone else's.\",\n        \"C. Leave auto-merge on but treat the gates as the reviewer: the change is confined to one dependency-free script, the population move is provably zero (both population sha256s identical), and CI reruns the whole gate farm regardless.\"\n      ],\n      \"recommendation\": \"B if the flip was not yours — the dispatch reserved the landing decision explicitly, and an unreviewed auto-merge is exactly what that reservation was for. But identify the actor first: os-zhuang is also #10683's assignee, so this is most likely your own seat or a repo automation, in which case A. Either way the decision is yours, not mine — I am reporting the state, not correcting it.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"filed as #10793: the recognizer now reads codeOnly but still not typeDeclRegions — a literal-union `route: 'GET /a' | 'GET /b'` TYPE member still mints a silent phantom row (measured rows 2 · routesDeclared 2 · declined 0 on a file declaring one; 0 live instances across all seven ledgers). domain:devx + tooling, unassigned, no pm:queue.\",\n    \"filed as #10794: declinedIn still reads raw text at both call sites, so a `route: \\\"GET /x\\\"` quoted in a comment is billed as an unread row — measured rows 1 · routesDeclared 2 · declined 1, which raises a PARTIAL-read verdict and exits 1 on an accurate ledger. A false RED (named and loud), the opposite failure direction from this card, which is why it is a separate card. 0 live instances. domain:devx + tooling, unassigned, no pm:queue.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
