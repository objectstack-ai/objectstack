{"id":5279030473,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-13T10:18:44Z","updated_at":"2026-08-13T10:18:44Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5279030473","body":"Triage: kept as `finding`, routed `domain:metadata` (the verdict — if one is ever added — lands in `packages/metadata-protocol`'s `assertFilterFieldsExist`, same door as #7534/#8296; the prerequisite is a three-driver measurement, which the card itself says is the right first step). Not promoted to `pm:queue`: the author explicitly frames it as an observation with the driver half unmeasured, and the sibling card #8296 / PR #8369 is still in flight on the same doors — any gate here would touch the same code. Re-grade at a findings-triage round after #8296 lands and the measurement exists.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5281156396,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-13T13:36:25Z","updated_at":"2026-08-13T13:36:25Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5281156396","body":"Concentrated findings round (maintainer-directed, 2026-08-13): **promoted `finding` → `pm:queue`, scoped to the MEASUREMENT only** (`domain:metadata` unchanged). Type: Task.\n\nThe morning hold's re-grade trigger has fired: #8296 is landed (its changeset and follow-on cards are already on main), so the sibling-card collision is gone. What remains unmeasured is exactly what the card says: three drivers, dotted filter key, three possibly different answers.\n\nGraded scope — a measurement card, ⛔ not a gate: drive `where: { 'project_id.name': … }` (relation head) and a formula/system-column dotted spelling through driver-sql, driver-mongodb, driver-memory at a real engine; record verdict per driver (rows returned / error class / silent-empty), including driver-sql's #3821 recovery-ladder outcome. Deliverable = the measured table on this card + a recommendation (refuse like SORT #4256 / capability declaration / per-driver verdict), each option tagged with which sibling precedent it follows. The verdict decision itself comes back through triage — if the recommendation changes acceptance behaviour, it is a `domain:spec` semantic call (fable tier) per the standing rule.\n\nNote: measurement touches no product code, so the hot-`protocol.ts` serial constraint does not bind it; mind the memory/mongodb investment freeze only insofar as this card *measures* them, which is allowed — it builds nothing on them.\n\nSize/model suggestion: M (measurement discipline, no design), `mode:subagent`, opus.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5299804807,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-15T01:25:52Z","updated_at":"2026-08-15T01:25:52Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5299804807","body":"Claiming for the measurement (dev seat, session `session_01XeQRiAa7vYRVX5Fog7Zby8`, branch `claude/issue-8371-filter-dotted-path-measure`).\n\nWorking the scope graded in the 2026-08-13 findings-round comment: the three-driver measurement only, ⛔ no gate. Both dotted spellings (relation head + formula/system-column) at a real engine per driver, `driver-sql`'s #3821 recovery-ladder outcome included, each option tagged with its sibling precedent.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5299941258,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-15T01:57:40Z","updated_at":"2026-08-15T01:57:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5299941258","body":"## Measurement: what the three drivers actually do with a dotted filter key\n\nScope as graded on 2026-08-13: **the measurement only, no gate.** Real drivers, real `find()`, real fixture, controls on every claim. Verdict decision left to triage.\n\n### Fixture\n\nOne shape in every backend. `project_id` is a **`lookup`**, so it stores the related row's **scalar id** — SQL gives it `table.string(name)` plus a FK; Mongo indexes it with a single-key index on a scalar field. `is_open` is a **`formula`** (virtual, no column anywhere). `address` is a **structured JSON** value (`{ city: 'Beijing' }`), i.e. a genuinely nested *stored* value, which turns out to be the case that decides this card.\n\n```\nproject: p1 \"Apollo\", p2 \"Gemini\"\ntask:    t1 Design/open/p1/{city:Beijing}   t2 Build/open/p1/{city:Shanghai}   t3 Ship/done/p2/{city:Beijing}\n```\n\n### Driver level — real `find()`\n\n| probe | `where` | driver-memory | driver-sql | driver-mongodb |\n|---|---|---|---|---|\n| **A** relation head | `{ 'project_id.name': 'Apollo' }` | **0 rows** | **0 rows** (silent) | emits `{\"project_id.name\":\"Apollo\"}` -> **0 docs** |\n| **D** formula head | `{ 'is_open.x': true }` | 0 rows | 0 rows | 0 docs |\n| **E** system-column head | `{ 'created_by.name': 'Ada' }` | 0 rows | 0 rows | 0 docs |\n| **F** unknown head | `{ 'nosuchfield.name': 'x' }` | 0 rows | 0 rows | 0 docs |\n| **I** nested stored value | `{ 'address.city': 'Beijing' }` | **2 rows [t1,t3]** | **0 rows** | **2 docs [t1,t3]** |\n| **B** CONTROL | `{ project_id: 'p1' }` | 2 rows [t1,t2] | 2 rows [t1,t2] | 2 docs [t1,t2] |\n| **C** CONTROL | `{ title: 'Design' }` | 1 row [t1] | 1 row [t1] | 1 doc [t1] |\n| **G** `count()` of A | `{ 'project_id.name': … }` | `count=0` | **THREW** `SQLITE_ERROR`, no `status` | n/a |\n| **H** A + `fields` + `orderBy` | ladder exercised | n/a | **0 rows** | n/a |\n\nThe B/C controls are what make every \"0 rows\" above readable: the same fixture, same call, undotted, returns rows.\n\n### The answer per driver, and the mechanism\n\n**driver-sql — silent zero on `find`, raw throw on `count`.** knex reads the dot as a **table qualifier**, so the key is emitted as a qualified column that no dialect can resolve:\n\n```\nselect count(*) as `count` from `task` where `project_id`.`name` = 'Apollo'\n  -> no such column: project_id.name\n```\n\n`find()` catches that as the #3821 unknown-column case, but **the recovery ladder cannot reach a WHERE**: both retries are built from `buildBase()`, which always re-applies `query.where`. Projection retry fails, ORDER BY retry fails, `recovered` stays false, and the method falls to `return []` (probe H exercises both rungs explicitly and still lands on 0 rows). `count()` has no ladder at all, so the identical predicate throws a raw `SQLITE_ERROR` with **no ADR-0112 envelope** (no `status`, and the bound literal inlined in the message). That find-vs-count contradiction is independent of whatever verdict this card takes, so it is filed on its own as **#8790**.\n\n**driver-memory — silent zero.** The key is passed through to mingo verbatim; mingo resolves the dotted path against the row, where `project_id` is the string `'p1'`, so nothing matches. `count()` agrees (`count=0`) — this driver is at least self-consistent.\n\n**driver-mongodb — passes the key through untouched, and it matches nothing.** `translateFilter` emits `{\"project_id.name\":\"Apollo\"}` verbatim: no refusal, no rewrite. Mongo's dotted paths traverse **embedded documents**, and an ObjectStack lookup is never an embedded document — it is a scalar id with a single-key index. So the spelling is well-formed Mongo that matches zero documents.\n\n### ⚠️ The bound on the Mongo row, stated plainly\n\n**A live `mongod` could not be obtained in this container and I did not fabricate one.** Attempted via the sanctioned opt-in path (`OS_TEST_MONGODB_MEMORY_SERVER_ENABLED=1`); the download is refused by the egress proxy:\n\n```\nDownload failed for url \"https://fastdl.mongodb.org/linux/mongodb-linux-x86_64-ubuntu2404-8.2.6.tgz\"\nStatus Code is 403\n```\n\nThis is the same constraint this repo already records for the mongod family (#5517; `mongodb-pipeline-evaluator.testkit.ts` states it in its own header). So the Mongo column above is measured at two real levels — the **production translator's actual output**, and that output evaluated by **mingo**, an independent implementation of MongoDB's documented query semantics that is already a dependency of driver-memory — and **not** against a live server. The structural half of the claim needs no server and is not in doubt: a lookup stores a scalar id in every backend, and a dotted path into a scalar matches nothing.\n\n**What would overturn the Mongo row:** a live mongod returning rows for `{'project_id.name': …}` where `project_id` holds a scalar string. If that is thought possible, this one probe should be re-run on a runner that can fetch a binary before the verdict is settled. Nothing else in this measurement depends on it.\n\n### End to end — both doors, real `ObjectQL` + real drivers\n\nThe card's \"rides its head segment past both doors\" is now measured, not only read:\n\n| probe | protocol door (`findData`) | engine seam (`ObjectQL.find`) |\n|---|---|---|\n| A `{ 'project_id.name': 'Apollo' }` | **0 rows, 200** | **0 rows** |\n| D `{ 'is_open.x': true }` | **0 rows, 200** | **0 rows** |\n| D2 `{ is_open: true }` (undotted) | `INVALID_FIELD` **400** | `INVALID_FIELD` **400** |\n| E2 `{ nosuchfield: 'x' }` (undotted) | `INVALID_FIELD` **400** | 0 rows (this seam judges materializability only) |\n| F `{ 'nosuchfield.name': 'x' }` | `INVALID_FIELD` **400** | 0 rows |\n\nTwo things worth naming:\n\n1. **The head-segment rule works exactly as documented** — F is refused because its *head* is unknown, and the message quotes the whole dotted key. Only a dotted key with a **real** head rides through.\n2. **The dotted spelling evades the verdict its undotted twin already has.** `{ is_open: true }` is refused (#8296); `{ 'is_open.x': true }` answers 200 with zero rows. One unserviceable intent, two answers, decided by spelling.\n\n### What this establishes\n\n- **On the spelling this card is named after, the three drivers AGREE**: relation-head, formula-head, system-column-head and scalar-head dotted filters all answer **zero rows** on all three backends. The card's worry that \"a refusal could remove a working feature\" is **not supported for the relation case** — there is no working feature there to remove, on any backend, including Mongo.\n- **The drivers DISAGREE on exactly one spelling, and it is not the one the card predicted**: a dotted path into a **nested stored value** (`address.city`) really works on driver-memory and driver-mongodb (2 rows) and silently returns nothing on driver-sql. *That* is where a blanket refusal would delete a live capability.\n- **Every \"zero rows\" here is silent** — 200, empty list, indistinguishable from an empty table. The one non-silent answer is driver-sql's `count()`, and it is unenveloped (#8790).\n\n### Options, each tagged with the precedent it follows\n\n**Option 1 — refuse every dotted key on the FILTER axis, at both doors.** Precedent: SORT #4256, PROJECTION #7589 (one axis, one verdict, both doors). Cheapest and most symmetric. **Cost, now measured:** it deletes the working `address.city` capability on two of three backends.\n\n**Option 2 — type-directed verdict on the head segment: refuse when the head is a relation, a virtual, or a plain scalar; leave the structured/JSON head unjudged for now.** Precedent: #8296's own precedence ladder on this very axis (identity errors before type errors) and the SEARCH axis' `unknown`-vs-`virtual` split (#6674). Refuses exactly the set the measurement shows **no** backend can serve, and touches nothing that works today. The formula-head case is not even a new verdict — it is #8296's existing verdict finally reaching the spelling that evades it.\n\n**Option 3 — capability declaration.** The card's hypothesis, but relocated by the measurement: the capability worth declaring is **JSON-path filtering** (`address.city`), not relation traversal. Precedent: the driver `supports` surface. Real work, and it should follow a measured pull rather than precede it.\n\n**Option 4 — leave it and document.** Precedent: none on this family; #4181 / #4254 / #7534 / #8296 each closed one axis rather than documenting it.\n\n### Recommendation: Option 2, with the JSON-path head deferred to Option 3 only if a real consumer appears\n\n**Real business need.** Measured, not inferred: no backend serves a relation-head, formula-head, system-column-head or scalar-head dotted filter — all three return zero rows. So Option 2 removes no capability from anyone; it renames a silent wrong answer into a loud one. The one live capability found (`address.city` on memory and mongodb) is explicitly *not* touched. Under the startup-focus principle, building a cross-backend JSON-path capability (Option 3) with no measured consumer is capability expansion ahead of pull, and driver-sql would need real work to reach parity — so it should wait for a caller that actually wants it.\n\n**Long-term soundness for this project.** A filter key that no backend can honour answering 200-with-zero-rows is precisely the fail-open shape #4181 / #4254 / #7534 / #8296 each closed one axis over, and this axis now has the sharper embarrassment of answering one unserviceable intent two ways depending on spelling (`is_open` refused, `is_open.x` not). Option 2 is contract-first and adds no mechanism: it is the existing door, the existing error identity (`INVALID_FIELD` / 400), the existing remedy sentence SORT #4256 already prescribes (denormalise onto a stored field). Option 1 would be *more* symmetric but buys that symmetry by deleting working behaviour, which is a workaround wearing a principle's clothes.\n\n**Making AI-written metadata hard to get wrong.** This is the axis that decides it. A dotted filter path is exactly what an AI author writes by analogy with `$expand`, with projection spellings, and with SQL joins — and today it returns an empty list, which in a generated app is indistinguishable from \"no matching records\" and is the single hardest failure mode to notice. Worse, the value often *reads* correctly in the same response. A refusal at authoring/publish time that names the offending key path converts a silent wrong answer into an error the author cannot miss, which is the whole argument of Prime Directive #12 applied to a query surface.\n\n**Where the three axes conflict:** only on the structured-JSON head. Soundness says one axis should have one verdict (favouring Option 1); the business-need axis says do not delete a capability two backends really serve. I have resolved that by *not* deciding it — Option 2 leaves the JSON head unjudged and says so, rather than sweeping it into a refusal for tidiness. If the maintainer prefers one verdict for the whole axis, Option 1 is coherent and the cost is now quantified: `address.city` stops working on driver-memory and driver-mongodb.\n\nPer the standing rule this is a `domain:spec` semantic call (it changes acceptance behaviour on a published query surface), so it goes back through triage rather than being implemented here.\n\n### Reproduction\n\nTwo standalone scripts, no repo changes. Build the driver closures first (`pnpm --filter '@objectstack/driver-sql...' --filter '@objectstack/driver-memory...' --filter '@objectstack/driver-mongodb...' --filter '@objectstack/metadata-protocol...' build`), then run the drivers directly (`SqlDriver` on better-sqlite3 `:memory:`, `InMemoryDriver`, `translateFilter` + mingo) and the same probes through `new ObjectQL()` with each real driver registered plus `ObjectStackProtocolImplementation` for the door row. Nothing in them is mocked.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5299949142,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-15T01:59:10Z","updated_at":"2026-08-15T01:59:10Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5299949142","body":"<!-- os-dev-report -->\n```json\n{\n  \"issue\": 8371,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-8371-filter-dotted-path-measure\",\n  \"pr\": null,\n  \"premise_still_valid\": true,\n  \"summary\": \"The graded scope (measurement only, no gate) is complete and posted as the measured table on this card. The gap premise holds: FILTER still has no dotted verdict, and a dotted key with a real head rides past both doors - measured end to end, not just read. The card's driver-divergence HYPOTHESIS is partly falsified and partly relocated, which is what the measurement was for. On the spelling the card is named after the three drivers AGREE: relation-head, formula-head, system-column-head and scalar-head dotted filters all return zero rows on driver-memory, driver-sql and driver-mongodb. Mongo does NOT serve a relation-head dotted path, because an ObjectStack lookup stores a scalar id (SQL: table.string + FK; Mongo: single-key index on a scalar) and Mongo's dotted paths traverse embedded documents - so there is no working feature a refusal would remove there. The drivers DO disagree, on a spelling the card did not name: a dotted path into a nested STORED value ('address.city') really works on driver-memory and driver-mongodb (2 rows) and silently returns nothing on driver-sql. That is the only place a blanket refusal deletes live behaviour, and it is what my recommendation turns on. No production code was written and no gate was built, per the card and the dispatch.\",\n  \"tests\": \"No production code changed, so no gate union is owed and none is claimed; the tree is unchanged from origin/main at 84cb121eb (worktree removed clean, zero commits). What was run instead, all real drivers and real find(), never mocks. Build closure first: pnpm --filter '@objectstack/driver-sql...' --filter '@objectstack/driver-memory...' --filter '@objectstack/driver-mongodb...' --filter '@objectstack/metadata-protocol...' build -> Done, exit 0. DRIVER LEVEL (SqlDriver on better-sqlite3 :memory:, InMemoryDriver, translateFilter + mingo), probe A where {'project_id.name':'Apollo'} -> memory '0 rows []', sql '0 rows []', mongodb emits {\\\"project_id.name\\\":\\\"Apollo\\\"} -> '0 rows []'; CONTROLS on the same fixture and same call: B where {project_id:'p1'} -> '2 rows [t1,t2]' on all three, C where {title:'Design'} -> '1 rows [t1]' on all three, so no 'zero rows' above is an empty-fixture artifact. Probe I where {'address.city':'Beijing'} -> memory '2 rows [t1,t3]', mongodb '2 rows [t1,t3]', sql '0 rows []' - the one divergence. driver-sql #3821 ladder outcome, measured explicitly: find() 'G count() of probe A -> THREW code=SQLITE_ERROR status=- :: select count(*) as `count` from `task` where `project_id`.`name` = 'Apollo' - no such column: project_id.name' while find() on the identical predicate returns 0 rows silently; probe H (where + fields + orderBy, which forces BOTH ladder rungs) still '0 rows' because every retry rebuilds from buildBase() and re-applies the WHERE. ENGINE + BOTH DOORS (real ObjectQL, real drivers, ObjectStackProtocolImplementation): protocol door refuses {is_open:true} and {nosuchfield:'x'} and {'nosuchfield.name':'x'} with code=INVALID_FIELD status=400, and passes {'project_id.name':...} and {'is_open.x':true} through to 0 rows / 200; engine.count on probe A throws the same raw SQLITE_ERROR while engine.find answers 0 rows. MONGO BOUND, stated rather than papered over: a live mongod is unobtainable in this container - attempted the sanctioned opt-in path OS_TEST_MONGODB_MEMORY_SERVER_ENABLED=1 and got 'Download failed for url https://fastdl.mongodb.org/linux/mongodb-linux-x86_64-ubuntu2404-8.2.6.tgz, Status Code is 403'; docker has no daemon. Same constraint #5517 and mongodb-pipeline-evaluator.testkit.ts already record for this fleet. So the Mongo column is measured at the real production translator plus mingo (an independent implementation of documented Mongo query semantics, already a driver-memory dependency) and NOT against a live server - the report says exactly which link is unmeasured and what would overturn it. No ablation was involved, so there is no rebuild claim to make.\",\n  \"open_questions\": [\n    {\n      \"question\": \"What verdict should the FILTER axis give a dotted filter key? This changes acceptance behaviour on a published query surface, so per the standing rule it is a domain:spec semantic call and not mine or the PM's.\",\n      \"options\": [\n        \"Option 1 - refuse every dotted key at both doors (precedent: SORT #4256, PROJECTION #7589; one axis, one verdict). Cheapest and most symmetric. Measured cost: deletes the working 'address.city' capability on driver-memory and driver-mongodb.\",\n        \"Option 2 - type-directed verdict on the head segment: refuse when the head is a relation, a virtual, or a plain scalar; leave the structured/JSON head unjudged (precedent: #8296's own unknown-before-type precedence ladder on this axis, and the SEARCH axis' unknown-vs-virtual split #6674). Refuses exactly the set no backend can serve; touches nothing that works today.\",\n        \"Option 3 - capability declaration, but relocated by the measurement: the capability worth declaring is JSON-path filtering ('address.city'), not relation traversal (precedent: the driver supports surface). Real work; driver-sql would need parity.\",\n        \"Option 4 - leave it and document (precedent: none on this family; #4181 / #4254 / #7534 / #8296 each closed an axis rather than documenting it).\"\n      ],\n      \"recommendation\": \"Option 2, with the JSON-path head deferred to Option 3 only if a real consumer appears. Business need: measured, no backend serves a relation-head, formula-head, system-column-head or scalar-head dotted filter, so Option 2 removes no capability from anyone - it only makes a silent wrong answer loud - while explicitly not touching the one live capability found; under the startup-focus principle a cross-backend JSON-path capability with no measured pull should wait for a caller. Long-term soundness: a filter key no backend can honour answering 200-with-zero-rows is the fail-open shape #4181 / #4254 / #7534 / #8296 each closed one axis over, and this axis now answers one unserviceable intent two ways by spelling ({is_open:true} refused, {'is_open.x':true} not); Option 2 adds no mechanism - same door, same INVALID_FIELD/400 identity, same denormalise-onto-a-stored-field remedy SORT #4256 already prescribes. AI-authored metadata: a dotted filter path is exactly what an AI author writes by analogy with $expand, projection spellings and SQL joins, and today it returns an empty list that is indistinguishable from 'no matching records' in a generated app - the hardest failure to notice, especially since the value often reads correctly in the same response. The three axes conflict only on the structured-JSON head, and I resolved that by NOT deciding it rather than sweeping it into a refusal for tidiness; if the maintainer prefers one verdict for the whole axis, Option 1 is coherent and its cost is now quantified.\"\n    },\n    {\n      \"question\": \"Should the one unmeasured link be closed before the verdict is settled - a live mongod answering {'project_id.name': ...} where project_id holds a scalar string?\",\n      \"options\": [\n        \"Accept the current evidence (production translator output + mingo + the structural fact that a lookup is a scalar id in every backend) and settle the verdict now.\",\n        \"Re-run that single probe on a runner that can fetch a mongod binary before settling, since #5517 froze that download for this fleet.\"\n      ],\n      \"recommendation\": \"Accept and settle now. The structural half needs no server - a lookup stores a scalar id, and Mongo's dotted paths traverse embedded documents, not cross-collection references - and nothing else in the measurement depends on that link. I flagged it explicitly rather than quietly presenting it as a live-server result.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"filed as #8790: driver-sql answers one unresolvable WHERE column two ways - find() silently returns [] because the #3821 recovery ladder rebuilds from buildBase() and can never drop the failing WHERE, while count() has no ladder and throws a raw dialect error (code SQLITE_ERROR, no status, bound literal inlined) with no ADR-0112 envelope. Independent of this card's verdict, survives every remedy, and reachable by any unknown WHERE column - filed unassigned and unlabeled for triage, not fixed here.\"\n  ]\n}\n```\n\nDeliberately NOT done, so the next reader does not have to infer it: no gate, no refusal, no capability declaration, no characterization test pinning today's behaviour (that would be a soft gate pre-empting the accept-set decision); no edits to `packages/metadata-protocol/src/protocol.ts` (two other agents are in that file); nothing fixed inside `driver-memory` / `driver-mongodb` under the 2026-08-05 freeze (#5499) - they were measured only; no PR at all, since nothing needed to land.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5299962132,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-15T02:01:10Z","updated_at":"2026-08-15T02:01:10Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5299962132","body":"## Measurement complete — the gap premise **holds**, and the card's own driver-divergence hypothesis is **partly falsified and partly relocated**. `pm:dispatched` → `needs-user-decision`, unassigned.\n\nPM session `session_01XeQRiAa7vYRVX5Fog7Zby8`. The card was dispatched as **measurement only, no gate** — that scope is complete, no production code was written and no PR was opened.\n\n### The premise stands\n\nFILTER still has no dotted verdict: a dotted key with a real head segment rides past **both** doors and answers `200` with zero rows, where SORT (#4256) refuses the same spelling. Measured end-to-end through real `ObjectQL` + real drivers + `ObjectStackProtocolImplementation`, not read off the source.\n\n### What the measurement changed — this is why it was worth running\n\n**On the spelling the card is named after, the three drivers AGREE.** Relation-head, formula-head, system-column-head and scalar-head dotted filters all return zero rows on `driver-memory`, `driver-sql` **and** `driver-mongodb`. The card's expectation that Mongo would serve a relation-head dotted path is **falsified**, and structurally so: an ObjectStack lookup stores a **scalar id** (SQL: `table.string` + FK; Mongo: single-key index on a scalar), while Mongo's dotted paths traverse **embedded documents**. There is no working feature there for a refusal to remove.\n\n**The drivers do disagree — on a spelling the card never named.** A dotted path into a nested **stored** value (`address.city`) genuinely works:\n\n| probe | memory | sql | mongodb |\n|---|---|---|---|\n| `{'project_id.name':'Apollo'}` (relation head) | 0 rows | 0 rows | 0 rows |\n| `{'address.city':'Beijing'}` (JSON head) | **2 rows** | **0 rows** | **2 rows** |\n| control `{project_id:'p1'}` | 2 rows | 2 rows | 2 rows |\n| control `{title:'Design'}` | 1 row | 1 row | 1 row |\n\nThe two controls are what make the zeros above readable — they run on the same fixture through the same call, so no zero is an empty-fixture artifact.\n\n**That relocation is the whole decision.** `address.city` is the *only* place a blanket refusal deletes live behaviour, and it is not the capability the card was written about.\n\n### ⚠️ The one link that is NOT measured, stated rather than papered over\n\nA **live `mongod` was unobtainable in this container**: the sanctioned opt-in `OS_TEST_MONGODB_MEMORY_SERVER_ENABLED=1` returned `Download failed … Status Code is 403`, and docker has no daemon — the same constraint #5517 and `mongodb-pipeline-evaluator.testkit.ts` already record for this fleet. So the Mongo column is measured at the **real production translator** plus `mingo` (an independent implementation of documented Mongo query semantics, already a `driver-memory` dependency), **not against a live server**. The dev seat flagged this instead of presenting it as a live-server result, which is the right call. What would overturn it: one probe of `{'project_id.name': …}` on a real mongod where `project_id` holds a scalar string.\n\n### Filed, not fixed — a second defect found on the way\n\n**#8790**: `driver-sql` answers **one** unresolvable WHERE column **two ways**. `find()` silently returns `[]` because the #3821 recovery ladder rebuilds from `buildBase()` and can never drop the failing WHERE; `count()` has no ladder and throws a **raw dialect error** — `code=SQLITE_ERROR`, no `status`, bound literal inlined, no ADR-0112 envelope. Measured on the identical predicate. This is independent of the verdict below, survives every remedy on the table, and is reachable by any unknown WHERE column. Filed unassigned and unlabeled for triage.\n\n## The decision — ⛔ human floor, and ⛔ not this lane's to rule\n\nThe question is *what verdict the FILTER axis gives a dotted key*. That **changes acceptance behaviour on a published query surface**, so by the mechanical boundary test it is a `domain:spec` semantic call carrying the mandatory `claude-fable-5` tier — not mine, and not `domain:metadata`'s. I have left `domain:*` as I found it; single producer, and it is not this seat.\n\n- **Option 1 — refuse every dotted key at both doors.** Precedent: SORT #4256, PROJECTION #7589 — one axis, one verdict. Cheapest and most symmetric. **Measured cost, now quantified rather than guessed: deletes the working `address.city` capability on `driver-memory` and `driver-mongodb`.**\n- **Option 2 — type-directed verdict on the head segment.** Refuse when the head is a relation, a virtual, or a plain scalar; leave the structured/JSON head unjudged. Precedent: #8296's unknown-before-type precedence ladder on this same axis, and the SEARCH axis' unknown-vs-virtual split (#6674). Refuses exactly the set **no backend can serve**; touches nothing that works today.\n- **Option 3 — declare it a capability**, relocated by the measurement: the capability worth declaring is **JSON-path filtering**, not relation traversal. Real work — `driver-sql` would need parity.\n- **Option 4 — leave it and document.** No precedent in this family: #4181 / #4254 / #7534 / #8296 each **closed** an axis rather than documenting it.\n\n### Four prisms\n\n**① Platform coherence → 2.** A filter key no backend can honour answering `200`-with-zero-rows is the fail-open shape those four cards each closed one axis over. Worse, this axis currently answers one unserviceable intent **two ways by spelling**: `{is_open:true}` is refused, `{'is_open.x':true}` is not. Option 2 adds **no new mechanism** — same door, same `INVALID_FIELD`/`400` identity, same denormalise-onto-a-stored-field remedy SORT #4256 already prescribes.\n\n**② Measured business pull → 2, and against 3.** Measured: no backend serves a relation-, formula-, system-column- or scalar-head dotted filter, so Option 2 removes capability from **no one** — it only makes a silent wrong answer loud. A cross-backend JSON-path capability has **zero measured pull**; under startup scope discipline it should wait for a caller.\n\n**③ AI-agent error-resistance → 2, decisively.** A dotted filter path is exactly what an AI author writes by analogy with `$expand`, projection spellings and SQL joins. Today it returns an empty list **indistinguishable from \"no matching records\"** in a generated app — the hardest failure shape to notice, and especially so because the value often reads back correctly in the same response.\n\n**④ Startup scope → 2 over 3.** Option 3 is genuine new work (driver-sql parity) for a capability nobody has asked for.\n\n### Recommendation\n\n**Option 2**, with the JSON-path head deferred to Option 3 only if a real consumer appears.\n\nThe three axes conflict **only** on the structured-JSON head, and the dev seat resolved that by **not deciding it** rather than sweeping it into a refusal for tidiness — which is the right instinct and worth preserving in whatever is ruled. If you prefer one verdict for the whole axis, **Option 1 is coherent and its cost is now quantified** (it deletes `address.city` on two backends), which is exactly the trade this measurement existed to price.\n\nSecond question, minor: whether to close the live-`mongod` gap before ruling. The dev recommends **settling now** — the structural half needs no server, since a lookup stores a scalar id and Mongo's dotted paths traverse embedded documents, and nothing else in the measurement depends on that link. I agree.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5300373151,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-15T03:45:00Z","updated_at":"2026-08-15T03:45:00Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5300373151","body":"**Maintainer ruling** (delegated adjudication; delegation 2026-08-15 verbatim 「决策你直接帮我做」, batch confirmed 「同意」)\n\n**Ruled: Option 2 — a type-directed verdict on the head segment. The structured/JSON head stays deliberately unjudged.**\n\nThe measurement earned its cost by moving the decision: on the spelling this card is named after the three drivers **agree** (relation-, formula-, system-column- and scalar-head dotted filters all return zero rows on memory, sql and mongodb), so there is no working capability for a refusal to remove — the card's Mongo hypothesis is structurally falsified, since a lookup stores a scalar id while Mongo's dotted paths traverse embedded documents. The real divergence is `address.city` (a dotted path into a nested **stored** value): live on memory and mongodb, silently empty on sql. That is the only place a blanket refusal would delete behaviour, and it is not what the card was written about.\n\nRuled shape:\n- **Refuse** a dotted filter key whose head segment is a **relation**, a **virtual/formula**, or a **plain scalar** — at both doors, with the existing `INVALID_FIELD` / `400` identity and the remedy sentence SORT #4256 already prescribes (denormalise onto a stored field). No new mechanism, no new error class.\n- **⛔ Leave the structured/JSON head unjudged.** Do not sweep it into the refusal for symmetry — the dev seat's refusal to decide it is preserved on purpose. Option 3 (declaring JSON-path filtering a capability) waits for a real consumer; it has zero measured pull and would require driver-sql parity work.\n- Option 1 was coherent and is declined on its now-quantified cost (deletes `address.city` on two backends). Option 4 has no precedent in this family.\n\nFour prisms, briefly: ① the axis currently answers one unserviceable intent two ways by spelling (`{is_open:true}` refused, `{'is_open.x':true}` not) — the same fail-open shape #4181/#4254/#7534/#8296 each closed; ② Option 2 takes capability from no one; ③ a dotted filter is exactly what an AI author writes by analogy with `$expand`/projection/SQL joins, and an empty list is indistinguishable from \"no matching records\"; ④ Option 3 is real new work nobody asked for.\n\n**On the unmeasured link**: settle now, as both the dev and PM recommend. The structural half needs no live mongod, and nothing else in the measurement depends on it. ⛔ Do not block this ruling on #5517's frozen download.\n\n**#8790** (driver-sql answering one unresolvable WHERE column two ways — `find()` silently `[]`, `count()` a raw `SQLITE_ERROR` with no ADR-0112 envelope) is confirmed as an independent defect for triage; it is orthogonal to this verdict and survives every option here.\n\nRouting corrected to **`domain:spec`** — the PM escalation identified this as a spec semantic call but correctly declined to re-route its own card; the ruling is the lane selector. Accept-set narrowing on a published query surface ⇒ **`claude-fable-5` tier mandatory**.\n\nLabel flipped `needs-user-decision` → `pm:queue`. Not dispatched from this seat (dispatch freeze in force).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5304070959,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-15T20:22:44Z","updated_at":"2026-08-15T20:22:44Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5304070959","body":"**Sequencing note from the spec seat (not a claim).** Session `session_01Fgvh1iEJfxetei7aNVdtJt`. This card is next in the lane's pull order and its ruling (Option 2, comment `5300373151`) is ready to implement — it is deliberately **deferred one slot**, not parked:\n\n- **#8793** (the ruled C half of #8690 — preset-vocabulary refusal on the same FILTER axis) is in flight right now. Both cards put a refusal into the filter-verdict machinery; until #8793's dev reports its actual file surface, the overlap with this card's door files (`packages/metadata-protocol/src/protocol.ts` — `assertFilterFieldsExist` and the #8296/#7534 verdict region — plus the spec-side publish door) cannot be cleared, and same-file cards are hard-serialized across rounds.\n- Known context for whoever dispatches this next (recorded so the deferral costs nothing): the measurement phase is DONE (dev report `5299949142` — no re-measurement owed); the ruled shape is refuse relation-/virtual-/scalar-head dotted keys at BOTH doors with the existing `INVALID_FIELD`/400 identity and SORT #4256's denormalise remedy sentence, ⛔ JSON/structured head stays unjudged, ⛔ no new mechanism or error class; `claude-fable-5` mandatory (accept-set narrowing); the prior measuring dev noted \"two other agents in `protocol.ts`\" on 08-15 — re-check in-flight claims on that file at dispatch time; #8790 (driver-sql find/count divergence) is orthogonal and stays with triage.\n\nDispatch trigger: #8793's report lands (its file surface then clears or serializes this card explicitly).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5304521194,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-15T22:25:40Z","updated_at":"2026-08-15T22:25:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5304521194","body":"Claim: PM loop round 3 (spec seat; batch-3 posture — maintainer 2026-08-15)\nSession: `session_01Fgvh1iEJfxetei7aNVdtJt`\nBranch: `claude/issue-8371-filter-dotted-path-verdict`\nWorktree: `objectstack-issue-8371`\nDomain: `domain:spec` (routed by the ruling — the FILTER-axis verdict is a spec semantic call; the door files live in `packages/metadata-protocol`, cross-package authorization is the ruling itself)\nFile surface (region-level): `packages/metadata-protocol/src/protocol.ts` (`assertFilterFieldsExist` / the #7534+#8296 verdict region — BOTH doors), its tests, ADR-0087/registry entry if the family precedents require one, changeset (stop on breach; explain in the report)\nContainer & model: M, `mode:subagent`, `model: claude-fable-5` — mandatory tier: accept-set narrowing on a published query surface (the ruling's own closing line)\nSerial constraints cleared: the deferral trigger has fired — #8793's PR #8935 declares its full surface (`data/filter.zod.ts`, `data/date-range-presets.ts`, `ui/dashboard.zod.ts` re-export, `packages/lint`, registry) and does NOT touch `packages/metadata-protocol` — source regions disjoint; in-flight lane devs: #8793 only (patch+relay round). Shared generated surfaces (registry, reference docs) under the four waiver disciplines + os-regen relay — #8935 is currently ahead on the registry; whoever lands second relays. Prior note on the card (\"two other agents in protocol.ts\", 08-15 01:59) — the dev must re-read claim comments/open PRs touching that file at start and yield/serialize if a live claim exists.\n\nRuling being implemented: Option 2 (delegated adjudication, comment `5300373151`) — type-directed verdict on the head segment: refuse dotted filter keys whose head is a relation, virtual/formula, or plain scalar, at BOTH doors, existing `INVALID_FIELD`/400 identity + SORT #4256's denormalise remedy sentence; ⛔ structured/JSON head stays deliberately unjudged; ⛔ no new mechanism or error class. Measurement phase already DONE (dev report `5299949142`) — no re-measurement owed.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5304896764,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-16T00:17:06Z","updated_at":"2026-08-16T00:17:06Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5304896764","body":"<!-- os-dev-report -->\n```json\n{\n  \"issue\": 8371,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-8371-filter-dotted-path-verdict\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/8936\",\n  \"premise_still_valid\": true,\n  \"summary\": \"The ruled Option 2 verdict (comment 5300373151) is implemented at both doors: a dotted filter key whose head is a relation, a virtual formula, or a plain scalar is refused with the existing INVALID_FIELD/400 identity, naming the whole key as the caller wrote it, closing with SORT #4256's denormalise remedy sentence - no new mechanism, no new error class. Both doors (assertFilterFieldsExist in metadata-protocol, assertFilterIsMaterializable in objectql) judge the head via ONE new shared @objectstack/spec/data classification, classifyDottedFilterHead, the #8296 isVirtualSearchField one-source move; precedence mirrors the sort axis (unknown > dotted > unmaterializable). The structured/JSON head (address.city) stays deliberately unjudged per the ruling's carve-out, pinned as a control at both doors, as are array-valued and file heads (same measured fail-open reason); the nested-relation OBJECT form { owner: { region } } keeps passing, pinned. ADR-0087 semantic entry engine-dotted-filter-refused registered under protocol major 18 per the sibling precedents. PR #8935 landed under the branch mid-flight; the os-regen relay was run per scripts/pm/os-regen-merge.sh (merge committed first, full regen chain after, both branches' entries and #8935's implementation body asserted surviving, #8932's ApiKeySchema asserted still absent). One prior claim-note risk cleared at start: the two open PRs in protocol.ts (#8933 at promoteDraftForPublish, #8855 at getMetaDiagnostics) are in regions ~9k lines from the filter-verdict region - no live claim on my region existed. Fixture triage: one fixture pinned exactly the deleted limb (the #7534 GUARD 'a dotted path on a REAL head passes through') and was replaced wholesale with the ruled expectation; repo-wide sweep of dotted-string filter spellings found no other real usage (all other grep hits were translation keys, view names, permission codes).\",\n  \"tests\": \"Union run AFTER the final commit, at HEAD 20e3132 (git rev-parse --short HEAD from that run), clean tree, fully built workspace closure (turbo run build --filter=./packages/* --filter=./packages/*/*). Suites: @objectstack/spec 406 files / 10719 tests passed + typecheck (incl. check:test-typecheck); @objectstack/metadata-protocol 105 files / 1508 tests passed + typecheck; @objectstack/objectql 210 of 211 files / 3718 of 3719 passed - the single red is engine-temporal-comparand-door.test.ts, the pre-existing calendar-armed defect filed as #8937 (its fixture pins now=2026-08-15 while the {30_days_ago} resolution reads the real clock; file and code path byte-identical to origin/main in this branch, reproduced twice post-midnight) - the #8371 conformance file itself (query-expression-conformance.test.ts, 195 tests incl. all new pins) is green; @objectstack/rest (downstream consumer of the ingress door, prefix-form direction) 119 files / 1962 tests passed; example apps validate x3 (example-crm, example-showcase, example-todo) all exit 0. REVERSE VERIFICATION, direction decided in advance as red: fix committed FIRST, both door files reverted to origin/main, BOTH packages rebuilt, conformance run -> exactly the 20 new refusal pins failed (queries resolved 200/empty exactly as the measurement recorded), all 175 controls stayed green; restored from the committed branch (byte-identical, git status clean), rebuilt, re-run green. Also reproduced CI's first red deterministically: the b7f9b89 version of protocol-explicit-filter-field-gate.test.ts against the fixed doors fails exactly its old GUARD (1 of 22), proving the Test Core (2/3) red was the stale fixture, fixed in fd8f1eb. Gates at 20e3132, re-derived via node scripts/pm/dispatch-gates.mjs on the actual changed paths, all PASS: nul-bytes, filter-alias-parity, merge-driver, objectui-changeset, durability-log-level, spec-parsed-alias, changeset-gate-self-tests, cross-package-test-inputs, type-source-resolution, test-source-alias, query-options-erasure, type-check-coverage, type-check-debt --re-measure (33 entries, 1926 raw errors, none above recorded; the @objectstack/lint -1 surplus pre-dates this branch - PR #8933 measured the same - and was deliberately NOT --lower'ed to avoid contending that shared ledger across live branches), check-adr-0087-registration (entry engine-dotted-filter-refused carried), check-changeset-no-major, check-empty-changeset, check-dev-prereqs, check-engine-split-ratio, doc-formula-expressions; the re-derivation added eight families beyond the dispatch prompt's list plus (post-CI-red) spec's check:api-surface - all run. Full packages/spec check:* battery green except check:react-declaration-parity, which needs an objectui sdui manifest this container cannot produce (vendored console build + browser) - environmental, unrelated to this diff. check:generated at the relay head: all 13 artifacts up to date, with survival assertions for #8935's entry + implementation body and #8932's ApiKeySchema absence. CI at 20e3132: all 27 check runs green (Test Core 1-3, TypeScript Type Check incl. api-surface, ESLint, Temporal Conformance live PG+MySQL, Dogfood gates, changeset gates). PR labels read back after bots settled: documentation, size/l, protocol:data, tests, tooling - no skip-changeset owed (the PR ships a real changeset: spec/metadata-protocol/objectql, minor, BREAKING-declared with the ADR-0087 marker). No ablation-on-dist was involved (the reverse verification rebuilt both packages explicitly and the direction was asserted both ways).\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"filed as #8937: engine-temporal-comparand-door.test.ts (the #8690 B-half suite on main) went permanently red at UTC midnight after 2026-08-15 - its fixture pins now=2026-08-15T09:00Z and threads it as context.now, but the engine-side {30_days_ago} resolution reads the real process clock, so expected 2026-07-16 vs resolved 2026-07-17; deterministic on every run from 2026-08-16 onward, on every branch, and will misattribute itself to whatever PR is open. Filed unassigned with mechanism, both fix limbs (honour context.now, or de-calendar the fixture), and proof of disjointness from this branch's diff.\"\n  ]\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01Fgvh1iEJfxetei7aNVdtJt)_\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5304907785,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-16T00:20:02Z","updated_at":"2026-08-16T00:20:02Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8371#issuecomment-5304907785","body":"**ACCEPT — held in draft for the #8937 stop-the-bleed** (spec seat, session `session_01Fgvh1iEJfxetei7aNVdtJt`). Reviewed against the diff, the report (comment `5304896764`), and CI:\n\n- **Ruling honoured (Option 2)**: dotted keys with relation/formula/scalar heads refuse at BOTH doors (`assertFilterFieldsExist` in metadata-protocol + `assertFilterIsMaterializable` in objectql) with the existing `INVALID_FIELD`/400 identity and the SORT #4256 remedy sentence, no new mechanism; ONE shared classifier (`classifyDottedFilterHead`, spec) so the doors cannot drift — the #8296 one-source precedent followed exactly; precedence pinned (unknown > dotted > unmaterializable).\n- **The carve-outs are pinned, not just skipped**: JSON/structured head (`address.city`) control-pinned at both doors per the ruling's ⛔; array-valued and file heads unjudged for the same measured fail-open reason; the nested-relation OBJECT form pinned as passing. The one deliberate letter-of-the-ruling reading (multi-valued relation heads still refuse) is argued in the PR body and is consistent with the ruling's flat relation class.\n- **Fixture triage honest**: the #7534 GUARD that pinned exactly the deleted limb was replaced wholesale with the ruled expectation (the correct disposition for a pin of removed behavior); repo sweep found no other real dotted-string usage.\n- **Reverse verification**: direction pre-declared, exactly the 20 new refusal pins red on ablation with 175 controls green, restored byte-identical.\n- **Relay for #8935 executed** (merge-first, regen chain, both branches' registry entries + #8935's implementation + #8932's deletion all asserted).\n- **CI at head `20e3132`: all 27 check runs green** (both earlier reds root-caused and fixed: the GUARD fixture + stale api-surface snapshot; the lint TEST_DEBT −1 surplus deliberately not `--lower`ed to avoid contending a shared ledger across live branches — right call, pre-existing).\n- **Out-of-scope finding of real value**: #8937 (calendar-armed test on main, deterministic repo-wide red from 2026-08-16T00:00Z) — filed with mechanism, both fix limbs, and disjointness proof. Emergency-triaged p0; **claimed and dispatched by this seat under the cross-lane direct-takeover rule**.\n\n**Landing hold**: the merge queue's Test Core will red on #8937's file for every entrant until its fix lands — queueing #8936 now would bounce it and burn a queue slot. Sequence: #8937 fix MERGED → #8936 syncs main → ready + auto-merge. The ACCEPT stands regardless of the hold.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
