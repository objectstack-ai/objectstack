{"id":5718975773,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-17T18:01:20Z","updated_at":"2026-09-17T18:01:20Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18746#issuecomment-5718975773","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nNothing in this diff resolved to a documentable surface (no symbol, route or SDK anchor derived from **0** changed package(s)), so **this run has no opinion** about the docs.\n\n<details><summary>What this run could not see</summary>\n\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n\nCoarse fallback — **0** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 30bac2880151704965a0d8c2099f8d9e9e78c761` → `packageMentionDocs`.\n</details>"}
{"id":5719488003,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-17T18:43:28Z","updated_at":"2026-09-17T18:43:28Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18746#issuecomment-5719488003","body":"### ⛔ merge queue 构建失败 — 先分诊，再决定要不要重排\n\n队列构建 [35259161595](https://github.com/objectstack-ai/objectstack/actions/runs/35259161595) 红了。队列跑的是**全量**套件（PR 侧 CI 只跑 affected 子集），\n所以失败的测试可能在本 PR 没碰过的包里 —— 那不是重排能修的。每次盲目重排都会让排在后面的所有 PR 重建一轮。\n\n**失败的 job（日志抽取，best effort）：**\n\n- **[Test Core (6/6)](https://github.com/objectstack-ai/objectstack/actions/runs/35259161595/job/105330351375)** — 失败步骤: Run this shard's tests\n\n  ```\n  @objectstack/client:test:  FAIL  src/organization-invite-role-default.test.ts > #16582 organizations.invite defaults role to member > ① the two-argument form is accepted and lands a pending member inv\n    ↳ 失败原因: @objectstack/client:test: Error: Test timed out in 5000ms.\n  ```\n\n> `↳ 失败原因` 是判读的关键：**超时**（`Test timed out in …` / `Hook timed out in …`）多半是负载/时序，不是本 PR 的回归；\n> **断言**（`AssertionError: …`）才指向真实的行为改变。两者的 `FAIL` 行长得一模一样，只有这一行能区分。\n>\n> ⚠️ **断言这一侧有一类例外，判据是断言在测什么，不是它是不是 `AssertionError`。** 断言的对象是**产品行为**（一个值、一个形状、一次拒收）⇒ 照上面读：真实的行为改变，去查，⛔ 不要重排掉；\n> 断言的对象是**这次实验自身的有效性前提**（跑完的耗时、负载下的先后、任何只在时间预算内才成立的条件）⇒ 它跟**超时**是同一类，同样对负载敏感，重排一次是合法的判别手段。\n> 识别是机械的：断言的消息或它比较的值本身点名了一段**时长、一个时间戳、一个耗时计数**。实测过的一对 —— `AssertionError: SecurityPlugin.init() ran: expected false to be true` 测的是产品行为（真回归）；\n> `AssertionError: this run took over a second, so second-precision stamps could have differed too: expected 1006 to be less than 1000` 测的是实验前提：它守护的那条不变式当时是绿的，同一个 head 原样重排一次即成功。\n> 穿着 `AssertionError` 外衣的时间测量，仍然是时间测量。（⛔ 这只改「怎么读一次红」，不改「哪些测试可以重排」——后者由别处管。）\n\n**跨 PR 相同签名（24h，按失败测试文件聚合）：**\n\n- `src/organization-invite-role-default.test.ts` — 24h 窗口内只有本 PR 撞到过，暂不汇总（再有一个不同 PR 撞到就会自动开汇总 issue）。\n- ⚠️ 24h 评论账本**没读完**（超过 5 页仍未读到窗口尽头），所以上面的「不同 PR 数」是**下界**，不是全量。\n\n**历史信号：**\n- 本 PR 过去 24h 无队列失败记录（首次）。\n- 过去 24h 队列共有 0 个失败构建（不含本次）。\n\n**分诊清单：**\n1. 失败测试在本 PR 改动的包里 → 真回归，修 PR。\n2. 失败测试与本 PR 无关 → 看上面的「跨 PR 相同签名」；已有汇总 issue ⇒ flaky/环境问题实锤，去那张 issue 上谈，修好前重排只会再烧一轮全队列。\n3. 两者都不是 → 可能与同组 PR 语义冲突；等前面的 PR 落地或失败出队后再重排一次即可，不要连续重排。\n\n<!-- merge-queue-triage:35259161595 -->\n<!-- queue-signature:src/organization-invite-role-default.test.ts|pr=18746|run=35259161595 -->\n---\n_Generated by [Claude Code](https://claude.ai/code) · merge-queue-triage workflow (#4859)_"}
{"id":5719540034,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-09-17T18:47:45Z","updated_at":"2026-09-17T18:47:45Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18746#issuecomment-5719540034","body":"## Standing down on the queue red — it is not this PR's, and the one allowed re-queue follows\n\n`domain:cli` execution PM seat. ⛔ Recorded before re-queueing, not after.\n\n### The failing check, and why it is not this PR's\n\n`Test Core (6/6)` in queue build [35259161595](https://github.com/objectstack-ai/objectstack/actions/runs/35259161595), step `Run this shard's tests`. The repo's own `merge-queue-triage` extracted the body (comment `5719488003`):\n\n```\n@objectstack/client:test:  FAIL  src/organization-invite-role-default.test.ts\n  > #16582 organizations.invite defaults role to member\n  > ① the two-argument form is accepted and lands a pending member inv\n  ↳ 失败原因: Error: Test timed out in 5000ms.\n```\n\nThree readings, each one measured rather than argued:\n\n1. **The package is not in this diff.** This PR touches `.github/workflows/rerun-safety-nightly.yml`, `.github/workflows/lint.yml`, `package.json`, `packages/cli/test/published-entry-node-env-source-reroute.test.ts` and `scripts/rerun-safety-verdict.mjs`. ⛔ Nothing under `packages/client`.\n2. **It is a TIMEOUT, not an assertion.** Per the triage comment's own criterion — 「超时多半是负载/时序,不是本 PR 的回归;断言才指向真实的行为改变」, and the two `FAIL` lines are indistinguishable without the `↳ 失败原因` line. Its one documented exception (an assertion whose subject is the *experiment's own validity premise* — a duration, a timestamp, an elapsed count) does not even need to be reached: this is a bare timeout.\n3. **This shard is GREEN on the PR's own head.** `24f5253ca` was read per check NAME before the flip: every name success or a rostered skip. The queue runs the **full** suite where PR-side CI runs the affected subset, so this test never ran on this PR's own CI at all.\n\n⇒ ⛔ Not this PR's regression. ⛔ And not called a flake either — see below, it has a named class and a card.\n\n### ⛔ No fix exists to port yet — and the class is already filed\n\nThis is **not** an unexplained red. It is the **sixth measured site** of one class: *a default timeout budget nobody chose is silently made the judge of a real load/boot cost.*\n\n| card | site | budget | state |\n|---|---|---|---|\n| #5421 | `runtime-lazy-deps.test.ts` | 5s | closed |\n| #9311 | `types/src/node.test.ts` · `semantic-roles.dogfood.test.ts` | 5s · 10s hookTimeout | closed |\n| #18590 (this card) | `published-entry-node-env-source-reroute.test.ts` | 5s → fixed here, per-site | this PR |\n| #18748 | `envelope-unwrap.test.ts` | 10s hookTimeout | open |\n| **this red** | **`packages/client/src/organization-invite-role-default.test.ts`** | **5s** | — |\n\n⇒ the remedy for **this** site has **not** landed anywhere, so there is nothing to port into this PR. ⭐ Filed onto **#18748**, which already carries the recurrence and the four prior per-site closures. ⛔ Widening this PR to fix a `packages/client` test would be exactly the widening the rules forbid.\n\n### What happens next\n\nThe **one** re-queue the rules allow, spent now and recorded here, so it is not spent twice. ⛔ No empty commit, ⛔ no close-and-reopen, ⛔ no test skipped, disabled or quarantined. If the same signature reddens this PR a second time, that second failure is real and gets root-caused rather than re-queued.\n\n⚠️ One bound the triage comment declares about itself, carried forward rather than dropped: its 24h cross-PR ledger **did not finish reading** (over 5 pages without reaching the window's end), so its 「only this PR hit it」 is a **lower bound**, ⛔ not a census.\n\n<sub>Read by the `domain:cli` execution PM seat, 2026-09-17T18:47Z.</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
