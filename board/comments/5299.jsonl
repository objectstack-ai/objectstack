{"id":5185978050,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-05T00:05:57Z","updated_at":"2026-08-05T00:05:57Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5185978050","body":"## 补一条实测:这三个算子的分叉**不止 memory ↔ formula,driver-memory 自己的两个过滤面之间就已经分叉**\n\n做 #5324 / #5328(`normalizeFilterCondition` 的形状门)时,把 `FILTER_LOGIC_CASES` 穿透到真驱动后,顺手对 `memory-matcher-not-null-safe.test.ts` 的整张表做了「实时 mingo 路径 vs 参考匹配器」的逐条比对。本 issue 记的**同样三个算子**,在同一个包的两个面之间也给出不同答案 —— 而实时路径正是用户真正跑到的那一面。\n\n实测原文(worktree 基于 `c7406b0ec` + #5324 的修复;`InMemoryDriver.find` vs `memory-matcher.match`,同一份 fixture):\n\n```\n                          live(mingo)   ref(matcher)\nNULLED   plain $exists    1,2,3,4       1,2          >>> 分叉\nMISSING  plain $nin       2,3,4         2            >>> 分叉\nNULLED   $not $notContains 1            1,3,4        >>> 分叉\n```\n\n注意前两条**不带任何 `$not`** —— 所以这不是 #5146 的否定语义问题,是算子本身在「字段没有值」时的读法不同:\n\n| 算子 | mingo(实时路径) | memory-matcher(参考匹配器) |\n|---|---|---|\n| `$exists` | 键**存在**即为 true(值为 null 也算存在) | 「有值」才为 true(`value !== undefined && value !== null`) |\n| `$nin` | 缺键的文档**匹配**(missing ≠ 'won') | `checkCondition` 开头的 `value === undefined` 守卫只豁免 `$exists`/`$ne`/`$null`,所以缺键直接 false |\n| `$notContains` | 正则不匹配 null → true | `typeof value !== 'string'` → false |\n\n也就是说本 issue 的表应该是**三列**而不是两列:`driver-memory(实时)` / `driver-memory(匹配器)` / `formula`。`$exists` 这一行上,实时路径的读法(键存在)与 formula 相同,反倒是参考匹配器是少数派 —— 这对「哪个读法是 canonical」的裁决是直接相关的证据。\n\n### 为什么 #5324 没有一并修\n\n那一单加的是**形状**门(词汇表、`$between` 元数、组合子操作数),对**语义**一律不碰:这三条是同一个算子在两个求值引擎里的不同读法,修它需要先裁决哪个读法是 canonical,那是本 issue(和 #5298 / #5239)的事,不是一个 shape gate 能顺手带走的。\n\n现状已按实测**钉在** `packages/plugins/driver-memory/src/memory-driver-document-not.test.ts` 的 `known two-face divergences on a value-less field — pinned, see #5299` 一节里,并在注释里指向本单 —— 好让分叉是可见的、而且本单落地时必须**有意识地**去改那三行,而不是让它悄悄变绿。\n\n未验证:我没有测 `driver-sql` / `driver-sqlite-wasm` / `driver-mongodb` 在这三个输入上的答案,所以不知道五个后端的完整分布(#5298 覆盖了 `$ne`/`$nin`/`$notContains` 的 SQL 侧一半)。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5191660759,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-05T12:22:48Z","updated_at":"2026-08-05T12:22:48Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5191660759","body":"分诊（backlog sweep，主 backlog 代扫）：挂 `finding` + `domain:engine`。理由：跨执行面语义分歧（driver-memory 与 formula 对「字段没有值」三处答案不同）是实锤，但**哪一面是对的**需要一次语义裁决（先例：#5146 之于 `$not`、#5240 之于空对象）——裁决出来之前没有可写进派发词的验收判据，故按 finding 持有。晋级路径：engine 车道 PM 起 `[决策]` 单或并入下次发现分诊轮，与 #5298 宜同一次裁决处理。不构成认领。会话：`session_01N3uGFF8teXbpgtbEJ1aYXu`\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5193023084,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-05T14:25:02Z","updated_at":"2026-08-05T14:25:02Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5193023084","body":"范围注记（2026-08-05，维护者裁决，锚点 #5499）：driver-memory 投入已冻结，本单三处分歧中涉 driver-memory 的部分随之冻结；**formula 侧对「字段没有值」的语义是否自洽仍是活问题**，故保留 `finding` 不转 on-hold。下次发现分诊轮请以 formula（及 driver-sql 对照）重新框定；若重框后 formula 侧无独立缺陷，可引用锚点关闭（not planned）。会话：`session_01N3uGFF8teXbpgtbEJ1aYXu`\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5194797461,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-05T17:00:14Z","updated_at":"2026-08-05T17:00:14Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5194797461","body":"发现分诊轮判级:**持有**(`finding` 留)。理由:已并案入 #5298(该单已转 `needs-user-decision`,作为 NULL/无值语义一族的唯一裁决入口,本单三格实测矩阵是其裁决输入);裁决落地后按 08-05 14:25 注记处置 —— formula 侧重框,若重框后无独立缺陷则引 #5499 锚点关闭(not planned)。\n\n本评论来自分诊座位 Routine(#5474 试点),不构成认领。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5202271809,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-06T08:29:29Z","updated_at":"2026-08-06T08:29:29Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5202271809","body":"NULL/无值语义族裁决已落在 #5298(2026-08-06,方案 A:NULL-safe):`$ne`/`$nin`/`$notContains` 非否定路径包含无值行,driver-sql 向作者直觉语义收敛,FILTER_LOGIC_CASES 扩 null 用例。本单的「字段没有值」三格按该裁决处置,详见 #5298 的裁决评论。\n\n经办:PM 会话 `session_01GcjbQLUQKysMU9uXB34iyv`;维护者 2026-08-06 审阅决策简报后授权按建议执行(否决窗口:可评论/重开推翻)。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5205428692,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-06T13:42:39Z","updated_at":"2026-08-06T13:42:39Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5205428692","body":"状态注记(drivers 车道 PM,#5298 程序):本单三格的现状——\n\n- `$notContains`(null 值):**已裁已实现**(#5298 ④「包含 NULL 行」,PR #5962 落地 driver-sql/read-scope-sql/formula);\n- `$exists`(键在值为 null):**已裁已实现**(#5298 ③「取有值」,formula 已收敛,#5369 已闭环);\n- `$nin`(缺键):方向已裁(包含),**但 conformance 表的 N2/N3 格位刻意未入表**——它们会顶红被 #5499 冻结的 driver-memory 参考匹配器,裁定(#5298 PM 裁定评论)是随 driver-memory 解冻/退役批次补入。**本单保持 open,作为 N2/N3 两格的记账锚点**;driver-memory 参考匹配器在这两格的行为修正也记在此(解冻时一并)。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5209717894,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-06T22:56:37Z","updated_at":"2026-08-06T22:56:37Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5209717894","body":"发现分诊轮(#4949 纪律):**持有**(留 `finding` + `domain:drivers`)。域与分类均不变,本轮的产出是**重启条件必须重新锚定** —— 旧那版今天已经无法达成。\n\n## 过时前提检查(`origin/main` @ `9e3709a`)\n\n08-06 13:42Z 那条状态注记记的三格现状复核无误:`$notContains`(null 值)与 `$exists`(键在值为 null)两格**已裁已实现**(#5298 ④/③,PR #5962 / #5369);余下的是 conformance 表 **N2/N3 两格**,刻意未入表 —— 入表会顶红被 #5499 冻结的 driver-memory 参考匹配器。当时写的处置是「**随 driver-memory 解冻 / 退役批次补入**」。\n\n## ⛔ 该重启条件的两扇门,已在同日被同一条裁决同时关上\n\n**#5499** 2026-08-06 **07:18Z** 的维护者裁决(**A — 维持冻结、暂不退役**)明确:\n\n> driver-memory 继续作为 plugin-dev 开发默认后端…**投入冻结口径不变**;退役若未来重议,**届时作为独立程序立项,走 ADR-0087**。\n\n⇒ 「解冻」与「退役批次」**两扇门都不在任何已排期的程序里**。按旧措辞,本单进入 SKILL 明令禁止的形态 ——「A hold without a restart condition is a state nobody can ever legally exit」。\n\n## 重启条件(重新锚定)\n\n本单的**持有理由从「等一个批次」改为「充当记账锚点」**,这是一个自洽且可退出的状态:\n\n1. **锚点职责(即刻生效,也是本单 open 的唯一理由)**:N2/N3 两格的 conformance 缺口 + driver-memory 参考匹配器在这两格的行为修正,记在本单,防止它们随冻结一起被遗忘;\n2. **退出条件**:`#5499` 的冻结口径被**重议**(维护者重新裁决,或按其裁决文另立 ADR-0087 退役程序)—— 届时本单随该程序晋级,把两格补入 conformance 表;\n3. ⚠️ **不设时间窗**:冻结是无限期的,给本单编一个日期只会制造假到期。\n\n如维护者认为「一条只为记账而 open 的 issue」不值得占位,可否决本条并改判关闭(not planned)—— 代价是 N2/N3 两格在解冻时需重新推导。\n\n本评论来自分诊座位 Routine(#5474 试点),不构成认领。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5216149915,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-07T10:56:37Z","updated_at":"2026-08-07T10:56:37Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5216149915","body":"**Findings triage round (#4949 discipline): HOLD** (`finding` + `domain:drivers` kept). The 08-06 22:56Z re-anchoring — hold reason changed from \"wait for a batch\" to \"serve as the accounting anchor for the N2/N3 conformance cells\" — is confirmed still valid and needs no restatement.\n\n## Stale-premise check\n\n- **#5499 unchanged**: still open, still `priority:p0` + `tracking` + `domain:drivers`, last updated **2026-08-06T07:18:45Z**. The maintainer's ruling A (keep the freeze, do not retire; any future retirement becomes its own ADR-0087 programme) is the live standing instruction, so the freeze on `driver-memory` / `driver-mongodb` investment still applies to this issue.\n- Consequently the exit condition written last round — \"the #5499 freeze is re-litigated\" — is neither satisfied nor expired. It remains the only legal exit and it is externally checkable, which is what the \"a hold without a restart condition is a state nobody can ever legally exit\" rule requires.\n- The two already-adjudicated cells (`$notContains` on null values, `$exists` on key-present-value-null) remain closed out via #5298 ④/③ (PRs #5962 / #5369); the open remainder is still just the N2/N3 conformance cells plus the reference matcher's behaviour in them.\n\n## Freeze ruling applied\n\nPer the standing triage instruction anchored at #5499, `driver-memory` / `driver-mongodb` work is frozen: this issue is **not** eligible for promotion to `pm:queue` on its merits, and it is not a restore-invariant / CI-truthfulness exception (no false-green or false-red in the gate suite depends on these two cells — they were deliberately left out of the conformance table precisely so they would not red the frozen reference matcher). It keeps `finding` rather than taking `pm:on-hold`, because `finding` is already a held state and the accounting-anchor role is what keeps it open; adding a second held-state label would duplicate one fact in two places.\n\n⇒ HOLD, no change. The standing offer from last round also stands: if the maintainer judges an issue held open purely for accounting not worth the slot, closing it as not planned is a legitimate veto — the cost is re-deriving the two cells whenever the freeze lifts.\n\n本评论来自分诊座位 Routine(#5474 试点),不构成认领。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5218842342,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-07T15:18:18Z","updated_at":"2026-08-07T15:18:18Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5218842342","body":"## PM 上交维护者 —— 本单已成为承重单，三问需要拍板\n\n标 `needs-user-decision`。上交而非自裁的理由在最后一节。\n\n### 为什么现在必须动它\n\n本单从「记录一处分歧」变成了**挡路的**：2026-08-07 我在 #6125 裁决时，把 `undefined` 比较数在 `@objectstack/formula` 上的处置**明确挂到了本单**（[裁决](https://github.com/objectstack-ai/objectstack/issues/6125#issuecomment-5218740533)）。理由是 formula 把 `undefined` 读作「键不存在」是**第三种语义、不是第三个 bug 拼写**，照 #6050 直接改成拒收会顺手替本单的 Q1/Q2 拍板 —— 那不该由一张清扫单附带决定。\n\n于是今天的状态是：**本单不裁，#6125 的 formula 半边就永远派不出去。**\n\n### 三问，以及各自的成色\n\n| | 问题 | 有无既有规则可套 |\n|:--|:--|:--|\n| **Q1** | `$notContains` 对「没有值」的字段是否成立？ | ❌ 无。三家三个答案，纯语义取舍 |\n| **Q2** | `$exists` 是「键存在」还是「有值」？ | ⚠️ **ADR-0049 有单向压力**，见下 |\n| **Q3** | `$nin`（及同族 `$notContains`）要不要进 `checkCondition` 的早退豁免名单？ | ❌ 无。是 Q1 定了之后的实现推论 |\n\n**Q2 我本可以按 ADR-0049 自裁，但刻意不裁。** enforce-or-remove 的压力是明确且单向的：`$exists` 若取「键存在」，SQL 侧**永远**无法兑现（SQL 分不出「键不在」与「值为 null」），那就成了一个声明了却无法执行的语义 —— ADR-0049 只留两条出路：改成「有值」，或者退役 `$exists`。本单正文也已倾向前者。\n\n不自裁是因为**它和 Q1 是同一个决定的两半**：三处分歧的成因（`memory-matcher.ts` 的早退守卫 + `$notContains` 的类型判否 + `$exists` 读的东西不同）指向同一个根问题 —— **平台要不要把「键不存在」和「值为 null」当成两件事**。把 Q2 单独按 ADR-0049 敲掉，等于用一条工程规则替一个产品语义提前定调，而 Q1 还悬着。这正是我在 #6125 上拒绝顺手拍板的同一条理由，不该在这里自己破例。\n\n### 决定之前必须知道的三件事\n\n**1. 裁完之后，能改的比想象的少 —— `driver-memory` 是冻结面。**\n\n三格的成因**全部**在 `packages/plugins/driver-memory/src/memory-matcher.ts` 的 `checkCondition`。而 #5499 冻结了 driver-memory 的缺陷修复与语义补齐。所以裁决落地时会被切成：\n\n- ✅ `formula` 半边 —— 可改，且能解开 #6125；\n- ⛔ `driver-memory` 半边 —— 除非同时解冻，否则只能记账，**分歧会继续存在**；\n- ✅ `FILTER_LOGIC_CASES`（#5239）扩表 —— 可做，且是本单最该先做的一步（见下）。\n\n请把这一点计入取舍：裁一个「三家统一」的答案，今天只能兑现两家。\n\n**2. 好消息：改哪里已经是确定的，不需要再勘探。**\n\n本单正文已把三个后端的现状答案**钉在测试里**（`memory-matcher-not-null-safe.test.ts` 的 \"known disagreements with formula\" 一节、`matches-filter-not-null-safe.test.ts` 的 \"known disagreement with driver-memory\" 一节）。裁决之后要改的地方会立刻被这两组 pin 顶出来 —— 这是我今天在本车道见到的少数「决策成本高、执行成本低」的单子。\n\n**3. 今天没有任何门禁能发现这三处分叉。**\n\n`FILTER_LOGIC_CASES` 那张跨后端表**刻意**不含 null 处理。所以无论裁成什么，**扩表都得做** —— 否则下一个人重新踩一遍。建议无论 Q1/Q3 怎么裁，先把这三格按裁决结果写进表里；这一步不依赖解冻。\n\n### 这不是学术问题（本单正文的这段请务必读）\n\n`matchesFilterCondition` 是 RLS **写侧** `check` 的求值器（insert/update 的后像），`driver-memory` 的 `match` 是内存驱动的读过滤。同一条策略里写 `{ notes: { $notContains: 'secret' } }`：\n\n- 一条 `notes` 为 null 的记录，**写侧判「满足」**（formula：匹配）\n- 同一条记录，**内存读侧判「不满足」**（driver-memory：不匹配）\n- SQL 侧是第三个答案（`NOT LIKE` 对 NULL 是 UNKNOWN → 不返回）\n\n一条安全策略在写入时和读取时对同一行给出相反判断 —— 这是本单真正的分量所在，不在三格的对错，而在**它们不一致**。\n\n### 我的建议（不是结论）\n\n按 Q2 → Q1 → Q3 的顺序裁，因为 ADR-0049 已经把 Q2 的解空间压到两个（「有值」或退役 `$exists`），而 Q1 一旦跟随 Q2 取「无值即无值、不区分键在不在」，Q3 就只剩一个实现推论（把 `$nin` / `$notContains` 一并移出早退豁免名单）。这条路径的代价是承认「平台不区分键缺失与 null」—— 好处是三家**能**统一、SQL 侧**能**兑现、`FILTER_LOGIC_CASES` 有一个可写死的答案。\n\n反方向（保「键存在」语义）的代价我也如实列出：SQL 永远兑现不了，等于接受一个按后端分裂的算子，且与 ADR-0049 正面冲突。\n\n⚠️ 以上是倾向，不是裁决。三问都请维护者定。\n\n关联：#6125（其 formula 半边阻塞于本单）、#5146 / PR #5296、#5298、#5239、#5240、#5499、#5905。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5219830182,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-07T16:57:45Z","updated_at":"2026-08-07T16:57:45Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5219830182","body":"**Triage (state repair): `finding` removed; `needs-user-decision` is now the only state. `domain:drivers` and `priority:p2` unchanged.**\n\nHeld and awaiting-a-ruling are different inboxes, and this issue was sitting in both: it appeared in the findings backlog (where each round re-grades it) *and* in the maintainer's decision inbox (where it waits for an answer). The two labels give opposite instructions to the next reader.\n\n**`needs-user-decision` is the live state and `finding` is the stale one.** This seat held it three rounds running (08-05 17:00Z, 08-06 22:56Z, 08-07 10:56Z), each time re-anchoring the hold because the exit condition kept dissolving under the #5499 freeze. On 2026-08-07 15:18Z the drivers lane escalated it with three concrete questions (Q1 `$notContains` over valueless fields, Q2 `$exists` = key-present vs has-value, Q3 the `$nin` early-exit exemption) and the reason the escalation is well-formed: the card stopped being a record and became **load-bearing** — #6125's formula half was hung on this ruling, so nothing downstream moves until it is answered.\n\nThat escalation is the grading this card was waiting for. Once a question is in front of the maintainer, re-grading it every findings round is not diligence, it is noise on top of a queue that is already over threshold.\n\n⛔ Nothing else touched: no assignee, no priority change, and the escalation comment stands as filed — including its correct refusal to settle Q2 under ADR-0049 alone while Q1 is open, since both are halves of one product question (does the platform treat \"key absent\" and \"value null\" as two things?).\n\n本评论来自分诊座位 Routine(#5474 试点),不构成认领。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5219858433,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-07T17:00:50Z","updated_at":"2026-08-07T17:00:50Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5219858433","body":"**Maintainer ruling — 2026-08-07.** The maintainer reviewed the PM's three-axis analysis of the decision inbox and accepted the recommendations (「你帮我综合评估，我接受你的建议」). Recorded by the PM session; `needs-user-decision` and `finding` come off with this comment (ruled ⇒ dispatchable).\n\n**Decision: value semantics, all three cells ruled at once.**\n1. `$notContains` does **not** match a field with no value.\n2. `$exists` means **\"has a value\"** (non-null) — not \"key present\".\n3. `$nin` does **not** match value-less fields: keep the early-exit guard (no exemption), and the null-value cell aligns to SQL as well.\n\nThe unifying rule, stated once for authors and conformance tables alike: **negative operators never match no-value rows; the only ways to select \"no value\" are `$exists: false` / `$null: true`.**\n\nRationale: SQL cannot distinguish key-missing from null, so value semantics is the only three-way-unifiable answer — declared = enforced forbids declaring what SQL can never deliver; one learnable rule beats per-backend folklore for AI authors; the RLS write-side vs read-side split on the same policy string is a real correctness hole, not an academic one.\n\nImplementation constraints:\n- Primary landing site is `packages/formula` (`matchesFilterCondition` flips the `$notContains` and `$exists` cells) plus FILTER_LOGIC_CASES rows for these cells (with #5239) so the cells are gated from now on.\n- `driver-memory`'s null-value `$nin` cell is also out of conformance but sits inside the #5499 freeze — re-annotate the existing disagreement pins as \"ruled target semantics; behavior frozen at current state pending unfreeze\", do not flip them now.\n- Flipped pins must keep bearing load: assert the new row sets, not merely delete old assertions.\n- Note for the triage seat (not self-relabeling): with the fix landing in `packages/formula`, the anchoring rule points to `domain:engine-core` rather than the current `domain:drivers`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5220371652,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-07T17:55:38Z","updated_at":"2026-08-07T17:55:38Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5220371652","body":"**Relabel: `domain:drivers` → `domain:engine-core`.** Acting on the note in the 2026-08-07 ruling comment (raised for the seat, correctly not self-applied). `pm:queue` and `priority:p2` unchanged.\n\n**Anchoring rule applied to the ruled shape, not the filed title.** The card is *about* a driver-memory vs formula divergence, but the ruling fixes the landing site: `packages/formula` (`matchesFilterCondition` flips the `$notContains` and `$exists` cells) plus the `FILTER_LOGIC_CASES` rows that gate them. `packages/formula` is the `domain:engine-core` row. Labeling by the title's vocabulary would have parked this in the drivers lane, where the ruled work does not land — the #4775 failure mode exactly.\n\n**Freeze interaction checked and clear.** `driver-memory`'s null-value `$nin` cell is inside the #5499 investment freeze, and the ruling handles that by instruction rather than by exception: re-annotate the existing disagreement pins as \"ruled target semantics; behavior frozen at current state pending unfreeze\", do **not** flip them now. So no part of this card asks for work inside the freeze, and no `pm:on-hold` is warranted.\n\n**For whoever claims it:** the pins in `packages/plugins/driver-memory/src/memory-matcher-not-null-safe.test.ts` and `packages/formula/src/matches-filter-not-null-safe.test.ts` are both touched — one flipped to the new row sets, one re-annotated — so the claim's file surface crosses into the drivers lane's package even though the behavior change does not. Declare it in the claim comment.\n\n本评论来自分诊座位 Routine（#5474 试点），不构成认领。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5236054621,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-10T04:58:14Z","updated_at":"2026-08-10T04:58:14Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5236054621","body":"**CLAIM — `domain:engine-core` seat (#6019).** Session `session_01MwoubC3jL271FYt9rGXwxb` · branch `claude/issue-5299-negative-operators-value-semantics`.\n\nRace re-read: all 12 comments re-read; no competing session ID. `assignees` measured EMPTY via REST immediately before assigning. `pm:queue` dropped on dispatch; `priority:p2` kept.\n\n**Correcting my own reading before dispatch, in case a future round repeats it.** I nearly held this card as frozen: the body locates the cause in `packages/drivers/driver-memory/src/memory-matcher.ts`, which is inside the `packages/drivers/**` freeze (#5499), and the body's stated path (`packages/plugins/driver-memory/`) is stale. **But the ruling moved the landing site.** The 2026-08-07 maintainer ruling fixes `packages/formula`, and Q3 explicitly says to **keep** driver-memory's early-exit guard — so the ruled work touches nothing frozen. That is also exactly why the triage seat relabeled this `domain:drivers` → `domain:engine-core`: anchoring by the ruled landing site, not the filed title. ⛔ Reading the body without the ruling gets this card wrong.\n\n**Ruled 2026-08-07, all three cells at once — ⛔ settled, not reopenable:**\n\n1. `$notContains` does **not** match a field with no value.\n2. `$exists` means **\"has a value\"** (non-null) — ⛔ not \"key present\".\n3. `$nin` does **not** match value-less fields: **keep** the early-exit guard (no exemption); the null-value cell aligns to SQL too.\n\n**The unifying rule, to be stated once for authors and conformance tables alike:** *negative operators never match no-value rows; the only ways to select \"no value\" are `$exists: false` / `$null: true`.*\n\nRationale on record: SQL cannot distinguish key-missing from null, so value semantics is the only three-way-unifiable answer — declared = enforced forbids declaring what SQL can never deliver; one learnable rule beats per-backend folklore for AI authors; and the RLS write-side vs read-side split on the same policy string is a real correctness hole.\n\n**Three implementation constraints from the ruling, passed through verbatim:**\n\n- Primary landing site is `packages/formula` — `matchesFilterCondition` flips the `$notContains` and `$exists` cells — **plus** the `FILTER_LOGIC_CASES` rows for those cells (with #5239), so the cells are gated from now on.\n- ⛔ `driver-memory`'s null-value `$nin` cell is **also** out of conformance but sits inside the #5499 freeze: **re-annotate** the existing disagreement pins as *\"ruled target semantics; behavior frozen at current state pending unfreeze\"* — ⛔ **do not flip them now.**\n- ⛔ Flipped pins must keep bearing load: **assert the new row sets**, do not merely delete the old assertions.\n\n**This card is load-bearing**: #6125's `undefined`-comparand half on `@objectstack/formula` was deliberately hung on this ruling, so landing this unblocks that.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5236191864,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-10T05:18:22Z","updated_at":"2026-08-10T05:18:22Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5236191864","body":"## ⛔ STOPPED — nothing landed. Two of the three ruled cells reverse a *completed* maintainer ruling; the third is already shipped.\n\n`domain:engine-core` seat, session `session_01MwoubC3jL271FYt9rGXwxb`, branch `claude/issue-5299-negative-operators-value-semantics`. Claim re-read before first edit — no competing session.\n\nI am invoking \"if a cell is wrong or unimplementable, stop and report, do not ship a different answer\". **Zero files changed, no PR opened.** Below is the measurement behind that, all of it executable on `origin/main` @ `89470f7`.\n\n### 1. Cell 2 (`$exists` = \"has a value\") is already true — landed 2026-08-06\n\n`packages/formula/src/matches-filter.ts:263` is already `v === true ? actual != null : actual == null`, with a comment citing **#5298/#5369**. Landed in **PR #5962**. Measured:\n\n```\n$exists:true    NULLED=[1,2]   MISSING=[1,2]\n$exists:false   NULLED=[3,4]   MISSING=[3,4]\n$null:true      NULLED=[3,4]   MISSING=[3,4]\n```\n\n`$exists` is already the strict complement of `$null`, and key-missing already answers identically to present-but-null. This cell is a **no-op** — there is nothing to flip.\n\n### 2. Cells 1 and 3 are the direct reverse of #5298, which completed on 2026-08-07\n\nThe ruling says `$notContains` and `$nin` must **not** match no-value rows. #5298 ruled the **opposite** for the same operator family and shipped it across every backend:\n\n- **#5298 ruling (2026-08-06 08:29)**: 方案 A — `$ne`/`$nin`/`$notContains` non-negated path **includes** no-value rows. That comment states it **covers this card's three cells** (「本裁决…覆盖 **#5299**(「字段没有值」三格)」).\n- **#5298 four-point confirmation (2026-08-06 12:16)**: ④ direction = **include NULL rows**. It explicitly records that the word 「排除」 (\"exclude\") in the 10:41 comment was a **typo** (「笔误」), and that the 08:29 include-direction governs.\n- **#5298 programme close (2026-08-07 00:53)**: complete. PR chain #5296 → #5962 → #6004 → #6006 → #6047.\n\nShipped code, current `main`:\n\n| Site | Line | Answer for a no-value row |\n|---|---|---|\n| `read-scope-sql.ts` | `:815` | `$notContains` → `nullSafeNegative(...)` — NULL rows **included** |\n| `read-scope-sql.ts` | `:898`, `:903` | `nullValueSatisfiesOperator`: `$nin` → `true`, `$notContains` → `true` |\n| `sql-driver.ts` | `:1986`, `:1993` | identical — `$nin` → `true`, `$notContains` → `true` |\n\nMeasured in `packages/formula` (fixtures: rows 3–4 have no value):\n\n```\n$notContains                           NULLED=[2,3,4]  MISSING=[2,3,4]\n$nin                                   NULLED=[2,3,4]  MISSING=[2,3,4]\n$ne  (ENROLLED in FILTER_LOGIC_CASES)  NULLED=[2,3,4]  MISSING=[2,3,4]\n```\n\nThe three are one family and currently give one answer.\n\n### 3. The gate already enforces the include direction\n\n`FILTER_LOGIC_CASES` (`packages/spec/src/data/filter-logic-conformance.ts:317-322`) **already enrols**:\n\n```ts\nname: '$ne returns the rows with no value',\nfilter: { d: { $ne: 'v1' } },\nexpected: ['2', '3', '4'],\n```\n\n`pnpm check:driver-conformance` on a clean tree: **OK — 36 covered cells, 4 DEBT, 0 exempt**, `FILTER_LOGIC` = `ok` for all five drivers.\n\nSo the instruction \"add `FILTER_LOGIC_CASES` rows for these cells so they are gated\" cannot be carried out as ruled. A row asserting `$notContains`/`$nin` **exclude** no-value rows would sit three lines from a row asserting `$ne` **includes** them — the same question, the same family — and would go **red on all five drivers at once**, not just the frozen one. That is the \"門禁不装已知红\" / \"a gate must not report a known red\" rule #5298 ② was explicit about.\n\n### 4. The ruling's own rationale supports cell 2 only\n\nThe recorded rationale is: *\"SQL cannot distinguish key-missing from null, so value semantics is the only three-way-unifiable answer — declared = enforced forbids declaring what SQL can never deliver.\"*\n\nThat argument is about **key-present vs null**, which is cell 2 — and it is correct, which is why cell 2 shipped. It says nothing about whether a NULL row satisfies `NOT LIKE`. And for cells 1 and 3 the \"SQL can never deliver it\" premise is **factually false on current main**: `read-scope-sql.ts:815` delivers exactly that today, via `col IS NULL OR col NOT LIKE ?`. #5298 measured the index cost of that rewrite (`EXPLAIN QUERY PLAN`, 20k rows) and found **zero regression**, because those predicates were already a SCAN.\n\nThe rationale also cites the RLS write-side/read-side split as live. **It was closed on 2026-08-06**: PR #5962 converged `formula` and `read-scope-sql` in one atomic change precisely because they are the security-coupled pair. Implementing cells 1 and 3 in `packages/formula` alone — which is what the ruling's landing-site constraint permits — would **re-open that exact hole with the sign reversed**: an RLS `check` would deny a write on a null field that the read scope's SQL still returns.\n\n### 5. What I did not do, and why\n\n| Deliverable | Status |\n|---|---|\n| Flip `$notContains`/`$nin` in `packages/formula` | ⛔ not done — would split `formula` against its own `$ne` and against `driver-sql`/`read-scope-sql` |\n| Flip `$exists` | ✅ already true since PR #5962 — nothing to do |\n| Add `FILTER_LOGIC_CASES` rows | ⛔ not done — would red all five drivers and contradict the enrolled `$ne` row |\n| Re-annotate `driver-memory` pins | ⛔ not done — the annotation text (\"ruled target semantics\") depends on which direction governs |\n| Changeset | ⛔ not done — no behaviour change to declare |\n\nNote the re-annotation is blocked for a substantive reason, not a procedural one: under #5298's accounting the target for driver-memory's null-value `$nin` cell is **include** (N2/N3, to be enrolled at thaw); under this ruling it is **exclude**. Writing either wording without a resolution would put a false statement into a frozen-surface pin, which is the one thing those pins exist to prevent.\n\n### 6. Two stale premises in the dispatch, corrected\n\n- **The card body's table is stale twice over**: the path `packages/plugins/driver-memory/` no longer exists (now `packages/drivers/`), and the `$exists` row was overtaken by #5298 ③ / #5369.\n- **#6125 is CLOSED** (2026-08-07T16:38:47Z, `completed`) — 22 minutes *before* the ruling was recorded, so landing this cannot \"unblock the card\". Its `formula` half is real and still parked here, but what that half needed was the **key-missing ≡ null** ontology, i.e. **cell 2 — which landed on 2026-08-06**. It never needed cells 1 or 3. If that work is wanted it needs a fresh issue; #6125's own closing comment says ⛔ do not reopen it.\n\n### 7. What I need decided\n\nThe two rulings cannot both stand. Either:\n\n- **(A)** #5298 governs and this card's remaining scope is what its own 2026-08-06 13:42 status note said — the **N2/N3 conformance cells for the frozen `driver-memory` reference matcher**, enrolled at thaw. Cells 1 and 3 of the 2026-08-07 ruling are withdrawn as having been written against a stale premise; cell 2 is closed as already delivered. This card then has nothing dispatchable today.\n- **(B)** The 2026-08-07 ruling supersedes #5298 and the platform moves to SQL-native exclude semantics. That is **not** a `packages/formula` change — it is a reversal across `driver-sql`, `read-scope-sql`, `filter-normalizer`, `driver-turso` remote, `driver-mongodb`, `driver-memory` and the enrolled `$ne`/`$not` conformance rows, i.e. re-doing the five-PR #5298 programme in the other direction, much of it inside the #5499 freeze. It needs its own programme and a cost pass, not this card.\n\nI have not assumed either. ⚠️ For the record, the wording of the 2026-08-07 ruling (\"negative operators never match no-value rows\") is the same direction the maintainer already identified as a **typo** on 2026-08-06 12:16 — which is why I think (A) is the likelier intent, but I am deliberately not acting on that inference.\n\nWorktree `../objectstack-5299` is left in place and clean, ready to execute whichever answer comes back.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5236243222,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-10T05:26:31Z","updated_at":"2026-08-10T05:26:31Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5236243222","body":"## PM: escalating to the maintainer — ⛔ two maintainer rulings are in direct opposition, and I will not pick between them.\n\n`domain:engine-core` seat (#6019). Labelling `needs-user-decision`, unassigning, **nothing landed**, dev stood down. The dev invoked the stop-and-report clause correctly and I am ⛔ not overriding it.\n\n**I re-verified its three load-bearing claims myself on `origin/main` @ `89470f7` rather than take them on trust. All three hold:**\n\n| Claim | Verified |\n|---|---|\n| Cell 2 (`$exists` = has a value) already shipped | ✅ `matches-filter.ts` reads `v === true ? actual != null : actual == null` |\n| The gate already enforces the **include** direction | ✅ `filter-logic-conformance.ts` enrols *\"`$ne` returns the rows with no value\"* → `expected: ['2','3','4']`, noted `#5298` |\n| #6125 could not be unblocked by this card | ✅ closed `completed` at **2026-08-07T16:38:47Z** — **22 minutes before** the 17:00:50Z ruling |\n\n### The conflict\n\n- **#5298** (ruled 2026-08-06, shipped, programme closed 08-07 00:53 across PRs #5296 → #5962 → #6004 → #6006 → #6047): `$ne`/`$nin`/`$notContains` on the non-negated path **INCLUDE** no-value rows. Its ruling comment states in terms that it **covers this card's three cells**.\n- **#5299** (ruled 2026-08-07 17:00): the same operators must **NOT** match no-value rows.\n\nThey cannot both stand. And the second one's stated rationale does not survive contact with the tree: *\"SQL can never deliver it\"* is **false today** — `read-scope-sql.ts:815` delivers exactly that via `col IS NULL OR col NOT LIKE ?`, and #5298 measured the index cost (`EXPLAIN QUERY PLAN`, 20k rows) at **zero regression**. The RLS write/read split it cites as motivation was **closed** on 2026-08-06 by PR #5962; implementing cells 1 and 3 in `packages/formula` alone — which is all the ruling's landing-site constraint permits — would **re-open that same hole with the sign reversed**.\n\n⛔ The instruction \"add `FILTER_LOGIC_CASES` rows so these cells are gated\" is also not executable as written: the new rows would sit three lines from the enrolled `$ne` row asserting the opposite for the same family, and would go red on **all five** drivers — the \"a gate must not report a known red\" rule that #5298 ② was explicit about.\n\n### ⚠️ Two errors in my dispatch brief, mine not the dev's\n\n1. I wrote that landing this **unblocks #6125's formula half**. #6125 was already closed, 22 minutes before the ruling I was quoting. I took that from the 2026-08-07 15:18Z PM comment without checking it was still true.\n2. I passed the ruling's three constraints through **verbatim and confidently**, including one that cannot be executed. Passing a ruling through faithfully is right; passing it through **unmeasured** is what produced this. The dev caught it in 20 minutes with zero files changed, which is the outcome the stop-and-report clause exists for.\n\n### The decision\n\n**(A) #5298 governs.** Cells 1 and 3 of the 2026-08-07 ruling are withdrawn as written against a stale premise; cell 2 is closed as already delivered. This card's remaining scope is the N2/N3 conformance cells for the frozen `driver-memory` reference matcher, enrolled at thaw ⇒ **nothing dispatchable today**.\n\n**(B) The 2026-08-07 ruling supersedes #5298** and the platform moves to SQL-native exclude semantics. ⚠️ That is **not** a `packages/formula` change — it reverses `driver-sql`, `read-scope-sql`, `filter-normalizer`, `driver-turso` remote, `driver-mongodb`, `driver-memory` and the enrolled `$ne`/`$not` conformance rows: the five-PR #5298 programme re-run in the other direction, much of it inside the #5499 freeze. It needs its own programme and a cost pass, ⛔ not this card.\n\n⚠️ One fact for the maintainer, which the dev surfaced and deliberately did **not** act on: the 2026-08-07 wording (\"negative operators never match no-value rows\") is the same direction recorded as a **typo (笔误)** on 2026-08-06 12:16, where the four-point confirmation fixed the governing direction as *include*. That makes (A) look likelier — ⛔ but a typo in one comment is not authority to withdraw a signed ruling, so it stays a decision.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5236250995,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-10T05:27:49Z","updated_at":"2026-08-10T05:27:49Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5236250995","body":"Triage (state repair): **`needs-user-decision` appended**. `priority:p2` / `domain:engine-core` unchanged. ⛔ **Claim untouched** — the `domain:engine-core` seat's claim (04:58Z, session `session_01MwoubC3jL271FYt9rGXwxb`, branch `claude/issue-5299-negative-operators-value-semantics`) stands, and its 05:18Z STOP report is the reason for this label, not a reason to reclaim.\n\n- **Why the decision box, again**: the dev's stop-and-report (05:18Z) establishes with merge-level evidence that the 2026-08-07 ruling's cells 1 and 3 reverse the **completed** #5298 programme (五 PR chain, include-direction shipped across `formula` / `read-scope-sql` / `sql-driver`, `$ne` row already enrolled in `FILTER_LOGic_CASES`), and cell 2 is already delivered by PR #5962. The report ends with a genuine A/B fork — (A) #5298 governs, cells 1/3 withdrawn as written against a stale premise, nothing dispatchable today; (B) the 08-07 ruling supersedes and becomes a cross-driver reversal programme, much of it inside the #5499 freeze. Two maintainer rulings in conflict can only be resolved by the maintainer; the state machine has to say a decision is pending or the card sits invisible to the inbox while the dev waits.\n- **Worth weighing for the ruling**: the dev's observation that the 08-07 ruling's wording matches the direction the maintainer already flagged as a **typo** on #5298 (2026-08-06 12:16 「笔误」 correction) is on record and checkable — it makes (A) the likelier intent, and (A) costs nothing today.\n- **State honesty**: this card has now been through `finding` → `needs-user-decision` → ruled → `pm:queue` → claimed → stopped-on-conflict. The label follows the live state (a decision is pending); when ruled, whoever records it should restore the dispatch state and the claim can resume in place — the worktree is left clean per the report.\n\n本评论来自分诊座位 Routine（#5474 试点），不构成认领。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5237204567,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-10T07:33:46Z","updated_at":"2026-08-10T07:33:46Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5237204567","body":"**Maintainer ruling (2026-08-10, directed in session `session_01BPWqbmEFU8gJepBJTHESXd`): SQL three-valued logic is the common denominator — align both JS evaluators to it.**\n\nOne principle settles all three cells, because SQL is the backend that cannot bend and declared = enforced forbids a semantics one backend can never honor:\n\n1. `$notContains` on a value-less field → **does not match** (formula changes; driver-memory and SQL already agree).\n2. `$exists` means **has a value** (`!= null`), not key-presence — key-presence is unexpressible in SQL forever (formula changes; driver-memory already reads has-value).\n3. `$nin` on a missing key → **does not match** (formula changes; driver-memory's early-exit guard stays as-is; SQL's `NOT IN` over NULL is UNKNOWN → no row).\n\nImplementation notes: the two existing \"known disagreement\" pin-test sections flip from documenting divergence to asserting agreement; add the null/missing rows to `FILTER_LOGIC_CASES` so the conformance gate sees these cells forever; coordinate with #5298/#5240 spellings where they touch the same cells.\n\n`needs-user-decision` → `pm:queue`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5237687645,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-10T08:21:06Z","updated_at":"2026-08-10T08:21:06Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5237687645","body":"**RE-CLAIM after the 07:33Z ruling — `domain:engine-core` seat (#6019).** Session `session_01MwoubC3jL271FYt9rGXwxb` · branch `claude/issue-5299-negative-operators-value-semantics` (same branch; the worktree from the stopped run was left clean and ready).\n\n**The direction is now settled and ⛔ not reopenable**: SQL three-valued logic is the common denominator; `$notContains` and `$nin` do **not** match no-value rows; `$exists` means **has a value**. The 08-06/#5298 include-direction is superseded **for these cells** by this ruling.\n\n**What I am passing to the dev as measurement, ⛔ not as fact** — because two of the ruling's parentheticals do not match `origin/main` as verified this morning, and lesson ⑳ applies in both directions:\n\n1. *\"$exists (formula changes)\"* — `matches-filter.ts` already reads `actual != null` (shipped in PR #5962). Likely nothing to change; **verify and report**, do not re-implement what exists.\n2. *\"driver-memory and SQL already agree\"* on `$notContains` — current SQL surfaces **include** no-value rows via #5298's deliberate `nullSafeNegative` rewrite (`read-scope-sql.ts` and `sql-driver.ts`'s `nullValueSatisfiesOperator`). The ruled target is native three-valued behaviour; the tree today is the bent version. **The gap between the two is the real scope of this card** and must be measured surface-by-surface before anything flips.\n3. The enrolled `FILTER_LOGIC_CASES` row *\"`$ne` returns the rows with no value\"* → `['2','3','4']` asserts the include direction for the same family, three lines from where the new rows go. The ruling's *\"coordinate with #5298/#5240 spellings where they touch the same cells\"* is the instruction that covers this — how, exactly, is the dev's to propose ⛔ but not to unilaterally decide if it means flipping `$ne` or rewriting shipped SQL: **stop and report with a measured plan** in that case.\n4. `check:driver-conformance` reports a **DEBT mechanism** (\"36 covered cells, 4 DEBT, 0 exempt\") — locate how a DEBT cell is spelled; it is likely the legal way to enroll the new rows while non-conforming backends catch up, instead of a gate that reds five drivers on day one.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5238115585,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-10T09:06:50Z","updated_at":"2026-08-10T09:06:50Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5238115585","body":"## Dev report — the annotation half is landed (draft PR #7391); the behaviour half is a measured STOP with a plan\n\n`domain:engine-core` seat, session `session_01MwoubC3jL271FYt9rGXwxb`, branch `claude/issue-5299-negative-operators-value-semantics`, base `60f0dd8` (not `89470f7` — everything below is re-measured on my own base, per the re-claim's instruction to treat the ruling's parentheticals as measurement rather than fact).\n\nComments re-read before the first edit: no competing session ID.\n\n### 1. What landed — PR #7391 (draft)\n\nNo behaviour moves. What does:\n\n- **`packages/spec`** — a \"family 4\" entry in `filter-logic-conformance.ts`'s existing *\"Case families that are RULED but not yet enrolled\"* section: the ruling, the eleven-surface measurement, the blockers. The four enrolled `d`-column cases get a pointer saying they now state the superseded direction and why they stay.\n- **`packages/formula`** — the flipped-pin instruction, applied in the direction that is actually available: the previously-**unpinned** non-negated `$notContains` / `$nin` row sets are now **asserted** (`['2','3','4']`), beside `$ne` answering identically. The cross-backend PR must move these lines deliberately; today they are the tripwire.\n- **`packages/drivers/driver-memory`** — re-annotation only, per #5499 and per the ruling's *\"the early-exit guard stays as-is\"*. Both sections move from \"an unruled divergence\" to \"a ruled target, distance measured\".\n\n### 2. Premise re-measurement — the re-claim's four flags, resolved\n\n**① `$exists` is a no-op. Confirmed.** `matches-filter.ts:263` already reads `v === true ? actual != null : actual == null`. Measured: `$exists:true` → `[1,2]` on both the null-valued and the missing-key fixture; strict complement of `$null`. ⛔ Not re-implemented.\n\n**② \"driver-memory and SQL already agree\" — false on this tree, and false in *both* directions.** SQL was deliberately bent to the include direction by #5298. And `driver-memory` reads the ruled semantics only on its **reference matcher**; its **live mingo path** — the one users reach — answers the include direction on `$nin` / `$notContains` and reads `$exists` as **key-presence**. That is the two-face divergence already pinned in `memory-driver-document-not.test.ts`.\n\nThe measurement, taken by adding the candidate rows to `FILTER_LOGIC_CASES`, rebuilding spec, and running every suite that drives the table. `MATCH` = a no-value row satisfies the operator (the include direction the ruling reverses):\n\n| Surface | `$notContains` | `$nin` | `$exists: true` on a null value |\n|---|---|---|---|\n| `formula` `matchesFilterCondition` | MATCH | MATCH | no — ruled-correct already |\n| `driver-memory` reference matcher | no — ruled-correct already | MATCH on null, no on missing key | no — ruled-correct already |\n| `driver-memory` live mingo path | MATCH | MATCH | MATCH — key-presence |\n| `driver-memory` analytics face | MATCH | MATCH | MATCH — key-presence |\n| `driver-sql` / `driver-sqlite-wasm` / `driver-turso` local | MATCH | MATCH | no — `IS NOT NULL` |\n| `driver-turso` REMOTE | MATCH | MATCH | no — `IS NOT NULL` |\n| `driver-mongodb` `translateFilter` | MATCH | MATCH | MATCH — mongo `$exists` **is** key-presence |\n| `service-analytics` `read-scope-sql` | MATCH | MATCH | no — `IS NOT NULL` |\n| `service-analytics` `filter-normalizer` | MATCH | MATCH | no — `IS NOT NULL` |\n\nMechanism behind every `MATCH`: `nullSafeNegative` (`col IS NULL OR col NOT LIKE ?`) plus **four independent copies** of `nullValueSatisfiesOperator` (`$nin` → true, `$notContains` → true) in `sql-driver.ts:1986/1993`, `read-scope-sql.ts:877`, `filter-normalizer.ts:1167`, `remote-transport.ts:267`.\n\n**③ The enrolled `$ne` row.** Confirmed live and load-bearing (`filter-logic-conformance.ts`, `expected: ['2','3','4']`). `$nin` is the list form of `$ne`, and the `$not` row beside it (#5146) is the same family reached through the combinator. Under native three-valued SQL both become `['2']`.\n\n**④ ⛔ The DEBT mechanism does not do what the re-claim hoped, and this is the load-bearing correction.** I read `scripts/check-driver-conformance.mjs`: the ledger is **one entry per (driver × case-set) cell**, and \"covered\" is judged by whether some file under the package's `src/` **imports the case-set's marker export**. A DEBT entry therefore means *\"this driver's suite does not run this case-set at all\"* — the four live entries are `driver-memory`/`driver-mongodb` × `FILTER_TEXT_CASES`/`AGGREGATION_CASES`, each of which is a whole column, not a row.\n\nThere is **no spelling for \"this driver fails one row while passing the other thirty-five\"**, and inventing one by dropping a driver's `FILTER_LOGIC_CASES` import would delete thirty-five passing cells to hide one. So the ruled instruction *\"add the null/missing rows to `FILTER_LOGIC_CASES` so the gate sees these cells forever\"* is **not executable today** — not for lack of wording, but because two of the five scored drivers would go red with no legal marker, which is the \"a gate must not report a known red\" rule.\n\n### 3. The RLS-coupling decision — measured, and it is STOP\n\nI checked whether coherence could be bought by moving `read-scope-sql`'s two cells alongside `formula` (defensible under *\"coordinate with #5298 spellings\"*, and `service-analytics` is not frozen). It cannot, and the reason is arithmetic rather than judgement. Coherence requires **all four** of the triggers the dispatch named as STOP conditions:\n\n1. **Flipping `$ne`** — the enrolled conformance row, ruled by #5298 and enforced on every backend.\n2. **Rewriting `sql-driver`** — plus `read-scope-sql`, `filter-normalizer` and `remote-transport`, four independent copies of the same table, or the write side and the read side disagree.\n3. **Touching frozen `driver-mongodb`** — its `$nin` maps straight through and its `$exists` *is* key-presence; both are ruled wrong and both are inside #5499.\n4. **Re-ruling the enrolled `$ne` row** — and `$not` with it, i.e. #5146 as well as #5298.\n\n⛔ So I did not ship the formula-only flip. Flipping `formula` alone re-opens exactly the hole PR #5962 closed, **sign-reversed**: an RLS `check` would deny a write on a null field that the read scope still returns. And it would split `formula` against its own `$ne`, which the gate enrols.\n\n### 4. The plan, if the ruled semantics is to land\n\nIts own programme, not this card. Ordered so nothing is ever red:\n\n1. **Unfreeze decision first.** `driver-memory` (three faces) and `driver-mongodb` are 2 of the 5 scored drivers. Without a #5499 exception the rows can never enrol, whatever the other nine surfaces do — so this is a gate on the programme, not a step inside it.\n2. **Re-rule the enrolled rows explicitly.** `$ne returns the rows with no value` and `$not returns the rows with no value` flip to `['2']`. This is a conscious partial reversal of #5146 and #5298 and should be recorded as such.\n3. **One PR per coupled pair, write side with read side.** `formula` + `read-scope-sql` together (the #5962 pairing); `sql-driver` + `filter-normalizer` + `remote-transport` together (one `nullValueSatisfiesOperator` answer, four copies); the frozen drivers last.\n4. **Enrol the rows in the final PR**, when every backend answers — the discipline the header's family-2 note already documents.\n5. Re-run `EXPLAIN QUERY PLAN`: #5298 measured zero index regression *adding* `IS NULL OR …`; removing it is not automatically neutral in the other direction.\n\n### 5. #5240 read-back (the ruling named it; the PM did not know it)\n\n**#5240 is CLOSED (`completed`, 2026-08-04)** — `{ field: {} }`, a field constrained by zero operators, ruled **REJECT / `INVALID_FILTER`**, landed by #5327 on `driver-sql` (top level and inside combinators), `driver-sqlite-wasm`, `driver-memory` (both surfaces) and `formula`; `driver-mongodb` still answers it, tracked as #5376.\n\n**It does not touch these cells and changes nothing in the picture.** It is about a filter with *zero operators*, not about a *row with no value* — a different axis. Its only intersection with this card is that it is the other RULED-but-not-enrolled family in the same header, and it is blocked by a different shape problem: `FilterLogicCase.expected` is a row-id list with no way to spell \"must be REJECTED\", since `[]` already means \"matched nothing\". No spelling collision to coordinate.\n\n### 6. Gates — all `completed: success`\n\n`check:driver-conformance` **before** (clean tree) and **after**, identical: `OK — 36 covered cell(s), 4 in the DEBT ledger, 0 exempt.`\n\n`packages/spec` is touched, so its gates were run individually, all pass: `check:generated`, `check:skill-docs`, `check:spec-changes`, `check:upgrade-guide`, `check:export-origins`, `check:authorable-surface`, `check:docs`, `check:skill-refs`, `check:react-blocks`, `check:api-surface`, `check:exported-any`, `check:dual-source-exports`, `check:skill-examples`.\n\nAlso green: `check:driver-memory-census`, `check:empty-changeset`, `check:nul-bytes`, `check:changeset-gate-self-tests`, `pnpm lint`, `typecheck` for all three touched packages, and full `vitest` for `packages/spec` (9480), `packages/formula` (623), `packages/drivers/driver-memory` (560).\n\nChangeset: one, non-empty, `@objectstack/spec: patch` — no published runtime behaviour changed, so no `major` and no ADR-0087 disposition trio.\n\n### 7. What I deliberately did not move, and what the record got wrong\n\n- ⛔ `formula`'s `$notContains` / `$nin` arms — §3.\n- ⛔ New `FILTER_LOGIC_CASES` rows — §2④; no legal DEBT spelling exists.\n- ⛔ Any `driver-memory` / `driver-mongodb` behaviour — #5499, and the ruling's own instruction.\n- ⛔ `$exists` — already delivered by PR #5962; verified, not re-implemented.\n\nTwo things in the record that measurement contradicts, neither of which changes the ruled direction:\n\n1. The ruling's *\"(formula changes; driver-memory and SQL already agree)\"* is wrong on both halves for `$notContains` and `$nin`. SQL agrees with `formula`, not with the reference matcher — #5298 bent it there on purpose. And `driver-memory` \"agrees\" only on its reference matcher; its live mingo path and its analytics face answer the include direction, and read `$exists` as key-presence.\n2. `driver-memory`'s pin claimed `formula` reads `$exists` as key-presence. That stopped being true in PR #5962, and it is precisely the sentence a reader would use to pick which evaluator to change. Corrected in #7391.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5238543789,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-10T09:45:43Z","updated_at":"2026-08-10T09:45:43Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5238543789","body":"**PM verification of PR #7391 — ACCEPT as the shippable half; ready + queued; the remainder goes back to the maintainer with a sharper question than before.**\n\n**What the PR does, verified by diff**: ⛔ no behaviour moves anywhere. The 07:33Z ruling is recorded as a *\"RULED but not yet enrolled\"* family in `filter-logic-conformance.ts`'s header (the section that exists for exactly this state); the previously-unpinned include-direction row sets are now **asserted** in formula's pins so the future flip must move them deliberately; frozen driver-memory pins are re-annotated from \"unruled divergence\" to \"ruled target, distance measured\" — annotation only, per #5499 and the ruling's own \"guard stays as-is\". `check:driver-conformance` is byte-identical before/after. It also corrects a stale pin comment that claimed formula still reads `$exists` as key-presence (false since PR #5962) — the exact sentence a future implementor would have used to pick the wrong evaluator.\n\n**Two corrections to my dispatch brief, both accepted**:\n1. My \"enroll via the DEBT spelling\" instruction was **not executable** — the DEBT ledger in `check-driver-conformance.mjs` is per **(driver × case-set)**, not per case. There is no spelling for \"fails one row, passes thirty-five\". Enrolment ahead of the backends is simply a red gate.\n2. The eleven-surface measurement table supersedes my four-collision-surface sketch. Every include-direction answer in the tree is **deliberate** (#5298's `nullSafeNegative` + four copies of `nullValueSatisfiesOperator`), not drift.\n\n**What remains, stated as the decision it actually is** — the ruled direction cannot be implemented coherently as anything smaller than a cross-backend programme that:\n- flips `formula` **and** `read-scope-sql` together (the #5962 security coupling; formula alone re-opens the RLS split sign-reversed),\n- rewrites the four `nullValueSatisfiesOperator` copies + `nullSafeNegative` (`driver-sql`, `driver-turso` remote, both `service-analytics` faces),\n- **re-rules the enrolled `$ne` and `$not` conformance rows** (`['2','3','4']` → `['2']`) — reversing what #5146 and #5298 shipped, since `$nin` is the list form of `$ne`,\n- and touches the **live query paths of two frozen backends** (driver-memory mingo path, driver-mongodb — both currently include, both inside #5499).\n\nThat is the #5298 five-PR programme run in reverse. ⛔ Not dispatchable from this seat on the current ruling text: it requires re-ruling enrolled semantics and granting freeze exceptions, both of which are the maintainer's. Escalating on the seat channel; this card stays open, anchored by the PR's recorded target.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5238865560,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-10T10:18:02Z","updated_at":"2026-08-10T10:18:02Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/5299#issuecomment-5238865560","body":"**Maintainer ruling (2026-08-10, directed in this seat's session `session_01MwoubC3jL271FYt9rGXwxb`): MAINTAIN the #5298 include direction. Cells 1 and 3 of the 2026-08-10 07:33Z ruling are WITHDRAWN. This card closes.**\n\nThe maintainer's decision, quoted untranslated: 「选「维持 include」」 — choosing the first option of the three put to them with the full eleven-surface measurement (PR #7391) in hand.\n\n**What is now settled, in one place:**\n\n1. **`$ne` / `$nin` / `$notContains` on a no-value row MATCH** (the include direction) — as ruled by #5146 (`$not`), extended by #5298 (the operator family), shipped across all eleven filter surfaces via `nullSafeNegative` and `nullValueSatisfiesOperator`, and enrolled in `FILTER_LOGIC_CASES`. This is the platform semantics, re-affirmed with the reversal option on the table.\n2. **`$exists` means \"has a value\" (`!= null`)** — cell 2 of the 07:33Z ruling, which was already shipped by PR #5962 and was never in conflict.\n3. The 07:33Z exclude ruling for cells 1/3 is **withdrawn after measurement**, not overturned by this seat: the decision brief showed the reversal is a cross-backend programme (flip formula+read-scope-sql together, rewrite four `nullValueSatisfiesOperator` copies, re-rule the enrolled `$ne`/`$not` rows, touch two frozen backends' live paths) purchasing a worse failure mode — silent-absence instead of visible-surplus — with zero business pull.\n\n**Follow-up filed**: PR #7391 merged (`5f7669e0c`) minutes before this ruling, so `filter-logic-conformance.ts`'s header currently records the exclude direction as *\"RULED but not yet enrolled\"*. A dedicated XS card rewrites that record to \"withdrawn; include re-affirmed\" so the tree does not carry a standing declared≠enforced contradiction. The pins #7391 added stay exactly as they are — they assert the include row sets, which is now doubly correct.\n\nClosing `completed`: the original three divergences are resolved (cells converged by #5298/#5962; the frozen driver-memory residue is tracked for thaw), and the semantics question that made this card load-bearing is settled.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
