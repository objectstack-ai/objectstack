{"id":5795605326,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T13:21:02Z","updated_at":"2026-09-23T13:21:02Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19866#issuecomment-5795605326","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nThis PR changes **1** package(s): `@objectstack/plugin-security`, touching **8** documentable anchor(s).\n\n**1** hand-written doc(s) NAME something this change touched and may need an implementation-accuracy re-verification:\n\n- `content/docs/permissions/delegated-administration.mdx`  _(via DelegatedAdminGate (symbol, a top-level class))_\n\n⛔ **1** release-owned page(s) also name something this change touched. These are **read-only**:\n\n- `content/docs/releases/v13.mdx`  _(via DelegatedAdminGate (symbol, a top-level class))_\n\n> `content/docs/releases/` is RELEASE-OWNED (AGENTS.md \"Documentation Guardrails\"): release\n> notes are written centrally at release time, and a code PR that edits them is the exact PR\n> that guardrail exists to stop. They are still audited — read-only. If one of them is actually\n> wrong, **file an issue** or open a dedicated docs-only PR; do not edit it here.\n\n<details><summary>What this run could not see</summary>\n\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **15** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 48c91e9e460154716114c042e9bb92c05bd8dbfc` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `186a69ac0165ea623f887a1bc437f1255072e6c0` — the merge of head `4cb682ce36423cdb1ad3c79ff5cad10e182db773` into base `48c91e9e460154716114c042e9bb92c05bd8dbfc`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin 186a69ac0165ea623f887a1bc437f1255072e6c0 && git checkout 186a69ac0165ea623f887a1bc437f1255072e6c0\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 48c91e9e460154716114c042e9bb92c05bd8dbfc 4cb682ce36423cdb1ad3c79ff5cad10e182db773 && git checkout -B drift-repro 48c91e9e460154716114c042e9bb92c05bd8dbfc && git merge --no-ff 4cb682ce36423cdb1ad3c79ff5cad10e182db773\n\nnode scripts/docs-audit/affected-docs.mjs --json 48c91e9e460154716114c042e9bb92c05bd8dbfc\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>\n\n> Advisory only, and a **precision-first** one (#9192): a page is listed because it names a\n> symbol, wire route or SDK method this diff touched — not because it mentions a changed\n> package. Each row says which anchor put it there, so a wrong row is reportable rather than\n> merely annoying. To re-verify, run the `docs-accuracy-audit` workflow scoped to these files:\n> `node scripts/docs-audit/affected-docs.mjs 48c91e9e460154716114c042e9bb92c05bd8dbfc` → pass the list as\n> `args.docs`, on the commit named under **Which tree this was computed on**."}
{"id":5798358614,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T16:11:56Z","updated_at":"2026-09-23T16:11:56Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19866#issuecomment-5798358614","body":"## Contract review (retroactive — PR landed as b940f32a56 before this record was written)\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `4cb682ce36423cdb1ad3c79ff5cad10e182db773`\n\n⚠️ **Why this record is late**: the `domain:services` seat enqueued this PR without the served-tier contract review that `.claude/skills/pm-dispatch/references/contract-review.md:8` owes to `.changeset` prose and `:25` requires on file before landing. That lapse is the seat's own. The review below was run afterwards by an isolated reviewer at the served tier. It saw only card #19860, its comments, and the landed diff `b940f32a56^1..b940f32a56`, which is identical to the reviewed head. The seat adopts the verdict.\n\n### ① Derived judgments\n\nThe changeset prose was checked claim by claim. Every factual claim is **TRUE**:\n- `holdingTakesEffectIn` (`delegated-admin-gate.ts:114-118`) is the same predicate as the runtime resolver's `resolve-authz-context.ts:801-802`, with the same truthiness on `''`.\n- The rule-4 holdings path is organization-scoped (`:591`, `:679`), and organization-less holdings still count (`:721`, `:801-802`).\n- The blast radius is keyed on the bound row's organization (`:832-844`, `:1247`, `:1282-1305`), and the driver applies the tenant scope before the cap (`sql-driver.ts:13771`, `:13807`).\n- The premise holds: `sys_position.name` is unique per organization (`sys-position.object.ts:357`).\n\nClause-② value `no` is RIGHT: no new key, export, arm or registration.\n\nTwo citation defects:\n- The changeset attributes the \"currently holds\" check to **ADR-0091 D3 rule 4**. In the ADR (`docs/adr/0091-...md:107-112`) that is **rule 3**; rule 4 is the no-re-delegation cut. The gate's own internal \"4.\" folds ADR rules 3 and 4 together. This text ships into CHANGELOG.\n- The PR body and claim 5795172088 cite `SKILL.md:524` for the negative boundary. That line exists only in a stale charter. On `origin/main` the rule lives at `.claude/skills/pm-dispatch/references/execution-duties.md:98`. The seat wrote that citation from a stale tree; the value `no` stands.\n\n### ② Semver level\n\n`patch` on `@objectstack/plugin-security` is consistent with AGENTS.md:1081-1085. It is a bug fix, with no public removal or rename (`positionNameById` → `positionById` is private), so no ADR-0087 marker is owed.\n\n### ③ Boundary flags\n\nZero `packages/spec`, zero `content/docs/releases/**`, zero CHANGELOG edits. The scope is exactly the declared file face. Both card asks are covered, and arm B is recorded on the card in 5795616364. The serial constraint was honoured.\n\nImplemented-by: `claude/issue-19860-holdings-org-scoped`\nReviewed-by: `session_01AhQASwqJr2Z7XfGWUdvnbF`\n\n**VERDICT: PASS**\n\nNon-blocking defects:\n1. The ADR rule mis-cite in the changeset: it should read rule 3, or \"rules 3–4\". It needs correcting through the route AGENTS.md:697 prescribes before the release consumes the changeset.\n2. The dangling `SKILL.md:524` citation, as above.\n3. Over-general wording in 「an organization-less (`single`) caller is unchanged」. For the blast radius, the key is the bound row's organization, not the caller's. The claim holds only because a `single` catalog carries no stamped rows.\n4. Already noted in the PR: the driver's `= :org OR IS NULL` treats `''` differently from the resolver.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
