{"id":5734318077,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-18T18:19:09Z","updated_at":"2026-09-18T21:59:16Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19092#issuecomment-5734318077","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nNothing in this diff resolved to a documentable surface (no symbol, route or SDK anchor derived from **0** changed package(s)), so **this run has no opinion** about the docs.\n\n<details><summary>What this run could not see</summary>\n\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **0** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json a675ad4ef0111bf0ae72b39f24c93ffdde82c092` → `packageMentionDocs`.\n</details>"}
{"id":5736835454,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-18T22:15:15Z","updated_at":"2026-09-18T22:15:15Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19092#issuecomment-5736835454","body":"## Seat review — re-measured against the tree, not the report\n\n`domain:spec` seat 2. The three stale statements the dev flagged are now corrected in the body above. Below is what the seat measured **itself**, on head `3d49d7a199`, before landing (2026-09-18T22:08Z).\n\n| reading | instrument | result — all taken 2026-09-18T22:08Z |\n|---|---|---|\n| the ordering blocker | #19059 merged as `a362e0e425`; `main` merged into this branch | **discharged** |\n| the gate on the real ledgers | `tsx scripts/liveness/check-liveness.mts` | **exit 0** — `79 … 79 graded with a status the tombstone allows` |\n| the suite | `vitest run scripts/liveness/` | **333 passed (333)**, 12 files, exit 0 |\n| the census | the gate's own `--json` `report.tombstones` | 79 enumerated, **all 79 graded `dead`**, 0 taking status from anything but an explicit row; 37 of the 79 sit below the top level, spanning 21 of the 39 types |\n| the derivation | the seat's own filter chain, written independently against `BOUND_PROOF_PATHS` and the ledgers | 79 → 42 top-level → 42 unbound → 42 `dead`, carrier `action/bulkEnabled` — reproduces the block's own figures exactly |\n| `merge=os-regen` | `git check-attr merge` on each of the 4 changed files | `unspecified` on all four — nothing here resolves silently |\n| governed surface | `check-governed-merges --pr 19092` | 0 of 4 paths hit the register — ordinary queue landing |\n| clause-② | `check-clause2-carriers --pair 19092` | exit 0; `Clause-②: no` on both the card claim (5733893029) and the body. A new forbidden combination **narrows** the accepted set, which is not clause-②. |\n\n**One observation, recorded rather than filed.** Of the derivation's three filters only the first bites today: after the top-level filter takes 79 to 42, the `BOUND_PROOF_PATHS` filter and the `dead` filter each drop **0**. That is not a defect — they hold a property of the *shape*, and the block asserts on `eligible > 0` rather than on any filter biting — but \"three filters\" should not be read as three measured reductions, so the body now says which one is load-bearing today.\n\n**Radius of that re-measurement.** It is the gate's own walk, so it is silent about the 59 further `[REMOVED]` tombstones under blanket-verdict containers the walk never reaches. Known target necessarily outside it: #19118 — strict-route retirements leave no `[REMOVED]` marker at all, so they are invisible to `report.tombstones` by construction. The 79 is a correct answer to a narrower question than \"how many retirements exist\".\n\nReady flipped; auto-merge mounted on SQUASH.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
