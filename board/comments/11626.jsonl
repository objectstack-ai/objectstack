{"id":5400937880,"author":{"login":"os-steve","id":317605050,"type":"User"},"created_at":"2026-08-24T20:29:11Z","updated_at":"2026-08-24T20:29:11Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/11626#issuecomment-5400937880","body":"Claim: devx lane PM seat, session `e2eac1a7-8000-5c95-9749-38aec2ace6fc`, branch `claude/issue-11626-engine-double-single-verb`.\n\nGraded `finding` → `pm:dispatched`, `domain:devx`.\n\n⚠️ **Collision check done before claiming**, because a `scripts/` collision between two of my own dispatches cost a merge-conflict round earlier today. Your surface is `scripts/check-engine-double-contract.mjs`, which does **not** appear in the file set the in-flight #11511 will touch, and none of my five open governed PRs touch `scripts/` except #11780 (one file, unrelated).\n\n⛔ **Still verify it yourself before your first edit** — `git diff --name-only origin/main...origin/<branch>` over every open PR, not `base.sha`. ⚠️ A PR's `base.sha` is the base **at PR creation** and is never updated; using it today gave me a 2-commit window where the real one was 6, and I mis-attributed a conflict as a result. Use `git merge-base <head> origin/main`.\n\nIf you are a different session and you see this comment before starting work, this card is taken — pick another.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5401289609,"author":{"login":"os-steve","id":317605050,"type":"User"},"created_at":"2026-08-24T20:57:25Z","updated_at":"2026-08-24T20:57:25Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/11626#issuecomment-5401289609","body":"<!-- os-dev-report -->\n```json\n{\n  \"issue\": 11626,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-11626-engine-double-single-verb\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/11849\",\n  \"premise_still_valid\": true,\n  \"summary\": \"POPULATION FIRST (it decides the card): a patched copy of the gate with the sibling threshold parameterised — positive control, threshold 2 reproduces the gate's `401 pinned` exactly — admits 74 further constructs at threshold 0 (39 with zero engine siblings, 35 with one). 32 are HTTP routers/servers whose `delete` is the HTTP verb, 16 declare EmailPersistence/SettingsSecretStore, and exactly 2 declare IDataEngine: database-loader-update-id-fold-wins.test.ts:65 and settings-engine-id-fold-wins.test.ts:65, both single-verb doubles ALREADY calling assertEngineUpdateDispatch and getting no protection for it. So the blind spot is real and non-empty, and relaxing the threshold is unavailable — it sweeps in routers by the dozen. The fix is a SECOND admission route, not a looser first one: a construct that DECLARES the engine contract (annotation, `as unknown as`, factory return type, `implements`) is admitted at any sibling count, with the local name required to be import-bound so a file-local look-alike declares nothing. It replaces the sibling threshold and nothing else — isEngineVerbShape still runs, so a driver-shaped verb is still vetoed. Discovery and the census moved together (#10175's scoping split); `--census` output is byte-identical before and after. PADDING INCENTIVE: a hazard with a documented near-miss, NOT a standing harm — no padded double exists in the tree. The two ceremonial-looking candidates in seed-tenancy-backfill.test.ts are not padding (resolveSeedTenancyLedger duck-types on ['getObject','find','insert','update'], so those siblings are load-bearing for the production resolver). What the repo does have is plugin-startup-log-cause.test.ts:145-167, where an earlier draft carried an unexercised `async delete() { return true; }` that the author removed after proving dormancy with an injected marker (0 prints; control `find` 11). RESIDUAL GAP stated in the gate header rather than left to be rediscovered: an UNDECLARED single-verb double is still invisible, left open deliberately because the only evidence for it is member names — the inference route, at a threshold already measured to sweep in 32 routers.\",\n  \"tests\": \"Union re-run at dd8e1580c (final commit); exit codes captured by redirect-then-capture, verdicts quoted from each gate's own printed line. check:engine-double-contract — 'OK — 403 pinned, 133 in the DEBT ledger, 2 exempt' and '385 (file, verb) row(s) held by the RETAINED ledger'; self-test OK with every pre-existing case passing unchanged. POPULATION DELTA, every part explained: pinned 401→403 (+2, exactly the two declared single-verb doubles), DEBT 133→133, exempt 2→2, RETAINED 383→385 (+2, same two); `--write` reported '2 added or grown, 0 lost'; ledger diff is 10 added lines naming those two files. Nothing left any population and no correct double reddened. ABLATION, both directions, each under trap '<restore>' EXIT INT TERM, anchored on text unique to the intended site (not the byte-identical-looking threshold in censusSource). LEG A, remove the declaration route (`const declared = siblings.length < 2 && declaresEngineContract(node, contractNames)` → `const declared = false;`): anchor count 1→0, replacement 0→1, sha b99fef0d…→10197f1f…, git diff printed the changed line — self-test exit 1 with 6 failures (every #11626 population case) and the tree gate exit 1 with 'RETAINED [update]' naming both files. LEG B, restore: RESTORED_SHA equals SHA_BEFORE, cmp byte-identical, anchor back to 1, mutant text absent. LEG C, the mirror — declaresEngineContract forced to return true: sha b99fef0d…→9c0f87fb…, injected line present, self-test exit 1 with 9 failures covering all five new NEGATIVE cases (router, file-local look-alike, wrong-module import, type argument, driver-shaped verb) plus three pre-existing sibling guards; restore byte-identical. So neither the positive nor the negative cases are vacuous. No build sits between edit and reading — the gate runs from source via `node scripts/check-engine-double-contract.mjs`, so no dist/ can stale a leg. Other gates green at the same sha: check:nul-bytes 'OK (scanned 6597 text file(s) … no raw ASCII control bytes)', check:cross-package-test-inputs 'All 116 self-test cases passed', check:entry-guard '148 scripts/ file(s)', check:pnpm-filter-targets '134/167 --filter occurrence(s) … resolve', check:agent-test-spelling, check:parse-guard, plus check:where-matcher and check:pm-dispatch-gates (run because dispatch-gates --residue flags them as artifact rosters under scripts/, where a 'silent' verdict is not evidence either way). pnpm lint — FULL repo, no narrowing (`eslint . --no-inline-config`), exit 0 in 51s under scripts/pm/os-verify-lock.sh (VERDICT command-exit 0 · held the lock 51s · waited 0s). Gate family derived with `node scripts/pm/dispatch-gates.mjs --repo objectstack-ai/objectstack`, no paths.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"filed as #11850: 5 further engine doubles already call assertEngineUpdateDispatch and remain outside the ratchet because they carry no IDataEngine annotation — one annotation each would admit them via the new route; needs edits in 5 test files across 4 packages, outside this PR's scripts/-only surface\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
