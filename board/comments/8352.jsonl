{"id":5277810751,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-13T08:16:24Z","updated_at":"2026-08-13T08:16:24Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8352#issuecomment-5277810751","body":"Triage: lands in `scripts/pm/dispatch-gates.mjs`; rationale: mechanical derivation bug in the PM dispatch-gates tool (same file as #8162, which the sweep already routed to `domain:skills`) — named location, no design decision needed. `domain:skills`, `pm:queue`, type `Bug`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5278177184,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-13T08:54:46Z","updated_at":"2026-08-13T08:54:46Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8352#issuecomment-5278177184","body":"**Claim** — skills seat (#7623), session `session_01139NJ9Wg5pFeZi1Zh8WLg6`, branch `claude/issue-8352-gates-i18n-derive`, `mode:subagent`, `model: opus`, size M.\n\nScope as filed: `scripts/pm/dispatch-gates.mjs` only — a second `CHANGE_KIND_GATES` entry (or runtime-discovered watch-roots extension, whichever is smaller) that walks `packages/` for `i18n-extract.config.ts` exactly as `check-i18n-bundles.mjs` itself does, and matches any input path inside an owning package → `pnpm check:i18n`. Wide variant (whole owning package, config file included), not the `src/objects/**`-only narrowing the card rejects.\n\nGates (derivation run at claim time surfaced no path-matched families for this file; judgment-call set): `node scripts/pm/dispatch-gates.mjs --self-test` (grow cases pinning the new kind both directions + STALE branch), `npx eslint scripts/pm/dispatch-gates.mjs`, `pnpm check:nul-bytes`. Acceptance demo: a live run against `packages/services/service-messaging/src/objects/http-delivery.object.ts` must now name `check:i18n` (the measured miss on PR #8348).\n\nMerge path: draft PR, **awaiting human merge** — conservative pending the maintainer's live question in seat chat about whether `scripts/pm/**` shares the skills-lane human-merge convention.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5278318328,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-13T09:08:59Z","updated_at":"2026-08-13T09:08:59Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8352#issuecomment-5278318328","body":"<!-- os-dev-report -->\n```json\n{\n  \"issue\": 8352,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-8352-gates-i18n-derive\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/8363\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Confirmed the premise against origin/main before implementing: a live run on packages/services/service-messaging/src/objects/http-delivery.object.ts named four gates, none of them check:i18n. Added a second CHANGE_KIND_GATES entry (the smaller of the two shapes the card offered) whose matches repeats the checker's own walk of packages/ for i18n-extract.config.ts — same skip set (node_modules, dist, dotted entries), same filename-plus-/scripts/ test — and matches any input path inside an owning package, whole package including the config file. It lists nothing: the KIND is written down, the population is discovered per run, verified at exact parity with the gate's own nine owners. Two measurements are SHARPER than the card stated and the PM should note both: (1) the checker is not hint-less — it carries eleven path-ish literals (CLI prerequisite, stale-dist), so because hints.length is non-zero, check:i18n was ALSO excluded from the 'repo-wide / undetermined' fallback bucket; it was invisible in BOTH halves of the output, not merely unmatched; (2) the card cites the checker's discovery function as findExtractConfigs, its actual name is findConfigs (~line 107) — the mechanism the card describes is exact, only the name differs.\",\n  \"tests\": \"node scripts/pm/dispatch-gates.mjs --self-test → '✓ dispatch-gates self-test: 47 cases pass.' (grown from 28, +19 cases). npx eslint scripts/pm/dispatch-gates.mjs → exit 0. pnpm check:nul-bytes → 'check-nul-bytes: OK (scanned 7577 text file(s) … no raw ASCII control bytes)' plus its own 75-assertion self-test; also ran a targeted grep -naP control-byte self-scan on the changed file (no hits). ACCEPTANCE DEMO, live runs: the measured incident path now prints 'edits a file in a package that owns an i18n-extract.config.ts: …http-delivery.object.ts' / '- pnpm check:i18n   — …'; packages/objectql/src/engine.ts yields grep -c check:i18n = 0; the config file itself qualifies. PARITY CROSS-CHECK: 9 owners derived == 9 the checker's own walk finds, identical sorted sets. REVERSE VERIFICATION, direction predicted first: renaming the entry's gate must leave the KIND matching and rot only name resolution → live run printed '⚠ check:i18n-renamed-probe: STALE — no workflow runs a gate under this name.' as predicted. The second prediction was uncomfortable and correct: the self-test STAYED GREEN at 47/47, because includes('pnpm check:i18n') is satisfied by any name that merely starts with it. That hole was in coverage this PR itself added, so it is fixed here in a second commit (both pins now anchor on the rendered delimiters); re-running the identical break now fails 2 of 47, and the right two — kind-match cases stay green. Restored byte-identical after each break via git restore, blob hash re-checked against HEAD (599d887…, then 59546b4…), git status clean; never git checkout ref -- path. Self-re-derivation against my own changed path surfaced no gate families beyond the named set.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"filed as #8364: the pre-existing test-file entry's self-test pins its gate names with substring includes() (line 392), so a prefix-preserving rename passes the pin the STALE branch exists to trigger — the identical shape I measured and fixed for the new entry, left untouched in PR #8363 to stay inside this card's scope. Unassigned, labelled finding + domain:skills, no pm:queue (observation class).\"\n  ]\n}\n```\n\n**PM notes at review.** PR #8363 is **draft and stays draft** — no auto-merge, no merge queue, no ready-flip, per the claim comment's conservative merge path pending the maintainer's answer on whether `scripts/pm/**` shares the skills-lane human-merge convention. Labels read back after bots settled: `size/m` (bot-applied) + `skip-changeset` — written as the union, since a bare write would have wiped `size/m`. No changeset file (internal PM tooling, nothing user-visible ships). The only PR comment is `vercel[bot]`'s skipped-deployment notice. CI status at report time is `in_progress`; per the dispatch contract the convergence wait is yours, not mine.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
