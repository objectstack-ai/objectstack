{"id":5799264568,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T17:08:25Z","updated_at":"2026-09-23T17:08:25Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19905#issuecomment-5799264568","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**6** anchor(s) derived from **1** changed package(s); no hand-written page names any of them, so this run has **nothing to list** — **not a clean bill of health**. This check sees only pages that NAME a derived anchor: one that documents this change in prose, or enumerates it in an authoring dialect, names none and stays invisible to it on every run.\n\n<details><summary>What this run could not see</summary>\n\n- **2** name(s) were too generic to anchor anything (single lowercase words)\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **17** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 44ce049a8c52b533f8a4ae195c0dca697309e400` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `5be91fd1c17cd512c13fce065031de600eff4a39` — the merge of head `1afbcef386025afaea712d14e2d349326cedb758` into base `44ce049a8c52b533f8a4ae195c0dca697309e400`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin 5be91fd1c17cd512c13fce065031de600eff4a39 && git checkout 5be91fd1c17cd512c13fce065031de600eff4a39\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 44ce049a8c52b533f8a4ae195c0dca697309e400 1afbcef386025afaea712d14e2d349326cedb758 && git checkout -B drift-repro 44ce049a8c52b533f8a4ae195c0dca697309e400 && git merge --no-ff 1afbcef386025afaea712d14e2d349326cedb758\n\nnode scripts/docs-audit/affected-docs.mjs --json 44ce049a8c52b533f8a4ae195c0dca697309e400\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5799550450,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T17:24:42Z","updated_at":"2026-09-23T17:24:42Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19905#issuecomment-5799550450","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `1afbcef386025afaea712d14e2d349326cedb758`\n\n### ① Derived judgments\n\nHead = branch tip = PR head; merge-base `dabf8d795e`; four files. Measured in a detached throwaway worktree, removed afterwards.\n\n- (a) TRUE, by-id and bulk. `staticReadonlyStoredView` is `stripReadonlyFields` run silently with the same `supplied`, `hookWrittenKeys`, `preserveAudit` and the same hoisted `isSystem` gate the real strip consumes; that strip's per-key verdict depends only on the key, so the view names exactly the keys the real strip removes later. `readonlyWhenBindings(options.stored ?? data, …)` is the only read of `stored`; the keys judged, the authorship test and the returned payload stay `data`'s. All four conditional-strip call sites pass `stored`. Both card shapes keep `amount: 100`, by-id and bulk. Ablation (the two source files at the base blobs): 12 of the 18 new pins fail, exactly the six unchanged-behaviour pins pass, and PR #19877's file stays 38/38.\n- (b) TRUE, and each movement is the declared rule (\"ignores writes to fields whose `readonlyWhen` predicate is `TRUE`\", judged on the stored row): reason attribution; the strict refusal naming `['amount', 'status']` with the same code and name; the reverse over-lock removed; a field with both locks reported `readonly_when`, still dropped; the FK's own record-scoped lock composing with PR #19877's settlement; `requiredWhen` in both directions from `evaluateValidationRules` running on the doubly-stripped payload, with no change to the `requiredWhen` code.\n- (c) TRUE: the `isSystem` view equals `data`; `preserveAudit`, hook-written and hook-overwritten values stay; controls hold; PR #19877's pins pass.\n- (d) TRUE: the `exports` map is `.` and `./core`; `ReadonlyWhenStripOptions` is a non-exported interface and the strip functions are reachable from no entry. No published type widens.\n- (e) TRUE: no hunk touches `resolveMasterDetailParent(s)` (#19837) or the settlement's body.\n- (f) Every changeset sentence, docblock and test name holds. One imprecision that misleads no one: the PR body says under `strictReadonlyWrites` \"only `fields` / `drops` grow\"; on the reverse shape they shrink. Refused before and after on both shapes.\n\n### ② Semver level\n\n`Clause-②: no` and `patch` are right. A restored lock is a runtime security behaviour change, not clause ②; nothing authorable, exported or in the error-code ledger moves. The accept-direction `requiredWhen` movement removes a refusal the published text already negated, and the PR body carries the required citation, verified verbatim against `content/docs/data-modeling/fields.mdx` (\"The server enforces `requiredWhen` on submit and ignores writes to fields whose `readonlyWhen` predicate is `TRUE`\"; \"A TRUE `readonlyWhen` predicate locks the field for **every API-boundary caller**\").\n\n### ③ Boundary flags\n\n1. The conditional-on-conditional sibling: REAL, reproduced at head and on the base. `status` `readonlyWhen: \"previous.status == 'closed'\"`, `amount` `readonlyWhen: \"record.status == 'closed'\"`, on a closed row: `update(c1, { status: 'open', amount: 999 })` stores `{ status: 'closed', amount: 999 }`; the control stays 100. Same class and reach, a different mechanism (interdependent locks in one pass make it a fixpoint), so a decision rather than this card's shape. FILE SEPARATELY (`priority:p1`, `security` suggested); it must not block this PR.\n2. The extra silent strip pass per by-id update: ACCEPTABLE; same cost class. A lazily built view can ride a later touch.\n3. Neighbour PR #19728: merge-tree clean against its head and `origin/main` (reproduced). ACCEPTABLE.\n4. The \"only grow\" wording in the PR body: ACCEPTABLE.\n5. User docs are untouched and still true. ACCEPTABLE.\n\nImplemented-by: `claude/issue-19887-readonlywhen-post-static-view`\nReviewed-by: `session_01TEhopqrWQYBycZzyJHpAZr`\n\n**VERDICT: PASS**\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
