{"id":5152030301,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-01T15:15:47Z","updated_at":"2026-08-01T15:15:47Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/4469#issuecomment-5152030301","body":"已由 #4502 修复（已合入 `main`，commit `5a84d4148`）。把说明留在这里，因为这是下一个读者最可能来的地方。\n\n## 根因 —— 为什么它能长期无声\n\n故障形态就是本 issue 描述的：请求行已翻成 `approved` / `rejected` / `returned`，而它的 `flow_run_id` 指向的 run 已不存在 —— 决策落库了，流程永远不动。\n\n`releaseDeadRunRequests` 扫不到它们，原因值得原样写下来：**它只扫 `status: 'pending'`，而把请求推入僵尸态的那一步，正是把它推出 `pending` 的那一步**。弄坏它的动作同时把它移出了唯一一个会去看它的清扫器的视野。\n\n而且即便它去看了也答不上来：它的存活性判据是 `getRun`，读的是执行日志环形缓冲 —— 重启后对一个**活着的**挂起 run 同样返回 `null`。它把 `null` 当作「还活着」（保守，且对它自己要做的事是正确的）—— 这恰恰意味着它没有能力说出「这个 run 是真的没了」。\n\n## 改动\n\n新增 `ApprovalService.inspectStrandedRequests()`（`packages/plugins/plugin-approvals/src/approval-service.ts`），**同时**用两个判据，两个都不通过才报告：\n\n- `hasSuspendedRun(runId) === false` —— 直接问挂起存储，没有活的暂停。该方法在库读不到时**抛错**，这种情况被**跳过并计入 `undetermined`**，绝不判死：存储不可读 ≠ run 不存在。\n- `getRun(runId) == null` —— 也没有终态历史行（`sys_automation_run` 里 `run_` 前缀的行）。跑完的 run 不算悬空；既不在挂起、也没跑完的才算。\n\n**只报告，绝不改写** —— 按 issue 的建议做的。决策是真实发生过的，自动回滚会让审计与事实不符。报告面向人工：哪些请求卡在哪一级（`requestId / status / runId / nodeId / flowName / objectName / recordId`），以及业务记录上镜像的状态字段现在读作什么（通常就是用户正盯着的那个过期值）。新增导出 `StrandedApprovalRequest` 为报告行的类型。\n\n它挂在既有的 SLA/死信清扫时钟上（`approvals-plugin.ts`），所以运维不必知道要去找，日志里就会浮出来。\n\n`recalled` 刻意排除：撤回本就是有意放弃它的 run（`recall` 明确容忍一个无法 resume 的 run），把它们报出来只会让真正的发现被淹没。\n\n## 验证证据\n\n`packages/plugins/plugin-approvals/src/stranded-request-inspection.test.ts`，13 例全过。关键几条：\n\n- 一例直接把**盲区本身**钉成断言 —— 对一行 `approved` 的僵尸，`releaseDeadRunRequests()` 返回 `{ scanned: 0, released: 0 }`（它的扫描集是空的）。\n- 找到僵尸并报出全部定位信息，以及**过期的镜像状态**（决策说 `approved`，记录仍读 `pending`）。\n- 两条阴性：run 仍挂起 → 不报；run 已终态 → 不报。\n- 两条「不可知」：`hasSuspendedRun` 抛错、`getRun` 抛错 —— 都跳过且 `undetermined === 1`，所以「0 stranded」永远不会被误读成「一切正常」。\n- 一例快照整个假数据库前后 JSON 相等，钉死**绝不改写**。\n- 两条降级：引擎没有 `hasSuspendedRun`、或根本没接 automation → 不给结论（没有判据就不下判决）。\n\n**撤销验证**：把存活性判据取反（`if (!suspended) continue`）后该套件失败。\n\n文档（`content/docs/automation/approvals.mdx` 的死信 callout 补上这一类看不见的终态请求）与 changeset 因合并队列锁分支而拆在 #4517。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
