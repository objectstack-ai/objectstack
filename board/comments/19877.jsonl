{"id":5796722674,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T14:30:57Z","updated_at":"2026-09-23T14:30:57Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19877#issuecomment-5796722674","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**5** anchor(s) derived from **1** changed package(s); no hand-written page names any of them, so this run has **nothing to list** — **not a clean bill of health**. This check sees only pages that NAME a derived anchor: one that documents this change in prose, or enumerates it in an authoring dialect, names none and stays invisible to it on every run.\n\n<details><summary>What this run could not see</summary>\n\n- **1** anchor(s) matched too much of the corpus to be a work list: `ObjectQL (symbol, 68 pages)`\n- **1** name(s) were too generic to anchor anything (single lowercase words)\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **17** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json b940f32a568e6b07b97b1b08572c632ecfb1660f` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `e43cf07983260885ea161f1e6f2a0d396c01d5e1` — the merge of head `0a538af9d76981e45ac4affafdd1db71e140ee74` into base `b940f32a568e6b07b97b1b08572c632ecfb1660f`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin e43cf07983260885ea161f1e6f2a0d396c01d5e1 && git checkout e43cf07983260885ea161f1e6f2a0d396c01d5e1\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin b940f32a568e6b07b97b1b08572c632ecfb1660f 0a538af9d76981e45ac4affafdd1db71e140ee74 && git checkout -B drift-repro b940f32a568e6b07b97b1b08572c632ecfb1660f && git merge --no-ff 0a538af9d76981e45ac4affafdd1db71e140ee74\n\nnode scripts/docs-audit/affected-docs.mjs --json b940f32a568e6b07b97b1b08572c632ecfb1660f\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5797027414,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T14:48:59Z","updated_at":"2026-09-23T14:48:59Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19877#issuecomment-5797027414","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `0a538af9d76981e45ac4affafdd1db71e140ee74`\n\n### ① Derived judgments\n\nHead = branch tip = PR head; merge-base `2bbb462335`; three files. Measured in a detached throwaway worktree, removed afterwards.\n\n- (a) By-id: TRUE. `settleMasterDetailLanding` runs before the `readonlyWhen` strip. The FK's own lock is judged alone against the header the FK names. The static strip's verdict comes from the same `stripReadonlyFields` over the same `suppliedValues`, `hookWrittenKeys` and `preserveAudit`, with the `isSystem` gate read once into the const the real strip consumes. `parent` then resolves from the view the write stores. Every combination checked judges the non-FK parent-scoped locks against the header the row keeps: a `readonly` FK from a non-system caller; `isSystem`; `preserveAudit`; a hook-written FK and a hook overwriting a caller-named FK; the FK's own record-scoped lock; its parent-scoped lock refusing, or naming an unresolvable header; `strictReadonlyWrites`. Nothing else on either branch moves the FK after the settle point: field-level security throws rather than strips.\n- (b) Bulk: TRUE. The same helper, per matched row; the FK's own lock goes through `stripReadonlyWhenFieldsMulti` (#3042's rule).\n- (c) The FK's own lock is still judged against the header it names: TRUE, consistent and documented (#4889; `content/docs/data-modeling/fields.mdx`). The docblock's read-count note (\"two only when a parent-scoped lock on the FK itself refuses the landing\") is imprecise: a `readonly` FK with a non-refusing parent-scoped lock also costs two reads. No caller acts on it.\n- (d) The `requiredWhen` binding moves with the shared header (#4977): TRUE in both directions, and each movement is the declared rule, not a new one. Both pins pass at head and fail on the base blob.\n- (e) The #19837 tenant scope on the header read is untouched.\n- (f) Reporting: TRUE. The FK's verdict and the rest arrive as one `readonly_when` event, then the static strip reports `readonly`; the strict refusal names both fields on the card's shape. All pinned.\n- (g) Every changeset sentence holds. Ablation (`engine.ts` restored to the base blob): 11 of the 16 new pins fail and exactly the five unchanged-behaviour pins pass; restored: 38/38. All 16 test names describe their bodies. Checks at review time: 28 success, 3 skipped, 0 failed, 2 in progress.\n\n### ② Semver level\n\n`Clause-②: no` and `patch` are right. Nothing authorable, exported or in the error-code ledger moves. The refuse-direction movement is a restored guarantee. The accept-direction movement removes a refusal the published text already negated; per execution-duties such a `no` must cite that text: `content/docs/data-modeling/fields.mdx`, \"`parent` = the record on the other end of this object's master_detail field\" and \"The server binds `parent` on the update path by reading the master the row points at (a repointing write is judged against the master it lands on)\". A row whose repoint never lands points at, and lands on, its stored master. (The seat has added this citation to the PR body.)\n\n### ③ Boundary flags\n\n1. The class (a) sibling on the `record` root: REAL, reproduced at head and on the base blob. `status` `readonly: true`, `amount` `readonlyWhen: \"record.status == 'closed'\"`: `update(t1, { status: 'open', amount: 999 })` stores `{ status: 'closed', amount: 999 }` while the control `{ amount: 555 }` stays 100. The same shape through the FK: `amount` `readonlyWhen: \"record.invoice == 'inv_a'\"` with a read-only `invoice`: `update(r1, { invoice: 'inv_b', amount: 999 })` stores `{ invoice: 'inv_a', amount: 999 }`. Pre-existing and outside this card's property. FILE SEPARATELY (`priority:p1`, `security` suggested); it must not block this PR.\n2. The #4889 landing rule on the FK's own parent-scoped lock: documented and pinned, unchanged. ACCEPTABLE.\n3. Neighbour PR #19728: merge-tree clean against its tip `3b9c5f2fca` and against `origin/main` `1f89ba0d70`. ACCEPTABLE.\n4. The read-count note in the docblock and the PR body: ACCEPTABLE; a one-clause correction can ride a later touch.\n5. User docs are untouched and still true after the fix. ACCEPTABLE.\n\nImplemented-by: `claude/issue-19853-readonlywhen-post-strip-parent`\nReviewed-by: `session_01TEhopqrWQYBycZzyJHpAZr`\n\n**VERDICT: PASS**\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
