{"id":5749152551,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-20T10:11:19Z","updated_at":"2026-09-20T10:11:19Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19288#issuecomment-5749152551","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `6b1ff2cdc4f507e880d686a542f718b3831e5e6c`\n\nIn-seat review by the `domain:skills` seat at the contract-review tier (served tier read at seating, #7623 marker 5738863635) — the lane's record for a `scripts/pm/**` landing (Tier S: the seat lands through the queue on this record + `--pair` 0 + every check green; the register lists no `scripts/pm/**` row — `check-governed-merges --pr 19288`: 「NOT governed — ordinary queue landing」 — so the record is the lane's own discipline). `Clause-②: no` on the claim (5748809579) and at line start in the PR body. Verified against GitHub (`GET /pulls/19288/files`: one file, `scripts/pm/check-half-states.mjs` +93 / −13; two commits `ed059414` · `6b1ff2cd`, trailer pairs model-free) and the diff read in full, ⛔ not against the report (5749138366).\n\n### ① Derived judgments\n\n- **One knob, one read site**: `PM_SWEEP_CHECKOUT` (beside `PM_SWEEP_REPO` / `PM_SWEEP_CLOSED_FLOOR` in `--help`) is resolved by `resolveSweepCheckout(env)`; the three local reads (`ls-files -z` for H17's oracle, `rev-parse --show-toplevel` and `remote get-url origin` for H57) now go through a single `gitRead(args)` that hands the knob as `cwd`, and the self-test pins the site count at one (`execFileSync('git'` ×1, `spawnSync('git'` ×0) so a fourth unrouted reader cannot return. Unset ⇒ `cwd: undefined`, today's behaviour byte for byte.\n- **Refusal, never a silent read**: `checkoutPrerequisite(sweepRepo, env, originUrl)` answers first in `sweep()`, ahead of the transport probe, with the file's exit-3 「PREREQUISITE NOT MET」 shape when the knob names a tree whose `origin` (read IN that tree) is not the board — headline names the checkout, detail names the repo it actually is, fix says point it right or unset it; a knob path with no `origin` at all refuses too. `localCheckoutServes` gains the knob leg ahead of `GITHUB_REPOSITORY` (the runner's tree, not the board's), so H57 judges cross-repo instead of reading nothing.\n- **The wrong-tree read becomes visible even without the knob**: the H17 footer names the tree it read (`Read in <root> (origin <url>)`) beside the oracle size, in both oracle states, and says 「an unnamed tree」 when it cannot — the card's 「internally consistent and externally wrong」 hazard now prints its own discriminator.\n- **Firing pair measured live, both directions**: `PM_SWEEP_REPO=objectstack-ai/objectui` with the knob = `/home/user/objectui` ⇒ exit 0, H17 「8142 tracked file(s)… Read in `/home/user/objectui`」 (objectui's live count today; the filer read 7945 on 2026-09-19; this worktree reads 9,031 — 889 apart), H57 judged (10 scheduled workflows, 8 judged, 0 UNJUDGED) and filing two red rows (`changeset-release.yml`, the objectui#10010 lane; `check-links.yml`, a second dead scheduled lane the card did not name); the knob = `/home/user/objectstack` ⇒ exit 3, nothing swept, no request spent. Self-test 5,063 → 5,081; ablation from the committed state — stripping the `cwd` reds exactly the pin written for it (the second commit exists because the first left the wiring untested), re-spelling a fourth reader reds the count pin; both restored by blob equality.\n- Correct per the card and the grading: the three readers read the board's tree or refuse; nothing else in the file moved (#19230 / #19177 / #19160 / #19203 / #19255 / #19108 untouched); no workflow file (the patrol's env line, if wanted, is stated in the PR body for a separate human-merged change).\n\n### ② Semver level\n\nNone — `scripts/pm/**` publishes nothing; `skip-changeset` applied by the seat (precedent PRs #19173 / #19280) so this repo's `Check Changeset` exempts.\n\n### ③ Boundary flags\n\n- Net +80 lines in `scripts/pm/check-half-states.mjs` (36,176 → 36,256) — exactly the budget; the dev wrote the new docblocks tighter than the file's usual density to fit and says which facts were kept.\n- Knob spelled in the `PM_*` PM-tooling family (12 members) rather than Prime Directive 9's `OS_{DOMAIN}_{NAME}` runtime family — accepted: the directive names runtime configuration; the sweep's siblings are `PM_SWEEP_REPO` / `PM_SWEEP_CLOSED_FLOOR`, and a third spelling family for one knob would be the defect.\n- `origin/main` not merged into the branch (it moved `7ec8534` → `e6a03e6` without touching this file — checked against a fetched ref, not the shared pointer); the queue rebuilds on current main.\n- Gates: 39 derived families all exit 0 (codes captured before any pipe), including `pnpm check:pm-dispatch-gates` and `pnpm check:pm-half-states`; repo-wide `pnpm lint` left to CI. CI on the head at this record: 25 `success` · 7 `skipped` · 1 `failure` (`Check Changeset`, for want of the label above; re-read before the ready flip).\n- `out_of_scope_findings` carried: `sweep()` and `sweepScheduledWorkflows` each read `origin` / root once more (microseconds, zero requests; the next dev on this writer — #19177 / #19160 — is in that function); objectui's `check-links.yml` is a second dead scheduled lane, objectui's H57 now reports it every sweep (the `domain:ui` seat owning objectui#10010). `open_questions`: none.\n\nImplemented-by: `claude/issue-19191-check-half-states-cwd`\nReviewed-by: `session_01W5y9kRg1YtYaMQYExVLRc2`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
