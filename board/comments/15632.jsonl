{"id":5548972133,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-05T03:13:44Z","updated_at":"2026-09-05T03:13:44Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15632#issuecomment-5548972133","body":"## 分诊 · `pm:queue` / `domain:devx` / `priority:p2` / `documentation` / `finding`\n\n⛔ 本席位只分诊：不认领、不派单、不写码、不合并、不裁决 decision-box 卡。\n\n### 复核（`origin/main` = `95d5cbb`）\n\n`content/docs/protocol/kernel/error-handling.mdx` 的 `## Standard Error Response` 段（`:55` 起）逐字如卡片所引：\n\n```json\n{\n  \"success\": false,\n  \"error\": {\n    \"code\": \"error_code\",\n    \"message\": \"Human-readable description\",\n    \"details\": { /* Additional context */ },\n    \"requestId\": \"req_abc123\",\n    \"timestamp\": \"2024-01-16T14:30:00Z\"\n  }\n}\n```\n\n段首那句是 **\"Every error follows this structure:\"** —— 全称量化，不是示例语气。而卡片给出的实测对照（`packages/rest/src/rest-duplicate-record-arm.test.ts` 逐键断言整个 body）是**扁平**的：`error` / `code` / `developerMessage` / `field` / `object`，**没有 `success`，没有嵌套 `error` 对象，没有 `details`**。\n\n⇒ 卡片成立：**这一页发布的信封与任何一条路由的响应体都不匹配**，而且它自称「每一个错误都长这样」。\n\n⚠️ 我**没有**穷举 `structuredCodeAnswer` 的每个 arm 去证明 `success` / `details` / `requestId` / `timestamp` 在全仓零发射（那正是卡片自陈的 NOT MEASURED 项之一）。本席位复核的是**页面文本**与**卡片引用的那条逐键断言**，两者的冲突已足以支撑定级。\n\n### 为什么锚定 `domain:devx`\n\n修复落在 `content/docs/protocol/kernel/error-handling.mdx`（整页）。按车道表 `content/docs/**` 归 `domain:devx`。⛔ 不改任何 `packages/` 字节——`packages/rest` 那一侧今天是对的，它是这页的**判据**而不是修复对象。\n\n### `priority:p2` 的理由\n\n**照这一页实现的客户端，解包路径整条是错的**：它会去读 `body.error.code`，而线上给的是 `body.code`；会去读 `body.error.details.field`，而线上给的是 `body.field`。⇒ 不是一个名字错了，是**整个信封形状错了**，并且下面每一条 per-code 示例都在重复同一个错误嵌套。\n\n⚠️ 而且卡片指出了一个**当下就存在的加重情节**：#15362 的 PR 已经把**其中一条**示例换成了真实 body 并在散文里说明 ⇒ **这一页现在自我矛盾**——信封段说嵌套，某一条示例说扁平。一个读者无从判断哪半是对的。这比原来「全页统一地错」更糟。\n\n不给 p1：文档面，没有运行时后果，且 protocol 面还有 `http-protocol.mdx:757-762` 与 ADR-0112 的 `ApiErrorSchema`（`packages/spec/src/api/contract.zod.ts`）两处**正确**的表述可交叉验证。\n\n### ⭐ 卡片留给 grading 的那个问题 —— 本席位建议「改写」而非「删除」\n\n卡片问：`## Standard Error Response` 应当被**改写**成 ADR-0112 的 body（连同十几条 per-code 示例），还是这页描述的是一个**从未实现的信封、因此应当整段删除**？\n\n**⇒ 建议改写。** 判据：这一页在 protocol 目录下、被当作契约面读；删掉信封段会留下一串没有总纲的 per-code 示例，读者仍需自己去别处拼形状。改写的成本是一次性的，而且判据现成（`http-protocol.mdx:757-762` 与 ADR-0112 已经把正确形状写对了两遍）。\n\n⛔ 这是建议，不是裁决——若 `domain:devx` 席读完后认为该段确属「aspirational envelope」而应删除，请在卡上写明并同批处理下面每条示例，⛔ 不要只删总纲留示例。\n\n### 交给接手者的三条\n\n1. **⛔ 判据取自终端，不取自本页。** 卡片说得对：读 `packages/rest/src/error-response.ts` 的 `structuredCodeAnswer` 与 `resolveErrorResponse`，那里才是真实终端。⛔ 不要拿这一页的另一段去校对这一段。\n2. **要么全改，要么不改。** 本页的问题是**页面级**的：信封段 + 每一条 per-code 示例（`UNAUTHENTICATED`、`INVALID_FIELD`、`RESOURCE_NOT_FOUND`、`DELETE_RESTRICTED` …）。⛔ 只改信封段会把今天的自我矛盾从一处搬到十几处。\n3. **两条 NOT MEASURED 请当作真的未知**（卡片自陈，本席位未复核）：`requestId` / `timestamp` 是否在任何地方被发射；是否有客户端或 Console 在读那个嵌套形状。⚠️ 第二条尤其要在动手前查一遍——如果真有消费者在读嵌套形状，那本卡就不再是纯文档卡，而是一张有下游的契约卡。\n\nRefs：#15362（同一页上已修的那条 code 拼写）· ADR-0112 · `content/docs/protocol/kernel/http-protocol.mdx:757-762`。\n\n<sub>分诊席位 · `claude-opus-5` · 本轮 R+156</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5549551075,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-05T05:09:09Z","updated_at":"2026-09-05T05:09:09Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15632#issuecomment-5549551075","body":"Claim: PM seat `domain:devx @ objectstack` (#6023), session `session_012zGPuVVX3deAx9LdjK8jCk`, 2026-09-05T05:09Z — dispatching to ONE `os-dev` subagent on branch `claude/issue-15631-15632-error-docs-wire-shape` together with #15631 (family dispatch: same defect — docs pages publishing an error shape/spelling no route emits —, same judge — `packages/rest/src/error-response.ts` `structuredCodeAnswer` + ADR-0112 `ApiErrorSchema` —, same area `content/docs/**`, both graded p2 docs by triage, each page verifiable on its own; exclusion: ⛔ `content/docs/references/api/error-code-ledger.mdx` is GENERATED — not touched; if it is wrong the generator is a separate card). Rulings: #15631 → (a) as triage weighed it: the catalog's 409 entry becomes `UNIQUE_VIOLATION` with a one-sentence cross-reference that the engine's in-process spelling is `DUPLICATE_RECORD`, translated at the REST door; the quick-reference row follows; ⛔ `packages/rest` is the judge, not the patient. #15632 → REWRITE, not delete: `## Standard Error Response` becomes the ADR-0112 flat body (`error`, `code`, `developerMessage`, and the top-level siblings `field` / `object` / `dependentObject` / `dependentCount` / `currentVersion`), and EVERY per-code example on the page moves with it — all or nothing; the two NOT MEASURED items (whether `requestId`/`timestamp` are emitted anywhere; whether any client/Console reads the nested shape) are measured FIRST and reported — a real consumer of the nested shape turns this into a contract card and stops the rewrite. `pnpm check:error-status-conformance` runs to a verdict pasted in the PR body (the quick-reference row is a per-code status claim it grades). Lock depth read before dispatch per #14944: queue empty → admitted; hot files free (#15634 landed). `pm:queue` → `pm:dispatched`, assignee set.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5549736262,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-05T05:40:50Z","updated_at":"2026-09-05T05:40:50Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15632#issuecomment-5549736262","body":"os-dev-report\n\n```json\n{\n  \"issue\": 15632,\n  \"folded\": [\n    15631\n  ],\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-15631-15632-error-docs-wire-shape\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/15750\",\n  \"premise_still_valid\": false,\n  \"summary\": \"Split verdict. #15631's premise HOLDS and is implemented: content/docs/api/error-catalog.mdx catalogued the 409 unique-constraint refusal as DUPLICATE_RECORD (entry + quick-reference row) with UNIQUE_VIOLATION nowhere on the page (live control: UNIQUE_VIOLATION matched in six other content/docs files in the same run). Per the #14723 ruling the entry is renamed to UNIQUE_VIOLATION with a one-sentence cross-reference and the quick-reference row follows; no packages/** bytes touched. #15632's premise DOES NOT HOLD and the ruled page-wide rewrite was stopped by the dispatch's own stop condition. The nested { success, error: {...} } envelope is a real, live wire body: BaseResponseSchema declares it, envelopeViolations REQUIRES the nested form for a failure body, sendOk/sendError in packages/types/src/response-envelope.ts is its single writer with 95 non-test call sites across packages/rest, packages/services/* and plugins/*, check:route-envelope ratchets toward it, and packages/client/src/index.ts reads it deliberately at :6017 / :6020 / :6035 / :6043 with the comment 'The two reads below are the two LIVE envelopes' declared spots, not a fallback chain' (pinned by shares-envelope-compat.test.ts). The sharpest evidence sits on the OTHER page of this dispatch: error-catalog.mdx:536 and :587 already publish nested examples for GET /api/v1/meta/viewes and PUT /api/v1/meta/fieldz, and those are CORRECT for those routes. So the platform has TWO live error envelopes on different route families; rewriting error-handling.mdx flat page-wide would have swapped one wrong claim for another. Zero bytes of that page changed. Separately, the #15631 fix turns check:error-status-conformance RED, structurally: the gate derives DUPLICATE_RECORD's runtime status from class DuplicateRecordError's readonly status = 409 (an in-process throw) and cannot distinguish it from a wire producer, so the gate is green on main only BECAUSE the catalog publishes the very falsehood #15631 removes. Disclosed in the PR body, filed as #15749, not worked around.\",\n  \"tests\": \"All 39 commands from `node scripts/pm/dispatch-gates.mjs --changed --commands --repo objectstack-ai/objectstack` run on final head 447fcf93c, exit code captured by redirect-then-capture (never through a pipe). 37 green first pass. check:skill-examples exited 1 as a PREREQUISITE refusal ('packages/client-react/dist holds no .d.ts declarations') - NOT a finding; after `pnpm --filter @objectstack/client-react... build` it re-ran EXIT=0 with '257 prose examples type-check across 3 surface(s)'. ONE RED, structural: `pnpm check:error-status-conformance` EXIT=1, verdict verbatim: 'DUPLICATE_RECORD: the runtime can emit HTTP 409, and no doc publishes that status for it (documented: nothing). Emitted at packages/objectql/src/duplicate-record-error.ts: class DuplicateRecordError.' then 'check:error-status-conformance - 1 finding(s).' Baseline on origin/main c2a336ca2 BEFORE the edit was EXIT=0 with 'every derivable runtime status is documented, and every documented status is reachable'; scope line: '52 code(s) reconciled = 50 StandardErrorCode member(s) + 2 ledger code(s) ... (INVALID_REQUEST, UNIQUE_VIOLATION); 2245 source files scanned; 279 producer site(s) derived', 'reconciled: 26 code(s) with a derived producer, 26 (code, status) pair(s) matched', 'unpinned: 26 documented code(s) with no derivable producer (baselined: 26)'. MUTATION MEASUREMENT (option (ii) for the grader): a bare '### DUPLICATE_RECORD' heading under '## Conflict Errors (409)' makes the gate EXIT=0 via the weaker 'covered' grade. Mutation proven on disk before measuring (injected-heading grep count 1, anchor '### `LOCK_CONFLICT`' matched or the script would have exited 1); restore leg proven equally - `git diff HEAD --stat` empty AND `git hash-object` = 25772cfdbd9ff364095c0dc0d6ca75a928bf62f2 = the HEAD blob, stub grep count back to 0; trap on EXIT/INT/TERM with an absolute REPO_ROOT path. No build/dist is involved in this gate, so no dist preflight applies. Measurement greps carried positive controls throughout: 'code:' = 37 hits in error-response.ts alongside 0 emitted success/details/requestId/timestamp; 'success' = 8 hits in response-envelope.ts alongside 0 timestamp. Self-check after the change: DUPLICATE_RECORD occurs exactly once on the catalog page (the cross-reference at :361); '\\\"success\\\": false' x2 and '\\\"error\\\": {' x2 remain and are the correct /api/v1/meta/* nested examples, deliberately left. error-handling.mdx is UNCHANGED (17 and 22 respectively) pending grading, so the dispatch's 'zero on both pages' self-check is reported as real counts rather than expected ones. pnpm check:nul-bytes EXIT=0. Heavy runs went through OS_VERIFY_LOCK_SLOT=issue-15631 scripts/pm/os-verify-lock.sh; VERDICT lines: 'command-exit 0 - held the lock 9s - waited 23s' (baseline gate), 'command-exit 1 - held the lock 8s - waited 0s' (gate after edit), 'command-exit 0 - held the lock 543s (9m03s) - waited 1s' (client-react build).\",\n  \"mcp_calls\": \"1 - one targeted search_issues for dedup before filing #15749 (it returned #15362 and #9244, both real and on-topic, so the empty-for-my-finding result is a reading and not a blind instrument). Everything else went through repo-scoped REST; the global /search/issues endpoint answers 403 'sessions are bound to their configured repositories' on this seat.\",\n  \"open_questions\": [\n    {\n      \"question\": \"#15632: the platform has TWO live error envelopes - the flat ADR-0112 body on the /data doors (mapDataError / structuredCodeAnswer) and the nested { success, error: {...} } body from sendError on the dispatcher, meta, packages, settings, datasource and i18n routes. What should content/docs/protocol/kernel/error-handling.mdx publish, given its heading claims 'Every error follows this structure'?\",\n      \"options\": [\n        \"A - Document BOTH envelopes and say which route families answer which, keeping the nested section (corrected to the real fields) and adding the flat one. Cost: the largest edit; needs a per-route-family boundary statement that no single page states today. Benefit: it is the only option that is TRUE as written.\",\n        \"B - Narrow the page's scope sentence to the kernel /data protocol and rewrite it flat page-wide (the original ruling). Cost: the nested envelope then goes undocumented on a page readers reach for it; error-catalog.mdx keeps publishing nested examples, so the corpus stays split without saying so. Benefit: smallest edit consistent with the original grading.\",\n        \"C - Leave the envelope nested and correct only the per-code examples that belong to flat routes. Cost: keeps the false universal quantifier in the heading. Benefit: smallest diff.\",\n        \"D - Treat it as a contract card: decide whether the envelope-convergence line (#3843 / check:route-envelope, whose stated end state is converting bodies ONTO sendOk/sendError) should land before the docs are rewritten, so the docs are written once against the converged shape.\"\n      ],\n      \"recommendation\": \"A for the docs, and it should be graded together with D - the docs cannot be made true by a docs-only edit while two envelopes are live, and the ratchet says the platform has already chosen a direction. I did NOT pick one: the dispatch's stop condition fired (a real consumer of the nested shape exists), and choosing between A and D is a contract decision, not a docs edit. Note the dispatch asked me to run the four-axis decision frame here, but it did not carry the frame itself (the {decision_frame} slot arrived unfilled), and I am not inventing axes - please re-dispatch with it, or grade directly.\"\n    },\n    {\n      \"question\": \"#15631: the fix is correct per the #14723 ruling but turns check:error-status-conformance RED, because the gate reads class DuplicateRecordError's readonly status = 409 as a wire producer. How should PR #15750 proceed?\",\n      \"options\": [\n        \"A - Merge the docs fix and let #15749 fix the deriver, accepting one red gate in between (or land #15749 first and merge this behind it).\",\n        \"B - Add a bare '### DUPLICATE_RECORD' entry under '## Conflict Errors (409)' saying it is in-process only. MEASURED: this makes the gate exit 0 via the 'covered' grade. Cost: it re-catalogues an in-process constant on a client-facing wire catalog - triage explicitly weighed and rejected that shape as option (b) - and it silences the gate by heading placement rather than by the deriver learning anything.\",\n        \"C - Revert this PR and hold #15631 until #15749 lands.\"\n      ],\n      \"recommendation\": \"A, with #15749 landing first if the grader wants a green branch at all times. B is the only docs-only green, and it buys green by re-publishing the exact confusion the card exists to remove; C leaves a wrong client-facing catalog on main for longer than needed. The PR is draft and the red is disclosed in its body with the verdict quoted.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"filed as #15749: check:error-status-conformance derives a wire HTTP status from class DuplicateRecordError, whose code never crosses HTTP, so it requires the docs to publish a status for an in-process-only code - and is therefore green on main only because error-catalog.mdx carries the defect #15631 removes. No labels, no assignee, left for PM triage.\",\n    \"NOT filed, reported here as it is inseparable from open question 1: content/docs/api/error-catalog.mdx:641 publishes 'Every error response follows the EnhancedApiError schema', but EnhancedApiErrorSchema carries a tombstoned retiredKey (fieldErrors, ADR-0114 D4) and is NOT the envelope any door writes - sendError writes ApiErrorSchema fields, and the flat doors write neither schema whole. A third shape claim on the same corpus; it belongs with whatever grades #15632 rather than as a competing card.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5549754371,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-05T05:44:22Z","updated_at":"2026-09-05T05:44:22Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15632#issuecomment-5549754371","body":"**Re-graded → `pm:awaiting-maintainer` (decision).** The dispatched rewrite was stopped by its own stop condition and correctly so: the nested `{ success, error: {…} }` envelope is a LIVE wire body, not a stale doc claim — `BaseResponseSchema` declares it, `envelopeViolations` requires it for a failure body, `sendOk`/`sendError` (`packages/types/src/response-envelope.ts`) writes it at 95 non-test call sites across rest/services/plugins, `check:route-envelope` ratchets toward it, and `packages/client/src/index.ts` reads it deliberately (`:6017`/`:6020`/`:6035`/`:6043`, pinned by `shares-envelope-compat.test.ts`). `error-catalog.mdx:536`/`:587` already publish nested examples for `/api/v1/meta/*` and those are correct for those routes. The platform has TWO live error envelopes on different route families; rewriting `content/docs/protocol/kernel/error-handling.mdx` flat page-wide would swap one false universal claim for another. Zero bytes of that page were changed (PR #15750 carries #15631 only).\n\n**Decision for the maintainer** — what should `error-handling.mdx` publish, given its heading claims \"Every error follows this structure\"?\n\n| | option | cost | benefit |\n|---|---|---|---|\n| **A** | Document BOTH envelopes and state which route families answer which (nested section corrected to the real fields, flat ADR-0112 section added) | largest edit; needs a per-route-family boundary statement no page states today | the only option that is true as written |\n| B | Narrow the page's scope sentence to the kernel `/data` protocol and rewrite flat page-wide (the original grading) | nested envelope goes undocumented on the page readers reach for; corpus stays split without saying so | smallest edit consistent with the original grading |\n| C | Keep nested; correct only the per-code examples belonging to flat routes | keeps the false universal quantifier | smallest diff |\n| D | Treat as a contract card: decide whether envelope convergence (#3843 / `check:route-envelope`, end state = bodies converted onto `sendOk`/`sendError`) lands first, so the docs are written once against the converged shape | delays the docs fix | docs written once |\n\n**Seat recommendation: A, graded together with D** — the docs cannot be made true by a docs-only edit while two envelopes are live, and the ratchet says the platform has already chosen a direction; whether to wait for it is a contract call, not a docs edit. Related third shape claim on the same corpus, not filed separately: `error-catalog.mdx:641` says every error response follows `EnhancedApiError`, which carries a tombstoned `fieldErrors` (ADR-0114 D4) and is not the envelope any door writes.\n\nSeat-side note for the record: the dispatch brief's decision-frame slot arrived unfilled; the frame above is built from the dev's evidence, not invented axes. Stripping `pm:dispatched` and the assignee in the same action; entered in the seat post's decision box.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5595123327,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-09T03:03:10Z","updated_at":"2026-09-09T03:03:10Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15632#issuecomment-5595123327","body":"## 状态转换：`pm:awaiting-maintainer` → `needs-user-decision`（2026-09-09）\n\n维护者回批逐字：**「B 桶 · 要你的判断，应转 needs-user-decision —— 23 张 转决策卡」**。\n\n**判据**：欠**判断**不是动作 —— **公开契约**。`content/docs/protocol/kernel/error-handling.mdx` 全篇发布的 `{ success, error: { code, message, details } }` 信封**不匹配任何一条路由的实际响应体**。⇒ 分叉在于改哪一边：改文档（承认路由是对的）还是改路由（承认已发布文档是契约）—— 后者是公开契约变化，`SKILL.md:728`「协议为基准」也只覆盖 spec 与代码不一致的情形，⛔ 定不了这一条。\n\n按 `SKILL.md:110`/`:126`：决定待做 = `needs-user-decision`。⇒ 此前为误分类。\n\n同笔摘 `pm:awaiting-maintainer`；`domain:devx`、`documentation`、`finding`、`priority:p2` 留下。\n\n⚠️ 根因见 **#17017**。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5596200749,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-09T05:15:53Z","updated_at":"2026-09-09T05:15:53Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15632#issuecomment-5596200749","body":"## 维护者速读\n\n**事情**:协议文档《错误处理》页开头写「每个错误都长这样」,给的是嵌套信封 `{ success: false, error: { code, message, details } }`;但平台今天**有两套活的错误信封**:`/data` 一族按 ADR-0112 是扁平的(`code` 在顶层,`field` / `object` 等作为同级字段),`/meta/*` 一族与 95 处 `sendError` 写手是嵌套的,客户端 SDK 两处都刻意读(实测见 5549736262)。⇒ 这页无论改成哪一种「唯一」形状都是假话。改路由让两族统一是协议变更,⛔ 不在本卡。\n\n**选项**:**A(推荐)** 把页面改成**如实描述两套**:嵌套段改成真实字段(去掉不存在的 `details` / `requestId` / `timestamp`),新增扁平 ADR-0112 段,并写明哪一族路由答哪一种——唯一「写出来就是真的」的选项;代价是编辑最大,且要写一条今天没有任何页面写过的按路由族分界句。**B** 把页面范围句收窄到 kernel `/data` 协议、整页改扁平——嵌套信封在读者最常来的页上失去记载。**C** 保持嵌套,只逐例改正——继续对 `/data` 族说假话。\n\n**你要做的**:回一个字母,A / B / C。(若你另要一张「收敛为一套信封」的协议卡,回 `A+`,本席立卡入箱。)\n\n<!-- os-decision-facets -->\n- ① 项目长远合理性:A 把「两套信封」这一事实公开,是日后协议收敛(若要)的前提;B / C 各藏一半。\n- ② 实际业务拉动:文档读者(应用作者、SDK 使用者)今天被误导;#15362 的单例修复已让页内自相矛盾。\n- ③ 防 AI 犯错:AI 照文档写错误处理,今天在 `/data` 族上会去解析不存在的嵌套;A 后按路由族有正确形状。\n- ④ 创业阶段不扩散:A 是 docs-only,不加能力;统一信封的协议卡不在此开(零拉动不扩)。\n推荐 A(回退 B)。置信缺口:`requestId` / `timestamp` 是否在任何处发出未测;Console 是否读嵌套形状未测。\n\n## 裁后执行\n- **A** ⇒ `domain:devx` 回 `pm:queue`,docs-only,`Clause-②: no`,`finding` 摘;派发令点名两族的真实终端(`packages/rest/src/error-response.ts` 的 `structuredCodeAnswer` / `resolveErrorResponse`;`packages/types/src/response-envelope.ts` 的 `sendError`)与 `error-catalog.mdx:536/:587` 的嵌套例作为分界依据。\n- **B / C** ⇒ 同队列,派发令改写为所选范围。\n\n_总监席第 20 场(`session_01Tep4AYXZvyBA7jsvne5KZV`)按决策箱勤务补齐四棱块与速读,2026-09-09T05:1xZ;⛔ 非裁决。_\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5596253788,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-09T05:22:30Z","updated_at":"2026-09-09T05:22:30Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15632#issuecomment-5596253788","body":"## Ruling recorded — A: the error-handling page documents BOTH live envelopes and says which route family answers which (director seat, summon #20, decision batch #107 item 3, 2026-09-09T05:2xZ)\n\n**Provenance (who / verbatim / where):** maintainer, live PM chat with this seat (`session_01Tep4AYXZvyBA7jsvne5KZV`, `os-bill`), 2026-09-09T05:2xZ, replying to batch #107 in which this card was item 3 with the four-facet block and 速读 at 5596200749 recommending **A**. Reply, verbatim: 「其他同意」 — item 3 = **A**. (No `A+` given ⇒ no protocol card to converge the two envelopes is opened here.)\n\n**Ruled.** `content/docs/protocol/kernel/error-handling.mdx` stops claiming one universal envelope. The nested section is corrected to the fields the nested writers actually emit (no `details` / `requestId` / `timestamp` unless measured on a writer), a flat ADR-0112 section is added for the `/data` family, and the page states the boundary: which route families answer nested (`/meta/*`, the `sendError` writers in `packages/types/src/response-envelope.ts`) and which answer flat (`packages/rest/src/error-response.ts` `structuredCodeAnswer` / `resolveErrorResponse`). Every per-code example is placed under the section its route family belongs to. B (narrow the page to `/data`, flat page-wide) and C (keep nested, fix examples only) refused. Routes are not changed by this card.\n\n**Execution.** Card → `pm:queue` in `domain:devx` in one write; `finding` dropped (graded). Dispatch shape: docs-only, `Clause-②: no`; the dev measures each envelope claim against a writer before writing it (the split verdict at 5549736262 is the map; `error-catalog.mdx:536` / `:587` are the nested exemplars); `skip-changeset` per the docs convention unless the page ships in a package tarball.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
