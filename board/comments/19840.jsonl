{"id":5793077327,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T10:20:38Z","updated_at":"2026-09-23T10:37:26Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19840#issuecomment-5793077327","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**4** anchor(s) derived from **2** changed package(s); no hand-written page names any of them, so this run has **nothing to list** — **not a clean bill of health**. This check sees only pages that NAME a derived anchor: one that documents this change in prose, or enumerates it in an authoring dialect, names none and stays invisible to it on every run.\n\n<details><summary>What this run could not see</summary>\n\n- **1** anchor(s) matched too much of the corpus to be a work list: `ObjectQL (symbol, 68 pages)`\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **35** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json a6a436103f1c044b36dcc450c3315a4f0f854859` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `fb0c890cffe25730cc4a7d1a95d91aa888a21405` — the merge of head `cd6bd854dd084edbdc13e2a78cc68d5de835e5a2` into base `a6a436103f1c044b36dcc450c3315a4f0f854859`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin fb0c890cffe25730cc4a7d1a95d91aa888a21405 && git checkout fb0c890cffe25730cc4a7d1a95d91aa888a21405\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin a6a436103f1c044b36dcc450c3315a4f0f854859 cd6bd854dd084edbdc13e2a78cc68d5de835e5a2 && git checkout -B drift-repro a6a436103f1c044b36dcc450c3315a4f0f854859 && git merge --no-ff cd6bd854dd084edbdc13e2a78cc68d5de835e5a2\n\nnode scripts/docs-audit/affected-docs.mjs --json a6a436103f1c044b36dcc450c3315a4f0f854859\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5793633361,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T11:00:06Z","updated_at":"2026-09-23T11:00:06Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19840#issuecomment-5793633361","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `3eb48556160df776131ac07ffa47e920f64e64ae`\n\nRendered by an isolated at-tier reviewer subagent that was fed the card, its thread and this PR only, and adopted by the `domain:engine#1` seat. Merge-base `1cacfe4a42`. ⚠️ The PR head has since moved to `cd6bd854dd` by ONE comment-only line (a dead issue number dropped for `check-issue-citations`). The seat's ACCEPT on #17212 records that delta; no verdict below depends on it.\n\n### ① Derived judgments\n\n- **(a) The level is right.** AGENTS.md's *Degradation log levels* says 「a failure handed to the CALLER is not a degradation at all … Do not bolt a `logger.error` onto such a site」. The vocabulary remedy is structurally unavailable here: the log-level gate never scopes `find`'s catch (`'find'` is in `DRIVER_READ_CALLEES` only), and a `FAILURE_PROPAGATION_SITES` entry nothing consumes reds as stale. So `warn` is the level, matching the write doors' precedent. On the head, `find`'s catch is `this.reportFindFailure(object, e); throw e;`, with `throw e` its only exit. The missing-table `debug` branch is byte-identical.\n- **(b) Nothing is lost.** The old `error` slot rendered `{ ...meta, error: { message, stack } }`. `writeFailureLogMeta` returns the same keys (and the same non-Error fallback), both pass the same `redactSensitive`, and the `warn` line carries `object`, `error.message` and `error.stack` exactly as before. The only inherent differences are the level word and stdout instead of stderr.\n- **(c) No public-surface change.** `reportFindFailure` is private and `writeFailureLogMeta` is not exported. The thrown value is the same `e`, and no response shape moves.\n- **(d) Replacing the arm, rather than adding one, is right, and nothing leaks.** The head tree has exactly two emitters of `Find operation failed`, both in `reportFindFailure` (`debug` and `warn`), and no engine path emits it on `error`. The old `error` arm intercepted only that message and forwarded everything else, so no other frame depended on it; keeping it would be dead code, the refused option-B shape. The `warn` arm reads `meta.error.message`, while the `debug` arm still reads the bare string.\n- **(e) The pins are load-bearing.** MISSING_TABLE cases assert `error` 0 · `warn` 0 · `debug` 1. The loud cases assert `debug` 0 · `error` 0 · `warn` 1 with `{ object, error: { message, stack } }` equal to the thrown instance. The hydrate outage asserts the key set exactly `['error','object']`. The untouched half still pins the rethrow and the write door at `warn`.\n- **(f) The extra runtime test file is a real consumer that moved.** It carried its own `error`-only Proxy and asserted the frame was withheld; the edit is three minimal hunks.\n\n### ② Semver level\n\n`@objectstack/objectql: patch`, `Clause-②: no`, with no `@objectstack/runtime` entry → right. A log-level move in a private method changes no export, thrown value or response shape. `packages/runtime/package.json` ships `files: ['dist','README.md','CHANGELOG.md']` from `src/index.ts`, which never imports the edited module, so nothing runtime publishes changes.\n\n### ③ Boundary flags\n\n- The `writeFailureLogMeta` docblock still says \"write doors\" → acceptable (comment-only, outside the fence, recorded in Acceptance notes).\n- The table-keyed withholder's `warn` arm has no current consumer → acceptable. It replaces an equally unreached `error` arm one-for-one, as part of the deliberate two-channel design, and it is reachable in principle.\n- Untouched prose elsewhere still calls the frame ERROR → acceptable. None of it is an assertion; it is recorded in Acceptance notes.\n- The PR body's test-sha sentence → acceptable. The only diff between the two shas is the changeset.\n- `scripts/publish-smoke.sh`'s ERROR pattern no longer trips on a failed `find` → acceptable. The boot window is judged by predicate, and the write doors made the same move.\n- The frame moves from stderr to stdout → acceptable, inherent to a level change.\n- The branch carries the WIP commit and a merge commit → acceptable: no rebase or force-push, and the squash flattens it.\n\nImplemented-by: `claude/issue-17212-find-failure-log-level`\nReviewed-by: `session_01TEhopqrWQYBycZzyJHpAZr`\n\n**VERDICT: PASS**\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
