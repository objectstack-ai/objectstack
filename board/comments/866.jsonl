{"id":3989695961,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-03-03T09:17:23Z","updated_at":"2026-03-03T09:17:23Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/866#issuecomment-3989695961","body":"\n\nLet me do a thorough audit of all filter-related code paths to identify any similar issues before creating a comprehensive issue.\n\n好的，我已经完成了对两个仓库中所有过滤相关代码的全面排查。下面我来总结所有发现的问题，然后创建一个合并的任务。\n\n## 🔍 全面排查结果\n\n### 问题 1（原始报告）：`contains` 过滤器在服务端不工作\n\n**数据流完整追踪：**\n\n1. **前端 FilterBuilder** → 用户选择 `Contains`，生成 `operator: 'contains'` ✅\n2. **ListView `mapOperator()`** → `'contains'` → `'contains'` ✅\n3. **ObjectStackAdapter** → 序列化 `['name', 'contains', 'Evan']` 为 JSON 发送 ✅\n4. **服务端 `parseFilterAST()`** → `['name', 'contains', 'Evan']` → `{ name: { $contains: 'Evan' } }` ✅\n5. **🔴 `toQueryAST()`** → 直接透传 `options.filter` 到 `ast.where`，此时 filter 已经被 `parseFilterAST` 转换成了 `{ name: { $contains: 'Evan' } }` 这种 MongoDB 对象格式\n6. **🔴 Driver `convertToMongoQuery()`** → 接收到的是 **已转换的对象格式**，走 `MongoDB format passthrough` 分支直接返回 `{ name: { $contains: 'Evan' } }`\n7. **🔴 Mingo 查询引擎** → Mingo **不认识 `$contains` 操作符**！Mingo 使用 `$regex` 做模糊匹配，所以查询返回空���果。\n\n**根因**：`parseFilterAST` 将 AST `contains` 转换为 `$contains`，但 Mingo 不支持 `$contains`。Memory Driver 的 `convertConditionToMongo` 方法能正确处理 `contains` → `$regex`，但 **passthrough 分支跳过了这个转换**。\n\n### 问题 2：`notContains` 操作符缺失 AST 映射\n\n```typescript name=normalize-quick-filter.ts url=https://github.com/objectstack-ai/objectui/blob/fedefab4f44d9fa0bdd948594477e948e8a4442f/packages/core/src/utils/normalize-quick-filter.ts#L24-L43\nfunction mapSpecOperator(op: string): string {\n  switch (op) {\n    case 'contains': return 'contains';\n    case 'notContains': return 'notcontains';\n    // ...\n  }\n}\n```\n\n`notcontains` 在 `parseFilterAST` 的 `AST_OPERATOR_MAP` 中 **没有映射**：\n\n```typescript name=filter.zod.ts url=https://github.com/objectstack-ai/spec/blob/85735f159ebed7955f5c385dc34e4a1a3c779ef8/packages/spec/src/data/filter.zod.ts#L389-L485\nconst AST_OPERATOR_MAP: Record<string, string> = {\n  'contains': '$contains',\n  'like': '$contains',\n  // ❌ 缺少 'notcontains' 映射！\n};\n```\n\n`notcontains` 会走 fallback 变成 `$notcontains`，而 Driver 同样不认识。\n\n### 问题 3：`startswith` 操作符大小写不一致\n\n前端 `mapOperator` 中没有 `startsWith`/`endsWith` 映射（FilterBuilder 未暴露这些操作符给用户），但 `VALID_AST_OPERATORS` 中同时存在 `startswith` 和 `starts_with`：\n\n```typescript name=filter.zod.ts url=https://github.com/objectstack-ai/spec/blob/330ad325ffe017e917b1b09d79b31fe599ddf677/packages/spec/src/data/filter.zod.ts#L252-L356\nexport const VALID_AST_OPERATORS = new Set([\n  'contains', 'like',\n  'startswith', 'starts_with',\n  'endswith', 'ends_with',\n]);\n```\n\n而 Memory Driver `convertConditionToMongo` 处理了 `startswith`/`starts_with`，但 `AST_OPERATOR_MAP` 映射到的是 `$startsWith`（驼峰），如果 filter 对象格式被 passthrough，Mingo 同样不认识。\n\n### 问题 4：`isEmpty`/`isNotEmpty` 的 null 转换可能导致意外行为\n\n```typescript name=ListView.tsx url=https://github.com/objectstack-ai/objectui/blob/fedefab4f44d9fa0bdd948594477e948e8a4442f/packages/plugin-list/src/ListView.tsx#L94-L193\nfunction convertFilterGroupToAST(group: FilterGroup): any[] {\n  const conditions = group.conditions.map(c => {\n    if (c.operator === 'isEmpty') return [c.field, '=', null];\n    if (c.operator === 'isNotEmpty') return [c.field, '!=', null];\n    return [c.field, mapOperator(c.operator), c.value];\n  });\n```\n\n`isEmpty` 被转换成 `['field', '=', null]`，但服务端 `parseFilterAST` 会把它转成 `{ field: null }`（简单等值），在 MongoDB 中这无法匹配空字符串 `\"\"`。正确的做法应该使用 `is_null` 或 `$null` 操作符。\n\n### 问题 5：`between` 操作符前端无法正确传值\n\nFilterBuilder 的 `between` 操作符在 `defaultOperators` 中定义了，但 `renderValueInput` 只渲染一个单值输入框——无法输入两个值（min/max）。\n\n### 问题 6：`in`/`notIn` 的 `normalizeFilterCondition` 展开可能与服务端不兼容\n\n```typescript name=ListView.tsx url=https://github.com/objectstack-ai/objectui/blob/fedefab4f44d9fa0bdd948594477e948e8a4442f/packages/plugin-list/src/ListView.tsx#L19-L93\nexport function normalizeFilterCondition(condition: any[]): any[] {\n  if (op === 'in' && Array.isArray(value)) {\n    return ['or', ...value.map((v: any) => [field, '=', v])];\n  }\n```\n\n前端将 `in` 展开为 `['or', ['f','=','a'], ['f','=','b']]`。但如果服务端 Driver 原生支持 `in` 操作符，这种展开是多余的且性能更差。\n\n### 问题 7：`DataScopeManager` 的 `contains` 过滤区分大小写，但其他地方不区分\n\n```typescript name=DataScopeManager.ts url=https://github.com/objectstack-ai/objectui/blob/fedefab4f44d9fa0bdd948594477e948e8a4442f/packages/core/src/data-scope/DataScopeManager.ts#L238-L264\ncase 'contains':\n  return typeof fieldValue === 'string' && fieldValue.includes(String(filterValue));\n```\n\n`DataScopeManager` 用 `fieldValue.includes()`（**区分大小写**），而 `ValueDataSource` 用 `toLowerCase().includes()`（**不区分大小写**），行为不一致。\n\n---\n\n现在创建一个合并的 Issue："}
{"id":4365136556,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-05-03T01:37:35Z","updated_at":"2026-05-03T01:37:35Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/866#issuecomment-4365136556","body":"This issue has been automatically marked as stale because it has not had recent activity. It will be closed in 14 days if no further activity occurs. Thank you for your contributions."}
{"id":4468794609,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-05-17T01:38:52Z","updated_at":"2026-05-17T01:38:52Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/866#issuecomment-4468794609","body":"This issue was automatically closed because it has been stale for 14 days with no activity. Please feel free to reopen if needed."}
