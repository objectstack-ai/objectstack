{"id":5761948903,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-21T14:15:53Z","updated_at":"2026-09-23T08:26:17Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19598#issuecomment-5761948903","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**1** anchor(s) derived from **1** changed package(s); no hand-written page names any of them. ⚠️ **1** changed file(s) yielded no anchor (`packages/spec/liveness/view.json`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n<details><summary>What this run could not see</summary>\n\n- **1** changed file(s) yielded no anchor (`packages/spec/liveness/view.json`) — pages documenting those are invisible to this run\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **136** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 6eaa0f4a81a0146250f083df2b9d000b75dbec61` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `7ed15cc8f5de30e9b5b0964181c14c30e93c1eae` — the merge of head `e76041c2b472a401dd1fec67cb124de2da2fc9cd` into base `6eaa0f4a81a0146250f083df2b9d000b75dbec61`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin 7ed15cc8f5de30e9b5b0964181c14c30e93c1eae && git checkout 7ed15cc8f5de30e9b5b0964181c14c30e93c1eae\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 6eaa0f4a81a0146250f083df2b9d000b75dbec61 e76041c2b472a401dd1fec67cb124de2da2fc9cd && git checkout -B drift-repro 6eaa0f4a81a0146250f083df2b9d000b75dbec61 && git merge --no-ff e76041c2b472a401dd1fec67cb124de2da2fc9cd\n\nnode scripts/docs-audit/affected-docs.mjs --json 6eaa0f4a81a0146250f083df2b9d000b75dbec61\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5793631549,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T10:59:57Z","updated_at":"2026-09-23T10:59:57Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19598#issuecomment-5793631549","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `e76041c2b472a401dd1fec67cb124de2da2fc9cd`\n\n<sub>Reviewed and posted 2026-09-23T10:59Z by the at-tier review subagent the `domain:spec#5` seat spawned — read: card #15184 and its 15 comments, the PR body/commits/diff against merge-base `6eaa0f4a81`, the head's 42 check-runs, objectui at `.objectui-sha` `87af769e` via `git show`; ran: the pin (control + 7 ablations), an own accept-set probe base vs head, a TS quick-info probe, the governed predicate; NOT MEASURED: the derived gate family (relayed from CI at head), the DTS build, the `json-schema/` build.</sub>\n\n### ① Derived judgments\n\n**Surface (8 files, +433/−36).** Declared: `packages/spec/src/ui/view.zod.ts` — three `.describe()` strings and two JSDoc blocks, nothing else in the shape; `view-field-order-composition.pin.test.ts` (new, 21 cases); `liveness/view.json` (one row); the changeset. Generator outputs: three `content/docs/references/**.mdx` (27 rows in 9 tables, each row one of the three descriptions) and `skills/objectstack-ui/references/react-blocks.md` (1 row; 115→115 lines, SKILL.md 309→309; byte-identical between `cfc103a20a` and head because its cell truncates before the changed text). `packages/lint` and `packages/spec/scripts/` untouched; no export added/removed/renamed; no `retiredKey()`, no ADR-0087 entry. Judged: matches ruling B, nothing outside it.\n\n**Accept set — `Clause-②: no` holds (ran).** Own probe, 23 inputs × 4 doors (`ListViewSchema`, `ObjectListViewSchema`, `defineView`, registered `view`), base vs head: 52 accept / 40 reject at both; every verdict, parsed value and issue-code set identical; `ListViewSchema.shape` 50 keys, `columns` required, the other two optional, at both. Lit control: descriptions differ for exactly `columns` / `hiddenFields` / `fieldOrder`, not `rowColor` / `rowActions`. All four doors admit `columns: []` verbatim with the other two keys intact. Judged: no widening, no narrowing.\n\n**Governed (ran).** The diff hits `skills/**` (a Tier H register row) on one path. `node scripts/pm/check-governed-merges.mjs --pr 19598` from an installed worktree: exit 0, \"0 of 8 paths hit the register after 1 generated-artifact lift\" — `react-blocks.md` is a PURE REGENERATION byte-equal to `gen:react-blocks` recomputed on this tree; the head's `Governed Surface Queue Guard` check-run is success (empty output). The seat's \"not a governed surface\" is true only through the `spec-react-blocks` lift, and the trusted generator tree (`packages/spec/scripts/`) is untouched here. Judged: not governed.\n\n**Declared composition vs objectui `@87af769e` (read).** `plugin-list/src/ListView.tsx` `effectiveFields` memo `:2751-2784`: seed `schema.columns` `:2754` → FLS read filter `:2757-2763` → hidden subtraction `:2766-2771` → order map + sort `:2774-2781`; `:2784` lists `schema.fieldOrder` as a dependency. Each shipped step checked: \"cannot add a field omitted here\" — the memo only filters and sorts, true; `hiddenFields` subtracts before ordering and a never-projected name subtracts nothing — true (`hiddenFields` is state seeded from `schema.hiddenFields`, `:1465-1475`); unlisted survivors sort LAST keeping `columns`-relative order, a listed non-survivor orders nothing — true (`?? Infinity`; `Infinity − Infinity` is NaN, which the sort treats as +0, and the sort is stable); the FLS step \"between steps 1 and 2\" — true. **`columns: []` boundary:** `hasAuthoredColumns` `:2828-2831` is `length > 0`; the grid branch `:2880-2882` sends `{ fields: undefined, columns: undefined }`; `plugin-grid/src/ObjectGrid.tsx:3228` derives default columns and names `hiddenFields` 0× and `fieldOrder` 0× (control: `columns` 71×); the hide popover's `allFields` `:3802-3816` is built from `schema.columns`. So neither key applies and the grid derives the object's default columns — true for the `ListView` grid, which is what the text names. Kanban `:2977` (`cardFields` first) and gallery `:3058` (`titleField` defaulting to `'name'`) justify the narrowed opener \"together build one field list\". Ledger row: `producer` `plugin-view/src/ObjectView.tsx:2284` (inside `renderContent`, `:2147`) relays `currentNamedViewConfig?.fieldOrder`, view-sourced only; `packages/react/src/spec-bridge/` 0 files vs `packages/react/` 160; app-shell `ObjectView.tsx:2480` carries `hiddenFields` and its census test `:480` records `fieldOrder` as `known-gap` (objectui#7516); `producer` is a documented ledger field (README, 16 ledgers). All true at the pin.\n\n**Pins are real (ran; control 21/21; every restore blob `5a0b5cf9d298` = HEAD, tree clean after).** D boundary dropped from the `columns` describe → 1 red; A `fieldOrder` describe reverted → 3 red; F `.min(1)` on both `columns` arrays so the doors refuse `[]` → 1 red (the door case); G \"cannot add\" sentence dropped → 1 red; E docblock boundary dropped → 1 red; B `fieldOrder` key deleted → 12 red; C docblock step reworded → 1 red. The PR body's seven legs reproduce exactly.\n\n**Every sentence that ships.** Describes, both JSDoc blocks, the changeset, the generated rows and the pin header trace to the readings above. Both former fail bases are gone: no hover claim survives anywhere in the diff, and the `[]` boundary is stated in the `columns` describe, its JSDoc, docblock step 1 and the changeset. Quick-info probe (ran, TS language service over `packages/spec` at head): documentation chars for `fieldOrder` 0 / 0 / 0 at `defineView`, `parse` and the type member; `columns` 447, `hiddenFields` 1859; no site carries a describe lead — the PR body's numbers reproduce. Relayed, NOT MEASURED here because they are the derived gate family and CI carries them at head: spec test 520 files / 15259 tests (`Test Core`), `check:generated` 15/15 with `check:docs` / `check:react-blocks` / `check:liveness` (`TypeScript Type Check`, `Lint & Repo Gates`, `Spec property liveness`), `dispatch-gates --ran` 113/111/2, the narrowed lint, `check:objectui-pin-citations` 46. NOT MEASURED: `json-schema/ui/ListView.json` (gitignored build output; `json-schema` is in `files[]` and `.describe()` is what the generator emits as `description`) and \"0 describe strings across 94 dist declaration files\" (no DTS build here; a `.describe()` is a runtime value and cannot reach a `.d.ts`).\n\n**Model identifiers.** Swept every added diff line, the four commit messages, the changeset, the generated docs and the 21,484-byte PR body for every model-identifier spelling (product, family, tier and provider names, and the bracketed context suffix): 0, apart from the mandated attribution footer (1 line) and the mandated model-free commit trailer pair, both exempt forms AGENTS.md names; lit control on synthetic input 1/1.\n\n**CI at head (42 check-runs, de-duplicated to 35 names by latest `started_at`).** 31 success, 4 skipped, 0 failure; all seven required contexts success (`Lint & Repo Gates`, `TypeScript Type Check`, `Test Core`, `Dogfood Regression Gate`, `Build Core`, `Temporal Conformance (live PG + MySQL)`, `Governed Surface Queue Guard`). Skips: `Auto Label` and `Check PR Size` — their latest run is the `edited`-event run after the seat's body write, and both jobs' `if:` exclude `edited` (their earlier `synchronize` runs at this head were success); `Console Pin Gate` — `needs.filter.outputs.console` is false, no console or pin path in the diff; `Packed-tarball smoke (opt-in)` — the opt-in label `needs:pack-smoke` is absent.\n\n### ② Semver level\n\n`'@objectstack/spec': patch`, `Clause-②: no`, no `(widening)` / `(narrowing)` arm. Consistent: the accept set is byte-identical base vs head (own probe); what changes is published description text (`src/**/*.zod.ts`, `json-schema`) and one `liveness/` row, both in `files[]`, so a changeset is owed and `patch` is the level — the pair ruling B fixed. Judged: correct.\n\n### ③ Boundary flags\n\nBlocking: none.\n\nNon-blocking:\n1. **objectui gap, reported for filing, not yet filed** (PR body Acceptance note 2; dev report `5792450002` \"to file, class c\"; seat 2 filed nothing). Re-read at the pin: `plugin-view/src/ObjectView.tsx` relays `hiddenFields` / `fieldOrder` only inside `if (renderListView)` (`:2172`; `:2268`, `:2284`); the direct `ObjectGrid` path `:2335-2344` renders from `gridSchema` (`:1909-1977`, 0 reads of either key) and `ObjectGrid.tsx` reads neither; `viewColumnFieldNames` keeps `[]` (`:225-228`), so a named grid view with `columns: []` renders zero columns there (`:3245-3246`). A distinct seam from objectui#7516 (the app-shell relay's missing rung), so an objectui card is owed by the `domain:spec#5` seat. It falsifies no shipped sentence: the boundary text names `ListView`.\n2. **Sibling `hiddenFields` ledger row rot** (`spec-bridge/bridges/list-view.ts:123` and drifted `ListView.tsx` lines) — reported for filing, deliberately left under ruling B's one-row scope; no gate reds on it. Seat to file.\n3. Answered: the first dev round's A/B/C question (ruling B, `5635654931`); the dev's PR-body-write conflict (the seat wrote the body; one footer line read back); the prior record's governed-reading and `File surface:` notes (re-declared in `5791411218`; predicate exit 0 above); the card's stale `Blocked-by: #19671` (the card body now carries no `Blocked-by:` line and an UNBLOCKED banner); C9 (Release/Claim chain `5790817662` → `5793391854`; \"The card this PR closes must claim this branch\" is success at head).\n\nImplemented-by: `claude/issue-15184-field-order-composition`\nReviewed-by: `session_01Sfe5YjBLwB9J3y8fvm2xq1` — at-tier review subagent spawned by the `domain:spec#5` seat\n\n**VERDICT: PASS**\n"}
