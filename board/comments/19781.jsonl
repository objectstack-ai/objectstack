{"id":5788484117,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T03:30:18Z","updated_at":"2026-09-23T03:30:18Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19781#issuecomment-5788484117","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n⚠️ **1** changed file(s) yielded no anchor (`packages/lint/src/validate-action-dispatch-contract.ts`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files. Nothing else in this diff resolved to a documentable surface (no symbol, route or SDK anchor derived from **1** changed package(s)).\n\n<details><summary>What this run could not see</summary>\n\n- **1** changed file(s) yielded no anchor (`packages/lint/src/validate-action-dispatch-contract.ts`) — pages documenting those are invisible to this run\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **4** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 67add1301aa2bce363953b3f424f416d43bf29f0` → `packageMentionDocs`.\n</details>"}
{"id":5788647102,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-09-23T03:49:58Z","updated_at":"2026-09-23T03:49:58Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19781#issuecomment-5788647102","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `4433ece870fe9d52f758db2901f122b4c325f4a2`\n\nCard #17923, one round. Base `main`, merge-base `70a2e752b8`. Reading moment: 2026-09-23T03:49Z. A record binds ONE head and one reading moment; it is ⛔ not a standing statement about CI.\n\n**Tier, measured by the SEAT from the reviewer's transcript** (⛔ not its self-report): `CONTRACT_REVIEW_TIER` on `origin/main` is `claude-fable-5-1`. Governing assistant-request stamps **116 / 116** `claude-fable-5-1`; superset `\"model\"` + `\"advisorModel\"` **256 / 256** the same value; dark control **empty**; the fallback/overload sweep, read in context, is tool-schema text and repo prose — **⛔ none a notice**. ⇒ **AT TIER.** This seat is off-tier and ⛔ did not 自审.\n\n### ① Derived judgments\n\n- **No logic change, proved three ways that formatting cannot fool.** The rule file's base and head blobs differ, but stripping comments with `typescript` `transpileModule`, with `esbuild` `transformSync`, and with the TS printer over the parsed AST gives byte-identical output on both sides for all three. The `.ts` hunk sits wholly inside the `collectDeclaredContracts` docblock.\n- **The pins pin.** The reviewer re-ran the ablation with its own helper, removing the `'none'` arm. The rule's test file then has **3 failed / 16 passed**: exactly the three disarm pins (a sibling on the view's own object, on an unrelated object, and a global beside an object-embedded declaration). Both controls and all 14 pre-existing tests stay green. The whole lint suite under the mutation reads 3 failed / 4108 passed / 5 skipped, the same three. The restore was verified by blob hash with an empty porcelain. Unmutated head: **4116 / 4116**.\n- **Each pin carries its own lit control.** Every disarm test first asserts one finding on the identical stack without the sibling. An agreeing-sibling control (a sibling that DOES declare the same contract) leaves the finding standing, which isolates the cause.\n- **The docblock is true on its literal reading, clause by clause**, including 「anywhere in the flat namespace」: the map pools exactly the global `actions` and every object's embedded `actions`, the same two sources `validate-action-name-refs` resolves against, and no view-object filter exists in the file.\n\n### ② Semver level\n\nNo release; `skip-changeset` (route 2) is right. `@objectstack/lint` ships `dist`, `README.md` and `CHANGELOG.md`. Built at base and at head, all ten non-map `dist` files are byte-identical. The four `.map` files differ in `mappings` only, and none carries `sourcesContent`. No `.changeset` row is changed, so it is ⛔ not route 0.\n\n### ③ Boundary flags\n\nNone tripped. Two files (+107 / −6), no governed path, `Fixes #17923`, line-leading `Clause-②: no`, model-free trailers (`check-commit-card-trailers` exit 0).\n\n**CI at this head, the reviewer's final reading:** 34 names, **28 success + 6 skipped, 0 failure / in progress / queued / cancelled**. All seven required contexts `success`. `Check Changeset` reads skipped on the post-label run; its pre-label red is superseded.\n\n**Non-blocking, recorded, ⛔ not a round:**\n- The docblock says the disarm applies 「for every view that wires it」. It also silences object-embedded `listViews` wirings, so the sentence names fewer wiring sites than exist, but it is true as stated.\n- An `execution` value outside the contract vocabulary also enters the set as `'none'`. The docblock does not claim otherwise; whether a schema-parsed stack can carry one is unmeasured.\n- The file header's 「What this rule refuses — and what it deliberately does not」 section still states only the undeclared-action case. It was outside this claim's surface, and the dev listed it in the PR's Acceptance notes.\n\nImplemented-by: `claude/issue-17923-dispatch-contract-disarm-pin`\nReviewed-by: `session_013RDBh5DqXd2xnLwvHLgLFr`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
