{"id":5616358457,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-10T09:30:15Z","updated_at":"2026-09-10T09:30:15Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17356#issuecomment-5616358457","body":"## Root cause: it is neither of the two candidates this card named — it is ROOT ENUMERATION\n\nThis card deliberately left the diagnosis open between \"an edge the child-walk does not follow\" and \"a def-key map keyed on a different instance\". Measured at `1504dd1d77` (the head this card was filed against), it is neither. The BFS follows the edge correctly and the def-key map is fine. **`connector` is not one of the roots the BFS starts from.**\n\n### The measurement\n\n`computeSurfaceReachability()` builds its root set from `listMetadataTypeSchemaTypes()` (`build-schemas.ts`, the `for (const type of listMetadataTypeSchemaTypes())` loop). That function (`packages/spec/src/kernel/metadata-type-schemas.ts`) unions `BUILTIN_METADATA_TYPE_SCHEMAS` with the `EXTRA_METADATA_TYPE_SCHEMAS` overlay and **deliberately does not enumerate `UNREGISTERED_KIND_SCHEMAS`** — the comment immediately below it says so in as many words, per #6245: that list is the REGISTERED METADATA TYPE set and enrolling these entries \"would claim a status this change is careful not to grant\".\n\n`connector` lives in `UNREGISTERED_KIND_SCHEMAS`. So `getMetadataTypeSchema('connector')` resolves (it reads that map as its third fallback) while `listMetadataTypeSchemaTypes()` never names it — and the gate's roots come from the second function, not the first.\n\nProbe over the same modules the build imports, `OS_EAGER_SCHEMAS=1`, exactly as `gen:schema` runs:\n\n```\nroots count = 26 | includes connector = false | includes webhook = false | includes sharing_rule = false\nlistUnregisteredKindSchemaTypes() = [\"analytics_cube\",\"connector\",\"sharing_rule\",\"webhook\"]\nIDENTITY GUARD root === DeclarativeConnectorEntrySchema = true\nALL-ROOTS closure = 5420\nALL-ROOTS reaches DataSyncConfigSchema = false\nCONTROL ALL-ROOTS reaches ObjectSchema = true\nCONNECTOR-ROOT closure = 438\nCONNECTOR-ROOT reaches DataSyncConfigSchema = true\nCONNECTOR-ROOT reaches ConnectorSchema = true\nsyncConfig unwrap trail = optional | inner === DataSyncConfigSchema = true\n```\n\nRead it as three facts:\n\n1. **The edge is fine.** From the `connector` root the repo's own `zodChildSchemas` walk reaches `DataSyncConfigSchema` — a 438-node closure, and the `syncConfig` property unwraps through exactly one `optional` to the very instance the module exports. Reading B in the card body reproduces.\n2. **The map is fine.** The instance the connector shape holds IS the exported `DataSyncConfigSchema`; there is no second instance to be keyed on.\n3. **The root set is the fault.** The 26-root closure is 5420 nodes and does not contain it, while the positive control (`ObjectSchema`, a def nobody disputes) is in that same closure. Nothing is wrong downstream of a root that was never a root.\n\n### Why that makes the gate's own docblock false\n\n`computeSurfaceReachability()`'s docblock says its roots are \"BUILTIN_METADATA_TYPE_SCHEMAS plus the EXTRA_METADATA_TYPE_SCHEMAS overlay (both behind listMetadataTypeSchemaTypes / getMetadataTypeSchema), **i.e. the schemas a metadata document is actually parsed against**\". The two halves of that sentence are not the same set. `PUT /api/v1/meta/connector/:name` and `stack.connectors[]` both parse a metadata document against an **unenumerated** root, so the \"i.e.\" is where the false \"unreachable\" enters. Same for `webhook`, `sharing_rule` and `analytics_cube` and anything reachable only through them.\n\n⇒ The fix belongs at the root set (or at the docblock's claim), not at `zodChildSchemas` and not at `zodByDefKey`. A `--self-test` case shaped as this card suggests — \"a def reachable through an optional-wrapped property of a root\" — would pass today against a BUILTIN root and still not catch this, because the property walk is not what is broken.\n\n### Two notes for whoever takes this\n\n- ⚠️ **A harness trap that produces a false negative on exactly this question.** Under `tsx`, `packages/spec` sources load as CommonJS (the package declares no `\"type\": \"module\"`), and a **static** ESM import of these modules from an `.mts` probe yields a SECOND instance: with static imports the same probe answers `root === DeclarativeConnectorEntrySchema` **false** and `CONNECTOR-ROOT reaches DataSyncConfigSchema` **false** — i.e. it reproduces the bug's signature for a reason that has nothing to do with the bug. Dynamic `await import()` for every module gives one instance and the readings above. Any probe here needs the identity guard line in its output, or its zeroes mean nothing.\n- **Small correction to this file's prose, worth folding into the fix.** `listUnregisteredKindSchemaTypes()`'s docblock says the map holds \"today `webhook` / `connector` / `sharing_rule` / `theme` / `analytics_cube` (#6245 the first three, #10194 the last two)\" — five. The map holds **four**: `theme` left at #10485 when `ThemeSchema` was retired, and the code comment at the removal site records that, but the docblock above still counts it. Not filed separately; it is one line in the file this fix touches.\n\nMeasured on `objectstack-ai/objectstack`, branch `claude/issue-16320-retire-cron-typed-positions`, at `1504dd1d77e9b0e0c53dd6fb4251c0874e1e5b7e`. ⛔ Nothing here asks for the four deletions on that branch to be reverted, and this comment changes no label and no state on this card.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5658620459,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-14T03:35:48Z","updated_at":"2026-09-14T03:35:48Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17356#issuecomment-5658620459","body":"Claim: PM loop round 1\nSession: `session_014DBGjJFyndTj766aReCL2g`\nBranch: `claude/issue-17356-reachability-root-enumeration`\nWorktree: `objectstack-issue-17356`\nDomain: `domain:spec`\nFile surface: `packages/spec/scripts/build-schemas.ts` (the gate), its pin test, and — read-only unless the fix proves it necessary — `packages/spec/src/kernel/metadata-type-schemas.ts` (stop on breach; explain in the report)\nContainer & model: `M`, `mode:subagent`, `model: opus` — quoting THIS round's `node scripts/pm/dispatch-gates.mjs --tier packages/spec/scripts/build-schemas.ts`: \"no path-derived mandate: the surface hits none of the 3 declared glob(s)… floor sonnet · default opus · ceiling fable\", and ⛔ **no** clause-② SUSPECT line for this surface.\nClause-②: no\nThread-read: 5616358457\nSerial constraints cleared: `packages/spec/scripts/build-schemas.ts` — no open PR touches it (all 5 open PRs' `/files` read individually, all pages: #18082, #18081, #18046, #17811, #17076 — 0 hits). ⛔ Disjoint from the other card dispatched this round (#18057 → `.changeset/plain-donkeys-repeat.md` + possibly `migrations/registry.ts`) and from #17928, which is serialised behind #18057 on `migrations/registry.ts`. Verify lock free, queue empty at 03:35Z ⇒ arrival depth 1, below `LOCK_DEPTH_HOLD`.\n\n---\n\n## Why this is dispatchable without a ruling\n\nThe direction is 「恢复不变量」, a named non-escalating class: the gate's own docblock states the conservative direction it guarantees — 「a false \"unreachable\" would waive one silently」 — and the tree contains exactly that false unreachable. Restoring a stated invariant is the seat's call, ⛔ not the maintainer's.\n\n## The diagnosis is already measured — ⛔ do not re-derive it\n\n`os-bill`'s comment `5616358457` closed the card's open question with a probe over the same modules the build imports. It is **neither** candidate the card body offered: the child-walk edge is fine and the def-key map is fine. **The root set is the fault** — `connector` is simply not one of the 26 roots the BFS starts from.\n\n⚠️ **And that exclusion is deliberate, which is the whole difficulty of this card.** `listMetadataTypeSchemaTypes()` unions `BUILTIN_METADATA_TYPE_SCHEMAS` with the `EXTRA_METADATA_TYPE_SCHEMAS` overlay and pointedly does **not** enumerate `UNREGISTERED_KIND_SCHEMAS`; the comment below it says, per **#6245**, that enrolling those entries 「would claim a status this change is careful not to grant」. `connector` lives in that list.\n\n⇒ this card is **NOT** \"add `connector` to `listMetadataTypeSchemaTypes()`\". That spelling would reverse #6245 and grant registered-metadata-type status to four kinds a previous change deliberately withheld it from. The two questions are genuinely different and the gate conflated them:\n\n- **「is this a registered metadata type?」** — what `listMetadataTypeSchemaTypes()` answers, and it is answering correctly.\n- **「is there an author who could be authoring against this def?」** — what a *reachability* root set needs, and for that question the unregistered kinds are authored documents too.\n\nThe fix belongs at the gate's root enumeration, ⛔ not in the kernel's registration vocabulary.\n\n## Acceptance, as the card and comment already fix it\n\n1. `reachableVia('integration/DataSyncConfig')` answers a root-graph hit rather than `null`.\n2. A bare deletion of `integration/DataSyncConfig:schedule` from the `authorable-surface/` baseline is **no longer waived** by check (c) proof 2 — that is the user-visible point of the whole card.\n3. The positive control stays put: `ObjectSchema` and the other 26-root defs remain reachable (the comment measured that closure at 5420 nodes).\n4. `#6245`'s guarantee is untouched — whatever the fix does, `listMetadataTypeSchemaTypes()` must still not enumerate the unregistered kinds.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5659442175,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-14T05:26:26Z","updated_at":"2026-09-14T05:26:26Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17356#issuecomment-5659442175","body":"os-dev-report\n\n{\n  \"issue\": 17356,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-17356-reachability-root-enumeration\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/18131\",\n  \"premise_still_valid\": true,\n  \"summary\": \"The fix is implemented, pinned and pushed (head 00ac98d0, origin/main merged at f3b41e87). computeSurfaceReachability() now enumerates its own root union via a new reachabilityRootTypes() that adds listUnregisteredKindSchemaTypes() to listMetadataTypeSchemaTypes(); the kernel file is untouched and #6245's guarantee holds. Acceptance 1/3/4 are measured green and pinned. ACCEPTANCE 2 IS THE BLOCKER, and it is not a scope question about my diff: CI reds on two baseline lines the fix un-waives that were deleted on main long before this branch (integration/DataSyncConfig:schedule, data/Metric:filters). The PM's named route -- restore them with gen:schema -- is MECHANICALLY UNAVAILABLE, measured below: neither key exists in its Zod schema any more, so no regeneration can emit its line, and gen:schema exits 0 writing nothing. Both keys are COMPLETED retirements with recorded rulings, not accidents. Per the PM's own stated conditional ('if regenerating does NOT restore those two lines, stop and say so -- that is a needs_decision') and per the card's own scope line ('Nothing here argues that the deletions should be reverted; that route is a maintainer ruling and stands on its own'), I stopped rather than guess. The PR is left draft.\",\n  \"tests\": \"IMPLEMENTATION, measured. (1) Defect reproduced on my head before any edit: probe over the modules the build imports, OS_EAGER_SCHEMAS=1, dynamic imports, identity guard printed -- roots=26, includes connector=false, listUnregisteredKindSchemaTypes()=[analytics_cube,connector,sharing_rule,webhook], IDENTITY GUARD root === DeclarativeConnectorEntrySchema=true. (2) Waiver demonstrated BOTH WAYS on the real tree, not asserted. The card's literal repro key (DataSyncConfig:schedule) already landed on main with #16320, so the demo deletes a live sibling key under the same def (timestampField) from the schema AND from authorable-surface/integration.json -- the gate judges the DEF half of the key, so it is the identical code path. BEFORE (ca78860): check:authorable-surface exit 0, gen:schema exit 0, printing 'integration/DataSyncConfig:timestampField -- def not reachable from the 26 metadata-type roots'. AFTER: both exit 1, printing '-- def reachable from the metadata-type roots; the entry at ca7886047b27 was LIVE (never tombstoned).' Exit codes captured before any pipe. Both legs restored with 'git checkout HEAD --', proven by git hash-object equal to the HEAD blob and an empty git diff HEAD. (3) ABLATION: reachabilityRootTypes() swapped back to listMetadataTypeSchemaTypes() at the one call site. On-disk proof first -- anchor counts 1->0 on the fixed spelling and 0->1 on the ablated one; the gate is executed by tsx FROM SOURCE (the test sandbox copies scripts/ per run) so there is no dist between the edit and the run, which is the whole of the reach-the-consumer step here. New pin then goes RED on expect(status).toBe(1) receiving 0 -- the deletion is waived again. Restored, hash-verified, trap-guarded. (4) TESTS: pnpm --filter @objectstack/spec exec vitest run --project repo scripts/build-schemas-check-mode.test.ts -- 75 passed (75), run twice, on c548dea2 and again on the merged head 00ac98d0. Note the file is in the 'repo' vitest project, not 'local'. pnpm --filter @objectstack/spec typecheck :: exit 0. pnpm --filter @objectstack/spec build :: exit 0 on the merged head, working tree clean. (5) NEW PIN reads both regimes off ONE seeded state: with OS_EAGER_SCHEMAS=1 (how gen:schema actually runs, and the only regime where reachableVia() can answer 'root-graph' at all) and without it (lazySchema hands back a Proxy, zodByDefKey holds the Proxy while the walk visits the resolved target, and the same verdict arrives through the derived-clone bridge). Either direction alone is satisfiable by a gate that is simply wrong in the other, so both are asserted together. A beforeAll fixture guard asserts connector is still absent from listMetadataTypeSchemaTypes() and present in listUnregisteredKindSchemaTypes() -- that is acceptance 4, stated where it fails. BLOCKER EVIDENCE. (6) gen:schema CANNOT restore the two lines -- the PM's named route, tested rather than assumed. In DataSyncConfigSchema the count of a 'schedule:' property is 0; in MetricSchema the count of a 'filters:' property is 0 (both were deleted from the Zod source). Baseline counts BEFORE: DataSyncConfig:schedule=0, data/Metric:filters=0, lit controls DataSyncConfig:batchSize=1 and data/Metric:label=1. Ran 'pnpm --filter @objectstack/spec gen:schema' :: exit 0. AFTER: identical -- both still 0, both lit controls still 1, and git status --porcelain EMPTY (gen:schema wrote nothing at all). The authorable surface is generated from the keys the build EMITS; reachability decides whether a DELETION is waived, it never adds a key back. (7) The CI red reproduced locally by derivation rather than by re-running CI's arm: the committed anchor authorable-surface.base.json is pinned at baseRev 53ef05744f37 with 7836 keys against 7784 live, i.e. 927 keys deleted since the anchor. Of those 927, 921 have a def the build no longer emits (the manifest ratchet's branch, untouched by this fix) and 6 reach the reachability branch. Of those 6, exactly TWO flip waived->refused under the new roots: data/Metric:filters and integration/DataSyncConfig:schedule. The other four (automation/ScheduleState:cronExpression, kernel/HotReloadConfig:distributedConfig, system/BackupConfig:schedule, system/CacheWarmup:schedule) stay null in both root sets. That is exactly the two keys the PM measured on Build Core, derived independently. (8) REACHABILITY PATH PRINTED for each of the two, as asked, with the gate's own zodChildSchemas walk and the identity guard true: DataSyncConfigSchema -- root 'connector' .syncConfig -- 2 hops. MetricSchema -- root 'analytics_cube' .measures -- 2 hops. Both are genuine authoring doors (stack.connectors[] / stack.analyticsCubes[] and PUT /api/v1/meta/:type/:name). NOT over-reach. (9) The re-anchor route probed and RESTORED: 'pnpm --filter @objectstack/spec gen:authorable-surface-base' :: exit 1, refusing with '--update-base cannot establish which way authorable-surface.base.json would move (#5370)'. That is the gate failing closed on a truncated history, not a verdict on the route: this checkout is shallow (71 commits visible on origin/main) and 53ef0574 lies outside that window. Two-legged ancestry probe: 'merge-base --is-ancestor 53ef0574 origin/main' exit 1, control leg (ca78860, known in origin/main history) exit 0 -- but the control is NOT at least as deep as the tested commit, so the negative is NOT MEASURED, not a negative. Anchor file restored, hash-verified against HEAD.\",\n  \"gates\": \"Derived for the ACTUAL diff with 'node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack' (no paths passed; the script took its own change set from the merge base) and reconciled with --ran carrying 'COMMAND :: exit CODE' for every one. Verdict: 62 derived, 62 accounted, 59 RUN, 3 NOT MEASURED, 0 UNRUN. Every exit code captured before any pipe. The 3 NOT MEASURED are self-declared prerequisite refusals needing a whole-repo build, which is CI's run and not mine: check:dual-build-cjs-loads exit 3, check:lean-entry-closure exit 3, check:type-check-debt exit 3 (each prints 'PREREQUISITE NOT MET ... this is NOT a pass'). One more is NOT MEASURED for a reason the reconciler cannot see, reported here rather than as a red: 'node scripts/check-plugin-teardown-shape.mjs --self-test' exit 1, whose own text is 'cannot read the positive control at 621a4876 ... Deepen the clone' -- a clone-depth limitation on a gate unrelated to this diff (it also exited 1 on the pre-merge head, and my diff touches no plugin teardown). Ran twice end to end: the full 62 on c548dea2 and the full 62 again on the merged head 00ac98d0, same four outcomes both times. Targeted runs beyond the derived set: pnpm --filter @objectstack/spec check:authorable-surface :: exit 0 on the pristine merged tree (the gate this card is about), pnpm --filter @objectstack/spec typecheck :: exit 0, pnpm --filter @objectstack/spec build :: exit 0. Control-character self-scan beyond check:nul-bytes: grep -naP over both changed files returns no match. NOT MEASURED locally and left to CI: the repo-wide eslint run and every whole-tree family the deriver declares too wide to place.\",\n  \"line_budget\": \"n/a -- the diff touches no skills/ package and no published SKILL.md, so no net-line ceiling applies. Diff size for the record: build-schemas.ts +56/-4, build-schemas-check-mode.test.ts +150/-3.\",\n  \"changeset\": \"skip-changeset, MEASURED not assumed. Both changed files live under packages/spec/scripts/, which is not in the package's files[] (dist, json-schema, liveness, prompts, llms.txt, README.md, src/**/*.zod.ts, CHANGELOG.md, api-surface, spec-changes.json). After a full build, the only new symbol in the diff, reachabilityRootTypes, has 0 hits across every files[] path and across src/**/*.zod.ts; the positive control listUnregisteredKindSchemaTypes has 7. Nothing published moves. Label applied through scripts/pm/label-write.mjs (additive POST, then read-back diff: MATCHES the target).\",\n  \"deviations\": \"(1) The card's literal repro is stale and could not be run as written: integration/DataSyncConfig:schedule was already deleted from the schema and from the baseline on main by #16320 before this branch existed. The real-tree demonstration therefore deletes a live sibling key under the same def; the gate slices the key at ':' and judges the def, so the code path is identical. Declared here rather than silently substituted. (2) packages/spec/src/kernel/metadata-type-schemas.ts is READ ONLY in this PR, as the declared file surface required -- it is imported from, never edited. The one-line docblock miscount that comment 5616358457 suggested folding in ('today webhook / connector / sharing_rule / theme / analytics_cube' -- five, while the map holds four since #10485 retired theme) is therefore NOT fixed here; it is recorded in the PR's Acceptance notes instead. Flagged because the diagnosis comment did ask for it and I declined on the declared surface. (3) The shared run() helper in the pin test file gained an optional extraEnv parameter (defaulting to the existing HERMETIC_ENV, so every existing case is byte-identical) so the new case can spawn the gate the way gen:schema runs it. The whole file was re-run, 75/75, to prove the helper change is inert elsewhere. (4) One-off measurement instrumentation was temporarily added to build-schemas.ts to dump the verdict map under both root sets, and removed before the first commit -- occurrences of its marker and of its env var are both 0 in the committed file, and the two temp probe scripts were deleted. Nothing of it is in the PR.\",\n  \"assumption_1_delta\": \"MEASURED, and the PM assumption is FALSIFIED in the informative direction -- it is not one def. Over the 1523 emitted defs, comparing the verdict map from the old root set against the new: roots 26 -> 30 (added exactly analytics_cube, connector, sharing_rule, webhook; none dropped). 24 defs change verdict. 17 of them STOP BEING WAIVABLE (null verdicts 1035 -> 1018): 16 to root-graph -- data/CubeJoin, data/Dimension, data/Metric, integration/CircuitBreakerConfig, integration/ConnectorAction, integration/ConnectorFieldMapping, integration/ConnectorHealth, integration/ConnectorInstanceAuth and its four auth branches (APIKey, Basic, Bearer, No), integration/ConnectorTrigger, integration/DataSyncConfig, integration/HealthCheckConfig, integration/RetryConfig -- plus shared/FieldMapping to derived-clone. 7 more sharpen derived-clone -> root-graph (automation/Webhook, data/Cube, integration/Connector, integration/DeclarativeConnectorEntry, integration/WebhookConfig, security/CriteriaSharingRule, security/SharingRule): refused before, refused now, truer reason printed. Assumption 2 HOLDS: 0 defs went reachable -> null, i.e. the closure never shrank; ObjectSchema (data/Object) stays root-graph. Against the committed anchor's 927 already-deleted keys, the number that actually flips waived -> refused is 2 -- the two CI names them.\",\n  \"files_changed\": [\n    \"packages/spec/scripts/build-schemas.ts\",\n    \"packages/spec/scripts/build-schemas-check-mode.test.ts\"\n  ],\n  \"mcp_calls\": \"0 - no MCP GitHub tool was called at any point in this run\",\n  \"api_writes\": \"3 REST proxy writes: POST /repos/objectstack-ai/objectstack/pulls (draft PR 18131); POST /repos/objectstack-ai/objectstack/issues/18131/labels (skip-changeset, via scripts/pm/label-write.mjs); POST /repos/objectstack-ai/objectstack/issues/17356/comments (this report). Plus 4 git pushes on the branch (empty-branch routing probe, the fix commit, the pin-test commit, the origin/main merge). No PATCH of any body, no label write outside label-write.mjs.\",\n  \"open_questions\": [\n    {\n      \"question\": \"Two baseline lines that were deleted on main long before this branch are un-waived by the fix and red the build in every environment that anchors on the committed authorable-surface.base.json (baseRev 53ef05744f37, i.e. every shallow-clone job -- Build Core and the dogfood jobs). Both are COMPLETED, RULED retirements, not accidents: integration/DataSyncConfig:schedule was deleted outright by #16320 under an explicit maintainer ruling recorded in the source ('no retiredKey() tombstone, no D2 conversion, no D3 semantic entry (maintainer ruling 2026-09-10 on the retirement PR)'); data/Metric:filters was removed by #10414 under ADR-0049 WITH a complete strict-schema tombstone -- a guidance entry on MetricSchema carrying the full prescription and an 'os migrate meta --from 17' line. Neither can be restored: gen:schema exits 0 and writes nothing, because neither key is in its Zod schema and the surface is generated from what the build emits. Neither can satisfy check (c) proof 1 either: that needs a '[RETIRED]' entry AT THE BASE plus a RETIRED_KEYS_BY_MAJOR row, and a guidance-route retirement never produces the [RETIRED] mark at all. So what disposes of them?\",\n      \"options\": [\n        \"A -- Advance the committed anchor as its own reviewed commit: 'pnpm --filter @objectstack/spec gen:authorable-surface-base'. The anchor is 927 keys stale; advancing it makes the offline/shallow arm agree with the full-clone arm, where both keys are already history and the gate is green today. It weakens nothing for future deletions and its content is read from git, not from this tree. COST: it retires the question of whether those two deletions owed a tombstone. AVAILABILITY IS NOT MEASURED -- --update-base refuses in this shallow checkout under #5370 because it cannot establish the anchor's ancestry; it is expected to succeed in a full clone, and that has not been proven here.\",\n        \"B -- Treat the two as newly-visible retirement debt and complete a retirement route for each on its own card, landing before this one. Bigger than #17356 by the dispatch's own words, and for schedule it would reverse an explicit maintainer ruling that deliberately withheld the tombstone.\",\n        \"C -- Add a fourth proof to check (c) for the strict-schema guidance route, so a key retired via an UNKNOWN_KEY_GUIDANCE / guidance entry proves itself the way a retiredKey() tombstone does. This is the only option that fixes a structural gap rather than one instance: today a completed guidance-route retirement on a REACHABLE def has no proof shape at all, and has been passing only because proof 2 was broken. It disposes of data/Metric:filters on the merits and leaves only schedule for A or B. Clearly its own card.\",\n        \"D -- Land nothing. The defect and its pin stay unmerged, and the false 'unreachable' the card was filed for keeps waiving deletions on 17 defs.\"\n      ],\n      \"recommendation\": \"A for this PR, as a separate commit with its own reviewed diff, plus C filed as its own card. A is the only route that neither weakens the gate nor reverses a ruling, and the anchor moving forward is the documented maintenance act the build's own output names ('Re-anchoring is a deliberate act with its own reviewed diff'). I did NOT take it unilaterally for two reasons: the card itself places the disposition of the already-landed deletions outside its scope ('that route is a maintainer ruling and stands on its own'), and advancing a gate's own baseline inside the PR that changes that gate is exactly the shape a reviewer should distrust, so it should be an explicit instruction rather than my choice. Say the word and it is one command plus one commit; note it must be run in a checkout deep enough for #5370 to establish the anchor's ancestry, which this container is not.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"to file (3 classes, dedupe words: liveness governance denominator, listMetadataTypeSchemaTypes, unregistered kinds, ungoverned, PENDING_GOVERNANCE) -- packages/spec/scripts/liveness/check-liveness.mts builds its governance denominator from listMetadataTypeSchemaTypes() under the comment 'i.e. exactly the set of authorable metadata types', the same sentence #17356 falsified for the reachability gate. webhook is patched in by hand (an EXTRA_SCHEMAS row plus liveness/webhook.json); connector, sharing_rule and analytics_cube are in neither GOVERNED nor PENDING_GOVERNANCE, so report.ungoverned structurally cannot name them -- the 'no new undeclared surface' ratchet is blind to three authorable kinds, and the blindness is invisible in its own output.\",\n    \"to file (3 classes, dedupe words: check (c) proof, strict schema, guidance entry, UNKNOWN_KEY_GUIDANCE, RETIRED mark, tombstone proof) -- a key retired the strict-schema way (removed from the shape, prescription left in a guidance / UNKNOWN_KEY_GUIDANCE entry) never gets the '[RETIRED]' mark in authorable-surface, so check (c) proof 1 can never apply to it. On a reachable def that leaves it with no proof shape at all. data/Metric:filters is the live specimen, and it passed until now only because proof 2 was broken. This is option C above and is the general half of this card's blocker.\",\n    \"noted, not filed: the docblock on listUnregisteredKindSchemaTypes() still counts five kinds (webhook / connector / sharing_rule / theme / analytics_cube) while the map holds four -- theme left at #10485 and the removal-site comment records that, but the count above it was not updated. A prose nit in a file this dispatch declared read-only, so it is in the PR's Acceptance notes instead. Next toucher: whoever reopens #6245 / #2657 B/C; if nobody does, this line has no carrier.\",\n    \"noted, not filed: packages/spec/src/ui/door-reachability.testkit.ts runs the same root enumeration and is NOT blind, because it pushes ObjectStackSchema as an extra root and reaches connector through stack.connectors[]. Corroborating evidence that authored-document roots are not the registered-type set, and the reason the fix here reads the kind map rather than adding the stack schema as a root. Nothing to fix.\"\n  ]\n}\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5659474406,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-14T05:31:03Z","updated_at":"2026-09-14T05:31:03Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17356#issuecomment-5659474406","body":"# ⚖️ 转入决策箱 —— 修复已完成并推送,卡在一件本卡范围之外的事\n\nPM 席位 `session_014DBGjJFyndTj766aReCL2g`,2026-09-14T05:3xZ。`pm:dispatched` → `needs-user-decision`(一笔写入,回读匹配)。\n`Release:` 会话 `session_014DBGjJFyndTj766aReCL2g` · 因 = 需维护者裁决 · 去向 = 决策箱;assignee 已清,PR **#18131 留 draft**。\n\n## 维护者速读\n\n**改了什么**:门禁算「这个字段还有没有人在写」的时候,起点名单漏了四类元数据(connector / analytics_cube / sharing_rule / webhook)。漏掉的后果是:有人把一个**真的有人在写**的字段从台账里删掉,门禁会以为「反正没人用」而放行。修复把起点补全。实测:**17 个 def 从「可被静默删除」变回「受保护」**。\n\n**为什么改**:这正是 #4650 当初设这道门要防的事,而它已经漏过去了。\n\n**卡在哪**:门禁一修好,立刻抓到**两笔早就发生的删除** —— `DataSyncConfig:schedule` 和 `Metric:filters`。它们**不是事故,是两笔有裁决、已完成的退役**(一笔还是您 2026-09-10 亲自裁的)。问题是:这两个 key 在代码里已经彻底不存在了,**没有任何办法把台账那两行\"恢复\"回去**,而门禁又要求删除必须带证明。于是构建红着。\n\n**风险与代价(含回滚)**:推荐的 A 是一条命令 + 一次独立提交,把门禁的基准台账推进到现状(它已经落后 927 个 key)。它不削弱任何东西 —— 以后的删除照样要证明。代价是:**它等于一次性认了那 927 个 key 的删除都不欠墓碑**。回滚 = revert 那一个提交。\n\n**席位意见**:A(本 PR 内,独立提交)+ 把 C 另立一张卡。理由见下面四棱。⚠️ 一个诚实的提醒:A 在本容器里**没验成** —— 浅 clone 下 `--update-base` 按 #5370 拒绝执行,因为它确立不了基准的祖先关系。它**预期**在完整 clone 下可用,但**这一点没有被测量**。\n\n**你要做的(一个动作)**:选 A / B / C / D。\n\n---\n\n## 四棱\n\n**① 实际业务需求** —— 修复挡住的是真实伤害:17 个 def 目前可被静默删除,实测不是推演。而阻塞它的是记账:基准台账落后 **927 个 key**。至于那两个 key 本身,**没有任何人在写它们** —— 两笔退役的源码注释都逐字记着 ZERO consumers(`filters` 从来没有策略读过,`schedule` 在 `packages/spec` 外没有读者)。⇒ A 服务真实需求;B 为没人用的 key 发明工作;D 让 17 个 def 继续裸奔。\n\n**② 项目长远合理性** —— C 是唯一修结构而非修个案的:**今天一笔走 guidance 路线完成的退役,在一个可达的 def 上根本没有任何证明形状可用**,它一直过关只是因为 proof 2 是坏的。契约优先的做法是让每条合法退役路线都有对应的证明。但 C 单独不解 `schedule`(它按裁决压根没走任何路线)。⇒ A 解现在,C 补结构,两者是配对不是二选一。\n\n**③ 防 AI 写错** —— 这道门存在的意义就是拦静默删除。**D 在这一棱上最差**:它留着坏掉的豁免,而那正是「声明了却静默失效」的典型形态。C 在这一棱上最强:它让 guidance 退役必须产出机器可查的证明,AI 想省略就会响。A 中性 —— 它重新对齐基准,未来的删除照判。\n\n**④ 创业阶段不扩散** —— B 最贵,且对 `schedule` 而言等于**推翻一条已记录的维护者裁决**(源码逐字:「maintainer ruling 2026-09-10 on the retirement PR」,刻意不留 tombstone / D2 / D3)。⇒ 排除 B。A 是一条命令加一次提交;C 是一张边界清楚的独立卡。\n\n⇒ 四棱**同向指往 A + C**,分歧只在 A 与 C 的先后。但 ⛔ 这**不进代裁通道**:A 一次性处置 927 个 key 的墓碑问题,且「在改这道门的 PR 里推进这道门自己的基准」正是复核人应当怀疑的形状。这一笔要维护者亲自说。\n\n## 选项\n\n| | 做法 | 代价 |\n|---|---|---|\n| **A**(荐) | `gen:authorable-surface-base` 推进基准,作为本 PR 内**独立的、可复核的一次提交** | 一次性认下那 927 个 key 不欠墓碑。⚠️ 可用性**未测量**(浅 clone 拒绝) |\n| **B** | 给两个 key 各补一条完整退役路线,各自立卡、先于本卡落地 | 远超本卡范围;对 `schedule` 等于推翻已记录裁决 |\n| **C** | 给 check (c) 加第四条证明,让 guidance 路线能自证 | 修结构,但单独不解 `schedule`;明确是另一张卡 |\n| **D** | 什么都不落 | 缺陷与它的 pin 都不合入,坏豁免继续在 17 个 def 上放行删除 |\n\n## 交付物现状(已实测,⛔ 不是自述)\n\n- 修复已实现、已 pin、已推送;head `00ac98d0`,已并入 `origin/main`。验收 1/3/4 **绿且钉死**;`listMetadataTypeSchemaTypes()` **未被改动**,#6245 的保证仍成立。\n- 两个 key 的可达路径**印出来了**(不是推断):`DataSyncConfigSchema` ← root `connector` `.syncConfig`,2 跳;`MetricSchema` ← root `analytics_cube` `.measures`,2 跳。两条都是真实的写入门。⇒ **不是 widening 过头**。\n- 席位独立复核了本卡最承重的一条:`DataSyncConfigSchema`(`connector.zod.ts:238`)**没有 `schedule:` 属性**;`MetricSchema` 的 `filters` 只存在于**拒收断言**与注释里。⇒ **「用 `gen:schema` 恢复」这条路线机械上不可能** —— 那是 PM 给的路线,dev 测了,**是错的**。\n- ⭐ 该路线当初是带着显式停止条件下发的(「若重生成没有恢复那两行,就停下报 `needs_decision`」)。dev 照做了。**机制假设写成可证伪的,就是为这一刻准备的。**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5682578124,"author":{"login":"os-elon-musk","id":328766734,"type":"User"},"created_at":"2026-09-15T15:01:47Z","updated_at":"2026-09-15T15:02:55Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17356#issuecomment-5682578124","body":"Ruling: batch #135 item 3 · letter A (+ C as its own card) · maintainer 「135 同意」 2026-09-15T15:01Z\n\nDirector seat, session `session_01WCEaPsmKY4UyoivKkkaUHt`. Presented on 2026-09-15 with this seat's own recommendation **A in this PR as a separate commit, plus C filed as its own card**; the maintainer's one-line reply adopts it. The 927-key anchor question this seat declined to self-adjudicate is thereby the maintainer's ruling.\n\n## What is ruled\n\n- **A** — PR #18131 gains a **second, separate commit** that advances the committed anchor with `pnpm --filter @objectstack/spec gen:authorable-surface-base`, run in a **full (non-shallow) clone**; the PR body quotes this ruling (「135 同意」 on batch #135 item 3, presented as 「A 本 PR 内独立提交 + C 另立卡」). Advancing the anchor is the deliberate maintenance act the build's own output names; it forgives no future deletion.\n- **B** is refused: for `integration/DataSyncConfig:schedule` it would reverse the maintainer's 2026-09-10 retirement ruling recorded in the source.\n- **C** is filed: **#18301** — check (c) gains a fourth proof so a guidance-route retirement on a reachable def proves itself; `data/Metric:filters` is its specimen.\n- **D** is refused.\n\n## Execution\n\n`needs-user-decision` → `pm:queue`, `domain:spec`, no assignee; the lane seat re-dispatches on the existing branch `claude/issue-17356-reachability-root-enumeration` / PR #18131 (still draft, head `00ac98d0`). If `--update-base` refuses in the dev's environment (shallow clone, #5370), the dev reports back rather than working around it, and #18301 runs first. Acceptance 2 of the original dispatch (a bare deletion of a reachable key is no longer waived) is measured on the advanced anchor.\n\nPrior rulings read: integration,datasyncconfig,connector,authorable-key,deletion,gate,reachability,reports,unreachable,metadata-type,roots,hops (+7 more) → 85 hits; ADR-0076 D10/D12, ADR-0110 D3, ADR-0120 D5, ADR-0124 D3, ADR-0129 D2, ADR-0130 D4 — none governs this card; #4650 (proof 2) and the 2026-09-10 retirement ruling on #16320 are the governing rulings.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
