{"id":5815454665,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-24T13:53:41Z","updated_at":"2026-09-24T14:28:59Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19972#issuecomment-5815454665","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nThis PR changes **2** package(s): `@objectstack/driver-sql`, `@objectstack/driver-turso`, touching **6** documentable anchor(s). ⚠️ **1** changed file(s) yielded no anchor (`packages/drivers/driver-sql/src/index.ts`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n**8** hand-written doc(s) NAME something this change touched and may need an implementation-accuracy re-verification:\n\n- `content/docs/data-modeling/drivers.mdx`  _(via SqlDriver (symbol, a top-level class))_\n- `content/docs/data-modeling/index.mdx`  _(via SqlDriver (symbol, a top-level class))_\n- `content/docs/permissions/tenant-audit-census.mdx`  _(via SqlDriver (symbol, a top-level class))_\n- `content/docs/plugins/packages.mdx`  _(via SqlDriver (symbol, a top-level class))_\n- `content/docs/protocol/kernel/index.mdx`  _(via SqlDriver (symbol, a top-level class))_\n- `content/docs/protocol/kernel/lifecycle.mdx`  _(via SqlDriver (symbol, a top-level class))_\n- `content/docs/protocol/objectql/query-syntax.mdx`  _(via SqlDriver (symbol, a top-level class))_\n- `content/docs/protocol/objectql/types.mdx`  _(via SqlDriver (symbol, a top-level class))_\n\n⛔ **1** release-owned page(s) also name something this change touched. These are **read-only**:\n\n- `content/docs/releases/v17/17-0.mdx`  _(via SqlDriver (symbol, a top-level class))_\n\n> `content/docs/releases/` is RELEASE-OWNED (AGENTS.md \"Documentation Guardrails\"): release\n> notes are written centrally at release time, and a code PR that edits them is the exact PR\n> that guardrail exists to stop. They are still audited — read-only. If one of them is actually\n> wrong, **file an issue** or open a dedicated docs-only PR; do not edit it here.\n\n<details><summary>What this run could not see</summary>\n\n- **1** changed file(s) yielded no anchor (`packages/drivers/driver-sql/src/index.ts`) — pages documenting those are invisible to this run\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **13** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 2c1011b01bc071c545f72f2761647b8d9ab56375` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `ae48274781cc250b9329ca9019908a1adcf23e3d` — the merge of head `ff9881da77612518a51c934c9b593f2bf54397aa` into base `2c1011b01bc071c545f72f2761647b8d9ab56375`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin ae48274781cc250b9329ca9019908a1adcf23e3d && git checkout ae48274781cc250b9329ca9019908a1adcf23e3d\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 2c1011b01bc071c545f72f2761647b8d9ab56375 ff9881da77612518a51c934c9b593f2bf54397aa && git checkout -B drift-repro 2c1011b01bc071c545f72f2761647b8d9ab56375 && git merge --no-ff ff9881da77612518a51c934c9b593f2bf54397aa\n\nnode scripts/docs-audit/affected-docs.mjs --json 2c1011b01bc071c545f72f2761647b8d9ab56375\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>\n\n> Advisory only, and a **precision-first** one (#9192): a page is listed because it names a\n> symbol, wire route or SDK method this diff touched — not because it mentions a changed\n> package. Each row says which anchor put it there, so a wrong row is reportable rather than\n> merely annoying. To re-verify, run the `docs-accuracy-audit` workflow scoped to these files:\n> `node scripts/docs-audit/affected-docs.mjs 2c1011b01bc071c545f72f2761647b8d9ab56375` → pass the list as\n> `args.docs`, on the commit named under **Which tree this was computed on**."}
{"id":5816045784,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T14:25:56Z","updated_at":"2026-09-24T14:25:56Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19972#issuecomment-5816045784","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `8cb2d8ea9efdb62f5dda2d3ac36abac5ba52591f`\n\n### ① Derived judgments\n\n1. The pre-filter is unchanged (`typeof(col) = 'text' and json_valid(col) = 0`), and every write is a strict subset of the old statement's: a candidate is rewritten only when `JSON.parse` rejects it and its stored bytes still equal the text read. No cell the old statement left alone is now rewritten. RIGHT: measured on node:sqlite.\n2. Cells the old statement converted that the new pass leaves alone: (a) text `json_valid` rejects but `JSON.parse` reads, i.e. depth past 1000 (the fix); (b) a candidate whose stored bytes the engine does not read back verbatim, so the compare-and-set misses. RIGHT as behaviour, in the safe direction: in every such case the read is unchanged from before the sync. Measured: (b) on better-sqlite3 13.0.3 (invalid UTF-8) and on sql.js 1.14.1 (a leading U+FEFF). See ③ for the changeset.\n3. Each rewrite is byte-identical to the retired `json_quote` output: 74,298 store-read-quote samples, 0 mismatches, on better-sqlite3 13.0.3 and 12.11.1. RIGHT.\n4. The compare-and-set guards a concurrent write: it compares under BINARY collation; test 5 injects through a second connection. RIGHT.\n5. Paging terminates and covers every candidate, including a page whose rows are all withheld; test 6 pins 1,100 rows. RIGHT.\n6. Idempotence: a converged re-run moves `total_changes()` by 0 even with a withheld cell present. RIGHT.\n7. New export `recoverUnencodedJsonText` on `@objectstack/driver-sql`'s root entry, addressable through `exports[\".\"]`. RIGHT.\n8. The Turso remote codec-residue backfill's behaviour is unchanged: the removed inline block is the body of the new function. RIGHT.\n9. Faces reached: `SqliteWasmDriver`, and `TursoDriver` local through `syncSchema`, `initObjects` and `syncSchemasBatch`. RIGHT. `SqlDriver` on the `sqlite3` / `sqlite` client spellings also inherits it (`SQLITE_EMIT_CLIENTS`).\n10. Failure posture stays `warn`, and the rows stay legacy; the durability log-level gate is green. RIGHT.\n\n### ② Semver level\n\nConsistent: `@objectstack/driver-sql: minor` for the additive export, `@objectstack/driver-turso: patch`, `Clause-②: no`, and no BREAKING marker owed. `Check Changeset` is `success` at head.\n\n**CI at head: `Lint & Repo Gates` is `failure`**, on step \"Issue citations this change adds resolve on the board\" (`check-issue-citations.mjs`, exit 2): `packages/drivers/driver-sql/src/unencoded-json-text.ts` cites `#12380` twice, and that number answers 404. The seat confirmed the 404. `main` at `2c1011b` is green on the same job, so the red is PR-caused. Two gates behind it never ran.\n\n### ③ Boundary flags\n\n1. FALSE, changeset: \"a cell it cannot read is a legacy plain string and is rewritten to `JSON.stringify` of that string — byte-for-byte what the old statement wrote for it\", read together with the face list that names `SqliteWasmDriver`. Measured on sql.js 1.14.1: a legacy bare text with a leading U+FEFF is NOT rewritten, because the compare-and-set misses on the stripped read. The old statement did rewrite it. The code's doc block names this exception; the CHANGELOG text does not.\n2. INCOMPLETE, changeset face list: `SqlDriver` on the `sqlite3` / `sqlite` clients also inherits the backfill.\n3. The changeset cites `(#12380)`, which no longer resolves. The published CHANGELOG already cites it, so it is standing debt, but new consumer-facing prose should name the live record or say in words that the number no longer resolves.\n4. TRUE: \"SQLite's JSON depth limit in every build this repository bundles\" (measured on all four builds the lockfile installs, including better-sqlite3 12.11.1 under `plugin-auth`); \"the remote backfill's behaviour is unchanged\".\n5. Dev deviations: the driver-turso test file outside the declared surface is benign (a new file, not `turso-driver.ts`; the dispatch asked for that round-trip). The `index.ts` export is inside the claim's allowance. The wasm face is unpinned; that is real. The shared object-store fetch is harmless. Patch versus minor is resolved at this head.\n6. Dev out-of-scope findings: the sql.js NUL / BOM loss is REAL (filed as #19978). The remote module header naming only the date NUL exception is REAL, a docs gap in the safe direction; not filed.\n\nImplemented-by: `claude/issue-19912-json-backfill-depth-limit`\nReviewed-by: `session_01Bvd69VPa6puiNzzPUroDBx`\n\n**VERDICT: FAIL**\n\nEdits that make it PASS (no behaviour change is required):\n1. `packages/drivers/driver-sql/src/unencoded-json-text.ts`: drop the two bare `#12380` citations, or keep the number with a word saying it no longer resolves, and name the live record (the `SqlDriver.backfillCanonicalJsonEncoding` doc block and `sql-driver-12380-json-roundtrip.test.ts`). Never substitute a guessed number.\n2. `.changeset/19912-json-backfill-depth-limit.md`:\n   - Qualify the rewrite sentence: byte-for-byte only where the engine reads the stored bytes back verbatim. A cell it does not read verbatim is left as stored and keeps reading as it did, where the old statement rewrote it: on `SqliteWasmDriver`, a legacy bare text with a leading U+FEFF; on any engine, invalid UTF-8.\n   - Extend the face list to every client `SqlDriver` treats as SQLite.\n   - Handle `(#12380)` the same way as edit 1.\n\nIsolated reviewer: a separate contract-review-tier subagent, fed the card, the PR and AGENTS.md only. The seat verified the `Lint & Repo Gates` failure and the 404 before adopting.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5816211391,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T14:35:37Z","updated_at":"2026-09-24T14:35:37Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19972#issuecomment-5816211391","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `ff9881da77612518a51c934c9b593f2bf54397aa`\n\n### ① Derived judgments\n\n1. The delta from the previously reviewed head `8cb2d8ea` touches no logic. It is `.changeset/19912-json-backfill-depth-limit.md` (+2/−2) and the docblock of `packages/drivers/driver-sql/src/unencoded-json-text.ts` (+8/−4); a comment-stripped compare of that file across the two heads is identical. `sql-driver.ts`, `index.ts`, `remote-codec-residue-backfill.ts` and both test files are byte-unchanged. RIGHT.\n2. Every ① judgment of record 5816045784 (at `8cb2d8ea`) carries over unchanged:\n   - the pre-filter is unchanged, and writes are a strict subset of the old statement's;\n   - rewrites are byte-identical to `json_quote`;\n   - the BINARY compare-and-set is pinned through a second connection;\n   - paging advances strictly;\n   - idempotence holds;\n   - the root-entry export goes through `exports[\".\"]`;\n   - the remote arm is unchanged;\n   - all three Turso local doors reach the method;\n   - the failure posture stays `warn`.\n   RIGHT.\n3. The extended face list matches `SQLITE_EMIT_CLIENTS` (`sqlite3`, `sqlite`, `better-sqlite3`) and the `isSqlite` doc. RIGHT.\n4. The new \"not read back verbatim\" list was measured on raw sql.js 1.14.1. An embedded NUL (`610062`) reads `a`; a leading U+FEFF (`EFBBBF78`) reads `x`; invalid UTF-8 reads with U+FFFD. In all three cases the head's pass converts 0 and each cell reads as before, while the old statement rewrote all three. RIGHT.\n5. The live record named in place of the dead number exists: `sql-driver-12380-json-roundtrip.test.ts` and the `backfillCanonicalJsonEncoding` doc block. RIGHT.\n\n### ② Semver level\n\nUnchanged and consistent: `@objectstack/driver-sql: minor` (additive public export), `@objectstack/driver-turso: patch`, `Clause-②: no`, and no BREAKING marker owed. `Check Changeset` is `success` at this head. When this record was written, the required contexts (including `Lint & Repo Gates`, red at the previous head) were still `in_progress`. The gate's input is clean on inspection: the only `#N` citations left in the touched files are #19868, #19844 and #19912, and each resolves.\n\n### ③ Boundary flags\n\n1. The rewrite sentence, now qualified \"wherever the engine reads the stored bytes back verbatim\": TRUE.\n2. The not-read-verbatim sentence (leading U+FEFF or an embedded NUL on `SqliteWasmDriver`; invalid UTF-8 on any engine): TRUE, measured. Precision note: sql.js drops everything from the NUL on, which the sentence's consequence already covers.\n3. \"SQLite's JSON depth limit in every build this repository bundles\": TRUE, on all four lockfile builds.\n4. The face list: TRUE. \"The remote backfill's behaviour is unchanged\": TRUE.\n5. \"the change that made the SQLite write path JSON-encode every json value\": an observation, not a FAIL item (a json `null` stays SQL NULL). Optional tightening: \"every non-null json value\".\n6. The dead `#12380` is gone from the new prose, described in words, with the live record named.\n7. The dev's disclosures this round (the NUL item; the round-1 `check:issue-citations` alias running only `--self-test`): real and correct.\n\nImplemented-by: `claude/issue-19912-json-backfill-depth-limit`\nReviewed-by: `session_01Bvd69VPa6puiNzzPUroDBx`\n\n**VERDICT: PASS**\n\nIsolated reviewer: the same contract-review-tier subagent, re-run on the new head; adopted by the seat. Landing condition, outside this record: every check on `ff9881da` must conclude `success` or an expected skip before the seat arms it.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
