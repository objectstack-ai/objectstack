{"id":5774044804,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-22T09:18:08Z","updated_at":"2026-09-22T09:18:08Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19699#issuecomment-5774044804","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**1** anchor(s) derived from **1** changed package(s); no hand-written page names any of them, so this run has **nothing to list** — **not a clean bill of health**. This check sees only pages that NAME a derived anchor: one that documents this change in prose, or enumerates it in an authoring dialect, names none and stays invisible to it on every run.\n\n<details><summary>What this run could not see</summary>\n\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **136** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 7e1b048a1dbd7142bd6aa2cd9cbcccf7e1c69345` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `cdf09f9d865c0ec496d93bbfd1ec8849aff9065a` — the merge of head `1fae1ecc405616715050383da71dc690dd3a707f` into base `7e1b048a1dbd7142bd6aa2cd9cbcccf7e1c69345`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin cdf09f9d865c0ec496d93bbfd1ec8849aff9065a && git checkout cdf09f9d865c0ec496d93bbfd1ec8849aff9065a\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 7e1b048a1dbd7142bd6aa2cd9cbcccf7e1c69345 1fae1ecc405616715050383da71dc690dd3a707f && git checkout -B drift-repro 7e1b048a1dbd7142bd6aa2cd9cbcccf7e1c69345 && git merge --no-ff 1fae1ecc405616715050383da71dc690dd3a707f\n\nnode scripts/docs-audit/affected-docs.mjs --json 7e1b048a1dbd7142bd6aa2cd9cbcccf7e1c69345\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5776864993,"author":{"login":"os-support-ai","id":318092878,"type":"User"},"created_at":"2026-09-22T12:53:22Z","updated_at":"2026-09-22T12:53:22Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19699#issuecomment-5776864993","body":"## Contract review\n\nServed-tier: 79/79 `CONTRACT_REVIEW_TIER` (seat-measured, 2026-09-22T12:53Z)\nHead-sha: `1fae1ecc405616715050383da71dc690dd3a707f`\n\nRendered by an isolated at-tier review subagent on the head above.\n\n### ① Derived judgments\n\n1. **Declaration `Clause-②: no` — judged CORRECT.** The diff's only source change is one string literal inside `.describe()` at `packages/spec/src/data/object.zod.ts:1220`. `normalizeSemanticRoleAliases` (`:2818–2843`) and the `ObjectSchema` parse wrappers (`:2861`, `:2864`) are untouched; no key is added, removed or re-typed; no export changes (`check:api-surface` on a full DTS build: unchanged). Neither limb of the clause fires — the accept set is not widened and the public surface is not enlarged. A `Contract-text:` citation is NOT owed: the rule attaches it to a `no` that deletes a false rejection, and this diff deletes no rejection. The declaration carries no `(widening)`/`(narrowing)` arm, which AGENTS.md permits. Both carriers read `no` — `check-clause2-carriers.mjs --pair 19699` exit 0, governing claim comment and PR body line agreeing.\n2. **Every claim in the new contract text verified against the built package at head** — a 12-input probe through `ObjectSchema.safeParse` against `dist/data/index.js`: `collapsed:true` alone → `'collapsed'`; it needs no `collapsible` (same row); `collapsed:true + collapsible:false` → `'collapsed'` (the ternary at `:2831` tests `collapsed === true` first, so \"outranks\" is literally the code order); `collapsed:false` alone, `+collapsible:false`, `+defaultExpanded:false` all → `'none'`; `collapsed:false + collapsible:true` → `'expanded'`; the parenthetical \"(collapsible, starts closed)\" is the enum's own describe at `:1209` verbatim. Lit controls in the same run: neither key → `'none'` (default arm), explicit `collapse:'expanded'` beside `collapsed:true` → `'expanded'` (aliases unread). **No published statement is false.** The pinned objectui (`.objectui-sha` `87af769e`) derives the same precedence at `ObjectGroupInspector.tsx:74`, so the text contradicts no downstream reader either.\n3. **Reach of the claim — a precision limit that predates the PR, recorded not failed.** The mapping runs only inside `ObjectSchema.parse`/`safeParse` (the sync wrappers). Measured on the same build: standalone `ObjectFieldGroupSchema.safeParse({key, label, collapsed:true})` → `collapse:'none'` with `collapsed:true` preserved; `parseAsync`/`safeParseAsync` are not wrapped. The describe names no entry point — but neither does the sibling `defaultExpanded` describe it mirrors (`:1216`), which carries the identical conditionality. No standalone or async consumer exists: in-repo the group schema is reached only via `ObjectSchema.fieldGroups` (`:2022`); at the objectui pin it is imported as a type only (`object-fields-io.ts:334`). Judged consistent with the established describe precision on this schema.\n4. **`collapsible`'s describe left uncorrected (`:1218`) — defensible, and a finding.** Not a contradiction: the corrected `collapsed` row now says \"needs no `collapsible`\" and covers `collapsible:true` beside `collapsed:false`. What stays unstated is `collapsible:true` alone → `'expanded'` and `collapsible:false` alone → `'none'` — a silence that predates the PR, reported by the dev in the acceptance notes and within the dispatch's \"add that clause\" for `collapsed`. Shipping the pair half-described is a sibling-card follow-up, ⛔ not grounds to hold correct text back.\n5. **Public face:** the describe is published contract text — shipped verbatim in `src/data/object.zod.ts` (`files[]` entry `src/**/*.zod.ts`), in 20 `dist` files and 11 `json-schema` files including `json-schema/data/ObjectFieldGroup.json` and `json-schema/data/Object.json`, and in the regenerated `content/docs/references/data/object.mdx`. That is why this review is owed; it enlarges nothing.\n\n### ② Semver level\n\nDeclared `patch` on `@objectstack/spec` (`.changeset/19311-collapsed-alias-alone-mapping.md`). Measured, not pattern-matched: a real build in an isolated worktree at head (`gen:schema` 1546 schemas, `gen:openapi`, `tsup` JS, `tsup` DTS — each exit 0), then `pnpm pack` → `objectstack-spec-17.4.0.tgz`, 1902 entries.\n\n- **Positive controls:** `package/src/data/object.zod.ts` present, `package/dist/index.js` present, 1538 `package/json-schema/` entries; the new text found in 1 src + 20 dist + 11 json-schema files INSIDE the tarball; the old text 0.\n- **Negative controls:** `package/scripts/build-docs.ts` absent, zero `*.test.ts`, `src/data/field-group-layout.ts` (a non-`.zod.ts` src file) absent; the generator banner \"are GENERATED — do not hand-edit them\" 0 in the tarball against 1 in the unshipped `scripts/lib/generated-output.ts`. The instrument separates shipped from unshipped.\n\nIt publishes, so a changeset is owed and `skip-changeset` would be a false declaration. Description-only, `check:api-surface` unchanged, and a `no` declaration ⇒ `patch` is the correct level; the at-least-`minor` rule binds only a `yes`. Agreed.\n\nCheck conclusions are read from the head's check-runs, not locally: `Check Changeset` newest run by that name (`106687408973`, 09:20:30Z) is `success`; the earlier failure (`106686491479`, 09:17:30Z) ran against the pre-edit body and is **superseded by name**.\n\n### ③ Boundary flags\n\n- **Manual floor: none crossed** — no ADR, no `docs/adr/**`, no `content/docs/releases/**`, no CHANGELOG edit; no security or permission surface, no runtime behaviour.\n- **Regeneration verified from the tree:** `pnpm --filter @objectstack/spec check:docs` on the head worktree after a real build → exit 0, 225 generated files in sync, worktree clean afterwards. The two changed rows in `object.mdx` (`:314` and `:670` — the shape nests under two parents) are generator output, not hand edits. `Regen-provenance:` not owed: one commit, no regen-only head move.\n- **Dev flags and `open_questions`:** (1) card/PR surface mismatch — settled by the seat as `Part of`, outside this question; the delivered change is sound on its own terms. (2) REST-proxy channel deviation — process, not contract; escalated to the seat as the report records. (3) the `collapsible` mirror silence and the `@deprecated` TSDoc \"pair with X\" framing — answered in ① item 4: leave, file a sibling card. (4) not raised by the dev, raised by the review: the mapping is conditional on the `ObjectSchema` sync parse entry (① item 3) — recorded, no change requested.\n- **Construction tier — recorded, ⛔ not a FAIL.** The claim's container line reads `model: claude-opus-5` while asserting construction \"runs at `CONTRACT_REVIEW_TIER`\"; on `origin/main` that constant has read `claude-fable-5-1` since `372931e512` (#19684, 2026-09-22T08:04Z), which is before the claim was written (2026-09-22T08:22Z). Construction was therefore at TIER_DEFAULT, and this at-tier review is the designed compensation for exactly that — so not a FAIL. The claim's self-description was simply stale when written; the seat acknowledged it in an earlier comment on this PR.\n- **CI on head, newest run per check name:** all `success` or `skipped` by design (Auto Label, Check PR Size, Console Pin Gate, Packed-tarball smoke); `Type Check · source gates` (`check:generated` reconcile) and `Type Check · consumer gates` (`check:api-surface`) both success.\n\nImplemented-by: `claude/issue-19311-collapsed-implies-collapsible`\nReviewed-by: `session_013RDBh5DqXd2xnLwvHLgLFr`\n\n**VERDICT: PASS**\n\n---\n\n### Tier control — measured by the seat, not reported by the reviewer\n\nThe reviewer self-reported `16/16` by counting its own turns, and said plainly that a self-count is not a reading. It is right: the governing control is the transcript's per-request harness `model` stamp, and `get_session` is ⛔ not admissible as corroboration. So the seat counted the transcript itself:\n\n- **79 assistant requests, 79 stamped `claude-fable-5-1`** — every per-request `\"message\":{\"model\":…}`, no exceptions.\n- **93/93** counting every `model` field in the transcript, per-request stamps and `advisorModel` alike.\n- **Dark control: 0.** Filtering every `\"model\"`/`\"advisorModel\"` occurrence for anything other than `claude-fable-5-1` returns empty — no other model name appears anywhere in the transcript.\n- The four keyword hits for `fallback`/`overload` were read in context: three are tool-schema prose, one is the reviewer's own sentence about not being able to observe a fallback from inside. ⛔ None is a fallback notice.\n- `CONTRACT_REVIEW_TIER` re-read at source (`origin/main:scripts/pm/dispatch-gates.mjs`) = `claude-fable-5-1`, matching the stamps.\n\nThe reviewer was served at tier for every request it made.\n\n### Two carried readings the seat is recording rather than burying\n\n- **Carried, not re-measured** (by the rule that derived gate families are read from CI, never re-run by the reviewer): the dev's `pnpm --filter @objectstack/spec test` (14991 passed) and whole-repo `pnpm lint`. Test Core 1–6 and Lint & Repo Gates are green on this head.\n- **The reviewer's first local `check:generated` flagged `api-surface/`** — because that build had no `.d.ts` yet, which the tool itself says. After the DTS pass `check:api-surface` reads clean, exit 0, worktree clean. The stale flag was the missing-DTS artefact, ⛔ not the PR.\n- **⛔ Could not measure:** the merge-base \"before\" behaviour by execution — a structural argument stands in its place (the diff touches only a string literal inside `.describe()`, which cannot alter parsing); and the objectui renderer beyond `git grep` at the pin (no build or run of it).\n\n### Seat follow-ups this record does not discharge\n\n1. **`collapsible`'s describe silence** (① item 4) — a sibling card, ⛔ not owed on this PR.\n2. **The `ObjectSchema`-sync-entry precision limit** (① item 3) applies to all three aliases alike, not just `collapsed` — pre-existing, recorded.\n3. **`#19311` is NOT satisfied by this PR.** Its card names `packages/spec/src/ui/view.zod.ts:3315–3316`, where `collapsible` and `collapsed` both sit with **no `.describe()` at all** — a different schema from the `packages/spec/src/data/object.zod.ts` row this PR corrects. The seat re-dispatches #19311 at its own file; this PR closes as `Part of`, not `Fixes`.\n4. One shared side effect to clean up: the reviewer's `git worktree add` registered a worktree in the shared repo's list. Removable with `git worktree remove`; left in place as evidence for now.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
