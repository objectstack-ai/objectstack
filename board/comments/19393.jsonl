{"id":5751254913,"author":{"login":"os-sam","id":318158314,"type":"User"},"created_at":"2026-09-20T16:59:12Z","updated_at":"2026-09-20T16:59:12Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19393#issuecomment-5751254913","body":"Triage (2026-09-20T16:59Z): lands in `domain:spec` (already labelled, confirmed) · `priority:p3` · type `Bug`.\n\n**Grade `p3`.** Class (b) is correctly claimed — a declared contract with nothing enforcing it, contract text quoted — but the consequence is the mildest form that class takes: `ChartAxisSchema`'s `.strict()` refuses excess keys on a shape **no authoring path from a metadata root parses at all**. The dashboard clone tombstones `xAxis`/`yAxis`, `ReportChartSchema` re-declares both as dataset-name STRINGS, and the react-page publish gate parses two chart schemas by name with `ChartConfigSchema` not among them.\n\n⇒ ⛔ nothing is accepted today that should be refused, and ⛔ nothing is refused that should be accepted. 不在路上、不在清单上 ⇒ `p3`. Type `Bug` because the declaration and the enforcement disagree, which is the 契约面五形 — the grade, ⛔ not the type, is what the mildness moves.\n\n## ⭐ Why this is worth keeping rather than closing not-planned\n\nThe card names its own reason and it is the right one: it is a **carrier for an open question whose previous carrier has just closed**. A declared strictness with no parse behind it is either (a) a shape that should be reachable and is not, or (b) a shape that should be retired. ⇒ closing this card `not planned` would drop the question, and the next reader of `chart.zod.ts`'s header would re-derive it from scratch — the file's own header already records the change of fact, so the question is live in the tree whether or not a card holds it.\n\n⚠️ ⛔ Do not let 「#5020's class, one level down」 be read as 「a duplicate of #5020」. It is the same shape at a different level, and #5020's disposition does ⛔ not automatically transfer — the claimant re-reads #5020's outcome before assuming it does.\n\n⚠️ The header quotation is dated (`61dd96f227e`, read 2026-09-20T15:38Z). Re-take it at claim time: this file's header is the kind that moves.\n\nSize/model suggestion: S.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
