{"id":5627225233,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-11T00:05:38Z","updated_at":"2026-09-11T00:05:38Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/17581#issuecomment-5627225233","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nNothing in this diff resolved to a documentable surface (no symbol, route or SDK anchor derived from **0** changed package(s)), so **this run has no opinion** about the docs.\n\n<details><summary>What this run could not see</summary>\n\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n\nCoarse fallback — **0** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 216b066828b377e84c33b2e480e767b68b5b6e89` → `packageMentionDocs`.\n</details>"}
{"id":5627312836,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-11T00:14:14Z","updated_at":"2026-09-11T00:14:14Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/17581#issuecomment-5627312836","body":"**Seat acceptance — content reviewed, ①②③ read; ④ outstanding.** `domain:spec` execution seat, `session_01MkQhmuuJAVDjmeWNixwDDH`, 2026-09-11T00:25Z, head `2b266a8ced`.\n\n| pre-check | reading |\n|---|---|\n| ① at-tier review | **not required** — `Clause-②: no`; reviewed at default tier by this seat, on the diff |\n| ② carriers | `--pair 17581` → **exit 0**, both carriers agree, no widening tell |\n| ③ governed | `--test packages/spec/src/ui/action-row-update.test.ts` → **exit 0, NOT governed** (0 of 1 on the register) |\n| ④ checks | head `2b266a8ced`: 31 check NAMES, **0 non-green**, 16 in progress at 00:08Z |\n\nThe diff is **one line**: `target: '/api/v1/sys_api_key/{id}'` → `'/api/v1/data/sys_api_key/${ctx.recordId}'`. Both defects in one move — the missing `/data` segment and the placeholder convention (`{id}` is `newTabUrl`'s form; `target` documents `${ctx.X}` / `${param.X}`) — and the result is shape-identical to the corrected twin docblock at `action.zod.ts:1403` that PR #15098 landed. `ActionSchema` untouched, as triage required.\n\n⭐ **The round declined the card's alternative, and declined it for the right kind of reason.** The card offered a second option — replace the path with something obviously illustrative so it can never be mistaken for a worked example — and the earlier triage comment noted it scores better on the anti-AI-error axis. The order allowed it **only** on a measurement showing option 1 to be actively misleading. The round measured and found the opposite: after this fix **no source file under `packages/**` spells the pre-fix shape at all**, the sole survivor being the fenced `CHANGELOG.md:8775` historical record. ⇒ There is no second wrong example left to be confused with, so the \"two examples, one right one wrong, nothing marking which\" cost the card is about is simply gone. ⛔ It took the default direction because the evidence pointed there, not because the default was easier.\n\n⭐ **The ablation this card actually owed was run, and it is the interesting half.** The card's premise is that `target` is `z.string()` so the fixture is inert — which is *why* nobody noticed. The round drove that to a reading instead of asserting it:\n\n```\nLEG 0  baseline at HEAD                      31 passed, exit 0\nLEG 1  target := 'ABSURD::::not-a-url::::@@@@/nowhere/{{{'\n       anchors before old=1 new=0, after old=0 new=1; on-disk hash 4f9a15b689 ≠ HEAD blob\n                                             31 passed, exit 0   ⇒ the string IS inert\nLEG 2  LIT CONTROL: type:'api' := 'NOT_AN_ACTION_TYPE', same test\n       anchors proven the same way; hash 085f8a7edd ≠ HEAD blob\n                                             1 failed | 30 passed, exit 1   ⇒ the suite CAN redden\nRESTORE hash == HEAD blob 09871ea685, `git diff HEAD` empty — proven by STATE\n```\n\n⚠️ Leg 1 alone would have been worthless: \"nothing went red\" is indistinguishable from \"nothing ran\". The lit control is what makes the inertness a measurement. ⇒ Nothing pins that string, so the card stays a commit and ⛔ not a report.\n\n**`skip-changeset`, measured on the published artifact rather than argued from `files[]`.** `npm pack --dry-run --json` on the freshly built package enumerates **2011** files; the edited file is absent and so is every other `.test.ts` — **0 of 2011** — with its published twin `src/ui/action.zod.ts` **present** as the lit control, so the absence is a reading and not an empty corpus. ⭐ And then the content check, which is the part most rounds skip: a token unique to the test file scores **0** across all 2011 published files while a control token from published source scores **61** on the same corpus ⇒ no generated artifact (`json-schema`, `api-surface`, `spec-changes.json`, `llms.txt`, `prompts`, `liveness`, `dist`) re-authors it. Nothing already published moves.\n\n⚠️ Occurrence counts throughout used `grep -o | wc -l`, ⛔ never `grep -c`, which counts lines and would have voided the readings. The label write was an additive REST `POST` confirmed by a **comparative** read-back, so the concurrent size-labeler's write was neither stripped nor stripping.\n\n⛔ Nothing is enqueued and no auto-merge is armed until ④ is green. ⚠️ Note the standing blocker recorded on PR #17572: with the MCP identity rate-limited, this seat currently has **no channel** to flip a draft to ready — REST answers 200 and silently does nothing, GraphQL is refused 403. That will apply to this PR too when its checks finish.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
