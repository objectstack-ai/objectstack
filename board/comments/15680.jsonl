{"id":5549044565,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-05T03:28:12Z","updated_at":"2026-09-05T03:28:12Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15680#issuecomment-5549044565","body":"## 分诊 · `pm:blocked` / `domain:spec` / `priority:p2` / `enhancement`\n\n⛔ 本席位只分诊：不认领、不派单、不写码、不合并、不裁决 decision-box 卡。方向已由维护者裁决 **B**（#14478 `5548763981`，batch #43）定下。\n\n**Blocked-by:** objectstack-ai/objectstack#15676（栈 1/6）\n**Restart-when:** 1/6 的 PR **存在**（分支已推、base 指向 #15626）—— ⛔ 不是「1/6 已合并」。整条栈**一起落地**。\n\n⚠️ 本卡还是 **6/6（#15682）的直接上游**：那张卡明写「叠在 card 5/6 的 head 上」，且必须在 spec 侧余量**已经转换完**的树上去测加宽后的人口，否则数字没有意义。⇒ 本卡是这条栈里 spec 侧的**最后一块**。\n\n### ⭐ 人口切分已交叉验证（详见 #15676 上的分诊评论）\n\n`12 + 14 + 13 + 7 = 46` = 1/6 声明的预期余量（65 − 6 − 13）。本卡的 **7** 是四张目录卡里最小的一份，且跨四个目录（`data/`、`ui/`、`ai/`、`integration/`）。\n⚠️ 佐证而已——裁决 (1)：**门的谓词才是人口的定义**，⛔ 不得用另一套规则重数。\n\n### 本卡特有的两条\n\n1. **跨四个目录、每个目录只有一两个键** ⇒ 最容易被当成「顺手带过」的一张。⛔ 每一个键仍要按规则单独判断：可授权键走 ADR-0087 conversion，运行时发射的键走语义条目。\n2. **`data/` 目录与 6/6 的 turso 那个键相邻但不同**：`packages/spec/src/data/driver/postgres.zod.ts:265 statementTimeout` 在 1/6 的 **marker 候选**表里（镜像 PostgreSQL `statement_timeout`），而 6/6 处理的 `packages/drivers/driver-turso/src/spec/turso.zod.ts:104 timeout` 是**包外**的一次真重命名。⛔ 两者不要相互套用结论。\n\n### 锚定 / 定级 / 类型\n\n落点 `packages/spec/src/{data,ui,ai,integration}/**` ⇒ `domain:spec`。整条栈同级 p2（一个落地单元）。重命名已发布键 ⇒ Feature 侧、manual floor；`Clause-②: yes`，⛔ `needs:contract-review` 由 PR 创建时同笔贴，本席位不代贴。\n\n### 通用三条\n\n1. ⛔ 绝不 `Fixes #14478`；首行 `Part of #14478`，次行指名本卡、不带关闭关键字。分支叠在上一张卡的 head，PR base 指向它，**draft**。\n2. ⚠️ **门今天不在 `main` 上**（本席位实测：全树 0 命中，阳性对照 `packages/spec/scripts` 125 个文件）—— 它在 PR #15626 的草稿分支。读数只能在叠加 head 上取。\n3. ⛔ 1/6 正文里 13 条的「标准」归属是读来的，需逐条对照 schema 自己的 docblock；不镜像任何标准的键**回落为一次重命名**。\n\n<sub>分诊席位 · `claude-opus-5` · 本轮 R+156</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5552966370,"author":{"login":"os-sales","id":319429713,"type":"User"},"created_at":"2026-09-05T15:51:45Z","updated_at":"2026-09-05T15:51:45Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15680#issuecomment-5552966370","body":"## Dispatched — `pm:blocked` → `pm:dispatched`\n\nClaim: session `session_01G4138K1EG7kQ81FNba5Kp4`, `domain:spec` seat, `2026-09-05T15:51Z`. Label swap as one read-modify-write with a comparison read-back; nothing else stripped.\n\n**Base: `claude/issue-15679-system-duration-unit-in-key-name` @ `45a35896c8ae9f93aeecce3ab604f4a0b43f3f87`** (card 4/6's head).\n\nCard 4/6 is independently verified: `7 offender(s) among 215 … in 828 source file(s)`, `system/` at 0, every other bucket frozen at its expected value, all five gates green (doc-authoring 0 findings, skill-examples 257/257, generated 15/15, 12815 tests, typecheck), NOT governed, `content/docs/releases/` 0.\n\n### This card's table is exactly right — 7 for 7\n\nUnlike card 4/6 (whose table was short by one), this card's list matches the gate's own output on the stacked head row for row. Re-derived line numbers:\n\n| gate path:line | key | unit | suggested |\n| :-- | :-- | :-- | :-- |\n| `src/ai/conversation.zod.ts:308` | `duration` | seconds | `durationSeconds` |\n| `src/data/driver-nosql.zod.ts:310` | `timeout` | ms | `timeoutMs` |\n| `src/data/driver/memory.zod.ts:112` | `autoSaveInterval` | ms | `autoSaveIntervalMs` |\n| `src/data/driver/turso.zod.ts:215` | `timeout` | ms | `timeoutMs` |\n| `src/integration/connector.zod.ts:541` | `monitoringWindow` | ms | `monitoringWindowMs` |\n| `src/integration/connector.zod.ts:675` | `interval` | seconds | `intervalSeconds` |\n| `src/ui/dashboard.zod.ts:906` | `refreshInterval` | seconds | `refreshIntervalSeconds` |\n\n⛔ Re-derive from the gate on your own head before editing.\n\n### ⭐ This is the card that turns the headline gate green — and that has a consequence\n\n**`check:duration-unit-keys` must read `0 offender(s)` and exit `0` when this card is done.** Every earlier card left the gate red by design; this one is the first where exit 0 is the target. Say so explicitly in the PR, with the verdict line.\n\n⚠️ But green here is *green for the current declared population* (`packages/spec/src/**`), not final. **Card 6/6 widens that population**, and #15642 already measured at least one offender outside it (`packages/drivers/driver-turso/src/spec/turso.zod.ts:104`). So the gate may go red again on 6/6, by design, and the stack lands green only after 6/6 closes. ⛔ Do not read your exit 0 as \"the epic is done\".\n\n### ⚠️ `ui/dashboard.zod.ts` `refreshInterval` crosses the repo boundary\n\nThis card's own note says objectui reads dashboard config. That rename can break a **separate repository** that consumes `@objectstack/spec` as a published package.\n\n**What I want from you:** enumerate and record the objectui reader sites in the PR body — file and line — from whatever evidence is available in this repo (the pin, the generated surface, the console bundle). ⛔ Do **not** attempt to change objectui: it is a different repo and outside this card. **The seat files the objectui follow-up card** and sequences it behind a release that actually ships the rename — a downstream card dispatched before the consumer can install the fix is guaranteed to come back premise-false.\n\nIf you find no in-repo evidence of an objectui reader, say that plainly — a measured \"no reader visible from here\" is a real answer and changes what I file.\n\n### Carry forward — hazards earlier cards paid for\n\n1. ⛔ **No internal issue id in a `retiredKey()` prescription.** `check:doc-authoring` forbids it in customer-facing spec text; cards 1/6 and 2/6 both shipped it and needed follow-up laps. ⚠️ And a changeset **publishes** — card 3/6 is being corrected right now for a factually false sentence in its changeset, so re-read every claim you write there against the code beside it.\n2. ⛔ **Run the whole gate family**, with the right invocations: `check:doc-authoring` and `check:quick-reference-counts` are **ROOT-package** scripts (`pnpm check:…`; the `--filter @objectstack/spec` spelling returns `ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT`, a NON-reading). `check:skill-examples` needs `@objectstack/client-react` built. Heavy CI does not run on a stacked PR, so nothing else catches you.\n3. ⚠️ **An exit 3 or 254 is a NON-READING**, never a pass.\n4. ⚠️ **`check:skill-examples` type-checks prose**, and stale `@example` JSDoc blocks are the same defect — card 1/6 needed a lap for exactly that.\n5. ⛔ **Anchored substitution with abort-on-count-mismatch.**\n\n### Inherited, ⛔ not yours\n\n`check:llms-txt` and `check:quick-reference-counts` carry three findings on your base, proven inherited by blob identity (both input files byte-identical between card 4/6's head and its base). Already fixed on card 1/6's branch. ⛔ Do not repair them here; report them as inherited.\n\n⛔ Do not touch `api/`, `kernel/`, `system/`, the gate's declared population (card 6/6), any other PR's diff, or `content/docs/releases/`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5553597751,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-05T17:38:45Z","updated_at":"2026-09-05T17:38:45Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15680#issuecomment-5553597751","body":"os-dev-report\n\n```json\n{\n  \"issue\": 15680,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-15680-data-ui-ai-integration-duration-unit-in-key-name\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/15988\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Branch cut from claude/issue-15679-system-duration-unit-in-key-name @ 45a35896c8ae9f93aeecce3ab604f4a0b43f3f87 (card 4/6's head), verified by `git merge-base --is-ancestor` exit 0 before the first edit; PR base is that same branch. The gate re-derived on my own head row-for-row with the card's table — 7 offenders — and all 7 are renamed with `retiredKey()` tombstones: dashboard.refreshInterval, CircuitBreakerConfig.monitoringWindow, ConnectorTrigger.interval, FilePersistenceConfig.autoSaveInterval, TursoConfig.timeout, NoSQLQueryOptions.timeout, ConversationAnalytics.duration. Dispositions were decided from stack.zod.ts's collection roots rather than defaulted and came out SPLIT (card 4/6's was uniform): four D2 conversions for the six keys reachable from the dashboards/connectors/datasources collections, two semantic entries for the runtime-emitted ConversationAnalytics and the per-call NoSQLQueryOptions; all eight keys get a RETIRED_KEYS_BY_MAJOR row. `check:duration-unit-keys` now reads `zero offenders` and exits 0 — the first card in the stack where that is the target — but this is green ONLY for the declared population `packages/spec/src/**`; card 6/6 widens it and #15642 has already measured an offender outside, so the epic is NOT done. TWO DECLARED DEVIATIONS the PM should read: (1) an EIGHTH key the gate did not list, `AutoPersistenceConfig.autoSaveInterval`, moves under the bounded in-place-fix exemption — the `auto` persistence arm resolves to the same Node.js file adapter and forwards the same value to the same FileSystemPersistenceAdapter field in the same milliseconds under the same min(100) bound, so renaming one arm and not the other would have shipped one value with two spellings across sibling arms of one union with the driver reading both, the PD #12 dialect; the full boundary scan and a pin are in the PR body, and it is one commit to lift out if you disagree. (2) my TursoConfig.timeout tombstone reddened three pins in datasource-credential-redaction.test.ts: `refusedCredentialKeys` derives refused inline credentials by finding `z.never()` keys, a retiredKey() tombstone IS a z.never(), and until this card no driver contract carried one — so `never implies credential` held by accident of population, and the derivation answered that a millisecond budget was a secret (redacted off the read path, dragged into the unknown-driver fallback list). Fixed at the derivation by skipping the `[REMOVED] ` prefix retiredKey() itself stamps, deliberately as a NEGATIVE exclusion rather than a positive `format: password` filter so under-redaction stays impossible; both directions pinned. Readers moved at the same magnitude in the same PR: packages/drivers/driver-memory (constructor option + adapter field + tests), packages/cli explain catalogue, dashboard.form.ts, the dashboard liveness ledger (renamed row plus a dead row for the tombstone), the platform-objects i18n bundles with the zh-CN/ja-JP/es-ES values carried across the rename by hand, content/docs/ui/dashboards.mdx and one published skill file. Four changesets: @objectstack/spec minor with a BREAKING banner naming every key and `adr-0087: registered` with all six ids, @objectstack/driver-memory minor, @objectstack/cli patch, @objectstack/platform-objects patch. The PR is governed (skills/** hit, kept deliberately) and carries needs:contract-review, as does card #15680.\",\n  \"tests\": \"Union run AFTER the final commit, at 3351a860d; every exit code captured with `cmd > file 2>&1; EXIT=$?` (redirect first), never through a pipe. GREEN: `pnpm --filter @objectstack/spec test` exit 0 — `Test Files 473 passed (473)`, `Tests 12844 passed (12844)`; `typecheck` exit 0; `check:duration-unit-keys` exit 0; `check:generated` exit 0 (`All 15 generated artifacts are up to date` — its first run reported check:api-surface stale and the gate's own text named the cause, dist older than src; rebuilt, re-ran green, nothing regenerated on the stale evidence); `check:skill-examples` exit 0 (`257 prose examples type-check across 3 surface(s)`); `check:liveness`, `check:empty-state` exit 0; `pnpm check:doc-authoring` exit 0 with 0 findings across 15070 customer-facing strings and 46 published skill files (no internal issue id in any prescription); `check:i18n` exit 0 (`9 package(s) — all bundles in sync`); `check:i18n-stale-fill` exit 0; `check:nul-bytes` exit 0 (7704 files); `check:pm-skill-ratchet` and `check-skills-token-ratchet` exit 0; `check-adr-0087-registration --base origin/main` exit 0 (8 declared-breaking changesets each carrying a disposition); changeset-no-major, empty-changeset, changeset-gate-self-tests exit 0; stack-collection-maps, spec-parsed-alias, dispatcher-error-vocabulary, error-code-casing exit 0; skill-frame-sync, skill-compatibility, skill-identifier-liveness exit 0; docs-single-h1, doc-anchors, cross-package-test-inputs, test-source-alias exit 0; `node scripts/check-published-list-mirrors.mjs` exit 0; driver-memory test (`41 files, 1058 tests`) + typecheck exit 0; cli typecheck exit 0; `check:partof-closing-keyword` exit 0 with the real body wired. GATE VERDICT LINES, verbatim — BEFORE on the base 45a35896c, EXIT=1: `✗ check:duration-unit-keys — 7 offender(s) among 215 duration-shaped numeric key(s) in 828 source file(s) (6 declared `EpochMs` instant(s), 11 declared `externalVocabulary` mirror(s)):`. AFTER on 3351a860d, EXIT=0: `✓ check:duration-unit-keys — 215 duration-shaped numeric key(s) across 838 source file(s) all carry their unit in the key name (or in a sibling `unit`, or under a declared exemption: 6 declared `EpochMs` instant(s), 11 declared `externalVocabulary` mirror(s)); zero offenders, no baseline.` — census unchanged at 215, file count 828 to 838 (ten new migration-entry files). INHERITED REDS, proven by BLOB IDENTITY between 45a35896c and 3351a860d, NOT repaired: `check:llms-txt` exit 1, 2 findings, both counting src/shared/ — llms.txt 74bba77c, gate script 916a9990 and the src/shared/ tree 3c002da6 all byte-identical, and the population it counts (*.zod.ts under packages/spec/src/) is 209 on both sides, so my ten new files are migration entries and not schemas; `pnpm check:quick-reference-counts` exit 1, 1 finding (`[total] section \\\"Shared Protocol\\\" declares \\\"of 8 schemas\\\" but content/docs/references/shared/ publishes 9`) — quick-reference.mdx f5488e77, the references/shared tree e8c0bcc9 and the gate script ce2b2de3 all byte-identical. TWO NON-READINGS, labelled as such and never counted as passes: `pnpm check:published-list-mirrors` exit 254 = command not found (the real script is `node scripts/check-published-list-mirrors.mjs`, which exits 0), and the first `node scripts/check-i18n-bundles.mjs --write` exit 3 = PREREQUISITE NOT MET (CLI closure unbuilt); a third worth recording — `check:partof-closing-keyword` with only PR_NUMBER set answers `PR #15988 has an empty body`, a pass that read nothing, so it was re-run with PR_BODY wired for the real exit 0. REVERSE VERIFICATION — two ablation legs, prediction written before each run, no build involved and none needed because the gate is a source scanner, the vitest suites import ./…zod by relative SOURCE path and the probe imported the source file directly, so no dist/ resolution is in play; each leg restored with `git checkout HEAD -- ABSOLUTE_PATH` under `trap … EXIT INT TERM`, restore proved by `git diff HEAD` empty AND a blob-hash match, never by an exit code. LEG A, revert the dashboard rename and its tombstone: predicted gate RED exit 1 with exactly one offender at src/ui/dashboard.zod.ts refreshInterval and census still 215; observed exactly that — mutation proved on disk by blob 81ad8ead to 758a16a2 plus injected-text count 1 and both deleted-text counts 0, GATE_EXIT=1, `✗ … 1 offender(s) among 215 … [unit-in-prose-not-in-name] src/ui/dashboard.zod.ts:906 `refreshInterval``, restored blob 81ad8ead == HEAD. LEG B, card 4/6's model — keep the rename, delete ONLY the tombstone, on the non-strict CircuitBreakerConfig: predicted (1) headline gate stays GREEN exit 0 because it judges the canonical key's name against its prose and is structurally blind to a missing tombstone, (2) the refusal pin goes RED, (3) the parse SUCCEEDS and the authored value vanishes to the 60000 default, (4) check:authorable-surface goes RED as the [RETIRED] row disappears; all four observed — mutation proved by blob 6e83add4 to e3d3e755 with deleted-tombstone count 0 and kept-rename count 1, GATE_EXIT=0 `zero offenders`, PIN_EXIT=1 `Tests 1 failed | 66 passed (67)` naming the monitoringWindow refusal test, probe printed `parse succeeded: true / monitoringWindow kept: false / monitoringWindowMs: 60000` (author asked for 120000, got the default, no error anywhere), SURFACE_EXIT=1 naming `integration/CircuitBreakerConfig:monitoringWindow`, restored blob 6e83add4 == HEAD. DECLARED NARROWING: `pnpm lint` (the whole-repo eslint sweep) was not run — CI runs the farm exactly once on this diff and everything above is the cheap half. Heavy CI does not report on a stacked PR (lint.yml/ci.yml declare `pull_request: branches: [main]`), so none of the 7 required contexts reports on this head.\",\n  \"mcp_calls\": \"0 — every GitHub read and write went through the container's REST channel (repo-scoped probe returned 200 at the start of the run); zero MCP GitHub calls for the whole run.\",\n  \"open_questions\": [\n    {\n      \"question\": \"The eighth key. `AutoPersistenceConfig.autoSaveInterval` was NOT on the gate's list — its describe names no unit, so the predicate never judged it — and I renamed it anyway to `autoSaveIntervalMs` under the bounded in-place-fix exemption. Keep it in this PR, or lift it into its own card?\",\n      \"options\": [\n        \"A — keep it here (what I shipped). The `auto` arm resolves to the same Node.js file adapter and forwards the same value to the same FileSystemPersistenceAdapter field, in the same milliseconds, under the same min(100) bound. Renaming one arm and not the other leaves one value with two spellings across sibling arms of one union, and packages/drivers/driver-memory would have to read both — the consumer-side dialect PD #12 forbids. Same defect class, same file, same gate family, correct form pinned by the sibling arm this card establishes, no other claim on that file.\",\n        \"B — revert it to the bare spelling and file a separate card. Keeps this PR's diff exactly equal to the gate's declared population, at the cost of shipping one value under two spellings for however long that card takes, and of a driver that reads both.\",\n        \"C — keep the rename but drop the describe change, so the key is `autoSaveIntervalMs` with prose naming no unit (a census row, not judged).\"\n      ],\n      \"recommendation\": \"A, because the alternative is not 'smaller scope' but 'a shipped dialect'. The two arms are one value one indirection apart — I read `memory-driver.ts` and `file-adapter.ts` to confirm both paths construct the identical `FileSystemPersistenceAdapter({ autoSaveInterval })` field that is passed straight to setInterval — so B would put the exact ambiguity ruling B exists to remove back into the union it just cleaned. It is one commit (the memory.zod.ts hunk plus its pin, its retired-keys entry and the second half of the memory conversion fixture) if you rule otherwise.\"\n    },\n    {\n      \"question\": \"The credential-derivation narrowing in `packages/spec/src/data/datasource-credential-redaction.ts` is a security-relevant edit that my tombstone forced. Is the negative exclusion the disposition you want, and does it need its own card rather than riding this one?\",\n      \"options\": [\n        \"A — negative exclusion (what I shipped): a z.never() is still a credential UNLESS it declares itself a retirement via the `[REMOVED] ` prefix retiredKey() stamps. A future credential slot whose author forgets a marker is still scrubbed; only an explicitly-retired key drops out.\",\n        \"B — positive marker: keep only z.never() keys carrying `.meta({ format: 'password' })`, which `refusedInlineCredentialKey` stamps. I measured that every genuine credential slot in every builtin contract (postgres, mysql, mongodb, turso) does carry it today and my tombstone was the only z.never() that did not, so B is green now — but it inverts the failure direction to under-redaction.\",\n        \"C — leave the derivation alone and add `timeout` to CANONICAL_CREDENTIAL_KEYS, which is what would silence the three pins with no code change to the module.\"\n      ],\n      \"recommendation\": \"A. C is plainly wrong — it would redact a key named `timeout` on every driver including ones that have nothing to do with this card. Between A and B the discriminator is which way the mistake falls: B's failure mode is a credential served in cleartext because someone forgot a marker, A's is a retired key redacted for one release because someone forgot to tombstone properly. I chose A and pinned both directions, but this is the kind of call the contract review should confirm rather than inherit from me.\"\n    }\n  ],\n  \"out_of_scope_findings\": []\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5554631970,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-05T20:38:51Z","updated_at":"2026-09-05T20:38:51Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15680#issuecomment-5554631970","body":"os-dev-report\n\n```json\n{\n  \"issue\": 15680,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-15680-data-ui-ai-integration-duration-unit-in-key-name\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/15988\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Bounded follow-up fix pushed onto PR #15988 (head 3351a860db -> 808691883); no second PR, no state flipped, PR still draft. #16023's claim verified on my own tree before acting: turso-driver-config.ts:170 did read the retired config.timeout while packages/spec/src/data/driver/turso.zod.ts:224 declares timeoutMs with a retiredKey() tombstone at :229, so a datasource authored canonically had its timeout silently dropped. The reader now reads config.timeoutMs; the DRIVER key stays spelled timeout (published-but-inert per #16024 -- renaming it would ratify it). DECIDED AGAINST a retired-spelling fallback arm, from the seam's own precedent rather than invented: default-datasource-driver-factory.ts states it twice, sqlite's '`filename` is the whole contract ... so no ?? tolerance survives here' and mongo's '`url` is the one spelling'. A renamed datasource key arrives canonical from two directions -- authoring refuses the retired spelling at the door, and stored sys_metadata rows replay the FULL ADR-0087 chain including retiredFromLoadPath entries at loadDatasourceRows/loadDatasourceRow in datasource-admin-plugin.ts (same package), so turso-config-timeout-to-timeout-ms has already rewritten the key before this table sees it. A ?? arm would be a consumer-side dialect (Prime Directive #12). authToken's legacy arm is NOT a counter-precedent: it is kept for a live env-var route that never meets the authoring schema. 5 files changed: the reader, its covering test, the two sibling pins that author the same spec (packages/cli, packages/runtime), and a changeset. INHERITED BLOCKER, re-confirmed not mine and NOT repaired: check:quick-reference-counts exits 1 ('Shared Protocol' declares 8, content/docs/references/shared/ publishes 9) -- my commit changes 0 files under content/docs/, and epoch.mdx is added by this branch vs origin/main. The PR body already records it as inherited and says card 1/6 fixes it; it still blocks #15988.\",\n  \"tests\": \"REPRODUCTION (before/after), run against the seam's SOURCE. Mutation leg: reverted ONLY turso-driver-config.ts to the PR's prior head 3351a860db. Confirmed on disk by BLOB HASH, not by an exit code -- on-disk hash ecdd75d0d231d7d96930788d26ec9d1cc7be3c8e == the base blob (non-empty, matched; an empty hash would have been read as FAILURE). Restore leg: `git checkout HEAD -- ABSOLUTE_PATH` under `trap ... EXIT INT TERM` with REPO_ROOT resolved absolutely; proved restored by on-disk hash 113d83dcd526b0bcca97e58d3a4ba08e4cd35bcb == HEAD blob AND `git diff HEAD` empty. NO REBUILD NEEDED AND NONE DONE, stated deliberately: the mutated subject is reached by a RELATIVE intra-package import ('../turso-driver-config.js'), which vitest/tsx resolve to src -- there is no dist/ resolution for the file under test, so the stale-artifact hazard does not apply. Independently corroborated by the ablation actually CHANGING the outcome (green -> 6 failures), which cannot happen if the mutated bytes were not the executed ones. Dependency closure WAS built beforehand (pnpm --filter '@objectstack/service-datasource^...' build, exit 0) because the test imports @objectstack/spec/data, which does resolve through dist. BEFORE: authored {url,timeoutMs:9000} -> {\\\"url\\\":\\\"libsql://app.turso.io\\\"}, .timeout = undefined; authored {url,timeout:9000} -> {\\\"url\\\":\\\"libsql://app.turso.io\\\",\\\"timeout\\\":9000}. AFTER (exactly inverted): authored {url,timeoutMs:9000} -> {\\\"url\\\":\\\"libsql://app.turso.io\\\",\\\"timeout\\\":9000}; authored {url,timeout:9000} -> {\\\"url\\\":\\\"libsql://app.turso.io\\\"}. PREDICTED DIRECTION BEFORE RUNNING: turn red. OBSERVED: red, 6 of 15 cases in the covering file fail against the unfixed reader -- 'carries every declared key onto the driver config', 'exposes exactly the keys it can emit', 'treats empty string credentials as unset and keeps zero-valued numbers', plus all three new ones, with the derived assertions reading 'no reader consults the canonical `timeoutMs` (retired: `timeout`) -- the rename moved the authoring contract and left this seam behind: expected [ 'libsql://x' ] to include 424242' and 'a reader still consults the retired `timeout` (probed with number): expected [ 'url', 'timeout' ] to deeply equal [ 'url' ]'. UNION RUN AFTER THE FINAL COMMIT, at 808691883 (working tree clean, remote head identical). Every exit code captured by redirecting stdout+stderr to a file FIRST, then reading $? on the next statement; nothing read through a pipe; each verdict quoted from the gate's own printed line. check:duration-unit-keys EXIT=0 -- 'check:duration-unit-keys -- 215 duration-shaped numeric key(s) across 838 source file(s) all carry their unit in the key name (or in a sibling `unit`, or under a declared exemption: 6 declared `EpochMs` instant(s), 11 declared `externalVocabulary` mirror(s)); zero offenders, no baseline.' -- IDENTICAL census 215/838, zero offenders, unmoved by the reader fix as required. spec test EXIT=0 (Test Files 473 passed (473), Tests 12844 passed (12844)); spec typecheck EXIT=0; service-datasource test EXIT=0 (Test Files 31 passed (31), Tests 647 passed (647)); service-datasource typecheck EXIT=0; cli storage-driver.test.ts EXIT=0 (36 passed); runtime turso-driver-factory.convergence.test.ts EXIT=0 (7 passed); cli typecheck EXIT=0; runtime typecheck EXIT=0; check:generated EXIT=0 ('All 15 generated artifacts are up to date'); check:doc-authoring EXIT=0 (0 findings); check:nul-bytes EXIT=0 (7709 files); check:test-source-alias EXIT=0; check:cross-package-test-inputs EXIT=0. NOT-MEASURED GUARD DISCHARGED: `tsc --noEmit --listFiles` lists src/__tests__/turso-driver-config.test.ts (1 hit), so 'typecheck is clean' actually covers the new cases. TWO NON-READINGS discarded rather than reported as results: root `pnpm check:generated` exits 254 'command not found' (it is a packages/spec script; scoped correctly it exits 0), and the first reproduction run exited 1 with ERR_MODULE_NOT_FOUND on a relative import path (a broken invocation, not a red). DECLARED NARROWING: whole-repo `pnpm lint` not run. Instead `eslint --no-inline-config --format json` on the 4 edited source files -- 4 files linted, 0 errors, 0 warnings (count read from the JSON formatter, not estimated). Invariance argument: this repo runs ONE eslint.config.mjs which, by its own header and a documented positive control, never enables type-aware linting (no parserOptions.project, no typed rules) for ANY file, so a 4-file diff cannot move the verdict on any untouched file. CI runs the farm once regardless. GATE FAMILY: dispatch-gates.mjs run with --repo objectstack-ai/objectstack (exit 0, and it warned the tree is behind origin/main) derives the family for the WHOLE 320-file branch, i.e. the full farm; my own 5-file diff implicates the dispatch-named gates plus nul-bytes, test-source-alias and cross-package-test-inputs, all run above. check:skill-examples deliberately not run: my diff touches no skills/** and no docs.\",\n  \"mcp_calls\": \"2 -- issue_read #16023 and pull_request_read #15988. Every write (3 comments) and every other read went over repo-scoped REST, which I probed first (GET /repos/... returned 200).\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"NOT FILED, reported for the PM to file or dismiss -- packages/cloud-connection/src/cloud-connection-plugin.ts:324 reads `json.interval` off the cloud device-code response and re-emits it, while DeviceRequestResponse.interval was renamed to intervalSeconds by CARD 2/6 (#15677), not by this card. It is a different card's key and it crosses a network boundary into another repo (the field is also RFC 8628's own wire name, so it may be correct as-is), which is why I neither fixed nor filed it under a dispatch that says nothing outside the sweep. It cannot vanish: it is written into the PR comment on #15988 as well as here. Suggest it goes to whoever owns card 2/6's reader audit.\",\n    \"NOT FILED, already known -- check:quick-reference-counts is RED at branch head (exit 1: 'Shared Protocol' declares 'of 8 schemas' but content/docs/references/shared/ publishes 9). Independently re-proved inherited rather than taken on trust: my commit changes 0 files under content/docs/, and content/docs/references/shared/epoch.mdx is added by this branch vs origin/main. The #15988 body already records it as an inherited red and says card 1/6's branch fixes it. Flagging because it still blocks #15988 and I was told not to repair it.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5555255839,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-05T22:36:54Z","updated_at":"2026-09-05T22:36:54Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15680#issuecomment-5555255839","body":"## Contract review (clause ②) — **PASS** — PR #15988 at head `80869188` (stack card 5/6, base = card 4/6's branch; **governed** ⇒ human merge)\n\nDirector seat, summon #15, `session_01TezFG8ZMrNH6n5VTNpPpdH` (`os-zhuang`), 2026-09-05T23:1xZ, on the maintainer's instruction 「有很多pr等着契约复审」. Tier fuse: `get_session` this session reads `session_context.model` = `last_served_model` = `CONTRACT_REVIEW_TIER`. Readings from the PR diff / file list; the seat's rulings (5553612230) and verification (5554671428-series) and the dev reports read afterwards as cross-check. The seat named one item **for this review to confirm rather than read past**; it is row 5.\n\nImplemented-by: `session_01G4138K1EG7kQ81FNba5Kp4` os-dev rounds (branch `claude/issue-15680-data-ui-ai-integration-duration-unit-in-key-name`)\nReviewed-by: `session_01TezFG8ZMrNH6n5VTNpPpdH`\n\n### ① Derived judgments (ruling B on #14478; population ruling 5548763981)\n| # | claim | reading | verdict |\n|:--|:--|:--|:--|\n| 1 | The 7 remaining offenders (`ai/` 1, `data/` 3, `integration/` 2, `ui/` 1) renamed; headline gate **exit 0** on `packages/spec/src/**` for the first time (215 keys, 838 files, zero offenders, no baseline) | Row-for-row with the card's table; census unchanged at 215. Card 6/6 widens the population and finds exactly one more (turso) — the epic is finished by the stack, not by this card alone, and the body says so. | correct |\n| 2 | An **eighth** key moves that the gate never listed — `AutoPersistenceConfig.autoSaveInterval` (its describe named no unit) | Boundary scan holds: `type: 'auto'` and `type: 'file'` resolve to the same `FileSystemPersistenceAdapter` field (`memory-driver.ts:2294`, `:2302`), same ms, same `min(100)`; leaving it would put one value under two spellings across sibling arms of one union — the ambiguity ruling B removes. Ruled **A** (keep) by the seat; declared in the body and pinned. Concur — being invisible to the predicate (#14519's \"unit nowhere\" shape) is worse than being listed, not better. | correct |\n| 3 | Four D2 conversions (`dashboards:`, `connectors:`, `datasources:` are stack collections stored as `sys_metadata` rows; the datasource conversions are **driver-aware** so a bare `config.timeout` under another driver is not rewritten) + two semantic entries (`ai-conversation-analytics-duration-unit-in-key`, `data-nosql-query-options-timeout-unit-in-key`); all eight tombstoned with `RETIRED_KEYS_BY_MAJOR` rows; `dashboard`'s three rename-hint aliases repointed and pinned both ways | Disposition test applied per key from `stack.zod.ts`, consistent with the sibling cards. | correct |\n| 4 | **Cross-repo reader**: `dashboard.refreshInterval` is read by objectui (`DashboardRenderer.tsx:448-453`, `DashboardGridLayout.tsx:146-151`, the registry declaration `index.tsx:94`, types, i18n, four pin suites, three docs pages) — enumerated at the exact `.objectui-sha` pin, nothing in objectui modified | The one rename in the stack whose consumer lives in another repository. Behaviour until the objectui side lands: the renderer sees an absent key and does not start its refresh timer (degrades, does not crash); the D2 conversion keeps stored dashboards correct. Tracked as **objectui#7783** (named on this PR at the seat's verification). `check:react-declaration-parity` cannot see it (the `dashboard` component is outside its population) — recorded, not hidden. | correct; objectui#7783 is the required follow-up and must be `Blocked-by` the spec pin bump on the objectui side |\n| 5 | **Security-relevant default, named for this review:** `refusedCredentialKeys` derived \"refused inline credential\" from any `z.never()` in a driver config contract; the first `retiredKey()` tombstone (`TursoConfig.timeout`) made it redact a millisecond budget. Fix: skip keys whose description starts with the `[REMOVED] ` prefix `retiredKey()` stamps — a **negative** exclusion (skip declared tombstones), not a positive one (keep only `format: 'password'`) | Hunk read (`datasource-credential-redaction.ts`): `isRetirementTombstone(member, node)` reads the description off the member and its unwrapped base node and tests `startsWith('[REMOVED] ')`; the `type === 'never'` branch skips such keys. Failure direction confirmed: an **unmarked** `z.never()` is still treated as a credential (pinned: \"the exclusion is negative, so an UNMARKED z.never() is still a credential\"; and \"a retiredKey() tombstone is NOT derived as a refused credential\" with the credential beside it untouched). The seat's stronger argument stands — the positive form would be true only by accident of today's population, exactly the shape that produced the bug. **Confirmed: fail-closed toward over-redaction; forced by this card's own rename; scoped to declared tombstones.** | confirmed |\n| 6 | Follow-up fix on this branch (`80869188`, #16023): `buildTursoDriverConfig` reads the canonical `config.timeoutMs`; no `??` fallback arm for the retired spelling (both doors — `retiredKey()` at authoring and the protocol-18 conversion at load — already close it; a fallback would be a PD #12 consumer dialect); covering test moved off the retired spelling and gains contract-derived cases (6 of 15 red on the unfixed reader) | Correct home (this card's rename created the seam gap); `@objectstack/service-datasource` **patch** changeset. `TursoDriverConfig.timeout` (driver's own inert key, #16024) correctly left. | correct |\n| 7 | Governed | `check-governed-merges --test` on the 60 paths: **1 hit — `skills/objectstack-ui/rules/dashboards.md`** (the published rule authors `refreshInterval`; kept, not dropped). ⇒ human merge for the whole PR. | governed |\n\n### ② semver\n`@objectstack/spec` **minor** with BREAKING banner and `registered` marker for the six ids; `@objectstack/service-datasource` **patch**; readers in the same `fixed` group. Correct.\n\n### ③ Boundary flags\n- Dev Q1 (eighth key) → A; Q2 (credential derivation) → A — both ruled by the seat with the measurement; row 5 is this review's independent confirmation.\n- Ablation legs A (revert one rename ⇒ gate red, one offender) and B (delete only a tombstone on a non-strict shape ⇒ gate blind, pin red, authored value silently replaced by the default, `check:authorable-surface` red) — the second leg is the whole argument for tombstones and is recorded as such.\n- objectui#7783 must carry the objectui-side reader move and the pin updates listed in the body, sequenced behind objectui's next `@objectstack/spec` bump (objectui#7122's family). Not this PR's to do; it is this stack's landing obligation to the sibling repo.\n\n### Evidence\nCI on `80869188`: 8 success (stacked PR); local union at `3351a860` and re-run after the seam fix: spec 473 files / 12844 tests, typecheck, `check:generated` 15/15, `check:skill-examples`, `check:doc-authoring` 0 findings, `check:i18n`, driver-memory 41 files / 1058 tests, `check-adr-0087-registration --base origin/main` 8 declared-breaking changesets each with a disposition. `mergeable_state: clean` against 4/6's branch. `Part of #14478` (card deliberately left open until 6/6). \n\n**Clearing, same stroke:** `needs:contract-review` off this card and PR #15988 with provenance. ⛔ No ready flip / auto-merge: governed, and the stack lands in order under the `domain:spec` seat with stack 0 human-merged.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5556588466,"author":{"login":"os-sales","id":319429713,"type":"User"},"created_at":"2026-09-06T03:19:21Z","updated_at":"2026-09-06T03:19:21Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15680#issuecomment-5556588466","body":"Delivered by PR #15988 and landed on `main` at `e9fcd6bbd` (via the stack's trunk PR #15626, merged 2026-09-06T03:17Z).\n\nThis is the card that took the gate to zero on its declared population, and the card that surfaced the campaign's most instructive defect: **a rename verified \"by TYPE\" leaves an untyped bag untouched, by construction**. `service-datasource`'s libSQL seam still read the retired `config.timeout` after this card renamed it, so a datasource authored the canonical way had its timeout silently dropped (#16023, fixed here). The follow-up swept all eight renamed keys by name, by bag-shaped read and by string literal, across every workspace package — seven came back empty, and the measured emptiness is the finding.\n\nClosing as completed; `pm:dispatched` removed in the same write.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
