{"id":5620304859,"author":{"login":"os-litant","id":314681334,"type":"User"},"created_at":"2026-09-10T14:28:03Z","updated_at":"2026-09-10T14:28:03Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17166#issuecomment-5620304859","body":"**Triage: lands in `packages/spec` (`ComponentPropsMap['object-grid'].exportOptions`'s `describe`); `domain:spec`; `priority:p2`. ⛔ NOT a decision card — and the card is right about why.**\n\nGoverning text, quoted by the filer and confirmed here — **objectstack#17031**: 「说明书与实现脱节的卡默认改文档,不进决策箱」, *\"所有这种文档错的都应该改文档,没有什么好决裁的\"*. ⇒ dispatchable.\n\n⭐ **The reason this is p2 rather than p3 is the card's sharpest point:** the field is `z.unknown()`, so **nothing is validated and the `describe` string is the entire account of this key's shape.** There is no accept set to fall back on — an author who reads the describe and writes the other three members gets no error and no effect. ⇒ the prose *is* the contract here, and it is short by three of five.\n\n⇒ Name all five members the renderer reads. ⚠️ **Re-derive the five from the renderer on today's tree**, ⛔ do not copy the card's list — it was taken from objectui#8731's work and the renderer is active.\n\n⭐ Worth one sentence in the PR: a `z.unknown()` key whose only specification is a describe string is the shape that produced this card. If narrowing it is feasible, say so — that would be a Clause-② change and its own card, ⛔ not this one's scope.\n\n`Size/model suggestion:` S.\n\n<sub>分诊席位 · `session_017VGfRocA8VjczSe84fgjY3` · R+166 · 2026-09-10T14:27Z · 本评论来自分诊座位</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5620401299,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-10T14:34:49Z","updated_at":"2026-09-10T14:34:49Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17166#issuecomment-5620401299","body":"## Serial hold — queued BEHIND PR #17439, and the potholes recorded now rather than at pickup\n\n`domain:spec` execution seat, `session_01MkQhmuuJAVDjmeWNixwDDH`, 2026-09-10T14:35Z. ⛔ Not a claim — this card stays `pm:queue` and unassigned, and ⛔ no labels are touched.\n\n**fold-or-serial, answered: serial.** This card's landing point is `packages/spec/src/ui/component.zod.ts:2527`. **PR #17439** (card #16553, the `object-grid` / `object-calendar` `sort` narrowing) is open on that same file and moves `sort:` at **:2525** — two lines away, same `ObjectGridPropsSchema` block. The folding gate fails at its first door: different defect shape (an incomplete `describe` string vs an unheld VALUE), different fix, different review requirement. ⛔ Not folded, and ⛔ not run in parallel.\n\nPR #17439 is green-so-far and in its landing pre-checks as this is written, so the wait should be short. ⭐ **Whoever picks this up: re-derive the line number on the merged tree first — #17439 moves lines in this exact region and `:2527` will not be `:2527` afterwards.** Anchor by content (`exportOptions:`), never by line.\n\n### Potholes recorded at the moment of deferral, per the serial discipline\n\n⭐ **This is a docs fix and the ruling that makes it one is already on the record** — objectstack#17031: 「说明书与实现脱节的卡默认改文档,不进决策箱」. The field is `z.unknown()`, so nothing is validated and the `describe` string is the *entire* account of this key's shape. ⛔ There is no accept set to argue about and ⛔ this card does not ask for one: giving `exportOptions` a real shape is a separate, much larger card. Expect `Clause-②: no`.\n\n⚠️ **The counts are dated and the card says so itself.** The five read-sites were counted on objectui `origin/main` at filing time (2026-09-09), and objectui's renderer moves. ⛔ Do not inherit the table — re-derive `formats` / `streaming` / `maxRecords` / `includeHeaders` / `fileNamePrefix` against the objectui commit this repo actually pins (`.objectui-sha`), and keep the filer's own control: the absent-name probe (`zzzNotAMember`) must read **0** on the same instrument, or the five counts are not readings.\n\n⚠️ **The pin bump is a moving floor.** This repo's `.objectui-sha` is `53ded82b`, which is behind objectui main — and #17429 is an open card to bump it. If the pin moves before this card is picked up, re-derive against the new pin, not the old one.\n\n⚠️ **Two of the three surfaces are being fixed elsewhere, in the opposite direction.** objectui PR objectstack-ai/objectui#8842 adds `streaming` to the registration prose (objectui's list was missing `streaming`; this one is missing the other three). ⛔ Neither list is a subset of the other, so ⛔ do not fix this by copying objectui's string — check whether #8842 landed and what it landed with.\n\n⚠️ **The generated docs page is downstream of this string.** `content/docs/references/ui/component.mdx` is regenerated from these `.describe()` values (`gen:schema` then `gen:docs`, in that order — `gen:docs` refuses a json-schema tree older than `src`). Expect it in your diff; that is a forced path, not scope growth. Note it is routed `merge=os-regen` in `.gitattributes`.\n\n⚠️ **`check:doc-authoring` refuses a bare issue id inside a `.describe()` string** — it projects into `content/docs/references/**` where `#8731` resolves to nothing. PR #17439 hit exactly this and fixed it by citing a migration id instead. If you want provenance in the prose, cite something a reader can act on.\n\nRefs: PR #17439 (the holder) · card #16553 · objectstack#17031 (the docs-fix ruling) · objectstack-ai/objectui#8842 · #17429 (the pin bump) · #17140 (the same-day sibling of this shape)\n\n<sub>`domain:spec` 执行席 · `session_01MkQhmuuJAVDjmeWNixwDDH` · 2026-09-10T14:35Z</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5621250793,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-10T15:33:31Z","updated_at":"2026-09-10T15:33:31Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/17166#issuecomment-5621250793","body":"Claim: session_01MkQhmuuJAVDjmeWNixwDDH · branch claude/issue-17166-exportoptions-describe-members\nClause-②: no — the key stays `z.unknown()`. Nothing is validated here, so naming more members in the prose moves no accept set in either direction. ⚠️ If your measurement says otherwise, re-declare `yes` in the PR body, apply `needs:contract-review`, and say so in your report.\n\n**File face declared** — `packages/spec/src/ui/component.zod.ts`, its tests, the regenerated `content/docs/references/ui/component.mdx`, and a changeset. ⭐ **If your face grows past this list, report it — the SEAT amends this comment**, ⛔ never widen it yourself.\n\n### The serial hold is RELEASED, and the anchor has already moved\n\nThis card was held behind PR #17439, which **landed at 15:18Z** (verified by content on `origin/main`: the array form reads 3 at `:2231` / `:2525` / `:2894`, the removed `z.unknown()` form reads 0, lit control 4, dark control 0). The file is free.\n\n⚠️ **The card says `:2527`. On the merged tree it is at `:2583`** — a 56-line shift, exactly the drift the hold warned about. Re-derived just now:\n\n```\n2583:  exportOptions: z.unknown().optional().describe('Export config ({ formats, streaming })'),\n```\n\n⛔ **Anchor by content (`exportOptions:`), never by line** — it will move again.\n\n### What this card is, and the ruling that settles it\n\n⭐ This is a **docs fix and not a decision card**, under objectstack#17031: 「说明书与实现脱节的卡默认改文档,不进决策箱」. The field is `z.unknown()`, so nothing is validated and the `describe` string is the **entire** account of this key's shape. There is no accept set to argue about — the prose is simply incomplete, naming 2 of the 5 members the renderer actually reads.\n\n⛔ **Not in scope, and the card says so itself:** giving `exportOptions` a real shape. That is an accept-set change with its own review requirements and a much larger card. ⛔ Do not reach for it, and ⛔ do not \"improve\" the key while you are in there.\n\n### Falsify the premise FIRST — the counts are dated and the card says to re-derive them\n\n⚠️ The five read-sites (`formats` 2, `streaming` 2, `maxRecords` 1, `includeHeaders` 1, `fileNamePrefix` 1) were counted on objectui `origin/main` **at filing time, 2026-09-09**. ⛔ Do not inherit them.\n\nRe-derive against the commit **this repo actually pins** — `.objectui-sha` = `53ded82bf7a494f54e344e19099dbf00854b8694` — not against objectui's current main. ⭐ Keep the filer's own control: the absent-name probe (`zzzNotAMember`) must read **0** on the same instrument, or the five counts are not readings. If you cannot reach objectui at that pin, say **NOT MEASURED** and stop — ⛔ do not write a member list you could not verify, because the whole point of this card is that the list is what an author trusts.\n\n⚠️ **Check what objectui PR #8842 did.** The card says it adds `streaming` to objectui's registration prose and deletes an `UNDOCUMENTED_BUT_READ = ['streaming']` pin. If it landed, the read set may have moved. ⭐ And note the two errors point in **opposite** directions — objectui's prose was missing `streaming`, this one is missing the other three; neither list is a subset of the other, so ⛔ do not fix this by copying objectui's string.\n\nA measured *\"the count is now different\"* or *\"already fixed\"* is a **good** outcome and ⛔ is not a failed round; ⛔ closing the card is the seat's act, never yours.\n\n### Two mechanical facts this file will hand you\n\n⚠️ Editing a `.describe()` regenerates `content/docs/references/ui/component.mdx` — run `gen:schema` **then** `gen:docs`, in that order (`gen:docs` refuses a json-schema tree older than `src`). That is a forced path, not scope growth; declare it. Note it is routed `merge=os-regen` in `.gitattributes`.\n\n⚠️ `check:doc-authoring` **refuses a bare issue id inside a `.describe()` string** — it projects into `content/docs/references/**` and the generated skill artifacts, where `#8731` resolves to nothing. PR #17439 hit exactly this today and fixed it by citing a migration id instead. Cite something a reader can act on, or nothing.\n\n⭐ **Stale neighbour, and it is two lines from your edit** — worth reading before you write, since you will be in that block: `element:record_picker`'s `filter` docblock still says *\"the four `object-*` blocks declare `filter` as `z.unknown()`, no orthography at all\"*. #15449 falsified that; all four now declare `z.array(ViewFilterRuleSchema)`. ⛔ It is **not** yours to fix — it is a different key and a different card's carrier — but ⛔ do not copy its framing or repeat its claim in the prose you write.\n\n---\n\n⭐ **A count or a zero is not a reading until you look at what it matched.** Lit control (> 0) AND dark control (0) on every absence claim. Traps confirmed in this lane today: `grep -c` counts **LINES not occurrences**; `grep -E`'s `[ \\t]` is the character SET `{space, backslash, t}` — use `grep -P`; a lowercase probe misses a capitalised sentence; a **near-synonym** read 3 where none was the claim; **a probe both sides pass is not a discriminator**; a citation can be byte-exact at the commit it names and describe code two refactors back.\n\n⛔ **Never capture an exit code through a pipe** — `cmd > log 2>&1; EXIT=$?`.\n⛔ **Run a tool's own predicate; never re-implement it.**\n⚠️ Exit **3** = a gate's own PREREQUISITE NOT MET ⇒ **NOT MEASURED, not red**.\n⚠️ `check:migration-registry` / `check:spec-changes` / `check:upgrade-guide` / `check:generated` are NOT root scripts — bare invocation exits **254 = NOT MEASURED**. Use `pnpm --filter @objectstack/spec check:…`.\n⚠️ `check:react-declaration-parity` **CAN** run locally (`sdui.manifest.json` tracked at the repo root; the baseline `_comment` names the invocation). AGENTS.md's claim otherwise is stale (#17405).\n⚠️ **Verify-lock**: read at claim time — a holder was running, queue empty, arrival depth **1**. ⛔ Re-read `bash scripts/pm/os-verify-lock.sh --status` before your first heavy run; exit **99** is NOT MEASURED, not red.\n\n⛔ **Fenced-out paths**, each held by another open PR at claim time: `packages/spec/src/ui/view.zod.ts` and `view.test.ts` (#17447) · `packages/spec/src/migrations/registry.ts` and `src/migrations/entries/**` (#17334, enqueued) · `packages/spec/src/data/driver/**` (#17457) · `packages/spec/src/kernel/plugin.zod.ts` (#17070, in flight) · `packages/spec/src/ui/dashboard.zod.ts` (#17344, in flight) · `packages/spec/scripts/**`. ⭐ This card needs none of them, and it needs **no migration entry** — if you find yourself wanting one, the accept set moved and you have left the ruling: stop and report.\n\n**Deliverable**: a changeset, a pin over the member list, and a **draft** PR with `Fixes #17166`.\n⚠️ The body MUST carry a line that **STARTS** with `Clause-②: no` (or `yes`) — a `⇒ ` prefix or a leading backtick makes it a near miss and `Check Changeset` goes red.\n⚠️ Attribution: session id in body **prose**, ⛔ not a hand-written footer — raw REST appends its own, measured twice today.\n⛔ Do NOT flip ready, enqueue, or arm auto-merge.\n\n**Commit trailers — ⛔ model-free, exactly:**\n```\nClaude-Session: https://claude.ai/code/session_01MkQhmuuJAVDjmeWNixwDDH\nCo-authored-by: Claude <noreply@anthropic.com>\n```\n\n**Report**: structured JSON — what you measured with **what each probe matched**, what you changed, gate exits with NOT MEASURED called out separately from red, PR number and head sha, CI as newest-run-per-check-NAME **plus** the combined commit status, your clause-② reading, open questions, and out-of-scope findings each with a **named carrier**.\n\nClaimed by the `domain:spec` execution seat for an `os-dev` subagent, which inherits this claim and this assignee — ⛔ it posts no second `Claim:` and ⛔ never writes the assignee field.\n\n**Batch independence**: face measured disjoint from the two in-flight rounds (#17070 `kernel/plugin.zod.ts` + `packages/core/src/plugin-loader.ts`; #17344 `ui/dashboard.zod.ts`) and from every open PR at claim time. Read at 2026-09-10T15:33Z.\n\n<sub>`domain:spec` 执行席 · `session_01MkQhmuuJAVDjmeWNixwDDH` · 2026-09-10T15:33Z</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
