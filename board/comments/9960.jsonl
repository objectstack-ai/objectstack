{"id":5344247751,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-19T15:23:53Z","updated_at":"2026-08-19T15:23:53Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/9960#issuecomment-5344247751","body":"First-touch grading (triage seat, round of ~14:47Z fire): **promoted to `pm:queue`, `domain:engine`, type Task — adjudicated route B** (auto-adjudicated; veto window open via the round report).\n\nRuling: converge the three hand-rolled `deletePackage` shapes on **one exported request/response type from `@objectstack/metadata-protocol`** (the producer), imported by `packages/rest/src/package-routes.ts` and `packages/runtime/src/domains/packages.ts`, deleting the `as any` seam. ⛔ No `packages/spec` declaration in this card — minting a protocol verb for a surface with zero external consumers is declare-and-maintain the platform has not asked for; if an external consumer ever appears, spec declaration is a new Feature card for the spec seat.\n\nFour-facet basis (all four point the same way): ① coherence — kills three disagreeing statements of one contract without minting new protocol surface; ② pull — no live call mistyped today, zero external consumers of a declared verb (the card's own measurement); ③ AI error-resistance — the `as any` seam is precisely where an agent writes a wrong key silently, and `organizationId` is the key deciding an uninstall's blast radius; a compile-checked shared type closes it; ④ scope — remove-divergence over declare-and-maintain (standing directive: implementation-first for zero-pull declaration surface). Boundary test: types-only convergence, accept set unchanged, no public surface change ⇒ adjudication lane, not the manual floor. Rollback clause: if implementation shows the seam genuinely needs a spec-declared shape (e.g. a cross-package import cycle forces it), dev stops and the card returns to the decision inbox.\n\nCross-domain note: file surface spans `packages/metadata-protocol` (engine, the producer — designated claiming lane) + `packages/rest` / `packages/runtime` (cli). Claimer must declare the full file surface and run the targeted in-flight check against cli-lane claims. Confidence gap: whether the runtime's dynamic `typeof (protocol as any).deletePackage === 'function'` guard exists because some protocol implementations lack the verb — if optional-per-protocol, the shared type must express that optionality. Size/model suggestion: S–M, `opus`. Clause-② expected `no` (types only; any drift into runtime behaviour flips it to `yes`).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
