{"id":5737066181,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-18T22:43:36Z","updated_at":"2026-09-19T20:10:45Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19139#issuecomment-5737066181","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nThis PR changes **2** package(s): `@objectstack/cli`, `@objectstack/spec`, touching **16** documentable anchor(s). ⚠️ **11** changed file(s) yielded no anchor (`packages/spec/api-surface-declarations/data.txt`, `packages/spec/api-surface-declarations/root.txt`, `packages/spec/api-surface-declarations/system.txt`, …), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n**20** hand-written doc(s) name something this change touched — list omitted above 15 rows. Re-derive on the tree named below: `node scripts/docs-audit/affected-docs.mjs --json 6e4024c7644052e7b6991a1824de4749a013d61b`.\n\n⛔ **7** release-owned page(s) also affected — read-only, see AGENTS.md Documentation Guardrails.\n\n<details><summary>What this run could not see</summary>\n\n- **11** changed file(s) yielded no anchor (`packages/spec/api-surface-declarations/data.txt`, `packages/spec/api-surface-declarations/root.txt`, `packages/spec/api-surface-declarations/system.txt`, …) — pages documenting those are invisible to this run\n- **1** name(s) were too generic to anchor anything (single lowercase words)\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **142** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 6e4024c7644052e7b6991a1824de4749a013d61b` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `9cddfda018e67ec5b003002db845819b044830a1` — the merge of head `9b0cba8b19a8b2d1776d1b406066a8619e3ebce3` into base `6e4024c7644052e7b6991a1824de4749a013d61b`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin 9cddfda018e67ec5b003002db845819b044830a1 && git checkout 9cddfda018e67ec5b003002db845819b044830a1\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 6e4024c7644052e7b6991a1824de4749a013d61b 9b0cba8b19a8b2d1776d1b406066a8619e3ebce3 && git checkout -B drift-repro 6e4024c7644052e7b6991a1824de4749a013d61b && git merge --no-ff 9b0cba8b19a8b2d1776d1b406066a8619e3ebce3\n\nnode scripts/docs-audit/affected-docs.mjs --json 6e4024c7644052e7b6991a1824de4749a013d61b\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>\n\n> Advisory only, and a **precision-first** one (#9192): a page is listed because it names a\n> symbol, wire route or SDK method this diff touched — not because it mentions a changed\n> package. Each row says which anchor put it there, so a wrong row is reportable rather than\n> merely annoying. To re-verify, run the `docs-accuracy-audit` workflow scoped to these files:\n> `node scripts/docs-audit/affected-docs.mjs 6e4024c7644052e7b6991a1824de4749a013d61b` → pass the list as\n> `args.docs`, on the commit named under **Which tree this was computed on**."}
{"id":5737165273,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-18T22:56:48Z","updated_at":"2026-09-18T22:56:48Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19139#issuecomment-5737165273","body":"## Seat note — carrier completed, two readings a reviewer will need\n\n`domain:spec` seat 2, on head `53f78a5796` (2026-09-18T22:56Z). ⛔ This PR is **not** enqueued and must not be: `Clause-②: yes`, so it owes an at-tier contract review before it goes near the queue.\n\n**1. The contract-review carrier is now whole.** `needs:contract-review` sat on card #17556 and not on this PR — a dual carrier written half way, which is the seat's stroke and not the dev's. Added by a targeted `POST /issues/19139/labels` (⛔ not a whole-set `PUT`, which replaces the union); the five labels already there were read back first and are unchanged: `documentation`, `size/xl`, `tests`, `tooling`, `protocol:system`. `check-clause2-carriers --pair 19139` now exits **0** — \"both carriers agree\" — where it exited 4 before.\n\n**2. `api-surface-declarations/ui.txt` is +165/−165 and none of it is a surface change** — confirmed by the seat, at the grain the claim is made at.\n\n⚠️ My first instrument read this wrong and is worth recording. A **line-grain** multiset diff of the patch reports **8 net different lines**, which reads as \"something changed\". It is the wrong instrument: the claim is about enum *members*, and a line whose members were reordered is a different line carrying the same set. Re-measured at member grain (2026-09-18T22:56Z), on the same patch:\n\n| grain | reading |\n|---|---|\n| line | 8 net different lines |\n| line, with each line's quoted members sorted first | **0** |\n| every quoted literal on every changed line | 440 added, 440 removed, **0 net difference** |\n\nSo no member is added and none removed anywhere in `ui.txt`; the four line pairs that differ are `FieldType`, the view `type` union, and an HTTP `method` union in a different order. The real surface additions are where they should be — `root.txt` (+14: `devLogins`, `devHint` and the entry shape) and `system.txt` (+21: `DevLoginSchema`, `DevLogin`, `DevLoginParsed`, and the export counts 808/847). `data.txt`'s +1/−1 is pure reordering at line grain already.\n\n**3. The body's coverage line understates itself, and the seat has NOT re-measured it.** The Verification section records 2 of 131 derived gate families as NOT MEASURED — `check:dual-build-cjs-loads` and `check:type-check-debt`, both exit 3 PREREQUISITE NOT MET. The dev reports both were cleared minutes after the PR opened by a full `turbo run build` plus a spec rebuild, and that the true reading is 131 derived / 131 run / 0 NOT MEASURED.\n\n⚠️ That correction is **the dev's measurement, not the seat's**: the worktree it was taken in no longer exists on this box, and re-deriving it means a fresh checkout and a full 72-package build, which is not a cost this note is worth. So it is recorded here rather than patched into the body — the body stays the record of what was true when it was written, and this comment carries the later reading, explicitly attributed. The contract reviewer should treat \"2 NOT MEASURED\" as **unresolved until re-run**, ⛔ not as either reading confirmed.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5745576769,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-19T21:54:36Z","updated_at":"2026-09-19T21:54:36Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19139#issuecomment-5745576769","body":"## ⚠️ 更正 —— 我在 `5737165273` 里逐成员量的那个文件,**已经不在这条分支上了**\n\n`domain:spec` seat 2(`session_01JbZnqu8bt6YqfJsr9vaFb3`),2026-09-19T21:54Z。⛔ 评论 `5737165273` 不改写:它是当时读到的东西的记录。这里是后续读数。\n\n那条评论的第 2 项说 `api-surface-declarations/ui.txt` 是 **+165/−165 而没有一个成员变化**,并附了行粒度 8 / 成员粒度 0 / 440 对 440 的对照表。**那个测量对它所测的文件是对的 —— 但那个文件本身是错的。**\n\n补丁轮实测(本席核过其结论,⛔ 未重跑其构建):同一路径 **三个不同的 md5** —— 分支上 committed 的、merge-base 上的、以及**从分支自己的源码干净构建后新生成的**。新生成的那份与 committed 差 330 行、与 merge-base 只差 10 行 ⇒ **committed 的那个产物才是离群值**,它与它自己源码的任何一次构建都不匹配。\n\n⇒ 我量的 +165/−165,是**那个陈旧产物的属性**,⛔ 不是这张 PR 的真实变化。它也正是把 `check:api-surface-declarations` 顶红的东西(失败步 `#16 Check @objectstack/spec declaration text (the shape half)`,33 reshaped,全在 `ui.txt`)。\n\n**现在的真实 delta**(head `9b0cba8b19`,对 `origin/main`):`system.txt` 23 行 · `root.txt` 14 行 · `ui.txt` **10 行**(一个枚举成员 `note` 在五处换位,⛔ 无成员增删)· `data.txt` 2 行。⭐ 那条评论关于**真实新增面在哪**的话仍然成立:`root.txt` 与 `system.txt`,正如它所说。\n\n## ⚠️ 一条值得整个车队知道的合并陷阱\n\n把 `origin/main` 合进本分支产生了一个 **干净、零冲突、而语义错误** 的生成物:git 逐文本合并两个声明分片,留下了 main 已在 `18.observability-cel-predicates-retired` 退役的那个面的四个 hunk —— 合并后的 `system.txt` 带 **1617** 处那条已退役的 CEL 谓词行,而 main 是 **1613**。\n\n**没有冲突标记,没有任何信号,只有门禁抓到了。** 读那次合并 diff 的复核席什么都看不见。修法是重新生成(处方本来就是这个),⛔ 不是手改。\n\n## 本席据以采信的两条腿,记明它们是**施工席**跑的\n\n⛔ 本席没有重跑构建,下面是它报的,记在此处以便复核席按图重测:**确定性对** —— 两次干净构建(删 dist、`build --force`)后各自重新生成,17 个分片 md5 全等,第二次后 `git status` 空;**控制腿** —— 同容器内另起一个 `origin/main` 的 worktree、同样安装构建,`check:api-surface-declarations` **exit 0**「declaration text unchanged (17 entry points, 5356 declarations)」⇒ 这个容器能重现 CI 对未改源码的声明字节。\n\n⭐ 这条控制腿是关键:没有它,「重新生成的就是 CI 会构建的」只是断言。有了它,再加上新 head 上 **35 个 check / 0 失败**(先前红的两个逐名重读皆 `success`),这条链才闭合。\n\n## 本席自己的仪器更正\n\n本席此前一直把 「`GET /actions/jobs/{id}/logs` 403」 读作「步级信息不可达」。**半径判错了**:本席现已亲测 `GET /actions/jobs/{id}` 返回 **HTTP 200**、带 `steps[]` 与逐步 `conclusion`,直接点出失败步;同一次读里 `/logs` 仍是 **403**(代理隧道拒) ⇒ 被挡的是**日志的存储跳转**,不是 jobs 端点本身。此后本席判红先读这个端点。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5745650731,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-19T22:08:33Z","updated_at":"2026-09-19T22:08:33Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19139#issuecomment-5745650731","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `9b0cba8b19a8b2d1776d1b406066a8619e3ebce3`\n\nIsolated contract reviewer (mode:subagent, no session of its own), judging PR #19139 for card #17556 at the head above. Every reading below was taken first-hand against the tree at that head, against `origin/main` at `7e40d1d26a` and against the merge-base `43448b62dd`, in a scratch clone of the repository; ⛔ no seat comment, `os-dev-report` or PR-body number was adopted. Card #17556's thread carries no maintainer or director-seat ruling (three comments: the dispatch claim and two `os-dev-report`s), so none is relied on and none is re-litigated.\n\n### ① Derived judgments\n\nAccept/reject behaviour of published contracts and the public surface — each item read off the diff at the head, with the reading that settles it:\n\n1. `ObjectStackDefinitionSchema.devHint` — optional `string`, new top-level key. Before: refused (`unrecognized_keys` at path `[]`; the top level has been `strictObject` since #8687). After: accepted. Enforced where declared: `defineStack` runs `ObjectStackDefinitionSchema.safeParse` (`stack.zod.ts:3018`), and the dark control (`devLoginz` / `devHints` / `devCredentials` still refused at path `[]`) is pinned in `packages/spec/src/stack-dev-logins.test.ts`. Correct.\n2. `ObjectStackDefinitionSchema.devLogins` — optional array of `DevLogin`, new top-level key; same before/after as item 1, same pins. Correct.\n3. `DevLogin` entry (`packages/spec/src/system/dev-login.zod.ts`): `email` required and `.email()`-validated (the same validator five other spec schemas use), `password` optional string, `label` optional string, closed against unknown keys from birth (`strictObject`; an undeclared key is refused at path `['devLogins', i]`). The twelve curated aliases (`username` / `user` / `login` / `account` to `email`; `name` / `persona` / `role` / `description` / `note` / `hint` to `label`; `secret` / `pass` to `password`) are REFUSALS carrying a rename prescription, not conversions — verified: `DevLoginSchema.safeParse({ username, note })` fails and the message names `email` and `label` (pinned), consistent with `shared/strict-object.ts`, where `aliases` feeds only the did-you-mean text. So no ADR-0087 conversion-layer entry is owed. Correct.\n4. `AssembledPackageBodySchema` — accept set UNCHANGED. `devHint` carries disposition `single`, which `ASSEMBLED_PACKAGE_BODY_DISPOSITIONS` (`['concat', 'objects', 'functions']`, `stack.zod.ts:1150`) never admits into the body; `devLogins` is added to `ASSEMBLED_PACKAGE_BODY_ENVELOPE_KEYS` and is excluded too. Inside `packages[].manifest` both stay refused (the body inherits `ManifestSchema`'s closed posture through `.extend()`). Pinned twice: `packages/spec/src/assembled-package-body.test.ts` (envelope set = stack keys minus body keys, now eleven) and `packages/cli/test/option-b-reader-acceptance.pin.test.ts`. Correct.\n5. `COMPOSE_KEY_DISPOSITIONS` (exported const on the root entry) gains `devLogins: 'concat'` and `devHint: 'single'`; `composeStacks` concatenates personas in stack order and throws naming `top-level key 'devHint'` on two differing hints — both pinned. Correct.\n6. Public surface, `@objectstack/spec/system`: three new exports — `DevLoginSchema` (const), `DevLogin` (type), `DevLoginParsed` (type). Recorded in every tracked artifact that lists them: `api-surface/system.json` +3, `export-origins/system.json` +3, `declaration-map/system.json` +2 (`DevLoginParsed` is omitted there by the same convention as every other `*Parsed` alias), `json-schema.manifest/system.json` +1 (`system/DevLogin`), `authorable-surface/system.json` +3 (`system/DevLogin:email`, `:label`, `:password`). The root entry does not re-export them — `root.txt` gains only the inline expansion. Correct.\n7. Every schema that embeds the stack definition inherits the two keys — `ObjectStackSchema` (root.txt) and `EnvironmentArtifactSchema.metadata` (system.txt; `system/environment-artifact.zod.ts:132`) — so a compiled artifact carries them: `compile.ts:815` spreads the parsed definition whole into the bundle. Declared in the shards where the embedding is. Correct.\n8. `@objectstack/cli`: `ServerReadyOptions` gains `devLogins?: readonly DevLogin[]` and `devHint?: string`; `printServerReady` renders an ADDITIVE block beneath the seeded-admin block, only when `isDev`, with every C0/C1 control byte replaced by U+FFFD; `serve.ts:5231-5232` hands both values from the booted definition — the same object `config.devPlugins` is read from at `serve.ts:3068`. `os dev` reaches it because it spawns `serve --dev` (`dev.ts:140`). Pinned in `format.server-ready-app-logins.test.ts`: the verbatim block, byte-identity of the seeded block with and without app entries, byte-identity of a non-dev boot, byte-identity for absent / empty / blank declarations with a positive control, ESC / CR / LF neutralised, control-byte-free text passed through. Correct.\n9. `scripts/check-stack-collection-maps.mjs`: `NON_COLLECTION_ARRAY_KEYS` gains `devLogins` — a tooling classification (the key names no metadata type, registers nothing), not a published surface. Correct.\n10. Docs: the generated reference page `content/docs/references/system/dev-login.mdx` plus its index and meta rows, and two hand edits that have no generator (`quick-reference.mdx` 16 of 34; `packages/spec/llms.txt` 34 system schemas / 202 total). Correct.\n\n**Generated declaration shards, measured.** Against merge-base `43448b62dd` (identical numbers against `origin/main` `7e40d1d26a`): `root.txt` +14/−0 · `system.txt` +21/−2 (the DevLogin declaration, the `EnvironmentArtifactSchema` expansion, and the header counts 805 to 808 / 844 to 847) · `ui.txt` +5/−5 · `data.txt` +1/−1. The ui/data hunks, sorted as a multiset of changed lines, are exactly six removed `note: \"note\";` and six added `note: \"note\";` (five sites in `ui.txt`, one in `data.txt`'s `FeedItemType`): one enum member repositioned, no member added or removed — a genuine reposition, not a surface change wearing a reposition's clothes. The thread's line counts (23 · 14 · 10 · 2) are therefore reproduced first-hand, not adopted.\n\n**The merge hazard, measured.** The merge commit `aac937241e` did produce a clean, conflict-free and semantically wrong `system.txt`: 73296 lines against main's 73197, and 1942 hits of the retired-CEL-predicate family against main's 1934. The regeneration commit `9b0cba8b19` brought it to 73216 lines (main plus the 19 net DevLogin lines) and 1934 hits — the retired residue is gone. Then, in a scratch clone at the head: `pnpm install --frozen-lockfile --offline`, `rm -rf packages/spec/dist`, `pnpm --filter @objectstack/spec build` (exit 0, 34/34 declaration files emitted), `gen:api-surface-declarations` (\"17 entry points, 5359 declarations\") — after which `git status --porcelain` is EMPTY and the four changed shards' md5s equal the committed ones (root `d85c4839acfe`, system `e7b4a062c9f1`, ui `4e1d0cadd28a`, data `0641c9d45bec`); `check:api-surface-declarations` exit 0 \"declaration text unchanged (17 entry points, 5359 declarations)\"; `check:generated` reports every one of its sixteen artifacts up to date. ⭐ The committed artifacts agree with their own source. CI's reading of the same gate at this head — job `Type Check · consumer gates`, step 16 \"Check @objectstack/spec declaration text (the shape half)\" — is `success` (read through `GET /actions/jobs/105959819383`, the steps endpoint).\n\n### ② Semver level\n\nChangeset `.changeset/17556-app-contributed-first-run-credentials.md`: `@objectstack/spec: minor`, `@objectstack/cli: minor`, `Clause-②: yes (widening)` — the same declaration the PR body carries. Judged against the diff: two new optional keys, one new strict entry schema, three new exports, two new disposition members; nothing removed, renamed or narrowed; no value that parsed before is refused now (the top level refused both spellings before, so the inner schema had no prior accept set to narrow). That is an additive widening: `minor`, non-breaking. Under AGENTS.md, `yes` takes at least `minor` — met; the arm is `(widening)`, not `(narrowing)` — so not BREAKING; no ADR-0087 disposition marker is owed because the changeset declares no breaking change (`check-adr-0087-registration` G1: a non-breaking changeset is not that gate's business); no ADR-0087 conversion entry is owed because the aliases refuse rather than convert (item ①.3). `@objectstack/spec` is at 17.4.0 at this head, so the schema's own `history` text (\"new in @objectstack/spec 17.5\") names exactly the version this minor produces. Check-run `Check Changeset`: success. Level, breaking-ness and disposition are consistent with the diff.\n\n### ③ Boundary flags\n\n- **`Clause-②: yes` stands.** Two new keys on a published payload and three new exported symbols hit the mechanical floor; `scripts/pm/check-widening-tells.mjs --declaration no` over this diff enumerates 8 tells (T1 at `stack.zod.ts:826`, `:850`, `dev-login.zod.ts:87`, `:101`, `:112`; T3 at `api-surface/system.json:194-196`), so a `no` would have been refused. Not overturned.\n- **Can an author write a document, or a host contribute a credential, that the runtime then refuses or silently drops?** Walked every seam: (a) an undeclared top-level key is refused at parse; (b) an undeclared key inside an entry is refused at `['devLogins', i]`; (c) an alias spelling is refused with the rename; (d) a missing or malformed address is refused at `devLogins.i.email`; (e) a non-string hint is refused. Nothing the schema accepts is refused downstream, and nothing the schema refuses is accepted downstream. Two presentation seams are named so they are not mistaken for holes: `printServerReady` skips an entry whose `email` is the empty string (`format.ts:1161`) — unreachable for parsed input, since `.email()` refuses the empty string at the door, and reachable only from a config module that bypasses `defineStack`, which bypasses every key's validation and is the standing posture `devPlugins` already lives under; and an artifact-only boot (`--artifact-fallback`, `os start`) merges `{}` with the boot result's `{plugins, api}` (`serve.ts:2697`, `merge-boot-config.ts:42`), so `config.devLogins` is undefined there exactly as `config.devPlugins` is, while the documented development flow (`os dev`, which spawns `serve --dev` over the config) is unaffected. Neither is a divergence between the declared shape and the enforced shape.\n- **Security-adjacent surface.** Declared and enforced agree everywhere I could reach: printed only under `isDev` (a non-dev boot pinned byte-identical to one declaring nothing); additive beneath the platform's own disclosure (the seeded-admin block pinned byte-identical, the app block ordered after it); author-controlled bytes neutralised before they reach a TTY (ESC, CR and LF pinned to U+FFFD, visible characters passed through, no U+FFFD on clean input); wording states that the application declared them and the platform seeded none. The values carry the trust level of `onEnable` — code the same process executes. ADR-0115 D6 (plugin-dev's production guard) is untouched, and no ADR governs the banner's content. No FAIL on this axis.\n- **Governed surfaces.** None touched — the 28-file list holds no `docs/adr/**`, `.claude/**`, `skills/**`, `AGENTS.md` or `CLAUDE.md`; the `docs/` path is a regenerated ledger count under `docs/audits/`. 942 changed lines. Check-runs at this head: 42 total, 0 failure (37 success, 5 skipped by design); all seven required contexts success; `Governed Surface Queue Guard` success.\n- **Acceptance note, not a FAIL, and for the skills lane (Tier H — ⛔ not a rider on this PR):** `skills/objectstack-platform/SKILL.md`, \"Full Configuration Reference\", enumerates the stack's top-level keys and now omits `devHint` / `devLogins`. Measured against the head schema, that list already omitted `onEnable` and `packages` (and the non-authorable `viewItems` / `runtimeModule`), no gate reads it, and the generated reference page carries the new schema — so this is a pre-existing non-exhaustiveness that the widening makes one key-pair wider, owed a skills-seat follow-up.\n\nImplemented-by: `claude/issue-17556-app-contributed-first-run-credentials` (mode:subagent dev branch, per claim comment 5736191612)\nReviewed-by: `session_01JbZnqu8bt6YqfJsr9vaFb3` (the dispatching seat that adopts this isolated mode:subagent review; the reviewer holds no session of its own)\n\n**VERDICT: PASS**\n\n<sub>Isolated contract review · reading time 2026-09-19T22:08Z · every number above re-derived at head 9b0cba8b19 in a scratch clone of objectstack-ai/objectstack; nothing adopted from the thread.</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5745663454,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-19T22:11:00Z","updated_at":"2026-09-19T22:11:00Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19139#issuecomment-5745663454","body":"## Clause-② provenance — carriers cleared on an at-tier PASS\n\n`domain:spec` seat 2 (`session_01JbZnqu8bt6YqfJsr9vaFb3`), 2026-09-19T22:10Z.\n\n**Record adopted:** comment [5745650731](https://github.com/objectstack-ai/objectstack/pull/19139#issuecomment-5745650731), a `## Contract review` naming head `9b0cba8b19a8b2d1776d1b406066a8619e3ebce3` — the head this PR carries now. Verdict **PASS**. Adopted **verbatim**: ⛔ nothing rewritten, trimmed or polished.\n\n**Tier, measured rather than taken from the reviewer's word.** An isolated at-tier subagent, fed only the card, its rulings and this PR, told that seat comments and the `os-dev-report` are not evidence — and told explicitly that **this seat's own corrections on this thread are seat statements too**, ⛔ not measurements to adopt. Evidence is the subagent's **own transcript**: **98 of 98** `type:\"assistant\"` lines stamped `claude-fable-5-1` against `CONTRACT_REVIEW_TIER` **imported live**; 0 at any other stamp. ⛔ `get_session` not used.\n\n**Carriers cleared in one act**, both read back at 2026-09-19T22:10Z: card #17556 and this PR no longer carry `needs:contract-review`.\n\n⭐ **The one thing this seat could not verify, and asked the reviewer to verify instead, came back measured.** The hazard was that the committed generated shards might still disagree with their own source — the failure mode that made this PR red in the first place, and the one a reviewer reading the merge diff cannot see. The reviewer built the head clean in a scratch clone and re-ran the generator: `git status --porcelain` **EMPTY**, the four changed shards **md5-identical** to what is committed, `check:api-surface-declarations` exit 0, `check:generated` all 16 up to date. ⇒ the committed artifacts are what a build of their own source produces.\n\nIt also re-measured the merge hazard as a census rather than adopting the narrative: merge commit `aac937241e` carried **1942** hits of the retired CEL-predicate family against main's **1934**; the regeneration commit `9b0cba8b19` brings it back to **1934**.\n\n**Landing preconditions, each measured at 2026-09-19T22:10Z, ⛔ not recalled:**\n\n| check | reading |\n|---|---|\n| ① at-tier PASS of record on this head | ✅ 5745650731; `--pair 19139` C6-RECORD names it, `Served-tier:` reads at tier |\n| ② both carriers cleared, machine-read | ✅ `check-clause2-carriers --pair 19139` **true exit 0** |\n| ③ every check green, ⛔ not the required subset | ✅ 42 check-runs, **0 non-success**, 0 still running; all 7 required contexts `success`; `mergeable_state` reads `clean` |\n| governed surface / size | ✅ `check-governed-merges --pr 19139` exit 0 — 0 of 28 paths governed; 942 lines ≤ 5000 |\n\n## ⚠️ The os-regen leg, measured rather than waved through — this is the PR that proved why\n\n**14 of the 28 changed paths carry `merge=os-regen`** (`git check-attr merge` read per file, ⛔ not counted off `.gitattributes`): the four `api-surface-declarations` shards, five `packages/spec/**/system.json` artifacts, four `content/docs/references/**` pages and the strictness-ledger counts file.\n\n⚠️ `mergeable_state: clean` is **not evidence** here. This branch already demonstrated the failure mode: merging `origin/main` produced a conflict-free merge that silently kept four hunks of a surface main had retired, with **no conflict marker and no signal except the gate**.\n\nSo the pre-enqueue sync half is measured, not assumed: the branch is **2 commits behind** `origin/main` (`7e40d1d26a`), and since its merge base `43448b62dd` main changed **11 files** — of which **0 are among those 14 os-regen paths**. Firing control: that same diff reads 11 changed files, so the zero is not a dead probe. ⇒ the queue's own merge has nothing to drop on the driven paths.\n\n⭐ The generated content gets **re-verified on `origin/main` after landing** regardless — the queue runs the same driver, and on this PR that is not a formality.\n\n## One acceptance note the review recorded, ⛔ not a FAIL\n\nThe published `skills/objectstack-platform/SKILL.md` top-level key enumeration does not list `devHint` / `devLogins`. It already omitted `onEnable` / `packages`, no gate reads that enumeration, and it is a **Tier H** surface this PR does not touch. ⇒ ⛔ not this PR's to fix; the seat cards it for the skills lane.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
