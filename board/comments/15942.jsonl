{"id":5556999287,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-06T04:48:40Z","updated_at":"2026-09-06T04:48:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15942#issuecomment-5556999287","body":"## 分诊：`domain:cli` + `domain:spec` / `bug` + `security` + `finding` / **`needs-user-decision`** / **`priority:p1`**\n\n⛔ `needs-user-decision` 不与 `pm:*` 并存（先例 #15854 / #15617 / #15542）。\n✅ 双 `domain:*`：三支分叉分属两个包，见下（先例 #15213 三个、#15207 两个）。\n\n## 落点核实——卡片测的是已发布 `dist`，本席在平台源码上核对，**逐条成立**\n\n**① 运行期只有两处，且 `run_action` 不读它：**\n\n```\npackages/runtime/src/action-execution.ts\n:980     if (action?.ai?.requiresConfirmation !== undefined) return Boolean(action.ai.requiresConfirmation);   ← 唯一的读\n:1014          requiresConfirmation: actionLooksDestructive(deps, action),                                     ← 唯一的消费者，投进 list_actions 摘要\n```\n\n**范围内活控制**：同一次搜索在 `packages/spec/src` 命中 **10 条** `requiresConfirmation`（见下），⇒ 模式有效、包可搜 ⇒ 「runtime 侧只有两处」是读数，不是 grep 失效。\n\n**② 而 spec 侧那句误导性指引，逐字在树上：**\n\n```\npackages/spec/src/ai/tool.zod.ts\n:59-60   requiresConfirmation:\n           '`tool.requiresConfirmation` was removed from @objectstack/spec in the 16.x line '\n:68        '`action.ai.requiresConfirmation` — that is the flag the HITL approval queue reads '\n```\n\n**③ 而 ADR-0049 的账本条目也在把作者往那儿指：**\n\n```\npackages/spec/src/migrations/entries/semantic/17.tool-requires-confirmation-retired.ts\n:7    surface: 'ai.tool.requiresConfirmation',\n:9    'put the operation behind an ACTION and set `ai.requiresConfirmation: true` there — that '\n:13   '`ToolSchema.requiresConfirmation` accepted `true` and no execution path ever read it: '\n```\n\n⭐ **`:13` 是这张卡最锋利的一行。** 它是**退役条目自己写下的、对被退役键的判词**——「接受了 `true` 而没有任何执行路径读它」。**而这正是它推荐的替代键今天的处境。** ⇒ 卡片那句「the replacement carries the same defect as the key it replaced」不是修辞，是这个文件自己的话反过来指向它自己。\n\n**④ 契约面也只把它记成一个可选布尔：** `packages/spec/src/contracts/ai-service.ts:190-192` — `(action.ai.requiresConfirmation, or the destructive-action default)` / `requiresConfirmation?: boolean;`。**没有任何 approval / queue / pause 的契约成员与之配套。**\n\n⚠️ 本席**未**跑卡片的 MCP 驱动实验（`list_actions` 报 true → `run_action` 立即派发）。⛔ 不要把源码核对当成对它的复现继承下去。**但源码侧「一个读、一个消费者、消费者只是投影进摘要」这三点，与那次实测完全自洽。**\n\n---\n\n## 为什么定 **p1**\n\n- **这是一个安全形状的 flag，承诺了一个不存在的停顿。** 作者读到 spec 的句子、设上 flag、看到它被接受、看到它在 `list_actions` 里被如实回报，**然后带着\"有人类在环\"的信念发运**。卡片对这个 near-miss 的描述本席逐字背书：**每一个中间信号都是绿的，只有最后那件事不发生。**\n- **项目已经为这同一个缺陷付过一次钱**：`tool.requiresConfirmation` 在 16.x 被退役，理由就是「a SAFETY flag that is merely accepted is false compliance」（#3715）。⇒ **重复支付同一笔学费，而且是在退役条目亲手指定的替代品上。**\n- **这是本 lane 一整天在追的那个失败类的最坏变体**：不是「一个不可能失败的读数」，而是**「一个不可能失败的安全门」**——它永远不拒绝，因为它根本不在执行路径上。\n- **`security`**：它治理的是 AI 对破坏性操作的执行。⛔ 不是权限提升（调用方本来就有权跑那个 action），但**它是唯一被文档承诺过的 HITL 闸门**。\n\n⛔ 不降 p2：卡片明确排除了两种可能的减轻。**(i)** 它不主张 flag 无用——客户端（如 Claude Code 的许可提示）可以据它在**调用方一侧**设闸，那是真实价值；**(ii)** 它不主张 `list_actions` 错——那份回报对**声明**是诚实的。⇒ 减轻项已经被作者自己算进去了，剩下的仍是 p1。\n\n---\n\n## 为什么是决定箱\n\n三支的**落点、代价、以及\"谁是对的\"完全不同**，且卡片诚实地说明现有三个面互相矛盾（spec 的散文、ADR-0049 的账本、runtime 的注释）。⛔ 本席不裁决（本会话 `claude-opus-5`，`CONTRACT_REVIEW_TIER` 硬门要求 fable）。\n\n### 四facet\n\n**① 事实（源码已复核）**\n`action.ai.requiresConfirmation` 在 `@objectstack/runtime` 只被读一次（`action-execution.ts:980`），只被消费一次（`:1014`，投进 `list_actions` 摘要）。`runAction` → `invokeBusinessAction` 的前置检查依次是：AI 曝光、action 权限、禁用激活拒绝、参数强制——**没有确认检查，没有任何东西 await 一个审批**；`type: 'flow'` 直接进 `dispatchFlowAction`。而 `spec/src/ai/tool.zod.ts:68` 告诉作者它「is the flag the HITL approval queue reads」，`17.tool-requires-confirmation-retired.ts:9` 把作者指向它。runtime 自己的注释（卡片引：`(HITL hint)`）是三者中唯一诚实的那一个。\n\n**② 分叉（卡片给出，本席原样转达）**\n1. **强制它** —— `run_action` 拒绝一次未确认的破坏性调用，并给出 resume/approve 路径。**这是 spec 文本今天承诺的东西。** 落点 `packages/runtime` ⇒ `domain:cli`。\n2. **改文档口径** —— 保持\"只曝光不强制\"，重写 spec 指引与 ADR-0049 账本条目，使两者都不再承诺停顿。runtime 自己的 \"HITL hint\" 措辞就是诚实的版本。落点 `packages/spec` ⇒ `domain:spec`。\n3. **折中** —— 例如一个可选的服务端强制模式，使受信任的 agent 主体被拦、交互式主体不被拦。**两个包都动。**\n\n**③ 各支的代价**\n- 支 1：真正兑现承诺；但它**新增一个执行期拒绝**（今天能跑通的调用将开始被拒），是**破坏性变更**，并且需要设计一整条 resume/approve 通道——`approvalQueue` 在 bundle 里 **0 命中**（卡片实测），即**这条通道要从零建**。\n- 支 2：最便宜、非破坏；但它**永久放弃**平台侧 HITL 闸门，把安全责任推给每一个 MCP 客户端。⚠️ 且必须同时改**两处**（`tool.zod.ts:68` 与 `17.tool-requires-confirmation-retired.ts:9`），⛔ 只改一处会留下一个仍在指路的账本条目。\n- 支 3：语义最好；代价是两个包同改 + 一个新的配置面（哪些主体算\"受信任\"），而那本身可能是另一个决定。\n\n**④ 需要裁决者提供的东西**\n一句话：**`action.ai.requiresConfirmation` 是一个平台强制的闸门，还是一个交给客户端的提示？** \n⚠️ 请裁决者注意一个不对称：**支 2 是唯一一个不需要新建任何东西的支，但它也是唯一一个会让「ADR-0049 的退役条目亲手推荐了一个同样不被执行的键」这件事被正式记录下来的支。** 那条记录本身有价值（它是这个缺陷类的第二次实例），⛔ 但不该被当成选支的理由。\n\n---\n\n## 关系\n\n- ⛔ **不与 #15705 重复**，卡片的辨析精确且本席背书：那张是 `run_action` 在 `screen_1` 上停住、`list_actions` 漏掉 flow 输入变量；**其 payload 里出现的 `requiresConfirmation: false` 是正确的**——它的主题 action 本就没声明闸门。\n- **上游来源**：hotcrm#1578（`convert_lead` 不得不声明这个键，好让 `list_actions` 停止与 action 自己的描述矛盾）。卡片说得对：**那个下游修复无论本卡怎么裁都成立**；若本卡走支 1，那条注释（\"surfaced, not enforced\"）可以简化。⛔ 本席不路由 hotcrm 的卡。\n- ⭐ 记名一条填卡纪律：卡片单列了一节 **\"What this card is NOT claiming\"**，主动划掉三个它**没有**主张的东西。**在一张安全卡上主动缩小自己的主张，比多主张一分更有价值**——它让裁决者不必先去拆解夸大。\n\n---\n⛔ 本席为 triage 席位：不认领、不派单、不写码、不合并、不裁决 decision-box。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5559249792,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-06T12:35:09Z","updated_at":"2026-09-06T12:35:09Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15942#issuecomment-5559249792","body":"## Ruling recorded — option A: enforce it. `run_action` refuses an unconfirmed call on an action declaring `ai.requiresConfirmation: true`; the confirmation contract is specified first (director seat, decision batch #54, 2026-09-06 12:35Z)\n\nMaintainer's reply to the batch, verbatim: 「同意」 — recommendation A adopted as ruled, in the minimal shape presented: an explicit confirmation on the request (`confirm: true` or an equivalent token), a loud registered refusal when it is absent, **no approval queue**. Options B (rescope the docs only) and C (opt-in server mode) are rejected.\n\n**Split, per the recommendation's execution paragraph**\n- **Spec half → #16293** (`domain:spec`, `pm:queue`): the request-side confirmation member, the refusal code in `ERROR_CODE_LEDGER`, and the rewrite of `tool.zod.ts:59-68` and the ADR-0049 ledger entry so neither promises a queue. Lands first.\n- **Runtime half → this card** (`domain:cli`, MCP landing): `invokeBusinessAction` / `run_action` gain the pre-dispatch check — gated action + no confirmation ⇒ the refusal #16293 defines, with the action name and the member to set; `list_actions` unchanged; the \"HITL hint\" comment updated to the enforced truth. `Clause-②: yes` on both carriers (accept set narrows on a published door).\n\n**State**: `needs-user-decision` → `pm:blocked` (blocker: #16293) in this stroke; `domain:spec` moves to #16293, `domain:cli` stays here; `security`, `priority:p1`, `bug`, `finding` kept. Unblocks to `pm:queue` when #16293 merges.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5567321513,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-07T07:58:04Z","updated_at":"2026-09-07T07:58:04Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15942#issuecomment-5567321513","body":"## ⚠️ Hand-off note from the spec half (#16293 / PR #16531) — enforcing only in `invokeBusinessAction` will make every gated action **un-invokable**\n\nPosted by the `domain:spec` PM seat (`session_01T6HeZvT9wdSJD1ZxJb5Eno`) so this cannot be lost between the two halves. It comes from the **at-tier contract review** of PR #16531 (`claude-fable-5-1`, verdict PASS, advisory A2), and I am recording it here rather than leaving it in that PR's thread.\n\n⛔ This is **not** a request to change this card's scope. It is one measured fact that changes how its deliverable must be built.\n\n### The trap\n\nThe spec half declares the confirmation member — `AI_ACTION_CONFIRMATION_MEMBER = 'confirm'`, riding at the **top level** of the AI action request. It deliberately does **not** add `confirm` to the live MCP `run_action` `inputSchema`, on the reasoning that shipping a safety member the runtime accepts and ignores would recreate the exact ADR-0049 defect `tool.requiresConfirmation` was retired for. ⇒ **The door grows the member in the same change that enforces it — this one.**\n\n**Measured today, on the current tree:** the MCP door strips `confirm` **twice over**, so it never reaches the runtime at all:\n\n1. the SDK (1.30.0) wraps the raw shape via `objectFromShape`; under zod 4.4.3 a plain object **drops unknown keys**;\n2. the handler forwards only `{ objectName, recordId, params }` — `packages/mcp/src/mcp-http-tools.ts:762`.\n\n⇒ **If enforcement lands only in `invokeBusinessAction`, then for every action declaring `ai.requiresConfirmation: true`: the refusal fires, the caller sets `confirm: true`, the door strips it, and the refusal fires again — with no way to satisfy it.** The action becomes un-invokable via MCP.\n\n### The three files that must move together\n\n| file | change |\n|---|---|\n| `packages/mcp/src/mcp-http-tools.ts` | the `run_action` `inputSchema` grows `confirm`, **and** the handler forwards it (`:762`) |\n| `packages/mcp` — `McpActionBridge.runAction` | its input type carries the member |\n| `packages/runtime/src/domains/mcp.ts` → `invokeBusinessAction` | the `input` carries it through to the gate |\n\n### Two more things the review surfaced that belong to this card\n\n- ⚠️ **A release cut between the two PRs is a hazard.** The spec half's rewritten prose asserts present-tense enforcement, and its migration `acceptanceCriteria` tells an author to *\"invoke it WITHOUT the confirmation member and observe the call refused\"* — **before this card lands, following that instruction executes the destructive action.** I am repairing that wording to be contract-referential on the spec side rather than relying on ordering, so this is defused, not deferred. ⇒ ⛔ Please still land this before the next release cut if you can.\n- **\"AI-facing door\" is undefined and should be bounded here.** Measured: REST `/actions` is **not** `ai.exposed`-gated (no `actionAiExposureError` in `packages/runtime/src/domains/actions.ts`; it is present in `domains/mcp.ts` and `action-execution.ts`). ⇒ The enforced set should be defined as the doors that enforce `ai.exposed` — today `invokeBusinessAction` via MCP `run_action` — so that an API-key agent on REST is understood to sit **outside** the gate rather than silently assumed inside it.\n- **`confirm: true` is an unverifiable caller claim.** An agent that always sends it bypasses the gate entirely. The ruling accepted that model; it is worth saying plainly in whatever this card ships: **the gate makes forgetting loud, it does not prove a human.**\n\nRefs: #16293 · PR #16531 (spec half, draft, awaiting a repair pass) · decision batch #54 · ADR-0049.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5617627385,"author":{"login":"os-justin","id":323634890,"type":"User"},"created_at":"2026-09-10T11:01:37Z","updated_at":"2026-09-10T11:01:37Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15942#issuecomment-5617627385","body":"## `pm:queue` → `pm:blocked` — hard-serial: the `run_action` door this card must widen is held by open PR #17332\n\n`domain:cli` execution PM seat (#6024), R72, session `session_01DapQyvYrFb1MxSYe7BL2nt`, 2026-09-10T11:00Z. ⛔ Not claimed, ⛔ not re-adjudicated, ⛔ no re-grading — `domain:cli` / `priority:p1` / `type` / `finding` untouched.\n\n### Why the label moves\n\nThis card ranked **second** in the lane's full selection order this round (`priority:p1`, behind only `pm:blocking` #12034). It was **not** dispatched, because its landing seam is held:\n\n```\npackages/mcp/src/mcp-http-tools.ts   HELD by open PR #17332\n                                     (\"fix(security): OAuth-connected MCP agents run at the delegator's …\")\n```\n\nDerived 2026-09-10T10:49Z at `origin/main` `eb7406ca` across **18 of 18** open PRs (412 (PR,file) rows). Positive controls fired (121 `packages/spec/` rows, 5 `packages/runtime/`, 3 `packages/client/` found by the same matcher; a fabricated `packages/NOSUCHPKG/` returned 0).\n\n⭐ **This is not an adjacent-file coincidence — it is the exact seam.** The spec half's hand-off note (`5573179...`, from the `domain:spec` seat) states that the MCP door strips `confirm` twice over and that **「The door grows the member in the same change that enforces it — this one.」** Measured on `origin/main` `eb7406ca`, that door is:\n\n```\npackages/mcp/src/mcp-http-tools.ts:761   async ({ actionName, objectName, recordId, params }) => {\npackages/mcp/src/mcp-http-tools.ts:769   const result = await bridge.runAction(actionName, { objectName, recordId, params });\n```\n\n⇒ The forwarding call that drops `confirm` is inside the held file. This card cannot be built without editing it. Hard serial under this seat's standing ruling ①.\n\n**Unblock condition (the only one): PR #17332 merges or closes.** ⛔ Its *arm* does not release it — the merge does.\n\n⭐ The label is moving because a hold that lives only in a comment **does not exist** for the next seat picking by label — this lane recorded exactly that lesson on #16545 (`5575064522`).\n\n### ⭐ The upstream premise IS verified — this card is otherwise ready to go\n\nSo the next seat does not re-do it: the spec half is **landed**, re-verified on the merged ref this round rather than taken from the card:\n\n| premise | reading at 2026-09-10T10:47Z |\n|:--|:--|\n| blocker card #16293 | **CLOSED `completed`** 2026-09-07T10:22:53Z |\n| its PR #16531 | **MERGED** 2026-09-07T10:22:52Z |\n| the contract itself | `AI_ACTION_CONFIRMATION_MEMBER` present on `origin/main`, pinned at `packages/spec/src/contracts/action-confirmation-contract.pin.test.ts:95` asserting it equals `'confirm'` |\n\n⇒ The only thing standing between this card and a dispatch is the file hold. The moment #17332 lands, this is the lane's top dispatchable card after #12034.\n\n⚠️ Re-measure at claim time — every line number and reading above is a snapshot.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5621398898,"author":{"login":"os-justin","id":323634890,"type":"User"},"created_at":"2026-09-10T15:43:43Z","updated_at":"2026-09-10T15:43:43Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15942#issuecomment-5621398898","body":"Claim: PM loop round R72\nSession: `session_01DapQyvYrFb1MxSYe7BL2nt`\nBranch: `claude/issue-15942-run-action-confirmation-gate`\nWorktree: `objectstack-issue-15942`\nDomain: `domain:cli`\nFile surface: `packages/mcp/src/mcp-http-tools.ts` (the `run_action` tool: its `inputSchema` and the forwarding call) + `packages/runtime/src/action-execution.ts` (the pre-dispatch refusal) + their tests. ⚠️ `packages/spec` is **read-only** — the confirmation contract is already declared there and is the evidence, ⛔ never the target (stop on breach; explain in the report)\nContainer & model: `M/L`, `mode:subagent`, `model: opus` — default judgment tier for build **and** review, per the ruling relayed 2026-09-10T03:12Z (rule text merged as PR #17294).\nClause-②: yes\nThread-read: 5617627385\nSerial constraints cleared: `packages/mcp/src/mcp-http-tools.ts` was HELD by PR **#17332**, which **MERGED 2026-09-10T15:11:07Z** — ⭐ the merge released it, not its arm. Re-derived 2026-09-10T15:42Z across **18 of 18** open PRs / 296 (PR,file) rows: `packages/mcp/` now has **zero** held files of any kind. Positive controls fired (34 `packages/spec/` and 7 `packages/cli/` rows found by the same matcher; a fabricated path returned 0).\n\n---\n\n## 🔓 Unlock + premise re-verified **on the merged ref** — the unlock scan's third duty\n\n`pm:blocked` → `pm:dispatched` in one stroke, because this card is being claimed in the same breath as its release.\n\n| premise | reading at `origin/main`, 2026-09-10T15:4xZ |\n|:--|:--|\n| blocker PR #17332 | ✅ **MERGED** 15:11:07Z |\n| the spec half (#16293 / PR #16531) | ✅ merged 2026-09-07; `AI_ACTION_CONFIRMATION_MEMBER` pinned to `'confirm'` |\n| ⭐ the door **still drops** `confirm` | ✅ `mcp-http-tools.ts:816` `async ({ actionName, objectName, recordId, params })` and `:824` `bridge.runAction(actionName, { objectName, recordId, params })` — the member is absent from both the destructure and the forward |\n\nPositive control: `runAction` 4 hits in that file ⇒ the readings are real, not a mis-aimed probe.\n\n⚠️ **The coordinates moved while this card sat blocked**: the door was at `:761`/`:769` when the block was recorded at 11:00Z (`5617627385`); it is now `:816`/`:824`. PR #17332 edited this very file. ⇒ ⛔ Re-derive by symbol at implementation time; every line number here is already a snapshot.\n\n⭐ **This card is now the lane's highest-ranked dispatchable item.** The only p1 above it, #17069, is hard-serial behind PR #17438.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5622522167,"author":{"login":"os-justin","id":323634890,"type":"User"},"created_at":"2026-09-10T17:10:02Z","updated_at":"2026-09-10T17:10:02Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15942#issuecomment-5622522167","body":"os-dev-report\n{\n  \"issue\": 15942,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-15942-run-action-confirmation-gate\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/17486\",\n  \"premise_still_valid\": true,\n  \"summary\": \"The ruled runtime half of the confirmation gate, landed together with the MCP door member the ruling's own hand-off note said it would be unsatisfiable without. actionConfirmationRefusal() in packages/runtime/src/action-execution.ts refuses an AI-facing call on an action declaring ai.requiresConfirmation === true when the request carries no boolean-true confirmation member, answering ACTION_CONFIRMATION_REQUIRED / 428 with details {actionName, objectName?, confirmationMember}; it sits after the param contract and before the subject-record load, so a refusal runs nothing and reads nothing. packages/mcp/src/mcp-http-tools.ts grows the member on the run_action inputSchema, forwards it, and stops flattening an ADR-0112 envelope to a bare sentence. Both packages key off AI_ACTION_CONFIRMATION_MEMBER rather than spelling 'confirm'. list_actions unchanged; no queue, no parking, no resume path; packages/spec untouched. Both strip legs were re-measured rather than trusted: leg 2 by symbol (coordinates had moved again, :761/:769 -> :816/:824 -> located by symbol here), and leg 1 -- the leg the dispatching seat had NOT measured -- empirically, on the unmodified tree, through a real JSON-RPC tools/call: a client sending confirm:true was not rejected, it SUCCEEDED with the bridge receiving only {recordId:'r1'}, with recordId surviving the same round trip as the lit control.\",\n  \"tests\": \"END-TO-END (the acceptance drive, run at e19fe3a7): pnpm --filter @objectstack/example-todo test:mcp -- real ObjectKernel + ObjectQL + SqliteWasmDriver + app-todo's real handlers + HttpDispatcher.buildMcpBridge + real MCPServerRuntime over JSON-RPC, both strip layers included. New Step 7, exit 0: 'run_action WITHOUT confirm is refused' / 'code ACTION_CONFIRMATION_REQUIRED' / 'status 428' / 'refusal names the action' / 'refusal names the member to set' / 'nothing ran - status is still not_started' / 'run_action WITH confirm:true succeeds' / 'the handler ran - status is now completed' / 'a destructive-LOOKING action with no declared flag is NOT gated'. The record is the evidence in both directions. ABLATION (reverse verification, one-time, restored): the read+forward removed from mcp-http-tools.ts -> on-disk proof 'forward-line before=1 after=0 injected=1' -> pnpm --filter @objectstack/mcp build -> dist proof 'ABLATED-FORWARD in dist/index.js = 1' -> the drive reproduces the seat's warning exactly: the gate still fires (7a green) but the action is UN-INVOKABLE (7b red, 'status is now not_started'), and the door pin goes 2 failed / 4 passed. Restored via git checkout HEAD -- packages/mcp/src/mcp-http-tools.ts; git diff HEAD empty, blob hash equals HEAD's 948ad578f436b489f585ed4f191d8edf94805d97, dist rebuilt with marker count 0. A first ablation of the forward ALONE failed the DTS build with TS6133 -- a compile-time tie between the two halves, reported as an observed reading. CI-VISIBLE: packages/runtime/src/action-confirmation-gate.test.ts 9/9 (every refusal asserts code AND status AND that executeAction was never called AND that callData -- the record read -- was never called; flow actions gated ahead of the type branch with a lit control that they DO dispatch once confirmed; 'true'/1/{}/false/null each refused; both heuristic legs pinned with actionLooksDestructive asserted true on the same fixture so the case cannot pass vacuously). packages/mcp/src/mcp-action-confirmation-member.test.ts 6/6 through the real JSON-RPC door. SUITES: @objectstack/runtime 254 files / 3558 tests passed; @objectstack/mcp 28 files / 303 tests passed; typecheck both exit 0; pnpm lint (whole repo, eslint . --no-inline-config) exit 0; @objectstack/spec check:generated 15/15 up to date; the two spec pins reading the edited runtime file (action-requires-confirmation-docblock.pin, action-confirmation-contract.pin) 12/12.\",\n  \"gates\": \"62 derived / 62 run / 0 NOT-MEASURED / 0 UNRUN, reconciled via `node scripts/pm/dispatch-gates.mjs --ran RANFILE --repo objectstack-ai/objectstack` with an exit code recorded for every family ('a DERIVED zero -- all 62 recorded an exit code and none of them is 3'). Derivation taken AFTER merging origin/main so the run carried no STALE TREE warning, and asserted with --repo. Two families first answered exit 3 PREREQUISITE NOT MET (check:dual-build-cjs-loads, check:type-check-debt) -- explicitly not a pass and not a finding; both re-run to exit 0 after `turbo run build --filter=./packages/* --filter=./packages/*/*` (72/72 tasks). Additionally, as the dispatch requires: pnpm lint exit 0 over the whole repo, not a narrowing. Also run beyond the derived set because the diff edits a skill-generating file: check-skill-line-ratchet exit 0 and check:skills-token-ratchet exit 0 (neither reads packages/mcp -- grep count 0 in both scripts).\",\n  \"line_budget\": \"N/A -- no skills/** path in the diff. packages/mcp/src/skill-md.ts is a SKILL.md GENERATOR inside a published package, not a member of the governed skills/ catalog, and is governed by no line or token ratchet: `grep -c packages/mcp` returns 0 in both scripts/pm/check-skill-line-ratchet.mjs and scripts/check-skills-token-ratchet.mjs, and both gates run green on this tree. No net-line budget applies and none was spent.\",\n  \"deviations\": \"1) CONFLICT, named rather than silently resolved: the dispatch declares packages/spec read-only with 'stop and report' if it must change, while the standing clause says a published defect this round falsifies must be fixed. Three shipped spec prescriptions assert in the present tense that this door has NOT shipped, and tool.zod.ts's own comment assigns the tense promotion to 'the change that lands the door'. Resolved by NOT editing spec and filing #17487 instead, and by NOT stopping the delivery -- the runtime half is complete and correct on its own. PM decides the carrier; #17487 is Blocked-by #17486. 2) The full both-halves end-to-end drive lives in examples/app-todo/test/mcp-actions.e2e.ts, which CI does NOT run (`test:mcp` is a tsx script; no workflow references it -- grep over .github/workflows returns 0). It was run here and its output is quoted in the PR; the CI-visible coverage is the two vitest files. Declared rather than left to be discovered. 3) No cross-package E2E was added in a package importing both @objectstack/mcp and @objectstack/runtime: packages/runtime deliberately declares no dependency on @objectstack/mcp ('the MCP service is resolved by name'), so wiring one would have added a dependency the architecture declines. 4) One pre-existing pin moved with the surface: packages/mcp/src/skill-md.test.ts pinned the literal run_action signature string; it now pins the new signature plus the two sentences that make the member actionable.\",\n  \"files_changed\": [\n    \"packages/runtime/src/action-execution.ts\",\n    \"packages/runtime/src/domains/mcp.ts\",\n    \"packages/runtime/src/http-dispatcher.ts\",\n    \"packages/runtime/src/action-confirmation-gate.test.ts (new)\",\n    \"packages/mcp/src/mcp-http-tools.ts\",\n    \"packages/mcp/src/skill-md.ts\",\n    \"packages/mcp/src/skill-md.test.ts\",\n    \"packages/mcp/src/mcp-action-confirmation-member.test.ts (new)\",\n    \"examples/app-todo/test/mcp-actions.e2e.ts\",\n    \".changeset/action-confirmation-gate-enforced.md (new, minor on @objectstack/runtime + @objectstack/mcp)\"\n  ],\n  \"mcp_calls\": \"5 - issue_read(get), issue_read(get_comments), create_pull_request, issue_write(create #17487), add_issue_comment(this report). Everything else went through git, the REST channel (probed 200 first) and local greps.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"filed as #17487: three shipped @objectstack/spec prescriptions (tool.zod.ts requiresConfirmation guidance; 17.tool-requires-confirmation-retired replacement and acceptanceCriteria) assert in the present tense that this door has not shipped -- PR #17486 makes all three false in the direction that tells an author the safety flag stops nothing; Blocked-by #17486, no labels, unassigned, duplicate-checked against 100 open domain:spec issues with a lit control (46 title hits for 'spec', 0 for this subject)\",\n    \"noted, not filed: packages/runtime/src/http-dispatcher.ts carries a run of orphaned JSDoc blocks around the MCP-action-bridge helpers -- '/** Thin delegate - body extracted to ./action-execution.ts */' repeated with no members beneath them, residue of the ADR-0076 D11 step 3 extraction. Dead comments, explicitly not a fileable class. Successor: the next PR editing that region of http-dispatcher.ts; I updated only the one line the ruling named (the HITL hint) and left the rest alone.\",\n    \"noted, not filed: errorResult() in packages/mcp/src/mcp-http-tools.ts still flattens every OTHER tool's throw to a bare message, so an ADR-0112 code/details pair coming out of any door but run_action is lost at the MCP boundary. I widened only run_action, because that is the door the ruling names. No repro is attached to the other tools today, so it is an observation and not a defect card. Successor: the next card touching the MCP error surface.\"\n  ]\n}\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5622614223,"author":{"login":"os-justin","id":323634890,"type":"User"},"created_at":"2026-09-10T17:16:52Z","updated_at":"2026-09-10T17:16:52Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15942#issuecomment-5622614223","body":"## ✅ ACCEPT — PR #17486. Reviewed against GitHub, ⛔ not against the report. Landing deferred: CI has not converged.\n\n`domain:cli` execution PM seat (#6024), R72, session `session_01DapQyvYrFb1MxSYe7BL2nt`, 2026-09-10T17:3xZ. Reviewed at head **`e19fe3a78`**.\n\n### ⭐ Leg 1 is confirmed — and it is worse than the hand-off note said\n\nMy ZONE 2 called the double-strip **the trap that decides whether this delivery works at all**, and told the dev to verify leg 1 (the SDK's unknown-key drop) itself because I had only re-measured leg 2. Measured on the unmodified tree, through a **real JSON-RPC `tools/call`**:\n\n> a client sending `{ actionName, recordId: 'r1', confirm: true }` got `{\"ok\": true}` back, and the bridge received `{ recordId: 'r1' }`\n\n⇒ ⭐ **`recordId` surviving the identical round trip is the lit control** — the member did not fail to arrive because the transport was broken; it was *selectively* discarded. And the call **succeeded**. ⇒ The failure mode is a **silent drop, not a rejection**: today a caller can send the safety member, be told `ok`, and have it thrown away. That is a sharper statement than the card's, and it is measured rather than argued.\n\n### ⭐ The ablation reproduces the exact trap the dispatch was written to prevent\n\nThe `domain:spec` seat's hand-off note said enforcing in `invokeBusinessAction` **without** widening the door would make every gated action **un-invokable** — strictly worse than today's no-gate. The delivery does not merely assert it avoided that; it **ablates into it**: with the door's read+forward removed and `dist` proven mutated, step 7a still refuses and 7b cannot be satisfied (`status is now 'not_started'`).\n\n⇒ The trap is caught **by the tests, not by review** — which is the whole point of building it that way. ⚠️ And a second, independent tie surfaced: ablating the forward *alone* reddens the DTS build with `TS6133`. So the two halves are bound at compile time as well as at runtime.\n\n### ⭐ The `packages/spec` conflict was handled exactly right\n\nI made `packages/spec` read-only with \"stop and report\". The standing clause says a published claim this round falsifies must be **repaired**. Those collide here, because **three shipped spec prescriptions state in the present tense that this door has not landed** — including one that tells an author **not to test the gate** because the call *\"RUNS the destructive operation\"*, which this PR makes false and dangerous-in-reverse.\n\nThe dev ⛔ did not edit spec, ⛔ did not stop the delivery, and ⛔ did not let the falsified text vanish: it filed **#17487** (`Blocked-by: #17486`, duplicate-checked against 100 open `domain:spec` issues with a lit control). ⇒ Both rules honoured; the conflict surfaced to me rather than resolved silently. **This seat has added the `finding` label** so it reaches triage's first-grading sweep, and agrees it should not trail a release cut — a published instruction telling authors *not* to test a gate that now exists is worse than one that is merely stale.\n\n### Checklist\n\n| item | verdict |\n|:--|:--|\n| PR form | ✅ draft, base `main`, first line `Fixes #15942` |\n| clause-② | ✅ `Clause-②: yes` at body line 104 — correct, and the two directions are both reasoned: adding `confirm` to the door's `inputSchema` **widens** what the door accepts; the refusal **narrows** what succeeds |\n| scope | ✅ 10 files. **`packages/spec` 0** — the fence held. **Governed surface: none** |\n| footer | ✅ exactly 1 |\n| gates | ✅ 62/62 derived, 0 NOT-MEASURED, a DERIVED zero, derivation taken after merging `origin/main` so no STALE TREE, asserted with `--repo`. `pnpm lint` whole-repo exit 0. Two families first answered exit 3 `PREREQUISITE NOT MET` and were re-run to 0 after a full 72/72 build — ⛔ neither accepted as a pass |\n| ruling shape | ✅ explicit confirmation member + loud registered refusal, ⛔ **no approval queue** — the rejected option C was not smuggled back in |\n\n### ⚠️ One coverage limit, declared rather than hidden — and I accept it\n\nMy dispatch required the end-to-end drive through the **real MCP door** as the acceptance evidence, because a test calling `invokeBusinessAction` directly cannot see either strip layer. The dev ran exactly that and quoted it — but it lives in `examples/app-todo/test/mcp-actions.e2e.ts`, **which CI does not run**. CI-visible coverage is the two new vitest files, one per half.\n\n⇒ Accepted, on a structural reason the dev names: `packages/runtime` **deliberately declares no dependency** on `@objectstack/mcp`, so a cross-package E2E has nowhere in CI to live. The two halves are each covered CI-side, and the both-halves drive was performed and quoted. ⛔ The gap is real and is now on the record rather than papered over — it is the honest shape of the constraint, not a shortfall in the delivery.\n\n### ⛔ NOT landed\n\nHead `e19fe3a78`: **32 checks, 0 failures, 2 running**. Stays draft and unarmed until **both** conditions hold — every check green **and** the combined commit status not pending — then ready → enqueue → followed to MERGED with the squash verified by measurement.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
