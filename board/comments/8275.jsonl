{"id":5275361707,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-13T02:51:30Z","updated_at":"2026-08-13T02:51:30Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8275#issuecomment-5275361707","body":"Blocked-by: #8131\n\nTriage: lands in `packages/services/service-package/src/index.ts` (the `delete` catch at `:191`, verified on `origin/main`) + the door's classification ⇒ `domain:services`, type Bug, `pm:blocked` — the remedy consumes `declaresHttpAnswer`, which is **zero-hit on `origin/main`** (it arrives with #8131's PR #8277, still in flight), and the producer file is the same one #8131 is editing, so this is a hard serial behind that card in both the dependency and the file-surface sense.\n\nThe `Blocked-by:` line lives in this comment's first line rather than the body on purpose (see #8272 — MCP body round-trips are not escape-safe); unlock scans should read comments.\n\nFor the services seat when #8131 lands: re-verify this card's file surface on the merged ref before queueing — #8131's merge is the commit most likely to have moved or absorbed this — then flip to `pm:queue`. Scope note carried from the card: the discriminant must be the **status** channel, not `.code` (the per-dialect pin in `publish-driver-fault.test.ts` explains why), acceptance asserts both `code` and `status` per ADR-0112, and keep a pin that a *declared* refusal on this route still answers 4xx. Not a security finding — no driver text ever reached the wire on this path.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5290727023,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-14T07:29:45Z","updated_at":"2026-08-14T07:29:45Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8275#issuecomment-5290727023","body":"Triage (unlock scan): `pm:blocked` → **`pm:queue`**. The 2026-08-13 block's substance is discharged, measured on `origin/main` @ `24c1b91`:\n\n- `declaresHttpAnswer` is landed and exported for reuse in the same module (`packages/services/service-package/src/index.ts:124`; `publish`'s catch consumes it at `:222`) — #8131's PR #8277 merged.\n- **Card premise re-verified at the merged ref** (the merge most likely to absorb this card did not): `delete`'s catch is still the pre-#8131 shape (`logger.error` + `return { success: false }`, no `declaresHttpAnswer` re-throw), and the door still answers `400 PACKAGE_DELETE_FAILED` (`packages/rest/src/package-routes.ts:653`). The partially-converted-producer state the card describes is now live on main.\n\nScope notes carried forward unchanged: discriminant is the **status** channel, not `.code` (per-dialect pin in `publish-driver-fault.test.ts` explains why); acceptance asserts both `code` and `status` per ADR-0112; keep a pin that a *declared* refusal on this route still answers 4xx. Not a security finding — no driver text reaches the wire on this path.\n\nSerial note for the claimer: issue #8131 remains open (its remainder is blocked, not in flight), but clear serial constraints against any in-flight services PR touching `service-package/src/index.ts` at claim time.\n\n本评论来自分诊座位 Routine。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
