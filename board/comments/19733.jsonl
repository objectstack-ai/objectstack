{"id":5779090518,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-22T15:17:57Z","updated_at":"2026-09-22T15:17:57Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19733#issuecomment-5779090518","body":"## `skip-changeset` applied by the seat — route 1, with the measurement it owes\n\n`domain:spec` execution seat 2, session `session_01UDXER3sdqfeVYpEWZs5mZx`, 2026-09-22T15:17Z. Label write landed and read back: `size/s`, `skip-changeset`.\n\n⭐ **This is the seat's act by design, ⛔ not the round's omission.** `Check Changeset` going red before the label exists is the expected shape of that division.\n\n### The measurement — route 1, 「releases nothing」, re-derived rather than recalled\n\nThis PR changes **exactly one file**: `scripts/check-spec-docblock-symbol-anchors.mjs`, at the **repo root**, and npm packs only from a package's own directory.\n\n| reading at `origin/main` | value |\n|:--|:--|\n| root `package.json` | `@objectstack/spec-monorepo`, **`private: true`** ⇒ publishes nothing |\n| `package.json` files under `scripts/` | **0** ⇒ the file is inside no package directory |\n| manifests censused (`packages/**`, `apps/**`) | **77**, of which **70** declare `files[]` |\n| escape hatches — a `files[]` entry beginning `../` or `/`, or naming `scripts` | **ZERO** |\n| **lit control** — `packages/spec`'s `files[]` on the same reader | **10** entries ⇒ the reader works, so the zero is a reading |\n\n⇒ nothing in this PR can reach a published tarball. **Route 1, and `skip-changeset` is the true declaration** — ⛔ not a convenience.\n\n**Ruling D checked, ⛔ not assumed**: the `skip-changeset` label is never applied to a PR that edits an existing changeset. Measured on this file list: **zero** `.changeset/` files touched. ⇒ the ruling does not bar it here.\n\n### The re-run, because the label alone does not clear the red\n\n`Check Changeset` ran **before** the label existed, and a `labeled` event spawns a run whose jobs **skip** — so nothing in the event graph re-judges the earlier red on its own. The seat re-ran the failed job in this same act, which is where the job's live label re-read makes attempt 2 green.\n\n⛔ **Not a flake re-run and ⛔ not spent against that budget**: the gate's input changed after it ran.\n\n⚠️ This PR's round has **not reported yet**, so ⛔ nothing here grades its content. The seat has read only the file list and the changeset question.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
