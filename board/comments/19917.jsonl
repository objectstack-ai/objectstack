{"id":5800465675,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T18:21:40Z","updated_at":"2026-09-23T18:21:40Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19917#issuecomment-5800465675","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**3** anchor(s) derived from **1** changed package(s); no hand-written page names any of them, so this run has **nothing to list** — **not a clean bill of health**. This check sees only pages that NAME a derived anchor: one that documents this change in prose, or enumerates it in an authoring dialect, names none and stays invisible to it on every run.\n\n<details><summary>What this run could not see</summary>\n\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **136** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 8490127962834a125239ae85d9e6a88c0386896c` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `c75f07bd1c3ab29149e3cc1f0fa105fe8b0cfcb7` — the merge of head `7a30204ebf325b2aec731826cfce402b5fa13a00` into base `8490127962834a125239ae85d9e6a88c0386896c`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin c75f07bd1c3ab29149e3cc1f0fa105fe8b0cfcb7 && git checkout c75f07bd1c3ab29149e3cc1f0fa105fe8b0cfcb7\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 8490127962834a125239ae85d9e6a88c0386896c 7a30204ebf325b2aec731826cfce402b5fa13a00 && git checkout -B drift-repro 8490127962834a125239ae85d9e6a88c0386896c && git merge --no-ff 7a30204ebf325b2aec731826cfce402b5fa13a00\n\nnode scripts/docs-audit/affected-docs.mjs --json 8490127962834a125239ae85d9e6a88c0386896c\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5808356148,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T05:37:47Z","updated_at":"2026-09-24T05:37:47Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19917#issuecomment-5808356148","body":"## Contract review\n\nServed-tier: 132/132 `CONTRACT_REVIEW_TIER`\nHead-sha: `7a30204ebf325b2aec731826cfce402b5fa13a00`\n\nIsolated at-tier reviewer subagent, run by the `domain:spec` seat-4 session on the maintainer's instruction in that session (「帮我处理」, with the landing route chosen there); every one of its 132 transcript turns served at the tier the constant names. Adopted by the seat 2026-09-24T05:37Z. The record below is the reviewer's, unedited except the two header lines.\n\n\nScope of what was read: card #17507 body + all 5 comments; `pulls/19917` body, files, diff (Accept application/vnd.github.diff), issue comments (1, docs-drift bot), review comments (0), reviews (0); `commits/7a30204ebf325b2aec731826cfce402b5fa13a00/check-runs` = 35 total, 32 success, 3 skipped (Build Docs, Console Pin Gate, Packed-tarball smoke opt-in), 0 failed; `origin/main` at `c1641868a3da71537c9c6c572d2bd2044103236b`; PR ref fetched to `refs/review/pr-19917` = `7a30204ebf325b2aec731826cfce402b5fa13a00`; AGENTS.md at origin/main. Merge base `44ce049a8c52b533f8a4ae195c0dca697309e400` (matches the PR body's stated base). Diff = 3 files, +59/-43: `.changeset/17507-view-repeater-row-titles.md` (added), `packages/spec/src/kernel/repeater-item-titles.test.ts`, `packages/spec/src/ui/view.zod.ts`.\n\n### ① Derived judgments\n\n1. Accept set: UNCHANGED, right. Every hunk in `view.zod.ts` appends `.meta({ title })` to an existing key chain and adds no key, enum member, export or registration: `ListColumnSchema` 14 keys (refs/review/pr-19917:packages/spec/src/ui/view.zod.ts:1018-1046), `ViewTabSchema` 9 keys (:1673-1681), the list arm's inline sort entry 2 keys (:2716-2717). None of the widening-tells shapes T1-T4 (origin/main:scripts/pm/check-widening-tells.mjs:46-58) appears. CI corroborates: `check:authorable-surface` (lint.yml:5543) and `check:docs` (lint.yml:5562) run inside job `Type Check · source gates`, `check:api-surface` (lint.yml:6509) inside `Type Check · consumer gates`; both jobs success at head. `git diff --check origin/main...refs/review/pr-19917` exit 0.\n\n2. Published JSON Schema: GAINS annotation-only `title` nodes, right (it is the card's deliverable). `packages/spec/json-schema/` is gitignored (origin/main:.gitignore:63) but published (origin/main:packages/spec/package.json `files[]` lists `json-schema`), so `ui/ListColumn.json`, `ui/ViewTab.json` and every embedding schema gain `\"title\"` keys. `ViewTabSchema` is also reused by `UserFiltersSchema` (origin/main:packages/spec/src/ui/view.zod.ts:1802), which `InterfacePageConfigSchema` embeds (origin/main:packages/spec/src/ui/page.zod.ts:591), so the PR body's `InterfacePageConfig.json` example is substantiated by the schema graph. JSON Schema `title` changes no validation outcome. No consumer test on main pins an absent title on these carriers: `packages/rest/src/meta-types-schema-titles.test.ts` and `packages/metadata-protocol/src/protocol.meta-types-unauthorable-columns.test.ts` pin dashboard rows only (grep for `view|columns|tabs` returns no view assertions).\n\n3. Generated artefacts: NONE MOVED, right. The reference-doc generator emits a page-level title only (origin/main:packages/spec/scripts/build-docs.ts:469 `title: ${zodTitle}`) and union-variant titles (scripts/lib/schema-section.ts:587); no property-level `title`. `build-schemas.ts` has one `title` at :3743 (the OpenAPI document title). `build-api-surface.ts` has none. `i18n-extract.ts` keys `metadataForms.type.fields` off declared children only (origin/main:packages/cli/src/utils/i18n-extract.ts:1842-1847 walk `field.fields`), and `view.form.ts` declares `columns`/`sort`/`tabs` as repeaters with no children (origin/main:packages/spec/src/ui/view.form.ts:40, :42, :132), so no bundle key is emitted; `Check generated translation bundles` (lint.yml:6751, `pnpm check:i18n`) is inside a green job. Main's 18 post-base commits touch none of the PR's paths or spec's generated dirs: `git diff --stat 44ce049a8c origin/main -- packages/spec/src/ui/view.zod.ts packages/spec/src/kernel/repeater-item-titles.test.ts packages/spec/src/ui/view.form.ts packages/spec/authorable-surface packages/spec/api-surface content/docs/references/ui` prints nothing.\n\n4. Ledger: SHRUNK, NOT WEAKENED, right. `LEDGER` becomes `new Set([])` (refs/review/pr-19917:packages/spec/src/kernel/repeater-item-titles.test.ts:97). The load-bearing arm survives unchanged: every carrier outside the set must have `untitled` equal to `[]` (:330-335), and the derivation still keeps union arms separate (:174-191, trap 1) and reads the property node before its `$ref` (:262, the `$defs` sibling trap). The PR's firing control (delete one `.meta({ title: 'Display Order' })`) reports RED `view:tabs ... expected [ 'order' ] to deeply equal []`, which is the exact shape that arm produces, so the pin is weight-bearing after the change.\n\n5. Titles are the repo's convention, right. `.describe(...).meta({ title: 'Title Case' })` counts 66 on main vs 0 for the reverse order (`git grep -c` over `packages/spec/src/**/*.zod.ts`); the paid carriers use it (origin/main:packages/spec/src/data/field.zod.ts:338-361, ui/dashboard.zod.ts:809-818). The inline sort titles `Field`/`Direction` mirror `SortItemSchema` (origin/main:packages/spec/src/shared/enums.zod.ts:28-29). Both card traps are handled: the inline `{ field, order }` entry is titled itself (:2716-2717 at the PR ref), not the shared schema.\n\n6. Card mechanism statement corrected accurately: the card calls the ledger \"exact ... count\"; the PR body says it records membership only. The test code agrees (a `ReadonlySet` of ids, `untitled.length` greater than 0 for a listed carrier at :324-327). Right.\n\n7. Follow-up recorded, not lost: PR body Acceptance notes state \"The titles are English in every locale for now ... The follow-on is to enumerate these children in `view.form.ts` with translated leaves. That is outside this card's file surface\", and the os-dev-report `out_of_scope_findings` carries the same with dedupe words. The identical shape for the other fifteen repeaters shipped as `feat` under #17508 (origin/main commit `1a2bb9ec2`, changeset `17508-repeater-row-property-localisation.md`, `@objectstack/spec: minor` + `@objectstack/platform-objects: minor`), and the platform-objects pin scopes `view` out by name (origin/main:packages/platform-objects/src/apps/translations/repeater-row-properties.test.ts:31-32, :147-149 \"their titles are #17507's\"), so a separate card is the right vehicle. No card is filed yet; see ③.\n\n### ② Semver level\n\nChangeset `@objectstack/spec: patch`, `Clause-②: no` in both the PR body (line-start) and the changeset body. Correct. The Clause-② criterion is \"does this card widen the accept set or enlarge the public surface\" (origin/main:.claude/skills/pm-dispatch/references/execution-duties.md:65); neither moves (①.1), and contract-review.md:11 states shipped bytes that are not the published accept set do not trigger `yes`. AGENTS.md:1081-1085 (Post-Task Checklist step 3): a bug fix in a released package takes `patch`; `yes` takes at least `minor`. `Check Changeset` is success at head, and `check-changeset-no-major.mjs` (origin/main:scripts/check-changeset-no-major.mjs:934-935) only forces `minor` on a `yes` declaration, so `no`+`patch` is internally consistent. Precedent is split and the PR says so honestly: #17505 (origin/main `97233b90c`, `.changeset/17505-dashboard-repeater-row-titles.md`) took `minor` with \"`Clause-②: yes` ... each row property gains a `title` node in the emitted JSON Schema\"; #17506 (origin/main `c1997724a`, `.changeset/17506-select-option-row-titles.md`) took `patch` / `no` for the same shape and landed later. Under the written criterion, #17505 is the outlier; open question option A stands. No `skip-changeset` (published `dist` and `json-schema` bytes move).\n\n### ③ Boundary flags\n\n- Files outside the claim: NONE. Claim surface = `view.zod.ts` three regions, the ledger test, regenerated files, `.changeset/`. Diff = exactly those three files; in `view.zod.ts` only the three hunks at base lines 1015, 1664, 2706 move.\n- Non-blocking: follow-up card not yet filed. AGENTS.md:213-214 routes non-defect notes to acceptance notes, which is where it is; the maintainer's stated plan is a localisation card after merge. Whoever lands this should file it in the same round so the English-in-every-locale state on `view.columns` / `view.sort` / `view.tabs` has an owner; dedupe words are in the os-dev-report.\n- Non-blocking: the two inline sort properties are the only `.meta({ title })` in spec src with no `.describe()` (grep `z.(string|enum|number|boolean)(...).meta({ title` on main = 0 hits). Adding `SortItemSchema`'s descriptions would render into `content/docs/references/ui/view.mdx` (descriptions do render) and exceed the card, so leaving them bare is the narrow choice; a later touch can mirror enums.zod.ts:28-29.\n- Non-blocking: PR is `draft=true` and unlabelled for queue; `packages/spec/src/**` non-test is a governed surface (contract-review.md:6), so it waits for this record. Landing seat flips draft.\n- Non-blocking: PR body says main moved 4 commits past base; it is now 18 (`git log --oneline 44ce049a8c..origin/main | wc -l` = 18). No overlap (①.3), `mergeable_state=clean`, CI ran on merge ref `c75f07bd1c3ab29149e3cc1f0fa105fe8b0cfcb7` per the docs-drift comment. Stale count, not false prose at write time.\n- Non-blocking: with an empty set, the ledger's in-ledger arm (:321-329) and \"the ledger names only carriers that exist\" (:339-344) are vacuous. That is the declared steady state (:84-86); the header at :33-36 was reworded to past tense accurately.\n- Non-blocking: `check-widening-tells` runs only in dispatch gates (origin/main:scripts/pm/dispatch-gates.mjs), not in any workflow; the dev reports 82 families run. Diff inspection independently finds no tell shape.\n- Pins not weight-bearing: none found. Published prose made false: none found (ledger header, platform-objects pin comment, `translation.zod.ts:1222-1225` statement all remain true).\n\nImplemented-by: `claude/issue-17507-view-repeater-row-titles`\nReviewed-by: `session_019c3Hi6ZMU1p6m6aA6Bz45d`\n\n**VERDICT: PASS**\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
