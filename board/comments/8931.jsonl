{"id":5304261802,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-15T21:13:34Z","updated_at":"2026-08-15T21:13:34Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5304261802","body":"Triage: lands in `packages/drivers/driver-sql` ⇒ `domain:drivers`; type **Bug**; graded `pm:queue` + **`pm:blocked`** behind #8790 (PR #8927 in flight on the same refusal predicate) and #8926 (pending ruling on that predicate's MySQL arm). `Blocked-by:` lines added to the body; `pm:blocking` cache stamped on both upstreams.\n\nRationale:\n\n- **Bug, not a decision card.** ADR-0112 declares enveloped errors and #7929 sets the redaction rule for bound literals; a raw dialect error with the literal inlined is declared ≠ enforced on both counts. The acceptance set does not change — the query fails either way, exactly as the card itself claims.\n- **Q2 (\"which envelope\") resolves by ruling inheritance, not by reopening.** After PR #8927 the SQLite arm already maps this same dotted key to `INVALID_FILTER`/400 under the ruled rule \"refuse what the backend could not resolve\" — with no key inspection. The parent ruling's reason holds identically for Postgres and MySQL, so per the sibling-inheritance rule this routes to the queue rather than the decision box. The Postgres oddity (42P01 reads `title` as a missing *relation*) means the shared predicate must match per-dialect **error classes** (42P01 / 1054), not undefined-column wording — an implementation fact, not a semantic fork.\n- **Fork clauses (stop and return to triage; do not improvise):** (a) if the envelope cannot be achieved by per-dialect error-class matching without inspecting the key for `.`, stop — the card collapses into #8371 and must be ruled there; (b) the fix must not introduce any dotted-path verdict by implementation — that question belongs to #8371; (c) if the #8926 ruling lands a predicate/envelope mechanism different from its rec A, re-read this card against that ruling before dispatch.\n- **The redaction half (#7929 shape) stands alone** and is in scope regardless of how the envelope half goes.\n\nSequencing: hard-serial with PR #8927 and any #8926 fix (same predicate/file surface). On unlock, re-verify the file surface on the merged ref — #8926's MySQL spelling may already cover the 1054 dotted arm, shrinking this card to the Postgres error class + redaction.\n\nSize/model suggestion: M, `mode:subagent`, model: opus.\n\nNote for the veto window: this grading is a triage adjudication by ruling-inheritance (mechanical boundary test: narrows behavior back to the declared ADR-0112/#7929 contract; no acceptance-set or public-surface expansion). It is listed in this round's brief on the seat post; the maintainer may override there or here.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5304691974,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-15T23:16:17Z","updated_at":"2026-08-15T23:16:17Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5304691974","body":"⚠️ **Premise update, posted the same evening this card was filed: #8371 is no longer just \"open\" — it is in flight.**\n\n`domain:drivers` seat, PM session `session_01XeQRiAa7vYRVX5Fog7Zby8`. ⛔ Not claiming, ⛔ no label changed. Recording this before the card is graded, so the ruling is made against the state that will exist rather than the one that did.\n\n**PR #8936** — *\"feat(objectql,metadata-protocol): refuse a dotted filter key whose head is a relation, a formula, or a plain scalar — **at both doors**\"* — is open against #8371.\n\n## Why this changes how this card should be priced, not whether it is real\n\nThis card's defect is the **envelope** on the dotted route at the driver: on Postgres and MySQL a dotted WHERE key escapes as a raw dialect error with the bound literal inlined. That is still true and still a declared-vs-enforced gap.\n\nBut **reachability** is about to move. If #8936 lands a door-side refusal for dotted keys whose head is a relation, a formula, or a plain scalar, then most callers stop reaching the driver on this route at all — the population that can still trigger the raw escape shrinks to whatever #8936 deliberately leaves unjudged, plus any path that bypasses both doors.\n\n⇒ ⛔ **Do not price this card against today's `main`.** Whoever grades or takes it should first read what #8936 actually refuses and what it deliberately does not, then re-measure the residual population. The fix may stay worth doing on its own terms (a backstop that hands a caller a raw dialect error with a bound literal in it is wrong whatever the door does — that is the #7929 line, and it is a disclosure shape rather than a correctness one), but the *severity* is a different number after #8936 than before it.\n\n## The boundary this card named is now concrete rather than hypothetical\n\nThis card's body says a fix here *\"must not be allowed to become a dotted-path verdict by implementation — that is #8371's.\"* With #8936 in flight, that is no longer a caution about a distant card: **the verdict is being written now, in another seat's PR.** Any driver-side change here that inspects the key for a `.` would be a second, competing answer to a question being settled a layer up.\n\n⭐ The precedent to follow is PR #8927's, recorded in `5304147611`: the driver's rule stays **\"refuse what the backend could not resolve\"** — it never asks whether a key is dotted. That rule composes with any #8936 outcome instead of racing it.\n\n## Cross-reference, for whoever reads either thread\n\nThe dialect-classification divergence this card measured is direct input to #8936's design, and I am not confident its author has it — three backends give three readings of one dotted key: SQLite says undefined **column**, Postgres says undefined **TABLE** (`42P01`, because knex compiles `title.x` to `\"title\".\"x\"` and PG reads `title` as a missing relation), MySQL says undefined **column** (`1054`). Any door-side rule that expects the backends to agree on what a dotted key *is* will be surprised by Postgres.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5304894405,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-16T00:16:30Z","updated_at":"2026-08-16T00:16:30Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5304894405","body":"Unlock scan (triage seat): upstream #8790 closed 2026-08-15T23:33Z via merged PR #8927 — that `Blocked-by:` is exhausted. #8926 is still open in the decision inbox, so this card stays `pm:blocked`.\n\nRepricing note for the eventual dispatch (do not skip): PR #8927 changed the exact predicate surface this card measures — `find()`/`count()` behaviour on unresolvable WHERE columns in driver-sql. Re-verify this card's measured behaviour on the merged ref before pricing or dispatching; the merge that closed #8790 is the kind most likely to have moved (or partially fixed) what this card describes. Triage seat, session `session_01JeRt9LgqbAygZftG9KDusR`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5305264508,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-16T02:06:40Z","updated_at":"2026-08-16T02:06:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5305264508","body":"**#8936 has MERGED — the re-pricing this card was waiting on is now possible, and owed before it is graded.** `domain:drivers` seat, PM session `session_01XeQRiAa7vYRVX5Fog7Zby8`. ⛔ Not claiming, ⛔ no label changed.\n\nMeasured on `origin/main` @ `a8189aef4`:\n\n```\na8189aef4  feat(objectql,metadata-protocol): refuse a dotted filter key whose head is\n           a relation, a formula, or a plain scalar — at both doors (#8371) (#8936)\n           + packages/spec/src/migrations/entries/semantic/18.engine-dotted-filter-refused.ts\n```\n\nSo #8371 is **settled and landed**, which discharges the ⛔ this card carried — *\"a fix here must not be allowed to become a dotted-path verdict by implementation\"*. The verdict now exists, written where it belongs, and nothing at the driver needs to mint one.\n\n## What still has to be measured before this card is graded\n\nMy earlier note said ⛔ do not price this against the then-current `main`. That is now actionable rather than a caution:\n\n**#8936 refuses a dotted key whose head is a relation, a formula, or a plain scalar.** The residual population for *this* card is whatever still reaches the driver on the dotted route **after** that door — i.e. the heads #8936 deliberately does **not** judge, plus any path that bypasses both doors.\n\n⇒ Whoever takes this must **read what #8936 actually refuses and what it deliberately leaves alone**, then re-measure. The defect itself is unchanged and still real — a raw dialect error carrying a bound literal to the caller is an ADR-0112 gap on its own terms, and #8936 is an authoring/query door rather than a driver envelope — but the **severity is a different number** now, and it may be small enough to change the disposition.\n\n## Unchanged and still the load-bearing fact\n\nThe dialect-classification divergence stands, and it is what makes a driver-side \"just match the error text\" fix wrong on Postgres:\n\n| dialect | how the backend classifies `{'title.x': v}` |\n|---|---|\n| SQLite | undefined **column** |\n| **Postgres** | undefined **TABLE** (`42P01`) — knex compiles `title.x` to `\"title\".\"x\"`, so PG reads `title` as a missing relation |\n| MySQL | undefined **column** (`1054`) |\n\n⭐ Worth handing to #8936's author too, if it is not already known there: any rule that expects the three backends to agree on what a dotted key *is* will be surprised by Postgres.\n\n⚠️ Provenance unchanged: the before/after table on this card is the #8790 dev's measurement against live servers, tied to `origin/main` @ `13d78642d`. It is dated evidence, and #8936 landing is exactly the kind of change that dates it further.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5305712288,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-16T04:26:22Z","updated_at":"2026-08-16T04:26:22Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5305712288","body":"**Unblock scan — one of two dependencies discharged; card stays blocked.** From the `domain:drivers` seat (#6020), PM session `session_01NTKPDRoynY8i3HmdSFUxFj`. ⛔ Not a claim, ⛔ no label changed.\n\n| `Blocked-by:` | state today | verdict |\n|---|---|---|\n| **#8790** | **CLOSED completed**, PR **#8927 MERGED** | ✅ **discharged** |\n| **#8926** | **OPEN**, `needs-user-decision` with the maintainer | ⛔ still blocking |\n\n⇒ `pm:blocked` is **still the honest label** and is deliberately left in place. Recording the half-discharge so the next unblock scan does not re-derive it, and so nobody reads \"still blocked\" as \"nothing has moved\".\n\n**Re-verification owed at unlock, ⛔ not now:** when #8926 is ruled, this card's file surface must be re-verified **on the merged ref** before dispatch. That is not a formality here — PR #8927 has landed since this card's evidence was taken, and the card itself says so:\n\n> The before/after table is the #8790 dev's measurement against live servers, re-stated here rather than re-run by this seat. Treat it as dated evidence tied to `origin/main` @ `13d78642d`.\n\n`origin/main` is now at `b537855`. The Postgres `42P01` / MySQL `1054` readings are dated evidence about a tree that has moved, and the ruling on #8926 will likely reshape the very predicate this card extends. ⇒ **re-measure before implementing; do not inherit the table.**\n\nAlso flagged upward this round: with #8790 gone, this card is the only queued work in the lane, and #8926 is the single ruling gating it — noted on #8926 itself, where the triage comment's \"nothing is gated on this ruling\" had gone stale.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5306398042,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-16T07:50:18Z","updated_at":"2026-08-16T07:50:18Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5306398042","body":"## ⚠️ Scope change incoming — this card narrows to **Postgres-only** when PR #9061 merges\n\n`domain:drivers` seat, session `session_01NTKPDRoynY8i3HmdSFUxFj`. ⛔ Not a claim, ⛔ no label changed — the card stays `pm:blocked`. Recording now so the next dev does not re-measure a cell that has already been closed.\n\n**#8926 was ruled option A** by the maintainer and its PR (**#9061**) is reviewed ACCEPT and awaiting CI. It adds MySQL's `ER_BAD_FIELD_ERROR` wording to the shared `isUnresolvableColumnError` predicate.\n\n**MySQL spells a dotted key with the same sentence as a plain missing column** (`Unknown column 'title.x' in 'where clause'`), so widening the predicate catches the dotted route on MySQL **as a side effect of the wording** — the key is never inspected for a dot. Measured on a live MySQL 8.0.46 in that PR:\n\n| dialect | this card's evidence (`13d78642d`) | after #9061 |\n|---|---|---|\n| **MySQL** | raw `1054`, bound literal inlined | **`INVALID_FILTER` / 400, enveloped** ✅ |\n| **Postgres** | raw `42P01`, bound literal inlined | **unchanged — still raw** |\n\n⇒ **The MySQL half of this card is closed by #9061**, not by anything done here. What remains is the Postgres half, and it remains for the reason this card already identified as the hard part: **Postgres is the odd one out.** knex compiles `{'title.x': v}` to `\"title\".\"x\"`, so PG reports a missing **relation** (`42P01`, *\"missing FROM-clause entry for table\"*) — a verdict that a column-wording predicate structurally **cannot** see. Widening the shared predicate does not reach it, and the same trick will not work twice.\n\n### What that changes for whoever takes this\n\n- ⛔ **Do not re-run the MySQL cells.** They are measured and fixed; re-measuring them will show green and could be misread as \"the card is stale\".\n- The card's three framing questions (envelope-without-judging / which envelope / redaction as the separable cheap half) now apply to **one dialect**, which makes the \"can it be enveloped without forming an opinion about the key?\" question **sharper, not easier** — on Postgres there is no column-shaped signal to key on at all.\n- ⚠️ Its before/after table is still **dated evidence** tied to `origin/main` @ `13d78642d`. Re-measure on the merged ref at unlock; ⛔ do not inherit it.\n\n**Blocking status unchanged:** `Blocked-by: #8790` (discharged) · `Blocked-by: #8926` (still open until #9061 merges — then this card is free).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5306521664,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-16T08:24:59Z","updated_at":"2026-08-16T08:24:59Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5306521664","body":"## 🔓 UNBLOCKED — both dependencies discharged. `pm:blocked` removed, back to `pm:queue`.\n\n`domain:drivers` seat (#6020), session `session_01NTKPDRoynY8i3HmdSFUxFj`. ⛔ Not a claim; unassigned and open to whoever takes it.\n\n| `Blocked-by:` | state | evidence |\n|---|---|---|\n| **#8790** | discharged | PR #8927 merged (`716ac9bf8`) |\n| **#8926** | discharged | PR **#9061 merged** (`9c4d096f3`), verified by artifact on `origin/main` with a positive control |\n\n### ⚠️ Read this before doing anything: **this card is now smaller than its body says**\n\nPer the unlock rule, the two least trustworthy things at this exact moment are the card's own claims and the PM's summary of them. So, measured rather than asserted:\n\n**#9061 closed the MySQL half.** MySQL spells a dotted key with the same `ER_BAD_FIELD_ERROR` sentence as a plain missing column, so widening the shared predicate caught the dotted route on MySQL **as a consequence of the wording** — the key is never inspected for a dot, so it is ⛔ not a dotted-path verdict and does not trespass on #8371.\n\n| dialect | this card's evidence (`13d78642d`) | on `main` today |\n|---|---|---|\n| **MySQL** | raw `1054`, bound literal inlined | **`INVALID_FILTER` / 400, enveloped** ✅ closed |\n| **Postgres** | raw `42P01`, bound literal inlined | **unchanged — still raw** ← this is the whole card now |\n\n⛔ **Do not re-run the MySQL cells.** They will come back green and could be misread as \"the card is stale\". It is not stale; it is **narrower**.\n\n### What remains is the harder half, and the same trick will not work twice\n\nPostgres is the odd one out, exactly as this card first identified: knex compiles `{'title.x': v}` to `\"title\".\"x\"`, so PG reports a missing **relation** (`42P01`, *\"missing FROM-clause entry for table\"*). A column-wording predicate **structurally cannot see it**. Widening `isUnresolvableColumnError` again does not reach this.\n\nSo the card's three questions now apply to one dialect, and question 1 gets **sharper, not easier**: on Postgres there is no column-shaped signal to key on at all, which is precisely what makes \"can this be enveloped without forming an opinion about the key?\" a real question rather than a formality. ⚠️ If it cannot be done without inspecting the key, the card's own body says it collapses into **#8371** and should be ruled there — that remains the honest exit, not a failure.\n\n⭐ Question 3 is untouched by any of this and may still be the cheap, separable win: **stripping the bound literal from the caller-visible message needs no verdict at all** and is the half with a security shape.\n\n### Owed at dispatch, ⛔ not now\n\nThe before/after table is **dated evidence** tied to `origin/main` @ `13d78642d`. `main` has moved a long way since, including #8927 and #9061 into this very predicate. **Re-measure on the merged ref before implementing** — ⛔ do not inherit that table.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5306724019,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-16T09:21:13Z","updated_at":"2026-08-16T09:21:13Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5306724019","body":"Unlock scan (triage seat): **both `Blocked-by:` lines are now discharged** — #8790 closed earlier, #8926 closed 08:24Z via **merged** PR #9061. This card is fully dispatchable for the drivers lane.\n\n⚠️ **Re-priced on the merged ref — the upstream merge absorbed half the card.** #9061 widened `isUnresolvableColumnError` to MySQL's `Unknown column` wordings, and MySQL classifies a dotted key as an undefined **column** (`1054`) — so on MySQL the dotted key now takes the refusal envelope. This is stated on `origin/main` itself: `sql-driver-unresolvable-where-column-refusal.test.ts` documents \"Since #8926 MySQL sits in the same cell as SQLite\" for the dotted key. What remains is exactly the odd dialect the card starred:\n\n- **Postgres**: `42P01` undefined-**TABLE** (`missing FROM-clause entry for table \"title\"`) — a shape the predicate does not match and never has; still a raw dialect error with the bound literal inlined. The card's questions 1–3 (envelope-without-judging, which envelope, separable redaction) now apply to this one dialect.\n- The **redaction half** (#7929 shape) remains fully live for Postgres and is still the cheap standalone piece.\n\nDispatch must re-measure both dialects on the merged ref rather than trusting the card's `13d78642d` table — the MySQL row of that table is known-stale by construction. The #8371 fence stands: ⛔ no key-inspection for dots; refuse what the backend could not resolve.\n\nTriage: this comment is from the triage seat (scheduled run) — no claim.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5306729666,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-16T09:22:47Z","updated_at":"2026-08-16T09:22:47Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5306729666","body":"## Maintainer authorisation — **option (a): question 3 ONLY**\n\nQuoted verbatim, 2026-08-16, in-session: **「同意 a」** (confirming an earlier **「8931 同意啊」**).\n\nI put two paths to the maintainer and stated my lean:\n\n> **(a)** dispatch question 3 only (pure disclosure stop-gap, ⛔ does not touch envelope semantics); **(b)** rule the envelope first, then dispatch the whole card.\n> My lean is **(a)** — the leak is happening today, and the envelope choice can wait; ⛔ but I do not rule a public-contract shape on the maintainer's behalf.\n\n⇒ **(a) is authorised.** Questions **1 and 2 remain unruled and unclaimed.**\n\n---\n\nClaim: PM loop round 3 — **question 3 ONLY**\nSession: `session_01NTKPDRoynY8i3HmdSFUxFj`\nBranch: `claude/issue-8931-postgres-dotted-literal-redaction`\nWorktree: `objectstack-issue-8931`\nDomain: `domain:drivers`\nFile surface: `packages/drivers/driver-sql/src/sql-driver.ts` — the Postgres raw-error escape path **only**, plus pins. (stop on breach; explain in the report)\nContainer & model: `M`, `mode:subagent`, `model: opus`. ⛔ **No fable floor**: this does not change what the platform accepts or refuses — the query fails either way. It changes only what the *caller-visible message* carries. If the work drifts into changing accept/reject, that is a fork.\n\n## ⛔⛔ SCOPE FENCE — the whole point of option (a)\n\n**IN scope — question 3 only:** strip the bound literal from the caller-visible message on the Postgres dotted route, and send the full text to the server log instead (the #7929 shape, and what `unresolvableColumnNameOf`'s docblock already prescribes: *\"The full text goes to the server log … never to the caller\"*).\n\n⛔ **OUT of scope, and both are the maintainer's, NOT yours:**\n- **Question 1** — whether the error can be enveloped without the driver forming an opinion about the key.\n- **Question 2** — **which** envelope. `INVALID_FILTER` asserts a verdict *about the filter*; a transport/dialect-fault envelope asserts only *\"the backend rejected this statement\"*. These are different claims and the choice **is** the card. ⛔ Do not pick one by implementation.\n\n⇒ **The error's `code`, `status` and classification must come out of this PR UNCHANGED.** If you find the literal cannot be stripped without also deciding the envelope, **STOP and report the fork** — that is a real finding and returns the card to the maintainer, ⛔ not something to resolve by choosing.\n\n## Premise re-verified on `origin/main` today\n\n⚠️ The card's evidence is **dated** to `origin/main` @ `13d78642d`; `main` has moved a long way, including **#8927** and **#9061** into this exact predicate. ⛔ Do not inherit its before/after table — **re-measure**.\n\nMeasured at dispatch:\n- `unresolvableFilterColumnRefusal` at **`:6374`**; call sites `:4594` (the `findRows` ladder terminal) and `:6407` (`count`).\n- `redactStatementFromMessage` **already exists** — `packages/objectql/src/driver-fault-redaction.ts:126`, with 2 consumers. ⭐ Prefer reusing it over minting a second redaction vocabulary; if it does not fit the PG shape, say why rather than working around it.\n- `DOTTED_STATUS_QUO` pin at `…refusal.test.ts:152` — ⚠️ it records the **current** per-dialect verdicts. Your change must leave the Postgres row's `code`/`enveloped` values **as they are** and adjust only what the message carries. If that pin has no axis for \"message content\", that is worth adding.\n\n## ⭐ This card is now SMALLER than its body says — read `5306521664` first\n\n**#9061 closed the MySQL half** (MySQL spells a dotted key like a plain missing column, so the shared predicate caught it — a consequence of wording, ⛔ not a dotted-path verdict). ⛔ **Do not re-run or re-fix the MySQL cells** — they will come back green and could be misread as \"the card is stale\". It is not stale; it is **Postgres-only**.\n\nPostgres is the odd one out and that is the whole remaining difficulty: knex compiles `{'title.x': v}` to `\"title\".\"x\"`, so PG reports a missing **relation** (`42P01`, *\"missing FROM-clause entry for table\"*). ⚠️ **Do not try to widen `isUnresolvableColumnError` to catch it** — that is question 1/2 territory and would trespass on #8371.\n\n## Gates — a LEAD, not a spec\n\nRe-derive with `node scripts/pm/dispatch-gates.mjs` and report additions. ⚠️ `check:error-code-casing` and `check:error-status-conformance` are likely live here. ⚠️ **The affected set is the dependency GRAPH** (`turbo ls --affected`), not this package — a package-local green is not evidence.\n\n⚠️ **Live-Postgres measurement:** the card's evidence came from a live PG 16.13. If none is provisionable in your container, **say so plainly** and report what you could and could not measure — ⛔ never present fixture text as a live measurement. (The #8926 dev installed a real MySQL when its container had none; the same standard applies, but it is not a ruling condition here — an honest \"could not\" is acceptable.)\n\nDraft PR, base `main`, first line **`Part of #8931`** — ⛔ **not `Fixes`**. Questions 1 and 2 remain open on this card, and merging must not close it. Changeset required (caller-visible message changes). Report as an issue comment with the `os-dev-report` marker. Push early.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5306972621,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-16T10:30:11Z","updated_at":"2026-08-16T10:30:11Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5306972621","body":"## ⛔ CORRECTION — this card's premise is FALSE for Postgres, and I repeated it to the maintainer\n\n`domain:drivers` seat, session `session_01NTKPDRoynY8i3HmdSFUxFj`. Posting the retraction where the claim was made, and where the maintainer's authorisation was recorded.\n\n### What the card says, and what is measured\n\nThe card's title and body assert *\"a raw dialect error **with the bound literal inlined** on Postgres and MySQL\"*. The #8931 dev re-measured it — as the dispatch required, rather than inheriting the table dated to `13d78642d` — against a **live PostgreSQL 16.13**:\n\n| | Postgres, dotted key |\n|---|---|\n| caller-visible message | `select * from \"…\" where \"title\".\"x\" = $1 - missing FROM-clause entry for table \"title\"` |\n| **bound literal in `message` / `stack` / any own property** | **absent** |\n\nAcross **7 filter shapes × both halves (`find`, `count`) = 14 cells**: literal absent, `$n` placeholder present.\n\n**I verified the mechanism independently from the installed library source** (knex 3.3.0), because it is load-bearing enough that \"the dev says so\" is not sufficient:\n\n- `lib/dialects/postgres/index.js:176` — `positionBindings()` rewrites every `?` to `$1`, `$2`, …\n- `lib/execution/internal/query-executioner.js:6` — `formatQuery()` substitutes **only** `/\\?\\?/g`, i.e. `?`\n- `:42` — the failure path builds `err.message = formatQuery(sql, bindings) + ' - ' + err.message`\n\n⇒ on pg, positioning runs first, **no `?` survives**, so `formatQuery` inlines **zero** bindings. SQLite and MySQL leave `?` standing and therefore *do* inline — which is the disclosure #7929 and #8790 genuinely redacted. ⭐ **Postgres was never leaking the value; it was immune by accident of a dependency's ordering.**\n\n### The part that is mine to own\n\nWhen I put options (a) and (b) to the maintainer I argued for (a) with: *\"the leak is happening today, and the envelope choice can wait.\"* **That reason was false for Postgres.** The authorisation 「同意 a」 was given on it.\n\n⇒ The narrow scope was still the right call — but ⛔ **not for the reason I gave**. The honest version: question 3 was worth dispatching because the premise was **unverified and dated**, and measuring it was cheap. It measured false. ⭐ That is a good outcome, not a wasted round — but it is not the outcome I predicted, and the maintainer decided on my prediction.\n\n⚠️ **My four-prism analysis of Q1/Q2 is unaffected in substance** — its crux was that `42P01` is already claimed by `isMissingTableError` as benign \"unprovisioned table\", which is independent of the literal question and still stands. But the analysis called Q3 \"止血 / the stop-gap\", and that framing is now wrong: **there was nothing to staunch on Postgres.**\n\n### What IS still open on Postgres, restated truthfully\n\nThe dotted route still answers a **raw `42P01` with no `status`**, carrying the compiled statement's **shape** — table name, quoted reference, `$n` placeholders. That is a disclosure of internal query structure and an ADR-0112 envelope gap. It is real, and it is **exactly questions 1 and 2** — ⛔ unruled, and the maintainer's.\n\n⇒ The card's remaining scope is now **precisely** Q1+Q2 on Postgres, with the security framing narrowed from *\"the caller's value leaks\"* to *\"the caller sees the query's structure and gets an unclassified 5xx\"*. ⚠️ Those are different severities and the card's `security` label should be read against the second, not the first.\n\n## ✅ ACCEPT — PR #9108\n\nTest-only, **zero production files**, `Part of #8931` (⛔ not `Fixes`) — correct, since Q1/Q2 keep the card open. `skip-changeset` correct: nothing user-visible changed, *precisely because the premise died*.\n\n`premise_still_valid: false` with the measurement pinned is the **right** delivery here, and the pin design is what makes it durable rather than a note:\n\n- ⭐ **`literalWithheldBy` per cell** — sqlite/mysql = *a real redaction*; postgres = *never inlined*. **Collapsing those two into \"no literal reaches the caller\" is exactly what let the false premise stand for this long.** Recording the *reason*, not just the outcome, is the fix for that class.\n- **`identifies` per cell** — so \"no literal\" cannot pass against an emptied message.\n- **Absence asserted on `message`, `stack` and every own string property** — an unenveloped pg error reaches the caller as the driver's own object with a dozen fields; the message is not the only way out.\n- **A positive control per cell**, and a **mechanism pin on knex's `positionBindings`** — if a dependency upgrade ever positions bindings after formatting, question 3 becomes a real fix and this goes **red the same day** instead of silently becoming true again.\n- **Reverse verification, both legs predicted then observed** (`1 failed / 46 passed` each), including a sabotage that relabels pg's mechanism — which reddens the *positive control*, proving the two mechanisms are genuinely distinguished and not just described.\n\n⛔ `isUnresolvableColumnError` untouched, so the #8371 fence stands; `code`/`status`/classification unchanged on every dialect, so Q1/Q2 remain the maintainer's.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5306973092,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-16T10:30:19Z","updated_at":"2026-08-16T10:30:19Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5306973092","body":"<!-- os-dev-report -->\n\n```json\n{\n  \"issue\": 8931,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-8931-postgres-dotted-literal-redaction\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/9108\",\n  \"premise_still_valid\": false,\n  \"summary\": \"Question 3's premise is FALSE on Postgres, measured live on PG 16.13: there is no bound literal in the caller-visible message to strip. Across 7 filter shapes x both halves (find/count), the literal is absent from message, from stack, and from every own property of the pg error object, with a $n placeholder standing where the value would be. Mechanism, confirmed in knex 3.3.0 and against the real library: enrichQueryObject runs client.positionBindings(sql) — the pg dialect rewrites every ? to $1 — BEFORE the failure path builds err.message = formatQuery(sql, bindings) + ' - ' + nativeMessage, and formatQuery substitutes ? and nothing else, so on pg it inlines ZERO bindings. Sqlite/mysql keep ?, so they DO inline it — which is the disclosure #7929/#8790 genuinely redacted. Both branches of that catch are literal-free on pg (the compileSqlOnError===false branch uses the already-positioned SQL too). So no production code was changed and no envelope was decided: the dotted route's code/status/classification come out of this PR unchanged on every dialect, isUnresolvableColumnError is untouched, and the #8371 fence stands. What shipped is the message-CONTENT axis DOTTED_STATUS_QUO lacked (explicitly welcomed in the dispatch): literalWithheldBy per cell distinguishing a real redaction (sqlite/mysql) from never-inlined (postgres), identifies per cell so 'no literal' cannot pass against an emptied message, absence asserted on message/stack/every own string property, a POSITIVE CONTROL per cell, and a knex positionBindings mechanism pin so a dependency upgrade that moves this goes red instead of silent. NOTE for the maintainer: the pg dotted route still answers a raw 42P01 with no status carrying the compiled statement's SHAPE — a real ADR-0112 envelope gap, but closing it is questions 1 and 2, which stay unruled and untouched.\",\n  \"tests\": \"All at final commit f6150697f (union re-run at that head, tree clean). LIVE MEASUREMENT: provisioned a real PostgreSQL 16.13 in-container (initdb + pg_ctl, port 54931) — same version as the card's evidence; nothing here is fixture text presented as a measurement, and the scratch server was torn down afterwards. (1) pnpm --filter @objectstack/driver-sql test with live PG configured CI-style (timezone=Asia/Shanghai, TZ=America/New_York): 'Test Files 102 passed | 1 skipped (103), Tests 1973 passed | 33 skipped (2006)' — the 1 skip is the mysql cell, no live MySQL provisioned, and per the dispatch the MySQL cells were deliberately NOT re-run or re-fixed. (2) Same suite with no live server (Test Core config): 'Test Files 99 passed | 4 skipped, Tests 1734 passed | 57 skipped'. (3) An earlier live run showed 2 failures in sql-driver-temporal-conformance.test.ts — that file is byte-identical to main (diff vs origin/main is 1 file) and the assertion names the cause: my first PG ran at UTC, tripping the suite's own three-way zone non-vacuity guard ('the live postgres server runs at UTC ... this cell proves nothing'). Re-provisioning PG at Asia/Shanghai cleared both, hence run (1). (4) pnpm --filter @objectstack/driver-sql typecheck — clean. (5) Gates re-derived with node scripts/pm/dispatch-gates.mjs against the ACTUAL changed path, not the prompt's list; all PASS: check:nul-bytes, check:test-source-alias, check:type-source-resolution, check:query-options-erasure, check:engine-double-contract, check:where-matcher, check:cross-package-test-inputs, plus the two the prompt flagged as likely live, check:error-code-casing and check:error-status-conformance. The derivation ADDED beyond the prompt: test-source-alias, type-source-resolution, query-options-erasure, engine-double-contract, where-matcher (all convention-triggered by 'adds or edits a test file'). REVERSE VERIFICATION, direction predicted before running, both legs as predicted: (a) relabel pg's literalWithheldBy to the redaction mechanism -> '1 failed | 46 passed', failing exactly the pg POSITIVE CONTROL with 'the dialect text should have reached the server log with the literal intact' (proves the two mechanisms are distinguished by observation, not by label); (b) add an absent needle to pg's identifies -> '1 failed | 46 passed', failing the pg disclosure pin with 'the caller must still be told zz-not-in-any-message' (proves the empty-message non-triviality guard is live). Fix was committed BEFORE both legs; restored with git checkout <branch> -- <path>, tree verified clean after each. No ablation in this card, so no dist rebuild claim is made. All build/test runs wrapped in flock -E 99 -w 540 /tmp/os-heavy-verify.lock. Affected-set note: turbo ls --affected --filter=...[origin/main] names 69 packages because driver-sql sits deep in the graph, but the diff is ONE test file and zero production files — nothing imports a test file, so the downstream graph cannot observe this change; I did not run 69 packages' suites and say so rather than implying a sweep I did not do.\",\n  \"open_questions\": [\n    {\n      \"question\": \"Question 3 as authorised has no subject on Postgres — the bound literal is never in the message. Does the maintainer want the REMAINING pg disclosure closed (the raw 42P01 with no status, carrying the compiled statement's shape: table, quoted reference, $n), and if so under which envelope?\",\n      \"options\": [\n        \"A: leave it — the pg route keeps the raw 42P01; this PR's pins record the state and the card returns to the decision box for Q1/Q2 as originally scoped.\",\n        \"B: redact the STATEMENT SHAPE from the pg caller-visible message while preserving code=42P01 and status=undefined exactly (reuse redactStatementFromMessage). Mechanically possible, but it requires the driver to CATCH 42P01, which is a new predicate on the exact error class Q1/Q2 own — a scope decision, not an implementation detail.\",\n        \"C: fold the remainder into Q1/Q2 and rule the envelope once, so the driver stops answering an unenveloped dialect error at all.\"\n      ],\n      \"recommendation\": \"A for this PR (already delivered), and I explicitly do NOT pick between B and C. Real business need: the measured leak is the caller's VALUE, and that is not happening on pg — what remains is statement SHAPE, a materially lower-severity disclosure, so the urgency that justified splitting question 3 out ('the leak is happening today') does not transfer to the remainder. Long-term soundness: B is the patch-shaped option and its long-term cost is concrete — it mints a second, redaction-only predicate on 42P01 sitting next to the refusal predicate, i.e. two spellings of 'the driver recognises this dialect error' with different consequences, which is exactly the split-predicate shape the #8926 ruling already refused once as option B. Making AI-written code hard to get wrong: C is the structurally preventive option, since one ruled envelope means an AI-authored consumer sees a declared ADR-0112 error rather than having to pattern-match a dialect code that differs per backend. The axes agree that B is the weakest, and they do not settle A-vs-C because that is a scheduling question about the maintainer's time, not a technical one — hence no guess.\"\n    },\n    {\n      \"question\": \"The dispatch anticipated one fork ('if the literal cannot be stripped without also deciding the envelope, STOP'). The fork that actually occurred is upstream of that one: there is no literal to strip at all. I treated the pre-authorised message-content pin as the surviving deliverable rather than returning with no PR — flagging in case the maintainer would have preferred a bare no-PR report.\",\n      \"options\": [\n        \"A: the pin was the right call — it converts a measured, incidental dependency property into an asserted invariant at zero behavioural cost.\",\n        \"B: prefer a no-PR finding; close #9108 and record the measurement on the card only.\"\n      ],\n      \"recommendation\": \"A, because the dispatch explicitly pre-authorised this exact artifact ('If that pin has no axis for message content, adding one is in scope and welcome'), and because the finding is otherwise unenforced: Postgres' immunity lives inside knex's binding path, so without the mechanism pin the next person to read this card would have to re-provision a live PG to re-derive what is now a red test. Trivially reversible if the maintainer disagrees — it is one test file.\"\n    }\n  ],\n  \"out_of_scope_findings\": []\n}\n```\n\n---\n\n### Notes the JSON cannot carry well\n\n**Issue body truncation self-check (requested):** the body reads complete — it ends with the `Refs:` line, the two `Blocked-by:` lines and the parenthetical triage note, with no mid-sentence cut. The only corruption is the GitHub sanitizer's entity-escaping of quotes and apostrophes (`&#39;`, `&#34;`) inside the code spans, which is cosmetic and does not drop content.\n\n**The one thing worth a second reader's eye:** the card's Postgres row (`raw 42P01, both halves — bound literal inlined`) was correct about the envelope and wrong about the literal. Both halves of that cell were re-measured; the envelope half stands exactly as recorded, the disclosure half does not. That asymmetry is why this returns `premise_still_valid: false` while still leaving the card open — the defect the card names is real, just not the half question 3 was carved out to fix.\n\n_Generated by [Claude Code](https://claude.ai/code/session_01NTKPDRoynY8i3HmdSFUxFj)_\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5306979216,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-16T10:32:01Z","updated_at":"2026-08-16T10:32:01Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5306979216","body":"## PM answers to the dev's two questions\n\nSession `session_01NTKPDRoynY8i3HmdSFUxFj`. One is mine; one is not, and I am not taking it.\n\n### Q2 — \"was the pin the right deliverable, or should this have been a bare no-PR report?\" → **A, and it is my call, not a fork**\n\nThe pin stays. My dispatch pre-authorised this exact artifact (*\"If that pin has no axis for message content, adding one is in scope and welcome\"*), so this is not scope creep and did not need asking — though asking cost nothing.\n\n⭐ The substantive reason is the dev's own and it is the right one: **Postgres' immunity lives inside knex's binding path**. Without the mechanism pin, the next person to read this card would have to **re-provision a live PG to re-derive a fact that is now a red test**. A measured, incidental property of a *dependency* is exactly the thing that silently reverts on upgrade — pinning it converts \"we checked once in August\" into \"CI tells us the day it stops being true.\"\n\n⛔ `premise_still_valid: false` with the measurement pinned is a **full delivery**, not a shortfall. Recording it as anything less would teach the wrong lesson about what a measurement round is worth.\n\n### Q1 — the remaining pg disclosure (raw `42P01`, no `status`, carrying the statement's **shape**) → **the maintainer's, and I am not guessing**\n\nBut the dev's analysis **narrows the choice**, and that is worth stating loudly because it removes an option rather than adding commentary:\n\n⭐ **Option B is the weakest, and for a reason this repo has already ruled on once.** B would redact the statement shape while keeping `code`/`status`, but it requires the driver to **catch `42P01`** — a *second* predicate on the exact error class Q1/Q2 own, sitting beside `isUnresolvableColumnError`. That is **two spellings of \"the driver recognises this dialect error\" with different consequences**, in one file — the split-predicate shape the **#8926 ruling already refused as its own option B**, on the grounds that it is the Prime-Directive-#12 hazard.\n\n⇒ the live choice is **A (leave it, card returns to the decision box)** vs **C (fold into Q1/Q2 and rule the envelope once)**. ⛔ The dev explicitly declined to pick, correctly: that is a **scheduling** question about the maintainer's time, not a technical one, and the three axes agree only on eliminating B.\n\n⚠️ And the severity framing must travel with it, per the correction at `5306972621`: what remains is **statement shape**, not the caller's value. Materially lower severity than this card's title asserts. The urgency that justified splitting question 3 out — *\"the leak is happening today\"* — **does not transfer to the remainder**, because it was not true in the first place.\n\n### Two things in the verification worth keeping\n\n⭐ **The repo's own anti-vacuity guard fired and was obeyed.** The dev's first PG ran at UTC and `sql-driver-temporal-conformance.test.ts` went red with its own three-way zone guard — *\"the live postgres server runs at UTC … this cell proves nothing.\"* The dev did **not** dismiss a red in a file byte-identical to `main`; it re-provisioned PG at `Asia/Shanghai` and the red cleared. A suite that refuses to be satisfied by a degenerate environment is doing exactly its job, and so was the reader.\n\n⭐ **An honest affected-set statement, which is the #8896 lesson landing.** `turbo ls --affected` names **69** packages because driver-sql sits deep in the graph — but the diff is one test file, nothing imports test files, so the downstream graph **cannot observe** this change. The dev ran what was meaningful and **said plainly that it did not run 69 suites**, rather than implying a sweep it had not done. ⛔ That is the correct handling of a wide affected set: explain why it is not the relevant set, do not silently narrow it.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5307761353,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-16T13:50:29Z","updated_at":"2026-08-16T13:50:29Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5307761353","body":"## ✅ Q3 landed — `Part of` closure action executed. Card moves to the decision box.\n\n`domain:drivers` seat, session `session_01NTKPDRoynY8i3HmdSFUxFj`. **PR #9108 MERGED** — `a01ca7fba`, verified by artifact with a control (`literalWithheldBy` 7 hits, the knex `positionBindings` mechanism pin 4, control `DOTTED_STATUS_QUO` 9).\n\n⭐ **The card is still open, which is the whole point of `Part of`.** A `Fixes` here would have closed a card whose substantive question is unruled, and removed it from every open-issue view — the failure that verb exists to prevent, working correctly for the second time this shift.\n\n| | |\n|---|---|\n| **Delivered** | Question 3, measured and answered: **its premise was false on Postgres.** No literal was ever in the message to strip. What shipped is the message-content axis `DOTTED_STATUS_QUO` lacked, including a mechanism pin on knex's `positionBindings` so the day a dependency upgrade changes this, CI says so instead of the fact silently reverting. ⛔ Zero production files; `code`/`status`/classification unchanged on every dialect. |\n| **Remaining** | **Questions 1 and 2** — a maintainer decision, not queueable work. |\n| **Owner** | The maintainer. Seat claim spent: `pm:dispatched` → **`needs-user-decision`**, assignee cleared, in one action. |\n\n⇒ ⛔ Labelled `needs-user-decision` rather than `pm:queue` **deliberately**: what remains is a *decision waiting to be made*, and putting it in the work queue would misrepresent it as dispatchable and let a lane PM pick up a card whose shape is not settled.\n\n---\n\n## The decision, self-contained — ⛔ do not re-derive it from the thread\n\n### What is actually still wrong\n\nThe Postgres dotted route answers a **raw `42P01` with no `status`**, carrying the compiled statement's **shape** (table name, quoted reference, `$n` placeholders). Real, and an ADR-0112 envelope gap.\n\n⚠️ **Severity re-stated truthfully, correcting this card's own title**: the exposure is **statement structure**, ⛔ not the caller's value. The `security` label should be read against the former. The urgency I originally argued — *\"the leak is happening today\"* — **was false** and is retracted at `5306972621`.\n\n### Option B is eliminated — measured, not preferred\n\nB (redact the shape, keep `code`/`status`) requires the driver to **catch `42P01`** — a *second* predicate on the exact error class beside `isUnresolvableColumnError`. **Two spellings of \"the driver recognises this dialect error\" with different consequences, in one file** — the split-predicate shape the **#8926 ruling already refused as its own option B**. ⛔ Not re-litigable by implementation.\n\n### The live choice — four prisms\n\n**A — leave it.** The pins record the state; the card waits.\n**C — fold into Q1/Q2 and rule the envelope once**, so the driver stops answering an unenveloped dialect error at all.\n\n1. **Platform long-term coherence** → **C**. One ruled envelope removes a per-dialect special case; A preserves Postgres as the one backend answering raw.\n2. **Measured business pull** → **A**. What remains is structure disclosure on a query that cannot be served either way. ⛔ No deployment measured as depending on it. Under startup-focus, low pull argues for deferring, not for building.\n3. **AI-agent error-resistance** → **C**, and it is the sharpest axis: an AI-authored consumer meets a **declared ADR-0112 error** instead of pattern-matching a SQLSTATE that differs per backend. Today it must special-case Postgres to know it did anything wrong.\n4. **Startup scope discipline** → **A**. C is a contract decision with a permanent obligation; A costs nothing today and the pins keep the facts from decaying.\n\n**2 vs 2 — and that is the honest reading, not a failure to choose.** ⛔ The axes genuinely do not settle it, because the remaining question is *when* to spend a contract decision, which is the maintainer's time and appetite, not a technical fact.\n\n⚠️ **The measured constraint that makes C harder than it looks**, and which any C ruling must confront: Postgres's `42P01` is **already claimed** by `isMissingTableError` as benign *\"table not provisioned\"* — the predicate three cards this shift (#8895, #8896, #8906) rely on to mean *truthful emptiness*. Neither the SQLSTATE nor the `relation \"x\" does not exist` wording is available as a discriminator. The only untaken signal is **\"the relation the backend names ≠ the table the driver asked for\"**, which stays inside the ruled *\"refuse what the backend could not resolve\"* and ⛔ never inspects the key for a dot. **That is unverified** — it needs a live-PG measurement (schema qualification, case, quoting) before C is known to be buildable.\n\n⇒ **If C, the first step is a measurement card, not an implementation card.** If A, that measurement is unspent budget and should not be run.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5311455352,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-17T03:33:00Z","updated_at":"2026-08-17T03:33:00Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5311455352","body":"**Maintainer ruling (2026-08-17, recorded by PM session `session_01NYgmGheCzM6NrHZN436Cxf`).** The maintainer reviewed the business-angle analysis of the remaining scope (questions 1+2 — the Postgres raw `42P01` after the question-3 premise was falsified) and ruled, verbatim: 「同意 C」.\n\n**Ruled: Option C — the envelope is decided once, and it is a generic backend-fault envelope, implemented as the driver's terminal catch-all.**\n\n- **Envelope semantics**: an ADR-0112 enveloped error asserting only *\"the backend rejected this statement\"* — ⛔ **not** `INVALID_FILTER` and no verdict about the filter; on Postgres the signal (`42P01`, ambiguous between a dotted key and a genuinely missing relation) cannot honestly support a filter verdict.\n- **Mechanism**: the terminal catch-all at driver-sql's error exit — any dialect error **not** classified by the existing predicates gets wrapped. ⛔ No new per-error-class predicate (no second recognizer on `42P01` — the split-predicate shape the #8926 ruling refused); `isUnresolvableColumnError` and `isMissingTableError` untouched.\n- **Disclosure**: the caller-visible message carries no statement shape (no physical table names, quoted references, or `$n` placeholders); the full dialect text + compiled statement go to the server log via the existing `redactStatementFromMessage` (2 consumers — reuse, don't mint a second vocabulary).\n- **Pins**: `DOTTED_STATUS_QUO` and PR #9108's mechanism/message-content pins update in the same PR to assert the new enveloped state (the Postgres row's cells change deliberately, with the reason recorded).\n- **Priority**: normal queue — ⛔ not `target:v17`; zero measured incidents today, the pull is contingent (security-review line item, AI-caller self-correction, cross-dialect consistency).\n- **Tier**: `opus` — this pulls behaviour back to the declared ADR-0112 contract (declared=enforced restoration); accept/reject is unchanged (the query fails either way). If implementation finds itself changing what is accepted or refused, that is a fork.\n- **Closure**: this ruling settles questions 1 and 2 — the card's last open scope. The implementing PR says **`Fixes #8931`**; its merge closes the card. Severity basis on record per the 2026-08-16 correction: what this closes is statement-**shape** disclosure + an unclassified 5xx, not a value leak.\n\nLabel flipped `needs-user-decision` → `pm:queue` in the same stroke; `bug` / `security` / `domain:drivers` stay.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5313001079,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-17T07:16:12Z","updated_at":"2026-08-17T07:16:12Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5313001079","body":"**Claim** — PM dispatch seat, session `session_01Y26DJEHSBhhAQ6wwfsHNza`, branch `claude/issue-8931-backend-fault-envelope`.\n\nDispatching on the 03:33Z maintainer ruling: **Option C — a generic backend-fault envelope at driver-sql's terminal catch-all.** The PR carries `Fixes #8931`; its merge closes the card (questions 1+2 were the last open scope, question 3's premise having been falsified and pinned by PR #9108).\n\n⛔ The three fences from the ruling, restated because each is a way to get this wrong:\n1. **No filter verdict.** The envelope asserts only \"the backend rejected this statement\" — ⛔ not `INVALID_FILTER`. On Postgres `42P01` is ambiguous between a dotted key and a genuinely missing relation and cannot honestly support one.\n2. **No new per-error-class predicate.** Terminal catch-all only; `isUnresolvableColumnError` and `isMissingTableError` stay untouched. A second recognizer on `42P01` is the split-predicate shape #8926 refused.\n3. **No statement shape in the caller-visible message** — no physical table names, quoted references, or `$n` placeholders. Full dialect text + compiled statement go to the server log through the **existing** `redactStatementFromMessage` (2 consumers — reuse it, do not mint a second vocabulary).\n\nAlso in scope: update `DOTTED_STATUS_QUO` and PR #9108's mechanism/message-content pins in the same PR. The Postgres row's cells change **deliberately** — record the reason alongside.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5314088323,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-17T09:13:06Z","updated_at":"2026-08-17T09:13:06Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8931#issuecomment-5314088323","body":"<!-- os-dev-report -->\n\n```json\n{\n  \"issue\": 8931,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-8931-backend-fault-envelope\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/9273\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Implemented the 2026-08-17 ruling (option C) as a terminal catch-all at driver-sql's two read exits (the else of findRows' recovery ladder, and count's tail): any dialect error the existing classification does not claim now leaves as a generic backend-fault envelope, DATABASE_ERROR / 500, composed and carrying no dialect text. All three fences hold and were verified live on PostgreSQL 16.13: no filter verdict (the envelope is never INVALID_FILTER and says nothing about the filter — measured, a dotted key and a table that was never created both raise 42P01, differing only in prose, and isMissingTableError returns true for BOTH); no new per-error-class predicate (isUnresolvableColumnError and isMissingTableError are byte-untouched, no predicate learns 42P01, the key is still never inspected for a dot); no statement shape in the caller-visible message (no table names, no quoted references, no $n). The original dialect error is kept as a NON-ENUMERABLE cause — load-bearing, because isMissingTableError follows cause and 13 non-test call sites use it to tell a benign unprovisioned table from a failure that must stay loud; verified live that isMissingTableError(wrapped) is still true on pg and sqlite for both halves, so the accept set is unchanged. Pins updated in the same PR: DOTTED_STATUS_QUO's pg row moves to DATABASE_ERROR/500 with the reason recorded beside it, the enveloped boolean becomes a real status, and #9108's literalWithheldBy axis is preserved rather than collapsed — the knex never-inlined fact moves to a new dialectTextInlinesLiteral axis asserted against the SERVER LOG, which is where the dialect text still lives, and the knex positionBindings mechanism pin stays with its consumer restated. One neighbouring disclosure closes as a side effect and is named in the PR body: Postgres inlines the caller's rejected VALUE in its own 22P02 diagnostic, downstream of parameterisation, where no statement cut can remove it.\",\n  \"tests\": \"All at final commit fc3d82286 (tree clean; the gate union was re-run at that head after the last commit). LIVE MEASUREMENT: provisioned a real PostgreSQL 16.13 in-container (initdb + pg_ctl, port 54931, timezone=Asia/Shanghai with process TZ=America/New_York, the three-way zone skew the matrix's own non-vacuity guard demands) — same version as the card's evidence; torn down afterwards. No live MySQL was provisioned, so its cells are declared un-run (declareDialectCell), never silently skipped. (1) pnpm --filter @objectstack/driver-sql test with live PG: 'Test Files 103 passed | 1 skipped (104), Tests 1998 passed | 34 skipped (2032)'. (2) pnpm --filter @objectstack/driver-sql typecheck: clean. (3) DOWNSTREAM sweep — the PREFIX direction (packages that CONSUME driver-sql), each run and read: driver-turso 1003, driver-sqlite-wasm 395, rest 2011, runtime 2464, dogfood 780, service-analytics 1722, service-automation 967, service-datasource 478, service-messaging 242, cli 1367, plugin-security 1279, plugin-auth 1261, plugin-sharing 617, plugin-approvals 496, plugin-reports 70, trigger-record-change 78 — all green. That sweep CAUGHT one real consequence: runtime's #7842 ambient-vs-bare transaction test pinned the raw knex text 'Timeout acquiring a connection' reaching the caller; it is updated in this PR to assert the envelope AND read the same string off cause, so its discrimination is re-homed, not weakened. (4) GATES re-derived with node scripts/pm/dispatch-gates.mjs against the ACTUAL changed paths (not a recalled list); all PASS: check:changeset-gate-self-tests, check:cross-package-test-inputs, check:objectui-changeset, check:test-source-alias, check:type-source-resolution, check-adr-0087-registration, check-changeset-no-major, check-empty-changeset, check-cross-package-test-inputs, docs-audit/check-affected-docs; plus the convention-triggered check:query-options-erasure, check:type-check-coverage, check:type-check-debt (ratchet, run after a full turbo build), check:engine-double-contract, check:where-matcher; plus the implicated check:nul-bytes, check:error-code-casing, check:error-status-conformance, check:driver-conformance, check:route-envelope, check:driver-memory-census, check:durability-log-level, check:partof-closing-keyword. The derivation ADDED beyond the prompt's lead: changeset-gate-self-tests, objectui-changeset, cross-package-test-inputs, adr-0087-registration, changeset-no-major, empty-changeset, affected-docs. (5) BOUNDARY MEASUREMENT: ran the real mapDataError over the exact recorded driver errors, before and after. The card's own route is UNCHANGED on the wire (500 DATABASE_ERROR both ways — REST already derived that verdict by sniffing the message; it is now declared by the producer). One route DOES change and is flagged in the PR body and in open_questions: a read on a registered object whose table was never created goes 404 OBJECT_NOT_FOUND -> 500 DATABASE_ERROR. (6) REVERSE VERIFICATION, direction predicted before each run, all three as predicted: (a) revert findRows' catch-all to 'throw error' -> '19 failed | 53 passed', including 'find() and count() answer an unclassified fault the SAME way' going red because count's exit was still wrapped — a genuine discriminator, not a blanket red; (b) drop the cause -> '5 failed | 67 passed', exactly the two cause pins on both cells plus the 22P02 pin that reads cause.code; (c) make the cause enumerable -> '2 failed | 70 passed', exactly the serialisation pins. The fix was COMMITTED before all three legs; each restored with git checkout <branch> -- <path> and the tree verified clean after. (7) DIST DISCIPLINE, and it caught a false green: runtime resolves @objectstack/driver-sql through exports -> dist/ with no vitest alias, so the first cause-ablation leg against it came back GREEN off a stale artifact. Redone properly: mutate -> pnpm --filter @objectstack/driver-sql build -> node scripts/ablation-dist-preflight.mjs @objectstack/driver-sql \\\"defineProperty(err, 'cause'\\\" --absent (marker absent from all 6 built files) -> run, which then went red as predicted ('expected undefined to match /Timeout acquiring a connection/i'). Restore leg: source restored AND dist rebuilt, with the preflight re-run in the present direction on a marker that actually appears in executable output ('The database refused to run this query for object') to prove the mutation is gone from the artifact. All build/test runs wrapped in flock -E 99 -w 540 /tmp/os-heavy-verify.lock; heavy commands ran with NODE_OPTIONS=--max-old-space-size=4096. Changeset present (@objectstack/driver-sql patch) — the change is user-visible, so no skip-changeset label applies.\",\n  \"open_questions\": [\n    {\n      \"question\": \"A read against a REGISTERED object whose table was never provisioned now answers 500 DATABASE_ERROR where it previously answered 404 OBJECT_NOT_FOUND. Measured at the real mapDataError with the exact driver errors; no test pinned it end-to-end (the unknown-object heuristic suites feed synthetic strings straight to the mapper and are unaffected). It is a direct consequence of the ruled mechanism — the dotted-key 42P01 and the missing-relation 42P01 are indistinguishable without exactly the per-error-class predicate fence 2 forbids. Is that acceptable, or does it need its own follow-up?\",\n      \"options\": [\n        \"A: accept as shipped. The 404's body said \\\"Object 'x' is not registered\\\", which is FALSE in that state — the object IS registered, its table is not provisioned — and 500 DATABASE_ERROR is what DATA_STORE_FAULT was built to say for a store condition that may clear.\",\n        \"B: restore the 404 by teaching mapDataError to consult error.cause for the missing-relation phrasing. Keeps today's wire answer, costs an edit in packages/rest outside this card's surface, and re-introduces message sniffing over a wrapped error.\",\n        \"C: file it as a separate card and decide there, leaving this PR as ruled.\"\n      ],\n      \"recommendation\": \"A. Real business need: no deployment was measured as depending on the 404, and the sentence it shipped was untrue in exactly the state that produces it. Long-term soundness: B would put the boundary back in the business of re-deriving a verdict from prose that the producer now declares — the drift ADR-0112 exists to remove — and it would need its own tripwire. AI-agent error-resistance: a declared 500 DATABASE_ERROR tells an agent 'the server could not serve this, retry/escalate'; a 404 saying the object is not registered actively misleads it into deleting or re-declaring a registration that is fine. If the maintainer disagrees, C is the clean route — it is a REST-boundary question, not a driver one.\"\n    },\n    {\n      \"question\": \"The ruling said to reuse `redactStatementFromMessage`; this PR does not call it, for two measured reasons (recorded in the PR body). Does the maintainer want it reused anyway, which would require moving it out of @objectstack/objectql?\",\n      \"options\": [\n        \"A: keep the composed message as shipped. No second redaction vocabulary is minted (there is nothing to redact — the caller message carries no dialect text at all), and the server-log line is the dialect's text verbatim, matching unresolvableFilterColumnRefusal one method over.\",\n        \"B: move redactStatementFromMessage to @objectstack/types (where looksLikeInternalErrorLeak already lives) and call it from the driver. A cross-package refactor with its own blast radius, and it would still fail the disclosure clause.\"\n      ],\n      \"recommendation\": \"A, and the second reason is the decisive one rather than the layering: `redactStatementFromMessage` cuts at the last ' - ' and KEEPS the dialect's diagnostic tail. On this card's own route that tail is `missing FROM-clause entry for table \\\"title\\\"` — a physical table name in a quoted reference, which fence 3 forbids — and on the 22P02 route it is `invalid input syntax for type integer: \\\"...\\\"`, i.e. it would ship the caller's value. Reusing it would defeat the disclosure clause it was cited to serve. The layering point (driver-sql sits below the engine; no driver depends on objectql) is the second, independent reason.\"\n    },\n    {\n      \"question\": \"No ADR-0087 semantic-migration entry was added, unlike #8790's driver-side change. Correct?\",\n      \"options\": [\n        \"A: correct as shipped. SemanticMigration entries are structured TODOs for METADATA SOURCE FILES (`replacement` is 'the canonical replacement the author should move to'); nothing authorable changes here and there is no source for a consumer to migrate. check:adr-0087-registration passes and the changeset carries the not-required marker with that reasoning.\",\n        \"B: add one anyway, on the #8790 precedent, since a consumer that pattern-matched the raw dialect code on a failing read has to change.\"\n      ],\n      \"recommendation\": \"A. #8790 changed what find() RETURNS (an empty list became a throw) and its `replacement` names a real authoring action ('name a column the object actually has'). This card changes only the SHAPE of an error that was already a failure, with no authoring action available to take — a migration TODO would have an empty `replacement`. The consumer-side note (read error.cause) belongs in the changeset, where it is, rather than in a metadata migration chain.\"\n    }\n  ],\n  \"out_of_scope_findings\": []\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
