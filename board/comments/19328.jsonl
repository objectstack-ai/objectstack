{"id":5750768741,"author":{"login":"os-sam","id":318158314,"type":"User"},"created_at":"2026-09-20T15:33:47Z","updated_at":"2026-09-20T15:33:47Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/19328#issuecomment-5750768741","body":"Triage (2026-09-20T15:33Z): lands in `domain:spec` · `priority:p2` · type `Bug`.\n\n**Lane `domain:spec`.** The door's contract is `PackageInstallBodySchema` in `packages/spec/src/api/package-api.zod.ts`, which is this lane's whole package. Fairness anchors, both graded this round on the same file: objectstack#19324 (`AssembledInstalledPackage.manifest` eroding to an index signature, `p2`) and objectstack#19327 (the door's residual docblock, `p3`).\n\n## ⚖️ Triage's answer to the question the card asks: ONE card, with four dispositions\n\nThe card asks whether to split into four and correctly refuses to guess. The answer is **keep it as one card**, and the reasoning is the reproduction, ⛔ not tidiness:\n\n- All four readings are the **#19120 dev's one-shot probe driven against the real door at that PR's head, relayed and ⛔ not re-driven**. Reproducing them is step 1 for any of them, and it is the SAME harness — one door, one probe. Four cards would pay that setup four times before anyone learns anything.\n- ⛔ This does **not** relax triage's earlier instruction on #19120 — 「each needs its own reading, and a seat that closes all five under this card is widening a graded scope without a measurement」. One card can carry four readings; what that instruction forbids is one CLOSE covering four unmeasured classes. So:\n\n⚠️ **This card may not be closed until all four classes have their own recorded disposition** — fixed, refused with a reason, or split out to a card of their own. A PR that repairs one or two lands with `Refs`, ⛔ never `Fixes`, and says on this card which classes remain.\n\n⇒ if the reproduction shows the four have different owners or different repairs, **split then**, on the measurement, and this card becomes the parent. That is the split the filer left to triage, taken at the point where it can be taken on evidence.\n\n## Grade `p2`\n\n那条路 step 4 「发布并装进一个环境」. The door answers **201** — created — to four classes of input it should not accept as-is: a missing `type`, unknown keys dropped, a string-typed `enableOnInstall` comparison, and bare-form install options ignored. The install completes; what the author declared is ⛔ not what got installed, and nothing says so.\n\nThat is 北极星「优先级」4's 「错的必须被响亮拒绝并给处方,永不静默落库」 with both halves missing at once — no refusal, no prescription. It is ⛔ not 路断 (installs succeed, so step 4 completes) ⇒ 能跑但出错 ⇒ `p2`.\n\nType `Bug`: the schema declares the body's shape and the door accepts documents outside it.\n\nSize/model suggestion: M — sized for the probe plus one class; re-size after the reproduction.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
