{"id":5327337339,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-18T11:10:59Z","updated_at":"2026-08-18T11:10:59Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/9505#issuecomment-5327337339","body":"<!-- os-dev-report -->\n\n```json\n{\n  \"issue\": 9505,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-9505-retire-rename-grace\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/9569\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Re-measured the trigger at my branch point (base e7daea169): review-checklist.md has 0 'ESLint' and 1 'Lint & Repo Gates', so the card's second branch applies and BOTH steps landed — 'ESLint'.renameInFlight flipped to false AND its staleSites budget deleted outright (not set to 0; the ledger's own hygiene rule says a zero budget is a dropped key), making the row a standing ban on writing the dead name fresh. The row comment and the design-doc bullet that predicted a surviving historical mention were rewritten to record what actually happened. All three PM assumptions checked: (1) the gate DOES print both promised notices with the wording the card quotes — captured verbatim before the change; (2) the self-ordering safety claim holds, and the reason is now written next to it (the pin runs in lint.yml's 'lint' job — the 'Lint & Repo Gates' context — with no 'if:', which the file's own wiring block asserts), with the only residual being surfaces outside INSTRUCTION_SURFACES, which carry no mustName requirement and so cannot lean on the grace at all; (3) no coupling with the new 'ADR maintainer approval' row — replacedBy null excludes it from both the mustName grace and the flip-completion loop, it has no budgets, no substring collision, and zero occurrences across all five scanned surfaces (measured); rows are judged independently per surface. Two notes for the PM: the self-test baseline was 81 assertions on main, not the 85 in the dispatch prompt (now 83 after this PR); and the trim narrows what other in-flight PRs may write into the checklist — by design that shows up as a red on such a PR in the merge queue, never on main.\",\n  \"tests\": \"All at final commit 5c31e45bf, clean tree. `node scripts/check-required-contexts.mjs --self-test` => '✓ 83 assertions' (baseline on main was 81: 4 assertions removed by fixture triage, 6 added). `node scripts/check-required-contexts.mjs` => '✓ 8 required context name(s) pinned across 2 workflow(s); 5 instruction surface(s) scanned against 2 retired name(s)' and — the point of the card — ZERO notices, where before the change it printed both '...budget of 1 ... can be trimmed' and '...flip ESLint.renameInFlight to false...'. `node scripts/check-nul-bytes.mjs` => OK (6157 files). `pnpm exec eslint scripts/check-required-contexts.mjs --no-inline-config` => clean. Gate family derived from the actual changed path with `node scripts/pm/dispatch-gates.mjs scripts/check-required-contexts.mjs` => exactly one local family, `pnpm check:required-contexts`, which is the pair above; nothing new to add. Reverse verification, run from the COMMITTED state and restored by `git checkout <branch> -- <path>` (tree verified byte-clean after each): ablation A (renameInFlight false -> true) => exactly 2 failures, 'the shipped ledger's grace is closed...' and 'after the flip and the trim, the ESLint row is silent...', and the pin's flip notice returns; ablation B (staleSites budget restored) => exactly 2 failures, 'a retired name written fresh into a trimmed row's surface => red...' and the same silence pin, and the pin's trim notice returns. Each new green is therefore traceable to a named assertion, and each new assertion to a specific ablation. Both new red-direction pins also carry an in-file single-variable ablation (restore the budget => the same fresh mention is green; re-open only the grace => the naming red clears), so a future restore of either half cannot leave the pins passing vacuously.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": []\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
