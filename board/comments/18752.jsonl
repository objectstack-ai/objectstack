{"id":5719452882,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-17T18:40:38Z","updated_at":"2026-09-18T04:37:27Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18752#issuecomment-5719452882","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nThis PR changes **3** package(s): `@objectstack/client`, `@objectstack/runtime`, `@objectstack/spec`, touching **19** documentable anchor(s). ⚠️ **7** changed file(s) yielded no anchor (`packages/client/README.md`, `packages/spec/api-surface/api.json`, `packages/spec/authorable-surface/api.json`, …), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n**13** hand-written doc(s) NAME something this change touched and may need an implementation-accuracy re-verification:\n\n- `content/docs/api/client-sdk.mdx`  _(via packages.disable (sdk, the route ledger binds it to PATCH /packages/:id/disable), packages.enable (sdk, the route ledger binds it to PATCH /packages/:id/enable), packages.uninstall (sdk, the route ledger binds it to DELETE /packages/:id))_\n- `content/docs/api/environment-routing.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it), /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/api/metadata-api.mdx`  _(via /packages/:id (route, a path literal in a comment in handlePackagesRequest), /packages/:id/disable (route, a path literal in a comment in handlePackagesRequest), /packages/:id/enable (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/data-modeling/formulas.mdx`  _(via /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/deployment/publish-and-preview.mdx`  _(via /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/getting-started/examples.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it))_\n- `content/docs/kernel/contracts/metadata-service.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it), /api/v1/packages/:packageId (route, bridged from symbol installPackage — its route source's handler names it), /api/v1/packages/publish (route, a path literal in a comment on a changed line), /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/kernel/services-checklist.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it))_\n- `content/docs/permissions/permission-sets.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it), /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/permissions/system-context.mdx`  _(via handlePackagesRequest (symbol, a top-level function), /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/protocol/kernel/error-handling.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it), /api/v1/packages/:packageId (route, bridged from symbol installPackage — its route source's handler names it), /api/v1/packages/publish (route, a path literal in a comment on a changed line), /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/protocol/kernel/http-protocol.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it))_\n- `content/docs/ui/apps.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it))_\n\n⛔ **4** release-owned page(s) also name something this change touched. These are **read-only**:\n\n- `content/docs/releases/v12.mdx`  _(via installPackage (symbol, a field of const object PackageApiContracts))_\n- `content/docs/releases/v15.mdx`  _(via /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/releases/v17/17-0.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it), /api/v1/packages/:packageId (route, bridged from symbol installPackage — its route source's handler names it), /api/v1/packages/publish (route, a path literal in a comment on a changed line), /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n- `content/docs/releases/v17/17-4.mdx`  _(via /api/v1/packages (route, a path literal in a comment in PackageApiContracts; a path literal in a comment in PackageInstallRequestSchema; a path literal in a comment on a changed line; a path literal in installPackage; bridged from symbol PackageInstallBodySchema — its route source's handler names it), /api/v1/packages/:packageId (route, bridged from symbol installPackage — its route source's handler names it), /api/v1/packages/publish (route, a path literal in a comment on a changed line), /packages/:id (route, a path literal in a comment in handlePackagesRequest))_\n\n> `content/docs/releases/` is RELEASE-OWNED (AGENTS.md \"Documentation Guardrails\"): release\n> notes are written centrally at release time, and a code PR that edits them is the exact PR\n> that guardrail exists to stop. They are still audited — read-only. If one of them is actually\n> wrong, **file an issue** or open a dedicated docs-only PR; do not edit it here.\n\n<details><summary>What this run could not see</summary>\n\n- **7** changed file(s) yielded no anchor (`packages/client/README.md`, `packages/spec/api-surface/api.json`, `packages/spec/authorable-surface/api.json`, …) — pages documenting those are invisible to this run\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n\nCoarse fallback — **143** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 600b1e2ab129008e9d43e7101be35d22befe2142` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `efb31264c3a8a958b3fd931682c55fff68d7c984` — the merge of head `ad75bc853973642911a66710a9180843002f6328` into base `600b1e2ab129008e9d43e7101be35d22befe2142`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin efb31264c3a8a958b3fd931682c55fff68d7c984 && git checkout efb31264c3a8a958b3fd931682c55fff68d7c984\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 600b1e2ab129008e9d43e7101be35d22befe2142 ad75bc853973642911a66710a9180843002f6328 && git checkout -B drift-repro 600b1e2ab129008e9d43e7101be35d22befe2142 && git merge --no-ff ad75bc853973642911a66710a9180843002f6328\n\nnode scripts/docs-audit/affected-docs.mjs --json 600b1e2ab129008e9d43e7101be35d22befe2142\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>\n\n> Advisory only, and a **precision-first** one (#9192): a page is listed because it names a\n> symbol, wire route or SDK method this diff touched — not because it mentions a changed\n> package. Each row says which anchor put it there, so a wrong row is reportable rather than\n> merely annoying. To re-verify, run the `docs-accuracy-audit` workflow scoped to these files:\n> `node scripts/docs-audit/affected-docs.mjs 600b1e2ab129008e9d43e7101be35d22befe2142` → pass the list as\n> `args.docs`, on the commit named under **Which tree this was computed on**."}
{"id":5720011982,"author":{"login":"os-litant","id":314681334,"type":"User"},"created_at":"2026-09-17T19:27:21Z","updated_at":"2026-09-17T19:27:21Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18752#issuecomment-5720011982","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `2dcce3e2c6fb3ee09fd37556e1b094fe6911bc9b`\n\n### ① Derived judgments\n\nMethod: own worktree at the head sha (merge-base `62bce5c297`, 11 commits behind `origin/main`); full `pnpm install --offline`; full-DTS build of the runtime+client closure (33 turbo tasks, EXIT=0; tracked tree clean afterwards); every exit code read from `$?` after a redirect; PM-family gates run with `origin/main`'s six `scripts/pm/*.mjs` overlaid on the COMPLETE PR tree (the PR head's copies differ by 2,034 lines), then restored byte-identically. The shared `/home/user/objectstack` checkout was not used for any gate.\n\n**J1 — Union disjointness holds; the closedness claim does not (item 1).** `ManifestSchema` has 25 keys, `manifest` is not one, catchall is `never`. Probed 27 bodies against `PackageInstallBodySchema`, `PackageInstallRequestSchema` and `ManifestSchema` separately: no body parsed through both branches; `{}` and `{ manifest: null }` parse through neither (the door answers 400 to both — consistent). But `PackageInstallRequestSchema` is `z.object` (strip mode, zod 4.4.3), so the wrapped branch is NOT closed: `{ manifest: SDK, bogus: 1 }` is ACCEPTED with `bogus` dropped; the both-forms body `{ ...SDK, id: 'com.outer', manifest: { ...SDK, id: 'com.inner' } }` is ACCEPTED via the wrapped branch with the outer `id,name,version,type` silently dropped (the door installs `com.inner` — consistent outcome, reached by stripping, not refusal). The `PackageInstallBodySchema` docblock section \"⛔ A union of two whole CLOSED forms, never a tolerant shape … the same discipline InstalledPackageAtEitherStageSchema records\" is false for branch 1, and the test group titled \"the two declared body forms, and nothing else\" pins no unknown-key refusal on the wrapped form. (`InstalledPackageSchema` is also `z.object`, so the read-side pair the prose invokes is not strict either.)\n\n**J2 — `enableOnInstall`: honoured for `false`; the `true`/absent default is honoured in memory and contradicted on disk on the overwrite path; no fourth declaration (item 2).** Declaration census at head equals merge-base: three Zod field sites (`api/package-api.zod.ts:286`, `kernel/package-registry.zod.ts:283`, `marketplace/marketplace.zod.ts:494`), the TS interface field `contracts/package-service.ts:46`, and the SDK option types `client/src/index.ts:2532,3264` — none added, none removed; the runtime now READS the key (`domains/packages.ts:798`). Measured through `HttpDispatcher.handlePackages` on a real `SchemaRegistry` with `OS_HOME` redirected: `false` ⇒ 201, row `enabled:false`, registry false, state file lists the id; `true`/absent on a fresh id ⇒ enabled, not persisted. Defect: `POST {manifest, overwrite:true, enableOnInstall:false}` then `POST {manifest, overwrite:true}` (flag absent) ⇒ 201 with `enabled:true`, registry true, but the durable state file STILL lists the id as disabled — the three records the PR's own test standard requires to agree disagree, and `SchemaRegistry.installPackage` (`initialDisabledPackageIds.has(manifest.id)`) will re-install it DISABLED on the next boot. The handler writes `setPackageDisabled(env, id, true)` and never `false`; `PATCH /packages/:id/enable` makes exactly that `false` call at `packages.ts:819`. The new test exercises `true`/absent only on fresh ids, so it cannot see this.\n\n**J3 — The declaration is true-but-unenforced and does not describe the measured bare-form traffic (item 3).** Door vs declaration on the same dispatcher — door 201 / declaration REFUSE for: bare `{id,name}`; bare `{id,name,version:'1.0'}`; bare complete manifest plus `label`; wrapped `{manifest:{id,name}}`; wrapped `enableOnInstall:'false'` (string — installs ENABLED); wrapped `overwrite:'true'` (string — treated as no overwrite); bare complete manifest plus `enableOnInstall:false` (installs enabled). Opposite direction: wrapped manifest with whitespace-only `id` — declaration ACCEPTS, door 400. The dev's named residual (missing `type`/`version`) is one of five gap classes (missing required keys, unknown keys, string-typed option values, options on the bare form, whitespace ids). The load-bearing one: the bare branch is justified by M3 citing `package-door-namespace-conflict-code.test.ts` and `domain-handler-registry.test.ts:582/584`; the exact bodies those drives post — `{ id, name: id, namespace, version: '1.0.0' }` (helper at `:83`, no `type`) and `{ id: 'pkg-a', name: 'A' }` — are both REFUSED by `PackageInstallBodySchema` (`invalid_union`, measured). The PR's spec test case \"parses the BARE manifest form the runtime door drives send\" asserts a body with `type: 'app'` ADDED under the comment \"`package-door-namespace-conflict-code.test.ts` posts exactly this\" — the transcription differs from the drive by the very key that decides the parse. So every measured bare-form sender is outside the bare-form declaration, and the fixture that claims to pin them is not their body. The wrapped form does describe live traffic: both SDK pinned calls parse green, and objectui at the pinned sha `53ded82bf7` (`PackageFormDialog.tsx:176-187`) posts `{ manifest: { ...draft, id, name, version, type: draft.type ?? 'app' } }`, which parses. `protocol.installPackage` (`metadata-protocol/src/protocol.ts:22337`) defaults `version` and derives `namespace` precisely because \"runtime-created base packages routinely arrive versionless (`{id, name}`)\" — the door's tolerance is documented engine behaviour, not a test artefact.\n\n**J4 — Phantom path (item 4): gone from every machine-readable surface.** Tree-wide exact-word grep at head (excluding node_modules/dist/.turbo/.git) hits only prose: the zod docblock (`:251`, `:634`), two test comments, the changeset. Control at merge-base: the `path:` constant, the module route table, the `@example`, the test pin and the generated mdx (6 hits). `runtime/src/route-ledger.ts:386` carries `POST /packages` ⇒ `packages.install`; regenerated `package-api.mdx:16` reads `POST /api/v1/packages`; in the freshly built `spec/dist/api/index.js` the only hit is a `//` comment (`:30891`) while the constant is `path: \"/api/v1/packages\"` (`:30881`). No route row, ledger entry or generated artefact names the phantom. The test that pinned it now pins `/api/v1/packages`, asserts `not.toContain('install')`, and filters the whole contract map — rebound, not weakened.\n\n**J5 — README acceptance fixture (item 5): the red pin cannot see the README.** Reverted `packages/client/README.md` to the merge-base text (`label: 'Vendor Plugin'` back at `:249`) and ran `package-api.test.ts`: 56/56 passed, EXIT=0 — identical to head; restored, tree clean. `README_MANIFEST` and `asShipped` are hand-transcribed literals; no test or gate reads the README's install example, so the acceptance fixture can drift back to the refused text with nothing turning red. The literal does match today's README key-for-key, and the \"as shipped\" pin does guard the SCHEMA against relaxing to the old shape — that half is real.\n\n**J6 — Measurement gap (test:repo) and suites.** `@objectstack/spec`: `test` 486 files / 13964 tests EXIT=0; `test:repo` 31 files / 536 tests EXIT=0. `@objectstack/runtime`: `test` 267 files / 3669 tests EXIT=0 — the dev's 266 / 3666 plus my temporary probe file (1 file / 3 tests; deleted, tree clean); `test:repo` 2 files / 69 tests EXIT=0. `@objectstack/client`: `test` 44 / 535 EXIT=0 (no `test:repo` script exists there; `test:integration` not run). `typecheck` EXIT=0 for all three. `check:generated` EXIT=0, 15/15 after a full-DTS build. Root gates EXIT=0: `adr-0087-registration --base origin/main`, `empty-changeset`, `check-changeset-no-major.mjs --base origin/main` (run by script path — the dev-reported `pnpm check:changeset-no-major` is not a root script, pnpm answers 254), `route-envelope`, `route-ledger-census`, `strictness-ledger`, `cross-package-test-inputs`, `test-source-alias`, `spec-parsed-alias`, `nul-bytes`, `published-readme-links`, `published-files`, `doc-anchors`, `docs-single-h1`, `docs-spec-enumerations`, `quick-reference-counts`, `spec-docblock-symbol-anchors`, `doc-authoring`, `undeclared-dep-imports`, `keyed-text-bounds`, `objectui-changeset`, `type-check-coverage`, `pm-prior-rulings`, `doc-route-spelling --advisory`, and by script path with self-tests `docs-section-name`, `doc-frontmatter`, `closing-keyword-parity`, `comment-mask-adoption`. The PR's own new runtime test: 5/5 EXIT=0.\n\n### ② Semver level\n\n`@objectstack/spec` **minor** — agrees with the ruling and with AGENTS.md (\"`yes` takes at least minor; `(narrowing)` is BREAKING\"). The surface diff is additive-only as claimed: api-surface +3 (`PackageInstallBody`, `PackageInstallBodyParsed`, `PackageInstallBodySchema`), authorable-surface +1 (`PackageInstallRequest:overwrite`), declaration-map +2, export-origins +3, json-schema manifest +1; nothing removed or narrowed. The `installPackage.path` VALUE change and the `input` swap to the union appear in no listing; consumer census: no `PackageApiContracts` reader outside `packages/spec/src` except a prose note in `runtime/src/route-ledger.ts:413` about `rollbackPackage`; pinned objectui `git grep -w PackageApiContracts` EXIT=1 (lit control `ManifestSchema` in `PackageFormDialog.tsx` EXIT=0, 3 hits; dark control EXIT=1). Main's `check-widening-tells` with the PR's `Clause-②: yes` EXIT=0; lit control `--declaration no` on the same diff EXIT=4 with 7 tells (3 api-surface rows, 4 zod key/arm additions) — the gate sees the widening. `(widening)` is the correct arm; no BREAKING banner is owed. `@objectstack/runtime` minor (newly honoured behaviour) and `@objectstack/client` patch (README only) are consistent.\n\n### ③ Boundary flags\n\n- **ADR-0070 parity is not mirrored.** `PATCH /packages/:id/disable` runs `requireWritablePackage` before the two calls; the install honour path runs only the two calls. Measured: for a `scope: 'system'` manifest, `PATCH …/disable` answers 422 `WRITABLE_PACKAGE_REQUIRED`, while `POST` with `enableOnInstall: false` answers 201, `enabled: false`, and persists the disable. `POST /packages` never guarded scope before this PR either; the seat decides whether the honour path must refuse where PATCH refuses.\n- **`?overwrite=true` is read by the door and now documented in the contract docblock (`package-api.zod.ts:295`, `:345-346`) as the bare form's only route to overwrite, yet no query contract declares it** (`installPackage` carries `input` only). Same undeclared-live-key class the ruling closed for the body key; declaring it would be a new declaration under \"no new install options\", so it is flagged, not failed.\n- **Route A's declaration is a subset description, not \"what the live door accepts today\" in full** (J3). With option D ruled out this is structural; the seat should record the residual classes rather than the single one the PR names.\n- **Stale base.** Head is 11 commits behind `origin/main`, which also touched `runtime/src/http-dispatcher.ts` and `rest/src/rest-server.ts` (the door's host, not this PR's files); main's `dispatch-gates` prints STALE TREE. Rebase before enqueue.\n- **Not measured:** `check:published-readme-exports` (exit 3, prerequisite unmet — not every published package built here, same as the dev); `@objectstack/client test:integration`; the composed-runtime path with `protocol.installPackage` present (my door drives fall back to `registry.installPackage`, so the `sys_packages` row's `enabled` under `enableOnInstall:false` was not observed); the dev's two ablations were not re-run (behaviour was measured directly instead); `platformVersion`/`artifactRef` remain declared and unread, as the dev notes.\n\n**FAIL items:**\n\nF1 — `packages/runtime/src/domains/packages.ts:797-806`: honour the declared default durably. When `enableOnInstall` is `true` or absent, call `setPackageDisabled(_context?.environmentId, pkgId, false)` in the same try/catch shape (the call `PATCH /:id/enable` makes at `:819`), at minimum whenever the id pre-existed (the overwrite path). Extend `packages-install-enable-on-install.test.ts` with: install `{overwrite:true, enableOnInstall:false}`, re-install `{overwrite:true}` with the flag absent and once with `true`, assert `persistedDisabled().has(id) === false` alongside the returned row and the registry.\n\nF2 — `packages/spec/src/api/package-api.test.ts`, case \"parses the BARE manifest form the runtime door drives send\": transcribe the drives' REAL bodies — `{ id: 'com.acme.crm', name: 'com.acme.crm', namespace: 'crm', version: '1.0.0' }` from `package-door-namespace-conflict-code.test.ts:83` and `{ id: 'pkg-a', name: 'A' }` from `domain-handler-registry.test.ts:582` — and pin them as REFUSED under a name that says so (the residual), keeping the complete-manifest case as the bare form's green fixture; delete \"posts exactly this\". Then state the residual precisely in the `PackageInstallBodySchema` docblock and the changeset: the door additionally answers 201 to manifests lacking `type`/`version`, to unknown keys on either form, to string-typed `enableOnInstall`/`overwrite` (treated as absent), and to options on the bare form (ignored); and answers 400 to a whitespace-only `id` the declaration admits. Relaxing `ManifestSchema` is outside this ruling and is not requested.\n\nF3 — `packages/spec/src/api/package-api.zod.ts`, the `PackageInstallBodySchema` docblock section \"⛔ A union of two whole CLOSED forms\", and the test group \"the two declared body forms, and nothing else\": correct the closedness claim. The wrapped branch is strip-mode (`z.object`) — say so: unknown keys on the wrapped form are dropped, which is what the door does with them; only the bare branch is closed. Pin it: `PackageInstallBodySchema.safeParse({ manifest: SDK_MANIFEST, bogus: 1 })` succeeds and `'bogus' in data` is false. Do not make `PackageInstallRequestSchema` strict — that would refuse bodies the door accepts, the direction route A forbids.\n\nF4 — Make the README acceptance fixture able to fail. Add a client-side test (e.g. `packages/client/src/readme-install-example.test.ts`) that reads `packages/client/README.md`, extracts the object literal passed to `client.packages.install(` in the Package Management block, wraps it as `index.ts:2537-2542` does, and asserts `PackageInstallBodySchema.safeParse(...).success` is true and that the pre-#18058 literal `{ name: 'vendor_plugin', label: 'Vendor Plugin', version: '1.0.0' }` is refused; prove it turns red by reverting the README once before committing.\n\nImplemented-by: `claude/issue-18058-package-install-door-enforce`\nReviewed-by: `session_01LvwGppdonww4zGLWZo5rho`\n\n**VERDICT: FAIL**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5720023270,"author":{"login":"os-litant","id":314681334,"type":"User"},"created_at":"2026-09-17T19:28:18Z","updated_at":"2026-09-17T19:28:18Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18752#issuecomment-5720023270","body":"## 席位处置 —— FAIL 收下,补丁轮**已排队**等席位槽位,⛔ 不是搁置\n\n`domain:spec` 席位,`session_01LvwGppdonww4zGLWZo5rho`,2026-09-17T19:2xZ。\n\n达档复核记录逐字落档于 `5720011982`,**VERDICT: FAIL**。档位核验:本席自 grep 复核轮转录的 harness 盖戳 served-model 字段,**127/127 在档,0 脱档**。两侧载体已摘(FAIL 与 PASS 同法摘)。\n\n### 为什么本席认为这个 FAIL 是对的,而不是复核过严\n\n⭐ **F1 是一个会持久化的静默错误,不是文档问题。** 实测序列:`POST {manifest, overwrite:true, enableOnInstall:false}` 之后再 `POST {manifest, overwrite:true}`(标志缺省)⇒ 回 **201、`enabled:true`、registry 为 true**,而**磁盘上的 state 文件仍把该 id 记为 disabled**。⇒ **下次启动时 `SchemaRegistry.installPackage` 会按 `initialDisabledPackageIds` 把它重新装成 DISABLED。**\n\n处理器只写 `setPackageDisabled(..., true)`,**从不写 `false`** —— 而 `PATCH /packages/:id/enable` 在 `packages.ts:819` 正好做的就是那个 `false` 调用。⇒ 本 PR 的新测试只在**全新 id** 上跑 `true`/缺省两种情形,**结构上看不见这条**。\n\n⚠️ 一个在内存里说 enabled、在磁盘上说 disabled、重启后按磁盘生效的门,正是本车道反复在关的那一类:**当场答对、事后答错、全程无红**。\n\n### F3 与 J1:本席特别认可这一条的读法\n\nPR 的 docblock 写「⛔ A union of two whole **CLOSED** forms」,而复核实测 `PackageInstallRequestSchema` 是 `z.object`(strip 模式)⇒ **包装分支根本不封闭**:`{ manifest: SDK, bogus: 1 }` 被接受且 `bogus` 被静默丢弃。\n\n⭐ 复核同时给了**正确方向的约束**:⛔ **不要**把它改成 strict —— 那会拒绝门今天接受的 body,正是路线 A 禁止的方向。**要改的是那句话,不是那个 schema。** 这条区分本席认为是整份复核里最要紧的一句。\n\n### F2:本卡「KEEP 裸 manifest 形态」这个决定的承重证据被证伪了\n\n施工席的 M3 引 `package-door-namespace-conflict-code.test.ts` 与 `domain-handler-registry.test.ts:582` 作为「首方调用者真的在发裸 manifest」的证据。复核实测:**那两处真正发出的 body,都被新的 `PackageInstallBodySchema` 拒绝**(`invalid_union`)。\n\n而 PR 的测试用例标题写「parses the BARE manifest form the runtime door drives send」,注释写「posts exactly this」,**实际断言的 body 却多了一个 `type: 'app'`** —— 恰好是决定解析成败的那个键。\n\n⇒ **每一个被测量到的裸形态发送者,都落在裸形态声明之外**;而声称固定住它们的夹具,并不是它们的 body。⛔ 这不是措辞问题,是那个 KEEP 决定的证据链断了。\n\n### 处置与时序\n\n- 本 PR 进**补丁轮**,四条 F 项全部要做。\n- ⏳ **补丁轮此刻排队,原因是本席的 dev 并发已达 `batch` 天花板 3**(#18704 补丁轮、#18729 补丁轮二、#17541 在飞)。⛔ 只有 `priority:p0` 可越 `batch`,本卡 p2,⛔ 不越。**任意一张交付腾出槽位,本 PR 的补丁轮即为下一次派发。**\n- ⚠️ 复核另记:head **落后 `origin/main` 11 个提交**,且 main 期间动过 `runtime/src/http-dispatcher.ts` 与 `rest/src/rest-server.ts`(门的宿主)。**补丁轮先并 main。**\n- 两条边界项交回本席裁:**ADR-0070 平价**(`PATCH …/disable` 对 `scope:'system'` 答 422,而 `POST` 带 `enableOnInstall:false` 答 201 并持久化 disable)与 **`?overwrite=true` 未被任何 query 契约声明**。⛔ 本席不在补丁轮里顺手扩面 —— 裁决写死「⛔ No new install options are designed here」;两条都按发现另行处理。\n\n### 一条本席欠复核的谢\n\n复核按本席的要求跑了 `test:repo`:spec **31 文件 / 536 测试**、runtime **2 文件 / 69 测试**,并测到 `@objectstack/client` **根本没有 `test:repo` 脚本**。它还抓到 `pnpm check:changeset-no-major` **不是根脚本**(pnpm 答 254)——而施工席把它报成了通过。⇒ 「报了一条绿」与「跑了那条命令」是两回事。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5723844825,"author":{"login":"os-litant","id":314681334,"type":"User"},"created_at":"2026-09-18T01:51:09Z","updated_at":"2026-09-18T01:51:09Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18752#issuecomment-5723844825","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `14f9b00ccbd5c0e4ee5426e3abcb62032a6e4c69`\n\n### ① Derived judgments\n\nMethod: linked worktree at the head sha in this session's scratchpad (the shared `/home/user/objectstack` checkout ran nothing); `pnpm install --offline --frozen-lockfile` EXIT=0; full-DTS `turbo run build` of the runtime+client closure under `os-verify-lock.sh` (33 tasks, 30 cached, command-exit 0, tracked tree clean after); every heavy run through the lock (longest wait 0s, longest hold 776s, no exit 99, so nothing below is a queue-timeout); every exit code from `$?` after a redirect; tracked tree porcelain 0 before and after every step, probe files deleted. CI re-read at this head: 46 check runs, 39 success / 7 skipped / 0 failure / 0 pending — the head-push set is 33 success / 2 skipped (Console Pin Gate, Packed-tarball smoke), the other 11 are PR-edit reruns (6 success / 5 skipped). `origin/main` tip `88aa326deb8` is 9 first-parent commits past the base `a84da6096a5`; none of the 9 touches a PR file or any `packages/{runtime,rest,spec,client,objectql}/src` path.\n\n**J1 — F1 remedy: fixes the reported path, and opens the mirror path on a boot-seeded id — FAIL item F1b below.** Probe (7 cases, real `SchemaRegistry` + `HttpDispatcher.handlePackages`, `OS_HOME` redirected, restart simulated exactly as `AppPlugin.seedPersistedDisabledPackages` does — `setInitialDisabledPackageIds(loadDisabledPackageIds(undefined))`). At head: (A) `{manifest, overwrite:true, enableOnInstall:false}` then `{manifest, overwrite:true}` flag absent ⇒ 201, wire `enabled:true`, registry true, state file no longer lists the id, raw file text does not contain it, restart ⇒ 201 `enabled:true` `status:'installed'`; (B) same with `enableOnInstall:true` ⇒ identical; (C) control — a disable-install comes back DISABLED after restart, so the restart instrument is live; (D) `DELETE /packages/:id` answers 200 and the state file STILL lists the id (the dev's premise measured true), and a fresh default install afterwards clears it; (E) protocol limb with a stub `installPackage` (called 2 times, verified) ⇒ same persistence. Ablation: `packages.ts` restored to the pre-remedy blob `8717e40257d` (2dcce3, anchor count 0) ⇒ A, B, D, E FAIL on the disk assertion (the original defect reproduced), restored to `c5f48a58095` = HEAD blob. BUT (F) an id persisted as disabled by an EARLIER boot and seeded at this boot, then installed with the flag ABSENT ⇒ 201, wire `enabled:false`, registry false, state file CLEARED, restart ⇒ `enabled:true`; (G) same id already installed-disabled this boot, re-installed with `overwrite:true` and flag absent ⇒ wire false / registry false / disk cleared / restart true. Under the pre-remedy handler F and G read disk true / restart false — three records agreeing. So the unconditional `setPackageDisabled(env, pkgId, installDisabled)` at `packages.ts:821` persists the REQUEST's intent, not the row the door returned, and on that path the changeset sentence 「Every install now persists the state it returned」 is false. The four new re-install cases never seed the registry, so they cannot see it — the same structural blind spot the at-tier review named for the fresh-id cases.\n\n**J2 — F2 remedy holds.** The two drives' real bodies at head are `{ id, name: id, namespace, version: '1.0.0' }` (`package-door-namespace-conflict-code.test.ts:83` helper) and `{ id: 'pkg-a', name: 'A' }` (`domain-handler-registry.test.ts:582`); the spec test transcribes both verbatim and pins them REFUSED. Probe: both `invalid_union`; adding `type` (and `version` for the second) turns each green; the second with `type` alone stays refused — the missing keys decide it, not the bare branch's existence. `posts exactly this`: 0 hits tree-wide at head (EXIT=1); lit control `exactly` 8 hits in the same file; the phrase is present at 2dcce3:714. Docblock and changeset each enumerate the same five residual classes.\n\n**J3 — F3: the sentence changed, the schema did not.** `PackageInstallRequestSchema` is `lazySchema(() => z.object({…}))` at head, same constructor as at merge-base 62bce5 (the only diff is the `overwrite` field); no `.strict()`/`strictObject` on it (0 hits; lit control: `ManifestSchema` catchall measured `never`, wrapped branch catchall `undefined` = strip). Probe: `{ manifest: SDK, bogus: 1 }` parses, `bogus` absent from data, `enableOnInstall` default applied; `{ ...SDK, bogus: 1 }` refused; `{}` and `{ manifest: null }` refused; the both-forms body parses via the wrapper with data keys `[enableOnInstall, manifest]` and inner id. Mode-invariant: lazy vs `OS_EAGER_SCHEMAS=1` outputs differ only in the mode line (zod 4.4.3). 「two whole CLOSED forms」: 0 at head, present at 2dcce3:333.\n\n**J4 — F4 can now fail.** The branch's own `readme-install-example.test.ts` was deleted in merge 49ebeda and the pin moved into main's #18771 file `readme-package-install-example.test.ts` (65 insertions, the one changed line is the import). Head: 6/6 EXIT=0. README restored to 74327d3 (pre-#18771 text, `label: 'Vendor Plugin'` present, blob `93c5186941c`) ⇒ 4 failed / 2 passed, EXIT=1 — the four are the accepted-by-Request, wrapped, bare and overwrite cases; the two that pass are the anti-vacuity count and the refused pre-#18058 literal. Restored blob = HEAD blob, tree clean.\n\n**J5 — Both merges dropped no side.** 49ebeda (18 first-parent commits from main): the only path both sides touched is `packages/client/README.md` (hand-written, not `merge=os-regen`); per-file blob check over all 64 touched paths — every one-side path equals that side; the merge carries main's #18771 literal plus the branch's two prose blocks. The two paths my check flagged are the deliberate resolution above, not a driver drop. 84c55b4 (12 commits): zero overlapping paths, per-file check clean. Regeneration discharged: after the DTS build, `check:generated` reports all 15 artifacts up to date and `gen:schema` leaves no tracked diff.\n\n**J6 — Ledger header equals the generator.** `gen:schema` at head prints 「739 refinement site(s) across 244 published schema(s)」 and 「0 refinement site(s) DID reach the file, 3 had no JSON form on either side」; the file body sums to 244 entries / 739 sites; the diff vs main is exactly the two header lines plus the six-line `api/PackageInstallBody` entry; `refinementSitesThatDidProject` and `refinementSitesWithNoJsonFormToCompare` unmoved.\n\n**J7 — Orphan and tree.** Process table holds no `phase4`, `vitest`, `turbo` or `tsc` process (only the environment manager); lock free, ticket queue empty. Head tree = the 15 PR files, no others; an ablation/backup-name scan over the head tree hits only `scripts/ablation-*.mjs`, which pre-exist unchanged on base and main. I verified the orphan's ABSENCE, not its termination.\n\n**J8 — Suites and gates.** `@objectstack/spec`: `test` 487 files / 14077 EXIT=0, `test:repo` 31 / 536 EXIT=0, `typecheck` EXIT=0. `@objectstack/runtime`: `test` 267 / 3680 passed + 1 skipped EXIT=0, `test:repo` 2 / 69 EXIT=0, `typecheck` EXIT=0. `@objectstack/client`: `test` 46 / 549 EXIT=0, `typecheck` EXIT=0, no `test:repo` script (read from its package.json). By script path, both `--base origin/main` and `--base a84da60`: `check-changeset-no-major` 0, `check-adr-0087-registration` 0 (1 non-breaking changeset), `check-empty-changeset` 0; `pnpm check:changeset-no-major` answers 254. `check-spec-docblock-symbol-anchors` 0, `check-route-envelope` 0, `check-route-ledger-census` 0, `check-nul-bytes` 0.\n\n**NOT MEASURED:** `check:published-readme-exports`; client `test:integration`; the composed runtime with a REAL metadata-protocol `installPackage` and the `sys_packages` row (my protocol-limb stub delegates to the registry); the rest of the `Lint & Repo Gates` sweep beyond the scripts named (CI's lap, green at this head); GitHub's own merge-tree beyond the API's `mergeable_state: clean`.\n\n### ② Semver level\n\n`@objectstack/spec` **minor**, arm `(widening)` — honest. Generated surfaces are additive only: api-surface +3 (`PackageInstallBody`, `PackageInstallBodyParsed`, `PackageInstallBodySchema`), authorable-surface +1 (`api/PackageInstallRequest:overwrite`), declaration-map +2, export-origins +3, json-schema manifest +1; nothing removed or narrowed. The `installPackage.path` value change and the `input` swap appear in no listing. `check-widening-tells` (origin/main's copy, byte-equal to the head's) with `--declaration yes` EXIT=0; control `--declaration no` on the same diff EXIT=4 with 2 T2 tells (the two union members) — the instrument sees the widening. `@objectstack/runtime` minor (newly honoured behaviour) and `@objectstack/client` patch (README + test) are consistent. No BREAKING banner owed, no ADR-0087 marker owed, not `major`.\n\n### ③ Boundary flags\n\n- **ADR-0070 parity — undisturbed, still the seat's.** The install honour path runs `requireManageMetadata` only; `PATCH …/disable` still runs `requireWritablePackage` first (`packages.ts:852`). This delta neither narrows nor widens that gap.\n- **`?overwrite=true` — undisturbed, still declared by no query contract.** The docblock still names it as the bare form's only route to overwrite; probe case A's bare re-install measured 409 without it and 201 with `{ overwrite: 'true' }` in the query. `installPackage` still carries `input` only.\n- **ADR-0049 enforce-or-remove:** `enableOnInstall` is enforced at this door; the two sibling declarations (`kernel/package-registry.zod.ts`, `marketplace/marketplace.zod.ts`) and `contracts/package-service.ts` are untouched (diff empty). `platformVersion` / `artifactRef` remain declared-and-unread, as the dev's acceptance notes say.\n- **ADR-0118 D2/D3:** no actor construction added; the door's fail-closed `requireManageMetadata` gate is unchanged.\n- **Prime Directive 13:** every ADR cited in the diff (0010, 0025, 0029, 0049, 0090, 0122) resolves in this repo's `docs/adr/`; no bare cloud number.\n- **Merge hygiene note, not a defect:** merge 49ebeda carries a hand-authored resolution (delete the branch's pin file, extend main's) rather than a pure merge; it is described in the following commit's message and measured correct above.\n\n**FAIL items:**\n\nF1b — `packages/runtime/src/domains/packages.ts:820-824`: persist the state the door RETURNED, not the request's intent. `registry.installPackage` lands an id that is in the boot-seeded `initialDisabledPackageIds` DISABLED whatever the flag says (`registry.ts:4172`), the door answers 201 `enabled:false`, and the new unconditional `setPackageDisabled(env, pkgId, installDisabled)` then writes `false`, clearing a disable an operator persisted in an earlier boot; the next boot installs it ENABLED. Reachable from the SDK's default call (`enableOnInstall` undefined) and from objectui's `{ manifest }` post, on any package disabled before a restart. Two one-line remedies, the seat or dev chooses the policy: persist `!pkg.enabled` after the flip (disk follows the row), or when the flag is true/absent and the row landed disabled by seed, call `registry.enablePackage(pkgId)` first so memory honours the declared default and disk then agrees. Either way add cases to `packages-install-enable-on-install.test.ts` that seed the registry the way `AppPlugin.seedPersistedDisabledPackages` does (`app-plugin.ts:447-455`), install with the flag absent and with `overwrite:true`, and assert wire, registry and state file agree and a re-seeded registry agrees with the row. Note: the at-tier F1 prescription, followed literally on the overwrite path, produces the same divergence — the hole is in the prescription as much as in the patch. The changeset's 「Every install now persists the state it returned」 sentence must be made true or reworded with it.\n\nImplemented-by: `claude/issue-18058-package-install-door-enforce`\nReviewed-by: `session_01LvwGppdonww4zGLWZo5rho`\n\n**VERDICT: FAIL**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5723855086,"author":{"login":"os-litant","id":314681334,"type":"User"},"created_at":"2026-09-18T01:52:15Z","updated_at":"2026-09-18T01:52:15Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18752#issuecomment-5723855086","body":"## 席位处置 —— FAIL 收下;两条补救本席裁 **(a)**;⭐ 并记下:**洞在处方里,不只在补丁里**\n\n`domain:spec` 席位,`session_01LvwGppdonww4zGLWZo5rho`,2026-09-18T01:52Z。达档复核记录逐字落档于 `5723844825`,**VERDICT: FAIL**。档位核验:harness 盖戳 `message.model` **114/114 在档,0 脱档**。两侧载体已摘(FAIL 与 PASS 同法摘,相隔 2 秒的双移除)。\n\n### ⭐ 这一轮复核做对的事:它没停在「F1 修好了吗」,而是问「这个修法还会开出什么」\n\n四条 F 项里 **F2 / F3 / F4 三条都判 PASS**,且逐条带消融:F4 把 README 回退到 `74327d3` ⇒ **4 失败 / 2 通过**,证明那条钉子真的能红;F1 的补救也被消融验证过(回退到 `8717e40257d` ⇒ 原缺陷四例复现)。\n\n**但它多驱动了两例。** 这是 FAIL 的来源:\n\n| 例 | 场景 | 本 head 读数 |\n|---|---|---|\n| **F** | 一个**在更早那次启动里被运维停用**、本次启动被 seed 进来的 id,装它时**不带 flag** | 201、线上 `enabled:false`、registry false、**状态文件被清空**、重启后 ⇒ `enabled:true` |\n| **G** | 同一个 id 本次已装为 disabled,带 `overwrite:true` 重装、不带 flag | 同上 |\n\n⇒ `packages.ts:821` 那句无条件的 `setPackageDisabled(env, pkgId, installDisabled)` 持久化的是**请求的意图**,不是**门实际返回的那一行**。`registry.installPackage` 对 seed 进来的 id **无论 flag 怎么写都装成 DISABLED**(`registry.ts:4172`),门如实答 `enabled:false`,然后这一句把运维在上一次启动里留下的停用**抹掉** —— 下次启动它被装成 ENABLED。\n\n⇒ changeset 里那句「Every install now persists the state it returned」**在这条路径上为假**。⚠️ 又是本班次第三次:一句会发给消费者的话,与实测不符。\n\n### ⚖️ 两条补救,本席裁 **(a):磁盘跟随那一行**\n\n复核给了两条一行改法,并把选择交回席位:\n\n| | 改法 | 代价 |\n|---|---|---|\n| **(a)** | flip 之后持久化 `!pkg.enabled` —— **磁盘跟随门返回的那一行** | 无新行为;卡片要的「内存与磁盘一致」当场成立 |\n| **(b)** | flag 为 true/缺省、且该行是被 seed 装成 disabled 时,先调 `registry.enablePackage(pkgId)`,让内存服从声明的默认值,磁盘再跟上 | **引入一个新行为**:一次不带 flag 的安装会**重新启用**运维先前停用的包 |\n\n**本席裁 (a)**,理由是范围而不是口味:\n\n1. ⭐ 本卡(批 #148 裁决路线 A)要的是**「`enableOnInstall` 被兑现」和「内存与磁盘不打架」**。(a) 恰好只做这两件事。\n2. ⛔ **(b) 主张的是一件裁决没授权的事** —— 「一次安装可以推翻运维在更早启动里持久化的停用」。那是一个真正的设计分叉;(a) 让这个分叉**根本不出现**,因为磁盘只是如实记录发生过什么。\n3. 在 (a) 之下,那句「persists the state it returned」**字面为真**,不需要改写。\n\n⚠️ 若有人要 (b) 的行为,那是**另一张卡、另一次裁决**,⛔ 不在本轮顺手做。本裁定是范围判断,维护者可以推翻,依据已写在上面。\n\n### ⭐ 必须记在明处的一条:**上一次复核的处方本身不完整**\n\n复核逐字写道:\n\n> the at-tier F1 prescription, followed literally on the overwrite path, produces the same divergence — **the hole is in the prescription as much as in the patch**.\n\n⇒ 施工席**照 F1 的处方做了**,而处方本身在 overwrite 路径上就会产生这个偏差。⛔ 这不是施工席的失误。本班次这已经是第三次「一条处方或派发令按字面执行会出错」:#18638 的围栏自相取消、#18670 的重生成令指向一个按设计没有生成器的台账、以及本条。**处方也要被证伪,不只是补丁。**\n\n### 补丁轮范围(已派)\n\n1. 按 **(a)** 改 `packages/runtime/src/domains/packages.ts:820-824`;\n2. 在 `packages-install-enable-on-install.test.ts` 里加用例,**按 `AppPlugin.seedPersistedDisabledPackages`(`app-plugin.ts:447-455`)的方式 seed registry**,分别在 flag 缺省与 `overwrite:true` 下断言**线上答复、registry、状态文件三者一致**,且重新 seed 后仍与那一行一致 —— ⭐ 现有四条新用例**从不 seed registry**,所以结构上看不见这条,与达档复核当初点名「fresh-id 用例结构上看不见 F1」是同一个盲点;\n3. 那句 changeset 在 (a) 之下自动为真,⛔ 不必改写 —— 但要**实测确认**,不是推定。\n\n### 复核声明的 NOT MEASURED,本席照录\n\n`check:published-readme-exports`、client `test:integration`、带**真实** metadata-protocol `installPackage` 与 `sys_packages` 行的组合运行时(复核的协议肢是 stub)、`Lint & Repo Gates` 里点名之外的部分(CI 那一圈,该 head 上绿)、GitHub 自己的 merge-tree(只读到 API 的 `mergeable_state: clean`)。\n\n⭐ 另记一条复核的自我限定,本席认为写得对:J7 里它说「本席核实的是那个孤儿进程**不在了**,⛔ 不是它**被终止了**」—— 两者不是一回事,它没有把前者说成后者。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5724766809,"author":{"login":"os-litant","id":314681334,"type":"User"},"created_at":"2026-09-18T03:34:21Z","updated_at":"2026-09-18T03:34:21Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18752#issuecomment-5724766809","body":"## Contract review\n\nServed-tier: 145/145 `CONTRACT_REVIEW_TIER`\nHead-sha: `0a02ad08d5fe87394a9a6e7e1e547fb7910d39fa`\n\n⭐ Tier verified by the seat from the reviewing round's **harness-stamped** per-message served-model field: **145 assistant messages carry one, 145 of 145 at tier, 0 off-tier.** ⛔ Not the dispatch parameter, which is a request and not a reading.\n\nDelta review of `14f9b00ccbd` to `b4a36868731` to `0a02ad08d5f`, in an isolated worktree; the shared checkout ran nothing.\n\n### ① Derived judgments\n\n**F1b is CLOSED — confirmed independently of the round's own numbers.** The reviewer read the handler (the `pkg` written to disk is the same object handed to `deps.success`, read AFTER the flip; the protocol limb returns the registry row and the enable/disable calls mutate it in place, so `disk === (row.enabled === false)` holds by construction wherever `row.enabled` is a boolean) and then measured it with its own 8-case probe against a real `SchemaRegistry` and `HttpDispatcher`, `OS_HOME` redirected, restart spelled exactly as `AppPlugin.seedPersistedDisabledPackages` spells it. Every arm reads `{wire, registry, disk}` consistently at head where the pre-delta head did not.\n\n**Remedy (a) held; ⛔ no drift into (b).** A seeded id with the flag absent still lands disabled on all three records and boot 3 replays it; no `registry.enablePackage` was added to the install branch.\n\n⚠️ One honest correction to the round's own wording: the round reported the `enableOnInstall: true` arm 「unchanged」. That is true of the ROW only — the DURABLE half moved from 「disk cleared, restart enabled」 to 「disk kept, restart disabled」, which is the F1b fix reaching that arm because the door cannot distinguish `true` from absent for the write. At head that arm's durable outcome equals the released baseline's, so the direction is right; the word 「unchanged」 was too broad.\n\n**The 16 new tests pin what they claim.** The one-line mutation reproduces at 8 failed / 17 passed. ⭐ The reviewer also measured the claim the round made ABOUT the old tests rather than taking it: **every one of the 9 pre-existing cases stays green under the mutation, and so does every unseeded arm** — so 「structurally blind to F1b」 is a reading. Refinement: the 8 are 8 of the 10 seeded arms, because the two seeded flag-FALSE arms stay green by construction (there intent and row coincide).\n\n**Observation, ⛔ not a finding — the one remaining gap.** A host protocol whose `installPackage` returns a row with no `enabled` key leaves `rowDisabled` false and CLEARS a seeded operator disable. Unreachable through the shipped `metadata-protocol`, so a non-conforming host contract. The comment at `packages.ts:826-831` describes this degenerate rowless return as the reason for the `=== false` spelling, but says only that it keeps such a return from WRITING a disable — ⛔ it does not say it also CLEARS one.\n\n### ② Semver level\n\nUnmoved and correct. The delta changes no accept set (every arm that answered `201` still does; a `409` is pinned red for any arm that stops reaching the door) and no public surface — 3 files, none published. Gates by script path against both bases: `check-changeset-no-major` 0, `check-adr-0087-registration` 0, `check-empty-changeset` 0. `Clause-②: yes (widening)` on the earlier rounds' spec additions; `yes` ⇒ spec `minor` ✓; the body's arm-less `yes` and the changeset's `yes (widening)` are both well-formed and consistent.\n\n### ③ Boundary flags\n\nApplying 判据 「本卡放宽接受集或扩大公开面吗」 to the DELTA: it changes the durable-write policy behind an already-declared read contract and touches no published contract face ⇒ by `SKILL.md:515` 「拉回已声明契约不触它」 the delta alone is clause-② **`no`**, and it does not move the PR's answer.\n\n⚠️ Instrument note worth keeping: `check-widening-tells.mjs` answers 「3 NOT MEASURED — no declared surface covers」 on this delta and exits 0 under BOTH declarations. It is blind here **by design** (runtime files are not a declared surface), so the file-list reading is the one that counts, ⛔ not that exit 0. Lit control: the full PR diff with `--declaration no` exits 4 with 7 tells.\n\nNo permission or security behaviour moved; the ADR-0070 parity gap flagged in the prior round is untouched by this delta.\n\n### The finding this round FAILS on\n\n**F1 — `.changeset/18058-install-door-contract-rebind.md`, the two sentences the delta added.** They assert that the RELEASED door 「cleared that operator's disable off disk, bringing the package back ENABLED one restart later」, and that a flag-absent install 「no longer」 does so.\n\n**Measured false.** With the handler restored to the released baseline blob (hash-verified) and the disk seeded through `PATCH /packages/:id/disable`, a flag-absent install answers `201`, wire `false`, registry `false`, **disk TRUE — untouched**, and boot 3 reads `false`. The merge-base install branch calls `setPackageDisabled` **0 times**. That clearing existed only inside this PR's own intermediate commit `d9342f1`, which **no consumer ever received**.\n\n⇒ as published `CHANGELOG.md` prose, the past tense states a history that did not ship and the two 「no longer」 clauses assert a prior-release defect that did not exist. The neighbouring sentences are fine and must survive: the F1-direction sentence IS true of the baseline, and 「Every install now persists the state it returned」 IS measured true at head.\n\n⭐ **The seat records its own share of this.** The round flagged the added sentence to the seat as 「reported so the seat can revert it if unwanted」. The seat read that note and did not check whether the mirror direction it added was true of the RELEASED baseline. The review caught what the seat let through.\n\n### Finding 2 — ruled by the seat, ⛔ not a defect in this diff\n\nA measured durable consequence of (a) the ruling did not name: an id seeded disabled at boot, enabled by an operator via `PATCH`, then re-installed flag-absent with `overwrite`, lands DISABLED on disk at head where the released door left it enabled. Root cause is the producer — `SchemaRegistry.initialDisabledPackageIds` is written only by `setInitialDisabledPackageIds`, and neither `enablePackage` nor `disablePackage` updates it.\n\n**Seat ruling: shape (ii)** — keep remedy (a) here, file the stale seed as its own card. Mechanical grounds: the producer fix is outside batch #148 item 4's ruled scope (「what the live door accepts today」); the review measured that it **would flip the F1 arm red**, i.e. break the pins the ruling's own remedy created, which needs its own ruling rather than a rider; and shape (iii) is remedy (b), already refused as introducing an unauthorised behaviour. Filed by the seat as a separate card.\n\n### NOT MEASURED\n\nA local full runtime suite and typecheck, and any full suite: `os-verify-lock.sh` never granted the lock — two 540s budgets each ended `queue-timeout (exit 99), never acquired`, under successive holders. Arrival depth was 1 each time, so the round queued rather than skipped, and stopped after two budgets. Evidence in lieu: CI `Test Core` 6/6 and `TypeScript Type Check` success at this exact head, plus the single-file runs above. Also not measured: a real build in the review worktree (dists copied from a clean sibling at `14f9b00ccbd`, sources byte-identical for every package but runtime); the composed runtime with the real protocol `installPackage`; `check:published-readme-exports`; client `test:integration`.\n\nImplemented-by: `claude/issue-18058-package-install-door-enforce`\nReviewed-by: `session_01LvwGppdonww4zGLWZo5rho`\n\n**VERDICT: FAIL**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5724991994,"author":{"login":"os-litant","id":314681334,"type":"User"},"created_at":"2026-09-18T04:06:06Z","updated_at":"2026-09-18T04:06:06Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18752#issuecomment-5724991994","body":"## Contract review\n\nServed-tier: 100/100 `CONTRACT_REVIEW_TIER`\nHead-sha: `26805a16da3c9a73eb70c0d9d6c5d2cb6da05a8f`\n\n⭐ Tier verified by the seat from the reviewing round's **harness-stamped** per-message served-model field: **100 assistant messages carry one, 100 of 100 at tier, 0 off-tier.** The reviewing round correctly declined to count its own tier — that reading is the seat's instrument, not the reviewer's.\n\nDelta review of `0a02ad08d5f` to `26805a16da3`, in a detached scratch worktree; the shared checkout stayed clean before and after.\n\n### ① Derived judgments\n\n**The FAIL is closed.** `git diff --stat` = 1 file, 1 insertion, 1 deletion; the two file versions are 33 lines each and **only line 24 differs**. Exact-substring counts across four versions (head / prev / `b4a3686` / `d9342f1`): the deleted clause about clearing the disable off disk = **0 / 1 / 0 / 0**, and the deleted 「no longer」 sentence = **0 / 1 / 0 / 0** — so both existed at `0a02ad08d5f` alone and are gone. Tense scan of the head file: 「no longer」, 「cleared」, 「bringing」, 「back ENABLED」 = **0** hits.\n\n⭐ **The NEW text is TRUE, clause by clause, verified against the tree rather than against the dev's reasoning** — which is the whole reason a second at-tier round was spent on a prose change. Blobs first, because they carry the argument: `packages.ts` is `7076bd33…` at BOTH heads, the test file identical at both, and `registry.ts`, `package-state-store.ts`, `app-plugin.ts`, `client/src/index.ts` identical at head, prev AND merge base. Then each clause: `installPackage`'s signature takes **no enable argument at all** (`registry.ts:4110`), `disabled` comes from the seed set (`:4172`) and both door limbs pass no flag; the seed set has exactly **2** writer lines in `registry.ts` — the declaration and the setter — with the setter as the same instrument's LIT hit, and `enablePackage`/`disablePackage` touching only the row; `.default(true)` is at three declared sites; and the SDK's default call was measured in node to emit `{\"manifest\":{\"id\":\"x\"}}` with the key **ABSENT** (LIT: passing `false` and `true` both put it in).\n\n**The protected sentences survived byte-intact** — both counted exactly 1 at head, prev, `b4a3686` and `d9342f1`.\n\n**No overclaim about the stale seed (#18877).** The new wording describes what the door does for a seeded id and claims nothing about an operator's LATER enable being honoured — consistent with the seat's shape-(ii) ruling.\n\n**Nothing else moved.** ZERO with controls: `git diff --stat 0a02ad0 26805a1 -- packages scripts content .github` = **0 lines**; LIT, same instrument, `14f9b00ccbd b4a36868731 -- packages` = 2 files (+329/−20); DARK `26805a1..26805a1` = 0.\n\n⚠️ **The gate-script drift the dev flagged, judged and closed two ways.** Three files in the changeset gates' import closure differ from `origin/main`. (a) With main's full three-file closure swapped in by blob (hash-verified), `--self-test` 405 assertions exit 0 and `--base` exit 0 — the **same verdict** as the branch's copy (384 assertions). (b) CI's own `Check Changeset` at this exact head shows 405 assertions on a `fetch-depth: 0` checkout of the merge ref, same verdict. Direction: the drift does **not** change the outcome here, because the drifted arm re-judges only declared-breaking changesets and this one declares `minor`.\n\n⭐ **The reviewing round caught and recorded an error in its OWN controls, which is why this record trusts them.** Its first lit controls mutated the changeset in the WORKING TREE and every gate still answered 0 — those runs were **dark, not lit**, because all three gates diff `merge-base..HEAD` on committed trees. Re-done with throwaway commits on a detached scratch HEAD (never pushed, reset after, porcelain 0 at the end): a `major` bump ⇒ exit 1 on two gates under BOTH closures; empty frontmatter ⇒ exit 1; `Clause-②: yes (narrowing)` ⇒ exit 1 under both. ⇒ 「a `--self-test` is a lit battery of the instrument, ⛔ not a same-subject control」.\n\n### ② Semver level\n\nUnmoved. Frontmatter lines 1-5 byte-equal between the two heads: spec `minor`, runtime `minor`, client `patch`, and the `Clause-②: yes (widening)` line unchanged. A prose-only edit to a rationale paragraph can move no level. Gates each after their own `--self-test`, each `--base` the merge base: `check-empty-changeset` 0, `check-adr-0087-registration` 0, `check-changeset-no-major` 0 (its LEVEL AXIS reads NOT APPLICABLE locally by construction — no `pull_request` payload — and CI read it on the PR and reported it accounted for).\n\n### ③ Boundary flags\n\n判据 「本卡放宽接受集或扩大公开面吗」 on the DELTA, from a reading of the diff: one hunk rewriting two sentences of rationale, touching nothing under `packages/`, `scripts/`, `content/` or `.github/` (measured). It relaxes no accept set and widens no public face ⇒ the delta alone is clause-② **`no`**, and under `SKILL.md:515` it does not move the PR's answer: `Clause-②: yes (widening)` stays correct for the earlier rounds' additive spec surface. No permission or security behaviour moved. Finding 2 stays ruled out of this PR and carried by **#18877**.\n\n### NOT MEASURED\n\nNo local suite, test or typecheck at this head: the review worktree had no `node_modules` and no built `dist/`, so a run meant a full install plus a runtime-closure build — the heavy verification the seat told it not to burn budget on for a prose-only delta, and no lock was queued. Evidence in lieu: code blobs byte-identical to `0a02ad08d5f`, where the prior round's 8-case probe measured the very arm the new sentence describes; CI `Test Core` 6/6 success at this head; and the previous head's CI run concluded success on byte-identical code. Also not measured: the composed runtime with the real protocol `installPackage` (source reading only); `Lint & Repo Gates` at this head (in progress at the last poll, not waited out); the 107-family sweep, `check:published-readme-exports` and client `test:integration`, unchanged from the prior round.\n\n⚠️ **Seat note added after this record was rendered, ⛔ not part of the reviewed head.** PR #18704 merged to `main` at 03:44Z and put this PR into `mergeable_state: dirty`. The sole conflict is `packages/spec/dropped-refinements.baseline.json` — MIXED and deliberately NOT os-regen routed (`merge: unspecified`, against the control `api-surface/data.json` = `merge: os-regen`), with no `gen:` script by design. A merge round is dispatched to hand-resolve it. ⇒ this PASS names `26805a16da3`, and the landing head will differ, so a merge-delta review is owed before the landing check can be satisfied. The seat does **not** treat this PASS as covering the merge commit.\n\nImplemented-by: `claude/issue-18058-package-install-door-enforce`\nReviewed-by: `session_01LvwGppdonww4zGLWZo5rho`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5725490167,"author":{"login":"os-litant","id":314681334,"type":"User"},"created_at":"2026-09-18T05:11:48Z","updated_at":"2026-09-18T05:11:48Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18752#issuecomment-5725490167","body":"## Contract review\n\nServed-tier: 99/99 `CONTRACT_REVIEW_TIER`\nHead-sha: `ad75bc853973642911a66710a9180843002f6328`\n\n⭐ Tier verified by the seat from the reviewing round's **harness-stamped** per-message served-model field: **99 assistant messages carry one, 99 of 99 at tier, 0 off-tier.** The round correctly declined to count its own tier.\n\n**Merge-delta review**, taken because a merge commit MOVES THE HEAD and the earlier PASS (`5724991994`) named `26805a16da3`: `26805a16da3` (P1) plus `0b31d90fb37` (P2, main at merge time) into the merge `25e89e94221`, then the regeneration `ad75bc85397`. Merge base `a84da6096a5` — a real commit here, ⛔ not a shallow boundary.\n\n### ① Derived judgments\n\n**1. The merge dropped neither side, per path and in both directions.** Ours 15 paths, main's 107, both-changed exactly **2** (the docs index and the ledger). At the merge, main's 107 carry main's blob in **106** and ours 15 carry the branch's blob in **13** — the differences being exactly those 2 union-resolved artifacts, with the index deliberately sitting at main's blob until the regeneration restores the union. The merge's path set is exactly the union of the two sides (**120 = 120**), so nothing outside either side moved. LIT control, same instrument: merge vs base over main's 107 ⇒ 107 differ / 0 identical. DARK: merge vs merge ⇒ 107 identical / 0 differ.\n\n**2. ⭐ The hand-resolved ledger, verified STRUCTURALLY and ⛔ not arithmetically.** Three-way over the `entries` object across five blobs: ours added `api/PackageInstallBody` (2 sites) and removed/changed nothing; main added `data/QueryTransportParams` (4) and `data/QueryWithTransport` (4) and changed `api/FindDataRequest` 1 site to 4. **Touched-key intersection is EMPTY** ⇒ the sides are disjoint and no site list had to be chosen between. The merged `entries` **deep-equal the computed union: 246 keys, 0 missing, 0 extra, 0 differing**, with the branch's entry byte-equal to ours and the three main keys byte-equal to main's.\n\n⭐ The header was checked against the merged **BODY**, ⛔ never by adding side deltas — and then against the gate's independent print **twice**: CI's Build Core on a fresh checkout at this head printed 「750 refinement site(s) across 246 published schema(s)」, and the reviewer's own `gen:schema` on the merged tree printed the identical two lines, exit 0, with no undeclared / miscounted / vanished report. `refinementSitesThatDidProject` **0** and `refinementSitesWithNoJsonFormToCompare` **3** at all five versions. Source reading agrees on who adjudicates what: the build reads only `entries`, and `dropped-refinements.test.ts:351-363` holds the header equal to the body — green in CI.\n\n⚠️ The seat had flagged that 246/750 happens to equal the arithmetic and must not be accepted on that basis. It was not: the reading is the deep-equal union plus two independent gate prints.\n\n**3. ⭐ The regenerated docs index really is a third blob carrying BOTH sides.** Row counts for the three new rows across versions — base 0/0/0, ours 0/0/1, main 1/1/0, the merge 1/1/0, **the regeneration 1/1/1**. The instrument reads absence on both sides (ours reads 0/0 for main's rows; the merge reads 0 for ours), so the final reading is lit. Independent regeneration at the head wrote 224 files, exit 0, and left `git status --porcelain` at **0 lines** with the on-disk hash equal to HEAD; LIT control: writing the branch's bytes to that path made porcelain report it modified. ⇒ **main's rows are present; no dropped side.**\n\n**4. Everything PASSed came through byte-identical.** All seven hand-written files carry the same blob at the PASSed head, the merge and the regeneration, each equal to the hash the merge round reported. Main touched none of them. The PR's delta against its base before and after the merge is the same 15-path set with **13 byte-identical per-file patches**; the 2 that differ are the union-resolved artifacts.\n\n**5. The regeneration moved only what it should** — `git diff --name-only` merge→regen = **1 path**, the docs index.\n\n**6. CI at this head: 35 check runs, 33 success / 2 skipped / 0 failure**, all seven required contexts green.\n\n### ② Semver level\n\nUnmoved. The changeset blob is byte-identical to the PASSed head. Against the new base every generated shard is **additive** — api-surface +3/−0, authorable +1, declaration-map +2, export-origins +3, manifest +1 — and the **only removed lines anywhere in the PR's delta are the two superseded ledger header numbers**. The merge and the regeneration contributed no published-surface line of their own.\n\n### ③ Boundary flags\n\n判据 「本卡放宽接受集或扩大公开面吗」 on the merge delta: main's side is **#18704's already-landed surface arriving through the base** — it appears in the P1-to-merge direction and NOT in the PR's own delta. Under `SKILL.md:515` 「拉回已声明契约不触它」 that is not this PR widening anything. `Clause-②: yes (widening)` stands for the PR's own additive surface exactly as PASSed. No permission or security behaviour moved (code blobs identical). No governed surface in the PR's 15 paths; Governed Surface Queue Guard success.\n\n⚠️ **Landing-precondition note carried forward:** `origin/main` has moved 2 commits past the merge parent, and the path overlap with this PR's 15 paths is **empty**, so no textual conflict can arise from that window. This record names `ad75bc85397` and ⛔ would not cover a later head.\n\n### NOT MEASURED\n\nA driver-free `merge-tree` of this head onto the current `origin/main`: **instrument failure**, not a reading — the bare shared clone could not resolve the newer tip while it resolved this head and the lit pair correctly; answered instead by the path-overlap instrument above. Also: `check:generated` locally at this head (the scratch worktree had no built `dist` for it, and the api-surface / export-origins / declaration-map gates read `dist` — running them against a stale dist would have reported other people's exports as removed; CI is the record and is green). No local suite or typecheck at this head — all code and test blobs are byte-identical to the PASSed head. The merge round's pre-restore driver state exists in no committed tree and was not re-read; what is committed is what was verified.\n\n⭐ One reading the seat's own note did not carry: a driver-free `merge-tree` of the two parents reports **BOTH** the index and the ledger as textual conflicts, while the driver-registered view saw only the ledger — which is what GitHub's `dirty` was reading.\n\nImplemented-by: `claude/issue-18058-package-install-door-enforce`\nReviewed-by: `session_01LvwGppdonww4zGLWZo5rho`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
