{"id":5794416917,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T12:00:54Z","updated_at":"2026-09-23T12:00:54Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19858#issuecomment-5794416917","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nThis PR changes **2** package(s): `@objectstack/service-automation`, `@objectstack/trigger-schedule`, touching **17** documentable anchor(s). ⚠️ **3** changed file(s) yielded no anchor (`packages/triggers/trigger-schedule/README.md`, `packages/triggers/trigger-schedule/src/index.ts`, `packages/triggers/trigger-schedule/vitest.config.ts`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n**2** hand-written doc(s) NAME something this change touched and may need an implementation-accuracy re-verification:\n\n- `content/docs/automation/connectors.mdx`  _(via AutomationServicePluginOptions (symbol, a top-level interface))_\n- `content/docs/automation/flows.mdx`  _(via AutomationServicePlugin (symbol, a top-level class))_\n\n⛔ **2** release-owned page(s) also name something this change touched. These are **read-only**:\n\n- `content/docs/releases/v16.mdx`  _(via AutomationEngine (symbol, a top-level class), TimeRelativeTriggerPlugin (symbol, a top-level class))_\n- `content/docs/releases/v17/17-0.mdx`  _(via AutomationEngine (symbol, a top-level class))_\n\n> `content/docs/releases/` is RELEASE-OWNED (AGENTS.md \"Documentation Guardrails\"): release\n> notes are written centrally at release time, and a code PR that edits them is the exact PR\n> that guardrail exists to stop. They are still audited — read-only. If one of them is actually\n> wrong, **file an issue** or open a dedicated docs-only PR; do not edit it here.\n\n<details><summary>What this run could not see</summary>\n\n- **3** changed file(s) yielded no anchor (`packages/triggers/trigger-schedule/README.md`, `packages/triggers/trigger-schedule/src/index.ts`, `packages/triggers/trigger-schedule/vitest.config.ts`) — pages documenting those are invisible to this run\n- **2** name(s) were too generic to anchor anything (single lowercase words)\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **7** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 8cbc3c0084a3c3c8ef5e59763ab3be96aa1a6f07` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `d4890403e9d6b80bdc34038774091ff55b83c289` — the merge of head `817d4d38f2a17ab58d65413fa35e90914677f9e5` into base `8cbc3c0084a3c3c8ef5e59763ab3be96aa1a6f07`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin d4890403e9d6b80bdc34038774091ff55b83c289 && git checkout d4890403e9d6b80bdc34038774091ff55b83c289\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 8cbc3c0084a3c3c8ef5e59763ab3be96aa1a6f07 817d4d38f2a17ab58d65413fa35e90914677f9e5 && git checkout -B drift-repro 8cbc3c0084a3c3c8ef5e59763ab3be96aa1a6f07 && git merge --no-ff 817d4d38f2a17ab58d65413fa35e90914677f9e5\n\nnode scripts/docs-audit/affected-docs.mjs --json 8cbc3c0084a3c3c8ef5e59763ab3be96aa1a6f07\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>\n\n> Advisory only, and a **precision-first** one (#9192): a page is listed because it names a\n> symbol, wire route or SDK method this diff touched — not because it mentions a changed\n> package. Each row says which anchor put it there, so a wrong row is reportable rather than\n> merely annoying. To re-verify, run the `docs-accuracy-audit` workflow scoped to these files:\n> `node scripts/docs-audit/affected-docs.mjs 8cbc3c0084a3c3c8ef5e59763ab3be96aa1a6f07` → pass the list as\n> `args.docs`, on the commit named under **Which tree this was computed on**."}
{"id":5798350538,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T16:11:27Z","updated_at":"2026-09-23T16:11:27Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19858#issuecomment-5798350538","body":"## Contract review (retroactive — PR landed as 863a775872 before this record was written)\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `817d4d38f2a17ab58d65413fa35e90914677f9e5`\n\n⚠️ **Why this record is late**: the `domain:services` seat enqueued this PR without the served-tier contract review that `.claude/skills/pm-dispatch/references/contract-review.md:8` owes to `.changeset` prose and `:25` requires on file before landing. That lapse is the seat's own. The review below was run afterwards by an isolated reviewer at the served tier. It saw only card #19834, its ruling, and the landed diff `863a775872^1..863a775872`. The seat adopts the verdict.\n\n### ① Derived judgments\n\nThe changeset prose was checked claim by claim at `863a775872`. **All 14 claims are CORRECT.** Evidence:\n- `engine.ts:879` (the option), `:2532-2536` / `:3561` (read per bind), `:3606` (the engine gates first), `:3622` + `:4306-4316` (a differing trigger surfaces as a binding failure).\n- `plugin.ts:136`, `:610-614` (forwarding); trigger-schedule `plugin.ts:59/103`, `time-relative-plugin.ts:53/103`.\n- `schedule-trigger.ts:296-299`, `:663-668`; `time-relative-trigger.ts:294-300`, `:318`; `index.ts:4,22-23,27` (exported types); `env.ts:237-241`, `:330-349`, `:374-377` (the invariant).\n\n**New public surface** reachable through the `exports` maps:\n- `@objectstack/trigger-schedule`: four exported types, and new optional constructor params on four exported classes.\n- `@objectstack/service-automation`: `scheduledWorkPolicy` on `AutomationEngineOptions` and on `AutomationServicePluginOptions`.\n\n⚠️ **Clause-② classification — escalated, not charged to the PR.** The PR declared `Clause-②: no`, which is exactly what ruling 216 item 1 on the card handed it: 「widens a runtime options surface, not `packages/spec`」. The written criterion reads differently. `references/execution-duties.md:65` asks 「本卡放宽接受集或扩大公开面吗」, and `references/contract-review.md:10-11` measures the public surface by each package's `exports` map, not by `packages/spec`. On that criterion this diff is a widening, so `yes`. Semver is unaffected: it is `minor` either way. The ruling and the criterion need reconciling by the maintainer.\n\n### ② Semver level\n\n`minor` on both packages is consistent (AGENTS.md:1081-1085; ruling item 4). No CHANGELOG edits. `Check Changeset` passed at the head.\n\n### ③ Boundary flags\n\nZero `packages/spec`, zero `content/docs/**`. The scope sits inside the ruled shape. `AutomationServicePluginOptions` forwarding is outside the ruling's literal list, but it is inside item 1's intent (\"every consumer reads THAT\"). Item 3's acceptance test can only construct the engine through that plugin, and the forwarding was disclosed.\n\nThe dev's three open questions are escalated as follow-ups: the `defineStack({ jobs })` gate, the env-var remedy text, and a policy builder.\n\nImplemented-by: `claude/issue-19834-per-kernel-scheduled-work-policy`\nReviewed-by: `session_01AhQASwqJr2Z7XfGWUdvnbF`\n\n**VERDICT: PASS**\n\nNon-blocking defects:\n1. The Clause-② mismatch above.\n2. The invariant in the docblocks at `engine.ts:875` and `schedule-trigger.ts:316` is written without parentheses: `requiresActingOrganization === enabled && runOwnership === 'declared'`. Under JS precedence that is a different, false statement. The changeset's own parenthesised form is correct.\n3. `README.md:90-91` says \"is truthy\", but the parser accepts only `1|true|on|yes` (`env.ts:239-241`).\n4. The trigger-schedule acceptance suite reads `service-automation` from `dist`, not source. CI order builds `dist` first, so this is a hygiene note only.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
