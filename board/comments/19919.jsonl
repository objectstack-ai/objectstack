{"id":5801305474,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T19:17:35Z","updated_at":"2026-09-24T12:53:29Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19919#issuecomment-5801305474","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n**1** anchor(s) derived from **2** changed package(s); no hand-written page names any of them. ⚠️ **2** changed file(s) yielded no anchor (`packages/metadata/README.md`, `packages/metadata/src/serializers/typescript-serializer.ts`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n<details><summary>What this run could not see</summary>\n\n- **2** changed file(s) yielded no anchor (`packages/metadata/README.md`, `packages/metadata/src/serializers/typescript-serializer.ts`) — pages documenting those are invisible to this run\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **139** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json 2c1011b01bc071c545f72f2761647b8d9ab56375` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `5763b14fd6729d60510836b8a039d1684bd77a6d` — the merge of head `3cb0a8f6a226175eb523a9b4a60d0fb00d25a10c` into base `2c1011b01bc071c545f72f2761647b8d9ab56375`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin 5763b14fd6729d60510836b8a039d1684bd77a6d && git checkout 5763b14fd6729d60510836b8a039d1684bd77a6d\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin 2c1011b01bc071c545f72f2761647b8d9ab56375 3cb0a8f6a226175eb523a9b4a60d0fb00d25a10c && git checkout -B drift-repro 2c1011b01bc071c545f72f2761647b8d9ab56375 && git merge --no-ff 3cb0a8f6a226175eb523a9b4a60d0fb00d25a10c\n\nnode scripts/docs-audit/affected-docs.mjs --json 2c1011b01bc071c545f72f2761647b8d9ab56375\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>"}
{"id":5808373817,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T05:39:37Z","updated_at":"2026-09-24T05:39:37Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19919#issuecomment-5808373817","body":"## Contract review\n\nServed-tier: 61/61 `CONTRACT_REVIEW_TIER`\nHead-sha: `2f378ec5cfda272f20cdc961c084f27d660b37ce`\n\nIsolated at-tier reviewer subagent, run by the `domain:spec` seat-4 session on the maintainer's instruction in that session (「帮我处理」, with the landing route chosen there); every one of its 61 transcript turns served at the tier the constant names. Adopted by the seat 2026-09-24T05:39Z. The record below is the reviewer's, unedited except the two header lines. The reviewer's own `Served-tier:` line declared a lower tier than the transcript shows; the seat replaced it with the transcript measurement.\n\n\n### ① Derived judgments\n\n1. The change, verified at the ref. `git diff --stat 8490127962834a125239ae85d9e6a88c0386896c refs/review/pr-19919` (merge-base to head): 5 files, +143/−6, identical to `pulls/19919/files`. The one declaration moved is `packages/spec/src/ui/view.zod.ts:6327` at refs/review/pr-19919: `export type ViewMetadata = z.input over (typeof VIEW_METADATA_MEMBERS)[ViewMetadataBranch]` (origin/main `:6310` had `z.input over typeof ViewMetadataSchema`). `ViewMetadataParsed` (`:6329`) is a context line, unchanged. `ViewMetadataSchema` (`:6020-6037`), `VIEW_METADATA_MEMBERS` (`:5615-5624`) and the preprocess are untouched, so the runtime accept set cannot have moved. RIGHT against the card, which asked for exactly \"the input of the post-preprocess union (the members the union runs)\".\n\n2. Derivable and honest. `VIEW_METADATA_MEMBERS` is `as const satisfies Record of ViewMetadataBranch to z.ZodTypeAny` (`:5624`), so its values keep concrete types; `lazySchema` is typed `T extends z.ZodTypeAny, (factory: () to T): T` (`packages/spec/src/shared/lazy-schema.ts:21`), so `z.input` over the indexed union distributes to the four members' inputs. The schema is built from the same record (`:6026` maps `VIEW_METADATA_BRANCHES` over it), so member list and type cannot drift. The card's premise (preprocess input is unknown; members cast to `z.ZodTypeAny` at `:6035`) holds at both refs.\n\n3. Exactly the runtime accept set? No, and no TS type can be, and the PR says so in the TSDoc (`:6320-6325`) and the changeset. Wider than the door: refinements (`containerHasAView` on the container member `:5538` region, `assertViewIdentity` in the preprocess `:6029`, `checkListViewCalendarVisualization` `:5573`) and mixed-member literals (container member all-optional; TS judges an object literal's keys against the union as a whole). Narrower than the door: `stripViewConsoleDecorations` removes row ids before any member judges, and strip-mode members drop undeclared keys, so those bodies parse but their literals are TS2353. The PR measured the exclusive-union alternative and rejected it with a reason (four platform-written PUT bodies would be refused). Not a defect against the card. The 17/3/8/16 matrix is the author's measurement; I did not re-run it (no local gate re-runs), but each class's direction follows from the schema text above.\n\n4. Pin is weight-bearing and CI-judged. `packages/spec/src/ui/view-metadata-type.test.ts:61-66` carries three `@ts-expect-error` directives (unknown, undeclared key, scalar), one body per member typed through the published name (`:33-49`), and a coverage assertion against `VIEW_METADATA_BRANCHES` (`:71-73`). The file sits inside `tsconfig.test.json` `include: [\"src/**/*\"]` and has no entry in `packages/spec/test-typecheck-debt.json` (grep for `view-metadata-type` exits 1; ledger byte-identical between origin/main and the PR ref), so an unused directive (TS2578) reds the gate. CI at head: `Type Check · workspace` job 107341254326 (head_sha 2f378ec5cf…), step \"Type check workspace packages\" success; log shows `@objectstack/spec:typecheck` running `tsc --noEmit && pnpm check:scripts-typecheck && pnpm check:test-typecheck`, then `check:test-typecheck: OK — @objectstack/spec's test layer compiles under packages/spec/tsconfig.test.json; 53 file(s) / 255 error(s) / 142 pinned signature(s) held`. With the new file in that program, that green line is CI-side proof the narrowing fired; the PR's ablation control (revert the line, 3x TS2578 at 61/63/65) is consistent.\n\n5. Consumers. `git grep -n -w ViewMetadata refs/review/pr-19919 -- ':!packages/spec/src/ui/view.zod.ts'`: no annotation consumer outside the new test — only comments (`packages/metadata/src/serializers/typescript-serializer.ts:31,34`), the test docblock, two changesets, `packages/metadata/README.md:77`, and the baseline rows `api-surface/ui.json:428`, `export-origins/ui.json:414`. objectui at the pin (`.objectui-sha` at origin/main = `62597c588072636e9c30ea35b3d89b1e46fd765d`): `git grep -n -w ViewMetadata` exits 1, `ViewMetadataParsed` exits 1; control grep `ViewMetadataSchema|VIEW_METADATA_MEMBERS|diagnoseViewMetadata` hits 11 files, so the negative reading is valid. All four Type Check lanes at head are success with head_sha 2f378ec5cf… (source gates 107341254519, workspace 107341254326, debt ledger 107341254648, consumer gates 107341254656). `Console Pin Gate` was skipped at head (ci.yml `console` path filter: `.objectui-sha`, `scripts/build-console.sh`, …; none touched), so objectui was not built against this head in CI; with zero references at the pin nothing there can break.\n\n6. Does the api-surface baseline record it? No, by design. `packages/spec/api-surface/ui.json:428` `\"ViewMetadata (type)\"` identical at both refs; `packages/spec/scripts/build-api-surface.ts` header: one `name (kind)` row per declared kind, and the signature hash \"prints a type REFERENCE and does not expand it\"; `api-surface-signatures.json` covers only `defineX` factories, none of which names `ViewMetadata`. AGENTS.md `:750`: the snapshot \"records that an export exists, never what it resolves to\". Consumer-gates step \"Check @objectstack/spec public API surface\" success. The changeset is therefore the only record of the narrowing, which is what the PR states.\n\n7. Collision with #19920: none. #19920 names `ViewMetadataParsed` (two lines below), `InlineAction`, `AssembledViewArtifact`, `JoinedReportBlock`; this diff changes none (view.zod.ts hunk is `:6309-6327` only). The changeset states `ViewMetadataParsed` is unchanged and still `unknown`. #19920 is `pm:blocked` Blocked-by #19871 with triage saying do not fold it into #19919; honoured.\n\n### ② Semver level\n\n- `.changeset/view-metadata-type-not-unknown.md`: `'@objectstack/spec': minor`; line-leading `Clause-②: no (narrowing)`; a BREAKING banner naming the affected party (TypeScript code that annotates with `ViewMetadata`); one adr-0087 HTML-comment marker `not-required (no-migration-prescription)` with its why. The PR body carries the same line-leading `Clause-②: no (narrowing)`; the claim's provisional `Clause-②: no` is declared superseded.\n- AGENTS.md `:1084-1085`: declaration is `yes|no` plus at most one arm; `(narrowing)` is BREAKING; `no (widening)` malformed. `no (narrowing)` is legal and honest here: no new key on a published payload (runtime accept set unchanged), a published TYPE narrows. Level: `scripts/check-changeset-no-major.mjs` header (launch window: \"a breaking change ships as minor … the mandatory information carriers are the BREAKING banner and the ADR-0087 disposition\") makes `minor` correct; `patch` would misgrade a breaking change, `major` is refused.\n- Disposition: `type-surface-only` (ADR-0087 addendum 2026-08-30, `docs/adr/0087-…md:717-871`) is closed by predicate 2 (diff touches `packages/spec/**`) and predicate 3 (a `*.zod.ts` moved); the addendum's closing paragraph itself says a spec-bumping case \"is not a member of this class\". `registered` would put a prescription in a ledger `objectstack migrate meta` cannot project. `no-migration-prescription` is the remaining honest category, and the marker says why. Nothing authorable is removed or renamed, so no FROM to TO block is owed.\n- CI at head, `Check Changeset` job 107341251773 log: `check-adr-0087-registration: 1 declared-breaking changeset(s) … [BREAKING+clause-②-narrowing] not-required (no-migration-prescription)`; `check-changeset-no-major`: \"This diff introduces no major bump\" and `LEVEL AXIS: this PR declares clause-② no (narrowing) … direction arm: narrowing — a BREAKING change; during the launch window it ships minor`; `check-empty-changeset`: no merge-base changeset modified or deleted.\n- Judgment: correctly declared at every layer.\n\n### ③ Boundary flags\n\nBLOCKING\n- Published prose made false and not corrected: `packages/metadata/README.md:77` at refs/review/pr-19919 still reads \"`view` (its `ViewMetadata` type is `unknown`)\". `packages/metadata/package.json` `files` is `[\"dist\",\"README.md\",\"CHANGELOG.md\"]`, so this sentence ships in the `@objectstack/metadata` tarball; after this PR the parenthetical is false, and it steers a consumer toward exactly the assumption the changeset's BREAKING banner warns against. The PR corrected the same sentence's two siblings (`typescript-serializer.ts:27-36` comment, `typescript-serializer-annotation.test.ts:15` docblock) but not the published one; the docs-drift bot on the PR warned that pages documenting `typescript-serializer.ts` were NOT COVERED by its run. Remedy is one clause (the reason `view` stays unannotated is that `ViewMetadata` is no longer the bound schema's `z.input`, as the serializer comment now says); comment-only for `@objectstack/metadata`, no second changeset needed.\n\nNon-blocking\n- Files outside the claim's named surface: `packages/metadata/src/serializers/typescript-serializer.ts` (comment only) and its `-annotation.test.ts` (one docblock word). Declared as deviations in the os-dev report; the claim anticipated the serializer comment \"becomes a reader of the fix\". No runtime change; metadata typecheck and tests green.\n- `.changeset/19852-typescript-serializer-per-type-annotation.md:11` (pending on origin/main, compiles into the same CHANGELOG release) still says \"`ViewMetadata` is `unknown`\". Editing another PR's pending changeset is the route-0 \"deliberate correction\" class in the `Check Changeset` step (written confirmation, check stays red), so leaving it is defensible; the two entries will contradict each other in one release note unless the maintainer rules otherwise. A decision, not a defect.\n- The card's second half (\"add the case to whatever gate is meant to catch exported types that collapse to unknown\") and triage's same direction have no carrier: the PR's acceptance notes say \"carrier: none\", #19920 only mentions the census as a possible gate. `check:exported-any` reads ZodType-of-any annotations only (AGENTS.md `:750-753`; consumer-gates step \"Check no exported spec type resolves to any\" green while the four #19920 sites still resolve to unknown). Worth its own card.\n- The dispatch expected `check:api-surface` to record the change; measured, it cannot (①.6). The PR states this rather than hand-editing a baseline. Correct handling.\n- `Console Pin Gate` skipped at head by path filter, so \"pinned sibling still compiles\" rests on grep evidence, not a build (①.5).\n- Declared process deviations: the empty-branch probe push was a bare `git push`, not via write-pace; `origin/main` had moved 2 commits at open and was not merged (now 10+, none touching `packages/spec` or `packages/metadata`; PR reads `mergeable: true, clean`). Neither affects the diff.\n- Commit trailers on `aa5156d9de…` and `2f378ec5cf…` carry `Claude-Session:` and the model-free `Co-authored-by: Claude` pair; no model identifier in title, body, changeset or comments. PR is `draft: true`, no auto-merge, no dev-written labels: consistent with the governed-surface stop rule.\n- Nit: the PR body cites objectui `origin/main` as `544ecba`; my fetched objectui `origin/main` is `62597c58…` (the pin). Zero references either way; not weight-bearing.\n\nImplemented-by: `claude/issue-19871-view-metadata-type`\nReviewed-by: `session_019c3Hi6ZMU1p6m6aA6Bz45d`\n\n**VERDICT: FAIL** — one blocking item: `packages/metadata/README.md:77` ships a sentence this PR makes false (`ViewMetadata` is no longer `unknown`). Fix that clause; every other judgment above is PASS-grade.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5815765984,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-24T14:11:40Z","updated_at":"2026-09-24T14:11:40Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19919#issuecomment-5815765984","body":"## Contract review\n\nServed-tier: 67/67 `CONTRACT_REVIEW_TIER`\nHead-sha: `3cb0a8f6a226175eb523a9b4a60d0fb00d25a10c`\n\nIsolated at-tier reviewer subagent, run by the `domain:spec` seat-4 session; every one of its 67 transcript turns served at the tier the constant names. Re-review after the takeover patch round (the maintainer's 「你接手派补丁轮」); it supersedes the FAIL record `5808373817`. Adopted by the seat 2026-09-24T14:11Z. The record below is the reviewer's, unedited except the two header lines.\n\n\n### ① Derived judgments\n\n1. The diff, verified at the ref. `git diff --stat 8490127962 refs/review/pr-19919` (merge-base to head): 6 files, +149/−7, identical to `pulls/19919/files`. The patch round `2f378ec5cf..3cb0a8f6a2` is two fast-forward commits (`7ca39fe366` README, `3cb0a8f6a2` changeset) touching only `packages/metadata/README.md` (+1/−1) and `.changeset/view-metadata-type-not-unknown.md` (+5/−0): exactly the takeover claim's surface (`5814360296`), no code moved. The one declaration is unchanged from the prior head: `packages/spec/src/ui/view.zod.ts:6327` `export type ViewMetadata = z.input over (typeof VIEW_METADATA_MEMBERS)[ViewMetadataBranch]`; `ViewMetadataParsed` (`:6329`) is a context line; `ViewMetadataSchema` (`:6020-6037`), `VIEW_METADATA_MEMBERS` (`:5615-5624`) and the preprocess (`:6029`) are untouched. RIGHT against the card, which asked for \"the input of the post-preprocess union (the members the union runs)\".\n\n2. Derivable and honest. `VIEW_METADATA_MEMBERS` is `as const satisfies Record of ViewMetadataBranch to z.ZodTypeAny` (`:5624`), so `satisfies` keeps each value's concrete type; `lazySchema` is typed `T extends z.ZodTypeAny, factory returning T, returns T` (`packages/spec/src/shared/lazy-schema.ts:21`); the schema maps the same record (`:6026`), so the member list and the type cannot drift. The members at head: `viewItem` = `ViewItemWireSchema`, a discriminated union of two `.strip()` objects (`:5089-5100`); `container` = `ViewSchema.refine(containerHasAView)` (`:5539`) over a `strictObject` (`:4695`); `listOverlay` = `ListViewShapeSchema.extend(...).strip().superRefine(...)` (`:5573-5574`); `formOverlay` = `FormViewSchema.extend(...).strip()` (`:5589`). So the TSDoc's and changeset's \"three members strip undeclared top-level keys\" is TRUE (three strip, the container is strict), and the serializer comment's \"the preprocess removes the console's row ids before any member judges the body\" is TRUE (`:6029`; the code comment at `:5171-5177` says the nested `filter[].id` / `sort[].id` are removed by `stripViewConsoleDecorations` BEFORE the union runs).\n\n3. The new README sentence is TRUE and consistent. `packages/metadata/README.md:77` at head: \"`view` (`ViewMetadataSchema` is a `z.preprocess`, whose `z.input` type is `unknown`; `ViewMetadata` is declared instead as the union of the input types of the members that schema's union runs, so it is not that schema's `z.input` type)\". Clause by clause: `ViewMetadataSchema = lazySchema(() => z.preprocess(...))` (`:6020-6028`) and `lazySchema` returns `T`, so its `z.input` is the preprocess's, `unknown`; `ViewMetadata` reads `VIEW_METADATA_MEMBERS` (`:6327`), so it is not that schema's `z.input`. It fits the paragraph's own rule (\"Each is exactly the `z.input` type of the schema `getMetadataTypeSchema()` resolves... A metadata type with no such spec type is written with no annotation\"): `getMetadataTypeSchema` binds `view: ViewMetadataSchema` (`packages/spec/src/kernel/metadata-type-schemas.ts:110`). It says the same thing in the same words as the TSDoc (`view.zod.ts:6310-6326`: \"the union of the INPUT types of the members ViewMetadataSchema's union runs... Deliberately NOT z.input of ViewMetadataSchema. That schema is a z.preprocess, whose input type is unknown\") and the serializer comment (`typescript-serializer.ts:30-36`, under the table's rule at `:18-22`: listed only when the spec type IS the bound schema's input type). The README makes no claim about the runtime accept set; the serializer comment does, and that claim holds (item 2). The prior blocking item is cleared.\n\n4. Pin is weight-bearing and CI-judged at this head. `packages/spec/src/ui/view-metadata-type.test.ts:61/:63/:65` carry the three `@ts-expect-error` directives; `tsconfig.test.json:45` `include: [\"src/**/*\"]`; the ledger `packages/spec/test-typecheck-debt.json` has no `view-metadata-type` entry (grep exit 1) and is byte-identical between `origin/main` and head; `scripts/check-test-typecheck.mts:425` reds on \"type error(s) in a file the ledger does not cover\", so an unused directive (TS2578) fails the gate. Chain: `Type Check · workspace` runs `turbo run typecheck --filter='./packages/*' ...` (`.github/workflows/lint.yml:6029-6030`); spec's `typecheck` is `tsc --noEmit && pnpm check:scripts-typecheck && pnpm check:test-typecheck` (`packages/spec/package.json`). Check-run 107640315088 at head: success. The job log was not readable through this session's proxy (CONNECT 403 on the log host), so the reading rests on workflow text + script text + the check-run conclusion; the ablation control (3x TS2578 on revert) is the author's and is consistent with that chain.\n\n5. Consumers. `git grep -n -w ViewMetadata refs/review/pr-19919` outside `view.zod.ts`: the new test, the serializer comment (`:31,:34`), the annotation test docblock (`:15`, \"once was\"), the two changesets, `README.md:77`, and the baseline rows `api-surface/ui.json:428`, `export-origins/ui.json:414`. No annotation consumer. objectui at the pin (`.objectui-sha` = `62597c588072636e9c30ea35b3d89b1e46fd765d`, identical at `origin/main` and head): `git grep -w ViewMetadata` exit 1, `ViewMetadataParsed` exit 1; control grep `ViewMetadataSchema|VIEW_METADATA_MEMBERS|diagnoseViewMetadata` hits 11 files, so the negative is valid. objectui `origin/main` is that same sha in my fetch. `Console Pin Gate` skipped at head (path filter); nothing there can break.\n\n6. Generated baselines: `api-surface/ui.json:428` \"ViewMetadata (type)\" identical at both refs; `git diff --stat origin/main refs/review/pr-19919 -- api-surface export-origins declaration-map test-typecheck-debt.json` is empty. AGENTS.md `:750`: the snapshot records that an export exists, never what it resolves to. `Type Check · consumer gates` (107640314963) success. The changeset remains the only record of the narrowing, as the PR states.\n\n7. #19920 collision: none. The `view.zod.ts` hunk is `:6309-6327` only. `origin/main` (`2c1011b01`) is 24 commits past the merge-base; the one that touches `view.zod.ts` (`f5a7250b7`, #19917) has hunks at `:1015`, `:1664`, `:2706`. `git merge-tree --write-tree origin/main refs/review/pr-19919` exits 0 (tree `22675b15a7`) with the new declaration at `:6335` of the merged file; PR reads `mergeable: true, clean`. CI at head ran on the merge commit `5763b14fd6` (docs-drift bot: merge of head into base `2c1011b01`), so the 35 green check-runs cover the merged tree.\n\n### ② Semver level\n\n- `.changeset/view-metadata-type-not-unknown.md` at head: `'@objectstack/spec': minor`; line-leading `Clause-②: no (narrowing)`; BREAKING banner naming the affected party; exactly one adr-0087 HTML-comment marker `not-required (no-migration-prescription)` with its why. Level, arm, banner and marker are byte-unchanged from the prior head; the patch added one paragraph (`:35-38`). The PR body carries the same line-leading `Clause-②: no (narrowing)`.\n- AGENTS.md `:1084-1085`: `(narrowing)` is BREAKING, `no (widening)` malformed; `no (narrowing)` is legal and honest here (runtime accept set unchanged, a published TYPE narrows). `scripts/check-changeset-no-major.mjs:47-51`: during the launch window a breaking change ships as `minor`, `major` is refused. ADR-0087 `docs/adr/0087-metadata-protocol-upgrade-contract.md:784-790`: `type-surface-only` predicate 2 (diff must not touch `packages/spec/**`) and predicate 3 (no `*.zod.ts` moved) both close it; `no-migration-prescription` is the honest remaining category and nothing authorable is removed or renamed, so no FROM to TO block is owed.\n- CI at head: `Check Changeset` (107640314037) success; its annotation on the changeset file reads \"ADR-0087 exemption (no-migration-prescription): Nothing authorable is removed, renamed or re-typed at runtime...\". `pr-automation.yml:899-900, 939-940, 1075` run `check-empty-changeset`, `check-adr-0087-registration` and `check-changeset-no-major` against the merge base.\n- No second changeset for `@objectstack/metadata`: confirmed against the rule text, with the prior record's reason corrected. AGENTS.md `:1081-1084` (\"Add a changeset for anything that publishes. Feature, functional improvement or fix... A bug fix in a released package takes a patch changeset\"); `.claude/agents/os-dev.md:298` (published = what each package's `files[]` ships), `:299` (comments do not publish); `.claude/skills/pm-dispatch/references/review-checklist.md:20` (docs-only takes the `skip-changeset` label in this repo). Measured: `packages/metadata/package.json` `files` = `[\"dist\",\"README.md\",\"CHANGELOG.md\"]`, so the README edit PUBLISHES; the prior record's \"comment-only\" was wrong and the dev's refutation is right. Even so, no second changeset is owed: (a) the PR carries a changeset and `.changeset/config.json` puts `@objectstack/spec` and `@objectstack/metadata` in the same `fixed` array, so metadata releases at the same version and the corrected README ships in that release; (b) the false clause never shipped: it came in with `e9eb2244d` (#19865, 2026-09-23), whose metadata patch changeset `19852-typescript-serializer-per-type-annotation.md` is still pending on `origin/main`, and the published `@objectstack/metadata@17.4.0` README (npm view, my own read) has 0 `ViewMetadata` hits with 1 `TypeScriptSerializer` hit as control; (c) the edit is neither a feature nor a bug fix in metadata: it corrects prose that this PR's own spec change falsifies, and that change is what this PR's changeset records. Judgment: correctly declared at every layer.\n\n### ③ Boundary flags\n\nNon-blocking\n- PR body not updated for the patch round: `updated_at` 2026-09-24T12:53:29Z precedes the dev report (13:31:44Z); the body names neither `README.md`, nor the head `3cb0a8f6a`, nor a patch-round section; \"What changed\" lists 4 bullets for 6 files and \"Local verification\" is at `2f378ec5cf`. Every claim in it stays true (no code moved), the takeover claim says \"PR body edits are the seat's\", and the dev report supplies the text (A)(B)(C). No AGENTS.md rule makes a stale body a gate. The seat's step before landing.\n- `.changeset/19852-typescript-serializer-per-type-annotation.md:11` on `origin/main` still says \"`ViewMetadata` is `unknown`\" and is correctly NOT edited here: editing it is the DELIBERATE CORRECTION class (`scripts/check-empty-changeset.mjs:126-129`; `pr-automation.yml:718-727`: written confirmation, the check stays red). The patch instead added a supersession sentence to this PR's spec changeset (`:35-38`); its claims verify (the #19852 note gives that reason; `getMetadataTypeSchema('view')` binds `ViewMetadataSchema`). AGENTS.md `:697`'s \"never an erratum in a later entry\" concerns released entries and #19852 is unreleased, so this is a cross-reference, not an erratum. The metadata CHANGELOG will still carry the false sentence in the release that consumes both notes unless the maintainer rules to correct the pending note (or to add a metadata line). Same decision item as before; not a defect.\n- Card's second half (a gate for exported types that collapse to `unknown`): still no carrier; the dev report says \"carrier: the domain:spec seat, noted, not filed\"; #19920 only mentions the census. Status unchanged; worth its own card.\n- `check:api-surface` cannot record the narrowing (①.6): unchanged, correctly handled by stating it rather than hand-editing a baseline.\n- `Console Pin Gate` skipped at head by path filter; the sibling reading rests on grep evidence (①.5). Unchanged.\n- Files outside the original claim's named surface (serializer comment, test docblock, README): each declared, the README one inside the takeover claim's surface; the patch round changed only the two files that claim names.\n- Process: the patch pushes were fast-forward (`2f378ec5c..3cb0a8f6a` in the fetch), through write-pace per the report; no force-push; `origin/main` not merged (24 commits, merge clean, ①.7).\n- Hygiene: both patch commits carry `Claude-Session:` and the model-free `Co-authored-by: Claude` trailer; grep for model names over the changeset, README, test, serializer, PR title and body is empty. PR `draft: true`, no auto-merge; labels are Auto Label's (`documentation`, `size/m`, `tests`, `tooling`, `protocol:ui`).\n- Docs drift at head: the bot says `README.md` and `typescript-serializer.ts` yield no anchor. Hand re-read: `content/docs`, `skills` and the other package READMEs have no `TypeScriptSerializer`, \"no annotation and no import\" or `ViewMetadata` prose (grep empty), so no other page restates the clause.\n- Prior nit on the objectui `origin/main` sha: my fetched objectui `origin/main` equals the pin `62597c58`; the PR body's `544ecba` stands as the author's reading. Zero references either way; not weight-bearing.\n- CI job logs were not fetchable through this session's proxy; check-run conclusions, annotations and the workflow/script text were used instead (①.4, ②).\n\nImplemented-by: `claude/issue-19871-view-metadata-type`\nReviewed-by: `session_019c3Hi6ZMU1p6m6aA6Bz45d`\n\n**VERDICT: PASS**\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
