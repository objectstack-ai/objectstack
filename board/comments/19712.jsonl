{"id":5774727539,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-22T10:13:03Z","updated_at":"2026-09-22T10:13:03Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19712#issuecomment-5774727539","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `d953fabbd38cc2e8298385a1a4919e5d8c4fa680`\n\n### ① Derived judgments\n\nRendered in-seat by the skills seat 1 session at the served tier the maintainer named on this date (the fable tier word at `get_session` → `external_metadata.last_served_model`; ruling record 5771798588; the constant on `origin/main` names it since PR #19684). The contract is the card's measured defect (`node` absent from `RESTART_WHEN_COMMAND_HEADS`, so a runnable `node -e` predicate classifies `prose` while its `pnpm exec node -e` re-heading classifies `command`), the lane's grading 5738993813 (direction ①: `node` joins the list, the docblock states what the head word can and cannot tell, the `pnpm exec` prefix is not sanctioned; direction ②: the H9 row names the channel it read) and the seat's dispatch, which scoped this PR to direction ①.\n\n- **(a) The fix is the card's, measured by this seat on both trees.** `RESTART_WHEN_COMMAND_HEADS` reads `git|node|npm|npx|pnpm` on the head; the seat's probe (`scratchpad/seat/probe-19712.mjs`, ten rows) reproduces the card's table on the base (`node -e …` → `prose`, `pnpm exec node -e …` → `command`, the three controls lit) and on the head the subject reads `command` with every control unchanged: `git grep …` → `command`, `closed …#19049` → `closed-ref`, prose → `prose`, and the closed-list controls `bash …`, `nodemon …`, `node_modules/.bin/…` → `prose`. The head set stays closed — `bash`, `sh`, `gradle` are pinned as `prose`, each with its reason in the docblock. the head match lower-cases and trims the value before it reads the first word — measured on the BASE with `PNPM …`, a leading-space `pnpm …` and `Git …`, all `command` there already, so the `NODE …` / leading-space `node …` readings on the head are that pre-existing rule applied to the new head, ⛔ not this PR's widening.\n- **(b) The docblock says what the grading asked.** The `node` row carries its provenance (objectui#9868's live hold, read 2026-09-18); the new paragraph states that the head word can tell only whether some mechanism could run the line, not whether running it means anything (`node -e \"process.exit(0)\"` fires and answers nothing), and records the `pnpm exec` re-heading as a WORKAROUND for a hole in the list, ⛔ not a sanctioned spelling — the grading's direction ① verbatim in substance.\n- **(c) Pins — a declared battery, floors up.** `H9 node command head` registered on the frozen roster with pin 9 (10 cases: the heads list, the starred subject, the re-headed twin, the fireable controls, the `prose` control, the closed-set control, the whole-word / `manual` control, the row in both channels, the gathering policy, the floor); `SELF_TEST_BATTERY_FLOOR` 8 → 9 with the five sibling floor assertions (H18901, H18664, H19255, H67, H68) moved with it. Seat's run: `check:pm-half-states` 5170 cases pass (5160 on the base), `H9 node command head 10/9`. The dev's ablation (`node` removed from the list ⇒ 5 of 5170 red — the subject pins, the heads-list pins and the gathering-policy pin — every control green; blob restored `6d9f2f36aef9`, the blob at this head) reads as reported.\n- **(d) Direction ② is NOT in this PR — and that is the seat's doing, carried on the card.** The dispatch's Zone 1 and the claim's file surface scoped the PR to the command head; the grading had written ② as 「same function, same PR」. The dev measured the premise: `h9OnHoldNoRestartWhen` receives comment BODIES as strings, the ids never reach it, and naming the channel changes the shared comment cache's shape and the signature at roughly thirty pinned call sites across three rows. That is a separate change, not a sentence. Under the filing door this seat files no card, so ② stays on THIS card as its remainder: the seat PATCHes the PR body's first line to `Part of #19129`, and at MERGED the card returns to `pm:queue` with a `Release:` line naming ② and the dev's measurement of its cost, for the lane to take or the maintainer to retire. The dev's open question is thereby answered in-lane (its option A without the card it could not file); ⛔ not a decision-box item.\n- **(e) Gates — measured by this seat on a clean worktree at `d953fabbd` after `pnpm install --frozen-lockfile --offline`.** `check:pm-half-states`, `check:nul-bytes`, `check-self-test-wired`, `check-scripts-symbol-anchors` — exit 0; eslint over the file 1 judged, 0 / 0; no raw control byte; `check-governed-merges.mjs --pr 19712` exit 0 — NOT governed, 0 of 1 path on the register, 62 lines. The dev's 42-family derivation reconciled `--ran` 42 / 42 / 0 NOT-MEASURED, repo-wide `pnpm lint` exit 0, `check:published-readme-exports` exit 3 recorded NOT MEASURED (no dist; nothing built) — read as reported. One file, +54 / −8 (net +46 against Zone 3's optional ≤ +30: the battery and the docblock — declared, accepted); two commits (the fix and the `origin/main` merge at `1d41aa885`); trailers model-free per `check:commit-card-trailers`. CI at this head: 31 latest-per-name check runs, four in progress at the seat's read (`Test Core (1/6)`, `Lint & Repo Gates`, `Type Check · consumer gates`, `Live half-state sweep`), the rest success or skipped.\n\n### ② Semver level\n\nNone — no release. `scripts/pm/**` is in no package's `files[]` (the dev measured 75 manifests, none naming `scripts`); `skip-changeset` is the correct carrier, written through `label-write.mjs` with read-back; `size/s` is the labeler's.\n\n### ③ Boundary flags\n\n- **Not governed** — ordinary queue landing by the seat on all green and `--pair` 0, after the body PATCH below.\n- **The seat's writes on this PR**: this record; one body PATCH (`Fixes #19129` → `Part of #19129`, the remainder stated) — announced here; the dev made ⛔ no body PATCH. No 维护者速读 — not owed on a non-governed path.\n- **Report** 5774... on the card parses (`os-dev-report` first line); `mcp_calls` 0; four REST writes including the pushes, as budgeted. Writes went through the REST proxy because the fleet-gate variables are absent in this container — as dispatched.\n- **Serial**: the grading's holds (#18862 fold, #18901) are on the base already (H68 is in the file); no open PR touches `check-half-states.mjs` at the seat's claim-time read.\n\nImplemented-by: `claude/issue-19129-half-states-h9-node-command-head`\nReviewed-by: `session_01Wnstp2kTth7sGXfr8fXypc`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
