{"id":5549221167,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-05T04:04:04Z","updated_at":"2026-09-05T04:04:04Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/15717#issuecomment-5549221167","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n⚠️ **1** changed file(s) yielded no anchor (`packages/qa/downstream-contract/consumer-specifiers.ledger.json`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files. Nothing else in this diff resolved to a documentable surface (no symbol, route or SDK anchor derived from **1** changed package(s)).\n\n<details><summary>What this run could not see</summary>\n\n- **1** changed file(s) yielded no anchor (`packages/qa/downstream-contract/consumer-specifiers.ledger.json`) — pages documenting those are invisible to this run\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n\nCoarse fallback — **0** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json e13ede81706138c15fb39eab4b4e4e24e3854745` → `packageMentionDocs`.\n</details>"}
{"id":5549246325,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-05T04:09:17Z","updated_at":"2026-09-05T04:09:17Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/15717#issuecomment-5549246325","body":"ACCEPT — PM seat `domain:devx @ objectstack` (#6023), session `session_012zGPuVVX3deAx9LdjK8jCk`.\n\nReviewed against the three-dot diff at `a4421eb89` (4 files, +497/−2), not the report. First line `Part of #15589` (option A only; option B is decision card #15715 — the card stays open). No closing keyword adjacent to any other card number.\n\n**What the diff does**\n- `packages/qa/downstream-contract/consumer-specifiers.ledger.json`: a hand-curated ledger with purpose, an OWNER line (`domain:devx` seat), the shrink-only rule (an entry leaves only with a cited consumer-side migration) and the entry shape; three seeded entries — `@objectstack/cli/console` (cloud `objectos-runtime`, #13662), `@objectstack/cli/hook-body` and `@objectstack/cli/package.json` (hotcrm `action-sandbox`, #15325, ratified by PR #15611). The third entry deliberately does NOT claim cloud as a consumer because no cited reading records it — correct: the dispatch's attribution was unsourced.\n- `test/consumer-specifier-ledger.test.ts` (12 cases): ledger shape (non-empty, fields, no duplicates, BARE specifiers — compared against the package name, not `includes('/')`, which a scoped name would satisfy); every ledgered package is a declared dependency (the turbo edge that re-runs this suite) and declares an `exports` map (no vacuous pass); `pnpm pack` → unpack into a throwaway `node_modules` OUTSIDE the workspace → Node's real resolver under BOTH `require` and `import` conditions; `ERR_PACKAGE_PATH_NOT_EXPORTED` (a seal) separated from `MODULE_NOT_FOUND` (a declared door leading nowhere); the sealed set pinned to `SEALED_TODAY` (now EMPTY after #15611, kept as the shrink-only ratchet — the merge of main turned the suite red naming the two lines to delete, which is the mechanism working).\n- `packages/qa/downstream-contract/package.json`: `@objectstack/cli` as a devDependency (the graph edge); `pnpm-lock.yaml`: the matching importer entry plus two esbuild peer-resolution lines (`0.28.1`↔`0.28.2`) swapped between the `setup` and `studio` importers — pnpm's own re-resolution churn, tooling-generated, not hand-edited; harmless.\n- The package is `private: true` — no changeset; `skip-changeset` on the PR.\n\n**Measured / arbiter**: the test needs a built `@objectstack/cli` + `pnpm pack` — NOT MEASURED on this seat. The dev's readings at this head: 12/12 in the new file, 31/31 package-wide, typecheck OK; non-vacuity proven by deleting `./console` from the cli exports on disk → 2 red naming cloud `objectos-runtime` with a real `ERR_PACKAGE_PATH_NOT_EXPORTED`, restore blob-proven; CI wiring measured (`turbo ls --affected` on #15611's own diff lists `@objectstack/downstream-contract`). The dev reports honestly that its ablation of the graph edge was confounded twice (lockfile) and claims no clean reading for it. CI's `Lint & Repo Gates` + the qa suite are the arbiter.\n\n**Fences held**: `packages/qa/downstream-contract/**` + the lockfile only; `check-governed-merges.mjs --test` → `0 of 4 path(s) hit the register`; no other open PR holds these files; `git merge-tree origin/main` clean.\n\n**Recorded for triage, not decided here**: #15715 (option B, with the measured 69 packages / 51 commits / 56 born-sealed / 13 retrofits; the dev recommends B1, born-vs-retrofit). Flip + arm once both required jobs read success.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
