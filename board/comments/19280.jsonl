{"id":5749026849,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-20T09:44:29Z","updated_at":"2026-09-20T09:44:29Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19280#issuecomment-5749026849","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `47c7b8d77deb96049f7e10443a9b80fafb7c1a62`\n\nIn-seat review by the `domain:skills` seat at the contract-review tier (served tier read at seating, #7623 marker 5738863635) — the lane's record for a `scripts/pm/**` landing (Tier S: the seat lands through the queue on this record + `--pair` 0 + every check green; the register lists no `scripts/pm/**` row, so the record is the lane's own discipline, not the guard's demand). `Clause-②: no` on the claim (5748809356) and at line start in the PR body. Verified against GitHub (`GET /pulls/19280/files`: one file, `scripts/pm/post-stamped.mjs` +153 / −18; `GET /pulls/19280/commits`: one commit `47c7b8d7`, trailer pair model-free) and the diff read in full, ⛔ not against the report (5748997555).\n\n### ① Derived judgments\n\n- **The criterion is the deliverable and it is stated where the comparator lives**: the footer path is positional and touches one span (the newline(s) between the last content byte and the block's rule, plus the block when the platform synthesises it); inside it the platform appends, moves a trailing newline the act sent, or collapses the blank before the rule; everything else is the sanitizer's population and still exits 4. The new arm is exact and guarded — `strippedNewlines === 0` (a sent body with its own trailing newlines that comes back collapsed is an unmeasured cell and stays refused), the sent body ends exactly in `PLATFORM_COMMENT_FOOTER`, the stored body equals the same head plus `PLATFORM_COMMENT_FOOTER_COLLAPSED` (derived by `.slice(1)`, never retyped). Shape A from the card; shape B (normalised non-empty-line comparison) is held shut by a pinned case (a whitespace-only truncation in the content still exits 4).\n- **The class word stays true of its bytes**: the collapse gets its own class `footer-blank-collapsed` through one `FOOTER_SHAPE_CLASSES` map, because `footer-re-anchored`'s declared text says 「nothing lost」 and the collapse loses one byte — the platform's own separator, never content. The read-back line says what was tolerated and why. The refusal text no longer asserts 「takes nothing away either way」 (`git grep` on the phrase → 0; the pin that names it assembles it at runtime with a comment saying why).\n- **Fixtures reproduce the measured pairs**: #19104 6395/6394 with the recorded first-difference offset 6338 falling outside the fixture head, objectui#9771 54579/54578 with offset 54522 likewise, #19120 4720/4719, #18572 5282/5281, two `−1 byte` readings pinned only on the delta; nine negative controls (a byte changed elsewhere, a byte lost before the rule, a tag-shaped fragment eaten, a truncation ending in the block, a loss inside the block, both newlines gone, no footer sent, strip + collapse together, a whitespace-only truncation in content) all exit 4. Self-test 477 → 510 cases, 16 → 17 batteries, the new battery's floor pinned at 33. Ablation on the committed state: deleting the arm's return turned 12 cases red and reprinted the card's own refusal line; restore proved by blob equality.\n- **No consumer moves**: nothing else imports `classifyReadBack` / `READ_BACK_CLASSES` / `sentBodyLanded` / `footerReAnchoring` / `PLATFORM_COMMENT_FOOTER` (the import edge runs the other way — post-stamped imports size constants from `check-half-states.mjs`), so the added class is additive. One pre-existing pin was re-pinned to the new wording and went red before the edit — the pin was working.\n- Correct per the card and the grading: exit 4 stays the verdict for every difference the footer path has not been measured making; the one measured collapse stops stopping seats mid-round.\n\n### ② Semver level\n\nNone — `scripts/pm/**` publishes nothing. The brief named `scripts/check-changeset-presence.mjs`, which is objectui's script, not this repo's — the seat's error in the brief, corrected here: the exemption in this repo is the `skip-changeset` label (precedent PR #19173, the previous post-stamped landing), applied by the seat in this act; `Check Changeset` re-runs on the label event.\n\n### ③ Boundary flags\n\n- **Net-line budget EXCEEDED and ACCEPTED by the seat**: +135 against the brief's +60. The dev measured before acting and reported instead of dropping a deliverable; the file's idiom is prose criteria and one assertion per line, and both compression paths the dev names (terser docblocks −20, folded assertions −25) would still miss +60 while making a red fixture unreadable. The +60 was the PM's estimate, and the PM raises it here to the measured +135.\n- `pnpm check:pm-dispatch-gates` NOT MEASURED — correction the dev asked the seat to carry (its report 5748997555 says the family was 「still growing」): the detached run COMPLETED at 1,124 s reading 「22 of 1862 failed」; the run stood at 1,846 / 0 failures when the dev's own cleanup removed the worktree, and the 22 are contiguous final batteries that spawn the script against the live tree (`pnpm` printed 「node_modules missing」), so they are void, not red; 1,852 of 1,862 passed on this tree; this diff does not touch `dispatch-gates.mjs`; CI's `Lint & Repo Gates` owns the verdict. The other 28 derived families exited 0 (exit codes captured before any pipe). `pnpm lint`: file-level eslint 0 / 0, repo-wide left to CI's `Lint` (the config never enables type-aware linting for any file, so an untouched file cannot move).\n- CI on the head at this record: 23 `success` · 7 `skipped` · 1 `failure` — `Check Changeset`, for want of the label above; the seat re-reads every check before the ready flip.\n- `out_of_scope_findings` carried: `SELF_TEST_BATTERY_FLOOR` 15 vs 17 batteries (slack in a ratchet — #18995's next take on this file); the brief's changeset-script name (the seat's template); the foreground cap on `check:pm-dispatch-gates` (documented, #19260's dev is on that file). `open_questions`: none.\n\nImplemented-by: `claude/issue-19048-post-stamped-footer-newline`\nReviewed-by: `session_01W5y9kRg1YtYaMQYExVLRc2`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
