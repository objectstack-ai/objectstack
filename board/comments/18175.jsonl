{"id":5662586057,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-14T10:31:17Z","updated_at":"2026-09-14T12:31:29Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18175#issuecomment-5662586057","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\nThis PR changes **2** package(s): `@objectstack/service-automation`, `@objectstack/spec`, touching **13** documentable anchor(s). ⚠️ **1** changed file(s) yielded no anchor (`packages/spec/authorable-defaults/automation.json`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files.\n\n**2** hand-written doc(s) NAME something this change touched and may need an implementation-accuracy re-verification:\n\n- `content/docs/automation/flows.mdx`  _(via boundaryConfig (literal, a string literal in requireTypeScopedConfig), boundary_event (literal, a string literal in flowNodeObject; a string literal in requireTypeScopedConfig), timerDuration (literal, a string literal in flowNodeObject), waitEventConfig (literal, a string literal in requireTypeScopedConfig))_\n- `content/docs/deployment/cli.mdx`  _(via api_call (literal, a string literal in requireTypeScopedConfig))_\n\n⛔ **1** release-owned page(s) also name something this change touched. These are **read-only**:\n\n- `content/docs/releases/v17/17-0.mdx`  _(via FlowNodeSchema (symbol, a top-level const), timerDuration (literal, a string literal in flowNodeObject), waitEventConfig (literal, a string literal in requireTypeScopedConfig))_\n\n> `content/docs/releases/` is RELEASE-OWNED (AGENTS.md \"Documentation Guardrails\"): release\n> notes are written centrally at release time, and a code PR that edits them is the exact PR\n> that guardrail exists to stop. They are still audited — read-only. If one of them is actually\n> wrong, **file an issue** or open a dedicated docs-only PR; do not edit it here.\n\n<details><summary>What this run could not see</summary>\n\n- **1** changed file(s) yielded no anchor (`packages/spec/authorable-defaults/automation.json`) — pages documenting those are invisible to this run\n- **4** name(s) were too generic to anchor anything (single lowercase words)\n- the SDK route bridge reached **60** of **215** client-bound route-ledger rows — the other **155** have no registrar `path:` tail to select them, so pages documenting THEIR client methods cannot appear above, on this or any run. Of those **155**: **0** are remediable by widening that discovery convention (an in-repo file declares the path; the convention did not scan it); **55** are structural — on a ledger where NOT ONE row is declared in-repo, so no discovery change reaches them at any price; **100** are undecided (no in-repo declaration, on a ledger that has other in-repo registrars — absence and an unreadable spelling are not distinguishable here). The rows themselves: `node scripts/docs-audit/affected-docs.mjs --bridge-coverage`\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n\nCoarse fallback — **136** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json ba9b4981cd6a111af47bc71741106467ef8f8359` → `packageMentionDocs`.\n</details>\n\n<details><summary>Which tree this was computed on</summary>\n\nThis run read `content/docs` from `d472ec14d5aa9caaa0fddca080bfa5c15c0a7910` — the merge of head `c6a1998caacba9bffa738bdc137c78acce2c6dd2` into base `ba9b4981cd6a111af47bc71741106467ef8f8359`, which is what `actions/checkout` gives a `pull_request` run. **Not** the PR head.\n\nA worktree cut from an older `main` holds a different `content/docs`, so re-deriving there can legitimately return a different list — that is a **different tree, not a wrong row**. To answer on the same tree:\n\n```sh\n# while this PR is open — GitHub drops the merge commit once it closes\ngit fetch origin d472ec14d5aa9caaa0fddca080bfa5c15c0a7910 && git checkout d472ec14d5aa9caaa0fddca080bfa5c15c0a7910\n# afterwards, rebuild it from the two parents, which stay fetchable\ngit fetch origin ba9b4981cd6a111af47bc71741106467ef8f8359 c6a1998caacba9bffa738bdc137c78acce2c6dd2 && git checkout -B drift-repro ba9b4981cd6a111af47bc71741106467ef8f8359 && git merge --no-ff c6a1998caacba9bffa738bdc137c78acce2c6dd2\n\nnode scripts/docs-audit/affected-docs.mjs --json ba9b4981cd6a111af47bc71741106467ef8f8359\n```\n\n⚠️ That checkout carried **uncommitted changes**, so the commit above does not fully identify what was read.\n</details>\n\n> Advisory only, and a **precision-first** one (#9192): a page is listed because it names a\n> symbol, wire route or SDK method this diff touched — not because it mentions a changed\n> package. Each row says which anchor put it there, so a wrong row is reportable rather than\n> merely annoying. To re-verify, run the `docs-accuracy-audit` workflow scoped to these files:\n> `node scripts/docs-audit/affected-docs.mjs ba9b4981cd6a111af47bc71741106467ef8f8359` → pass the list as\n> `args.docs`, on the commit named under **Which tree this was computed on**."}
{"id":5662886777,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-14T10:58:21Z","updated_at":"2026-09-14T10:58:21Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/18175#issuecomment-5662886777","body":"## In-seat clause-② contract review — **PASS**, pinned to `14cb2a66b37baba0150e5784ab6376e553a6664d`\n\n`domain:spec` execution seat, `session_01KB5PFtxuy1x3dcR5gxudx6`, 2026-09-14T10:5xZ. This is the record the enqueue gate requires: the diff touches `packages/spec/src/**`, so the **path limb** is hit and ⛔ no enqueue may happen without an in-seat review PASS on file — regardless of the claim's `Clause-②: no`.\n\n**`CLAUSE_2: no` — VERDICT: PASS.** The declared `no` is correct: the diff narrows on every axis it touches and widens on none.\n\n### Why this went to a subagent, and how the tier was verified\n\n⚠️ This seat is served by **`claude-opus-5`**, which is BELOW `CONTRACT_REVIEW_TIER` (`claude-fable-5-1`, read from `scripts/pm/dispatch-gates.mjs:10689`). Under the downgrade fuse the seat ⛔ does not self-judge clause ②; the review was run by an at-tier subagent and the tier was verified **per transcript, ⛔ not from its self-report**:\n\n| transcript | `\"model\"` field count |\n|---|---|\n| the reviewer | **106 × `claude-fable-5-1`** |\n| the `os-dev` control (same round, this card's implementer) | **161 × `claude-opus-5`** |\n\n⇒ The extractor discriminates between the two tiers on this harness, so the at-tier reading is a true positive rather than a field that always prints the same string.\n\n### The readings that carry the verdict\n\n- **Accept-set differential on the real schemas, both trees** — 3,421 inputs (19 node types × 20 `waitEventConfig` variants × 9 `boundaryConfig` variants): `main` accepts 1141, head accepts 821. **WIDENED (head accepts what main refused): 0. NARROWED: 320.** No sibling arm was loosened to compensate — the unchanged cells were enumerated and match main exactly.\n- **`.default('create')` on `ScreenConfig.mode`** — 14 inputs incl. absent, `undefined`, `null`, `''`, `'CREATE'`, `1`, unknown key: **inputs whose accept verdict differs: 0.** Only the OUTPUT moves (absent ⇒ `mode: \"create\"` materialised); the published type narrows from `'create'|'edit'|undefined` to `'create'|'edit'`. The output-mode JSON Schema gaining `required: [\"mode\"]` is the **pre-existing convention for every `.default()` key**, measured against the `ScheduleState.status` precedent on main — ⛔ not introduced here. No enum gains a member.\n- **`.default('GET')` is not in this diff at all** — `HttpConfigSchema.method` changed docblock only; 6/6 inputs identical verdict and output, emitted JSON Schema byte-identical in both io modes.\n- **New symbols / codes / keys**: one `export const entry` in the new semantic entry (generator input; `migrations/index.ts` re-exports only `registry.js`, `api-surface/automation.json` untouched). `requireTypeScopedConfig` is module-private. Issue codes are Zod `code: 'custom'`; the executor reuses the pre-existing `refuseNode`. **No new `StandardErrorCode`.**\n- **ADR-0087 entry registered by REGENERATION, not by hand**: `build-migration-registry --self-test --check` exits 0 on the head extract (217 semantic vs main's 216); positive control — mutating one word in the entry file flips it to `✗ stale, EXIT=1`.\n- **Gate**: `check-widening-tells --declaration no` exits 0 over the diff. Controls that make that zero a reading: `--self-test` 269 cases pass, and a synthetic one-key diff on `flow.zod.ts` correctly fires `✗ T1 … EXIT=4`. ⚠️ The two known weaknesses of this gate (the `retiredKey()` false positive of #17955, the re-declared-key T1 blind spot of #17848) were checked and **neither applies**: no `retiredKey(` line is added, and the only re-declared key line is a pre-existing `mode:`.\n\n### Independence\n\n`Implemented-by:` the `mode:subagent` dev on branch `claude/issue-17928-wait-node-config-required`. `Reviewed-by:` `session_01KB5PFtxuy1x3dcR5gxudx6` (this seat), through an isolated at-tier review subagent fed only the card, its thread, the PR body and the tree. ⚠️ Stated plainly rather than implied: **the implementer and the reviewer were dispatched from this same seat session**, so this is in-seat review with tier separation, ⛔ not cross-seat independence.\n\n### ⚠️ Scope of this PASS\n\nIt is pinned to `14cb2a66`. A **prose-only patch round is in flight** (see the REWORK below); a comment/docblock change cannot move the clause-② axis, but this seat will confirm the patch is prose-only at the new head before enqueue rather than assuming it.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
