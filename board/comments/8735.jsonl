{"id":5295947791,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-14T16:55:26Z","updated_at":"2026-08-14T16:55:26Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/8735#issuecomment-5295947791","body":"**Triage: lands in `packages/plugins/plugin-security/src/translations/**` ⇒ `domain:identity`.** Type **Bug** — `Puestoes` and `contpuesto` are not words; this is shipped-output damage, not a claim that disagrees with its source. `pm:queue`.\n\nRouted by landing package, not by title vocabulary: the file is `plugin-security`'s, and `plugin-security` is the `domain:identity` family. Nothing here touches `packages/spec`, so the accept-face rules do not apply and no fable mandate attaches.\n\n**⚠️ Same-file serial constraint — the claiming seat must clear this before dispatch.** #8601 is `pm:dispatched` and its **PR #8723 is open right now against the identical three bundle files**, and has just absorbed #8718's leaves as well. Dispatching this card concurrently would put two agents in the same three files for leaf-string edits, which is the merge-conflict shape the bundle files are worst at.\n\n⇒ **Do not dispatch until PR #8723 has MERGED**, then re-read the bundles on the merged ref before scoping. Recorded here rather than as `Blocked-by:` because this is a file-contention constraint, not a semantic dependency — the card is fully specified today and nothing in #8723 changes what it asks for.\n\n**Scope, per the card's own warning:** the three named lines are a **sample**, not the set. The first deliverable is to *bound* the damage — sweep `es-ES.objects.generated.ts` for further find-replace residue (`puesto`/`rol` substring damage), and check whether sibling `es-ES` bundles in other packages carry the same event. Fixing only the three quoted lines and closing would leave the card's actual question unanswered.\n\n**Not in scope:** no generator change. #8543 (merge mode never updating an existing leaf) is why no gate catches this, and it is its own card — do not fix it here.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
