{"id":5463189480,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-29T15:15:34Z","updated_at":"2026-08-29T15:15:34Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/12400#issuecomment-5463189480","body":"Claim: cross-lane direct takeover under the simple-blocker clause (maintainer 2026-08-13) — this card blocks four `domain:spec` cards (#11330–#11333), and it is a mechanical, fully-specified, read-only S-level measurement; the blocked seat takes it rather than waiting.\nSession: `session_01KX8wnyjStaZcuMyAMNsy3N` (domain:spec seat, R1)\nBranch: none — read-only measurement, zero file edits, no worktree\nDomain: `repo:cloud` queue card, executed by the `domain:spec` seat; cloud seat's hot-file serial queue untouched (nothing is written)\nFile surface: none — `git grep` against cloud `origin/main` (`15f55df`) only\nContainer & model: S mechanical, in-session (no dev dispatch)\nClause-②: no\nSerial constraints cleared: none apply — zero writes anywhere.\n\nBasis for the takeover: this session's authorized surface INCLUDES `objectstack-ai/cloud` (the seam card exists only because prior spec/triage sessions measured `add_repo` denied — that transport limitation does not hold here). Readings follow in the next comment.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5463192038,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-29T15:16:05Z","updated_at":"2026-08-29T15:16:05Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/12400#issuecomment-5463192038","body":"## Readings — cloud `origin/main` @ `15f55df` (fetched 2026-08-29T15:08Z)\n\nProtocol: the card's commands, adapted to `git grep` against `origin/main` (no checkout, no writes); test/spec files excluded; `dist/` excluded. Interpretation discipline per the refined #12456 protocol: a PRIMARY hit counts only if the receiver is a **plugin manifest** object.\n\n### PRIMARY (narrow: property reads, manifest-filtered) — 24 hits, classified line by line\n\n**Discharged as non-plugin-manifest receivers** (the #10812 `req.query?.contributes` template):\n- `packages/service-cloud/src/r2-registry-reader.ts:139,148,231` + `registry-mirror.ts:107-108` — receiver is `EnvironmentManifest` (environmentId / head.commit / encrypted runtime config), the environment registry shape, not the `.osplugin` manifest.\n- `scripts/ee-release-manifest.mjs:275-596` — release manifest; its `runtime` is an object (`version` / `compatibility`), a different shape entirely.\n- `apps/cloud/scripts/check-demo-manifest.ts:32` — demo manifest.\n\n**REAL plugin-manifest reads** (receiver verified by reading each context):\n\n```\npackages/service-cloud/src/plugin-artifact.ts:179:        runtime: manifest.runtime,\npackages/service-cloud/src/plugin-artifact.ts:181:        permissions: manifest.permissions ? JSON.stringify(manifest.permissions) : undefined,\npackages/service-cloud/src/plugin-artifact.ts:182:        integrity: manifest.integrity ? JSON.stringify(manifest.integrity) : undefined,\npackages/service-cloud/src/routes/environment-crud.ts:1607:  const declaredPerms2 = parsePermissions(manifest2?.permissions);\npackages/service-cloud/src/routes/package-install.ts:902:   const declared = parsePermissions(manifest?.permissions);\npackages/service-cloud/src/routes/package-publish.ts:531:   runtime: prepared.manifest!.runtime,\npackages/service-cloud/src/routes/package-publish.ts:532:   permissions: prepared.manifest!.permissions ?? null,\n```\n\n### CONTROLS\n\n- **CONTROL B** (manifest reads findable at all): `manifest.(namespace|plugins|id)` → **15** non-test hits. The probe sees this repo.\n- **CONTROL C** (key words in corpus, non-test): runtime 1126 · integrity 16 · capabilities 36 · configuration 55 · **extensions 0** · permissions 186 · sandbox 15 · **digests 0** · granted_permissions 9.\n- Bracket-access probe (`[\"key\"]` spellings, manifest-filtered): **0** hits.\n\n### Per-key verdicts\n\n| key | cloud verdict | evidence |\n|---|---|---|\n| `runtime` | **LIVE — behavioral dispatch** | `plugin-permission-audit.ts:60-90` (`auditPluginPermissions`, called from `package-publish.ts:530`): `runtime === 'node'` + unverified publisher ⇒ **hard block** (caller turns it into a 422, message: *\"The 'node' runtime tier (in-process, full-trust) requires a verified publisher signature. Sign the artifact with a registered publisher key, or use the 'sandbox' tier.\"*); `runtime:node` is also a sensitivity flag forcing human review (no inline auto-approve). This is ADR-0025 §3.6 default-deny, implemented at the **publish** door. Also persisted to the version row (`plugin-artifact.ts:179`, `package-publish.ts:531`). |\n| structured `permissions` | **LIVE — behavioral, structured arm confirmed** | `plugin-consent.ts` (ADR-0025 Phase 3, PD5): `parsePermissions` consumes exactly the structured `{services, hooks, network, fs}` shape (its own doc: *\"mirrors `@objectstack/spec/kernel`'s `PluginPermissions`\"*); `permissionsWiden` turns a widening upgrade without re-consent into a **409** (`package-install.ts:641,902`; `environment-crud.ts:1602-1641`); consented set persisted to `sys_package_installation.granted_permissions` (audit trail, 9 non-test hits), which the doc says the environment runtime later feeds to the framework's `PluginPermissionEnforcer` (F4). `auditPluginPermissions` additionally flags `fs` and broad `network` as sensitive at publish. `plugin-permission-audit.ts` imports `PluginPermissions` **from `@objectstack/spec/kernel`** — a direct spec-schema type consumer. |\n| `integrity` | **persist-only — no verifier** | Persisted at publish (`plugin-artifact.ts:182` → `PluginVersionFields.integrity`), but `digests` has **0** occurrences in the corpus and no unpack-side verification exists. Cloud stores the map; nothing checks it. |\n| `capabilities` | **CLEAN** | 0 plugin-manifest reads (36 word hits are unrelated receivers; narrow/wide/bracket probes all 0 on manifest receivers). |\n| `configuration` | **CLEAN** | 0 plugin-manifest reads (55 word hits unrelated). |\n| `extensions` | **CLEAN — word absent entirely** | 0 occurrences corpus-wide (CONTROL C), so a fortiori 0 manifest reads. |\n\n### Dispositions (executed on each card per its recorded unlock plan)\n\n- **#11332** (`capabilities`/`configuration`/`extensions`) — all three CLEAN ⇒ the queue-shaped member per its triage note; returning to `pm:queue`.\n- **#11330** (`runtime`) — premise re-scoped: the tier IS enforced at cloud's publish door (default-deny for unverified `node`), still unenforced at framework load time ⇒ decision box with this evidence, per the recorded disposition.\n- **#11331** (`integrity`) — cloud persists, nothing verifies ⇒ decision box (security-posture fork), per its triage note.\n- **#11333** (structured `permissions`) — PRIMARY hit ⇒ fork re-scopes around the live cloud consumer; decision box per the recorded disposition.\n\nClosing this card as completed — the readings above are the deliverable. Takeover receipt: measured and filed by the `domain:spec` seat under the simple-blocker clause; nothing in the cloud repo was written.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
