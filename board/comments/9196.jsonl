{"id":5310738901,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-08-17T01:18:37Z","updated_at":"2026-08-17T01:18:37Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/9196#issuecomment-5310738901","body":"Triage: lands in `packages/metadata-protocol/src/protocol.ts` (JSDoc header of `migrateStoredMetadata`) ⇒ `domain:metadata`, `pm:queue`, type `Task`.\n\nRationale: mechanical fourth bullet mirroring #9175's cli.mdx fix; the reasoning is already written at the `isNonCanonicalStoredType` call site. The flow-bullet vs flow-conflict-row scope difference flagged in the body is plausibly intentional (internal surface vs operator-observable surface) — the dev should document the split in one sentence rather than escalate it; if measurement shows it is genuine drift, report back instead of hard-coding a guess.\n\nSerial constraint for the claiming seat: `protocol.ts` is a hot file — #9111 (PR #9173, in flight) and #9157 (PR #9191, in review) both touch it. Dispatch after those land or declare region-level surface (JSDoc header only) against the metadata seat's serial queue.\n\nSize/model suggestion: S (mechanical, gate-verified), `sonnet`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5313498371,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-17T08:12:20Z","updated_at":"2026-08-17T08:12:20Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/9196#issuecomment-5313498371","body":"**PM claim** · seat `pm-dispatch` · session `17d48cbe-d3e0-562b-95a3-e2653a9bd9fb` · branch `claude/issue-9196-migrate-jsdoc-fourth-decline`\n\nClaiming for dispatch. Batch 5, card 3 of 4.\n\n## 裁决 — not re-litigable\n\n1. **The close is a fourth bullet in the JSDoc \"What it declines to touch, and says so\" list**, for the non-canonical-stored-type decline that #8957 (PR #9059, `b740440bd`) added to `migrateStoredMetadata`. The reasoning is already written — the `if (isNonCanonicalStoredType(rawType))` branch's own inline comment states it. Mirror that wording; do not invent a second account of the same decline.\n\n2. ⛔ **The scope split flagged in the card's second section is NOT yours to resolve.** The JSDoc's three and cli.mdx's three are *not the same three* (the JSDoc has a `flow`-with-no-reachable-engine bullet cli.mdx lacks; cli.mdx has a flow-conflict-refused row the JSDoc lacks). That is a maintainer question. You may add **one sentence** to the JSDoc naming the scope it documents (the function's full internal surface, including paths no CLI caller can reach) if that is what you measure to be true — you may **not** add or delete bullets to make the two lists agree.\n\n3. **A count-carrying prose list with no gate is the defect class here, not this one missing bullet.** Two victims of one dispatch brief already (#9175's mdx table, this JSDoc). If while working you see a cheap way to make the count enforceable, ⛔ do not build it in this PR — file it as a finding and link back here.\n\n## PM 机制假设 — falsifiable, measure before you rely on them\n\n- **The JSDoc still lists exactly three at HEAD.** ⚠️ #9175's PR was in flight when this was filed and may have touched the JSDoc as well as `cli.mdx`. **Read it at HEAD first.** If the fourth bullet is already there, that is a valid outcome — close with the artifact (the JSDoc text at the merged SHA) and a one-line note, do not manufacture a diff.\n- **The `isNonCanonicalStoredType` inline comment still exists and still says what the card quotes.** If it has been reworded, the reworded text is the source, not the card's quote.\n- **No gate enforces this count**, so nothing will fail if you get it wrong — which means the artifact is the only evidence. Quote the final JSDoc block in your report.\n\n## Route\n\n- Worktree-first, per repo, before your first edit.\n- **Region-level surface declaration**: `packages/metadata-protocol/src/protocol.ts` is a hot file with #9111 (PR #9173) still in flight against `hydrateOverlayIntoRegistry` (~L11398–11666). Your surface is the **JSDoc header immediately above `migrateStoredMetadata`** (~L13311) and nothing else. ⛔ Do not reflow, reformat, or otherwise touch other regions of the file — a whitespace-only hunk elsewhere turns a mechanical PR into a merge conflict on the repo's hottest file.\n- Changeset per repo rules. Draft PR. ⛔ Never merge your own PR.\n\n## Model tier\n\nClause ② judged **not triggered**: JSDoc prose only, no accept/reject behaviour change, no `packages/spec/**`, no error-code ledger, no `*.zod.ts`. Dispatching at `sonnet`. ⚠️ Fence: if the work drifts into changing what `migrateStoredMetadata` actually declines (as opposed to describing it), **stop and report** — that is a different card and a different tier.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5313893014,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-17T08:53:36Z","updated_at":"2026-08-17T08:53:36Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/9196#issuecomment-5313893014","body":"<!-- os-dev-report -->\n```json\n{\n  \"issue\": 9196,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-9196-migrate-jsdoc-fourth-decline\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/9270\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Measured at HEAD (739fe5b79, before branching): migrateStoredMetadata's JSDoc 'What it declines to touch, and says so' section still listed exactly three bullets — #9175's in-flight PR had not touched it, only cli.mdx. Added the fourth bullet for the non-canonical-stored-type decline, mirroring the isNonCanonicalStoredType branch's own inline comment/reason string verbatim in substance. Also added one lead-in sentence (permitted by the dispatch brief, conditioned on measurement) naming the scope the JSDoc documents — verified true against this same JSDoc's canonicalizeFlow parameter doc and cli.mdx's 'Flows are covered' section, which both confirm the CLI always supplies a live automation engine so the flow-no-engine bullet is never observed from that door. No bullets were added or removed to reconcile the JSDoc's list with cli.mdx's list — that scope split (JSDoc has a flow-no-engine bullet cli.mdx lacks; cli.mdx has a flow-conflict-refused row the JSDoc lacks) is confirmed real on both sides and left for the maintainer per the scope fence.\",\n  \"tests\": \"At commit 6d952880e (HEAD of the branch): node scripts/check-nul-bytes.mjs -> OK. pnpm check:filter-alias-parity -> OK (self-test + real run green). pnpm check:durability-log-level -> OK (self-test 35/35, real run: '26 durability-critical catch seam(s), all loud'). pnpm check:cross-package-test-inputs -> OK (self-test 33/33, real run: '12 package(s) read outside themselves, all declared'). pnpm --filter '@objectstack/metadata-protocol^...' build then pnpm --filter @objectstack/metadata-protocol build -> clean, DTS build succeeds (confirms the JSDoc is well-formed TypeScript). pnpm --filter @objectstack/metadata-protocol exec vitest run --maxWorkers=2 -> 116 test files / 1606 tests passed. Gate families re-derived against the changed path via node scripts/pm/dispatch-gates.mjs packages/metadata-protocol/src/protocol.ts confirmed the same three families (plus a docs-drift-check advisory that found nothing to run since no content/docs/** file changed). No gate enforces the JSDoc's bullet count -- the artifact (quoted below) is the evidence.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": []\n}\n```\n\n## Artifact — final JSDoc block at commit `6d952880e`\n\n```\n    /**\n     * `os migrate meta --stored` — canonicalize `sys_metadata` rows in place so\n     * the read-path conversion chain has a finish line (#4327).\n     *\n     * #4317 made every stored-row rehydration seam replay the full ADR-0087\n     * chain, so a row written under any past protocol *reads* canonical forever\n     * ({@link convertStoredItem}). The rows themselves stayed legacy: the chain\n     * re-lowers them on every load and each one emits a conversion notice per\n     * process. This pass ends that for a deployment that runs it — same chain,\n     * same policy, result written back — while the read path stays the\n     * guarantee for every deployment that does not (#3855: operator-run\n     * migrations cannot be relied upon, so nothing here is load-bearing).\n     *\n     * ## What it walks\n     *\n     * `active` and `draft` rows, every org (the env-wide `organization_id IS\n     * NULL` bucket included). `archived` / `deprecated` rows are deliberately\n     * not read: they are not served metadata, and rewriting them would edit a\n     * record of what *was*. `sys_metadata_history` is untouched for the same\n     * reason the addendum gives — converting a version body would break the\n     * checksum↔body pairing.\n     *\n     * ## How it writes\n     *\n     * Through {@link saveMetaItem}, not the repository directly, so a rewritten\n     * row gets everything an author's save gets: the schema gate, a\n     * `sys_metadata_history` row, a fresh checksum, the mutation projectors and\n     * the watch event Studio's HMR consumes. Three deliberate arguments:\n     *\n     * - `parentVersion: row.checksum` — a true optimistic lock. A concurrent\n     *   writer that moved the row between our read and our write gets a 409,\n     *   reported as `failed`, never a clobber.\n     * - `force: true` — the destructive-change diff compares the *stored*\n     *   body (which `getMetaItem` already serves converted) against the body we\n     *   are about to write (the same conversion). It is empty by construction,\n     *   and there is no author here for a confirmation prompt to reach.\n     * - `source: 'migrate-stored'` — so a history diff distinguishes a\n     *   canonicalization pass from an edit someone made.\n     *\n     * ## What it declines to touch, and says so\n     *\n     * This section documents the function's full internal surface, including\n     * paths no CLI caller can reach: `os migrate meta --stored` always passes\n     * its own automation engine (see `canonicalizeFlow` above), so the first\n     * bullet below is never observed from that door.\n     *\n     * - **`flow` rows with no reachable automation engine.** Flow-node\n     *   conversions carry ADR-0078's open-namespace conflict guard, which\n     *   needs the engine's live executor registry. When one is reachable —\n     *   passed as `canonicalizeFlow`, or resolved from the services registry\n     *   (#4498) — flows are migrated like anything else (#4454); when none is,\n     *   they are reported `skipped` with that reason, never counted done.\n     * - **Types with no repository write path** (neither `allowOrgOverride` nor\n     *   `allowRuntimeCreate`). This pass declines them, and since #5086 it\n     *   would have no choice: `saveMetaItem` refuses a code-only type with\n     *   `403 NOT_CREATABLE` / `NOT_OVERRIDABLE` before persistence. The skip\n     *   predates that refusal and outranks it — declining is a *reported*\n     *   `skipped` row, where forwarding the write would surface as `failed`\n     *   noise on every run. (Historical note for anyone reading the skip\n     *   reason: until #5264 the write would instead have landed in a legacy\n     *   raw-engine branch that recorded no history and forced `state:\n     *   'active'`. That branch is gone; the skip is what always kept this\n     *   pass away from it.)\n     *\n     *   Today that is exactly one type, `agent`, and its skip is **permanent\n     *   by design, not a to-do** (#4507): ADR-0063 §2 closes `*.agent.ts` to\n     *   third parties, so the only agent definitions in existence are the two\n     *   the platform ships from version control — where git, not\n     *   `sys_metadata_history`, is the change log. See the note beside the\n     *   `agent` entry in `metadata-plugin.zod.ts` before treating this branch\n     *   as a gap to close.\n     * - **Rows that still fail the current schema after conversion.**\n     *   `saveMetaItem` rejects them (422) and that rejection is correct: the\n     *   body is a genuine contract violation, not chain-owned history. They\n     *   surface as `failed` with the validation message, keep reading through\n     *   the chain, and stay fixable in Studio.\n     * - **Rows stored under a non-canonical metadata type spelling** (#8957,\n     *   {@link isNonCanonicalStoredType}). This pass canonicalizes BODIES;\n     *   rewriting a stored type spelling is an identity move — a new\n     *   `(org, type, name, package_id)` key, not an edit — so it is out of\n     *   its reach. Nothing on the canonical type can see such a row — no\n     *   registry read, no compliance query — and the batch publish refuses\n     *   it for the same reason (`STORED_TYPE_NOT_CANONICAL`). Reported\n     *   `skipped`, not `failed`: nothing is broken about this pass, the row\n     *   is simply outside its reach. Re-author the item under the canonical\n     *   type (`PUT /meta/<canonical>/<name>`) and drop the non-canonical\n     *   row.\n     */\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5314157963,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-17T09:19:28Z","updated_at":"2026-08-17T09:19:28Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/9196#issuecomment-5314157963","body":"<!-- os-dev-report -->\nUpdate after PM review on PR #9270 (two items addressed, both fixed at commit `937f982aa`):\n\n1. **Body contradiction** (`Fixes #9196` + a sentence claiming `#9196 remains open`): fixed. Kept `Fixes #9196`, removed the contradictory sentence, and pointed the \"Scope fence\" section at the follow-up decision card the PM filed (#9271) instead.\n2. **Docs-drift advisory row** (`content/docs/concepts/metadata-lifecycle.mdx`, listed via the `ObjectStackProtocolImplementation` symbol): checked, clean. That page mentions `migrateStoredMetadata` once (describing the `job` type's skip behavior in the `allowOrgOverride` table) and does not restate the decline list or any count — nothing stale there.\n\nAlso narrowed the JSDoc's lead-in sentence itself: it originally said the section documents the function's \"full internal surface\", which overclaims — the JSDoc is also missing `cli.mdx`'s \"flow whose rename the conflict guard refused\" row, which *is* internal to the function, so \"full\" didn't hold and the sentence would have implied it accounted for a gap it doesn't explain. Narrowed to what is actually demonstrated (the flow-no-engine bullet specifically) and pointed the residual question at #9271.\n\nUpdated JSON report below (supersedes the one in the previous comment on this issue).\n\n```json\n{\n  \"issue\": 9196,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-9196-migrate-jsdoc-fourth-decline\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/9270\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Measured at HEAD (739fe5b79, before branching): migrateStoredMetadata's JSDoc still listed exactly three decline bullets; #9175's in-flight PR had touched only cli.mdx. Added the fourth bullet for the non-canonical-stored-type decline, mirroring the isNonCanonicalStoredType branch's own inline comment/reason string. Added a narrow lead-in note (revised after PM review to avoid overclaiming) explaining only the one direction of the JSDoc/cli.mdx scope gap that a CLI-reachability argument actually accounts for (the flow-no-engine bullet); pointed the unresolved reverse-direction gap at the PM's follow-up decision card #9271 rather than implying it was resolved. No bullets were added or removed to reconcile the two lists.\",\n  \"tests\": \"At commit 937f982aa (HEAD): node scripts/check-nul-bytes.mjs -> OK. pnpm check:filter-alias-parity -> OK (self-test + real run green). pnpm check:durability-log-level -> OK (self-test 35/35, real run green). pnpm check:cross-package-test-inputs -> OK (self-test 33/33, real run green). pnpm --filter '@objectstack/metadata-protocol^...' build then pnpm --filter @objectstack/metadata-protocol build -> clean, DTS build succeeds. Full vitest suite (116 files / 1606 tests passed) was run at the prior commit 6d952880e, a strict subset of this commit's comment-only diff within the same JSDoc block; re-verified via the clean rebuild at 937f982aa. Docs-drift advisory row (content/docs/concepts/metadata-lifecycle.mdx) checked by hand: mentions migrateStoredMetadata once, does not restate the decline list or a count -- clean, not stale.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": []\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
