{"id":5653497806,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-09-13T13:13:32Z","updated_at":"2026-09-13T13:13:32Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18031#issuecomment-5653497806","body":"Claim: PM loop — dev seat, dispatched\nSession: `session_680c1c4e-4867-4565-b594-b5d2662e3e65`\nBranch: `claude/issue-18031-permissions-key-collision`\nWorktree: `objectstack-issue-18031`\nDomain: `domain:security` (card arrives ungraded — no `domain:*`, no type, no priority, no assignee; reported in the seat report)\nFile surface: `packages/spec/src/kernel/**` (manifest / package-registry declarations, docblocks only) · `packages/plugins/plugin-security/src/**` (suggested-audience-bindings) · `.changeset/**` (stop on breach; explain in the report)\nContainer & model: M, `mode:subagent`, `model: opus` (default judgement tier — `node scripts/pm/dispatch-gates.mjs --tier <paths>` this session prints \"no path-derived mandate … Clause ② SUSPECT surface\", a floor hint, not a clearance)\nClause-②: yes\nThread-read: none (no comments on the card at the moment this claim is written)\nSerial constraints cleared: `#18022` (claude/issue-17516-permission-set-collision-diagnostic) is in flight in the SAME package, `@objectstack/plugin-security` — its file list and mine are disjoint (it moves `bootstrap-declared-permissions.*`, `permission-set-name-collision.*`, `permission-set-projection.ts`, `index.ts`; I move `suggested-audience-bindings.ts` only). `#17811` and `#17814` touch the ADR-0090 D5 anchor semantics but neither moves a file on my surface. No in-flight claim asserts the behaviour this card pins.\n\nClause-② reading, both limbs stated separately:\n- PATH limb HITS — the diff moves `packages/spec/src/**` (`kernel/manifest.zod.ts`, `kernel/package-registry.zod.ts`). `needs:contract-review` goes on both carriers at PR-open time and the card waits outside the queue until the in-seat review clears it.\n- CONTENT limb does NOT hit, and the declaration above is the conservative direction rather than a claim that it does: this diff relaxes no accept set (`ManifestSchema` stays `strictObject`, both `permissions` arms unchanged, `InstalledPackageSchema.manifest` stays `ManifestSchema`) and adds no published export (`collectDeclaredSuggestions` is module-exported and is NOT re-exported from `plugin-security/src/index.ts`). The spec half is docblock/`describe` text only.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
{"id":5653610650,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-09-13T13:36:20Z","updated_at":"2026-09-13T13:36:20Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18031#issuecomment-5653610650","body":"os-dev-report\n\n```json\n{\n  \"issue\": 18031,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-18031-permissions-key-collision\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/18035\",\n  \"premise_still_valid\": true,\n  \"summary\": \"The card's live half is fixed and its first scope suggestion is REFUTED. collectDeclaredSuggestions source 2 now reports, once per engine per package and arm, that a package's manifest.permissions carried the ADR-0025 grant reading rather than the ADR-0090 permission-set collection it reads — naming the arm, the dropped-member count, the consequence (no suggestion row, no admin prompt, a deployment that goes on looking healthy) and the remedy (declare the sets in the package's own defineStack permissions collection). Scope item 1 was NOT done: ManifestSchema DOES declare objects and datasources (as glob patterns, so the mismatch is of type, not presence, and strictObject makes it worse than the card says), and the assembled-stage declaration the card asks for already landed at #14242 under the maintainer's road-B ruling of 2026-09-02, which rejected widening a manifest key by name as road C. What was genuinely missing were the back-pointers, which is what the spec half adds: ManifestSchema.permissions, the stack permissions collection and InstalledPackageSchema.manifest now each name the other stage. The card arrived ungraded and unassigned (no domain, type, priority or assignee).\",\n  \"tests\": \"ALL RUNS ARE A DECLARED NARROWING — scripts/pm/os-verify-lock.sh could not take the shared verify lock on this host (no usable flock; the lock is declared Linux-only), so every command ran UNLOCKED, declared in the PR body in the wrapper's own wording. GATES: spec build exit 0 · spec test 476 files / 13563 passed · spec typecheck exit 0 (check:test-typecheck 54 files / 259 errors / 144 pinned, unchanged) · spec check:generated — check:docs was the only stale artifact, --fix ran gen:docs, final re-run on HEAD prints 'All 15 generated artifacts are up to date' · plugin-security build exit 0 · plugin-security test 111 files / 2154 passed · plugin-security typecheck exit 0 · check:nul-bytes OK over 8604 text files · check:pm-widening-tells self-test 269 cases · check:durability-log-level 35 seams all loud, 68 read seams none inventing · check:optional-error-sink OK · check:stack-collection-maps 8 enumerations vs 31 collections · check:startup-registry-verdict 43 seams clean · check-empty-changeset exit 0 · check-adr-0087-registration exit 0 · check-changeset-no-major exit 0 · check-closing-keyword-parity OK · check-partof-closing-keyword run with the real PR body, exit 0 · check-clause2-carriers --pair 18035 exit 0, both carriers agree. Every verdict read from the gate's own printed line, never a bare exit code behind a pipe. ABLATION (run from the committed state): HEAD blob 2e6a770cd56f678cabd3fee3876d370631c32610; marker 'reportDroppedManifestPermissions(ql, logger, dropped);' present 1x in source; mutation deleted that one call and the marker count went to 0 on disk (the editor's exit status was not read as evidence); rebuilt plugin-security and ablation-dist-preflight --absent confirmed the marker gone from all 4 built files; PREDICTED red, OBSERVED red — 4 failed / 29 passed (33), exactly the four warn-asserting cases, while the three no-noise cases stayed green; restore via git checkout HEAD -- (never a bare git checkout --) put the marker back to 1, whole-tree git status --porcelain empty, git hash-object byte-identical to the HEAD blob, dist rebuilt and the preflight re-run positive. Incidental: with the call gone tsc also refuses the DTS build with TS6133 on the now-unused logger. CI on PR 18035 at report time: Check Changeset SUCCESS, Governed Surface Queue Guard SUCCESS, Spec property liveness SUCCESS, Check Documentation Links SUCCESS, claim/branch and single-writer-path guards SUCCESS; Lint and Repo Gates, Test Core 1-6, the four Type Check legs, Build Core, Build Docs, Dogfood 1-3 and Temporal Conformance all in_progress — NOT MEASURED, and not waited on.\",\n  \"mcp_calls\": \"0 — every GitHub read and write went through the gh CLI (REST); no MCP GitHub tool was called\",\n  \"open_questions\": [\n    {\n      \"question\": \"packages/spec/liveness/manifest.json grades `permissions` `live` and cites this very reader, noting that it 'reads the LEGACY arm'. Measured: it ITERATES that arm and reads no value out of it — consider()'s `typeof ps !== 'object'` returns on every bare string — so the legacy arm has zero EFFECTIVE readers, exactly as the structured arm does. Does the row's status move?\",\n      \"options\": [\n        \"A — leave status `live`, correct only the note to state the measured drop (what this PR did NOT do; the entry would then read as self-contradictory but honest)\",\n        \"B — move the status, which is the spec seat's verdict and feeds #13458's retirement route\",\n        \"C — leave the row untouched and let #11333 / #13458 carry it (what this PR did)\"\n      ],\n      \"recommendation\": \"C for this PR, then B in the spec seat. Moving a liveness status drives a retirement and is not a rider on a reader-side repair; the measurement is now pinned by tests and posted as evidence on #11333 so it cannot evaporate.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"filed as #18034: AppPlugin's SECURITY_FIELDS loop (packages/runtime/src/app-plugin.ts) drops a manifest `permissions` it cannot read at BOTH arms with no log — the same collision, the second consumer, and the half that runs at every boot for every stack\",\n    \"noted, not filed: packages/spec/liveness/manifest.json's `permissions` row calls the legacy arm live on a reader that discards every value of it; carrier is #11333 (evidence comment posted there) and its retirement route is #13458, so a duplicate card was not opened\"\n  ]\n}\n```\n\nTwo deviations, declared rather than silent:\n\n1. **Verification ran unlocked**, for the reason above — the wrapper's official wording is in the PR body.\n2. **The worktree `objectstack-issue-18031` is deliberately NOT removed.** The PR is docked under `needs:contract-review` on both carriers and is waiting on an in-seat contract review of this same branch, so a patch round on this claim is a named, expected next step rather than a finished landing. Nothing is uncommitted in it: `git status --porcelain` is empty and HEAD is pushed. Remove it once the review clears or the card is released.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
