{"id":5739691892,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-19T05:35:17Z","updated_at":"2026-09-19T05:35:17Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19173#issuecomment-5739691892","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `0e09e2b4b4fcbe3d8c0175cd62ea908ea9bb1c2b`\n\nIn-seat review by the `domain:skills` seat at the contract-review tier (served tier read at seating, marker 5738863635) — the record this lane owes on every round it delivers, `Clause-②: no` included. Verified against GitHub (`GET /pulls/19173/files`: one file, `scripts/pm/post-stamped.mjs`, +377 / −18; `head.sha` pasted from the PR object) and the fetched branch (`origin/claude/issue-19091-post-stamped-unstamped-artefact` at `0e09e2b` = `da5a024` + `df98cbf` + a merge of `origin/main` `ec00b53`; merge-base with today's `origin/main` `c229223` is `ec00b53`), ⛔ not against the report: two non-merge commits, both trailer pairs model-free; the file grows 3,259 → 3,618 lines, no control bytes. `check-governed-merges.mjs --pr 19173`: NOT governed, 395 ≤ 5000.\n\n### ① Derived judgments\n\n- The tool's accept set NARROWS by exactly one shape: a body whose only stamp tokens sit inside quoted spans and which declares nothing is REFUSED (`EXIT_REFUSED`, nothing written). Read in the diff: `substituteTokens` now also returns `verbatimTokens` / `verbatimTokenSpans` (the openers left verbatim that ARE `{{NOW}}` or `{{WAS:…}}`, with span kind and byte offset); `stampVerdict({ substituted, quoted, verbatimTokens })` is the pure exported predicate with `STAMP_VERDICTS` as its declared four-member vocabulary and `REFUSED_STAMP_VERDICT` naming the one refused; `renderBody` applies it LAST, after the existing positional / mixed / quoted / unknown refusals, so the refusal ordering is unchanged. Correct per the card: the predicate first, then the consequence; the naive criterion 「0 substitutions AND ≥1 verbatim opener」 is not the rule — `stampVerdict({ substituted: 0, quoted: 1, verbatimTokens: 1 })` is `stamped-by-declaration` (pinned as excluded), so the tool's own docblock shape and this seat's `{{WAS:…}}`-stamped claims stay accepted.\n- `no-token` stays accepted (the pre-existing, separately measured shape) — the card's 「does NOT settle」 boundary honoured; the dev's population reason (every filed instance carried a quoted token) is stated in the header, not merely preferred.\n- Refuse rather than warn: the card allowed a loud warning + distinct exit as the fallback if a refusal broke a legitimate caller shape; the dev measured none (no programmatic body producer in the tree; the remedy is one token outside the quotation; both legitimate shapes accepted) — accepted as the ruled direction (#17314: a timestamp a seat writes comes from the clock of the writing act).\n- The false read-back line is retired: `readBackVerdict` reads the verdict and prints a line true of each shape that can reach it (declaration-stamped; no-token; and the CLI-unreachable refused shape, kept truthful because the function is exported). The verdict also reaches `--dry-run` and `--json` (`stamp_verdict`, `verbatim_tokens`).\n- Fixtures triaged, not inverted: the one-line carve-out fixture gains a prose `{{NOW}}` so it still renders; `QUOTED_WAS_DIGITS` stays byte-identical and is read through `substituteTokens` (its acceptance decision pinned where it is made). Battery roster 15 → 16 with the new floor 63; `SELF_TEST_BATTERY_FLOOR` 14 → 15; the quoting battery 51 → 52; 413 → 477 cases per the report. Ablations: the guard → 17 / 477 red; the read-back line → 3 / 477 red (exactly the three false-line cases); both restored byte-identical.\n- Deviation from the suggested route accepted: `verbatimTokens` (the token subset) instead of a `verbatimOpeners` field — measured on a quoted `{{ user.name }}` template (verbatim 1, verbatimTokens 0, posts as `no-token`), which is the right boundary.\n\n### ② Semver level\n\nNone owed — `scripts/pm/**` ships in no package; `skip-changeset` applied and read back (labels on the head: `size/m`, `skip-changeset`).\n\n### ③ Boundary flags\n\n- Deviations (5) read: the field name (above); gates measured at the merged head `0e09e2b` while `origin/main` advanced three commits (`e8667ee`, `57cfedd`, `c229223`) — the dev checked none touches a symbol this file imports from `check-half-states.mjs`, and the queue rebuilds on the current generation (accepted); an earlier detached gate run lost its exit code and was re-run with the code captured (accepted); ablation 1's first attempt aborted on a `TypeError` and `df98cbf` makes the battery report per case (accepted); no 速读 (fact layer — correct).\n- `open_questions`: none. `out_of_scope_findings` (2, 「noted, not filed」): the CLI-unreachable read-back branch; the per-opener `Buffer.byteLength` cost — neither is one of the three classes; not filed.\n- Gates: 29 derived at the merged head, 29 run, 0 NOT-MEASURED per `--ran`, all exit 0 (`check:pm-dispatch-gates` 1866 cases at 1025.9 s); `pnpm lint` run WHOLE (6,894 files) exit 0. CI on the head at this record's writing: 16 `success`, 11 rostered skips, 6 in progress (`Lint & Repo Gates`, the Type Check matrix, `Test Core` shards) — the seat lands only when every check is `success` or a rostered skip.\n- Serial: #19048 and #18995 remain open on this file and are not addressed here (correct — three different refusal-table defects, serial by the seat's queue).\n\nImplemented-by: `claude/issue-19091-post-stamped-unstamped-artefact`\nReviewed-by: `session_01W5y9kRg1YtYaMQYExVLRc2`\n\n**VERDICT: PASS**\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
