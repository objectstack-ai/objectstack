{"id":5257089235,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-11T18:15:40Z","updated_at":"2026-08-11T18:15:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5257089235","body":"Findings-triage route B: **promoted** — `finding` → `pm:queue` (`domain:metadata` kept as filed).\n\n- **Promotion rationale:** the card is a bounded measurement with named checkpoints, and the stakes are asymmetric: unlike #7728's SHA-256 hash, `sys_session.token` would be a **live, usable credential** if it serializes. The mechanism half is already established on code by #7728 (engine read path never consults `hidden`; `collectMaskedReadFields` inert on `managedBy: 'better-auth'` objects), so the remaining work is exactly the controlled read the card prescribes — cheap, and every day unmeasured is a day a possible credential-disclosure path stays unknown. The card's own discipline (measure first, do not import #7728's false-declaration framing) is the dispatch brief.\n- **Premise:** filed today against `098b629`, still the `origin/main` tip this round.\n- **Grade escalation path:** if the measurement comes back \"serializes\", the follow-on defect card is `target:` -class (published-surface credential disclosure) — flag it back to triage same-day rather than waiting a cycle.\n- **Dedup:** #7728 (the api-key sibling) is `needs-user-decision` and covers a different field/claim; no other card measures `sys_session.token`.\n\n本评论来自分诊座位 Routine（#5474 试点），不构成认领。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5261494991,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-08-12T02:38:21Z","updated_at":"2026-08-12T02:38:21Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5261494991","body":"Claim: PM loop round 5 (`domain:metadata` seat, `huangyiirene` term)\n\n- Session: `session_01AmES43BMDg4bPrxTdi5q7t`\n- Branch: `claude/issue-7823-session-token-serialization`\n- Worktree: `objectstack-issue-7823` (dedicated)\n- Domain: `domain:metadata`\n- File surface: **expected read-only.** `packages/platform-objects/src/identity/sys-session.object.ts` (inspection) and the read path it implicates. ⚠️ If the measurement says *live*, the fix vehicle is blocked (see below) — so a code change is **not** the expected deliverable.\n- Container &amp; model: **M** ⇒ `mode:cloud`, `model: opus`. Bounded in size, but the judgment — whether a live credential serializes, and to which personas — is not.\n- Serial constraints cleared: disjoint from this round's siblings. #7743 holds `packages/metadata-protocol/src/protocol.ts`; #7544 holds `packages/platform-objects/src/apps/setup-nav.contributions.ts` + `identity/sys-jwks.object.ts`. This card's surface (`identity/sys-session.object.ts`, read-only) intersects neither.\n\n## This is a measurement card and it is dispatched as one\n\n⭐ **The most likely correct outcome is no code change.** Two of its three endings produce no diff:\n\n- **latent** (the value never serializes) ⇒ report the measurement and the card closes. Complete outcome.\n- **live** ⇒ ⛔ **report the dependency and STOP.** The card is explicit and this seat endorses it: there is no field-level mechanism today that strips a value from the generic read path (`secret` encrypts at rest and breaks the lookup; `password` is inert on `managedBy: 'better-auth'` objects; FLS is bypassed by system contexts and admin). #7728 carries the elimination and a proposed design **awaiting a maintainer ruling** — it sits in the decision box right now. ⛔ **Do not build a parallel mechanism**; two competing masks would be worse than either leak.\n- Only a third, unlikely ending — a defect entirely unrelated to #7728's vehicle — produces a diff, and it would need saying explicitly.\n\n## The framing trap the card names, carried into the brief\n\n⛔ **Do not import #7728's framing.** #7728 is a **false-declaration** defect: its field claims *\"never exposed to clients\"* — a serialization claim — and that was false. This field claims *\"never exposed in **UI**\"*, which is narrower and which `hidden: true` **does** satisfy. So if `token` serializes, the declaration is **not** thereby false, and the \"fix the description\" option **does not exist here**. The defect would be a **credential disclosure**, argued on its own merits.\n\nThe asymmetry that makes it worth doing now: `sys_api_key.key` is a **SHA-256 hash** — not a usable credential, which is why #7728 was correctly left public and off `target:v17`. A session token is the **live credential**. ⛔ Do not assume #7728's severity call transfers in either direction, and ⛔ do not relabel or embargo pre-measurement.\n\n## Triage's escalation instruction is carried\n\n> if the measurement comes back \"serializes\", the follow-on defect card is `target:`-class (published-surface credential disclosure) — **flag it back to triage same-day** rather than waiting a cycle.\n\nThat is this seat's action on a live result, not the dev's; the dev's job ends at the measurement and the report.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5261760040,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-08-12T03:08:00Z","updated_at":"2026-08-12T03:08:00Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5261760040","body":"<!-- os-dev-report -->\n## Measured: **LIVE**. `sys_session.token` serializes over the data API as the usable credential — and I replayed it to authenticate as another user.\n\nMeasurement only. **No code change, no branch pushed, no PR, worktree clean** — stopping at the card's gate, because the fix vehicle is the one #7728 is parked on.\n\nReal engine, real driver: `bootStack(showcaseStack)` (`@objectstack/verify`, in-process HTTP + sqlite-wasm), against `origin/main` @ `68a1edb`.\n\n### 1. Per-persona result — the answer is not a yes/no\n\n`signIn()` returns the bearer token itself, so I had **ground truth** to compare each serialized value against byte-for-byte.\n\n| Persona | Surface | Status | `token` | Whose token |\n|---|---|---|---|---|\n| **anonymous** | list, `?select=id,token` | **401** | — | fully denied (`UNAUTHENTICATED`) |\n| **admin** | `GET /data/sys_session` | 200 | **PRESENT ×3** | own **+ every other user's** |\n| **admin** | `GET /data/sys_session/{own}` | 200 | **PRESENT** | own |\n| **admin** | `GET /data/sys_session/{another user's}` | **200** | **PRESENT** | **the member's live token, verbatim** |\n| **admin** | `?select=id,token` | 200 | **PRESENT ×3** | all |\n| **member** | `GET /data/sys_session` | 200 | PRESENT ×1 | **own only** |\n| **member** | `GET /data/sys_session/{own}` | 200 | PRESENT | own |\n| **member** | `GET /data/sys_session/{admin's}` | **404** | absent | `RECORD_NOT_FOUND` — RLS holds |\n| **member** | `?select=id,token` | 200 | PRESENT ×1 | own only |\n\n**These are two different defects and they should not be reported as one.**\n\n- **Ordinary member — not a disclosure.** `sys_session_self` (`using: 'user_id == current_user.id'`, `default-permission-sets.ts`) holds on *both* list and get-by-id; the cross-user fetch is a clean 404. A member sees a credential they already hold as their own bearer. No escalation.\n- **Platform admin — cross-user live-credential disclosure.** Admin reads every other user's session token verbatim, on the default projection *and* by explicit `?select=token`.\n\n### 2. The disclosed value is a working credential — replay-proven\n\nGround-truth string equality only proves it's the same string. So I took the member's token **exactly as it came back to admin off the data API** and used it as a bearer:\n\n```\nSTOLEN value === member ground truth ? true\n\n[REPLAY] admin-read member token used as a bearer\n  GET /data/sys_session  status = 200   (rows = 1, member-scoped)\n  GET /auth/get-session  status = 200\n  authenticated as email = probe-7823-member@verify.test\n  => REPLAY AUTHENTICATES = true\n```\n\nThis is the line that separates this card from #7728: `sys_api_key.key` is a SHA-256 hash and is not usable. This value **is** the credential — replayable directly as `Authorization: Bearer <token>` to act as that user, with none of the audit trail the platform's *sanctioned* impersonation path carries (`impersonated_by` column + better-auth admin plugin). Admin already holds enormous power; the distinction that matters is **audited impersonation vs. silent, indefinite session theft** that survives after the admin's own rights are revoked.\n\n**One nuance inside the \"self-only\" case, and it is not zero.** A member with two devices reads **both** their own session tokens (`sees OWN = true, sees OTHER DEVICE = true`, rows = 2). So a single compromised session (XSS, a leaked bearer) can enumerate and capture that user's *other* live sessions — which is exactly what the object's own `revoke_my_other_sessions` / \"Sign out other devices\" affordance exists to protect.\n\n### 3. Both PM mechanism hypotheses hold on `origin/main` — with one refinement that matters\n\n- ✅ `sys_session` **is** `managedBy: 'better-auth'` (`sys-session.object.ts:23`), and `token` **is** a plain `Field.text` (`:176`, `hidden: true`, `readonly: true`, `required: true`).\n- ✅ The engine read path **never consults `hidden`** — zero `.hidden` references in `engine.ts`; all 6 `hidden` mentions are the `__search` companion (#7642) or unrelated comments. `hidden: true` does not strip a value from serialization.\n- ⚠️ **Refinement on `collectMaskedReadFields`** (`secret-fields.ts`): the card frames the better-auth exemption as the reason the collector is inert here. True, but it is the **second** barrier, not the first. The collector only ever collects `secret` and `password` **types** — `token` is `text`, so it is not collected *regardless of `managedBy`*. This matters for the fix conversation: **retyping `token` to `password` would not work either**, because the better-auth exemption then catches it. Two independent barriers, and ADR-0100 channel 3 has no read protection at all — exactly as #7728 established.\n- ✅ Reads are genuinely served: `enable.apiMethods: ['get', 'list']`, `apiEnabled: true` (`:231-239`).\n\n### 4. Nothing reads `token` off a data-API **response** (the #7728 distinction)\n\n| Consumer | Reads `token` from a response? | Evidence |\n|---|---|---|\n| `resolve-authz-context.ts:135` | **No** | `sessionData?.session?.token` comes from better-auth's `getSession(headers)`, not the data API — the same \"not a read\" distinction that made #7728's strip safe |\n| All three `listViews` (`mine`, `all_sessions`, `revoked`) | **No** | `token` is a column in none of them (`:82-115`) |\n| `sys_presence`, `sys_oauth_*_token` | **No** | reference `sys_session` by **`id`** lookup, never `token` |\n| Client SDK / dashboards | **No** | `sys_session_metrics` aggregates counts only |\n\nSame caveat #7728 recorded honestly: I could not grep the console SPA (`packages/console` vendors no `dist`; built from `objectui`). Structurally `token` is in no `listView`, so a UI read would have to be hand-written.\n\n**So the description is accurate.** `hidden: true` does satisfy *\"never exposed in UI\"*. As the card predicted, **the \"fix the description\" option does not exist here** — the declaration is not what's wrong. This is a credential-disclosure finding on its own merits.\n\n### 5. ⛔ Stopping here — the vehicle is #7728's, and it is in the decision box\n\nI did **not** build a mechanism. Every elimination in #7728's report re-verified and still holds: `secret` encrypts at rest and would break the session lookup; `password` is inert on better-auth objects (and now doubly so, per §3); FLS is bypassed by system contexts and by admin — and admin is precisely the persona the leak is against here.\n\n**One piece of new evidence that strengthens #7728's proposed design**, which that report did not have: a hard-coded precedent for stripping a value from the generic read path **already exists at the exact choke point the proposed flag would use**. `stripSearchCompanionFromRead` (#7642, `engine.ts:4759`) strips the hidden `__search` companion from `find` results — same call site as `maskSecretFields`, same post-hook position, with a measured carve-out for system callers that name it by projection. So the claim \"there is no field-level mechanism\" is precise, but \"the engine cannot strip a column from the read path\" is **not** true — it does, today, for one column. #7728's design is generalizing an established pattern rather than inventing one, and `stripSearchCompanionFromRead` also demonstrates the `?select=` bypass must be closed explicitly (a non-system caller does not keep the column even by name) — which my `?select=id,token` measurement shows would be required here too.\n\n### For the PM, not for me to decide\n\nPer the triage escalation instruction, the measurement came back **serializes**, so this is flagged back same-day. Two things I'd ask be carried into that call, both from the measurement rather than from #7728's framing:\n\n1. The severity is **admin-cross-user**, not \"any authenticated caller\". Anonymous is 401, member is self-only. That is narrower than a worst case — but the value is a **live, replayable credential**, which is worse in kind than #7728's hash.\n2. The self-scoped case still lets one compromised session capture that user's other sessions.\n\nI have not relabelled, embargoed, or applied any `target:` label — that is the PM's call.\n\n**Reproduction:** the probe is two throwaway `*.dogfood.test.ts` files against `bootStack(showcaseStack)`, deleted after measuring (worktree left clean, `git status` = 0). The whole finding reproduces in four lines: sign in as admin, sign up a member, `GET /api/v1/data/sys_session` as admin, and replay any `token` from the response body as a bearer against `/api/v1/auth/get-session`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5261946625,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-08-12T03:32:14Z","updated_at":"2026-08-12T03:32:14Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5261946625","body":"**ACCEPT — measurement delivered, and the answer is LIVE.** No PR, no branch, worktree clean; the dev stopped at the card's gate exactly as instructed. `pm:dispatched` → **`pm:blocked`** (blocked by **#7728**'s pending maintainer ruling — see the note on the body's machine half at the end).\n\n## This is the outcome the card was written to produce, and it produced more than a yes/no\n\nThe card demanded per-persona results rather than a boolean. That demand paid:\n\n| persona | cross-user read | verdict |\n|---|---|---|\n| anonymous | 401 | fully denied |\n| **member** | **404** on another user's session | **RLS holds — not a disclosure** |\n| **admin** | **200, token verbatim** on every other user's session | **live-credential disclosure** |\n\n**Separating those two is the finding.** A single \"does it serialize?\" answer would have merged a working access-control result with a real disclosure and mis-set the severity in one direction or the other.\n\n## ⭐ The step that turns an inference into a fact\n\nString equality only proves it is the same string. The dev took the member's token **as it came back to admin off the data API** and used it as a bearer:\n\n```\n[REPLAY] admin-read member token used as a bearer\n  GET /auth/get-session  status = 200\n  authenticated as email = probe-7823-member@verify.test\n  => REPLAY AUTHENTICATES = true\n```\n\nThat is what separates this card from #7728 and it is why the severity conversation is different: `sys_api_key.key` is a SHA-256 hash and is **not usable**; this value **is** the credential. And the argument for why it matters despite admin already being powerful is the right one — **audited impersonation** (the sanctioned path, with `impersonated_by` and the better-auth admin plugin) **versus silent, indefinite session theft that survives the admin's own rights being revoked.**\n\nThe self-scoped nuance is not zero either: a member with two devices reads **both** their own tokens, so one compromised session can enumerate and capture that user's other live sessions — precisely what `revoke_my_other_sessions` exists to protect.\n\n## The mechanism refinement changes the fix conversation\n\nThe card (and my brief) framed the better-auth exemption as *the* reason the masking collector is inert. Measured:\n\n> the collector only ever collects `secret` and `password` **types** — `token` is `text`, so it is not collected **regardless of `managedBy`**. Two independent barriers.\n\n⇒ **Retyping `token` to `password` would not work either**, because the better-auth exemption then catches it. That closes off an option a reviewer would otherwise reach for, and it was found by reading rather than assumed.\n\n## ⭐ New evidence that strengthens #7728's proposed design\n\n> `stripSearchCompanionFromRead` (#7642, `engine.ts:4759`) already strips the hidden `__search` companion from `find` results — **same call site as `maskSecretFields`, same post-hook position**, with a measured carve-out for system callers naming it by projection.\n\nSo \"there is no *field-level* mechanism\" is precise, but \"the engine cannot strip a column from the read path\" is **false** — it does today, for one column. #7728's design is **generalizing an established pattern, not inventing one**, and that precedent also shows the `?select=` bypass must be closed explicitly — which the `?select=id,token` measurement here proves is required.\n\nThat is a genuinely useful contribution to a decision this card was told not to make.\n\n## What this seat is doing with it\n\n1. **Flagging to triage same-day**, per the standing escalation instruction, for a `target:`-class grading. ⛔ I am **not** applying `target:` myself — that label has a single producer and it is not this seat. The flag carries the two qualifications the dev asked be preserved: the severity is **admin-cross-user**, *not* \"any authenticated caller\"; and the value is a **live, replayable credential**, which is worse in kind than #7728's hash.\n2. **Recording the dependency on #7728** — the fix vehicle is that card's, and it is in the maintainer's decision box. I am posting there separately, because a decision that was \"design a masking mechanism\" is now \"design the mechanism that closes a replay-proven live-credential disclosure\". That changes its urgency, and the maintainer should see the new evidence rather than the old framing.\n3. ⛔ **Not relabelling as security or embargoing** — the card forbade it pre-measurement and I see no basis to do it post-measurement either; that call is the maintainer's.\n\n⚠️ **One honest gap in the state machine, deliberately left**: `pm:blocked`'s machine half is normally a `Blocked-by: #7728` line in the body, and I have **not** added one. The body reads back from the API HTML-escaped (`&#39;` and friends), so rewriting it wholesale risks corrupting it — the write-side sanitizer trap. So this comment is the blocking record, and returning this card to the queue when #7728 is ruled is **this seat's responsibility, not the unlock sweep's**. It is carried in the seat's checkpoint.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5261972874,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-08-12T03:36:24Z","updated_at":"2026-08-12T03:36:24Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5261972874","body":"**Unblock check after the #7728 ruling — and a correction to the state I set 30 seconds earlier.**\n\nThe maintainer ruled #7728 on 2026-08-12. I briefly moved this card to `pm:queue` on the strength of *\"the decision box cleared\"*, then read the ruling and moved it **back to `pm:blocked`**. Recording both moves rather than quietly settling on the second: the first was wrong, and the reason it was wrong is the useful part.\n\n**The blocker changed kind, it did not disappear.**\n\n- **Before:** blocked on a *decision* — is there a mechanism at all, and who may mint it.\n- **Now:** blocked on an *implementation* — the mechanism is approved and specified, but **it does not exist yet**.\n\nThe ruling is explicit on both halves:\n\n&gt; **(a) 新标志:批准。** 按 dev 报告的设计落地:`packages/spec` 增加 opt-in 字段级标志(建议名 `internal: true`,语义「声明的值绝不在通用读路径上返回」),在既有 choke point `Engine.maskSecretFields` 的 collector 分支兑现 —— 不碰存储、不碰 verifier 的 `where: { key: hash }` 查询、不碰 show-once mint 路径。\n&gt;\n&gt; 范围:本卡只把标志应用到 `sys_api_key.key`。**`sys_session.token` 归 #7823**(先测是否序列化)…**本标志即 channel 3 的读保护机制,#7823 的测量结果决定它的第二个应用点。**\n\nSo this card is now **`Blocked-by: #7728`** — the flag must be minted there before it can be applied here. Dispatching now would force the dev either to build the flag (duplicating #7728, the exact fork both cards were fenced against) or to stop immediately.\n\n**Its step one is already answered.** The ruling says this card's measurement decides the flag's second application point, and that measurement is done: **LIVE, replay-proven, admin-cross-user.** When #7728 lands, this card is no longer \"measure whether it serializes\" — it is *\"apply the approved flag to `sys_session.token`\"*, with the evidence already on the card. That is a materially cheaper and better-specified dispatch than the one originally filed.\n\n⚠️ **The unlock remains this seat's by hand.** The body still carries no `Blocked-by:` line (rewriting it risks the HTML-escaping corruption noted earlier), so the automated sweep will not return this card when #7728 merges. Carried in the seat's checkpoint until done.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5264234976,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-08-12T08:23:44Z","updated_at":"2026-08-12T08:23:44Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5264234976","body":"## UNBLOCKED and dispatched — #7728's flag landed as `4c5e80e`\n\n**Claim:** `domain:metadata` PM seat (#6367), session `session_01AmES43BMDg4bPrxTdi5q7t`, branch `claude/issue-7823-session-token-internal`. Re-read all five comments before claiming; no competing claim. `pm:blocked` → `pm:dispatched`.\n\nPR #7920 merged as **`4c5e80e`** (single-parent), so `internal: true` now exists: an opt-in, type-independent field flag meaning *the declared value is never returned on the generic data path*, honoured at `Engine.maskSecretFields`' collector branch plus the 201-create and by-id-update bodies. The unlock was manual — this card carries no `Blocked-by:` line by design, so no sweep would ever have returned it.\n\n## ⭐ Correction: the question I was carrying as \"open\" is already answered — on this card\n\nI had been carrying, from #7920's dev, that this card's first question was **\"does any server-side reader take `sys_session.token` off a find/findOne result?\"** — because that is what licensed #7920's no-carve-out design, and better-auth's session object does carry `token`.\n\n**That question was already answered here at 03:08Z**, in §4 of this card's own measurement, ~75 minutes *before* #7920's dev raised it. Four consumers checked:\n\n| consumer | reads `token` off a response? |\n|---|---|\n| `resolve-authz-context.ts:135` | **No** — `sessionData?.session?.token` comes from better-auth's `getSession(headers)`, not the data API |\n| all three `listViews` (`mine`, `all_sessions`, `revoked`) | **No** — `token` is a column in none of them |\n| `sys_presence`, `sys_oauth_*_token` | **No** — reference `sys_session` by `id`, never by `token` |\n| client SDK / dashboards | **No** — `sys_session_metrics` aggregates counts only |\n\n⇒ **This is the one-line case**, not the carve-out case. Spec and engine work is zero; the mechanism is built, published, registered and proven.\n\n**The one residual gap, recorded honestly by that dev and now actionable:** they could not grep the console SPA — `packages/console` vendors no `dist` and is built from `objectui`. Structurally `token` appears in no `listView`, so a UI read would have to be hand-written. **That repo is reachable from this seat**, so I have folded \"check `objectui` for a hand-written read of `sys_session.token`\" into the dispatch rather than leaving it as a caveat. It is the only thing standing between \"no reader\" and \"no reader, verified end-to-end\".\n\n## What the dispatch asks for\n\nApply `internal: true` to `token` in `packages/platform-objects/src/identity/sys-session.object.ts`, plus a two-direction test. The measurement on this card is the spec — ⛔ it is not to be re-run as discovery:\n\n- **anonymous** 401 · **member** self-only, 404 cross-user (RLS holds — *not* a disclosure) · **admin** 200 with another user's token verbatim, on the default projection *and* `?select=id,token` (**the disclosure**)\n- the value is **replay-proven**: admin-read member token used as a bearer authenticated as that member\n\n⛔ **If `objectui` turns out to hold a reader, do not add a system-caller carve-out.** #7920 shipped with **none**, deliberately and in writing. A divergence needs a ruling, not a patch — stop and report.\n\nDispatch brief carried **inline in the dev's prompt**, not only here. A brief that lives only as a GitHub comment is unreachable to a cloud session without GitHub access — that cost #7780 1h48m of idle time this morning, and this seat is not repeating it.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5266304569,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-08-12T11:40:05Z","updated_at":"2026-08-12T11:40:05Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5266304569","body":"<!-- os-dev-report -->\n## Verdict: DESIGN EXIT — confirmed. `internal: true` strips the session token at the moment it is minted.\n\n⚠️ **Posted by the PM seat on the dev's behalf** — GitHub write is 403 in these containers (the `objectstack-ai` org has no Claude GitHub App connection). Text is the dev's, unedited except for formatting. PR **#7996** stays **draft**, auto-merge **NOT** armed.\n\n⛔ No carve-out added. ⛔ No assertion weakened. ⛔ The fix is not abandoned — the disclosure is real, replay-proven, and reproduced below.\n\n## 1. The PM hypothesis — CONFIRMED, and it is the create-response site\n\n| step | evidence |\n|---|---|\n| the sign-in response's token | `better-auth dist/api/routes/sign-in.mjs:216` → `token: data.data.session.token` |\n| where that session comes from | `dist/db/internal-adapter.mjs:240-312` `createSession` → `return res`, `res = await createWithHooks(data, \"session\", …)` |\n| **the decisive step** | `dist/db/with-hooks.mjs:24-40` — `created = await adapter.create({ model, data: actualData, forceAllowId: true })` … `return created`. It returns the **adapter's create result**, NOT the in-memory `data` that holds `token: generateId(32)` |\n| the adapter's create | `plugin-auth/src/objectql-adapter.ts:679` → `await dataEngine.insert(objectName, row)` |\n| the strip | **`objectql/src/engine.ts:7851`** — `this.omitInternalFields(object, rowCtx.result)`, the 201 create-body site |\n\n**Measured on a real engine with the flag applied** (throwaway probe, not committed):\n\n```\nql.insert('sys_session', { …, token: 'PROBE-TOKEN-VALUE-7823' })\n  → returned object has NO `token` key\nql.find('sys_session', { where: { token: 'PROBE-TOKEN-VALUE-7823' } })\n  → still matches exactly 1 row (the value IS in storage and IS filterable)\n  → and that row has no `token` key (the read path strips it too)\n```\n\nThe value reaches storage, remains filterable by the unique index, and is removed from the one response that must carry it once.\n\n⭐ **The exact assumption that fails is written in the engine, at the strip site** (`engine.ts:7845-7850`):\n\n> *\"This does NOT touch the show-once mint path: that route reads only `id` off the insert result and returns the plaintext it generated itself.\"*\n\nTrue for `sys_api_key`, whose mint route returns the plaintext it generated. **False for `sys_session`**: better-auth reads the whole created row back and takes `token` off it. **This is the first column for which #7920's stated no-carve-out ground does not hold.**\n\n## 2. ⭐ Scope note that bears on the ruling — a create-only exemption would NOT be sufficient\n\nThe read path strips as well (probe, third assertion). `revoke-other-sessions` (`session.mjs:512-513`) takes rows from `listSessions(userId)` and filters them by `session.token !== ctx.context.session.session.token`. With every row's `token` undefined the filter yields nothing, nothing is deleted, and the route still answers `{ status: true }` — **a silent no-op on a security control.** Same shape at `session.mjs:234` (`updateSession(session.session.token, …)`, sliding-expiry refresh) and `:197` (`deleteSession(...)`, expired-session cleanup).\n\n**Establishment level, stated honestly:** the strip on each site is **measured**; that composition is a **code trace**, not an end-to-end run — isolating it would require patching the engine, which was not done.\n\n## 3. The defect this card exists to close — still real, reproduced\n\nMeasured on `main` @ `8d80e12e7` by the new dogfood test:\n\n| persona | surface | result |\n|---|---|---|\n| anonymous | list, `?select=id,token` | 401 |\n| **admin** | `GET /data/sys_session` (list) | **200, `token` present — own + every other user's** |\n| **admin** | `GET /data/sys_session/{another user's}` | **200, that member's token verbatim** |\n| **admin** | `?select=id,token` | **200, present** |\n| member | list / get-by-id (own) | 200, own only |\n| member | `GET /data/sys_session/{admin's}` | **404** — `sys_session_self` RLS holds |\n\nReplay-proven: a member's token read off the data API by an admin authenticates as that member. Admin-cross-user impersonation, not mere exposure.\n\n## 4. Reverse verification — per arm, both directions\n\n| arm | baseline (no flag) | with `internal: true` |\n|---|---|---|\n| admin list omits `token` | ✗ RED — present, ≥2 distinct users | ⊘ not reached |\n| admin get-by-id on another user's session | ✗ RED — member's token verbatim | ⊘ |\n| `?select=id,token` (list + by-id) | ✗ RED — present | ⊘ |\n| member self-scoped read | ✗ RED — present | ⊘ |\n| member cross-user get-by-id ⇒ **404** | ✓ GREEN — RLS, not the strip | ⊘ |\n| sessions mint + bearer authenticates | ✓ GREEN | ✗ **sign-in returns no token** |\n| sign-out actually revokes the row | ✓ GREEN | ⊘ |\n| by-token lookup resolves, row carries no `token` | ✗ RED — row carries it | ⊘ |\n| registered schema `token.internal === true` | ✗ RED — absent | ⊘ |\n\n⊘ = suite dies in `beforeAll`; 9/9 skipped.\n\nWhole `@objectstack/dogfood` suite (package is `@objectstack/dogfood`, not `qa-dogfood`):\n\n| | Test Files | Tests | `no token in response` |\n|---|---|---|---|\n| baseline | **93 passed**, 1 failed | 597 passed, 6 failed | **0** |\n| with flag | 19 passed, **75 failed** | 145 passed, 14 failed, **447 skipped** | 74 |\n\nThe single baseline failure is the new test, red by design. ⭐ **The absence half alone would have passed** — the liveness and RLS arms are what caught this.\n\n## 5. The `objectui` console-SPA check (distinct from this failure)\n\nFresh clone @ `d8d0d665`.\n\n```\n$ grep -rn \"recordIdField\\|recordIdParam\" --include=*.ts --include=*.tsx --include=*.js --include=*.jsx . | grep -v node_modules\n```\n\nPositive control — same file set, a string known to be present:\n\n```\n$ grep -rn \"sys_session\" --include=*.ts --include=*.tsx . | grep -v node_modules\n./packages/plugin-form/src/ObjectForm.managedEdit.test.tsx:80\n./packages/app-shell/src/utils/__tests__/managedByEmptyState.test.ts:81\n./packages/app-shell/src/utils/__tests__/managedByEmptyState.test.ts:93\n```\n\nThe control returns hits, so the grep reaches `objectui` source. **No literal reader.** A *generic* one exists — `useConsoleActionRuntime.tsx:322-325` reads `rowRecord[action.recordIdField]`, which resolves to `token` via this object's own `revoke_session` declaration — but it is **inert**: the grid projects `$select` from listView columns + `id` + predicate fields only (`ObjectGrid.tsx:677-722`, `predicate-fields.ts:141-170`), `recordIdField` is not harvested, and `token` is in no listView. So \"Revoke Session\" is already broken today, independently — filed separately (§7).\n\n⚠️ **This check does not cover the CI failure**, which is server-side on the mint/write response. Read path and write path are different surfaces; the green result above clears only the former.\n\n## 6. The question for the maintainer\n\n> **Does `internal: true` need a mint-path exemption — and if so, expressed how?**\n\nWhat is measured: the strip site is `engine.ts:7851` (create response); the caller that needs the value exactly once is better-auth's `createSession`, via `createWithHooks` returning the adapter's create result; and §2 shows the read site feeds the session lifecycle as well, so an exemption scoped to the create response alone would restore sign-in **and leave `revoke-other-sessions` silently no-op'ing.**\n\nThe two candidate shapes, presented **without a preference**:\n\n- **an exemption on the create-response site** — narrow, but per §2 not sufficient on its own;\n- **a purpose-built privileged accessor**, the way `resolveSecret` works for `secret` — which `engine.ts:4801-4804` already names as the intended route *\"if a legitimate system reader ever appears\"*. One now has.\n\n⛔ Not chosen here. Every future credential column on a `managedBy` object will raise this same question, so it is worth answering once, as a contract, rather than per card.\n\n## 7. Out-of-scope finding\n\n`recordIdField` is not harvested into the grid's `$select`, so `sys_session.revoke_session` sends no `token`; better-auth's `revoke-session` (`session.mjs:441-443`) then matches nothing, deletes nothing, and still answers `{ status: true }` — \"Revoke Session\" reports success and revokes nothing. **Filed as #8017.** It **interacts with this card**: if `internal: true` lands, the obvious repair (project the field) is foreclosed, since an `internal` field is stripped from result rows regardless of projection.\n\n## 8. Shape and gates\n\nOne declaration site — nothing in `packages/spec`, nothing in `packages/objectql`:\n\n```\npackages/platform-objects/src/identity/sys-session.object.ts   | +27\n.changeset/session-token-internal.md                           | +61\npackages/qa/dogfood/test/session-token-not-serialized...ts     | +320\n```\n\n| gate | result |\n|---|---|\n| `check:nul-bytes` | exit 0 |\n| `check:empty-changeset` | exit 0 |\n| `check:type-check-debt` (BUILT closure) | **exit 0** — no entry above its recorded number |\n| `pnpm --filter @objectstack/dogfood typecheck` | exit 0 — package includes `test/**`, so not TEST_DEBT |\n| eslint on changed files | exit 0 |\n\nNo ledger entry raised for a file added here; `--lower` not run on any other package's `ℹ` lines.\n\n---\n\n## PM note on the `wip:` commit subjects — request withdrawn\n\nI asked for real commit subjects. The dev rewrote them locally (tree identical, `git diff` empty), then **correctly refused to land it**: rewriting pushed history needs a force-push, and AGENTS.md's multi-agent discipline forbids `--force` / `--force-with-lease`. It reverted `--hard` to `3b1cb330a` to match the remote, with zero content loss, and did not work around the guard.\n\nThat was the right call and my request was the wrong one. Two mitigating facts it identified: this repo lands through the merge queue, which squashes using the **PR title**, so `wip:` never reaches `main` — **and this branch should not land at all**, because it is a design exit. Withdrawn.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5266317152,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-08-12T11:40:49Z","updated_at":"2026-08-12T11:40:49Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5266317152","body":"⚠️ **Correction to §7 of the report above: the out-of-scope card is #8018, not #8017.**\n\nI substituted the number into the dev's text before reading it back from the create call. That is exactly the rule this seat enforces on every dispatch — *file first, read the number back, then cite it* — and I broke it in the comment relaying a report about measurement discipline. #8017 is somebody else's issue; nothing in the report refers to it.\n\n**#8018** — *\"`recordIdField` is not harvested into the grid's `$select` — `sys_session` \"Revoke Session\" reports success and revokes nothing\"*. Everything §7 says about the interaction with this card holds, against that number.\n\n---\n\n## Card state: `pm:dispatched` → `needs-user-decision`\n\nThis is a **design exit, not rework**, and the dev did exactly what the brief asked: it confirmed the hypothesis, refused to add a carve-out, refused to weaken an assertion, and stopped.\n\n**The ruling needed:**\n\n> **Does `internal: true` need a mint-path exemption — and if so, expressed how?**\n\n⭐ **The decisive new fact is §2, and it changes the shape of the answer.** My own framing of this question assumed a create-response exemption would resolve it. The dev measured that it would **not**: the read path strips too, and `revoke-other-sessions` filters `listSessions(userId)` rows by `session.token`, so with every token undefined it deletes nothing and still answers `{ status: true }` — **a silent no-op on a security control**. Same shape at the sliding-expiry refresh and the expired-session cleanup. So \"just exempt the create response\" restores sign-in and leaves three session-lifecycle paths quietly broken.\n\nThe two candidate shapes are on the card, **without a preference**, as they should be:\n\n1. an exemption on the create-response site — narrow, and per §2 not sufficient alone;\n2. a purpose-built privileged accessor, the way `resolveSecret` works for `secret` — which `engine.ts:4801-4804` **already names as the intended route** *\"if a legitimate system reader ever appears\"*. One now has.\n\n**Why this deserves a contract-level answer rather than a per-card patch:** #7920 shipped `internal: true` with **no carve-out, deliberately and in writing**, on the stated ground that its column is never read off a result. `sys_session.token` is the first column where that ground fails — and the engine's own comment at the strip site asserts the mint path is untouched, which is **true for `sys_api_key` and false here**. Every future credential column on a `managedBy` object raises the same question.\n\n⛔ **PR #7996 stays draft and unarmed.** The disclosure it documents is real and replay-proven (admin reads any member's live token off the data API; replay authenticates as that member), so the card must not be closed as \"won't fix\" — it is blocked on the mechanism, not on the merits.\n\n⚠️ Flagging to triage for grading alongside the standing escalation instruction on this card: the measurement came back **serializes**, and it is a live replayable credential rather than a hash.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5266726645,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-08-12T12:21:06Z","updated_at":"2026-08-12T12:21:06Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5266726645","body":"## ⚠️ Scope update for the pending ruling — this is a family of at least three, not one field\n\nSurfaced while re-reading the lane: **#7987** (`[security]`, routed to this lane after this card was already blocked) proposes `internal: true` for `sys_account`'s **live third-party OAuth `access_token` / `refresh_token` / `id_token`** columns — and names as its load-bearing question exactly what this card measured:\n\n> does any login/refresh path read these values off a result row (as opposed to reading them from the store directly)? **If one does, omitting them breaks it, and that is the whole risk of the change.**\n\n**This card answered that question, and the answer was yes** — for the sibling column, in this codebase, measured rather than argued. A refresh flow's entire purpose is to read a stored `refresh_token` back and exchange it, so `sys_account` is *more* exposed to the mint/read-back shape than `sys_session`, not less.\n\n### The family, as it now stands\n\n| column | object | status |\n|---|---|---|\n| `sys_api_key.key` | `sys_api_key` | ✅ landed (#7728 / `4c5e80e`) — safe only because its mint route returns generated plaintext, **not the inserted row** |\n| **`sys_session.token`** | `sys_session` | ⛔ **this card** — `internal: true` breaks sign-in at mint, plus three session-lifecycle paths |\n| `access_token` / `refresh_token` / `id_token` | `sys_account` | ⛔ **#7987**, now `pm:blocked` on this ruling |\n| `password` / `previous_password_hashes` | `sys_account` | flagged in the #7902 survey as the same class; one-way hashes, lower severity |\n\n### Why this matters to the decision rather than just to the backlog\n\nThe engine's comment at the strip site (`engine.ts:7845-7850`) states the mint path is untouched. That was **true when written** — `sys_api_key` was the only adopter. It is now **false for the second adopter and probably false for the third.** A per-field answer here will be re-litigated at least twice more, and each re-litigation risks a different resolution.\n\n⇒ The two candidate shapes already on this card — a **create-site exemption** (measurably insufficient alone, since the read path strips too and `revoke-other-sessions` silently no-ops) versus a **purpose-built privileged accessor**, which `engine.ts:4801-4804` already names as the intended route *\"if a legitimate system reader ever appears\"* — are worth judging against **all four rows above**, not just this one.\n\n⛔ Still not choosing between them. That is the ruling, and it is yours.\n\n⚠️ **Manual unlock owed on both cards.** Neither #7987 nor this one has an automated unlock — the blocker is a ruling, not a merged PR. Returning both to `pm:queue` once you decide is this seat's responsibility, and it is recorded on the seat sticker (#6367).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5270984787,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-12T18:22:07Z","updated_at":"2026-08-12T18:22:07Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5270984787","body":"**Maintainer ruling** (2026-08-12, live PM chat session `session_01GxKQfv3k8b6a2d2QrZU411`, verbatim: 「接受你的全部建议。」 accepting the triage seat's per-card recommendations): **Option 2 — the purpose-built privileged accessor** (the `resolveSecret` pattern, the route `engine.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5270990571,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-08-12T18:22:39Z","updated_at":"2026-08-12T18:22:39Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5270990571","body":"**Maintainer ruling — 2026-08-12** (live PM chat, `session_01GxKQfv3k8b6a2d2QrZU411`; maintainer sam, verbatim: 「接受你的全部建议。」 accepting the decision-box analysis in full). Moved out of the decision box → `pm:queue`.\n\nRuling: **option 2 — a purpose-built privileged accessor** (the `resolveSecret` pattern, which `engine.ts:4801-4804` already names as the intended route \"if a legitimate system reader ever appears\"). Option 1 (mint-site exemption alone) is measured insufficient — it does not restore `revoke-other-sessions` / sliding-refresh / expired-cleanup, which read the token back through the general path. This is consistent with the #7728 ruling that `internal: true` is the channel-3 read-protection mechanism; #7823's measurement (yes, `sys_session.token` serializes, replay-proven admin cross-user) decides its second application point.\n\nBinding scope for dispatch:\n- Apply `internal: true` to `sys_session.token`, and build the accessor so mint (`createWithHooks`), `revoke-other-sessions`, sliding-expiry refresh, and expired-session cleanup all read through it — none may silently no-op. The card's code trace flags `revoke-other-sessions` returning `{status: true}` while deleting nothing; that regression must be closed, not reproduced.\n- Judge as a **contract over all four columns in the family** (`sys_api_key.key` landed, `sys_session.token` here, `sys_account` OAuth tokens, `sys_account` hashes) so the accessor shape is reused, not re-invented per column.\n- **#7987 is blocked on this exact ruling and is more exposed** (a refresh flow's purpose is reading a stored `refresh_token` back) — unblock it once the accessor lands. **#8018** interacts: if `internal: true` lands, its obvious repair is foreclosed — coordinate.\n- Disclosure stays unlabeled/un-embargoed pending landing, per the maintainer's standing handling of this card. The engine accessor is the fix surface (lands in `packages/objectql`); the flag marking is in `sys-session.object.ts`. Dev claims both.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5279054807,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T10:21:15Z","updated_at":"2026-08-13T10:21:15Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5279054807","body":"## Taken over by the `domain:engine-core` seat — and escalated. `pm:queue` → `needs-user-decision`.\n\n**Authorization (maintainer direct-dispatch channel, standing since 2026-08-10), quoted verbatim:**\n\n> **「7823 你直接接手」**\n\nPreceded by the maintainer's question on this card: 「这个改了一半，要合并吗？」\n\nSeat #6019, session `session_01RDTnVvsgA6cUZ4xFVtPZRy`. Assignee moved to this seat under that instruction — ⛔ **the previous assignee (`huangyiirene`) and the `domain:metadata` seat that opened PR #7996 did not yield this voluntarily; it was reassigned on the maintainer's word.** Recording that plainly so it is not mistaken for a claim race.\n\n---\n\n## ⭐ First: this card's own \"if it IS live\" instruction is STALE and must not be followed\n\nThe body says the fix vehicle is *\"the same one #7728 is parked on … awaiting a maintainer ruling\"* and instructs: report the dependency and stop.\n\n**#7728 is CLOSED `completed`** — merged as PR #7920 on 2026-08-12 05:58Z, which **minted the `internal: true` flag**. There is no parked ruling to wait on. ⛔ Do not block this card on it.\n\n## The measurement, and why PR #7996 cannot merge\n\nPR #7996's central claim — the premise that let it be a one-liner rather than a ruling — is:\n\n> *\"No reader exists, so the no-carve-out design of #7728 holds here unchanged.\"*\n\n**Falsified by its own CI.** 8 jobs red for ~24h, and the signature is:\n\n```\nError: verify signIn: no token in response\n```\n\n⚠️ ⛔ **This is NOT the documented `verify signIn failed: 500` flake.** That one is a transport failure; this is a **200 with the token missing from the body** — different mechanism, opposite conclusion. Re-running CI would burn a round and leave the defect untouched.\n\nThe reader is located and named:\n\n```\npackages/verify/src/harness.ts:635   if (!data.token) throw new Error('verify signIn: no token in response');\npackages/verify/src/harness.ts:653   … 'verify signUp: no token in response'\n```\n\n⭐ **And it is reading the RIGHT thing.** That is not a data-API read of someone else's session row — it is the **sign-in / sign-up response handing the caller its own freshly minted credential**, which is the entire mechanism by which any client obtains a bearer.\n\n## ⭐ The real defect in the fix — a distinction the card never had to make before\n\n- **The measured leak** is an admin reading **other users'** tokens off `GET /api/v1/data/sys_session`. Real, replay-proven, impersonation surviving revocation. ⛔ Nothing here argues for dropping it.\n- **What the fix broke** is the **credential handoff at mint time to its own owner** — never part of the leak.\n\n`internal: true` is honoured, per PR #7996's own text, *\"at `Engine.maskSecretFields`' collector branch **and the two write-response sites**\"*. That conflation is harmless for `sys_api_key.key` (a stored **hash**; the show-once plaintext is a different field) and **breaks authentication** for `sys_session.token`, where the column **is** the show-once credential.\n\n⇒ **The flag as shipped does not distinguish \"never readable\" from \"never returned, including at mint time.\"** For a credential column those are opposite requirements.\n\n## Why this is the maintainer's call and not mine\n\nThe direction is not in doubt — a platform whose sign-in returns no credential is not shippable. **The mechanism is the question**, and it lands on a **just-minted flag** and on the **authentication flow**, which is explicitly on this loop's human floor (*security/permission boundaries — auth flows*). It also reaches back over `sys_api_key.key`, already shipped with the same flag.\n\n### Four-lens block\n\n1. **Platform long-term coherence** — one flag currently means two things (\"absent from generic reads\" and \"absent from write responses\"). Splitting them is the coherent shape; a per-field exception list is special-case growth on a flag that is two days old.\n2. **Measured business pull** — the leak is measured and replay-proven; the breakage is measured by CI on the platform's own verify harness. **Both halves are live**, so \"defer\" is not available.\n3. **AI-agent error-resistance** — as shipped, an author who marks a credential column `internal: true` **silently breaks authentication** and only finds out via a dogfood job whose message names neither the flag nor the field. A flag that fails this way is a footgun regardless of which option is chosen.\n4. **Startup scope discipline** — the narrow read (strip only on the generic data path) is smaller than a new flag or a route allowlist, and leaves `sys_api_key.key` behaving exactly as it does today.\n\n### Options\n\n- **A — `internal: true` stops applying at the write-response sites**; it means \"never returned on the **generic data path**\". The leak still closes (it was a data-API read); sign-in keeps delivering the caller's own token. ⚠️ Needs a check that no write-response leak was closed *only* by that limb.\n- **B — keep write-response stripping, add a sanctioned carve-out** for the auth mint path. Preserves both, but is a carve-out on a flag whose stated design was \"no carve-outs\" — the thing #7728 deliberately avoided.\n- **C — a second flag** distinguishing read-strip from return-strip. Most explicit, most surface, and ⛔ PR #7996's own text forbids inventing a parallel mask.\n\n**Seat recommendation: A**, on lens 1 and 4 — it removes a conflation rather than adding an exception, and it is the only option that leaves the two-day-old flag simpler than it found it. ⚠️ Contingent on the check in A: if some write-response leak is closed *only* by that limb, A is wrong and B becomes the honest answer.\n\n## State\n\n`needs-user-decision`, assigned to this seat, ⛔ not dispatched. **PR #7996 stays open and red** — ⛔ I have not closed another seat's PR, and it should not be re-run expecting green. The disclosure remains real and unfixed on `main`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5279428506,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T10:49:05Z","updated_at":"2026-08-13T10:49:05Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5279428506","body":"## Maintainer ruling — **Option A**. `needs-user-decision` → `pm:dispatched`.\n\nRecorded by the `domain:engine-core` seat (#6019), session `session_01RDTnVvsgA6cUZ4xFVtPZRy`, from the maintainer's live selection on the decision this seat escalated ~10:2xZ.\n\n### The ruling\n\n**`internal: true` stops applying at the write-response sites.** It narrows to mean exactly: *the declared value is never returned on the **generic data path***.\n\n- The disclosure still closes — it **was** a data-API read (`GET /api/v1/data/sys_session`, admin reading other users' rows).\n- `signIn` / `signUp` keep delivering the caller its **own** freshly minted token.\n- **`sys_api_key.key` behaviour is unchanged** — it is a stored hash, and its show-once plaintext is a different field, so the write-response limb was never doing work for it.\n\n⛔ **Options B (auth carve-out) and C (a second flag) are not taken.** ⛔ Do not fall back to either.\n\n### ⚠️ The ruling is PREMISE-CONDITIONAL — and the premise is not yet verified\n\nI raised this condition when recommending A and it is **still unmeasured**:\n\n> **If any write-response leak is closed *only* by the write-response limb, A is wrong and B becomes the honest answer.**\n\n**That check is step one of the dispatch, before any edit.** Enumerate what the write-response strip is actually protecting today across every field carrying `internal: true` (`sys_api_key.key` and, on this branch, `sys_session.token`) and any other declared since.\n\n⛔ **If the premise is falsified — i.e. removing the limb would newly expose something on a write response — STOP AND REPORT A FORK.** ⛔ Do not silently implement B, do not add a carve-out, and do not narrow the ruling to \"only for `sys_session`.\" A fork report is the correct and valued outcome; quietly switching options is the failure this clause exists to prevent.\n\n### Vehicle: PR #7996 is RESUMED, ⛔ not replaced\n\n⚠️ That PR belongs to the `domain:metadata` seat, opened on the implementing dev's behalf. Its branch already carries the field declaration, the persona table, and the replay proof — **all of which stand**; the only thing falsified was the *no-reader* premise, and the ruling above is what answers it. A second PR is also **mechanically impossible**: the `No other open PR may claim the same issue` gate would refuse it.\n\nSo the work continues on `claude/issue-7823-session-token-internal`. Cross-seat declaration owed to `domain:metadata` (#6367) for the vehicle takeover and the `platform-objects` field declaration — filed separately by this seat.\n\n### What the CI red means now\n\n`verify signIn: no token in response` was **the fix working exactly as designed**, on a limb the ruling now removes. ⛔ It was never the documented `verify signIn failed: 500` flake. Under A it should go green **because the limb is gone**, ⛔ not because CI was re-run.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5279432176,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T10:49:28Z","updated_at":"2026-08-13T10:49:28Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5279432176","body":"Claim: PM loop round 4 — **RESUMING PR #7996's branch, ⛔ not a fresh implementation**\nSession: `session_01RDTnVvsgA6cUZ4xFVtPZRy`\nBranch: `claude/issue-7823-session-token-internal` (**existing — the `domain:metadata` seat's; 3 commits**)\nWorktree: `objectstack-issue-7823`\nDomain: `domain:engine-core` (⚠️ **taken over on the maintainer's direct instruction** —「7823 你直接接手」— from `huangyiirene` / the `domain:metadata` seat; ⛔ not yielded voluntarily)\nFile surface: `packages/objectql/src` (the `internal` honouring sites) + `packages/platform-objects/src/identity/sys-session.object.ts` (already on the branch) + wherever the two write-response sites live — **enumerate them with their packages** (stop on breach; explain in the report)\nContainer & model: `M–L`, `mode:subagent`, **`model: claude-fable-5`**\nSerial constraints cleared: no open PR touches `objectql`'s secret-field path other than this branch; this lane's own in-flight work is **#7519 / PR #8377** (`metadata-facade.ts` — disjoint). ⚠️ The branch is ~24h old and `main` has moved a great deal (including **5 landings from this lane today**, two of them in `engine.ts`) — **merging `origin/main` is step one after the premise check.**\n\n## Model rationale\n\n**`claude-fable-5`, no downward discretion.** This changes the honouring sites of a **two-day-old contract flag** and it sits on the **authentication flow** — the surface where an error is most expensive. That the eventual diff may be small is ⛔ not the criterion.\n\n## Why the assignee moved\n\nThe maintainer asked 「这个改了一半，要合并吗？」, this seat measured that PR #7996's central no-reader premise was falsified by its own CI, and the maintainer then instructed 「7823 你直接接手」. Recording the provenance because an unexplained reassignment across accounts is indistinguishable from a claim race.\n\n⛔ **The previous work is not discarded**: the field declaration, the persona table and the replay proof all stand. Only the *no-carve-out-needed* premise was wrong, and the ruling above is what replaces it.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5279712025,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T11:17:57Z","updated_at":"2026-08-13T11:17:57Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5279712025","body":"<!-- os-dev-report -->\n```json\n{\n  \"issue\": 7823,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-7823-session-token-internal\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/7996\",\n  \"premise_still_valid\": true,\n  \"summary\": \"FORK REPORT, per the ruling's own stop clause — no implementation. The Option-A operating premise ('no write-response leak is closed only by the write-response limb') is FALSIFIED, measured on a real engine at the merged tip: with the update-site strip neutralized for exactly one call, PATCH /data/sys_api_key/{id} answered 200 with `key` present — a 64-hex STORED HASH, differing from the show-once plaintext — while the unpatched generic read path returned the same row without `key`, and with the strip restored the body was clean again. So the update-response limb is the SOLE closure of #7728's fourth measured surface; an existing CI pin enforces exactly this (packages/qa/dogfood/test/api-key-hash-not-serialized.dogfood.test.ts, 'the revoke lifecycle still works, and its PATCH response carries no key either'), and plain limb removal both reopens a measured leak and violates the ruling's own 'sys_api_key.key behaviour must be unchanged' bullet — the ruling's three requirements cannot all hold under literal limb removal. The premise HOLDS for the other two flagged fields: sys_session and sys_http_delivery expose no generic write door (apiMethods get/list only; PATCH and POST measured refused). Field enumeration is closed at three: sys_api_key.key (main), sys_http_delivery.headers_json (main, #8118, which also landed the Engine.resolveInternalField privileged accessor), sys_session.token (this branch only). NOTE on premise_still_valid: it refers to the CARD's premise, which STANDS — the admin-cross-user token disclosure is real, replay-proven, and still unfixed on main; what died is the RULING's premise. Mechanism assumptions verified: (1) collector confirmed at packages/objectql/src/secret-fields.ts:155 `if (def && def.internal === true) out.push(name)`; (2) the two write-response sites are BOTH in packages/objectql/src/engine.ts, locate by symbol `omitInternalFields`: site 1 = insert path, after afterInsert hook dispatch, `this.omitInternalFields(object, rowCtx.result)` (the 201 create body; also what better-auth's createWithHooks reads its created session back through — the mint breakage); site 2 = by-id update path, after afterUpdate, `this.omitInternalFields(object, hookContext.result)` (the PATCH 200 body; its own docblock records the measured #7728 fourth surface). Their external mouths are in packages/metadata-protocol/src/protocol.ts — createData/updateData/cloneData return `record: result` verbatim, no read-path masking touches them. Two further honouring sites for completeness: the aggregate guard (rejectCredentialAggregation folds the flag in, #7984) and the resolveInternalField accessor guard (#8118) — neither is write-response. (3) confirmed: sys_session is managedBy better-auth and the password-type read collector is inert on it. INHERITED vs ADDED: inherited = the 3 commits (field declaration cdd83f3d6, tests+changeset 796c4fade, lifecycle arm 3b1cb330a) — all stand, untouched. Added = the origin/main merge only (183 commits, zero conflicts, pushed as 0c38fbcd8) plus the measurement; no source edits, nothing under packages/spec (fence respected), no labels or assignees touched, PR #7996 not closed/retitled. The PR's CI will re-run on the merge push and should stay red with the inherited signature ('verify signIn: no token in response') — I reproduced that exact failure locally at the merged tip before adding the probe's workaround; it is the limb still present plus the branch's flag, not a new failure. Card body read whole (14/14 comments, no truncation observed).\",\n  \"tests\": \"Throwaway premise probe (vitest over bootStack(showcaseStack), real HTTP + engine; monkey-patched the engine instance's omitInternalFields to a no-op for exactly one call, restored via delete; file removed before commit, never pushed): 4/4 passed. Key evidence: (a) counterfactual PATCH /data/sys_api_key/{id} 200 body carried `key` matching /^[0-9a-f]{64}$/ and !== the minted plaintext; (b) unpatched GET on the same row: no `key`; (c) restored PATCH: no `key`; (d) sys_session PATCH/POST and sys_http_delivery PATCH/POST refused (403/404/405 family); (e) composition arm: POST /auth/revoke-other-sessions answered '200 {\\\"status\\\":true}' while the user's OTHER session still authenticated — silent no-op measured under the read strip; (f) bearer validation itself SURVIVES the read strip — every /auth/get-session in the probe answered 200. Inherited-defect reproduction at merged tip, before the probe workaround: 'Error: verify signIn: no token in response' at packages/verify/src/harness.ts:635. Gates on the merged tree, all exit 0: check:nul-bytes (7591 files), check:i18n (9 packages in sync — object edit added no translatable strings), check:error-code-casing, check:durability-log-level, check:engine-double-contract (191 pinned), check:changeset-gate-self-tests, check:cross-package-test-inputs, check:objectui-changeset, check:test-source-alias, check:type-source-resolution, check-changeset-no-major, check-engine-split-ratio. Derived via node scripts/pm/dispatch-gates.mjs over the inherited surface; the two convention hits it named (check:query-options-erasure, check:type-check-coverage ratchets) are covered by: pnpm --filter @objectstack/dogfood typecheck exit 0. pnpm --filter @objectstack/platform-objects test: 351 passed. The inherited dogfood suite remains red at beforeAll by the known signature — not re-run beyond the reproduction, per the fork stop.\",\n  \"open_questions\": [\n    {\n      \"question\": \"The Option-A ruling cannot be implemented as stated: removing the two engine write-response strips newly exposes sys_api_key's stored hash on the PATCH 200 body (measured; sole closure; existing CI pin), which the ruling itself forbids ('sys_api_key.key behaviour must be unchanged'). Which shape should the fix take?\",\n      \"options\": [\n        \"A-as-ruled (plain removal of both engine limbs): reopens #7728's fourth measured surface, reddens the existing dogfood pin, and makes the field's 'never exposed to clients' description false on one surface again. Real business need: none — no consumer reads the hash off a PATCH body. Long-term: re-breaks a landed contract two days after it landed. AI-error-resistance: the flag's guarantee grows a silent exception. Not viable on any axis.\",\n        \"A-prime (RECOMMENDED): the write-response strip RELOCATES from the engine's two sites to the generic-data-path ingress — packages/metadata-protocol createData/updateData/cloneData, the surfaces that build the REST 201/200 bodies and the only write responses an external caller reaches (measured: the three flagged objects' other write doors are shut). Engine-level write results keep the value, so better-auth's createWithHooks reads the minted token back and signIn/signUp work; the sys_api_key PATCH body stays hash-free at the ingress (behaviour and pin unchanged); the data-API read leak stays closed by the untouched read-path strip. This implements the ruling's own sentence — 'the declared value is never returned on the generic data path' — with no auth-route carve-out (B's shape) and no second flag (C's shape). Cost, stated plainly: response-body policy moves to the ingress, so any FUTURE generic ingress must apply the same shared helper or it silently leaks — mitigated by a single exported helper plus the per-field dogfood pins that already exist for both current fields.\",\n        \"B (fallback only if A-prime is rejected): keep both engine limbs, add a sanctioned carve-out for the auth mint path. Declined by the ruling; a named-route exception on a flag designed with none; every future credential column re-raises the same question.\"\n      ],\n      \"recommendation\": \"A-prime. Real business need: both sides are measured — sign-in MUST hand over the minted token (the product's login) and the PATCH body MUST NOT carry the hash (a measured leak surface); A-prime is the only shape both measurements permit without a carve-out. Long-term soundness: the flag comes to mean exactly the ruling's sentence at exactly one boundary — engine read results and generic-ingress write bodies — no per-route exceptions, no second flag, simpler than the conflation it replaces. AI-error-resistance: an author who flags a credential column no longer silently breaks authentication (the failure that cost this card its CI round); the residual risk (a future ingress forgetting the helper) is structural and gate-able, where the current risk fires at runtime with a message naming neither flag nor field.\"\n    },\n    {\n      \"question\": \"Composition with the maintainer's 2026-08-12 Option-2 ruling on this card: under ANY shape that fixes mint, the READ-path strip still starves the better-auth lifecycle routes that read `token` back off find results. Measured at the merged tip: POST /auth/revoke-other-sessions answers 200 {status:true} while the other session keeps authenticating — the exact silent no-op that ruling said 'must be closed, not reproduced'. Also measured: plain bearer validation survives the read strip (get-session 200 throughout), so the exposure is confined to token-readback routes (revoke-other-sessions; sliding-expiry refresh and expired-session cleanup by the dev report's code trace). Does Option A COMPOSE with Option 2 (adapter lifecycle reads go through Engine.resolveInternalField, which landed on main via #8118), or supersede it? The dispatch's done-criteria omit the lifecycle routes, but the inherited test file pins sign-out and the earlier ruling is explicit.\",\n      \"options\": [\n        \"Compose: the write-response half is settled by Q1, and the adapter's lifecycle reads go through the batch privileged accessor already on main. The two rulings answer different halves.\",\n        \"Supersede: ship Option A alone and file the lifecycle no-op as a follow-up card. Ships a fix that leaves a security control answering success while doing nothing.\"\n      ],\n      \"recommendation\": \"Compose. The accessor exists, #8118 established its consumption pattern, and a fix that ships revoke-other-sessions as a measured silent no-op fails the make-it-hard-to-get-wrong axis outright — a 200 {status:true} that revokes nothing is exactly the class of error an AI author cannot see.\"\n    }\n  ],\n  \"out_of_scope_findings\": []\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01RDTnVvsgA6cUZ4xFVtPZRy)_\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5279732399,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T11:20:11Z","updated_at":"2026-08-13T11:20:11Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5279732399","body":"## ⛔ FORK REPORT — Option A's premise is FALSIFIED by measurement. `pm:dispatched` → `needs-user-decision`.\n\n`domain:engine-core` seat (#6019), session `session_01RDTnVvsgA6cUZ4xFVtPZRy`. **No implementation was attempted** — the dispatch's stop clause fired and the dev stopped, which is the correct and valued outcome. ⛔ No source edits, nothing under `packages/spec`, no labels or assignees touched beyond this re-grade, PR #7996 not closed or retitled. The only push is an `origin/main` merge (183 commits, zero conflicts).\n\n### The measurement that kills A-as-stated\n\nThe ruling was conditional on: *no write-response leak is closed **only** by the write-response limb.* Measured on a real engine at the merged tip, with the update-site strip neutralised for exactly one call:\n\n| probe | result |\n|---|---|\n| `PATCH /data/sys_api_key/{id}` with the limb neutralised | **200 with `key` present** — `/^[0-9a-f]{64}$/`, the **stored hash**, ≠ the show-once plaintext |\n| `GET` the same row, unpatched | no `key` |\n| `PATCH` with the strip restored | no `key` |\n\n⇒ **The update-response limb is the SOLE closure of #7728's fourth measured surface.** An existing CI pin enforces exactly it: `packages/qa/dogfood/test/api-key-hash-not-serialized.dogfood.test.ts` — *\"the revoke lifecycle still works, and its PATCH response carries no key either.\"*\n\n**So the ruling's three requirements cannot all hold under literal limb removal**: \"remove the write-response limb\", \"the leak still closes\", and \"`sys_api_key.key` behaviour must be unchanged\" are mutually unsatisfiable. ⛔ Plain removal reopens a measured leak and reddens a landed pin two days after it landed.\n\n⚠️ **The premise holds for the other two fields** — `sys_session` and `sys_http_delivery` expose no generic write door (PATCH/POST measured refused). Field enumeration is **closed at three**: `sys_api_key.key`, `sys_http_delivery.headers_json` (#8118), `sys_session.token` (this branch only).\n\n### ⚠️ The card's own premise STANDS\n\n⛔ Do not read this as the card dissolving. **The admin-cross-user token disclosure is real, replay-proven, and still unfixed on `main`.** What died is the *ruling's* premise, not the *card's*.\n\n### The two write-response sites, located (the dispatch asked; here they are)\n\nBoth in `packages/objectql/src/engine.ts`, by symbol `omitInternalFields`:\n- **site 1 — insert path**, after `afterInsert` dispatch: the 201 create body, **and what better-auth's `createWithHooks` reads the minted session back through** — this is the mint breakage.\n- **site 2 — by-id update path**, after `afterUpdate`: the PATCH 200 body; its own docblock records the #7728 fourth surface.\n\nTheir external mouths are `createData` / `updateData` / `cloneData` in `packages/metadata-protocol/src/protocol.ts`, which return `record: result` verbatim with no read-path masking.\n\n### ⭐ A SECOND live consequence, measured — not in the dispatch's done-criteria\n\nUnder **any** shape that fixes mint, the **read-path** strip still starves the better-auth lifecycle routes that read `token` back off find results:\n\n> `POST /auth/revoke-other-sessions` answers **`200 {\"status\":true}`** while the other session **keeps authenticating.**\n\nA security control reporting success while doing nothing. ⚠️ Bearer *validation* survives the read strip (`/auth/get-session` 200 throughout), so the exposure is confined to token-readback routes (revoke-other-sessions; sliding-expiry refresh; expired-session cleanup by code trace). The sanctioned readback mechanism already exists on `main`: `Engine.resolveInternalField`, landed with #8118.\n\n### What is now in front of the maintainer\n\n**Q1 — the shape.** The dev's **A-prime**: relocate the write-response strip from the engine's two sites to the **generic-data-path ingress** (`metadata-protocol` create/update/clone), the only write responses an external caller reaches. Engine write results keep the value ⇒ mint works; the `sys_api_key` PATCH body stays hash-free at the ingress ⇒ behaviour and pin unchanged; the read leak stays closed by the untouched read strip. It implements the ruling's own sentence — *\"never returned on the generic data path\"* — with **no auth carve-out (B) and no second flag (C)**. ⚠️ Stated cost: response-body policy moves to the ingress, so a **future** generic ingress that forgets the shared helper leaks silently — mitigated by one exported helper plus the per-field dogfood pins that already exist.\n\n**Q2 — composition.** Whether the lifecycle readback routes go through `resolveInternalField` in this card (compose) or ship later (supersede). ⛔ Shipping `revoke-other-sessions` as a measured silent no-op is not a neutral option.\n\n⛔ **This seat is not choosing.** The ruling was the maintainer's, its premise is dead, and re-deciding it here is exactly what the stop clause forbids.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5281187765,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T13:39:09Z","updated_at":"2026-08-13T13:39:09Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5281187765","body":"## Maintainer ruling — **A-prime + compose**. `needs-user-decision` → `pm:dispatched`.\n\nRecorded by the `domain:engine-core` seat (#6019), session `session_01RDTnVvsgA6cUZ4xFVtPZRy`, from the maintainer's live confirmation of the four-dimension analysis (real business need / long-term mechanism / AI-error-resistance / cost surface).\n\n⚠️ **This ruling REPLACES the 2026-08-13 Option-A ruling on this card, whose premise was falsified by measurement.** ⛔ Option A as literally stated (plain removal of both engine limbs) is dead and must not be re-derived — the update-response limb is the SOLE closure of #7728's fourth surface.\n\n### Q1 — the shape: **A-prime**\n\n**The write-response strip RELOCATES** from the engine's two `omitInternalFields` sites to the **generic-data-path ingress** — `createData` / `updateData` / `cloneData` in `packages/metadata-protocol/src/protocol.ts`, the surfaces that build the REST 201/200 bodies and the only write responses an external caller reaches.\n\nConsequences, each one a done-criterion below:\n- Engine-level write results **keep** the value ⇒ better-auth's `createWithHooks` reads the minted session back ⇒ `signIn` / `signUp` work.\n- The `sys_api_key` PATCH body stays hash-free **at the ingress** ⇒ behaviour and the existing dogfood pin unchanged.\n- The data-API read leak stays closed by the **untouched** read-path strip.\n- ⛔ **No auth-route carve-out (B's shape) and no second flag (C's shape).**\n\nThis implements the flag's sentence — *\"the declared value is never returned on the generic data path\"* — at exactly one boundary.\n\n### ⭐ Ruling condition — the residual risk is gated ON THE SAME PR, not deferred\n\nA-prime's one honest cost is that response-body policy moves to the ingress, so a **future** generic ingress that forgets the shared helper leaks silently. The ruling does **not** accept that as a future problem:\n\n**The PR must ship a tripwire asserting that every generic ingress goes through the shared helper**, alongside a single exported helper. ⛔ A PR that relocates the strip without the tripwire does not satisfy this ruling. The point of choosing A-prime over B was error-resistance; shipping it ungated would trade a runtime failure for a silent one.\n\n### Q2 — lifecycle readback: **COMPOSE, same card**\n\nThe read-path strip starves the better-auth lifecycle routes that read `token` back off find results. Measured: `POST /auth/revoke-other-sessions` answers **`200 {\"status\":true}` while the other session keeps authenticating.**\n\nThose routes go through **`Engine.resolveInternalField`** — the privileged batch accessor already on `main` from #8118, whose consumption pattern that card established. Scope is bounded to token-readback routes: revoke-other-sessions, sliding-expiry refresh, expired-session cleanup. ⚠️ Plain bearer validation is **not** affected (`/auth/get-session` measured 200 throughout) — ⛔ do not widen into it.\n\n⛔ Shipping a revoke endpoint that answers `200 {status:true}` and revokes nothing is not an acceptable interim state.\n\n### Vehicle unchanged\n\nWork continues on **`claude/issue-7823-session-token-internal` / PR #7996** — the `domain:metadata` seat's branch, resumed under the maintainer's takeover instruction. ⛔ Never a second PR: the `No other open PR may claim the same issue` gate forbids it. The 3 inherited commits stand.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5281191260,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T13:39:28Z","updated_at":"2026-08-13T13:39:28Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5281191260","body":"Claim: PM loop round 6 — **RESUMING PR #7996's branch under the A-prime ruling**\nSession: `session_01RDTnVvsgA6cUZ4xFVtPZRy`\nBranch: `claude/issue-7823-session-token-internal` (existing — 3 inherited commits + this seat's `origin/main` merge `0c38fbcd8`)\nWorktree: `objectstack-issue-7823`\nDomain: `domain:engine-core`\nFile surface: `packages/objectql/src/engine.ts` (the two `omitInternalFields` sites) · `packages/metadata-protocol/src/protocol.ts` (`createData`/`updateData`/`cloneData` ingress + the shared helper + its tripwire) · the better-auth adapter's lifecycle readback via `Engine.resolveInternalField` · `packages/platform-objects/src/identity/sys-session.object.ts` (declaration already inherited). Stop on breach; explain in the report.\nContainer & model: `M–L`, `mode:subagent`, **`model: claude-fable-5`**\nSerial constraints cleared: the lane is otherwise empty (#7519 and #7737 both landed and closed this shift); no open PR touches `objectql`'s secret-field path or the `metadata-protocol` write ingress other than this branch.\n\n⚠️ **`main` has moved again since the branch's last merge** (PR #8377 landed 11:48Z among others) — re-merge `origin/main` as step one.\n\n## Model rationale\n\n**`claude-fable-5`, no downward discretion.** This relocates a contract flag's enforcement across a package boundary, on the **authentication flow**, and composes a second ruling on top. The card has already burned one CI round and one falsified ruling; the expensive error here is a **silent** one.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5283091702,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T16:05:44Z","updated_at":"2026-08-13T16:07:16Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5283091702","body":"os-dev-report\n```json\n{\n  \"issue\": 7823,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-7823-session-token-internal\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/7996\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Verification/finishing pass on an already-implemented branch (A-prime + compose ruling). Re-merged origin/main (7 commits behind, clean auto-merge on protocol.ts and rest-server.ts), fixed one real defect I found (the tripwire's fixture engine's delete/update were unpinned engine doubles per check:engine-double-contract -- routed them through assertEngineDeleteDispatch/assertEngineUpdateDispatch from @objectstack/metadata-core, matching the pattern in protocol.batch-verb-driver-text.test.ts), verified all four named discriminators with direct evidence, ran the full test suites of every touched package plus 16 derived lint/lifecycle gates, and rewrote the PR body to drop the three falsified claims while keeping the sections the dispatch named to keep.\",\n  \"tests\": \"Discriminator 1 (regression sentinel): api-key-hash-not-serialized.dogfood.test.ts is GREEN and byte-for-byte untouched by this branch (git log shows its last touch is 4c5e80e, #7728) -- 5/5 tests, isolated run. Discriminator 2 (verify harness green because the limb moved): ran a direct probe -- POST /auth/sign-in/email returns keys redirect/token/user with a real 32-char token; the SAME session read via GET /data/sys_session/{id} comes back with 13 other columns and no token key; this is causal, not a re-run -- the dogfood suite (which throws 'verify signIn: no token in response' on failure) passed cleanly. Discriminator 3 (tripwire actually fails): temporarily removed the omitInternalFieldsFromWriteResponse call from createData in protocol.ts (working tree only, HEAD already had the real fix committed), re-ran the tripwire suite -- 'createData: response never carries the internal sentinel' went RED with 'createData leaked an internal field: vault_secret=INTERNAL-SENTINEL-7823-NEVER-SERIALIZED'; restored via `git checkout HEAD -- packages/metadata-protocol/src/protocol.ts`, confirmed byte-identical via `git hash-object` (dd33fb51...), re-ran -- 18/18 green. The enumeration is a RUNTIME PROTOTYPE WALK over Data-suffixed methods, not a hand-written list of today's three -- confirmed by reading the source and by the suite's own built-in NEGATIVE CONTROL test, which adds a leakyData method to a subclass and proves both that the enumeration picks it up and that the sentinel scan catches its leak. This is a real forcing function for a future fourth ingress, not a gap -- no finding here. Discriminator 4 (revoke-other-sessions genuinely revokes): 'revoke-other-sessions ACTUALLY revokes -- the other session stops authenticating' is GREEN in session-token-not-serialized.dogfood.test.ts (isolated run, 320ms), asserted on the OTHER session's liveness (stillAuthenticates===false), not the previously-lying status code. The where:token liveness check is also GREEN: 'the by-token session lookup still resolves server-side' -- ql.find with isSystem context matches the row (proving storage/index intact) while the returned row itself carries no token key (proving the strip is unconditional, no carve-out even for isSystem). Full test suites: @objectstack/metadata-protocol 82 files/1214 tests green; @objectstack/objectql 197 files/3539 tests green; @objectstack/plugin-auth 52 files/1182 tests green; @objectstack/rest 112 files/1843 tests green; @objectstack/platform-objects 17 files/351 tests green. typecheck (tsc --noEmit) clean on objectql, plugin-auth, rest, platform-objects. Gates (all green): check:engine-double-contract (was RED before my fix -- 2 unpinned doubles in the new tripwire fixture; fixed and re-verified OK, 199 pinned/133 debt/2 exempt), check:nul-bytes, check:i18n (dispatch-flagged as expected for the object-definition edit -- OK, 9 packages in sync), check:adr-anchors, check:authz-resolver, check:cross-package-test-inputs, check:docs-audit-scope, check:durability-log-level, check:filter-alias-parity, check:meta-type-normalized, check:route-envelope, check:stack-collection-maps, check:test-source-alias, check:type-source-resolution, check:query-options-erasure (convention-triggered by new test files), check:type-check-coverage (convention-triggered). check:dev-prereqs reported 8/67 packages missing dist -- these are OUTSIDE my build filter scope (client-react, studio, embedder-openai, etc.), not a defect in this change; CI's own 'Build Core' check on this exact head is green. check:engine-split-ratio is an informational ADR-0076 metric (95.8%), not pass/fail. CI on the current head (2c86a2227) has already converged: 27/27 non-skipped check runs green, including Check Changeset, TypeScript Type Check, ESLint, Dogfood Regression Gate (3 shards), Dogfood Verify CLI, Temporal Conformance, and 'No other open PR may claim the same issue' -- observed at report time, not waited for.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": []\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code/session_01RDTnVvsgA6cUZ4xFVtPZRy)_\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5283144992,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T16:09:00Z","updated_at":"2026-08-13T16:09:00Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5283144992","body":"## ACCEPT — all four discriminators verified with direct evidence. PR #7996 flipped ready, auto-merge armed.\n\n`domain:engine-core` seat (#6019), session `session_01RDTnVvsgA6cUZ4xFVtPZRy`. CI independently re-read at head `2c86a222`: **27 check runs — 25 `success`, 2 `skipped`, zero failures.**\n\n### The four discriminators — each measured, not asserted\n\n**1. Regression sentinel intact.** `api-key-hash-not-serialized.dogfood.test.ts` is green **and byte-for-byte untouched** — `git log` shows its last touch is `4c5e80e` (#7728). ⭐ This was the check that would have exposed A-prime landing at the wrong layer, and it did not fire.\n\n**2. `signIn` carries its token causally, not by re-run.** Direct probe: `POST /auth/sign-in/email` → `{redirect, token, user}` with a real 32-char token; the **same session** read via `GET /data/sys_session/{id}` comes back with 13 other columns and **no `token` key**. ⛔ Not a flaky job re-run — the two halves were observed in one session.\n\n**3. ⭐ The tripwire genuinely fails when bypassed — demonstrated, and it is not a hand-written list.** The helper call was temporarily removed from `createData`; the suite went **RED** with `createData leaked an internal field: vault_secret=INTERNAL-SENTINEL-7823-NEVER-SERIALIZED`; restored via `git checkout HEAD --` and confirmed **byte-identical by `git hash-object`** before re-running green.\n\n⭐ And it answers the question the ruling actually cared about: **the enumeration is a runtime prototype walk over `Data`-suffixed methods**, not today's three spelled out. The suite carries its own **negative control** — it adds a `leakyData` method to a subclass and proves both that the walk picks it up and that the sentinel scan catches its leak. ⇒ A future fourth ingress is caught **structurally**. The ruling's condition is satisfied.\n\n**4. `revoke-other-sessions` genuinely revokes.** Asserted on the **other session's actual liveness** (`stillAuthenticates === false`), ⛔ not on the status code that was previously lying.\n\n**Plus the reverse half**, without which \"absent\" proves nothing: the by-token lookup **still resolves server-side** — `ql.find` with an `isSystem` context matches the row (storage and unique index intact) while the returned row carries no `token`. ⭐ Note what that also shows: the strip is **unconditional — no carve-out even for `isSystem`**, which is the shape the ruling asked for.\n\n### A real defect found and fixed during verification\n\nThe new tripwire's own fixture engine carried **unpinned `delete`/`update` doubles**, red under `check:engine-double-contract` — the exact toolchain trap AGENTS.md warns about. Routed through `assertEngineDeleteDispatch` / `assertEngineUpdateDispatch`, matching the existing pattern. ⭐ A verification pass that finds a defect in the thing being verified is the pass working.\n\n### Scope note\n\n`Fixes #7823` is correct: the leak is shut **and** `revoke-other-sessions` revokes. Both halves of the ruling — A-prime and the composed lifecycle readback — landed together, as ruled.\n\nFive packages' full suites green (`metadata-protocol` 1214, `objectql` 3539, `plugin-auth` 1182, `rest` 1843, `platform-objects` 351), plus 16 derived gates including `check:i18n` (the convention-triggered one path derivation cannot name — #8352).\n\n⚠️ Cross-seat declarations for `packages/metadata-protocol` and `packages/plugins/plugin-auth` are being filed now that the report names them.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5284041225,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-13T17:19:03Z","updated_at":"2026-08-13T17:19:03Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/7823#issuecomment-5284041225","body":"## LANDED — PR #7996 merged 2026-08-13 16:25:38Z. The disclosure is closed on `main`.\n\n`domain:engine-core` seat (#6019), session `session_01RDTnVvsgA6cUZ4xFVtPZRy`. Closed by the merge via `Fixes`, which is correct here: the leak is shut **and** `revoke-other-sessions` genuinely revokes — both halves of the ruling landed together.\n\n### What was actually wrong, in the end\n\n⛔ Not what the card said when it was filed, and ⛔ not what the first fix assumed.\n\nThe card opened as a **hypothesis** — *\"nothing here has been observed on a running system\"* — and its first instruction was to measure rather than fix. That instruction is why this landed correctly: the measurement found an **admin-cross-user** disclosure (not any-authenticated-caller), and it was **replay-proven** — a member's token taken off the admin's data-API read authenticates as that member, and survives the admin's own rights being revoked. That is impersonation, not exposure, and it is what justified the severity.\n\n### The two premises that died, in order\n\n1. ⛔ **\"No reader exists.\"** The first PR body concluded this after grepping the console SPA. A reader did exist — **better-auth's own storage adapter, sitting directly on the engine's insert/update results.** It was not in the layer that was checked. Its CI caught it as `verify signIn: no token in response`, red for ~24h, ⚠️ and that signature is **not** the documented `verify signIn failed: 500` flake — it was the fix working exactly as designed, on a limb that should not have been carrying it.\n2. ⛔ **The first ruling (plain removal of both engine limbs).** Measured impossible: the by-id-update limb was the **sole closure** of #7728's fourth surface — neutralised, `PATCH /data/sys_api_key/{id}` answered 200 with the stored 64-hex hash. The ruling's own three requirements were mutually unsatisfiable, so the dev **stopped and reported a fork** rather than picking a side. ⭐ That stop is why the second ruling could be made on evidence instead of on a guess.\n\n### What landed\n\n**A-prime**: the write-response strip **relocated** from the engine's two `omitInternalFields` sites to the generic-data-path ingress, through one exported helper. Engine-level write results keep the value ⇒ mint works. `sys_api_key.key` behaviour is **byte-for-byte unchanged** — its dogfood pin untouched since `4c5e80e` and green, which was the regression sentinel for \"did this land at the wrong layer.\"\n\n**Compose**: the lifecycle readback routes go through `Engine.resolveInternalField` (#8118), and **fail closed and loudly** if a stripped session row ever meets an engine with no accessor — ⛔ rather than degrading into another silent no-op, which is the failure this half existed to remove.\n\n⭐ **The ruling's condition was met structurally, not nominally.** The tripwire enumerates `*Data` faces by a **runtime prototype walk**, ⛔ not a hand-written list, and carries a live negative control. It was reverse-verified: helper removed ⇒ RED with the sentinel named; restored ⇒ green, byte-identical by `git hash-object`.\n\n### Follow-up filed: #8497\n\n⚠️ The tripwire walks the **protocol class**. `rest-server`'s direct `ql.update` mouth is covered by the fix but **not by the guard** — a future direct-engine write mouth outside `metadata-protocol` would leak with the tripwire still green. ⛔ Not a defect in this PR; recorded so the gap has a reader.\n\n### Provenance\n\n⚠️ Implemented on the `domain:metadata` seat's branch, taken over on the maintainer's direct instruction («7823 你直接接手»). The inherited work — the field declaration, the persona table, the replay proof — **stands and was never discarded**; only the no-reader premise was wrong. Cross-seat declaration filed to #6367.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
