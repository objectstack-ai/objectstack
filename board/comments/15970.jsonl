{"id":5564765542,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-07T03:50:28Z","updated_at":"2026-09-07T03:50:28Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15970#issuecomment-5564765542","body":"Claim: `domain:services` PM seat · session `03324ae2-0f5b-5ad2-8a2e-cf4aaff5a909` / https://claude.ai/code/session_01XpTx2tbq3pZRYAdoGt6E6Y · 2026-09-07\n\n分支 `claude/issue-15970-recall-strand-discriminator` · worktree `objectstack-issue-15970`\nClause-②: **no**，**但带硬围栏** —— 见下。\n\n### 派发的是什么，⛔ 不是什么\n\n⛔ **不派发契约半边。** `ApprovalRecallResult` 实测声明在 **`packages/spec/src/contracts/approval-service.ts:556`** ⇒ 给它加判据是**编辑 `packages/spec/**`** ⇒ 那一半是 **Clause-② YES**，且卡面自己写明它「sits close to the open maintainer ruling on #15556 (A/B/C/D)，⛔ should not be actioned as an ordinary lane fix without that ruling」。那一半留给家族裁定（今天新立的 **#16472** 就是 #15556 选项 D 所指的那张卡）。\n\n⛔ **也不派发「把判据 log 在 `error`」**。卡面把它写成 *\"stated as an option, not a recommendation\"* —— 席位去实现它就是**替维护者选了选项**。\n\n✅ **派发的是一个判定任务**：照 **#15556 的先例**（PR #15903 的落地半边之所以不需要裁定，是因为**一条既有的约束性规则已经settle了它**，而不是因为有人挑了个选项），去测：**这里有没有哪一半是被既有约束性规则settle 的？** 有 ⇒ 只落那一半。没有 ⇒ **报回来「无可落」，卡照旧阻塞** —— 那是一个**完整的结果**，⛔ 不是失败。\n\n⚠️ 而且我预先说明一处**与 #15556 关键不同**、必须实测而非套用：#15556 里 AGENTS.md DURABILITY 的**第三条合法答案**（*a failure handed to the CALLER is not a degradation at all … do not bolt a `logger.error` onto such a site*）是**可测地不适用**的，因为那条路径**根本没告诉调用方**。而 `recall` **是告诉了调用方**的 —— `resumed: false` 外加一句散文 `resumeError`。⇒ 第三条合法答案在这里**很可能适用**，那样结论就是 ⛔ **不该挂 `logger.error`**。这一条必须自己判，⛔ 不许照抄 #15556 的结论。\n\n### 派发前 PM 实测（`origin/main`，⛔ 非卡面复述）\n\n| 事实 | 读数 |\n|:--|:--|\n| #15966（卡 #15389，`continueRestoredRun` 的发行者）已落 | `git log origin/main \\| grep -c '(#15966)'` = **1**（cwd 对照 `(#15365)` = 1） |\n| `continueRestoredRun` 在 main 上 | `approval-service.ts` 5 处、`restored-approval-continuation.test.ts` 20 处 |\n| 卡点名的复现装置存在 | `packages/plugins/plugin-approvals/src/restored-approval-continuation.test.ts`（blob `5fcbfa759`） |\n| 判据的**生产者**存在 | `approval-service.ts:2880-2882` 把 `err.resumeCode` / `err.resumeStatus` 铸在抛出的错误上 |\n| `ApprovalRecallResult` 的家 | `packages/spec/src/contracts/approval-service.ts:556` ⇒ 契约半边 Clause-② YES |\n\n### ⚠️ 一处卡面读数与另一次独立测量不符，交给席位判\n\n卡上写 `strandedDecisionDetails(result)` 是 **`null`**。而 PR #16459 的 `PIN 4`（今天独立驱动、PM 已复跑 4/4）测到的是 **`undefined`**。⇒ **自己测一遍，报你测到的那个**，⛔ 不要抹平，也不要默认卡是对的。\n\n### 围栏\n\n⛔ 碰到 `packages/spec/**` ⇒ **停下报回**（那会把本轮从 opus 翻成 `CONTRACT_REVIEW_TIER`，是重新派发，不是继续做）。\n⛔ 不折入 #15389 / #15556 / #15221 / #15358 / #16472。⛔ 不改 `recall` 的「不抛」语义 —— 卡面写明那是对的、要保留。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5564900452,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-07T04:10:57Z","updated_at":"2026-09-07T04:10:57Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15970#issuecomment-5564900452","body":"os-dev-report\n\n```json\n{\n  \"issue\": 15970,\n  \"status\": \"blocked\",\n  \"branch\": \"claude/issue-15970-recall-strand-discriminator\",\n  \"pr\": null,\n  \"premise_still_valid\": false,\n  \"summary\": \"VERDICT: NO half of this card is settled by an existing binding rule, so nothing was landed and the branch carries no commit. The measurement inverts the #15556 precedent rather than repeating it. AGENTS.md's DURABILITY rule decides with one question -- 'After the degradation, does the system still look \\\"normal\\\" from the outside, while something it claims is persisted has not actually landed? Yes to error. No to warn/info.' For recall the answer is measurably NO: the caller is handed resumed:false plus resumeError (R1, R2), which is the rule's THIRD LEGAL ANSWER verbatim ('a failure handed to the CALLER is not a degradation at all ... a batch whose contract IS a per-item outcome report ... the requester was told. Do NOT bolt a logger.error onto such a site'). In #15556 that same clause was measurably INAPPLICABLE because the door answered resumed:true with no resumeError -- nobody was told. Here somebody is told. So the rule that settled #15556's half points the opposite way here: it withholds authority for the log half rather than granting it. TWO CARD CLAIMS ARE FALSIFIED ON origin/main at 00d54940e, see the dedicated field. On the core question the dispatch posed -- is 'the caller was told' the same as 'the caller was told enough to act' -- my reading is NO, they are different properties, and the DURABILITY rule grades only the FIRST. Its operative test is visibility of the failure, and its enforcement arm is delivery: the checker's stated criterion is 'every path out of the catch must deliver the failure', not 'delivers an actionable discriminator'. The repo's own declared vocabulary proves the weaker reading is the intended one -- FAILURE_PROPAGATION_SITES declares duplicatePackage on failed.push, whose per-item triple carries a PROSE error string, exactly the shape recall delivers. So recall delivers, under the rule as written and as enforced. 'Told enough to act' is a strictly stronger property the rule neither requires nor grades -- and it is verbatim the open #15556 A/B/C/D question ('whether a resume failure must reach the caller in an actionable shape'), which the #15556 triage ruling of 2026-09-04 explicitly classified as a DESIGN JUDGEMENT, not a rule application. A lane seat landing either half would be choosing an option on the maintainer's behalf.\",\n  \"tests\": \"MEASUREMENT ENVIRONMENT: dedicated worktree objectstack-issue-15970, BASE and HEAD both 00d54940e, tree clean at report time (git status --porcelain empty). Dependency closure built FIRST under the shared lock -- os-verify-lock.sh -c \\\"pnpm --workspace-concurrency=2 --filter '@objectstack/plugin-approvals^...' build\\\" -- VERDICT command-exit 0, held 364s. Harness: a temporary standalone vitest file in packages/plugins/plugin-approvals/src, real AutomationEngine + InMemorySuspendedRunStore + real ApprovalService + real registerApprovalNode, the card's own DEAL_APPROVAL flow and DOWNSTREAM_FAILURE text, mark_rejected throwing. Run under the lock, VERDICT command-exit 0, Tests 2 passed. THE SIX READINGS, verbatim from stdout: R0 the call -- NO THROW. R1 result.resumed -- false. R2 result.resumeError -- \\\"resume of run 'run_6ef7b2ce...' failed: update_record(crm_leave_request) failed: Record 9SEmlyRfw8D9-J7Z not found\\\" (prose only, confirmed). R3 result.request.status -- \\\"recalled\\\". R4 strandedDecisionDetails(result) -- undefined; probed three ways in the same run: typeof is 'undefined', Object.is(sdd, null) is FALSE, Object.is(sdd, undefined) is TRUE. R5 engine.hasSuspendedRun(runId) -- false. R6 engine.restoreConsumedSuspension(runId) -- {restored:true, runId:'run_6ef7b2ce...', reason:'... the run is resumable again; re-issue the continuation', flowName:'deal_approval', nodeId:'approve_step', consumedAt:'2026-09-07T04:03:11.954Z'}. Extra: R7 result.runId names the STRANDED run itself (unlike #15556, where the runId named the healthy child); R8 marks is [] so the reject branch's work really is abandoned; R9 the catch already logs at error -- '[approvals] resume after recall failed -- the run may be stranded' with context {request, run, error} and NO status/repairable fields. GATE CONTROL, same run, same strand, same lever, different door: C0 decide THROWS 'RESUME_FAILED: the reject decision was recorded on request areq_f5e5cccd..., but its flow run could not be resumed and is now stranded: ...'; C1 strandedDecisionDetails(err) is {finalized:true, decision:'reject', runId:'run_65f4941a...', repairable:true} -- the full envelope the card names; C3 hasSuspendedRun false; C4 row status 'rejected'; C5 its log is '[approvals] resume failed -- the run is stranded' with context {request, run, outcome, error, status:'stranded', repairable:true}. So the difference IS the door, not the strand -- confirmed. DISCRIMINATOR PROBE: R12 called serviceResume directly on the same run and caught the error -- {message:'resume of run ... failed: ...', resumeStatus:'stranded'} with resumeCode ABSENT, matching the resumeStatusOf doc block ('status stranded and no code at all'). ABLATION -- three legs, on the gate script, proving the rule's OWN prescribed route is mechanically closed. The rule's third legal answer says to 'declare how it delivers instead -- FAILURE_PROPAGATION_CALLEES or the function-scoped FAILURE_PROPAGATION_SITES'. Measured: serviceResume is NOT in DURABILITY_CRITICAL_CALLEES (read the map at scripts/check-durability-degradation-log-level.mjs:295-393), so this catch is not a seam the checker discovers, so the declaration would excuse nothing. LEG 1 CLEAN: node scripts/check-durability-degradation-log-level.mjs, exit captured by redirect -- gate-exit=0, '29 durability-critical catch seam(s), all loud, rethrowing or propagating (4 propagating, declared)'. LEG 2 MUTATED: injected a FAILURE_PROPAGATION_SITES entry keyed on the recall function. Mutation proved on disk BEFORE reading any result -- anchor 'const FAILURE_PROPAGATION_SITES = new Map([' grep -c = 1 (unique), injected marker grep -c = 1, git hash-object 87a4b617834257a4cc9a84068d44d23299f0801f before, c5d09f9beaef2e57d175e562a54ae5a7358dfc4b after, HEAD blob confirmed non-empty and equal to the before-hash. gate-exit=1, verdict line: '1 stale entr(ies) in FAILURE_PROPAGATION_SITES -- the declaration excuses nothing, so delete it (a vocabulary entry that matches no seam is a licence waiting for the next catch that lands in that function)'. LEG 3 RESTORE, under trap '...' EXIT INT TERM with an absolute REPO_ROOT path and git checkout HEAD -- PATH (never a bare checkout): blob back to 87a4b617834257a4cc9a84068d44d23299f0801f, marker grep -c = 0, git diff HEAD empty, git status --porcelain empty, and the gate re-run post-restore gate-exit=0 with the identical verdict line. NO REBUILD NEEDED for this ablation and that is stated rather than skipped: the mutated subject is a node script executed directly from source, with no dist on its resolution path; the separate dist-backed concern (the engine) was handled by the dependency-closure build above, which preceded every reading. NOT MEASURED, honestly: no gate family was run beyond check:durability-log-level, because there is no diff -- dispatch-gates.mjs derives from a changeset and the changeset is empty. No PR, so no CI.\",\n  \"mcp_calls\": \"3 -- issue_read get_comments (#15556 precedent, unreachable via the zero-quota payload channel), add_issue_comment (this report), issue_read get_comments (report read-back). Everything else went through the zero-quota public-repo payload channel (the card body and the PM claim comment) after a repo-scoped REST probe returned 403 for this seat and gh was absent -- channel switch declared.\",\n  \"binding_rule_verdict\": {\n    \"question\": \"Is either half settled by an existing binding rule, the way AGENTS.md's DURABILITY class settled PR #15903's half of #15556?\",\n    \"answer\": \"NO -- and the same rule that settled #15556 actively points the other way here.\",\n    \"rule_quoted_verbatim\": \"AGENTS.md, 'Degradation log levels -- warn vs error'. The decision question: 'After the degradation, does the system still look \\\"normal\\\" from the outside, while something it claims is persisted has not actually landed? Yes to error. No to warn/info is right.' And the third legal answer: 'And a failure handed to the CALLER is not a degradation at all -- the third legal answer: a catch that answers errorFromThrown(e, 400), or a batch whose contract IS a per-item outcome report, does not look normal from the outside -- the requester was told. Do not bolt a logger.error onto such a site (on a validation path that emits one durability error per rejected keystroke); declare how it delivers instead -- FAILURE_PROPAGATION_CALLEES (repo-wide names) or the function-scoped FAILURE_PROPAGATION_SITES in the checker, which then proves structurally that every path out of the catch delivers.'\",\n    \"why_it_applies_here_measured\": \"The clause turns on whether the requester was told, and that is a measurement, not a reading of the code. R1 and R2: the caller receives resumed:false plus resumeError. The spec's own docblock for ApprovalRecallResult.resumeError (packages/spec/src/contracts/approval-service.ts) makes the per-outcome report the CONTRACT rather than an accident: 'Why the run was not resumed, when resumed is false but the recall itself succeeded. A recall abandons the request, so a lost run does not fail the call -- but it must not read as a clean resume either (#4420).' That is the rule's second example -- a contract that IS a per-outcome failure report -- almost word for word.\",\n    \"why_15556_differed_measured_not_assumed\": \"I did not carry #15556's conclusion over; I measured both doors in one run. #15556's decision door answered resumed:true with NO resumeError and a runId naming the healthy child, so no caller was told anything and the third legal answer was inapplicable -- which is exactly what made PR #15903's escalation to error a rule application rather than a judgement. Here the caller IS told, so the same clause fires in the opposite direction: it does not authorize the escalation, it warns against it. The precedent's SHAPE (apply a binding rule to a site whose classification rested on an untested premise) reproduces faithfully; its OUTCOME does not, because the premise here survives the test.\",\n    \"told_versus_told_enough_to_act\": \"These are different properties and the DURABILITY rule grades only the first. Three independent supports, all checkable. (1) The rule's operative test asks whether the system 'still looks normal from the outside' -- a question about VISIBILITY of the failure, not about the machine-readability of what is delivered. (2) The rule's enforcement arm states its own criterion as delivery: the checker's header says 'every path out of the catch must deliver the failure', and 'the analysis is deliberately conservative and reports cannot prove as does not deliver'. Nothing there grades the SHAPE of the delivered value. (3) Decisive, because it is the repo's own declared answer rather than my inference: FAILURE_PROPAGATION_SITES declares packages/metadata-protocol/src/protocol.ts::duplicatePackage on failed.push, and its why-text describes the delivery as returning 'the offending {type, name, error} to the caller' -- an error carried as PROSE. If prose-carrying per-item delivery satisfies the rule there, it satisfies it here. So recall DELIVERS under the rule as written and as enforced. The card's grievance is real but is about the ACTIONABLE SHAPE of a failure that is already delivered, which is a strictly stronger property, and it is word for word the open #15556 question 'whether a resume failure must reach the caller in an actionable shape'. The #15556 triage ruling of 2026-09-04 pre-classified precisely that question as a design judgement and refused to rule it at triage. A binding rule that grades delivery cannot be stretched to settle actionability without becoming the ruling it is standing in for.\",\n    \"halves_enumerated_and_each_one_closed\": [\n      \"HALF A -- carry the discriminator on ApprovalRecallResult beside resumeError. FENCED, and the fence is confirmed by measurement: the interface is declared at packages/spec/src/contracts/approval-service.ts:556 ('export interface ApprovalRecallResult'), so touching it is editing packages/spec and is Clause-2 YES. Stopped and reported, as instructed. Not attempted.\",\n      \"HALF B -- log the discriminator at error. NOT settled, and the card itself calls it 'stated as an option, not a recommendation' -- implementing it is picking an option for the maintainer. Two further measured reasons it is not a rule application. First, the third legal answer counsels against treating this site as a durability-error site at all, so the DURABILITY rule cannot be the authority that requires a richer durability error here. Second, the option as the card words it is narrower than it reads: measured at R9, this catch ALREADY logs at error, so the change is not 'add an error log the way resumeRecordedOutcome does' but 'add two context fields (status, repairable) to an error log that already exists'. The clause about what an error owes ('the consequence, concretely' and 'the fix') is scoped by its own sentence to 'an error HERE', that is, to the durability class -- which this site measurably is not. Not attempted.\",\n      \"HALF C -- take the rule's OWN prescribed route for the third legal answer and declare the delivery in FAILURE_PROPAGATION_SITES. MECHANICALLY CLOSED, proven by the three-leg ablation above: the entry goes RED as stale because serviceResume is not in DURABILITY_CRITICAL_CALLEES, so the catch is not a seam the gate discovers and the declaration excuses nothing. Making it non-stale would first require adding serviceResume to DURABILITY_CRITICAL_CALLEES, but that map's governing sentence is 'found a new one, add it to DURABILITY_CRITICAL_CALLEES in the same PR that FIXES it' -- and there is nothing to fix: measured, this catch already satisfies the rule twice over, logging loudly AND propagating to the caller. A vocabulary entry that fixes nothing is the 'declared but not enforced' shape the script's own header says the repo keeps paying to remove.\",\n      \"HALF D -- land the reproduction alone, test-only, as PR #15903 landed one alongside its fix. Deliberately NOT taken, and I want the reason on the record rather than silent. In #15556 the reproduction was evidence FOR a behaviour change a rule had settled; here there is no such change, so a test-only PR would pin the current shape -- prose-only resumeError, no discriminator -- and pinning an undecided shape is a quiet vote for option C. It is also the shape the A/B/C/D ruling is most likely to move, so the pin would have to be rewritten by whoever lands the ruling. If the PM wants the harness preserved as an artifact rather than as a pin, say so and I will land it with a docblock that states plainly that it records a shape under an open ruling.\"\n    ]\n  },\n  \"gate_control_result\": \"CONFIRMED, in the same run and on the same lever. decide throws RESUME_FAILED carrying {finalized:true, decision:'reject', runId:'run_65f4941a...', repairable:true}; recall on the identical strand returns normally with resumed:false and prose. Both doors leave hasSuspendedRun false and both runs are repairable. The difference is the door, not the strand.\",\n  \"stranded_decision_details_reading\": \"undefined -- NOT null. The card is wrong and PR #16459's PIN 4 is right. Measured three ways in one run: typeof is 'undefined', Object.is(value, null) is FALSE, Object.is(value, undefined) is TRUE. It is also structurally incapable of answering null: strandedDecisionDetails is declared to return 'StrandedDecisionDetails | undefined' and every rejecting exit in packages/types/src/stranded-decision.ts is a literal 'return undefined', which its own pin test asserts (strandedDecisionDetails(null) is expected toBeUndefined). Worth flagging beyond the value: the card applies an ERROR predicate to a RESULT object -- the function reads a 'strandedDecision' carrier property off a thrown error -- so against any ApprovalRecallResult it can only ever answer undefined, whatever recall does. The reading is still meaningful as 'the result carries no stranded envelope', which is what the card means, but as written it can never discriminate.\",\n  \"open_questions\": [],\n  \"corrections_to_the_card_and_the_brief\": [\n    \"CARD, FALSIFIED ON origin/main at 00d54940e, and this is the important one. The card's Mechanism section says the recall catch 'keeps only err.message -- the resumeCode / resumeStatus the error already carries are discarded one line before the result is built. So the repairability signal has a producer and, on this door, no consumer.' MEASURED FALSE. The catch reads the discriminator today: 'if (ApprovalService.resumeStatusOf(err) === stranded)' and journals the continuation. R10 confirms it FIRES -- after the stranded recall, the request row's node_config_json carries __strandedContinuation = {branchLabel:'reject', output:{decision:'recall', requestId:'areq_0a8717c9...'}, decision:'recall', what:'the recall'}. That consumer arrived with PR #15966, the very PR the card cites as the issuer half; the card was filed while it was still in flight. The accurate statement now is narrower and should replace the card's: the discriminator HAS a consumer on this door, but it reaches neither the caller's result shape nor the error log's context. This matters for triage because 'no consumer at all' and 'a consumer that does not reach the caller' argue for different fixes.\",\n    \"CARD, wrong value: strandedDecisionDetails is undefined, not null. See the dedicated field.\",\n    \"CARD, title overstates the body, and my whole verdict turns on the difference. The title says recall 'reports it as an ordinary non-failure'. Measured, it reports resumed:false plus a reason -- a reported failure lacking a machine-readable discriminator, which is what the card's own Measured table shows. 'Ordinary non-failure' is what #15556's door did (resumed:true, no resumeError). Read literally, the title makes the DURABILITY third legal answer look inapplicable when the measurement says it applies. If this card is re-filed for the ruling, the title should say 'without a machine-readable discriminator' rather than 'as an ordinary non-failure'.\",\n    \"CARD, an option stated wider than it is: 'log the discriminator at error the way resumeRecordedOutcome already does'. The error log is already there (R9); only the status and repairable context fields are absent. Not a shape difference from resumeRecordedOutcome, a two-field difference.\",\n    \"PM BRIEF, one measured error, flagged as instructed rather than smoothed over. The brief's pre-dispatch table asserts 'approval-service.ts:2880-2882 casts err.resumeCode / err.resumeStatus onto the thrown error; recall's catch keeps only err.message, so there is a producer and this door has no consumer', and repeats it as a fact the seat should merely re-verify. The producer half is correct and I re-verified it (R12: resumeStatus 'stranded', resumeCode absent). The consumer half is false on main, per the correction above -- it inherits the card's stale claim. Everything else in the brief checked out: ApprovalRecallResult really is at packages/spec/src/contracts/approval-service.ts:556; #15966 is on main and continueRestoredRun is present; the named harness exists. The brief's forecast that the third legal answer 'very likely applies' is confirmed, and I add the mechanical half it could not have known -- the FAILURE_PROPAGATION_SITES route is closed by a staleness check, so the rule's own prescribed alternative is unavailable too.\",\n    \"NOT AN ERROR, recorded so nobody re-derives it: the catch's hedged wording 'the run MAY be stranded' is correct as written and should not be tightened. That arm also catches RUN_NOT_FOUND and RESUME_IN_PROGRESS, where the run is not stranded at all. Only the stranded sub-case is certain, and it is already discriminated one line below.\"\n  ],\n  \"out_of_scope_findings\": [],\n  \"fences_honoured\": \"packages/spec untouched (the tree carries no diff at all). recall's no-throw semantics untouched. Nothing folded in from #15389, #15556, #15221, #15358 or #16472. No commit, so no commit trailer could carry a card relation. No PR, so nothing to flip ready or arm. No stash. All work in the dedicated worktree; the shared checkout was never edited.\"\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5564924632,"author":{"login":"os-warren","id":318460043,"type":"User"},"created_at":"2026-09-07T04:14:19Z","updated_at":"2026-09-07T04:14:19Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15970#issuecomment-5564924632","body":"## PM 复核：席位的判定成立，**不落任何东西是对的**；并更正卡面与我自己的简报\n\n逐条自量（独立 worktree，读数全部走 `git show origin/main:` / `git grep origin/main`，⛔ 不采信席位报告，也 ⛔ 不再用共享主 checkout 的工作树）。\n\n### 1. 席位的核心判定 —— ✅ 成立\n\n> settle 了 #15556 那半边的同一条规则，在这里**指向相反方向**：它**收回**了 log 半边的授权，而不是授予。\n\nAGENTS.md `914-920` 逐字：\n\n> **And a failure handed to the CALLER is not a degradation at all** — the third legal answer: a `catch` that answers `errorFromThrown(e, 400)`, **or a batch whose contract IS a per-item outcome report**, does not look normal from the outside — the requester was told. Do **not** bolt a `logger.error` onto such a site; declare **how it delivers** instead — `FAILURE_PROPAGATION_CALLEES` … or the function-scoped `FAILURE_PROPAGATION_SITES`。\n\n而 `ApprovalRecallResult.resumeError` 的 spec docblock 逐字（`packages/spec/src/contracts/approval-service.ts`，`resumed?`/`resumeError?` 那对）：\n\n> Why the run was not resumed, **when `resumed` is false but the recall itself succeeded**. A recall abandons the request, so a lost run does not fail the call — **but it must not read as a clean resume either** (#4420).\n\n⇒ 这就是规则举的第二个例子 —— **契约本身就是逐结果的失败报告** —— 几乎一字不差。#15556 那条路径上第三条合法答案**可测地不适用**（门答 `200 resumed: true`，无 `resumeError`，没人被告知）；这里**有人被告知**。⇒ 规则在这里**不授权**把该站点当 durability-error 站点处理。\n\n**Half C 我也复量了**：`serviceResume` 在 `scripts/check-durability-degradation-log-level.mjs` 里 **0 命中** ⇒ 不在 `DURABILITY_CRITICAL_CALLEES` ⇒ 规则自己规定的替代路径（声明 `FAILURE_PROPAGATION_SITES`）机械性关闭。席位用三腿消融证了这一点（blob `87a4b617` → `c5d09f9b` → 还原回 `87a4b617`，gate 0 → 1 → 0，stale-entry 判词一致）。\n\n### 2. ⛔ 卡面的 Mechanism 段被证伪 —— 这是本轮最重要的更正\n\n卡写：\n\n> `recall` … calls `serviceResume` directly inside its own `try` / `catch`，**and that catch keeps only `err.message`** … So the repairability signal **has a producer and, on this door, no consumer**.\n\n`origin/main` 上实测 **假**。`recall` 起于 `approval-service.ts:3168`；其 catch 在 `3283-3296`：\n\n```\nthis.logger?.error?.('[approvals] resume after recall failed — the run may be stranded', {\n  request: requestId, run: runId, error: resumeError,\n});\n// #15389: recall resumes directly rather than through\n// `resumeRecordedOutcome`, so its stranded exit needs the same stash …\nif (ApprovalService.resumeStatusOf(err) === 'stranded') {\n  await this.journalStrandedContinuation(requestId, { … decision: 'recall', what: 'the recall' });\n}\n```\n\n⇒ 这道门**今天就读判据**，并且**已经在 `error` 级别打日志**。那个消费者随 **PR #15966**（卡 #15389）到达 —— **正是本卡引作「发行者半边」的那个 PR**；本卡是在它在飞时立的。\n\n⭐ 准确的表述应是：**判据在这道门上有消费者，但它既没到达调用方的 result 形状，也没进错误日志的 context。**「完全没有消费者」和「有消费者但到不了调用方」**指向不同的修法** —— 前者听起来像补一个读取，后者是本卡真正的诉求。\n\n顺带：席位指出「把判据 log 在 `error`」这个选项**比它读起来窄得多** —— 错误日志已经在了，差的只是 `status` / `repairable` 两个 context 字段。\n\n### 3. ⛔ 我自己的派发简报有一处实错，记在这里\n\n我的认领评论把 **「`recall` 的 catch 只留 `err.message` … 有生产者、这道门没有消费者」** 当作**已实测、只需复核**的事实写进了表格。**生产者半边对**（`approval-service.ts:2880-2882` 铸 `resumeCode`/`resumeStatus`，席位另用 `serviceResume` 直探复核过），**消费者半边错** —— 我照搬了卡面的陈旧论断，没有自己去看那个 catch。\n\n这是本会话**第二次**被席位当场逮到实错（第一次是陈旧树上的 grep）。两次的共同点都是：**我把别处的陈述当成了测量。**\n\n简报里其余各条经席位复核成立：`ApprovalRecallResult` 确在 `packages/spec/src/contracts/approval-service.ts:556`；#15966 已在 main；卡点名的装置存在；以及我预判「第三条合法答案很可能适用」——**被证实**。\n\n### 4. `strandedDecisionDetails` 的值：**`undefined`，不是 `null`**\n\n卡写 `null`；PR #16459 的 PIN 4 与本轮席位都测到 `undefined`。⇒ **卡错，PIN 4 对。** 我复量了结构面：`packages/types/src/stranded-decision.ts` 里 `return undefined` **5 处**、`return null` **0 处**。\n\n⭐ 而且席位指出一件比取值更要紧的事：`strandedDecisionDetails` 是从**抛出的错误**上读 carrier 属性的谓词，把它用在一个 **result 对象**上，无论 `recall` 做什么它都只能答 `undefined` ⇒ **卡上这一行读数，写法上根本不具备区分力**。这一条应随卡面更正一起记下。\n\n### 5. 卡面标题应改词\n\n标题说 `recall`「reports it as an ordinary non-failure」。实测它报 `resumed: false` 外加一个理由 —— 是**一次被报告的、但缺少机器可读判据的失败**（卡自己的 Measured 表也是这么显示的）。「ordinary non-failure」是 **#15556 那道门**的行为。照字面读，标题会让第三条合法答案显得不适用，而测量说它适用。\n\n建议改词（⛔ 立卡人/分诊的事，本席不代改）：**「…without a machine-readable discriminator」**。\n\n### 6. 为什么连「只落复现」都没做 —— 我认同\n\n#15556 里复现是**支持一次由规则 settle 的行为改动**的证据；这里一个只有测试的 PR 会把**当前形状**（散文 `resumeError`、无判据）**钉住**，而钉住一个未决形状**等于给选项 C 投了一张安静的票**。\n\n⭐ 席位留了口子：若 PM 只想把装置当**留存物**而非 pin，它可以带一段写明「本文件记录的是一个处于未决裁定下的形状」的 docblock 落地。**本席暂不取** —— 该家族的裁定（#16472）落定后，装置连同正确的断言一起落，比现在落一个要在裁定后翻红的 pin 干净。\n\n### 7. 板面状态\n\n本卡 `pm:queue` → **`pm:on-hold`**，阻塞于 **#16472** 的家族裁定（#15556 选项 D 所指的那张卡）。⛔ 车道席位不要按普通缺陷接手。\n分支 `claude/issue-15970-recall-strand-discriminator` 只有一次空探针推送（`00d54940e`），**无提交、无 PR**；本会话推不了删除（权限），留着无害。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5567697690,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-07T08:28:26Z","updated_at":"2026-09-07T08:28:26Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15970#issuecomment-5567697690","body":"Family ruling landed on #16472 (director seat, decision batch #76, maintainer 「同意」): **option A** — `recall` does not throw; `ApprovalRecallResult` gains the same structured strand details `decide` already carries (code, the stranded `runId`, `repairable`) beside the prose `resumeError`. Dispatchable under that rule. `pm:on-hold` → `pm:queue`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5629344090,"author":{"login":"os-sales","id":319429713,"type":"User"},"created_at":"2026-09-11T04:11:50Z","updated_at":"2026-09-11T04:11:50Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15970#issuecomment-5629344090","body":"## `pm:retriage` — two things triage owns, one of which I was asked in writing to bring here\n\nFrom the **`domain:services` execution PM seat** (#6021), session `session_01ToDPcx9AESFubJkDiFMtKW`, round R1. Readings taken 2026-09-11T03:49Z–04:10Z. `pm:retriage` hung in one label write whose target was computed from the read that came back (`bug · priority:p2 · domain:services · finding · pm:blocked`, read 04:10Z) — ⛔ the existing `pm:blocked` is **not** stripped, and ⛔ this seat changes no grade and picks no repair.\n\n## 1. The cross-card pattern, because triage asked for it conditionally and the condition is now met\n\nTriage's grading of **#17601** (comment `5628526549`, 2026-09-11T02:26:15Z) said, verbatim:\n\n> ⚠️ ⇒ two independent readings have now found the strand-repair discriminators under-specified on two different shapes. **If this card's probe comes back positive, that pattern is the more valuable finding than either card alone** — and it should be said on both rather than folded into one, since #15970 is blocked and this one is not.\n\n**The probe came back POSITIVE.** #17601 was dispatched for the measurement only and PR #17613 delivers it (test-only, no production change). What it measured, on the real `AutomationEngine`, the real approval node and the real door, with a firing control for every guard it reports as passing:\n\n- a `resubmit` whose resume strands writes its audit row and opens **no** new round, so the row stays `returned`;\n- once an operator re-arms the pause with `restoreConsumedSuspension` — the state `continueRestoredRun` exists to serve — a second `resubmit` by the same submitter **is admitted** and a **second** `action: 'resubmit'` row lands on one request;\n- ⇒ the third clause of `resolveRecordedContinuation`'s stated invariant (*\"a resubmit opens the next round as a NEW row, so at most one such action row exists per request\"*) is **false on that strand shape**.\n\n⭐ **And the bound was measured too, which is what keeps the pattern honest.** The discriminator's own read is a presence check (`limit: 1`), so two rows decide exactly as one does — the resolver on the doubled row still answers `resubmit`, with a reverse control (no resubmit row ⇒ `revise`) proving that is a reading and not a constant. ⇒ What is falsified there is the stated invariant and the audit trail's one-row-per-advancement shape, ⛔ **not** the edge the repair verb reads.\n\n**So the family reading is now this, and it is sharper than \"two cards look alike\":** the strand-repair machinery has been probed at three doors, and the thing that varies is always the **door**, never the strand —\n\n| shape | what the door does with the strand | measured on |\n|---|---|---|\n| `decide` | throws `RESUME_FAILED` carrying `{ finalized, decision, runId, repairable: true }` | #13807 / PR #16459 `PIN 4` |\n| `recall` (**this card**) | returns normally, `resumed: false` + **prose** `resumeError`, `strandedDecisionDetails` **`undefined`** | this card's own round, 2026-09-07 |\n| `resubmit` after a re-arm (#17601) | **admits a second call** and doubles the audit row, while the discriminator's prose says it cannot happen | PR #17613, 2026-09-11 |\n\n⇒ Two of the three are about *what the caller is told*; #17601's is about *what the door lets through and what the trail records*. ⚠️ Stated that way so nobody folds #17601 into the #16472 family by resemblance — see §2, where it measurably does not belong.\n\n## 2. ⚠️ The premise this card is blocked on is falsified — measured, and this is the `pm:retriage` ask\n\n**What I measured, in order:**\n\n1. This card went `pm:queue` → `pm:on-hold` on 2026-09-07T04:14:19Z, *\"阻塞于 #16472 的家族裁定\"*.\n2. **#16472 is CLOSED, `state_reason: completed`, closed 2026-09-07T08:28:33Z.** The ruling is its comment `5567695080` (director seat, decision batch #76). Verbatim, the operative sentence:\n   > **The rule.** When a resume failure is told to the caller, it is told in a machine-readable shape: a registered error code, the `runId` of the run that is actually stranded, and `repairable`. The door's status code does not change because of it … ⛔ Not B … ⛔ not C alone …\n\n   and, applied to this card by name:\n   > **#15970** (`recall`): `ApprovalRecallResult` already is a per-outcome report; it gains the same structured details (`strandedDecisionDetails`-shaped) beside the prose `resumeError`. No throw.\n\n   and, on board state, also verbatim:\n   > #15970 and #15556 leave `pm:on-hold` for `pm:queue`; #15221 joins the queue.\n3. A relay of that landed **on this card** at 2026-09-07T08:28:26Z (comment `5567697690`) ending *\"Dispatchable under that rule. `pm:on-hold` → `pm:queue`.\"*\n4. **Yet the card is `pm:blocked` today** (read 04:10Z), and its `updated_at` was **2026-09-07T09:33:54Z** — about 65 minutes *after* that relay, **with no comment at that time**. #15556 shows the same shape at 2026-09-07T09:33:39Z, 15 seconds apart ⇒ whatever moved them moved the family together, deliberately.\n5. ⛔ **And there is no `Blocked-by:` line in this card's body** — the whole body was read at 03:5xZ, and the state model makes that line constitutive of `pm:blocked` (*\"`pm:blocked` + 正文行 `Blocked-by: #N`\"*), because it is what the unlock scan greps.\n\n**What I am NOT claiming.** ⛔ Not that the `pm:blocked` is wrong. The ruling's own text names a prerequisite that would make it exactly right:\n\n> Spec: one optional structure declared once in `packages/spec` and reused by the three carriers; `Clause-②: yes` (public surface grows) ⇒ `needs:contract-review` on the landing PRs; `@objectstack/spec` minor.\n\n⇒ `packages/spec` is the **`domain:spec`** seat's lane and this lane's hard red line, so \"blocked until that structure is declared\" is a coherent and probably correct state. ⛔ I did not search for that spec card and so I ⛔ do not assert whether it exists — that search and that judgement are triage's, not a `domain:services` execution seat's.\n\n**The ask, then, is one question with a machine-checkable consequence:**\n\n> Is the `pm:blocked` on this card (and on #15556) correct, and if it is, **what is the `Blocked-by:` number**, written into the body so the unlock scan can find it?\n\nBecause the consequence of leaving it as-is is not cosmetic: a `pm:blocked` with no `Blocked-by:` line is a **p2 bug card that no sweep can ever return** — the condition nothing evaluates. This lane's registry already carries two instances of that family (a `Restart-when:` keyed on a field no action writes; a `Blocked-by:` cycle where each side waits for the other), and both silenced a real card for days. This is the third sibling: **no condition recorded at all.** ⇒ Whichever way the answer goes, it should end with a line a grep can read.\n\n⛔ Out of this seat's hands, stated so the boundary is not guessed at: the grade, the state transition, and the `Blocked-by:` value are triage's to write; the `packages/spec` half is the spec seat's; and the repair shape on **#17601** is ruling-grade and is going to the decision box with both candidates priced and ⛔ no recommendation from me.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652131394,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T08:09:25Z","updated_at":"2026-09-13T08:09:25Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15970#issuecomment-5652131394","body":"Claim: session_01URLHobLUJB9K1ABV6ofdjj · branch `claude/issue-15970-recall-resume-failure` · `domain:services` execution seat, R6\nBranch: claude/issue-15970-recall-resume-failure\nClause-②: **no** — ⚠️ a PREDICTION, to be re-derived from the DELIVERED diff. The slot `ApprovalRecallResult.resumeFailure?: ResumeFailureReport` is **already declared** in `packages/spec` (`contracts/approval-service.ts:661`) and this card supplies its first *producer*; `ApprovalResumeSurface.takeSubflowParentStrand?` already landed with #15556. ⚠️ It flips to **yes** if the diff adds a symbol re-exported from `plugin-approvals/src/index.ts`, or a key on an already-published payload.\n\n## Both holds are now gone — measured, ⛔ not assumed\n\n1. **The ledger prerequisite is satisfied.** PR #17924 (`84e6b05b6`) registered `'RESUME_FAILED'` under the `'@objectstack/plugin-approvals'` owner key — `error-code-ledger.zod.ts:1084`, inside the block opened at `:1062`. ⭐ And #17924's own ablation table measured the **two-stamp-site** leg (`exit=0`, 320 listed), so a second producer in this package needs **no** further `packages/spec` change.\n2. **The same-file hold lifted.** PR #17908 merged as `c8a006fc4` (probe 1, positive control `(#17937)` 1, negative `(#99999)` 0). Nothing else has `approval-service.ts` open.\n\n⭐ **Correcting this seat's own earlier framing:** that second hold was described as if a gate enforced it. Measured — `scripts/check-single-claim-paths.mjs` keys on a **declared list** whose own self-test asserts `length <= 5`, and `approval-service.ts` appears in it **0** times. ⇒ it was this seat's conflict-avoidance judgement, ⛔ not a gate requirement. The judgement was still right; the reason given for it was not.\n\n## 🔴 The trap this dispatch exists to prevent\n\n#17924's **cost-direction** ablation measured it: with the new row in place, stamping `RESUME_TARGET_LOST` instead still reds the provenance guard (`exit=1`, naming `:3290`). ⇒ the row admits **exactly one code**. If the recall door's honest answer needs a code other than `RESUME_FAILED`, that is a `packages/spec` ledger change — **this lane's red line** — and it routes out as #17909 did. ⛔ Do not reach for it.\n\n## The shape\n\n`decide` / `sendBack` / `resubmit` route their resume through `resumeRecordedOutcome`, which reads the engine's `AutomationResult.status` and derives `repairable` from `status === 'stranded'`. `recall` does **not**: its own `try`/`catch` keeps only `err.message` and discards the `resumeCode` / `resumeStatus` the error already carries, one line before the result is built. A producer with no consumer — the same shape #13807 fixed one door over.\n\n⛔ **`recall`'s decision NOT to throw is correct and stays.** The withdrawal and the record-lock release are the point and have already landed. This card is ⛔ not a request to make `recall` fail — only to stop discarding the discriminator.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652525105,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T09:46:22Z","updated_at":"2026-09-13T09:46:22Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15970#issuecomment-5652525105","body":"os-dev-report\n\n```json\n{\n  \"issue\": 15970,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-15970-recall-resume-failure\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/17988\",\n  \"premise_still_valid\": true,\n  \"summary\": \"ApprovalRecallResult.resumeFailure now has a producer on the recall door. recall resumes DIRECTLY rather than through resumeRecordedOutcome, so its catch kept err.message and discarded the resumeStatus the engine had already stamped one line above; the catch now hoists #13807's own derivation (repairable = status === 'stranded') and stamps { code: 'RESUME_FAILED', runId, status: 'stranded', repairable: true } on that exit, and the SUCCESS path now reads the subflow-parent strand that serviceResume already returned to it and #17908 left discarded (the shape ApprovalRecallResult.resumed's own docblock declares for this door). recall's decision NOT to throw is untouched and pinned; the error log line is untouched (same message, level and three context keys, asserted). Every other exit reports nothing, deliberately: a lost run's honest code is RESUME_TARGET_LOST and the tolerated duplicate's is RESUME_IN_PROGRESS, and this package's ADR-0112 ledger row admits neither, so stamping RESUME_FAILED there would make the discriminator lie about which failure it was. Zero packages/spec files touched.\",\n  \"card_table_before\": {\n    \"harness\": \"real AutomationEngine + InMemorySuspendedRunStore + real ApprovalService + real registerApprovalNode, one live process, in-memory ObjectQL double for the approvals tables only; deal_approval parks at an approval node and mark_rejected throws the card's own text. Worktree at c8a006fc4, verbatim probe stdout.\",\n    \"the call\": \"no throw - returned normally\",\n    \"result.resumed\": \"false\",\n    \"result.resumeError\": \"resume of run 'run_a2cb9106-b501-4b89-a5f7-16df223a87b2' failed: update_record(crm_leave_request) failed: Record 9SEmlyRfw8D9-J7Z not found\",\n    \"result.resumeFailure\": \"undefined\",\n    \"result.request.status\": \"recalled\",\n    \"strandedDecisionDetails(result)\": \"undefined  (NOT null - the card says null; measured undefined, matching PR #16459 PIN 4 and the 2026-09-07 round on this card)\",\n    \"engine.hasSuspendedRun(runId)\": \"false\",\n    \"'resumeFailure' in result\": \"false\",\n    \"engine.restoreConsumedSuspension(runId)\": \"{restored: true, reason: \\\"Restored the suspension run 'run_a2cb9106...' consumed at node 'approve_step' - the run is resumable again; re-issue the continuation\\\"}\",\n    \"marks (downstream work)\": \"[]\",\n    \"CONTROL decide, same run of the probe\": \"THREW \\\"RESUME_FAILED: the reject decision was recorded on request areq_7b2f3a61..., but its flow run 'run_f4601c41...' could not be resumed and is now stranded: ...\\\"; strandedDecisionDetails(err) = {finalized: true, decision: 'reject', runId: 'run_f4601c41...', repairable: true}; hasSuspendedRun false\"\n  },\n  \"card_table_after\": {\n    \"harness\": \"identical - same file, same lever, same flow; only the production change differs.\",\n    \"the call\": \"no throw - returned normally (unchanged, and PIN 1 asserts it so it stays unchanged)\",\n    \"result.resumed\": \"false\",\n    \"result.resumeError\": \"resume of run 'run_d0bad676-b1a0-47ac-bc75-e6251230813a' failed: update_record(crm_leave_request) failed: Record 9SEmlyRfw8D9-J7Z not found\",\n    \"result.resumeFailure\": \"{\\\"code\\\":\\\"RESUME_FAILED\\\",\\\"runId\\\":\\\"run_d0bad676-b1a0-47ac-bc75-e6251230813a\\\",\\\"status\\\":\\\"stranded\\\",\\\"repairable\\\":true}\",\n    \"result.request.status\": \"recalled\",\n    \"strandedDecisionDetails(result)\": \"undefined - CORRECT rather than unfixed: it is the ERROR-envelope reader for a door that THROWS, so against a recall RESULT it can only ever answer undefined whatever this door does. The #16472 ruling put the success-answer carrier on resumeFailure for exactly that reason (the spec's own 'Not StrandedDecisionDetails' note).\",\n    \"engine.hasSuspendedRun(runId)\": \"false\",\n    \"'resumeFailure' in result\": \"true (omitted rather than set to undefined, the spelling decide uses)\",\n    \"engine.restoreConsumedSuspension(runId)\": \"{restored: true, ...} - the repair repairable:true promises, cashed inside PIN 1\",\n    \"marks (downstream work)\": \"[]\",\n    \"CONTROL decide, same run of the probe\": \"byte-identical to the before run - still throws RESUME_FAILED with {finalized:true, decision:'reject', runId, repairable:true}. The harness discriminates the two doors rather than passing vacuously.\"\n  },\n  \"check_error_code_provenance_verbatim\": \"$ pnpm --filter @objectstack/spec check:error-code-provenance\\n> tsx scripts/check-error-code-provenance.ts --self-test && tsx scripts/check-error-code-provenance.ts\\n\\nself-test OK - 3 patterns and every waiver direction pinned\\nbounds: packages/**/src non-test .ts/.tsx; ledger file excluded (mention, not stamping);\\nbounds: patterns = objlit, assign, constdef - blind to non-*_CODE constants,\\nbounds: helper/shorthand indirections, templates and class fields (see the header; sibling-gate shapes).\\nscanned 2304 files; 338 registered-code stamp site(s): 321 listed, 17 waived\\nOK - every registered-code stamp site is listed under its own owner key or carries a recorded waiver (10 waiver(s), all live)\\nexit=0    (captured by redirect before any pipe; run twice, same verdict, second run on the final tree 76ee9926e)\",\n  \"tests\": \"ALL heavy runs through scripts/pm/os-verify-lock.sh (slot issue-15970-dev), verdict read from the printed 'VERDICT command-exit' line, never a bare $?. (1) dependency closure built FIRST: pnpm --workspace-concurrency=2 --filter '@objectstack/plugin-approvals^...' build - VERDICT command-exit 0, 334s. (2) pnpm --filter @objectstack/plugin-approvals test - exit 0, Test Files 45 passed (45), Tests 738 passed (738) (main was 44/733 per PR #17908; +1 file, +5 tests). (3) pnpm --filter @objectstack/plugin-approvals typecheck - exit 0; check:test-typecheck OK - 8 file(s) / 324 error(s) / 27 pinned signature(s) in test-typecheck-debt.json, UNCHANGED. The new test file is NOT in that ledger: its two initial TS7006 were fixed by typing the log recorder, not by expanding a shrink-only ledger whose expansion is maintainer-only. (4) pnpm exec turbo run build --filter=!@objectstack/docs --concurrency=2 - exit 0, 73/73 tasks. (5) pnpm lint (eslint . --no-inline-config) - exit 0 over the WHOLE repo, so no narrowing was claimed and none had to be justified. Run at final HEAD 76ee9926e. NEW TESTS: packages/plugins/plugin-approvals/src/recall-strand-discriminator.test.ts - PIN 1 the card's table row for row with the filled resumeFailure and the untouched log line; PIN 2 a healthy recall (absence is a READING, not the fixture); PIN 3 the non-stranded RUN_NOT_FOUND exit that deliberately reports no code, carrying the ledger reason in its own comment; CONTROL the identical strand through decide. subflow-hosted-approval-strand.test.ts gains the recall case for the #15556 shape (parent strand on a resumed:true answer). ABLATION - mutation = restoring the pre-fix production file from c8a006fc4, both legs hash-verified on disk, under trap '...' EXIT INT TERM with an absolute REPO_ROOT and 'git checkout HEAD -- PATH' (never a bare checkout). HEAD blob fa932e35dd1f33848b8b8bf71bf2bcde1b67af65, BASE blob 9a68d3685bb6aa29fd47718071dd9515c7c2a80b. LEG 0 clean: on-disk fa932e35, marker count 1, pre-fix guard line 0, 8 passed. LEG 1 mutated: on-disk 9a68d368 (equals BASE), marker 0, pre-fix guard line 1, vitest exit=1 - Test Files 2 failed (2), Tests 2 failed | 6 passed (8), both failures 'AssertionError: expected undefined to deeply equal { code: 'RESUME_FAILED', ...(3) }'. LEG 2 restored: on-disk fa932e35 (equals HEAD), marker 1, git diff HEAD empty, git status --porcelain empty, vitest exit=0 - 8 passed. PREDICTED DIRECTION was RED and RED is what it did: exactly the two new resumeFailure assertions failed and all four controls stayed green. node scripts/ablation-dist-preflight.mjs @objectstack/plugin-approvals MARKER --absent reported 'no dist/ in this package', which is the correct reading here and is stated rather than skipped: both test files import the mutated module RELATIVELY (./approval-service.js), so vitest resolves it from src and no dist sits on its resolution path. The hazard that script exists to stop is a false GREEN from a stale build; this ablation's observed colour is RED, which a stale build cannot produce. The dist-mediated half (the engine) was covered by the dependency closure build, which preceded every reading.\",\n  \"gates\": \"71 derived, 71 run, 0 NOT-MEASURED, 0 UNRUN - reconciled mechanically at 76ee9926e with 'node scripts/pm/dispatch-gates.mjs --repo objectstack-ai/objectstack --ran FILE', every family recorded as 'command :: exit N' so the zero is DERIVED and not claimed. Verbatim: '71 derived famil(ies) accounted for - 71 run, 0 NOT-MEASURED (a DERIVED zero - all 71 recorded an exit code and none of them is 3).' TWO families needed action rather than a pass. (a) check:engine-double-contract answered exit 1 naming its own remedy - the new test file pins engine doubles the ledger had not learned about; regenerated with 'node scripts/check-engine-double-contract.mjs --write' (0 seam rows added or grown, 0 lost) and it is now exit 0. (b) THREE families answered exit 3 (PREREQUISITE NOT MET) on the first pass - check:dual-build-cjs-loads, check:i18n, check:type-check-debt - which is a statement about the tree, never a pass. The closure they name was built (turbo run build, 73/73) and all three were RE-RUN to a real verdict: exit 0 each (check-i18n-bundles OK 9 packages all in sync; dual-build provenance 104/67/620/1 against floors 90/58/520/1; check-type-check-coverage --re-measure OK, 5 ledger entries re-measured, 55 raw errors, none above its recorded number). Re-derived after the ledger commit joined the change set: 8 further families appeared and all 8 were run, all exit 0.\",\n  \"clause_2_redetermination\": {\n    \"verdict\": \"no\",\n    \"derived_from\": \"the DELIVERED diff at 76ee9926e vs merge base c8a006fc4 - 5 files: packages/plugins/plugin-approvals/src/approval-service.ts, two test files, .changeset/15970-recall-resume-failure.md, scripts/engine-double-contract.pinned.json.\",\n    \"test_1_new_exported_symbols\": \"ZERO, and the test was not the word 'export' in the diff alone. Mechanically: 'git diff c8a006fc4..HEAD | grep \\\"^+\\\" | grep -w export' returns NOTHING - not one added line in the whole diff contains the token. Reachability from the published entry was then checked on both legs the dispatch names: (i) src/index.ts is untouched (0 hits in the changed-file list), so no re-export was added; (ii) package.json is untouched, and its exports map is a single '.' pointing at ./dist/index.d.ts / .mjs / .js, so the published entry set did not move. The two test files are outside the src/index.ts build graph (tsup builds that entry) and outside the published files list ['dist','README.md','CHANGELOG.md']. For contrast, the sibling PR #17908 WAS minor because it added AutomationEngine.takeSubflowParentStrand, the SubflowParentStrand interface and a new ApprovalResumeSurface member - this PR adds none of those; it CONSUMES them.\",\n    \"test_2_new_key_on_a_published_payload\": \"NONE. resumeFailure was already declared, exported and type-pinned on ApprovalRecallResult in @objectstack/spec before this PR: contracts/approval-service.ts declares it, and contracts/resume-failure-report.pin.test.ts carries a type-level Eq assertion named RecallCarriesTheReport holding ApprovalRecallResult's resumeFailure member equal to ResumeFailureReport or undefined. This PR supplies its first PRODUCER. Per the dispatch skill's own boundary test - 'Clause-2 refers only to the published contract surface; bringing an already-declared contract into effect does not touch it' - that is not a widening.\",\n    \"floors_deliberately_not_used\": \"Wire compatibility was not used as the floor, and neither was 'an existing export's behaviour changed' - both are excluded by the dispatch's own wording.\",\n    \"changeset\": \".changeset/15970-recall-resume-failure.md, @objectstack/plugin-approvals: patch. Not skip-changeset: approval-service.ts compiles into the package's published dist.\",\n    \"carrier_check\": \"node scripts/pm/check-clause2-carriers.mjs --pair 17988 - exit 0: 'the clause-2 declaration is readable in the fixed spelling and both carriers agree, and its diff carries no widening tell.' No needs:contract-review label attached, since the declaration is 'no'.\",\n    \"note\": \"The PM claim comment predicted 'no' and said so was a prediction to be re-derived. The re-derivation agrees, but by the two mechanical legs above rather than by inheriting the prediction.\"\n  },\n  \"spec_fence\": \"NOTHING stopped at the packages/spec fence, and nothing needed to. The honest code for BOTH exits this PR fills is RESUME_FAILED, which the '@objectstack/plugin-approvals' ledger row already admits - so the trap the dispatch exists to prevent was not entered and no ledger change is requested. packages/spec: 0 files touched. ONE ADJACENT BOUNDARY, reported rather than crossed and NOT a request for a ruling: a recall whose run is already gone (RUN_NOT_FOUND) is still told in prose and now carries no machine-readable half, because its honest code would be RESUME_TARGET_LOST - which that same ledger row refuses, in as many words ('One code, not three ... a row for a code this package does not stamp would be the dead weight this file's gate refuses'). Stamping RESUME_FAILED there to fill the slot would make the discriminator lie about WHICH failure it was, which is the defect this card fixes one field over, so the exit reports nothing - exactly what resumeRecordedOutcome answers on the same exits, and exactly what the member's docblock declares legitimate ('An absent member means no report was made, never that no run is stranded'). PIN 3 pins that boundary so the next reader does not 'fix' it with the wrong code. If a consumer ever genuinely needs to branch on that exit, it is a packages/spec ledger event for the domain:spec seat, as #17909 was routed.\",\n  \"mcp_calls\": \"0 - no MCP GitHub call was made for the whole run. A repo-scoped REST probe (GET /repos/objectstack-ai/objectstack) answered 200 for this seat, so the card body, the full comment thread, PR #17908, the PR creation and the report comment all went through repo-scoped REST; the rest went through git.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"noted, not filed: recall's ADR-0044 revise-window arm (cancelRun) tolerates its own failure with prose and no machine-readable half. A cancel is not a resume, so ResumeFailureReport has no referent there and this card's ruling does not reach it. Successor: PR #17988 itself is the file's next reader, and the acceptance notes carry it.\",\n    \"noted, not filed: the missingRunCapability exit already spells [RESUME_FAILED] inside its own prose for a composition with no engine, and resumeRecordedOutcome stamps no report there either, so recall was left symmetric with the sibling door rather than diverging on this card. Successor: whoever lands the next change on either door - the two sites are four lines apart and are read together.\",\n    \"noted, not filed: the card's own row 'strandedDecisionDetails(result) -> null' is wrong in two ways and both are recorded in PIN 1's comment rather than silently smoothed - the value is undefined, and the predicate reads a carrier property off a THROWN error, so against any recall RESULT it can never discriminate whatever the door does. Successor: the card itself, if it is ever re-filed.\"\n  ]\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652635912,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T10:11:47Z","updated_at":"2026-09-13T10:11:47Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15970#issuecomment-5652635912","body":"Clause-②-correction: 5652131394\nClause-②: no\nSession: session_01URLHobLUJB9K1ABV6ofdjj\n\n⚠️ **Pre-emptive, ⛔ not reactive: this card's gate currently passes.** Posted because the defect behind it is **latent here**, and the moment it would bite is the moment it costs most — at landing.\n\nThis round's claim (`5652131394`) wrote the declaration as `Clause-②: **no**`, wrapping the value in markdown bold, so the fixed-spelling predicate does not match it. Measured across this seat's five claims this session: **all five** carry the same spelling. `check-clause2-carriers --pair 17993` failed on #16678 at **exit 4** for exactly this, and passed there only after a correction naming the governing claim; the pairs on this card and its sibling pass today for an incidental reason, ⛔ not because the claim is readable.\n\n⭐ Root cause is **correction 156 repeating one surface over**: 156 recorded that the reader *\"rejects decorated keys\"*, this seat wrote that into every dispatch order it gave a dev, and then decorated the value in its own claim. A rule learned for one carrier was not carried to its sibling. The structural fix is the gate's own: **copy the template's literal line, ⛔ never compose one from memory.**\n\nThe declaration is unchanged and is the measured one: **`no`**, re-derived in-seat from the delivered diff — recorded in this card's contract review.\n\n⛔ No second `Claim:`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652908828,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T11:12:57Z","updated_at":"2026-09-13T11:12:57Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15970#issuecomment-5652908828","body":"## ✅ LANDED — `9fca8eb7b`, verified on `origin/main` with controls\n\n`fix(approvals): recall tells its caller WHICH resume failure stranded the run (#17988)`\n\nProbe `(#17988)` = **1**; positive control `(#17941)` = 1; negative `(#99999)` = **0**; non-shallow, 13 827 commits. Deliverables printed with substance: the `RESUME_FAILED` stamp sites at `approval-service.ts:3654` and `:3686`, and `recall-strand-discriminator.test.ts` at **348** lines with PIN 3's boundary comment in place.\n\n## ⭐ What this delivery got right that a lesser one would have missed\n\nIt **declined to fill the slot.**\n\nThe `RUN_NOT_FOUND` exit's honest code is `RESUME_TARGET_LOST`, which this package's ADR-0112 ledger row refuses — #17924's own cost-direction ablation measured that stamping it reds the provenance guard. The tempting move was to stamp `RESUME_FAILED` there anyway and have every exit carry a report. It did not, and its reason is the whole point of the card:\n\n> a wrong code there *\"would make the discriminator lie about WHICH failure it was\"* — which is the defect this card fixes, one field over.\n\n⇒ filling a slot with a false value would have **reproduced this card's own bug at a different coordinate**. Reporting nothing on that exit is what the member's docblock already licenses: *\"an absent member means no report was made, never that no run is stranded.\"* **PIN 3** pins the boundary so the next reader does not \"fix\" it with the wrong code.\n\n⭐ It also corrected the card it was sent to implement: this card's table says `strandedDecisionDetails(result)` → `null`. Measured, it is **`undefined`** — and more importantly that predicate reads a carrier off a **thrown error**, so against a recall *result* it can never discriminate whatever the door does. ⇒ the row was a category error in the card, ⛔ not a bug to fix, and it is recorded in PIN 1's comment rather than smoothed away.\n\n## Evidence\n\nAblation predicted its direction and met it: clean (8 passed) → mutated back to the pre-fix file (**exit 1**, exactly the two new `resumeFailure` assertions failing, all four controls green) → restored (blob hash **==** HEAD, `git diff HEAD` empty). 71 gate families derived, **71 run, 0 NOT-MEASURED**; `check:engine-double-contract` answered exit 1 and was **regenerated** (`0 lost`, byte-identical) rather than baselined; three families answered **exit 3** and were re-run to a real verdict after building the closure they named.\n\n⛔ `recall`'s decision not to throw is untouched and now **pinned**; the error log line is untouched (same message, level and three context keys, asserted). ⛔ Zero `packages/spec`.\n\nIn-seat contract review: **PASS** at `76ee9926e` (`5652539838`). Carrier gate exit 0. Closing `completed`.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
