{"id":5793620332,"author":{"login":"github-actions[bot]","id":41898282,"type":"Bot"},"created_at":"2026-09-23T10:59:01Z","updated_at":"2026-09-23T10:59:01Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19850#issuecomment-5793620332","body":"<!-- docs-drift-check -->\n### 📓 Docs Drift Check\n⚠️ **1** changed file(s) yielded no anchor (`packages/metadata-core/src/item-key-discriminators.ts`), so the pages documenting them are **NOT COVERED by this run** — this is not a clean bill of health for those files. Nothing else in this diff resolved to a documentable surface (no symbol, route or SDK anchor derived from **1** changed package(s)).\n\n<details><summary>What this run could not see</summary>\n\n- **1** changed file(s) yielded no anchor (`packages/metadata-core/src/item-key-discriminators.ts`) — pages documenting those are invisible to this run\n- a page that states a rule by its **inputs** shares no identifier with the **emitter** that implements the rule, so an emitter-only diff cannot list it — not on this run and not on any run. Measured on #11430: `content/docs/protocol/objectql/types.mdx` documents the text-family column mapping by the ObjectQL type names it maps FROM (`text` / `textarea` / `html`) while the diff changed `createColumn`; it went unlisted, and it was the page that diff falsified, in four places. No shared token exists to detect this on, so a rule your change carries has to be re-read by hand in the pages that restate it.\n- a key NAME is not a key, so the hand re-read the line above prescribes can land on the wrong schema. The same spelling is authorable on one governed type and a `[REMOVED]` tombstone on another for each of `active`, `aria`, `joins`, `objects`, `template`, `tools` and `version` (censused on #19093 over the liveness ledger's governed types, top-level keys); nothing in a search result distinguishes the two, so a grep hit on a LIVE example reads as evidence about the DEAD key. Measured on #19059: `content/docs/ai/agents.mdx` was reported as contradicting the `agent.tools` tombstone over its `tools:` example at `:161`, which is inside the `defineSkill({` block opened at `:155` — the page was already correct. Settle ownership by PARSING the value against both schemas, never by the name: that literal PASSES `SkillSchema`, and as an `AgentSchema` it FAILS at `tools` with the tombstone prescription. ⛔ These names are not the whole class — a key retired through a `.strict()` guidance map leaves no tombstone in the walked shape and none of them here (`tool.category`, live as `AIToolDefinition.category`).\n\nCoarse fallback — **4** page(s) merely *mention* a changed package (the pre-#9192 predicate, kept for the deliberately-wide backstop): `node scripts/docs-audit/affected-docs.mjs --json afc3b64928914c21db8389e3383233ef18dc4134` → `packageMentionDocs`.\n</details>"}
{"id":5793905119,"author":{"login":"objectstack-fleet[bot]","id":332303061,"type":"Bot"},"created_at":"2026-09-23T11:21:00Z","updated_at":"2026-09-23T11:21:00Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/19850#issuecomment-5793905119","body":"## Contract review\n\nServed-tier: `CONTRACT_REVIEW_TIER`\nHead-sha: `abb129b73ebf1a83cd165c498eeebf87ddd87fc4`\n\nRendered by an isolated at-tier reviewer subagent that was fed the card and this PR only, and adopted by the `domain:engine#1` seat. One commit over merge-base `4112752ec3`; two files, +45/−5; no governed path.\n\n### ① Derived judgments\n\nEvery statement the new docblock and the changeset make is TRUE on `origin/main`:\n\n- **The identity fact**: `EmailTemplateDefinitionSchema.locale` is 「the second half of the bundle key」, and one row per locale tag holds at the keyed layer (the registry keys `name@locale`, and `sys_email_template` is unique on `(name, locale)` per organization).\n- **The one remaining quotation**, 「is resolved by `(name, locale)`」, exists verbatim in the schema header (it wraps over two lines; read directly, not by a single-line grep).\n- **「That resolution is exact」 / no best match, no subtag folding**: verbatim in `SendTemplateInput.template`, `packages/spec/src/contracts/email-service.ts`.\n- **The ladder as stated** (the named tag matched exactly, then the literal `en-US`, then, only for a no-locale call on a bundle with no `en-US` row, the bundle's lowest tag) is rung-for-rung `SendTemplateInput.locale`. **The implementation agrees**: `plugin-email` `resolveAndRenderTemplate` and `createSysEmailTemplateLoader.load`. A call that names `en-US` on a floorless bundle never reaches rung 3.\n- **Nothing still affirms a best, closest or nearest match**; the only one left is the denial.\n- **Stale-quote exposure** is reduced to one short header fragment plus symbol-cited paraphrase. A substantive ladder change would strand this paraphrase exactly as it would strand the ladder's other documented carriers, and the liveness ledger anchors the ladder to its implementation. The untouched `canonical` paragraph stays true (pinned).\n\n### ② Semver level\n\n`patch` for `@objectstack/metadata-core`, with `Clause-②: no` → right, and the changeset is owed. Measured independently from the published npm tarball `metadata-core-17.4.0.tgz`: the old sentence ships once in `dist/index.d.ts` and once in `dist/index.d.cts`. A false TSDoc in a published declaration file is a fix to a released package. No code byte changed: the comment-stripped sources of base and head are byte-identical, and every changed line sits inside the module docblock.\n\n### ③ Boundary flags\n\n- **The dev's A3 synonym-sweep table**: every entry was re-read on `origin/main`, and every one is accurate (a verbatim quote of live text, a denial, or historical framing) → acceptable.\n- **Found beyond the table**: `plugin-email` `TemplateLoader`'s 「deterministic best answer for the name」 describes rung 3, not a recipient-locale best match → acceptable. Pending changesets that quote the old sentence as the corrected falsehood → acceptable (historical release input). Doc-localisation \"best-matching locale\" wording in other subsystems is a different resolver that folds subtags by design, so it is not a carrier. No test pins the docblock prose.\n\nImplemented-by: `claude/issue-19592-item-key-discriminator-stale-quote`\nReviewed-by: `session_01TEhopqrWQYBycZzyJHpAZr`\n\n**VERDICT: PASS**\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_\n"}
