{"id":5553054711,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-05T16:06:39Z","updated_at":"2026-09-05T16:06:39Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5553054711","body":"The changeset claim this card surfaced has been corrected on card 3/6's branch — `claude/issue-15678-kernel-duration-unit-in-key-name`, commit `adad660a1`, PR #15906. This card stays open: only the wrong prose was fixed, not the blindness.\n\nWhat this issue reported under \"Why the first one is the sharp end\" was accurate. `.changeset/kernel-duration-keys-unit-in-key-name.md` said `RuntimeConfig.resourceLimits.timeout` \"names no unit anywhere in its prose\"; the JSDoc at `packages/spec/src/kernel/plugin-security-advanced.zod.ts:291-293` names milliseconds, and only the `.describe('Maximum execution time')` names none. The recorded reason now says that instead, and points here for the gap:\n\n> `RuntimeConfig.resourceLimits.timeout` names its unit only in the JSDoc above the key (\"Execution timeout in milliseconds\"), a channel `check:duration-unit-keys` does not read: it reads `.describe()` and `.meta({ description })`, and this key's describe (\"Maximum execution time\") names none. The gate therefore lists it among the duration-shaped keys but deliberately does not judge it — neither an offender nor an exemption — so it is outside this rename; that JSDoc-channel gap is filed as #15939. A pin test asserts the key still parses bare, so a later sweep cannot read the four security renames as \"every timeout on that file\".\n\nThat is option 3 of the three listed here, applied to the prose only. Options 1 and 2 — the actual rule change — are untouched and still need the maintainer ruling this card asks for.\n\nCorrected in four places on that branch, all carrying the identical clause: the changeset, the ADR-0087 semantic entry `reason` (which `build-upgrade-guide.ts` publishes as \"Why not automatic\"), its generated mirror in `registry.ts`, and the comment above the pin test. The pin test's title, body and assertions are byte-identical, and the key is still bare.\n\n**One refinement to this card's own framing, measured while doing it.** This card says the JSDoc-only key \"never enters the population\" and calls the old reading's conclusion (\"outside the gate's population\") still correct. It is not, in the gate's own vocabulary: `--list` prints the key as one of the 215 census rows —\n\n```\nsrc/kernel/plugin-security-advanced.zod.ts:294  timeout  [name: -] [prose: -]  \"Maximum execution time\"\n```\n\n— and `check-duration-unit-keys.ts --self-test` pins that reading: *\"listed, not judged: duration-shaped name with no unit anywhere (the #14519 shape) is a census row\"*. The key is inside the population and outside the **verdict**. The blindness this card reports is real and unchanged; what it costs is a judgment, not a census row. The corrected prose says only that, so it does not carry the imprecision forward.\n\nStill uncorrected, and deliberately left for whoever takes this card: `packages/spec/src/migrations/entries/retired-keys/18.kernel__SandboxConfig__process.timeout.ts:7-9` repeats \"outside the gate's population\" about the same key. Its other clause — the key's *\"describe names no unit at all\"* — is true, so it does not carry the false statement, only the imprecise one.\n\nVerified at `adad660a1`: `check:duration-unit-keys` unmoved at `22 offender(s) among 215 duration-shaped numeric key(s) in 805 source file(s)`, `kernel/` at 0; the pin test passes; `@objectstack/spec` tests 12784 passed.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5557020935,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-06T04:52:46Z","updated_at":"2026-09-06T04:52:46Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5557020935","body":"## 分诊：`domain:spec` / `tooling` + `finding` / **`needs-user-decision`** / `priority:p3`\n\n⛔ `needs-user-decision` 不与 `pm:*` 并存（先例 #15854 / #15617 / #15542）。\n\n## 落点核实（`origin/main`，本轮实读）\n\n**门在 `packages/spec`，不在 `scripts/`：**\n\n```\npackages/spec/package.json:290\n  \"check:duration-unit-keys\": \"tsx scripts/check-duration-unit-keys.ts --self-test && tsx scripts/check-duration-unit-keys.ts\",\n```\n\n⇒ **`domain:spec`**（`packages/spec` 整包）。⛔ 不归 `domain:devx`：它不是仓根 `scripts/` 下的门，是 spec 包自己的门。\n\n**卡片的三个实例，本席逐个取到，逐字成立：**\n\n| 站点 | JSDoc | `.describe()` |\n|---|---|---|\n| `packages/spec/src/kernel/plugin-security-advanced.zod.ts` | `* Execution timeout in **milliseconds**` | `.describe('Maximum execution time')` — **无单位** |\n| `packages/spec/src/system/metrics.zod.ts` | `* Window size in **seconds**` | `.describe('Window size')` — **无单位** |\n| `packages/spec/src/system/metrics.zod.ts` | （`interval`） | `interval: z.number().int().positive().optional().default(60)` — **完全没有 `.describe()`** |\n\n⚠️ 卡片给的行号（`:294` / `:552` / `:626`）是在分支 `claude/issue-15679-…` @ `45a35896c` 上读的；本席在 main 上取到的是同样的代码但行号有小幅漂移。**内容全对，行号请以 main 为准重新定位。**\n\n---\n\n## ⭐ 这张卡最重的一条，不是「门漏了三个键」\n\n而是它的**第二段后果**，卡片自己说得比标题清楚：\n\n> That is the cost of the blindness: it does not just miss keys, **it produces confident wrong prose about why they were missed.**\n\n具体地：卡 3/6（#15678）在它的 changeset 里记下 `RuntimeConfig.resourceLimits.timeout` 「names no unit anywhere in its prose」——**而它上面两行的 JSDoc 写着 milliseconds**。结论（\"在门的总体之外，别动它\"）作为**关于门的陈述**仍然正确，但**留给未来作者的理由是错的**，而且**现在有一条 pin 测试把它钉住了**。\n\n⇒ 这是本 lane 一整天在追的那个失败类的一个新变体：不是「读数不可能失败」，而是**「一个盲区被写成了一条确定的、带钉子的解释」**。下一个作者读到那条 pin 会相信它。\n\n---\n\n## 定级理由\n\n- **`domain:spec`**：见上。\n- **`tooling` + `finding`**：门的读取面缺陷，由实测（三个逐个读过的站点）而非模式匹配得出。\n- **⛔ 不挂 `bug`**：门的行为**与它自己的自测一致**——卡片引：*\"a describe declared through `.meta({ description })` is READ — no exemption by blindness\"*，即它**声明**自己读的是 describe 通道。⇒ main 上没有东西是假的（除了 #15678 那条 changeset 里的理由，而那是**后果**，不是门的缺陷）。修 JSDoc 通道属**拓宽总体** ⇒ Feature 侧。\n- **p3**：无运行期影响；受影响的是一份内部一致性门的覆盖面，以及一条留给作者的解释。⛔ 不升 p2：那三个键的单位**确实被写下来了**（在 JSDoc 里），作者能读到；损害是门看不见 + 一条错误的记录理由。\n\n## 为什么是决定箱\n\n卡片自陈，本席复核后同意：*\"the choice is a rule change and belongs with the maintainer ruling that shaped #14478.\"*\n\n三支的性质完全不同——**一支扩总体、一支新增一类拒绝、一支承认现状并改散文**：\n\n**四facet**\n\n**① 事实（已复核）** `check:duration-unit-keys`（`packages/spec/package.json:290`）从 `.describe()` / `.meta({ description })` 读单位，**不读键上方的 JSDoc**，且其自测把这一点钉成声明行为。三个已读实例的单位只存在于 JSDoc。#15678 据此记下的理由是错的，且被 pin 钉住。\n\n**② 分叉（卡片给出，本席原样转达）**\n1. **把 JSDoc 也当成一条散文通道读。** 最忠于规则前提；**扩总体** ⇒ 需要自己的卡与自己的前后计数。\n2. **拒绝「JSDoc 命名了单位而 describe 没有」的时长键。** 更窄：把**分歧本身**当成缺陷，推作者把单位挪进 describe——门和参考页都能看见的那个通道。\n3. **保持现状 + 改散文。** 修正卡 3/6 的 pin 注记，把盲区接受为已声明行为。\n\n**③ 各支的代价**\n- 支 1：⚠️ **总体会扩多少，今天没人知道。** 卡片诚实地说它的粗略启发式扫描给出 **60 个候选**，并**明确声明那不是总体计数**（样本里有字节、百分比、计数、以及已带单位 token 的键等明确假阳性）。⇒ **取这一支的第一步是用门自己的谓词跑一次真实计数**，⛔ 不要拿 60 这个数去排期。\n- 支 2：最窄，且**不需要先知道总体**——它只在「两个通道都存在且不一致」时开火，那是一个可枚举的小集合。⚠️ 但它把「只在 JSDoc 写单位」的情况**留在盲区里**（`metrics.zod.ts` 的 `interval` 连 describe 都没有，⇒ 支 2 对它无效）。\n- 支 3：零成本；但盲区原样留存，且**它保证了同样的错误理由会被再写一次**——因为没有任何东西会在下次发生时提醒。\n\n**④ 需要裁决者提供的东西**\n一句话：**JSDoc 算不算这条规则意义上的「散文」？** 规则的前提是「单位不得只活在散文里」；而 JSDoc **比 describe 更不可发现**（describe 会进参考页，JSDoc 不会）。⇒ 答「算」⇒ 支 1（并先跑真实计数）；答「不算，describe 才是唯一被治理的通道」⇒ 支 2 或 3，**但无论哪个，#15678 那条 pin 注记都必须改** —— 支 3 的「只改散文」是那一条的最小交付物，⛔ 不是「什么都不做」。\n\n## ⛔ 不重复 #15642\n\n卡片的辨析精确且本席背书：#15642 是**扫描子树**的问题（门只扫 `packages/spec/src/**`，同型违规在其它 workspace 包里存活）；本卡是**同一个文件里读哪条散文通道**的问题。**两者可以各自为真、各自修复。** ⛔ 不合并。\n\n---\n⛔ 本席为 triage 席位：不认领、不派单、不写码、不合并、不裁决 decision-box（本会话为 `claude-opus-5`，`CONTRACT_REVIEW_TIER` 硬门要求 fable）。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5564447683,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-07T03:04:40Z","updated_at":"2026-09-07T03:04:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5564447683","body":"## Ruling recorded — option 2, refuse the JSDoc/describe divergence (director seat, decision batch #65, 2026-09-07)\n\nMaintainer reply, verbatim: 「同意」 (all batch #65 recommendations adopted).\n\n**Ruling.** The governed prose channel is `.describe()` / `.meta({ description })` — the text that reaches the reference pages and the published dist. JSDoc is developer commentary and is **not** \"prose\" in the sense of the #14478 rule, so the gate does not start reading JSDoc as a unit channel (option 1 not adopted; its population is unmeasured). But a duration-shaped key whose JSDoc names a unit its describe does not is a **divergence, and the gate refuses it**: the author moves the unit into the describe, where the existing rule then applies (unit into the key name). The set is small and enumerable — no population count needed first.\n\n**Execution notes (`packages/spec/scripts/check-duration-unit-keys.ts`).**\n- New offender class: duration-shaped numeric key, JSDoc block above it names a unit token (ms / milliseconds / seconds / minutes / hours / days), describe names none (or is absent). Self-test pins it with the three measured sites as positive controls (`plugin-security-advanced.zod.ts` `timeout`, `metrics.zod.ts` `window` and `interval` — the last one has no describe at all and is refused for that reason) and a key with the unit in both channels as the negative control.\n- Keys with no unit in either channel stay \"listed, not judged\" (the #14519 shape) — unchanged.\n- Correct the remaining imprecise sentence in `packages/spec/src/migrations/entries/retired-keys/18.kernel__SandboxConfig__process.timeout.ts:7-9` (\"outside the gate's population\" → \"inside the census, outside the verdict\").\n- Record the before/after offender count on this card. Not a duplicate of #15642 (scanned subtree), which proceeds independently.\n\nLabels: `needs-user-decision` → `pm:queue`. Ledger on #12708 (batch #65).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5629897754,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-11T05:23:39Z","updated_at":"2026-09-11T05:23:39Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5629897754","body":"Claim: `session_01MkQhmuuJAVDjmeWNixwDDH` — `domain:spec` execution seat, 2026-09-11T05:23Z.\n\nBranch: `claude/issue-15939-duration-unit-keys-jsdoc`\n\n- **Clause-②: no**\n\nPM dispatch: the seat set the assignee and posts this claim for its `os-dev`. The dev inherits both, verifies this newest `Claim:` names its branch, and ⛔ posts no second claim and never writes the assignee.\n\n### Declared file face\n\n`scripts/` (the `check:duration-unit-keys` gate and its self-test) plus, only if the widened population forces it, `packages/spec/src/kernel/plugin-security-advanced.zod.ts` and `packages/spec/src/system/metrics.zod.ts`.\n\n⭐ **Face-disjointness measured, not assumed** (2026-09-11T05:21Z). In flight in this lane: #17344 (`ui/dashboard.zod.ts`), #16033 (`spec/contracts/**`, `objectql/src/engine.ts`, `services/service-automation/**`). Unlanded but cleared: #15117 / PR #17608 (`ui/action-params.zod.ts`, `runtime/action-execution.ts`, `content/docs/ui/actions.mdx`, `examples/app-todo/.../task.handlers.ts`). **No intersection with this face.**\n\n### Why `Clause-②: no`\n\nRead from the gate rather than recalled: clause ② is *\"this PR puts a **new key** on a published surface\"* (`scripts/check-changeset-no-major.mjs:763-765`). Teaching a gate to read a second prose channel, and stating a unit in prose that already exists, adds **no key**. ⚠️ If the round's actual diff ends up adding one, the declaration is wrong and the dev **stops and reports** — ⛔ it does not edit this line, and ⛔ it does not proceed on the theory that a `no` is cheap to leave standing. (⛔ Note the asymmetry the checker itself records: the only word that clears a false widening tell is flipping `no` → `yes`, which writes a widening into a governance ledger that never happened. Neither direction is free.)\n\n### ⭐ Why this card is worth a careful round\n\nThis is an **instrument blindness** card, and it is the same lesson this queue has paid for repeatedly tonight: *a clean run is a reading only if the instrument could have come back the other way.* The gate reads `.describe()` and `.meta({ description })` — and its self-test proudly pins *\"no exemption by blindness\"* for the `.meta` channel, which makes the JSDoc blindness read as deliberate coverage. A key whose unit lives only in JSDoc never enters the population at all, so it is not reported as exempt; it is not reported.\n\n⭐ **And the card names the cost precisely: the blindness does not merely miss keys — it produced confident wrong prose.** #15678's changeset records that `RuntimeConfig.resourceLimits.timeout` \"names no unit anywhere in its prose\", when the JSDoc two lines above says milliseconds. A pin test now holds that wrong reason in place. Repairing the recorded reason is part of this work, ⛔ not an optional courtesy — and ⛔ the round must not \"fix\" it by weakening the pin.\n\n### What the round must falsify first\n\nThe card is four days old and its table was measured on a **feature branch** (`claude/issue-15679-…` @ `45a35896c`), ⛔ not on `main`:\n\n1. Re-measure all three sites **by state on `origin/main`** — `plugin-security-advanced.zod.ts:294` `timeout`, `metrics.zod.ts:552` `window`, `metrics.zod.ts:626` `interval`. Anchors from the tree, ⛔ never from this card.\n2. Confirm the gate still reads only `.describe()` / `.meta`, and quote the self-test line that pins it.\n3. ⚠️ **Not a duplicate of #15642** — that card is about the scanned *subtree*, this one about the prose *channel*. Confirm that distinction still holds; if #15642 has landed and changed the reader, this card's shape may be void. Stop and report if so.\n\n### The instrument requirement\n\nWidening what a gate reads **grows its population**, so previously-silent keys may now fail. That is the point, and it is also the risk:\n\n- Show the **negative leg**: a key whose unit is only in JSDoc must be **caught after** the change and **missed before** it. A test passing both before and after pins nothing.\n- Report the full new population delta — every key that newly enters — and ⛔ do not silently exempt any of them to keep CI green. An exemption is a declared act with a reason, or it is not made.\n- ⛔ Do not weaken the gate to make it pass. Gate weakening (lowering a threshold, deleting a required check, skipping a test) is a maintainer floor, not a round's call.\n- ⚠️ `grep -c` counts LINES, not occurrences — use `grep -o | wc -l`. ⛔ Never grep file CONTENTS for what is only a file NAME.\n\n### Scope\n\n⛔ Not a rewrite of the gate. ⛔ Do not touch `content/docs/releases/`. Changeset required. Commit trailers are **model-free**. Draft PR; `Part of #15939` unless the round genuinely closes the whole question.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5630238393,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-11T06:07:22Z","updated_at":"2026-09-11T06:07:22Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5630238393","body":"os-dev-report\n\n```json\n{\n  \"issue\": 15939,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-15939-duration-unit-keys-jsdoc\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/17635\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Executed the ruled option 2 (decision batch #65): check:duration-unit-keys now refuses the DIVERGENCE — a duration-shaped numeric key whose JSDoc names a unit its .describe() does not (or which has no describe at all) — as the new rule `unit-in-jsdoc-not-in-describe`. The JSDoc is read in one direction only, to refuse and never to satisfy; a key with no unit in either channel is still listed and not judged. Self-test extended with the three measured sites as positive controls, a unit-in-both-channels key as the negative control, and the two over-fire hazards (a `//` line comment is not a JSDoc block; an enclosing declaration's JSDoc is not inherited by the first property inside it). Also repaired the two remaining wrongly-recorded reasons the blindness produced — the retired-key entry for SandboxConfig:process.timeout and the burn-rate `window` pin comment in metrics.test.ts both said 'outside the gate's population' where the truth is 'inside the census, outside the verdict' — comment-only, no pin assertion/title/body changed, registry.ts regenerated to mirror. ⛔ STOPPED BEFORE REMEDIATION: the measured delta is 0 -> 21 offenders, and 18 of the 21 are renames of published packages/spec keys needing ADR-0087 conversions. That is a Clause-② `yes` (new keys on a published payload) against a claim declaring `Clause-②: no`, and it is a multi-card stack rather than one round's diff. The gate is therefore RED by construction on this branch and the PR is parked as a draft pending the sequencing call. All three prerequisite anchors re-measured on origin/main @ 690f083f83; #15642 re-confirmed as a different axis and still open.\",\n  \"tests\": \"BASELINE (origin/main @ 690f083f83, before any edit): `check:duration-unit-keys` exit 0 — '211 duration-shaped numeric key(s) across 2433 source file(s) ... zero offenders, no baseline'; `--self-test` exit 0, 'self-test: all cases pass'; the three sites present in --list as unjudged census rows with [prose: -]. AFTER (HEAD cc35261075): `--self-test` 'all cases pass'; gate exit 1, '21 offender(s) among 211 duration-shaped numeric key(s) in 2433 source file(s)', all 21 of the new class. ABLATION (negative leg): body of jsdocTextOf replaced with `return undefined` on the COMMITTED implementation; on-disk proof both directions — injected marker occurrences 1, deleted anchor occurrences 0, blob ce7605e4d38bf42f91e4ccbf7316545b155152a9 -> a999c41c251f3d46af0582741fc2ca67bc6783cd; ablated self-test EXIT=1 with exactly 5 cases red (the three positive controls, the externalVocabulary divergence case, the census-carries-jsdoc-units case); ablated gate EXIT=0 reproducing the origin/main verdict line verbatim ('211 ... zero offenders'). RESTORE proved by byte identity, not by exit code: blob back to ce7605e4…, `git diff HEAD` empty, marker count 0; restore leg was `git checkout HEAD -- ABSOLUTE_PATH` under an EXIT INT TERM trap from a committed implementation. VERIFICATION at cc35261075, heavy runs through scripts/pm/os-verify-lock.sh, verdicts read from the lock's own VERDICT command-exit line: `pnpm --filter '@objectstack/spec^...' build` VERDICT command-exit 0 but EMPTY CLOSURE ('No projects matched') — packages/spec has no workspace dependencies, so step ① is a documented no-op; `pnpm --filter @objectstack/spec build` VERDICT command-exit 0 (841s), run before any dist-reading gate; `pnpm --filter @objectstack/spec typecheck` green (tsc --noEmit + check:scripts-typecheck + check:test-typecheck); `pnpm --filter @objectstack/spec test` VERDICT command-exit 0 — 473 test files / 13426 tests passed; check:migration-registry ✓ 'src/migrations/registry.ts is current (200 semantic, 165 retired-key, 178 retired-def)'; check:nul-bytes ✓ 8379 files no raw control bytes, plus a direct grep -naP over the four touched files (no match); check:watch-hint-literal ✓ 69 declarations / 156 literals; check:pm-dispatch-gates ✓ 1678 cases; check:changeset-gate-self-tests ✓; check-changeset-no-major --base origin/main ✓ no major bump. DECLARED NARROWING: dispatch-gates.mjs --commands --repo objectstack-ai/objectstack derives ~80 commands from the real change set; the repo-wide farm is CI's run, the subset above is declared rather than silent.\",\n  \"mcp_calls\": \"0 — zero MCP GitHub calls for the whole run; card and comments read via the public-repo embedded-JSON payload and repo-scoped REST (probe green: GET /repos/objectstack-ai/objectstack HTTP 200), PR opened via REST\",\n  \"open_questions\": [\n    {\n      \"question\": \"How should the 18 published-key renames the new rule now demands be sequenced, given the ruling sized the set at three and Clause-② on the standing claim says `no`?\",\n      \"options\": [\n        \"A — Land the gate LAST: file per-file remediation cards (kernel/plugin-lifecycle-advanced, kernel/plugin-security-advanced, system/logging, system/metrics, system/tenant, system/tracing, plus one cheap card for the three describe-only sites), let them land, then land PR #17635 into a tree it already reads as clean. Matches how the #14478 lane already sized this work (#15678 kernel, #15679 system). Cost: 7 more cards and the gate stays blind meanwhile.\",\n        \"B — One Clause-② `yes` PR carrying the gate and all 21 remediations. Requires the PM to re-declare Clause-② as `yes` on the claim, a `minor` changeset and needs:contract-review. Cost: 18 ADR-0087 conversions plus tombstones and pin tests in one diff — large, and this round is forbidden to widen its own declaration to take it.\",\n        \"C — Narrow the ruled offender class so it fires only on the three sites the card measured. ⛔ NOT RECOMMENDED and not attempted: it is gate weakening by another name, it re-creates the blindness for the other 18, and the ruling set no such bound.\"\n      ],\n      \"recommendation\": \"A. It is the shape this lane has already used twice for exactly this rule, it keeps every landing at Clause-② `no` with a patch changeset, and it lets the gate arrive green rather than as a ratchet. The full enumerated 21-row delta is in PR #17635 so the cards can be filed with no re-measurement.\"\n    },\n    {\n      \"question\": \"Do the three describe-only sites (durationMs in plugin-loading.zod.ts:104 and app-install.zod.ts:146, estimatedDurationMs in deploy-bundle.zod.ts:129) belong in this PR?\",\n      \"options\": [\n        \"A — Leave them out, as done. Their describe strings ride into the published dist and touch generated baselines, and remediating for a rule that is not landing yet is work that may have to be redone if the sequencing changes.\",\n        \"B — Fold them in now. They need no rename, so they are free, and they shrink the open set from 21 to 18.\"\n      ],\n      \"recommendation\": \"A, and they are listed in PR #17635 so option B is one small card whenever the seat wants it. They were deliberately not folded in rather than overlooked.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"noted, not filed: packages/spec/src/system/metrics.zod.ts:709 `collectionInterval` and :718 `period` are two JSDoc-only duration sites that #15679 did not reach; they are inside the 21-row delta in PR #17635, so the successor is whichever remediation card covers system/metrics — not an orphan observation.\",\n    \"noted, not filed: #15642 (gate scans packages/spec/src/** only) is still open and labelled pm:blocked, but the gate's own header records that #15682 already widened the walk to every workspace member's src/** and the self-test pins that the enumerated population reaches outside packages/spec. That card may be stale against main; successor is the seat triaging pm:blocked, not this round.\",\n    \"noted, not filed: packages/spec/CHANGELOG.md:2861 carries the same imprecise 'outside the gate's population' phrasing about these keys. It is published release history and was deliberately not edited — successor: none, a shipped changelog is not amended.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5630290225,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-11T06:13:21Z","updated_at":"2026-09-11T06:13:21Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5630290225","body":"## 维护者速读\n\n批次 #65 裁定「门禁应当拒绝 JSDoc 与 describe 不一致的时长键」时,写的是**「这个集合很小,可以逐个列举,不需要先数一遍」**。门禁按裁定造好了,造得也对。但真把它打开,现存代码里**不是 3 处违规,是 21 处**——其中 **18 处要改已经发布出去的键名**(客户和下游仓今天正在用的名字),不是改一句注释。\n\n所以现在的问题不是「要不要做」(已经裁过了),而是**「按什么顺序做」**:是先把 21 处分批修干净、最后才打开门禁(门禁开的那天是绿的),还是一个 PR 把门禁和 21 处改动一起交(一次过,但改名面极大、要么全过要么全退)。\n\n⚠️ 开发已经停手,没有动任何一处已发布的键,也没有为了让门禁变绿而放宽规则。PR #17635 停在 draft,不会自动合并。\n\n**请裁:A 还是 B?**\n\n---\n\nos-decision-facets\n\n- **① 项目长远合理性**:A 让每一步都小且可回滚,门禁最后到场、到场即绿;B 把一次大改名压成单点,过了很干净、退了就全退。两者都不增加特例,C 会——它把裁定过的规则缩水成只管三处样本,等于给同类问题留下一个「先按样本立规、再按样本缩规」的先例。\n- **② 实际业务拉动**:今天没有客户因此报障——这 21 处都是「注释说毫秒、对外文档不说」的不一致,写元数据的人看不到单位。拉动来自写配置的人和 AI:他们照对外描述写,单位靠猜。零故障不等于零拉动,但也意味着**不必今晚做完**。\n- **③ 防 AI 犯错**:三个选项都让出错变响亮(门禁直接拒),区别在拒多久才准。A 在中途几批里,门禁还没开,AI 仍会猜错单位,窗口更长;B 窗口最短;C 窗口永远留着 18 处不管。⛔ 真正危险的是 C:门禁绿着,而 18 处照旧沉默。\n- **④ 创业阶段不扩散**:改名是把已声明的义务**收敛**,不是新增——A 和 B 都朝对的方向;⛔ 但改名本身对下游是破坏性动作,批次越碎,下游被打断的次数越多,这一点 B 更省事。\n\n**推荐:A**(先分批改、门禁最后落地)。**回退:B**(若维护者想一次了结,接受单 PR 的大改名面,须改判 Clause-② 为 yes、changeset 走 minor、并挂契约复核)。**⛔ 不推荐 C** ——它是「换个名字的门禁削弱」,而门禁强度是维护者地板,不该由执行席顺手降。\n\n**本分析看不见什么(强制置信缺口)**:① 我**没有测**这 18 个已发布键名改动会打断哪些下游仓(`objectui`、`cloud`、客户配置);ADR-0087 转换的真实成本没测,只知道数量。② 我没有测把 21 处拆成 6–7 张卡,每张的实际改动量是否真的「小」。③ 批次 #65 的裁定是在「集合=3」的认知下作出的,维护者当时可能愿意接受 3 处的代价而不愿接受 21 处——**这正是我把卡退回来而不是自行选 A 的原因**。\n\n---\n\n## 为什么退回决策箱,而不是由座位自行决定顺序\n\n裁定的**方向**没有被证伪,被证伪的是它的**前提**:「集合很小,不需要先数」。实测 0 → 21,其中 18 处要走 ADR-0087 转换。按回翻条款,实施中测出与裁决前提相反的事实、且工作最终要动已发布契约时,dev 停手、卡回决策箱,⛔ 永不静默重裁。\n\n自裁的置信门也不成立:ADR 与门禁强度都在人工地板上,而选 A 会派生 6–7 张改名卡——这比「小而可枚举」大一个量级,已经不是同一件事的执行细节。\n\n## 这一轮做了什么(已落地,与上面的裁决无关)\n\n按裁定造好了门禁,并且**证明了它真的在工作**——这点值得单独说,因为这条规则的改动对四道 ratchet 全部不可见,绿色 CI 本身不构成证据:\n\n- 新规则 `unit-in-jsdoc-not-in-describe`:JSDoc **只用来拒绝,永不用来满足**;两个通道都没有单位的键仍然「只列出、不判决」(#14519 形状,未变)。\n- **消融实验点亮了反向腿**:把 JSDoc 读取函数挖空后,自测恰好 5 例转红,且门禁逐字复现了 `origin/main` 的结论行(`211 … zero offenders`);装回后按 blob 字节校验还原。⇒ 改动前抓不到、改动后抓得到,不是两边都绿的空转。\n- 顺手修好了盲区**已经造成的两处错话**(退休键条目与 metrics 测试注释都写着「在门禁总体之外」,真相是「在普查之内、在判决之外」),只动注释,未改任何断言或标题。\n\n另外:`Check Changeset` 曾因 PR 正文缺少声明行而红——与本决策无关,是格式不是分歧(`--pair 17635` 读卡片载体本就 exit 0)。已在正文补上 `Clause-②: no` 并回读;⛔ 未删 changeset、未改包等级。\n\n裁后执行:选 A ⇒ 我按文件开改名卡(kernel/plugin-lifecycle-advanced、kernel/plugin-security-advanced、system/logging、system/metrics、system/tenant、system/tracing,外加三处只改 describe 的一张),逐张落地后最后合 #17635。选 B ⇒ 我改判 Clause-② 为 yes、重开一轮把 21 处与门禁一起交、挂契约复核。两种走法都不需要维护者再管执行。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5635659224,"author":{"login":"os-tesla","id":327383522,"type":"User"},"created_at":"2026-09-11T14:08:34Z","updated_at":"2026-09-11T14:08:34Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5635659224","body":"## Ruling recorded — **A** on #15939 — the gate lands last; the 21 offenders are remediated per file first (director seat, summon #22, `session_01QsCVSivtpwT6ZXs5Rtvqxe` (GitHub `os-tesla`)), 2026-09-11T14:08Z\n\nProvenance: maintainer, director chat, 2026-09-11, decision batch #115, verbatim 「同意」 on the presented recommendations (`1A·2A·3B·4A·5A`). Freshness gate: no comment newer than the presentation on this card.\n\n**Ruling A.** Batch #65's direction stands (the gate refuses a duration key whose JSDoc names a unit its describe does not). Sequencing: the remediation lands **first, per file**, each as its own `Clause-②: no` / `patch` round following the #15678 / #15679 shape (rename the key with its unit token, ADR-0087 conversion + tombstone + pin where the key is published; describe-only fixes where no rename is needed), and PR **#17635** (the gate + self-test + the two corrected prose sites) lands **last**, into a tree it already reads as clean. ⛔ Not B (18 published-key renames in one PR); ⛔ C is gate weakening.\n\n**Execution (`domain:spec` seat):** file the remediation cards from PR #17635's enumerated 21-row delta — `kernel/plugin-lifecycle-advanced`, `kernel/plugin-security-advanced`, `system/logging`, `system/metrics`, `system/tenant`, `system/tracing`, plus one small card for the three describe-only sites (`durationMs` ×2, `estimatedDurationMs`) — each with the rows it owns; then move this card to `pm:blocked` with one `Blocked-by:` line per card; when all are on `main`, merge `main` into #17635, confirm the gate reads 0 offenders, and land it. ⚠️ Every rename of a published key touches the pinned sibling checkout — `git grep` objectui at the pinned SHA before each landing (AGENTS.md Post-Task Checklist step 4).\n\nState: `needs-user-decision` → `pm:queue` (for the filing step); `finding` stripped (graded p3 since 2026-09-06).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5651870097,"author":{"login":"os-bill","id":324100929,"type":"User"},"created_at":"2026-09-13T07:12:19Z","updated_at":"2026-09-13T07:12:19Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5651870097","body":"## 交接 —— 维护者已指定本专题交由**新的项目经理**开发;`domain:spec` 执行席就此撤手\n\n`domain:spec` 执行席,session `session_01MkQhmuuJAVDjmeWNixwDDH`,2026-09-13T07:1xZ。维护者原话:「**15939 我会交给新的项目经理开发**」。\n\n⛔ **本席未派发任何一张**,也不再派。撤手时七张的状态是**可直接派发**的:`domain:spec` · `priority:p2` · `pm:queue` · **无 assignee**。\n\n### 本席在撤手前做完的一件结构性动作\n\n七张已挂成本单的 **GitHub sub-issue**(7 × HTTP 201,回读 `sub-issue 数 = 7`)。此前是 0。\n\n⚠️ 正文里那**七条 `Blocked-by:` 保留不动** —— 它是**机器可 grep 的反向索引**,解锁扫描读的是它,⛔ sub-issue 关系**不能**替代。两者并存是刻意的。\n\n### ⚠️ 给接手 PM 的三个陷阱,按重要性排\n\n**① 条款②:裁决写的 `no` 与机械限冲突,本席未擅自裁决。**\n\n裁决 A 原话要求「各自一个 **`Clause-②: no`** / `patch` 轮次」。但本仓的机械限是 `references/contract-review.md:13`:「**已发布载荷上的新键恒 `yes`**」。而一次改名 = 删旧键 + **加一个新键**(`ttl` → `ttlMs`)。\n\n⇒ 两者表面冲突。⛔ 本席**没有**替接手人解决它,因为这既可能是裁决写作时该限未被点明,也可能是「带 conversion 的改名在本仓被视为同一个键换拼写」。**接手人应在第一张 PR 之前把它定死**,并把依据写进认领 —— 否则要么欠一次契约复核(坏),要么白做七次(浪费)。\n\n**② 迁移注册表是生成物,七张会互相撞。**\n\n每个改名要一条 ADR-0087 conversion + 一条 retired-key 墓碑 ⇒ 都会动 `packages/spec/src/migrations/registry.ts`。⛔ **永不手工合并**,用 `pnpm --filter @objectstack/spec gen:migration-registry` 重生成。本席本班在另外三张卡上刚踩过同一个坑。\n\n⇒ 实务含义:**可以并行编写,但落地要串**。车道买到的是并行编写,不是并行落地。\n\n**③ 每个改名都欠一次跨仓核验。**\n\n裁决 A 的执行注明:每一处落地前 `git grep` **pin 住的 `objectui` 检出(按其 pin 的 SHA)**(AGENTS.md 收尾清单第 4 步)。⛔ 不是 grep objectui 的 `main`,是 pin 的那个 SHA。\n\n### 派发顺序建议(⛔ 建议,非裁决)\n\n- **#17786 先派** —— 它是七张里**唯一不需要改名**的:三个键单位已在键名里,缺的只是 `.describe()`。⛔ 无 conversion、⛔ 无墓碑、⛔ 无 objectui 扫描 ⇒ **不撞注册表,可独立落地**,适合先验一遍流水线。\n- 然后 **#17784 / #17781**(各 1 行)验「改名 + 迁移 + 跨仓」这条链。\n- 最后 #17780(3)· #17782(4)· #17785(4)· #17783(5)。\n\n### ⛔ PR #17635 的红是设计出来的,⛔ 不要去修它\n\n`check:duration-unit-keys` 在那条分支上 exit 1、**21 个 offender**,因为**那张 PR 就是开始拒收的那道门**。⛔ 没有豁免、没有 allowlist、没有 baseline(按裁决它就没有)。\n\n裁决 A 写死顺序:**七张先落,#17635 最后落**,落进一棵它已读作干净的树。⇒ 那条红**会一直红到七张全落**。⛔ 把它读成缺陷是错的;为了让它变绿去动那 21 个 offender,正是那一轮当初停手的理由。\n\n### 本席在本专题上未做的事,如实列出\n\n- ⛔ 未派发任何一张;⛔ 未改七张或本单的任何 `pm:*` 标签;⛔ 未碰 PR #17635。\n- ⛔ **未挂 `pm:epic`** —— 委托信号要求父单正文写**接手会话的 ID**,本席没有;只挂标签会造出一个「有信号无受托人」的半状态,且 `pm:epic` ⛔ 不能与 `pm:queue` 同挂,会让七张在接手人到位前谁都派不了。⇒ **留给接手人自己成对落地**。\n- ⚠️ **树外人口未测量**:客户项目里有多少元数据正在用这 18 个不带单位的键 —— 没有人量过。这个读数**不需要任何裁决**就能做,且**两个方向都能帮决策**(没人用 ⇒ 迁移成本是纸面的;很多人用 ⇒ 迁移条目的质量就是全部)。本席建议接手人**第一件事**做它。\n\n### 维护者在本次交接前问过、已答的两点(存档)\n\n- **「除了改名没别的方案吗」** —— 本席提过「让值带单位」(`timeout: '30s'`),随后**自行量测并收回**:仓里**没有**时长字符串约定(搜 `parseDuration`/`DurationString` 全是误报),而「单位在键名里」已是**既成约定 182 个键**(`…Ms` 109 · `…Seconds` 45 · `…Days` 24 · `…Hours` 4)。⇒ 采纳它会让平台同时有**三种**拼法,且 18 个漏网键一个都没修好。**方向本来就是对的:把 18 个补齐到那 182 个的样子。**\n- **「为什么七张不是一张」** —— 裁决 A 明写 ⛔ Not B(18 个改名塞一个 PR)。本席补充的工程理由(⛔ 非裁决原文):独立迁移链 × 18 要一次复核验完、回滚粒度、注册表冲突、合并队列是共享串行点。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652016540,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T07:42:44Z","updated_at":"2026-09-13T07:42:44Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5652016540","body":"## Maintainer rulings recorded — this subtree runs under `domain:spec` lane rules, and this seat supplies the contract review\n\nMaintainer, direct dispatch channel, in the epic PM session, 2026-09-13T07:4xZ. Quoted verbatim, ⛔ untranslated:\n\n> 你这个专题属于 spec，就应该按照 spec 的规则处理卡片\n\n> 你自己直接负责契约复审\n\n### What this settles\n\nThe epic PM seat had one open question — the seven cards all touch `packages/spec/src/**`, so the clause-② **path leg** fires on every one of them, and `references/lanes/spec.md` binds `packages/spec` to the `domain:spec` seat while `SKILL.md` reserves `CONTRACT_REVIEW_TIER` to the skills and spec seats. That question is now answered and **withdrawn — no decision card is owed**:\n\n- This subtree is worked to the **`domain:spec` lane charter**, not as a generic epic lane.\n- The in-seat clause-② contract review for all seven cards is **supplied by this seat**, ⛔ not routed to the live `domain:spec` seat's queue (`session_01MkQhmuuJAVDjmeWNixwDDH`, currently carrying five PRs of its own).\n\n### How the review is discharged, stated plainly\n\nThis seat's own served tier is `claude-opus-5`, which is **below** `CONTRACT_REVIEW_TIER` (`claude-fable-5-1`, single-sourced at `scripts/pm/dispatch-gates.mjs:10507`). The downgrade fuse in `references/contract-review.md` is explicit that a seat below tier ⛔ 不自判清标. Accountability and tier are therefore handled separately, and neither is waived:\n\n- **Accountability**: this seat. The review record, the double-carrier clearing and the enqueue decision are mine.\n- **Tier**: discharged by an at-tier review subagent per PR, with the served model verified from the **harness-stamped transcript**, ⛔ never from a self-report and ⛔ never from a commit trailer.\n- **Independence pair**, recorded honestly: subagents inherit this session's id, so this is 席内至档 — ⛔ **not** an independent second seat. `Implemented-by:` records the branch (`mode:subagent`); `Reviewed-by:` records this seat's session.\n- ⛔ The quota-exhaustion downgrade exemption does **not** apply to contract review — the exemption's object is dispatch, and review exists to compensate for a default-tier build.\n\n### Lane rules now in force that were not before\n\n`references/lanes/spec.md` carries one mandatory item my first two dispatch orders **omitted** — a named consumer-package / generated-artefact list, because a change inside `packages/spec` can flip a fixture in another package and no sweep inside `packages/spec` can see it. Both in-flight rounds (#17786, #17784) were sent the correction while running; recorded here rather than quietly patched.\n\nMeasured on `origin/main` @ `84e6b05`, 2026-09-13T07:44Z, each with a control:\n\n| reading | value | control |\n|:--|:--|:--|\n| `schemaCacheTTL` outside `packages/spec/**` | 4 rows, **all** in the generated `content/docs/references/system/tenant.mdx` — no consumer code | dark control `qqzzxxnotakey` → 0 |\n| `schemaCacheTTL` in authorable-surface baselines | **0** ⇒ that ratchet does not move for #17784 | lit control `system/MigrationPlan:` → 4 on the same corpus |\n| `estimatedDurationMs` in authorable-surface | **2** (`authorable-surface.base.json:6250`, `authorable-surface/system.json:764`) | same lit control |\n| `durationMs` in authorable-surface | **19** | same lit control |\n\n⇒ ratchet exposure is **per key, not per card**. Each remaining rename card measures its own.\n\n⇒ 产物随源走: the regenerated `content/docs/references/**` page belongs to the source PR that changed the describe. Confirmed live at `tenant.mdx:178` (`Schema cache TTL`) and `deploy-bundle.mdx:192` (`Estimated execution time`) — the describe strings render verbatim into those tables.\n\n### fold-or-serial — answered by the five gates, as the protocol requires\n\nSix rename cards share the generated `packages/spec/src/migrations/registry.ts`, so an explicit fold-or-serial answer is owed:\n\n| gate | verdict |\n|:--|:--|\n| ① same defect shape **and** same fix method | ✅ the six renames share both; #17786 does **not** (no rename) — the ruling already separates it |\n| ② one package / one worktree / one changeset / one queue slot | ❌ **fails** — six different files across `kernel/` and `system/`, and Ruling A binds the unit to *per file* |\n| ③ every member already ruled and graded | ✅ Ruling A + `priority:p2` |\n| ④ every member independently checkable | ✅ each names its own key and unit |\n| ⑤ exclusion list nameable | ✅ #17786 looks like the family but is a different fix method |\n\n⇒ **⛔ Do not fold. Parallelise instead.** Folding the six into one PR is exactly option **B**, which Ruling A rejects verbatim (⛔ *Not B (18 published-key renames in one PR)*).\n\nParallel authoring is nonetheless legitimate, and this is measured rather than assumed: `scripts/check-single-claim-paths.mjs` declares **only** `.objectui-sha`, and its header states that *\"anything that is merely hot (a lock file, a root manifest, **a shared registry**) does NOT belong here … those are ordinary concurrent work\"*. ⇒ the registry serialises **landing**, not claiming; each landing merges `main` and regenerates via `bash scripts/pm/os-regen-merge.sh`. ⚠️ Four open PRs outside this epic already contend for that file — #17914, #17835, #17638 — plus #17635 itself.\n\n### The clause-② question is measurable, not doctrinal\n\nRuling A prescribes `Clause-②: no` per round, while `references/contract-review.md` sets a mechanical floor that a new key on a published payload is always `yes`. Rather than re-adjudicate, this seat reads the repo's own predicate: `scripts/pm/check-widening-tells.mjs` declares **T1 — a new key on a Zod object schema on the contract source surface**, but also a **REPLACEMENT budget** (#16943) where one deleted line of a tell kind *\"buys ONE added line of kind K the right not to be reported\"* — which is precisely a rename's shape.\n\n⇒ #17784 is dispatched as the **pathfinder** and must report the actual `check-widening-tells` verdict and `check-clause2-carriers --pair` exit code for its own diff. If the tells fire, this seat flips the declaration for all six rename cards **publicly on each card**; ⛔ no round reshapes its diff to silence a tell, and ⛔ no round edits its own claim line.\n\n<sub>epic PM for #15939 · `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T07:46Z</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652120294,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T08:06:51Z","updated_at":"2026-09-13T08:06:51Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5652120294","body":"## Ruling A's `patch` is falsified by the precedent Ruling A names — the six rename cards grade `minor`, not `patch`\n\nEpic PM, `session_015c5G6TmpMKgnusmTpD7Ntt`, 2026-09-13T08:16Z. Public correction of an execution detail. ⛔ Not a silent re-adjudication, and ⛔ the ruling's direction is untouched.\n\n### What Ruling A says\n\n> each as its own `Clause-②: no` / `patch` round **following the #15678 / #15679 shape**\n\n### What the #15678 / #15679 shape actually measures\n\nRead by me on `origin/main` in `packages/spec/CHANGELOG.md`, every entry under **`## 17.4.0`** — a **minor** bump:\n\n| line | entry |\n|--:|:--|\n| `:1804` | `feat(spec)!: the fourteen `kernel/` duration keys carry their unit in the key name (#15678, ruling B on #14478)` |\n| `:2764` | `feat(spec)!: the fifteen `system/` duration keys carry their unit in the key name (#15679, ruling B on #14478)` |\n| `:124` | `feat(spec)!: the twelve `api/` duration keys …  (#15677)` |\n| `:675` | `feat(spec)!: the last seven `data/` · `ui/` · `ai/` · `integration/` duration keys … (#15680)` |\n| `:912` | `feat(spec)!: a duration-shaped `z.number()` key carries its unit in the key name … new gate `check:duration-unit-keys` (#14478, #14519)` |\n\nEvery one is **`feat(spec)!`** — a breaking feature — at **minor**, each carrying an `<!-- adr-0087: registered … -->` disposition. ⛔ Not one is a `patch`.\n\n⇒ The two halves of Ruling A's clause disagree with each other: *\"`Clause-②: no` / `patch`\"* and *\"following the #15678 / #15679 shape\"* cannot both be satisfied. The shape is the measurable half, and it says `minor`.\n\n### Corroborated independently by the gate, on a live pair\n\n`Check Changeset` on PR #17954 failed with the maintainer ruling of 2026-09-04 (decision batch #35, on #15294) quoted in its own output:\n\n> A purely additive widening of a published package's public surface takes AT LEAST `minor`; the commit type may raise a bump but never lower it below what the act requires\n\n⇒ Two independent authorities — a landed precedent and a standing maintainer ruling — give `minor`. This is applying settled rules, not making a new call, so it is recorded here rather than sent to the decision box.\n\n### What changes, and what does not\n\n| | before | now |\n|:--|:--|:--|\n| declaration, six rename cards | `Clause-②: no` | **`yes`** (correction `5652103109` on #17784; the rest at claim time) |\n| changeset level, six rename cards | `patch` | **`minor`**, `feat(spec)!`, `[BREAKING]` + ADR-0087 disposition |\n| #17786 / PR #17953 | — | **unchanged**: `no` + `patch`. It adds no key; `--pair 17953` reads **exit 0** |\n\n⛔ Unchanged from Ruling A: still **per file**, still the #15678/#15679 shape, still ⛔ Not B, still ⛔ C is gate weakening, still **PR #17635 lands LAST** into a tree that reads 0 offenders.\n\n### Why the `patch` was in the ruling at all — recorded so the next author is not misled\n\nRuling A was written to size the work as small per-file rounds, and `patch` reads naturally alongside that framing. But a published-key rename is breaking whatever the round's size, and the ADR-0087 conversion is what makes it *survivable*, ⛔ not what makes it a patch. The four landed sibling cards all say `feat!` in the changelog this repo publishes.\n\n⚠️ **This is the second premise of this card's ruling chain to be falsified by measurement** — the first was batch #65's *\"the set is small and enumerable — no population count needed first\"*, which measured 0 → 21. Both were sizing assumptions that the tree did not bear out. Recording the pattern, not just the instance.\n\n<sub>epic PM for #15939 · `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T08:16Z</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652296795,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T08:47:54Z","updated_at":"2026-09-13T08:47:54Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5652296795","body":"## Settled once for the four remaining rename cards: every rename registers **both** a semantic entry **and** a `RETIRED_KEYS_BY_MAJOR` row\n\nEpic PM, `session_015c5G6TmpMKgnusmTpD7Ntt`, 2026-09-13T09:00Z. The #17784 round asked for this explicitly — *\"worth the PM settling it once in the dispatch rather than four times in review\"* — and it is a PM call, so here it is. Applies to **#17780, #17782, #17783, #17785**.\n\n### The question\n\nTwo earlier duration renames on `system/tenant.zod.ts` (#14478 / #14519) registered an ADR-0087 semantic entry but **no** `RETIRED_KEYS_BY_MAJOR` row. #15679's nested renames on `system/metrics.zod.ts` and `system/cache.zod.ts` registered **both**. Which is the shape to copy?\n\n### The answer: both. The earlier omission is a residue of a fixed bug, ⛔ not a convention.\n\n`packages/spec/scripts/build-schemas.ts` check (b) is a hard gate whose declared rule leaves no room:\n\n> (b) live → retired: a removal. **It must be registered** … Registration means EXACT set membership in `RETIRED_KEYS_BY_MAJOR` — the literal `${defKey}:${name}`.\n\nAnd its own docblock records how the two tenant keys escaped it:\n\n> Until **#4659** this check matched the key's LEAF against every conversion `surface` (`endsWith('.' + name)`, all majors, def ignored), so **any unrelated registration ending in the same leaf registered a tombstone for free**\n\nTheir leaves are `idleTimeout` / `sessionTimeout` — exactly that free-pass shape. ⇒ they are what #4659 fixed, and copying them would reproduce a bug.\n\nMeasured, with a same-subject lit control: `system/DatabaseLevelIsolationStrategy:connectionPool.idleTimeout` **0** rows, `system/TenantSecurityPolicy:accessControl.sessionTimeout` **0**, against `system/MetricAggregationConfig:window.size` **2** (the row is visible at `registry.ts:13136`), dark control 0.\n\n⇒ **Each of the four remaining rename cards registers both**, exactly as #17784 / PR #17954 did. ⛔ No round re-opens this; ⛔ no round cites the tenant pair as precedent.\n\n### The residue is filed separately, ⛔ not this epic's to fix\n\nThe two unregistered tombstones are **#17969**. They cannot be caught by check (b) on any future run, because it is a *transition* predicate (`newlyRetired` = live in the previous baseline, retired in this one) and these were already retired before it could see them. Their concrete cost is narrow and worth stating precisely: both keys **do** carry conversion entries, so the prescription does reach consumers — what they cannot do is ever **age out**, since check (c) proof 1 requires the exact row before a tombstone's baseline line may be deleted.\n\n⚠️ I initially framed that cost as \"consumers only learn by failing\" and that was **wrong** — the conversion entries exist. Corrected on #17969 before filing rather than after.\n\n<sub>epic PM for #15939 · `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T09:00Z</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652790672,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T10:46:12Z","updated_at":"2026-09-13T10:46:12Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5652790672","body":"## Epic landing checklist — refresh, and the Collision-3 inventory is now measured\n\nEpic PM `session_015c5G6TmpMKgnusmTpD7Ntt` · readings on `origin/main` @ `fc28c1d38`, 2026-09-13T10:45Z\n\n### Subtree state\n\n| card | file | rows | state |\n|:--|:--|--:|:--|\n| #17786 | — | 3 | **merged** |\n| #17784 | `system/tenant.zod.ts` | 1 | **merged** (PR #17954) |\n| #17781 | `kernel/plugin-security-advanced.zod.ts` | 1 | PR #17983 **in the merge queue** since 10:23Z, position 4 |\n| #17780 | `kernel/plugin-lifecycle-advanced.zod.ts` | 3 | PR #17986 draft, CI green but one job still running; in-seat clause-② review at `CONTRACT_REVIEW_TIER` **running** |\n| #17782 | `system/logging.zod.ts` | 4 | dev in flight |\n| #17783 | `system/metrics.zod.ts` | 5 | **dispatched 10:42Z** |\n| #17785 | `system/tracing.zod.ts` | 4 | reserved — held on the `os-verify-lock` depth rule, not on anything substantive |\n| PR #17635 | the gate + self-test + two corrected prose sites | — | **lands LAST**, per Ruling A |\n\n### Collision 3, previously logged as a risk, is now an inventory\n\nThe risk recorded earlier was that **a green `check:duration-unit-keys` is necessary but not sufficient** for #17635: the gate reads `.describe()` and `.meta({ description })`, so it cannot see a *sentence* that this epic makes false. Swept for it on `origin/main` (pattern: `outside (the) population` · `names no unit` · `no unit at all` · `left alone` · `untouched here` · `stays bare`, across `packages/spec/src`, `packages/spec/scripts`, `content/docs/protocol`; lit control `unit` → 1164, dark control → 0).\n\nFour hits are this epic's; everything else the sweep returned is past-tense, a different subject, or both.\n\n**A. `packages/spec/src/migrations/entries/retired-keys/18.kernel__SandboxConfig__process.timeout.ts:6-12`** — mirrored into the generated `migrations/registry.ts:12757-12758`, so one source edit fixes both. Its text, addressed in the present tense to \"anyone grepping this file\":\n\n> the neighbouring `RuntimeConfig.resourceLimits.timeout` is a DIFFERENT key whose describe names no unit at all, so it is outside the gate's population and is untouched here\n\nFour limbs, judged separately: \"is a DIFFERENT key\" stays true; \"untouched here\" is scoped to #15678 and stays true; **\"whose describe names no unit at all\" goes false the moment PR #17983 lands**; **\"so it is outside the gate's population\" goes false when #17635 lands**. Two false limbs inside navigational guidance. ⇒ owed a correction, and #17635 is where it belongs unless it turns out to be larger than a sentence.\n\n**B. `packages/spec/src/system/metrics.test.ts:560-572`** — `it('leaves the two non-duration keys on this file alone', …)` pins the error-budget burn-rate `window` bare. That is #17783's row `:552`; sent to its dev with the split instruction (the test bundles a genuine non-duration key, `batch.size`, which stays, and its **name** goes false when only one is left).\n\n**C. `packages/spec/src/system/tracing.test.ts:545`** — `it('leaves the OTel exporter `timeout` alone — its describe names no unit, so it is outside the population', …)`. That is #17785's row `:541`. Recorded on #17785 so it goes into that dispatch order rather than being discovered late.\n\n**D. PR #17635's own two corrected prose sites** — to be re-read against the tree at landing, not merely gated.\n\n⇒ B and C are absorbed by their own cards. **A and D are the residue that must be handled at #17635**, and neither is detectable by the gate reading 0 offenders.\n\n### Already settled, restated so the last two rounds do not re-litigate\n\n- `Clause-②: yes` and `minor`, both overriding Ruling A's `no`/`patch` (recorded on #17784 `5652094166` and here `5652120294`).\n- Both ADR-0087 registrations per renamed key: a semantic entry **and** a `RETIRED_KEYS_BY_MAJOR` row.\n- ⛔ A predecessor's semantic entry is never amended. It is a scoped past-tense record and stays true; the successor writes its own entry opening with how it relates. Established by PR #17983 for #15678's entry, and it is why hit A above is a *tombstone-prose* problem and not a *semantic-entry* problem.\n- The `authorable-surface` / `authorable-defaults` ratchet moves for **top-level** keys and must not move for **nested** ones. Both readings are attested by landed siblings — PR #17954 nested, moved nothing; PR #17986 top-level, moved. #17783 splits 2 top-level / 3 nested within one card.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652844746,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T10:58:37Z","updated_at":"2026-09-13T10:58:37Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5652844746","body":"### Audit line — the #17986 review's two out-of-scope escalations, dispositioned\n\nEpic PM `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T10:58Z. The at-tier review of PR #17986 (record `5652813288`, verdict PASS, adopted verbatim) raised two things outside that card. Both are routed here rather than absorbed into the epic.\n\n**1. Out to its own card: #18000.** `packages/core/PHASE2_IMPLEMENTATION.md`'s two `registerPlugin` examples throw when copied — seven keys across them are retired spellings. Independent issue in the fix's landing repo, dedupe run (7 hits, 2 open, no duplicate; #16208 is adjacent on the same file and flagged as a scheduling conflict). ⛔ Deliberately **not** a sub-issue of this epic: it is a surrounding finding, and the sub-issue channel is not a way to put un-triaged cards into the pool.\n\nOnly 3 of its 7 lines are this epic's doing; the other 4 were already false from the #15678-era retirements. That split is why it was not fixed inside PR #17986 — the example **already throws today**, so a partial fix would have widened that card while leaving a copy-paste equally broken.\n\n⚠️ The transferable lesson, and the reason this is on the epic rather than only on #18000: **PR #17986's consumer sweep excluded `packages/core` wholesale** because core was the package being changed. That exclusion is exactly what hid an in-package doc from a rename that package participated in. The remaining cards — #17782 (delivered, PR #17999), #17783 (in flight), #17785 (reserved) — must not repeat it. A package you are editing is still a consumer of the keys you are renaming.\n\n**2. Held as one class-wide question, not filed per key: the \"unit named nowhere\" shape.** `HotReloadConfig.shutdownTimeout` (default 30000, unit in neither JSDoc nor describe) is confirmed outside this card and outside ruling A's 21 rows. ⛔ Not filed as a card, and ⛔ not folded into any remediation round.\n\nThe reason is that the gate's own header records closing this class as a **describe-by-describe decision, not a defect** — which makes it a decision-box question, not a queue card, and this seat does not answer product questions on the maintainer's behalf. It also almost certainly has siblings: `RuntimeConfig.resourceLimits.maxMemory` was already recorded as the same shape on #17781 (a byte count, so outside #14478 twice over), and the gate's `--list` census keeps the whole class visible without a card.\n\n⇒ **the right shape is one class-wide question raised after #17635 lands**, when the census is finally clean of the 21 rows and the residual population is exactly this class and measurable in one reading. Raising it now would ask the maintainer to decide against a census still contaminated by the rows this epic is removing. Recorded here so it survives the epic rather than being re-discovered.\n\n### Landing state\n\nPR #17986 (#17780): review PASS at tier · carriers stripped from both sides at 10:55Z citing the record · 45 of 45 checks completed, 39 success / 6 skipped / **0 failure** · turned ready and auto-merge armed 10:56Z. ⚠️ Note for the record: the carrier strip itself re-triggered `Check Changeset`, which is why ready waited on a second convergence — the same label-write-triggers-CI behaviour that produced the red on sibling PR #17954.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5652978061,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T11:28:33Z","updated_at":"2026-09-13T11:28:33Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5652978061","body":"## Checklist refresh — 4 of 7 cards landed, 8 of the 21 rows on `main`\n\nEpic PM `session_015c5G6TmpMKgnusmTpD7Ntt` · `origin/main` @ `98bd7986fe`, 2026-09-13T11:27Z. Supersedes `5652790672`, whose card states are now stale.\n\n### The 21-row delta, reconciled against the tree for the first time\n\n| card | file | rows | state |\n|:--|:--|--:|:--|\n| #17786 | — (**describe-only**, no rename) | 3 | ✅ merged |\n| #17784 | `system/tenant.zod.ts` | 1 | ✅ merged — `fc28c1d381` |\n| #17781 | `kernel/plugin-security-advanced.zod.ts` | 1 | ✅ merged — `cbcae14bca` |\n| #17780 | `kernel/plugin-lifecycle-advanced.zod.ts` | 3 | ✅ merged — `98bd7986fe` |\n| #17782 | `system/logging.zod.ts` | 4 | PR #17999 draft, CI green, dev finishing |\n| #17783 | `system/metrics.zod.ts` | 5 | PR #18007 draft, CI green, dev finishing |\n| #17785 | `system/tracing.zod.ts` | 4 | **dispatched 11:26Z** |\n| | | **21** | **8 landed · 13 owed** |\n\n3 + 1 + 1 + 3 + 4 + 5 + 4 = **21**, exactly PR #17635's enumeration. ⇒ the card set is complete and the split is accounted for; no row is orphaned and none is double-counted.\n\n⚠️ Correcting my own working assumption, recorded because it affects how the last three rounds read the precedent set: **#17786 is a describe-only card, not a rename.** Its title states its three keys *already name their unit*. So of the four cards landed, only **three** are renames, and the rename precedent available to the remaining rounds is #17784 (1 nested key), #17781 (1 nested) and #17780 (3 top-level) — plus the older #15678 / #15679.\n\nVerified live on `main`: `plugin-lifecycle-advanced.zod.ts` now carries 3 suffixed key-position declarations (`intervalMs`, `timeoutMs`, `debounceDelayMs`).\n\n### The ratchet reading, now attested three ways\n\nThe `authorable-surface` / `authorable-defaults` ledger records **top-level keys per def**, so a rename moves it only for a top-level key. Landed evidence:\n\n| PR | keys | ledger |\n|:--|:--|:--|\n| #17954 (#17784) | nested (`performance.schemaCacheTTL`) | moved **nothing** ✅ |\n| #17986 (#17780) | 3 top-level | **moved** ✅ |\n| #17999 (#17782) | 1 top-level, 3 nested | moves for **one** |\n| #18007 (#17783) | 2 top-level, 3 nested | moves for **two** |\n| #17785 | **all four nested** (predicted) | should move **nothing** |\n\nSame rule, five different correct answers. Each round owes its own verification from `build-schemas.ts`, ⛔ not from this table.\n\n### Still owed at the end, neither detectable by a green `check:duration-unit-keys`\n\nUnchanged from `5652790672`, restated so it survives the remaining rounds:\n\n- **A.** `migrations/entries/retired-keys/18.kernel__SandboxConfig__process.timeout.ts:6-12` — two of its four present-tense limbs about `RuntimeConfig.resourceLimits.timeout` are **now false**, since #17781 landed at `cbcae14bca`. The \"outside the gate's population\" limb goes false when #17635 lands. Belongs to #17635.\n- **D.** PR #17635's own two corrected prose sites — a sentence-by-sentence re-read against the tree, not a gate reading.\n\n### Filed en route\n\n- **#18000** — `packages/core/PHASE2_IMPLEMENTATION.md`'s two `registerPlugin` examples throw when copied; 7 retired spellings, 3 of them falsified by #17780. Independent card, ⛔ not a sub-issue of this epic.\n- Held, ⛔ not filed: the **\"unit named nowhere\"** class (`HotReloadConfig.shutdownTimeout`, `RuntimeConfig.resourceLimits.maxMemory`) — one class-wide question for the decision box **after** #17635 lands, when the census is clean enough to decide against.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5653584477,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T13:31:05Z","updated_at":"2026-09-13T13:31:05Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5653584477","body":"## Residue item **E**, and one open question that could reach back into five merged PRs\n\nEpic PM `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T13:35Z. Both surfaced by #17785's round; both verified here against `origin/main` @ `1e20f816ea` before recording.\n\n### E. `packages/spec/CHANGELOG.md:2866` — a published passage two cards jointly falsify\n\nQuoted from the tree, not from the report:\n\n> `ServiceLevelObjective.errorBudget`'s burn-rate `window` and the OpenTelemetry exporter `timeout` name no unit anywhere in their prose, so both are outside the gate's population entirely. Pin tests assert each of these, so a later sweep cannot read this card as \"every duration-shaped number on these files\".\n\nBoth named keys are renamed by this epic — the burn-rate `window` by **#17783** (PR #18007) and the OTel exporter `timeout` by **#17785** (PR #18016) — so the passage is **jointly falsified by two cards**, and its last sentence describes precisely the two pin guards those cards were written to trip. That makes it residue item **E**, alongside A (the `SandboxConfig` tombstone note) and D (#17635's own prose).\n\n⚠️ **Correcting the recommended route.** #17785's dev proposed folding E into PR #17635, fallback a docs-only PR. Measured: the fallback is not hypothetical, it is the **established pattern with a precedent commit** — `ef47459474 docs(changelog): correct the two published `Seed.locale` claims PR #17013 falsified, with erratum lines (#17896)`. Same shape exactly: a released CHANGELOG passage falsified by a later PR, corrected by a `docs(changelog)` PR carrying **erratum lines** rather than a rewrite.\n\n⇒ **E goes to a `docs(changelog)` erratum PR on the #17896 pattern, not into #17635.** Reasons: it is jointly owned by two cards so it belongs to neither; #17635 is the gate and folding release-owned prose into it widens a PR that must land into a tree it reads as clean; and the erratum form preserves the original text, which matters for a released changelog in a way a silent edit does not. ⛔ Not hand-edited inside any code PR — `packages/*/CHANGELOG.md` is release-owned.\n\nAlso worth recording for whoever writes that erratum: the passage says the two keys \"name no unit **anywhere in their prose**\", but #17785 measured the OTel exporter `timeout` carrying a JSDoc reading \"Timeout in milliseconds\". So one limb was arguably **already inaccurate when published** — under the reading of \"prose\" that this whole epic exists to fix, namely that `.describe()` is not the only prose channel. The erratum should say which reading it is correcting.\n\n### Open: `acceptRetiredDefaultResidue()` — routed to the at-tier review, not settled here\n\n`retired-key.ts` ships `acceptRetiredDefaultResidue()`, which by its own account exists **for a retired key that carried a Zod `.default()`**. Every key #17785 retires carried one (10000 / 30000 / 5000 / 5000), and it used plain `retiredKey()` — matching every landed sibling of this epic.\n\n⚠️ **If plain `retiredKey()` is wrong for a defaulted key, five already-merged PRs are wrong too** — #17954, #17983, #17986, #17999, and #15679's originals. That is why it is not being settled by seat judgement: it is the primary attack point of the in-seat review now running on #18016, with an explicit instruction to say so plainly if the evidence leads there and not to soften it because the fix would be large.\n\nThe dev's own argument, for the review to verify or refute: the helper is needed only where an **authorable surface's parsed output is stored and re-parsed**, so a built artifact can carry the emitted default — and neither `OpenTelemetryCompatibility` nor `TracingConfig` is that, which is the same reading that makes these D3 semantic entries rather than D2 conversions. Its recommendation is to leave this PR as-is and settle the class epic-wide here if the answer is otherwise. I will record the outcome on this card either way.\n\n### Housekeeping — a defect in my own dispatch template\n\nZone 2.6 of my orders names **`@objectstack/qa`** in the consumer-package list. Measured: **no package by that name exists.** `packages/qa/` holds `@objectstack/dogfood`, `@objectstack/downstream-contract`, `@objectstack/http-conformance` and `@objectstack/refd-timer-testkit`. #17785's dev ran all four and reported an exit code for each rather than guessing, and flagged the name so the template gets fixed instead of re-guessed. Recorded; the list should name the four members.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5653691261,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T13:52:23Z","updated_at":"2026-09-13T13:53:18Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5653691261","body":"os-dev-report\n{\n  \"issue\": 15939,\n  \"status\": \"blocked\",\n  \"branch\": \"claude/issue-15939-duration-unit-keys-jsdoc\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/17635\",\n  \"premise_still_valid\": false,\n  \"summary\": \"No code change this round, by measurement. Merged origin/main into the branch twice (pushed head 18d9838f0e) and re-ran check:duration-unit-keys: it reports 9 offenders, not the 1 the dispatch order named, and every one of the 9 sits inside the declared face of an open sibling rename PR (5 rows in packages/spec/src/system/metrics.zod.ts, owned by PR #18007 / card #17783; 4 rows in packages/spec/src/system/tracing.zod.ts, owned by PR #18016 / card #17785). The order's premise that system/tracing.zod.ts is a fourth file no sibling covers is FALSE: card #17785 was filed for exactly those four rows, claimed 2026-09-13T11:25Z on branch claude/issue-17785-tracing-duration-units, and PR #18016 is open and draft on that branch. Measured live during this run: the 4 packages/spec/src/system/logging.zod.ts offenders present at the first reading (13 offenders, merge base 2f1a6f696) vanished when PR #17999 merged mid-run, leaving 9 at merge base 1e20f816ea, which is Ruling A's sequencing working as designed. #17635 is therefore blocked on #18007 and #18016 landing; it cannot go green before them, and nothing in its own face can move the count.\",\n  \"tests\": \"All on pushed head 18d9838f0e (worktree /home/user/objectstack-issue-15939), exit codes captured before any pipe. GATE UNION: node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack derived 88 families; all 88 executed; reconciled with dispatch-gates --ran -> exit 0, '88 derived famil(ies) accounted for - 84 run, 4 NOT-MEASURED (4 DERIVED from a recorded exit 3), 0 UNRUN'. RED (1, and it is the expected one): pnpm --filter @objectstack/spec run check:duration-unit-keys -> exit 1, verdict line '9 offender(s) among 211 duration-shaped numeric key(s) in 2470 source file(s) (6 declared EpochMs instant(s), 11 declared externalVocabulary mirror(s))'. NOT MEASURED (4, all exit 3 = PREREQUISITE NOT MET, all needing a full-repo build this round did not run; declared to CI Build Core, not read as pass or fail): pnpm --filter @objectstack/lint run check:doc-formula-expressions; pnpm check:dual-build-cjs-loads; pnpm check:lean-entry-closure; pnpm check:type-check-debt. GREEN (83 of the remaining derived families), plus: pnpm --filter @objectstack/spec build -> exit 0 under os-verify-lock (VERDICT command-exit 0); pnpm --filter @objectstack/spec typecheck -> exit 0, 0 lines matching 'error TS'; pnpm --filter @objectstack/spec test -> exit 0, 'Test Files 476 passed (476) / Tests 13563 passed (13563)'; pnpm --filter @objectstack/spec check:generated -> exit 0, 'All 15 generated artifacts are up to date' (run after BOTH merges, so the merge commits owe no regeneration and no os-regen deferral is outstanding). ABLATION: none run and none owed - this round changed no code, so there is no new guard to prove can fail.\",\n  \"mcp_calls\": \"0 - every GitHub read this round went through repo-scoped REST (probe: GET /repos/objectstack-ai/objectstack/pulls/17635 -> HTTP 200); no MCP GitHub tool was called, and no write other than the two git pushes and this report comment.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"noted, not filed: the offender list contains no file outside the three sibling cards' faces, so the 'fifth file nobody owns' gap the dispatch order asked me to look for does not exist on this tree. Carrier: none needed.\",\n    \"noted, not filed: 4 of the 88 derived gate families refuse with exit 3 (PREREQUISITE NOT MET) unless the whole workspace is built, not just @objectstack/spec; a spec-only card can never measure them locally inside one round. Carrier: CI Build Core already covers them on every PR.\"\n  ],\n  \"offenders_measured_on_18d9838f0e\": [\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/metrics.zod.ts:191\",\n      \"key\": \"maxAge\",\n      \"jsdoc_unit\": \"seconds\",\n      \"ownership\": \"NOT MINE - inside the declared face of open draft PR #18007 (card #17783, branch claude/issue-17783-metrics-duration-unit-keys, head bcfdd356); wait for it to land\"\n    },\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/metrics.zod.ts:552\",\n      \"key\": \"window\",\n      \"jsdoc_unit\": \"seconds\",\n      \"ownership\": \"NOT MINE - inside the declared face of open draft PR #18007 (card #17783, branch claude/issue-17783-metrics-duration-unit-keys, head bcfdd356); wait for it to land\"\n    },\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/metrics.zod.ts:623\",\n      \"key\": \"interval\",\n      \"jsdoc_unit\": \"seconds\",\n      \"ownership\": \"NOT MINE - inside the declared face of open draft PR #18007 (card #17783, branch claude/issue-17783-metrics-duration-unit-keys, head bcfdd356); wait for it to land\"\n    },\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/metrics.zod.ts:709\",\n      \"key\": \"collectionInterval\",\n      \"jsdoc_unit\": \"seconds\",\n      \"ownership\": \"NOT MINE - inside the declared face of open draft PR #18007 (card #17783, branch claude/issue-17783-metrics-duration-unit-keys, head bcfdd356); wait for it to land\"\n    },\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/metrics.zod.ts:718\",\n      \"key\": \"period\",\n      \"jsdoc_unit\": \"seconds\",\n      \"ownership\": \"NOT MINE - inside the declared face of open draft PR #18007 (card #17783, branch claude/issue-17783-metrics-duration-unit-keys, head bcfdd356); wait for it to land\"\n    },\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/tracing.zod.ts:541\",\n      \"key\": \"timeout\",\n      \"jsdoc_unit\": \"ms\",\n      \"ownership\": \"NOT MINE - inside the declared face of open draft PR #18016 (card #17785, branch claude/issue-17785-tracing-duration-units, head 96e730f0); wait for it to land\"\n    },\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/tracing.zod.ts:565\",\n      \"key\": \"exportTimeout\",\n      \"jsdoc_unit\": \"ms\",\n      \"ownership\": \"NOT MINE - inside the declared face of open draft PR #18016 (card #17785, branch claude/issue-17785-tracing-duration-units, head 96e730f0); wait for it to land\"\n    },\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/tracing.zod.ts:570\",\n      \"key\": \"scheduledDelay\",\n      \"jsdoc_unit\": \"ms\",\n      \"ownership\": \"NOT MINE - inside the declared face of open draft PR #18016 (card #17785, branch claude/issue-17785-tracing-duration-units, head 96e730f0); wait for it to land\"\n    },\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/tracing.zod.ts:723\",\n      \"key\": \"exportInterval\",\n      \"jsdoc_unit\": \"ms\",\n      \"ownership\": \"NOT MINE - inside the declared face of open draft PR #18016 (card #17785, branch claude/issue-17785-tracing-duration-units, head 96e730f0); wait for it to land\"\n    },\n    {\n      \"class\": \"unit-in-jsdoc-not-in-describe\",\n      \"site\": \"packages/spec/src/system/logging.zod.ts (4 rows: flushInterval:278, initialDelay:293, timeout:304, flushInterval:672)\",\n      \"key\": \"CLEARED DURING THIS RUN\",\n      \"jsdoc_unit\": \"ms\",\n      \"ownership\": \"RESOLVED - PR #17999 (card #17782) MERGED mid-run; these 4 rows were in the first reading (13 offenders at merge base 2f1a6f696) and are gone from the final reading (9 offenders at merge base 1e20f816ea)\"\n    }\n  ]\n}\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5653760586,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T14:05:49Z","updated_at":"2026-09-13T14:05:49Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5653760586","body":"## Two settlements and three new records — from the #18007 and #18016 at-tier reviews (both FAIL)\n\nEpic PM `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T14:08Z. Both verdicts adopted verbatim; served tier verified from each transcript's harness stamps (`claude-fable-5-1` × 137 and × 175, no other `model` value, 0 fallback notices, dark controls invented at read time reading 0).\n\n### ⭐ SETTLED — `acceptRetiredDefaultResidue()` is NOT owed, and the five merged PRs are sound\n\nThe question that could have invalidated #17954, #17983, #17986, #17999 and #15679's originals. Answer: **plain `retiredKey()` is right**, and for a sharper reason than \"the default is unchanged\".\n\nThe precondition was derived from the helper's own two call sites (`permission.zod.ts:415` / `:455`) and its founding tests. The helper is owed only when **all three** hold:\n\n| | condition |\n|:--|:--|\n| (a) | the key carried a `.default()`, so a released toolchain **emitted** it |\n| **(b)** | **that parsed output — defaults materialised — is serialised into something later re-parsed through the same shape** (a built plugin artifact, a stored `sys_permission_set` row, a wire response) |\n| (c) | the emitted default is behaviourally inert |\n\n**Only (b) decides**, and it fails on every axis for every def in this epic: `stack.zod.ts` names no collection for them, `MetadataTypeSchema` has no matching type (so never a `sys_metadata` row), no other def embeds them, zero readers outside `packages/spec`, zero in pinned objectui. No toolchain has ever materialised these defaults into anything that re-enters a parse. It is the same reachability test that makes these D3 rather than D2.\n\n⇒ **Adding the residue stage here would be actively wrong**, not merely unnecessary: it would silently accept the old key from hand-authored source — the exact population a tombstone exists to refuse.\n\n**Rule recorded for the future:** where a defaulted-key retirement meets (a)+(b)+(c), the helper is **required** and plain `retiredKey()` is the defect. ⚠️ Declared limit: #15679's other 14 rows were not re-derived (shallow history), and the review said so.\n\n### ⭐ SETTLED — tombstone prose names `@objectstack/spec 17`, not 18\n\nMeasured with concatenated string literals joined first — ⚠️ the phrase spans a `'…' + '…'` break, so a single-line grep reads **zero**, which is how my first two attempts at this measurement failed:\n\n| ref | `@objectstack/spec 17` | `@objectstack/spec 18` |\n|:--|--:|--:|\n| `origin/main` | **265** | 14 |\n| #18016's head | 260 | 18 |\n\n**17 is correct**, on two grounds and the second decides it:\n\n1. 265 : 14 — the convention is overwhelming.\n2. **The two numbers are different axes.** Tombstone prose names the **released package version**: this ships in `@objectstack/spec` **17.5.0** (package is at 17.4.0). `RETIRED_KEYS_BY_MAJOR[18]` names the **protocol major** (`PROTOCOL_VERSION` = `17.0.0`). A reader told \"renamed in @objectstack/spec 18\" looks in v18 and does not find it. Prose that means the protocol should say \"protocol 18\", never the package name.\n\n**Blast radius, measured.** Of main's 14, **five are this epic's** — all in `plugin-lifecycle-advanced.zod.ts` (#17986, merged), a file that is **internally mixed**, since its own `HEALTH_CHECK_INTERVAL_RETIRED` says 17. The other nine are non-epic (`disaster-recovery`, `cache`, `export`, `view`, `connector`, `execution`) and outside this epic. #18016's four are being corrected in its current push; **#17986's five need a follow-up** and are ⛔ not being folded into #17635.\n\n### Residue item **E** — routing confirmed by independent reading\n\n#18016's review reached the same place I did from the other direction: the dev's option C (fold the `CHANGELOG.md:2866` correction into #17635) is **unavailable** — #17635 is a code PR that does not touch the CHANGELOG, and AGENTS.md requires a dedicated docs-only PR. ⇒ **E is a `docs(changelog)` erratum PR on the #17896 precedent, after #17783 lands.** Two independent routes, same answer.\n\n### Residue item **A** — new detail\n\n`18.kernel__SandboxConfig__process.timeout.ts:7-9` was already residue A. New: **#17635's pending hunk on that file is itself stale.** Whoever rebases #17635 inherits both halves.\n\n### New: #17635 will also conflict on `metrics.test.ts`\n\n#17635's diff patches comment lines that #17783 deletes, inside a test that no longer exists in that form. ⇒ its rebase drops that hunk and keeps the `SandboxConfig` half. Recorded so the last card is not surprised.\n\n### Three defects of mine, recorded rather than churned\n\n1. **Both my claim comments (#17783 `5652931475`, #17785 `5652963724`) carry no `Session:` line**, flagged by both reviews. Read the template at `.claude/skills/pm-dispatch/SKILL.md` 〈模板与表〉 afterwards: they are missing most of it — `Session:`, `Worktree:`, `Domain:`, `File surface:`, `Container & model:`, `Thread-read:`, `Serial constraints cleared:`. The gates read green (`check-closing-target-claim` success, `--pair` exit 0 on both), and the protocol forbids a second `Claim:`, so ⛔ I am not posting more claim-shaped comments to chase it. Recorded as the fourth instance of one root cause: **a machine-read field composed from memory instead of copied from the template.**\n2. **`dispatch-gates --commands` does not derive `check:cli-command-ids`**, which is a step of a required job. #18016's \"109/109, 0 NOT-MEASURED\" reconciliation therefore could not have seen the gate that later redded it. A real tooling gap; not this epic's to fix.\n3. **My count-provenance rule produced both of #18016's FAIL items** — one phrasing that collided with `check:cli-command-ids`, one count whose corpus statement drifted from its measurement. The rule stands; the amendment sent to both devs is that **ref, corpus and number must come from one reading**, not be assembled from separate steps.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5654385018,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T16:02:18Z","updated_at":"2026-09-13T16:02:18Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654385018","body":"## Maintainer directive recorded — dispatch #17635 after the seventh card merges, carrying the five residues\n\nEpic PM `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T16:02Z.\n\n**Whose instruction, the words, and where.** The maintainer, in this session's PM conversation, 2026-09-13: 「合并后派 #17635,带上那五条残余」 — *dispatch #17635 after the merge, carrying the five residues*. That converts what had been my own proposed plan into a directive, and is the authorisation for this seat to dispatch a PR authored by another seat.\n\n### Standing to dispatch — established, not assumed\n\n| | |\n|:--|:--|\n| author | `os-bill`, the outgoing `domain:spec` execution seat |\n| linkage | `Part of #15939`, on branch `claude/issue-15939-duration-unit-keys-jsdoc` — i.e. claiming **this** card |\n| the seat | stood down on this card 2026-09-13T07:12Z, quoting the maintainer's 「15939 我会交给新的项目经理开发」 |\n| activity since | timeline 2026-09-12 → now is **entirely `cross-referenced`** — no pushes, no edits; branch head `18d9838f0e` equals the PR head |\n\n⇒ nobody is working it, and it is inside the subtree this seat was handed. ⛔ Had a push appeared on that branch, this would have gone back to the maintainer rather than forward.\n\n### ⚠️ A correction the epic must NOT carry over: #17635's `Clause-②: no` is probably RIGHT\n\nEvery remediation card had its `Clause-②: no` corrected to **`yes`**, on the mechanical floor that a rename puts a new key on a published payload. **That correction does not transfer to #17635, and applying it by habit would be a real error.**\n\nThe lane rule: 「放宽接受集或扩大公开面的卡,不论多小,即条款②;**收窄仍是语义面,不触条款②**」. #17635 does not rename a key or add one — it **widens a gate**, so the schema's accepted set gets strictly **narrower**. Narrowing is explicitly not clause ②.\n\n⇒ the dispatch order will ask the round to **re-derive the declaration from the diff**, not to copy either the siblings' `yes` or the PR's existing `no`. ⛔ This seat is not asserting the answer; it is flagging that the epic's own correction is the wrong default here.\n\n### Preconditions before the order goes out\n\n1. **#17785 / PR #18016 merged.** As of 16:01Z it is queue position 3 (main `a24978614a` → pr-18039 → pr-18041 → **pr-18016**), `clean`, PASS at tier. Six of seven are on `main`; 17 of 21 rows.\n2. **The tree reads clean under the *widened* gate.** ⚠️ Not the gate on `main` — that one already reads 0 and proves nothing here. #17635's own body records it as **PARKED by construction, RED with 21 offenders**, and those 21 are exactly this epic's rows. The reading that matters is #17635's widened gate on a tree carrying all seven cards, which only exists after its rebase.\n3. Every zero carries a lit control. A bare zero from a wrong path or an unbuilt gate is not a reading, and exit 3 (`PREREQUISITE NOT MET`) is neither a pass nor a finding.\n\n### The five, by owner — this is the part the gate cannot do\n\n⚠️ **A 0-offender reading is necessary but NOT sufficient.** `check:duration-unit-keys` reads `.describe()` and `.meta()`. It cannot see a sentence that this epic made false, and it found **none** of the following:\n\n**#17635's own work:**\n- **A** — `migrations/entries/retired-keys/18.kernel__SandboxConfig__process.timeout.ts:7-9`. Four limbs, two now false: *\"whose describe names no unit at all\"* (false since #17781 landed) and *\"so it is outside the gate's population\"* (false once #17635 lands). *\"is a DIFFERENT key\"* and *\"untouched here\"* stay true. **#17635's own pending hunk on that same file is itself stale.**\n- **D** — a sentence-by-sentence re-read of #17635's two corrected prose sites against a tree that has moved by seven cards since they were written.\n- **Rebase hazard** — it conflicts on `packages/spec/src/system/metrics.test.ts`: its hunk patches comment lines #17783 **deleted**. Drop that half, keep the `SandboxConfig` half.\n\n**Not #17635's:**\n- **E** — `packages/spec/CHANGELOG.md:2866`, falsified jointly by #17783 and #17785 ⇒ a dedicated `docs(changelog)` **erratum** PR on the `ef47459474` / #17896 precedent. ⛔ Not foldable: #17635 is a code PR that does not touch the CHANGELOG, and `packages/*/CHANGELOG.md` is release-owned.\n- **#18040** — 14 tombstones prescribing `@objectstack/spec 18` when the change ships in 17.5.0, five of them from this epic's own #17986. Already filed, as enforce-or-remove.\n\nAlso still held, ⛔ deliberately not filed: the **\"unit named nowhere\"** class (`HotReloadConfig.shutdownTimeout`, `RuntimeConfig.resourceLimits.maxMemory`) as **one** class-wide decision-box question *after* #17635 lands, when the census is finally clean enough to decide against.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5654437080,"author":{"login":"zhuangjianguo","id":19182527,"type":"User"},"created_at":"2026-09-13T16:11:29Z","updated_at":"2026-09-13T16:11:29Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654437080","body":"### Standing authorisation — dispatch #17635 on the merge, no further confirmation\n\nEpic PM `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T16:12Z. Addendum to `5654385018`.\n\n**Whose instruction, the words, where.** The maintainer, in this session's PM conversation, 2026-09-13: 「等 #18016 合并后直接派,不用再问我」 — *dispatch as soon as #18016 merges, do not ask again*.\n\n⇒ the dispatch is **pre-authorised across a future event**. This seat does not return to the maintainer when #18016 lands; it runs the preconditions and dispatches. Recorded here so a seat picking this card up after a context break does not stall waiting for a confirmation that has already been given.\n\n⛔ The authorisation covers **dispatching #17635**. It does not widen anything else: the standing red lines hold unchanged — this seat still never approves or merges a governed-surface PR, never turns one ready without a clause-② review PASS on the current head, and never folds residue **E** or **#18040** into #17635.\n\n**Preconditions still binding before the order goes out** (from `5654385018`): #18016 merged; the **widened** gate reading clean on a tree carrying all seven cards — ⚠️ not the gate on `main`, which already reads 0 and proves nothing here; every zero carrying a lit control.\n\nQueue state at 16:11Z: #18016 is position 3 — main `a24978614a` → pr-18039 → pr-18041 → **pr-18016** — `clean`, PASS at tier, 35/35 green.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5654500177,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T16:23:05Z","updated_at":"2026-09-13T16:23:05Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654500177","body":"Claim: PM loop round 8 — epic PM for the #15939 subtree\nSession: `session_015c5G6TmpMKgnusmTpD7Ntt`\nBranch: `claude/issue-15939-duration-unit-keys-jsdoc`\nWorktree: `objectstack-issue-15939`\nDomain: `domain:spec`\nFile surface: `packages/spec/scripts/`, `packages/spec/src/migrations/`, `packages/spec/src/system/`, `.changeset/` (stop on breach; explain in the report)\nContainer & model: `M — rebase plus two human prose re-reads`, `mode:subagent`, `model: opus` — quoting this dispatch's own `node scripts/pm/dispatch-gates.mjs --tier` over the PR's five paths, 2026-09-13T16:19Z: *\"Model tier — no path-derived mandate … The tier stays the PM's per-card judgment call (floor sonnet · default opus · ceiling fable)\"*. Default tier, not floor: residues **A** and **D** below are sentence-by-sentence judgments about whether a paragraph is still true, which is exactly what a floor-tier transcription round gets wrong.\nClause-②: no\nThread-read: 5654437080\nSerial constraints cleared: **measured, not recalled** (2026-09-13T16:20Z). All **11** open PRs in the repo were enumerated and each one's file list read over REST. Exactly **one** shares a path with PR #17635: **#17835** contends for `packages/spec/src/migrations/registry.ts` — it is a *draft*, not ready, not enqueued, and the round below regenerates that file with `gen:migration-registry` after its rebase, so the contention is a landing-order fact and not a blocker. The other nine and the version-packages PR are disjoint: #17076 was paged in full (**447** files, **287** `.changeset/` rows) and carries **0** rows under `packages/spec/src/` and **0** hits on this PR's changeset. That the intersection instrument works is shown by the one hit it did return on #17835, drawn the same way as the zeros. `scripts/pm/os-verify-lock.sh --status` read `lock is free`, `queue: empty` ⇒ arrival depth 1, below `LOCK_DEPTH_HOLD` (2). **No epic sibling is in flight** — all seven remediation cards (#17780–#17786) are merged.\n\n### Standing to dispatch\n\nPR #17635 is `os-bill`'s, opened 2026-09-11, `Part of #15939`, head `18d9838f0e` on `claude/issue-15939-duration-unit-keys-jsdoc`. The `domain:spec` execution seat handed this subtree over on 2026-09-13T07:12Z ([`5651870097`](https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5651870097)) and every timeline event on the PR since 09-12 is `cross-referenced` only. **Nobody is working it.** The maintainer's directive to dispatch it once the seventh card merged is [`5654385018`](https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654385018), and the standing authorisation to dispatch without a further confirmation is [`5654437080`](https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654437080). The seventh card's PR #18016 merged at 2026-09-13T16:14:38Z as `6d647858b7`, which is `origin/main`'s tip as this claim is written.\n\n### Why the answer on clause ② is `no`\n\nMeasured against the PR's actual diff (5 files, +255/−10): the gate script `packages/spec/scripts/check-duration-unit-keys.ts`, two comment-only prose repairs, the regenerated migration registry that mirrors one of them, and the changeset. **No new exported symbol, and no new key on a published payload.** The mechanical floor for a `yes` is not reached. What the diff *does* do is make an existing gate refuse more inputs — that **narrows** the accept set, and narrowing is the semantic face, never clause ②. The eighteen renames that *would* have been a `yes` are precisely what Ruling A moved out of this PR and into the seven cards that have now all landed.\n\n⚠️ The `--tier` run still names three of the five paths as `packages/spec/src/**`, *\"the contract surface … the normal landing zone of a clause-② card\"*. That is the **path leg** of the enqueue gate and it fires here regardless of this declaration, so an in-seat contract review at `CONTRACT_REVIEW_TIER` is owed on this PR **before** it may turn ready or enqueue. It is scheduled, not waived.\n\n⚠️ If the round's rebase changes that answer — a key added, an export added, an accept/reject outcome moved — this declaration is wrong: **stop and report**. ⛔ Do not edit this line.\n\n<sub>epic PM for #15939 · `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T16:21Z</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5654522659,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T16:27:12Z","updated_at":"2026-09-13T16:27:12Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654522659","body":"## Dispatch order — PR #17635, the gate, landing last\n\nEpic PM for the #15939 subtree, `session_015c5G6TmpMKgnusmTpD7Ntt`, 2026-09-13T16:25Z. Card claim: [`5654500177`](https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654500177). Target: **PR #17635**, head `18d9838f0e`, branch `claude/issue-15939-duration-unit-keys-jsdoc`, base sha on the PR `1e20f816ea` — `origin/main` is now **`6d647858b7`**.\n\n⛔ **Merge `main` into the branch. Never rebase it, never amend, never force-push.** The branch is `os-bill`'s; a merge commit keeps their checkout valid.\n\n---\n\n### Zone 1 — Adjudicated. ⛔ Not re-litigable, not re-derivable, quoted rather than paraphrased.\n\n**Ruling A on #15939** — director seat, summon #22, 2026-09-11T14:08Z; provenance maintainer, decision batch #115, verbatim 「同意」 ([`5635659224`](https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5635659224)):\n\n> **Ruling A.** Batch #65's direction stands (the gate refuses a duration key whose JSDoc names a unit its describe does not). Sequencing: the remediation lands **first, per file**, each as its own `Clause-②: no` / `patch` round following the #15678 / #15679 shape (rename the key with its unit token, ADR-0087 conversion + tombstone + pin where the key is published; describe-only fixes where no rename is needed), and PR **#17635** (the gate + self-test + the two corrected prose sites) lands **last**, into a tree it already reads as clean. ⛔ Not B (18 published-key renames in one PR); ⛔ C is gate weakening.\n\n> **Execution (`domain:spec` seat):** … when all are on `main`, merge `main` into #17635, confirm the gate reads 0 offenders, and land it. ⚠️ Every rename of a published key touches the pinned sibling checkout — `git grep` objectui at the pinned SHA before each landing (AGENTS.md Post-Task Checklist step 4).\n\n**Maintainer directive**, [`5654385018`](https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654385018) — dispatch #17635 once the seventh card merges, carrying the five residues. **Standing authorisation**, [`5654437080`](https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654437080) — 「等 #18016 合并后直接派,不用再问我」.\n\nFour settlements from this epic that bind this round and are ⛔ not to be reopened:\n\n1. **`acceptRetiredDefaultResidue()` is NOT owed here, and adding it would be a defect.** Its precondition is not \"the key carried a `.default()`\" — it is that the parsed output is *serialised and later re-parsed through the same shape*. That limb fails for every def in this epic. Plain `retiredKey()` is correct; residue acceptance would make the tombstone **silently accept** hand-authored old keys.\n2. **A tombstone's prose version is `17`** — the `@objectstack/spec` **package** major. `RETIRED_KEYS_BY_MAJOR[18]` is the **protocol** major: a different axis, not a second opinion about the same number.\n3. **Residue E is NOT this PR's.** `packages/spec/CHANGELOG.md:2866` goes to a separate `docs(changelog)` erratum PR on the `ef47459474` / #17896 precedent. ⛔ A code PR never edits `packages/*/CHANGELOG.md`.\n4. **#18040 is NOT this PR's.** The 14 tombstones prescribing `@objectstack/spec 18` are filed as their own enforce-or-remove card. ⛔ Do not widen this PR to sweep them, even where you touch a neighbouring line.\n\n---\n\n### Zone 2 — PM mechanism assumptions. Every line below is a **premise to falsify first**, ⛔ never a finding to trust.\n\nReadings taken by this seat at `origin/main` @ `6d647858b7`, 2026-09-13T16:15–16:24Z.\n\n**② ‑1 · The tree is ready.** All seven remediation cards (#17780–#17786) are merged; **21 of 21** rows are on `main`. Spot reading: the four `tracing` keys read suffixed (`timeoutMs` / `exportTimeoutMs` / `scheduledDelayMs` / `exportIntervalMs`, 1 each), lit control `z.number` 20, dark control 0. Re-measure before you rely on it.\n\n**② ‑2 · ⚠️ A 0-offender reading is NECESSARY BUT NOT SUFFICIENT. Residues A and D below must be human sentence-by-sentence readings, ⛔ never inferred from green.** The gate cannot see whether a *comment* is true.\n\n**② ‑3 · The gate reading that matters is #17635's WIDENED gate on a tree carrying all seven cards.** `check:duration-unit-keys` on today's `main` reads **0 offenders**, and that reading **proves nothing here**: `main` still carries the *old* gate. #17635's own body records its branch as *\"PARKED by construction, RED with 21 offenders\"*. ⛔ Do not quote a green from the wrong instrument.\n\n**② ‑4 · Residue A — #17635's replacement prose on the tombstone file is ITSELF STALE.** The file `packages/spec/src/migrations/entries/retired-keys/18.kernel__SandboxConfig__process.timeout.ts` is byte-unchanged between `1e20f816ea` and `6d647858b7`, so the hunk still applies cleanly — and lands **already wrong**. Its replacement text asserts, of the neighbouring `RuntimeConfig.resourceLimits.timeout`:\n\n> `// describe names no unit at all, so it is inside the gate's census and outside`\n> `// its verdict, and is untouched here. ⚠️ Its unit is not missing, only`\n> `// unpublished: … so this key is a rename waiting on that gate change, not a`\n> `// key that has nothing to fix.`\n\nMeasured on `6d647858b7` at `packages/spec/src/kernel/plugin-security-advanced.zod.ts`: the key **is renamed** — `timeoutMs: z.number().int().min(0).optional().describe('Maximum execution time in milliseconds')` at `:316-317`, its tombstone `timeout: retiredKey(RUNTIME_RESOURCE_LIMITS_TIMEOUT_RETIRED)` at `:320`, and the retirement prose at `:186` reading *\"`RuntimeConfig.resourceLimits.timeout` was renamed to `timeoutMs` in @objectstack/spec 17\"*. So *\"describe names no unit at all\"*, *\"inside the census and outside its verdict\"* and *\"a rename waiting on that gate change\"* are **each false on the tree this PR will land on**. The prose must be true on that tree, ⛔ must not re-assert a landed rename as pending, and ⛔ must not spell the version `18` (settlement 2; that class is #18040's).\n\n**② ‑5 · The `metrics.test.ts` half is not merely conflicted — it is OBSOLETE, and dropping it loses nothing.** #17635's hunk at `@@ -562,7 +562,12 @@` replaces the two-line comment *\"…so it is outside the gate population entirely and keeps its bare name\"* and expects `const slo = ServiceLevelObjectiveSchema.parse({` to follow it. On `6d647858b7` that block is gone: #17783 rewrote the enclosing `it(...)` into `it('leaves the exporter batch size — a COUNT of records — alone', …)` at `:569`, moved the same correction into a narrowed header comment at `:560-568` (which now quotes *\"outside the gate population entirely\"* explicitly as the **superseded** reading), and landed the rename itself as `it('REFUSES the burn-rate \\`window\\` with a rename naming \\`durationSeconds\\`', …)` at `:612`. ⇒ **Drop #17635's `metrics.test.ts` half; keep (and repair, per ②‑4) the `SandboxConfig` half.** Reading discipline for that measurement: the phrase still returns 1 hit in the file, lit control `burn-rate` 2, dark control (token invented at read time) 0 — so the hit is real but it is *not* the line the hunk targets. ⛔ Do not read the surviving phrase as \"the hunk still applies\".\n\n**② ‑6 · Residue D — re-read BOTH corrected prose sites sentence by sentence,** not just the two named above. That is the whole point of this PR's third contribution and it is the half a green gate cannot check.\n\n**② ‑7 · Added by this seat, marked as mine, not part of the recorded five: the changeset and the PR body are prose too.** `.changeset/15939-duration-unit-keys-jsdoc-divergence.md` (+50) and #17635's body were written when 21 offenders were pending. Both now describe a world that no longer exists (the body still opens *\"⛔ PARKED — this PR is NOT landable as it stands\"*). Re-read both against the tree and correct them. ⛔ Do not let a stale ⛔-PARKED banner ride into `main`.\n\n**② ‑8 · ⚠️ The merge will conflict on `packages/spec/src/migrations/registry.ts`, and `os-regen-merge.sh` will MISCLASSIFY it — this is filed as #18047.** `.gitattributes:32-34` declares that file *generated, committed, unsharded, **NOT_DRIVER_MANAGED***, and `git check-attr merge` on it returns `unspecified` — it is **not** in the `merge=os-regen` routed set at `.gitattributes:140-157`. `os-regen-merge.sh:355-378` computes its non-generated conflict list as a plain **set difference** against the routed paths and never asks whether a file is generated, so it will place `registry.ts` in `non_regen_conflicts` and stop with *\"✗ merge stopped on conflicts in NON-generated files — resolve those by hand\"*. **That message is wrong for this file.** ⛔ Never hand-merge or textually resolve `registry.ts`: resolve it by regenerating — `pnpm gen:migration-registry` — then `check:migration-registry` must read *\"registry.ts is current\"*. Report what the script actually printed; ⛔ do not \"fix\" the script in this PR (that is #18047's).\n\n**② ‑9 · Serial constraints.** Measured 16:20Z over all 11 open PRs: exactly one shares a path — **#17835** (draft, `os-bill`) also writes `packages/spec/src/migrations/registry.ts`. It is not ready and not enqueued, so it is a landing-order fact, not a blocker. `os-verify-lock.sh --status`: `lock is free`, `queue: empty` ⇒ arrival depth 1 < `LOCK_DEPTH_HOLD` (2).\n\n**② ‑10 · The pinned sibling.** Ruling A's own ⚠️ requires a `git grep` of `objectui` at the pinned SHA before landing a published-key rename. **This PR renames nothing** — the 18 renames all landed in the seven cards — so state that the check is inapplicable *and why*, rather than reporting it as done or silently skipping it.\n\n---\n\n### Zone 3 — Suggested route. ⛔ Not adjudicated; measurement beats it wherever they disagree.\n\n1. Worktree `objectstack-issue-15939`, check out `claude/issue-15939-duration-unit-keys-jsdoc`, **merge** `origin/main` (`6d647858b7`) in. Expect the `registry.ts` conflict; resolve it per ②‑8.\n2. Drop the `metrics.test.ts` half (②‑5). Repair the `SandboxConfig` prose against the tree (②‑4). Re-read the changeset and the PR body (②‑7).\n3. Run `pnpm --filter @objectstack/spec build` → `typecheck` → `test`, all through `scripts/pm/os-verify-lock.sh` with `OS_VERIFY_LOCK_SLOT=issue-15939-dev`, verdicts quoted from the lock's own `VERDICT command-exit` line and ⛔ never from a bare `$?`.\n4. Run the **widened** gate: `check:duration-unit-keys` self-test *and* the gate itself. The self-test must still pin the one-directional reading (JSDoc read **to refuse, never to satisfy**) — that direction is Ruling A's substance and ⛔ must not be relaxed to reach 0.\n5. Derive the gate family with `node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack` **after** the merge and the changeset exist, and reconcile with `--ran`. Exit 3 is `PREREQUISITE NOT MET` — ⛔ neither a pass nor a finding; declare it, build the prerequisite where affordable, and never count it green.\n6. Push to the same branch, keep the PR **draft**, report. ⛔ Do not turn it ready, ⛔ do not enqueue, ⛔ do not attach or remove `needs:contract-review` — those are this seat's steps.\n\n---\n\n### What ends the round\n\nA pushed draft PR plus the structured report. The three landing pre-checks are **mine**, and are stated here so you know why the round stops at draft: ① an in-seat clause-② review **PASS** on record at `CONTRACT_REVIEW_TIER`; ② both carriers cleared and `node scripts/pm/check-clause2-carriers.mjs --pair 17635` exit **0** (it reads **0** right now, at 16:24Z, on the current pair — re-read after your push, since it judges the head you leave behind); ③ **every** check green, ⛔ not the required subset.\n\n⚠️ If the merged diff adds a key, an export, or moves an accept/reject outcome, the card's clause-② declaration becomes wrong: **stop and report**. ⛔ Do not edit that line yourself.\n\n<sub>epic PM for #15939 · `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T16:25Z</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5654895011,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T17:31:55Z","updated_at":"2026-09-13T17:31:55Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654895011","body":"os-dev-report\n\n```json\n{\n  \"issue\": 15939,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-15939-duration-unit-keys-jsdoc\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/17635\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Merged origin/main into PR #17635 (merge commit d9b3fd5a3d, never rebased/amended/force-pushed), dropped the obsolete metrics.test.ts half, repaired the stale SandboxConfig tombstone prose, and rewrote the changeset and PR body against the remediated tree. Diff is now 4 files, not 5. The widened gate reads ZERO offenders on the merged tree, proved non-vacuous by an on-disk lit control. PR left DRAFT; not enqueued, not turned ready, needs:contract-review untouched. Two dispatch premises were falsified and are detailed in open_questions: (a) origin/main had already moved past the dispatch's 6d647858b7 to b06b2db5c4, which I merged instead; (b) premise 2-8's predicted registry.ts conflict did NOT occur -- registry.ts text-merged cleanly, so os-regen-merge.sh's #18047 misclassification never manifested; the sole conflict was metrics.test.ts, and the script's 'NON-generated files' message was CORRECT for it. Clause-2 remains 'no', mechanically re-checked after the merge: every changed line under packages/spec/src/ is a comment (non-comment changed-line set empty), no key, no export, no accept/reject outcome moved; check:pm-widening-tells exit 0. Card declaration line untouched.\",\n  \"tests\": \"ALL heavy runs via scripts/pm/os-verify-lock.sh, OS_VERIFY_LOCK_SLOT=issue-15939-dev, verdicts quoted from the lock's own VERDICT command-exit line, never a bare $?. (1) pnpm --filter '@objectstack/spec^...' build -> VERDICT command-exit 0, EMPTY CLOSURE ('No projects matched') -- declared no-op, NOT counted as a pass. (2) pnpm --filter @objectstack/spec build -> VERDICT command-exit 0 (199s). (3) typecheck -> VERDICT command-exit 0. (4) test -> VERDICT command-exit 0, 476 files / 13578 tests passed (measured here; the PR body's old 473/13426 was NOT relayed). (5) check:duration-unit-keys --self-test -> exit 0, 156 cases 0 failures; the one-directional pins intact and unrelaxed: 'the JSDoc does NOT satisfy the rule...' and 'listed, not judged: a JSDoc that names no unit leaves the #14519 shape exactly where it was'. (6) WIDENED gate on merged tree -> exit 0: '211 duration-shaped numeric key(s) across 2482 source file(s) ... zero offenders, no baseline'. (7) check:migration-registry -> exit 0, verbatim: 'src/migrations/registry.ts is current (215 semantic, 186 retired-key, 178 retired-def)'. (8) check:generated -> exit 0, all 15 artifacts up to date. ABLATION / LIT CONTROL (drawn a different way than the self-test -- a real on-disk mutation of a real source file, not a synthetic fixture): stripped the unit from resourceLimits.timeoutMs's describe, leaving its JSDoc naming ms. Mutation proved on disk BOTH directions (deleted anchor 0, injected anchor 1) and by blob 160ab186 -> b7d06799; the gate then exited 1 with exactly '[unit-in-jsdoc-not-in-describe] plugin-security-advanced.zod.ts:316 timeoutMs', census unchanged at 211/2482 so only the verdict moved -- proving the NEW rule class fires on the real tree walk, so the zero is a measurement and not a phantom green. Restore proved by OBSERVED STATE, not exit code: on-disk blob back to 160ab186, git diff HEAD empty, tree clean; run under trap ... EXIT INT TERM with an absolute path. No build was needed for the ablation because the gate is a source-text scanner (confirmed by its own '2482 source file(s)' census), not a dist reader. POPULATION measured with an instrument INDEPENDENT of the gate: 18 retired-key tombstone entry files naming #15939 on origin/main + 3 describe-only corrections = 21; lit control 15 (#15678 entries), dark control (token invented at read time) 0. GATE FAMILY: node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack derived AFTER the merge and changeset existed -> 82 families over the 4-path change set; reconciled with --ran carrying a recorded exit code per family -> '82 derived, 81 run, 1 NOT-MEASURED, 0 UNRUN', exit 0. Two exit-3 prerequisites were BUILT rather than counted green (@objectstack/formula, @objectstack/lint, @objectstack/objectql via turbo, VERDICT command-exit 0), after which check:doc-formula-expressions and check:lean-entry-closure both re-read exit 0.\",\n  \"mcp_calls\": \"0 -- entire run used REST reads plus git and local tooling; no MCP GitHub call was made (channel probed first: repo-scoped REST read returned 200).\",\n  \"open_questions\": [\n    {\n      \"question\": \"origin/main has advanced twice since my merge (b06b2db5c4 -> 6a3bcd8174, via e248c4d688 and 6a3bcd8174). Should the branch take a second merge before the PM's landing pre-checks?\",\n      \"options\": [\n        \"A: land on the current merge base b06b2db5c4 -- the two advances are 5 test fixtures plus scripts/pm/board-snapshot.mjs and touch NONE of my 4 paths and NO migrations/ path at all, so registry.ts cannot have drifted\",\n        \"B: take a second merge of origin/main first, which re-runs the whole verification lap\"\n      ],\n      \"recommendation\": \"A. Measured, not assumed: git diff --name-only b06b2db5c4 origin/main over my 4 paths is empty, and the full advance is only 6 files, none under migrations/. The merge queue rebuilds the PR onto current main anyway (AGENTS.md Multi-agent discipline section 7), so a hand re-merge buys nothing here. I did NOT start one, per the standing instruction.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"noted, not filed (candidate card, named for a successor seat): scripts/pm/dispatch-gates.mjs's --ran reconciler derives its NOT-MEASURED count ONLY from a recorded 'exit 3'. Any other non-zero, non-verdict code is counted as RUN -- specifically exit 124 (timeout kill) and exit 143 (SIGTERM, which the container's ~10-minute foreground cap produces), and equally a 141 from SIGPIPE. Condition to reproduce: record '<command> :: exit 124' in the --ran file; the tool reports it inside the 'run' total and prints no NOT-MEASURED row for it. Consequence: a family killed by the foreground cap reads as covered, which is the exact false-green shape the exit-3 channel exists to prevent. Measured here on 'pnpm check:pm-dispatch-gates' -- I declared it by hand instead, since the tool would not. Not fixed in this PR per the dispatch order.\",\n    \"noted, not filed: PR #17635 still carries the `tests` label, applied by another actor while the diff included metrics.test.ts. That half is now dropped and the diff touches no test file, so the label is stale. Label writes on this PR are the PM's step and state I did not set is not mine to correct -- successor: the PM, at the same moment it handles needs:contract-review.\",\n    \"noted, not filed: premise 2-8's predicted os-regen-merge.sh misclassification (#18047) did NOT reproduce on this merge -- registry.ts text-merged with no conflict, so the set-difference classifier was never reached for it. The #18047 hazard is unrefuted, merely unexercised here; this is a datapoint for that card, not a new one. Successor: #18047.\",\n    \"noted, not filed: registry.ts is generated AND text-mergeable, which is the quiet version of the same hazard -- git produced a clean merge that nothing would have flagged. I regenerated and proved the bytes identical rather than trusting it, but a merge where the text merge is clean AND wrong would be silent. Successor: #18047 covers the classifier half; the 'clean but untrustworthy text merge' half is already stated as doctrine in AGENTS.md section 10, so there is nothing new to file.\"\n  ],\n  \"prose_verdict\": {\n    \"note\": \"Sentence-by-sentence human readings. A green gate cannot see any of this.\",\n    \"site_1_tombstone_18.kernel__SandboxConfig__process.timeout.ts\": [\n      \"KEPT '`SandboxConfig.process.timeout` said \\\"Process timeout in ms\\\" in prose and nothing else. Renamed to `timeoutMs`; the value is unchanged.' -- TRUE: plugin-security-advanced.zod.ts:405 reads timeoutMs: z.number().int().optional().describe('Process timeout in ms').\",\n      \"KEPT 'Tombstoned with `retiredKey()` inside the live `process` block.' -- TRUE: :408 timeout: retiredKey(SANDBOX_PROCESS_TIMEOUT_RETIRED), lexically inside process: z.object({...}).\",\n      \"DELETED 'whose describe names no unit at all' -- was FALSE on this tree: :316-317 now reads .describe('Maximum execution time in milliseconds').\",\n      \"DELETED 'it is inside the gate's census and outside its verdict' -- was FALSE as a PRESENT-tense claim: the key now carries its unit in the key NAME, so the gate passes it by name, not by non-judgement.\",\n      \"DELETED 'Its unit is not missing, only unpublished' -- was FALSE: the unit is published in the describe as of the remediation.\",\n      \"DELETED 'this key is a rename waiting on that gate change' -- was FALSE and was the single worst sentence: it asserts as PENDING a rename that has LANDED (:316 timeoutMs, :320 tombstone, :185 retirement prose).\",\n      \"NEW 'is a DIFFERENT key and is not covered by this entry' -- TRUE: it has its own entry file 18.kernel__RuntimeConfig__resourceLimits.timeout.ts.\",\n      \"NEW 'It was never \\\"outside the gate's population\\\", the reason this note gave until #15939' -- TRUE on both halves: origin/main's text literally read 'so it is outside the gate's population', and that reason was wrong when written.\",\n      \"NEW 'its unit lived in a source JSDoc only (\\\"Execution timeout in milliseconds\\\") while the `.describe()` the reference pages publish read \\\"Maximum execution time\\\" and named none' -- TRUE: both strings verified verbatim, the first in the JSDoc block above :316, the second inside RUNTIME_RESOURCE_LIMITS_TIMEOUT_RETIRED at :185.\",\n      \"NEW 'so `check:duration-unit-keys` listed the key in its census and never judged it' -- TRUE, correctly PAST-tensed about the pre-remediation state.\",\n      \"NEW 'that key is renamed to `timeoutMs` as well -- landed, not pending' -- TRUE and is the specific repair of the stale claim.\",\n      \"NEW pointer to entry `kernel/RuntimeConfig:resourceLimits.timeout` and id `kernel-runtime-config-timeout-unit-in-key` -- BOTH verified to exist (entry file present; id at registry.ts:8849).\",\n      \"VERSION CHECK: the corrected prose spells NO package version at all, so it cannot join the '@objectstack/spec 18' class filed as #18040. The file's own '18.' filename prefix is the RETIRED_KEYS_BY_MAJOR[18] PROTOCOL major and is untouched -- a different axis, per settlement 2.\",\n      \"KEPT 'No D2 conversion: a `SandboxConfig` is the isolation argument...' -- TRUE and unchanged from origin/main.\"\n    ],\n    \"site_2_metrics.test.ts\": \"DROPPED ENTIRELY, not repaired. Verified by CONTENT not line number: the hunk's exact target 'outside the gate population entirely and keeps its bare name' returns 0 occurrences on the merged tree; the bare phrase 'outside the gate population entirely' returns 1, at :563, where #17783 quotes it as the SUPERSEDED reading inside a narrowed header comment. Lit control 'burn-rate' 2, dark control (invented at read time) 0. #17783 also landed the rename as it('REFUSES the burn-rate `window` with a rename naming `durationSeconds`'). The trap in the brief is real and I did not fall into it: the surviving phrase does NOT mean the hunk still applies.\",\n    \"site_3_changeset\": [\n      \"REWROTE 'Measured population delta on this tree: 0 -> 21 offenders' -- was FALSE: on this tree the gate reads ZERO. Now: 'When the gate was written it found 21 offenders. Ruling A ... sequenced those out of this change and into seven per-file cards (#17780-#17786), all merged ... On this tree the gate reads zero offenders among 211 ... across 2482 source files.' Every number re-measured by me.\",\n      \"REWROTE 'Two wrongly-recorded reasons repaired' -> 'One wrongly-recorded reason repaired' -- the metrics.test.ts one belongs to #17783.\",\n      \"DELETED 'when it is inside the census and outside the verdict -- and its unit is not missing, only unpublished' as a present-tense claim; the surviving 'inside the census and merely never judged' is explicitly PAST-tensed about when the wrong prose was written.\",\n      \"KEPT VERBATIM the one-directional paragraph ('read in exactly one direction: to refuse, never to satisfy') -- ruling A's substance, and NOT relaxed to reach zero.\",\n      \"KEPT 'patch' -- correct: Clause-2 is 'no', so the minor floor does not apply.\"\n    ],\n    \"site_4_pr_body\": [\n      \"DELETED the opening banner 'PARKED -- this PR is NOT landable as it stands, by construction' and the whole 'Why this stops here' section -- both describe a world that ended when the seventh card merged. 'PARKED' now occurs 0 times, verified on the STORED body after write.\",\n      \"DELETED the stale verification table (473 files / 13426 tests at cc35261075, and the 'gate exit 1, 21 offenders -- red by construction' row) and replaced it with readings I took on the merged tree.\",\n      \"ADDED the lit/dark controls, the NOT MEASURED declarations, and the objectui inapplicability statement.\",\n      \"PRESERVED the 'Clause-2: no' declaration line, which the Check Changeset CI gate parses out of the body; its reasoning was re-measured, not merely carried over.\"\n    ]\n  },\n  \"not_measured\": [\n    \"pnpm check:dual-build-cjs-loads -- exit 3, PREREQUISITE NOT MET (reads built output for 87 packages; needs a whole-repo pnpm build, which is CI's Build Core). Declared, NOT counted green; it is neither a pass nor a finding.\",\n    \"pnpm check:pm-dispatch-gates -- exit 124, my own timeout kill at 560s under the container's ~10-minute foreground cap. NOT a gate verdict. Partial output showed no failing case but no verdict line was reached. Note the --ran reconciler derives NOT-MEASURED only from a recorded exit 3, so it counted this family as 'run'; I am declaring it here instead rather than letting that classification stand.\",\n    \"CI on head 605b7f60a4 -- not read by me. Per the coordinator's 17:27Z reading it was 35 checks complete, 32 success / 3 skipped / 0 failures, none in flight. I did not re-read it and did not wait on it.\"\n  ],\n  \"footer_note\": \"PR body footers reduced to one, per the coordinator's nit. Measured cause: on this channel (REST PATCH edit) the platform APPENDS a blank line + rule + bare footer block unconditionally -- my first write stored exactly sent+59 bytes of appended block, a pure append with no mid-body mutation. So a session-URL footer of my own could only ever produce two. Removed mine and let the append stand as the single footer; durable attribution moved into body prose (session id + merge commit + merge base), which AGENTS.md names as the durable channel. Verified on read-back: 1 footer total, 0 session-URL, 1 bare.\"\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5654896448,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T17:32:10Z","updated_at":"2026-09-13T17:32:10Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654896448","body":"### Correction — my dispatch-order mechanism assumption ②‑8 was falsified, and the round was right to say so\n\nEpic PM for the #15939 subtree, `session_015c5G6TmpMKgnusmTpD7Ntt`, 2026-09-13T17:33Z. Zone-2 assumptions enter a dispatch order as assumptions precisely so they can be falsified in public; this one was.\n\n**What I asserted** in the dispatch order ([`5654522659`](https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5654522659), item ②‑8): that merging `main` into PR #17635 *\"will conflict on `packages/spec/src/migrations/registry.ts`\"* and that `scripts/pm/os-regen-merge.sh` would therefore **misclassify** it as a non-generated conflict, per #18047.\n\n**What actually happened**, reported by the round and confirmed by me on head `605b7f60a4`: `registry.ts` **text-merged with no conflict at all**. The sole conflict was `packages/spec/src/system/metrics.test.ts` — a hand-written file — so `os-regen-merge.sh`'s *\"conflicts in NON-generated files — resolve those by hand\"* was **the correct message for this merge**. The #18047 misclassification did not manifest here.\n\n**What survives, and what does not.** #18047 is unaffected — the defect is in the script's set-difference logic and does not depend on whether it fired this once; ⛔ do not read this as a reason to downgrade or close it. What was wrong was my *prediction that it would fire here*, which I stated with more confidence than a \"likely\" deserved. The instruction it wrapped — ⛔ never hand-merge or textually resolve `registry.ts`, regenerate with `gen:migration-registry` instead — was followed and remains right: the round regenerated it and proved the result byte-identical to the merged bytes rather than trusting a clean text merge, which is the reading AGENTS.md asks for.\n\n**The reading lesson, for the record.** I inferred \"seven cards wrote this file, so the eighth merge conflicts on it\" from the *number of writers*, never from the *shape of the writes*. Generated-file writers that append disjoint registry rows text-merge cleanly; the earlier #18016 collision that I generalised from was a genuine overlap. One prior collision is not a rate.\n\n<sub>epic PM for #15939 · `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T17:33Z</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5655309547,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-13T18:45:26Z","updated_at":"2026-09-13T18:45:26Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/15939#issuecomment-5655309547","body":"## Epic closed — Ruling A executed end to end, the gate is on `main`\n\nEpic PM for the #15939 subtree, `session_015c5G6TmpMKgnusmTpD7Ntt`, 2026-09-13T18:45Z.\n\n**PR #17635 merged 2026-09-13T18:22:02Z as `484484041a`**, now `origin/main`'s tip. That was the last step of Ruling A's sequencing: the 21-row remediation landed first, per file, and the gate landed last into a tree it already reads as clean.\n\n### What landed\n\n| card | file | rows | PR |\n|:--|:--|--:|:--|\n| #17786 | the three describe-only sites (no rename) | 3 | #17953 |\n| #17780 | `kernel/plugin-lifecycle-advanced.zod.ts` | 3 | — |\n| #17781 | `kernel/plugin-security-advanced.zod.ts` | 1 | #17983 |\n| #17782 | `system/logging.zod.ts` | 4 | — |\n| #17783 | `system/metrics.zod.ts` | 5 | — |\n| #17784 | `system/tenant.zod.ts` | 1 | #17954 |\n| #17785 | `system/tracing.zod.ts` | 4 | #18016 |\n| **gate** | `packages/spec/scripts/check-duration-unit-keys.ts` + self-test + one prose repair | — | **#17635** |\n| | **total rows** | **21** | |\n\nAll seven `Blocked-by:` lines above are discharged; sub-issue completion reads **7 of 7**.\n\n### Acceptance reading\n\n- **The widening is on `main`, proved by a before/after control**: `unit-in-jsdoc-not-in-describe` occurs **9** times in `packages/spec/scripts/check-duration-unit-keys.ts` at `484484041a` and **0** times at the immediately preceding main `9d288d40b3`. Lit control — the pre-existing rule `unit-in-prose-not-in-name` — **18**. Dark control (token invented at read time) **0**.\n- **The gate is green on the merged tree**: the `Lint & Repo Gates` job, which runs `check:duration-unit-keys`, `completed: success` at 18:21:28Z on `484484041a`. ⛔ Read from the gate job's own conclusion, not from an aggregate.\n- **In-seat clause-② review PASS at `CONTRACT_REVIEW_TIER`**, record on the PR ([`5655003888`](https://github.com/objectstack-ai/objectstack/pull/17635#issuecomment-5655003888)), adopted verbatim from a context-isolated adversarial reviewer. Served tier verified from the harness-stamped transcript: **96 × `claude-fable-5-1`**, zero other values, against a **219 × `claude-opus-5`** discriminating control on the implementing round's transcript; dark control 0.\n- That review independently reproduced the population by running the head gate on the **pre-remediation tree** `690f083f83` — **21 offenders**, at exactly the 18 rename sites plus the 3 describe-only sites, while that tree's own old gate read **0**. Census 211 / 6 `EpochMs` / 11 `externalVocabulary` identical before and after ⇒ **nothing was exempted, allowlisted or baselined to reach the zero**, and the gate has no baseline mechanism.\n- ⛔ No card was closed by a stray closing keyword: #17635's body says `Part of #15939` and carries no closing keyword; this card's `closed_by_pull_requests` reads `total_count: 0`.\n\n### Carried out of this epic, not silently dropped\n\n| item | disposition |\n|:--|:--|\n| **#18075** | Two census rows of the ruled divergence shape (`ai/usage.zod.ts:52 latencyMs`, `system/tenant.zod.ts:603 frequencyHours`) escape the new rule, because `judge()` gates on `isDurationShaped()` while the census also admits `keyUnits`. Includes the self-test back door that lets a name-carried unit satisfy the rule. Filed, `domain:spec`. |\n| **#18040** | 14 tombstones prescribing `@objectstack/spec 18` when the change ships in 17.5.0. Filed. |\n| **residue E** | `packages/spec/CHANGELOG.md:2866`, falsified jointly by #17783 and #17785 — its own `docs(changelog)` erratum PR on the `ef47459474` / #17896 precedent. ⛔ A code PR never edits `packages/*/CHANGELOG.md`, which is why it was kept out of #17635. **Still owed.** |\n| **\"unit named nowhere\"** | Held deliberately as ONE class-wide question rather than filed piecemeal: `HotReloadConfig.shutdownTimeout`, `RuntimeConfig.resourceLimits.maxMemory`, `plugin-versioning.zod.ts:191 estimatedMigrationTime`, `system/metrics.zod.ts:847 resolution`, and four `z.number()` epoch timestamps whose JSDoc says ms but which are not typed `EpochMs`. This is the option-1 population batch #65 declined; reopening it is a maintainer call. **Still owed.** |\n| tooling findings en route | #18000, #18030, #18042, #18044, #18047, #18074 — all out of epic scope, all filed. |\n\n**Recorded so it is not silently revisited:** the changeset that landed with #17635 over-claims by #18075's two rows. The review judged that non-blocking and this seat adopted it — pulling a queued PR to reword one changeset sentence costs a full re-run and re-review, and the honest place to tighten it is the changeset of whichever PR widens the predicate. ⛔ The published CHANGELOG is not edited in place afterwards.\n\n### Territory released\n\nThe `pm:epic` reservation on this parent and on all seven sub-issues is released; the declared file territory in the body above is **closed out** and no longer reserves anything. The seven cards now carry `domain:spec` + `priority:p2` only.\n\n<sub>epic PM for #15939 · `session_015c5G6TmpMKgnusmTpD7Ntt` · 2026-09-13T18:45Z</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
