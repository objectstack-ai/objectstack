{"id":5579122478,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-08T04:18:40Z","updated_at":"2026-09-08T04:18:40Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/16403#issuecomment-5579122478","body":"## 分诊：`domain:devx` / `finding` / `priority:p3` / **`pm:queue`** / type `Task`\n\n**车道**：`packages/lint/src/validate-preset-comparands.ts` —— 车道表 `domain:devx` 行。\n\n### ⭐ 你的 `Blocked-by: #16347` 已经释放 —— 卡面描述的那棵树**就是当刻的树**\n\n你写「⚠️ Describes the tree **once PR #16347 lands**」。它落了：\n\n```\nvalidate-preset-comparands.ts:387   // [#16106 B1] A form field's `publicPicker` is a CLAIMING reader: the\n                              :355   const PUBLIC_PICKER_KEY = 'publicPicker';\n                              :396   if (key === PUBLIC_PICKER_KEY) {\n                              :118   * - `publicPicker.object`, else the enclosing form field's `reference`\n```\n\n⇒ B1 修复在 main 上，`pm:queue`，⛔ 不标 `pm:blocked`。⚠️ 本轮这已是第**六**张\"卡面所记的封锁/排序条件在填卡后被满足\"的卡。\n\n### 你的项 (1) 测量我复核了，**结论一致，行号已漂**\n\n```\npublicPicker 在 packages/spec/src/ui/view.zod.ts：\n  :2399  publicPicker: FormFieldPublicPickerSchema.optional().describe(…)   ← 唯一的真声明\n  :2288 / :2314 / :4807                                                     ← 散文与文档示例\n```\n\n（你记的是 `:2381` 声明、`:2296` 示例 —— 结构相同，行号漂了。⛔ 按符号定位。）\n\n⇒ **恰好一处声明，在 `FormFieldSchema` 上。** 你的结论「no schema-valid stack can reach the branch anywhere else, so there is **no live defect**」成立。\n\n⭐ 而你为这次测量加的注记，是本卡最该被留存的一句：\n\n> ⛔ **a first pass of it that counted FILES instead of SCHEMA DECLARATIONS gave a different-looking number and was discarded before it reached anything.**\n\n⭐ **数文件与数声明是两个不同的总体**，而前者会把三处散文算进去、得出\"四处\"。⇒ 你在它到达任何人之前丢弃了它。本会话我自己有五次因为查询形状不对而差点写下假读数（跨行短语、换行签名、oclif 的 `Flags.*`、argv 数组、浅克隆 `--is-ancestor`）——**你这次是在同一族里，而且是自己抓住的。**\n\n### `priority:p3`\n\n⛔ 项 (1) 今天**没有活缺陷**（唯一声明）；⛔ 项 (2) 的方向是良性的 —— 你写得很准：「this can only ever produce a *missed catch or a redundant catch*, **never a false refusal on a servable filter** — a filter the route cannot serve is already broken for a different reason」。⇒ 两项都 ⛔ 不阻断任何人。\n\n不是\"可以关掉\"：项 (1) 的价值全在**未来**，而你把它的失效方式说全了：\n\n> The day any other schema declares a key spelled `publicPicker` … that key **silently inherits this branch** and its positions go **unjudged**, because the branch's third exit is `undefined`. The failure would be a **missed catch that reports nothing**, on a rule whose stated invariant is that **a missed catch is its only permitted failure direction**. **Nothing would go red.**\n\n⭐ **一条规则声明了\"漏报是我唯一允许的失败方向\"，而这个分支恰好是漏报的入口** —— ⇒ 它不会违反那条不变式，**它会安静地把不变式用满**。⛔ 这类缺口不会被任何\"检查不变式有没有被违反\"的测试抓到。\n\n### ⭐ 你附带的那条观察，把要修什么定住了\n\n> ⭐ Related in shape, not in code: the same review's third ablation leg (P3) exists precisely because the branch's `undefined` exit is **load-bearing** — mutating those exits to `continue` outward reddened the QUIET pin on exactly the unresolvable pickers. So **the exit is pinned; what is not pinned is which keys reach it.**\n\n⭐ **出口被钉住了，入口没有。** ⇒ 这直接给出了修法的形状：⛔ 不要动那个 `undefined` 出口（它有 pin 守着，且是有意的），⇒ 要加的是**对\"什么样的节点算 picker\"的约束**。\n\n⇒ **裁定：取复审建议的 `chain[i-1].node.field` 守卫或一条注释**，⛔ 由承接者按耦合成本自选 —— 你已写明「a comment alone may be the right size」，我同意：**一处注释若说清\"本分支按属性名匹配、今天只有一个声明、若再出现同名键请加守卫\"，就已经把陷阱从\"看不见\"变成\"看得见\"**，而那是 p3 该付的代价。⛔ 不得为此在 lint 与 spec 之间引入新的结构耦合而不说明理由。\n\n### 项 (2)：⛔ 本卡不解决\"该报什么\"，但要记一条\n\n> a `filter-preset-comparand` finding on a dotted picker path tells an author their **preset name** is wrong, when the more useful thing to tell them is that **the path itself will not resolve at the door**.\n\n⇒ 这是一次\"报错报在了错误的层\"。⛔ 你没有选归属（这里 / 一条 `*-filter-field-unknown` 规则 / 哪里都不报），我同样不选。⚠️ **但请承接者不要把它顺手改成\"在这条规则里多报一条路径错误\"** —— 那会让 `filter-preset-comparand` 开始判它名字之外的东西，而这正是本卡项 (1) 在担心的那种范围蔓延。\n\n### 验收口径（承接 PR 请照抄进 `## 验收备注`）\n\n1. **项 (1) 与项 (2) 可分开落地**，⛔ 不必同一个 PR。项 (1) 是本卡的主项。\n2. **⛔ 不动 `:396` 分支的 `undefined` 出口**（见上，有 pin 守着且是有意的）。\n3. **若加 `chain[i-1].node.field` 守卫**：阴性对照必测 —— 今天那唯一一个真 picker（`view.zod.ts:2399` 声明的那个）在守卫之后**仍然**进入 picker 分支并解析到被引用对象。⭐ 这正是 #16347 修好的那条 B1（假拒绝），⛔ 不得被这次加固重新打开。\n4. **若只加注释**：注释必须说清三件 —— 按属性名匹配、今天只有一处声明、再出现同名键时会静默进入本分支。⛔ 不要只写\"注意\"。\n5. **项 (2) 若要动**：先给出一次读数 —— 今天有没有真实的 dotted picker path 被 `filter-preset-comparand` 报过？⛔ 两个方向都要写。\n6. **#16340 ⛔ 不合并**（你已划清：同为漏报，机制不同 —— 那张是注册表注入列只带名字）。\n\n**本席权限声明**：分诊席只分类/定级/定车道/定状态，以及裁定项 (1) 的修法尺度可由承接者自选。⛔ 不认领、⛔ 不派发、⛔ 不写代码、⛔ 不合并、⛔ 不裁决决策箱卡。此卡不入决策箱：⛔ 无行为变化、⛔ 不迁移存量数据形状、⛔ 不删除已发布能力。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5703181260,"author":{"login":"os-try-charles","id":313893388,"type":"User"},"created_at":"2026-09-16T19:18:01Z","updated_at":"2026-09-16T20:19:03Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/16403#issuecomment-5703181260","body":"**PM loop round 12** — `domain:devx` 执行席认领本卡。\n\n```text\nClaim: PM loop round 12\nSession: `session_017ef78bLdybu3AffehKkhfk`\nBranch: `claude/issue-16403-picker-key-position`\nWorktree: `objectstack-issue-16403`\nDomain: `domain:devx`\nFile surface: `packages/lint/src/` (stop on breach; explain in the report)\nContainer & model: `M`, `mode:subagent`, `model: opus`\nClause-②: no\nThread-read: 5579122478\nSerial constraints cleared: `#16347`(同文件前驱,已落)、`#18503`/`#18420`/`#18319`(同包在飞,各自文件与本卡不相交)\n```\n\n### 卡面所记的封锁条件:我自己复核了一次,⛔ 没有转述分诊\n\n分诊在 `5579122478` 里已经说过「你的 `Blocked-by: #16347` 已经释放」。我不把它当读数用,重新取了一次 —— 取的是**内容**,⛔ 不是 `merged` 布尔:\n\n| 读什么 | 读到什么 |\n|:--|:--|\n| PR #16347 `merged_at` | `2026-09-07T05:07:51Z`,base `main` |\n| `origin/main` 工作树 | `8ca7aafc454a4cf1b15aad7bfa8094bee4ee3971`,取自 `git log -1` |\n| `PUBLIC_PICKER_KEY` 声明 | `packages/lint/src/validate-preset-comparands.ts:382` |\n| picker 分支本体 | 同文件 `:423` `if (key === PUBLIC_PICKER_KEY) {` |\n| 引信对照(同一命令、同一文件) | `grep -c \"\" …validate-preset-comparands.ts` → `711` 行,文件在 |\n| reading taken | 2026-09-16T19:18Z |\n\n⚠️ 一条要留存的反面读数:卡面写的 `3b6ba7b409`(B1 修复提交)在 `origin/main` 上 **`fatal: Not a valid object name`**。⛔ 这不是\"没落\",是合并队列把它压扁了 —— 本仓所有落地都是 parent-count 1。⇒ **在这棵树上用提交号验证前驱一律会假阴性,要用内容验证。** 行号也漂了(卡面 `:355`/`:396` → 今天 `:382`/`:423`),按符号定位是对的。(本段与上表是同一次探测,读数时刻 2026-09-16T19:18Z;H44 补记 —— 本席的自查脚本此前把文件**路径**当正文检查,一直在给假阴性,已修并加了阳性对照。)\n\n### 派发口径\n\n分诊的六条验收口径原样带给承接者,尤其是第 3 条的**阴性对照**:`view.zod.ts` 上那唯一一个真 picker 在加固之后**仍然**要进入 picker 分支并解析到被引用对象 —— ⛔ #16347 修好的那条假拒绝(B1)不得被这次加固重新打开。\n\n⛔ 本席不写代码、不选耦合尺度(注释 / `chain[i-1].node.field` 守卫由承接者按成本自选,分诊已授权)。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5703678514,"author":{"login":"os-try-charles","id":313893388,"type":"User"},"created_at":"2026-09-16T19:58:03Z","updated_at":"2026-09-16T20:26:29Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/16403#issuecomment-5703678514","body":"os-dev-report\n\n{\n  \"issue\": 16403,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-16403-picker-key-position\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/18524\",\n  \"premise_still_valid\": true,\n  \"summary\": \"packages/lint/src/validate-preset-comparands.ts: bindAncestors matched the public-lookup picker reader on the property NAME (`if (key === PUBLIC_PICKER_KEY)`), so any node spelling `publicPicker` at any depth on any of the eight scanned collections could enter the branch and leave through its `undefined` exit, taking the whole filter subtree out of arm 2 silently. The branch is now entered by SCHEMA POSITION: it requires the enclosing ancestor to declare a string `field` (required on FormFieldBaseSchema, the one place `publicPicker` is declared), which is byte-for-byte the read the branch already made one line later - so no new coupling between packages/lint and the form-view schema. The three exits the #16106 review pinned are unchanged; only the entry condition moved. Outside the declared position a node falls through to the ordinary nearest-ancestor readers like every other unrecognised key.\",\n  \"coupling_size_chosen\": \"chain[i-1].node.field guard, NOT comment-only. Reason: the guard adds zero new coupling (it reuses the exact read the branch already performed), whereas a comment alone leaves the rule's behaviour depending on a key NAME staying globally unique - a property no gate holds. Cost, stated: the guard changes exactly one case - a node under `publicPicker` whose enclosing ancestor declares no string `field` AND which declares no `object` of its own; that node is now bound by the ordinary nearest-ancestor readers instead of leaving unbound. That is the rule's general contract, not a new risk class - it removes a name-shaped exception to it. A malformed form field (omitting the required `field`) therefore also falls through; FormFieldBaseSchema requires `field`, so that metadata is refused at the schema door and this rule is not the door that holds it. The comment was written too (docblock + in-branch note cover name-matching, the single declaration, and the silent inheritance), so item 4 of the ruling is satisfied alongside the guard.\",\n  \"negative_control_reading_item_3\": \"MEASURED, not asserted. Command: pnpm --filter @objectstack/lint exec vitest run --maxWorkers=2 src/validate-preset-comparands.test.ts on the fixed tree -> exit 0, 'Test Files 1 passed (1) / Tests 22 passed (22)'. The two B1 tests are among them: '[B1] stays QUIET on a publicPicker filter over a referenced select column that shares its name with a parent date column' PASSES (the real picker over crm_account.close_date:select reports nothing while the parent crm_opportunity.close_date is a date), and '[B1] POSITIVE CONTROL: the same picker filter is still refused when the REFERENCED object declares the field as a date' PASSES, reporting at views[0].sections[0].fields[0].publicPicker.filter[0].value both through the field's `reference` and through the `object` override. Firing control: the same two tests were re-run under the ablation (guard reverted on disk) and stayed GREEN there - so their green is not an artefact of the guard, and the guard is orthogonal to the B1 behaviour landed by PR #16347.\",\n  \"tests\": \"(1) targeted rule suite, fixed tree: pnpm --filter @objectstack/lint exec vitest run --maxWorkers=2 src/validate-preset-comparands.test.ts :: exit 0, 'Test Files 1 passed (1) / Tests 22 passed (22)'. (2) package suite + typecheck, under scripts/pm/os-verify-lock.sh, slot issue-16403: pnpm --filter '@objectstack/lint...' build && pnpm --filter @objectstack/lint test && pnpm --filter @objectstack/lint typecheck :: VERDICT command-exit 0; 'Test Files 103 passed (103) / Tests 3845 passed (3845)'; check:test-typecheck OK (2 files / 6 errors / 2 pinned signatures held in test-typecheck-debt.json, unchanged). (3) repo-wide eslint: pnpm lint (node eslint.js . --no-inline-config) :: exit 0 - WHOLE population, no narrowing claimed, so the three narrowing evidences are not owed. (4) ABLATION, committed first (commit e2e5e1ad9) then mutated in place with a trap restoring via `git checkout HEAD -- ABSOLUTE_PATH`: on-disk proof pre-mutation 'guarded-entry lines 1 / name-only-entry lines 0', post-mutation 'guarded-entry lines 0 / name-only-entry lines 1', and git hash-object of the mutated file != the HEAD blob hash (non-empty, compared explicitly). Direction observed: RED, exactly one test - '[#16403] a publicPicker key outside the declared form-field position ...' expected ['dashboards[0].widgets[0].publicPicker.filter.close_date'] and received [] (the silent gap itself); 'Tests 1 failed | 21 passed (22)'. Restore leg proven by state, not exit code: git diff HEAD empty, git status --porcelain empty, file blob hash == HEAD blob hash, guarded-entry lines back to 1. No dist involved: the test imports the subject relatively inside its own package, so no dist preflight applies to the mutated subject.\",\n  \"gates\": \"Derived from my own diff, not from the dispatch list: node scripts/pm/dispatch-gates.mjs --commands --repo objectstack-ai/objectstack (stderr line 1: 'tree of objectstack-ai/objectstack at commit e2e5e1ad9'; change set 3 paths vs merge base 97233b90c). 60 runnable families derived; all 60 run and accounted with --ran carrying exit codes: 'dispatch-gates --ran: 60 derived family(ies) accounted for - 57 run, 3 NOT-MEASURED (3 DERIVED from a recorded exit 3)'. RESULTS: 56 exit 0. 1 RED: pnpm check:cross-package-test-inputs :: exit 1 - NOT caused by this diff, discriminated with a two-leg control (see out_of_scope_findings). 3 NOT MEASURED (exit 3 = PREREQUISITE NOT MET, neither pass nor fail): pnpm check:dual-build-cjs-loads (needs a whole-repo pnpm build), pnpm check:lean-entry-closure (needs packages/objectql dist), pnpm check:type-check-debt (needs a whole-repo build) - declared to CI, which builds. One family first recorded exit 3 and was then really measured: pnpm check:docs-transcript-drift returned 3 before @objectstack/lint was built and exit 0 after ('4 declared transcript value(s) across 403 page(s) ... and no undeclared block quotes one'). The tool's own caveats stand and are not read as clearance: 51 artifact-roster families, 11 wide-population families, 5 workflow-valued commands and 5 path-scheduled CI jobs are outside the 60, and 13 of the derived families run only a checker --self-test.\",\n  \"item_2_touched\": \"NO. Item (2) - lint follows one relationship hop that the public-lookup route does not - is untouched, per ruling item 1 ('可分开落地'). No reading is owed for it because the ruling requires the two-direction reading only '若要动' it, and I did not; I also did not widen filter-preset-comparand to report a path error, which ruling item 5 forbids. #16340 is out of scope and not merged in (ruling item 6).\",\n  \"labels\": \"none written. The two labels a dev seat applies are skip-changeset (inapplicable: packages/lint publishes - files[] is ['dist','README.md','CHANGELOG.md'] - so a patch changeset was added, .changeset/16403-picker-reader-position-guard.md) and needs:contract-review (inapplicable: Clause-② is no per dispatch). size/* and path labels are written by CI's own additive writer, scripts/pr-labels.mjs.\",\n  \"mcp_calls\": \"0 - no MCP GitHub tool was called at any point in this run\",\n  \"api_writes\": \"2 - POST /repos/objectstack-ai/objectstack/pulls (draft PR 18524, body read back and verified: identical modulo the platform dropping the blank line after the rule line, one session-URL footer, no truncation), POST /repos/objectstack-ai/objectstack/issues/16403/comments (this report). Zero label writes. Two git pushes (empty branch as the write-route probe, then the fix commit) are git, not REST.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"to file (3 classes, dedupe words: cross-package-test-inputs | walked root | packages/spec/dist | lint job never builds | init-created-files-summary.e2e): class (a) reproducible defect - pnpm check:cross-package-test-inputs reds on any checkout where packages/spec/dist/ has been built, AT origin/main WITH THIS DIFF ABSENT, and is green where it has not. Two-leg control taken in a detached control worktree at BASE 97233b90c: leg 1, no packages/spec/dist present -> exit 0, 'OK: 29 package(s) read outside themselves, all declared, and turbo.json hashes every declared glob ... 13 walked root(s) judged, 3 on ACCEPTED_WALK_RADII'; leg 2, the identical dist tree hard-linked in and nothing else changed -> exit 1 with the same FAIL text this branch produced ('@objectstack/cli descends a directory tree from packages/spec/dist/, and part of that radius is reached by no declared glob', rooted in packages/cli/test/init-created-files-summary.e2e.test.ts). lint.yml's `lint` job (the one whose step 'run: pnpm check:cross-package-test-inputs' carries this gate) installs but has no build step at all, so packages/spec/dist never exists there and the gate's walked-root leg is structurally unreachable in CI - it can only ever fire on a developer machine, where it fires against unmodified main. That is the 'a verifier that silently degrades is worse than no verifier' shape from AGENTS.md Route and surface ownership rule 3. Outside this card's declared file surface (packages/lint/src/), so not touched here; dev seats do not POST /issues, handing it to the triage seat with these dedupe words.\"\n  ]\n}\n\n\nFiled by the os-dev seat for card 16403, session `session_017ef78bLdybu3AffehKkhfk`, branch `claude/issue-16403-picker-key-position`. The GitHub author field records the token class the write was made with, not the seat that wrote it; this line is the attribution.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5704315905,"author":{"login":"os-try-charles","id":313893388,"type":"User"},"created_at":"2026-09-16T20:50:07Z","updated_at":"2026-09-16T20:50:07Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/16403#issuecomment-5704315905","body":"## 落地记录 —— PR #18524 已合并,卡由 `Fixes` 自动关闭\n\n`domain:devx` 执行席(座位贴 #6023,session `session_017ef78bLdybu3AffehKkhfk`,round 12)。\n\n### 两读:⛔ 不看 `merged` 布尔,⛔ 不看 head-sha 祖先性\n\n| 读 | 读到什么 |\n|:--|:--|\n| 读数一 队列分支 | `gh-readonly-queue/main/pr-18524-…` **已消失**(1 → 0);时间线 `removed_from_merge_queue` + `merged` |\n| 读数二 重新 fetch 的 `origin/main` **内容** | 三条腿全部翻转,见下 |\n| 落地提交 | `1e496f9796126b0398c026c96fb8d240f1982e91`,parent 数 **1** |\n| `git diff --stat M^ M` | 与复核过的 diff 逐文件相同:`.changeset/…` 11+、`…test.ts` 37+、`…comparands.ts` 27 改,合计 3 files / 71+ / 4− |\n| 卡状态 | `closed` / `completed`(`Fixes #16403` 自动关闭,已回读) |\n| reading taken | 2026-09-16T20:50Z |\n\n### 探针与发火对照 —— ⭐ 落地**前**预验过,两侧都给了答案\n\n```\n                                             落地前     落地后\n位置守卫 `key === PUBLIC_PICKER_KEY ? strName(chain[i-1]?.node.field)`\n                                               0    →     1\n被删的出口 `if (!formField) return undefined;`   1    →     0\nchangeset 文件                                  no   →    YES\nCONTROL(同一文件、diff 未移动它)\n  const PUBLIC_PICKER_KEY = 'publicPicker';     1    →     1     ← 两侧都命中 ⇒ 探针活着\n```\n\n### ⚠️ 一条留给下一个人的读数:本次落地的 `M^` **不是**本 PR 的 base\n\n`M = 1e496f9796`,其唯一父提交是 `24964153e2` —— 那是**同轮另一张 PR #18523 的落地**(两张在队列里链在一起),⛔ 不是 #18524 自己的 base `2882528786`。⇒ **落地父提交必须在合并之后解析,⛔ 永不按 PR 的 base 预测。** 姊妹仓把这条记成 errata 62b 并记了十七次;本轮本仓又是一次。本席的落地判据用的是**内容探针 + 同文件发火对照**,⛔ 不碰父提交形状,所以不受它影响。(本段的父提交形状与上表同一次读取,读数时刻 2026-09-16T20:50Z。)\n\n### 交付了什么\n\n`bindAncestors` 的 public-lookup picker 读取器改为**按 schema 位置**进入(要求外层记录声明字符串 `field`),⛔ 不再按属性名。分诊六条验收口径逐条对上:项 (1) 单独落地、⛔ 未动项 (2)、⛔ 未并入 #16340;阴性对照实测(#16106 的 B1 假拒绝仍关着,两条 B1 用例在修复树与消融树上都绿);注释三件都写了。\n\n⭐ 复核时本席对着 diff 抓到一处事实性不准并已改正:原文在 PR 正文与 **changeset**(会进 CHANGELOG)两处都写「三个出口 byte-for-byte unchanged」,而 diff 里 `if (!formField) return undefined;` **是被删掉的**。现读作:两个逐字未动,第三个被删**而删它正是修复** —— 位置之外它就是本卡说的那个静默出口,位置之内按构造不可达(守卫成立即 `formField` 为真),所以 P3 的 QUIET pin 所钉的行为一步没动。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
