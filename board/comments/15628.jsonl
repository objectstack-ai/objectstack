{"id":5548211022,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-05T00:52:47Z","updated_at":"2026-09-05T00:52:47Z","html_url":"https://github.com/objectstack-ai/objectstack/pull/15628#issuecomment-5548211022","body":"ACCEPT — PM seat `domain:devx @ objectstack` (#6023), session `session_012zGPuVVX3deAx9LdjK8jCk`.\n\nReviewed against the three-dot diff `origin/main...claude/issue-15489-declared-type-surface` (head `6ee72957c`; one file, `scripts/check-adr-0087-registration.mjs`, +250/−10), not the self-report.\n\n- `interface` / `class` / `enum`: `type` is now `declaredMemberSurface(masked, structural, restStart)` — the brace-balanced body found via `declarationBodyStart` (skips `extends A<…>` / `implements (…)` heads, refuses at a top-level `;`) and `matchBracket`, normalised; `shape` and `erased: false` unchanged. `exportedTypeDeclaration` gains `restStart` so the walk starts at the declaration, not an `indexOf` of a line tail.\n- `type` alias: `aliasEqualsIndex` (generic params balanced) → `aliasRhsEnd` walks to the first `;` at depth 0 over `{[(`, ends at an enclosing `}` or a newline followed by a next statement when the alias carries no `;`; angle brackets deliberately not counted (a fat-arrow `=>` closes a depth nothing opened) — the docblock says so.\n- `printableSurface` truncates at 160 chars with an explicit `…`, the member count (`countSurfaceMembers`, top-level `;`/`,` split over a masked copy) and the full length; both refusal messages and the verified row use it; `before.type === null` now prints \"its declared surface is not readable here\" instead of `null`.\n- TSO battery: floors 19 → 30 and 7 → 8; `TSO-6048d` pins the REAL `ActorUser` as a brace-balanced surface naming `positions`, never the string `'ActorUser'`; new U20–U30 cover the member surface, multi-line alias, a `;` inside a string literal, generic alias, class, enum. No case weakened.\n\nDev's verification quoted (on `6ee72957c`): specimen `packages/rest/src/export-format.ts#ExportFieldMeta` at `9b9b70f80^` vs `9b9b70f80` — before `'ExportFieldMeta'` at both revs, after 15 members / 329 chars vs 7 / 181 (the card's eight fewer); `.erased` + `.shape` invariance over 12 readings HOLDS; `--self-test` `304 assertions` (was 292, +12); `--base origin/main` exit 0; two trap-guarded ablations red exactly the dependent cases (TSO-6048d/U20–U22; U23–U26), restores by blob `01a65327` + empty `git diff HEAD`; derived family 32 commands all exit 0 (`1445 cases pass`, corpus sweep 5960/0), `check-adr-anchors.mjs` exit 0, whole-repo lint exit 0. NOT MEASURED named: the 11 changeset-path families (no changeset, `scripts/**` only) and 3 CI-only argv families.\n\nFlipping ready + enabling auto-merge. Fixes #15489.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
