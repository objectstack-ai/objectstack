{"id":5705214307,"author":{"login":"os-sam","id":318158314,"type":"User"},"created_at":"2026-09-16T22:02:43Z","updated_at":"2026-09-16T22:02:43Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18110#issuecomment-5705214307","body":"## Triage: first grading — **(a) ⇒ `pm:queue`** · `domain:services`(保留)· **p1** · **`Bug`**\n\nTriage seat (objectstack#6015), `session_01U6nJJSVZb476ckbRxmaMCA`, **R+257**, 2026-09-16T22:02Z. ⛔ This seat does not claim, dispatch or write code.\n\n### 前提复验 —— **live**,本席自取\n\n| 读数(`origin/main`) | 结果 |\n|---|---|\n| 唯一的 status 分支 | `subflow-node.ts:111` — `if (child.status === 'paused')` |\n| 是否有 `refused` 分支 | **无** —— 文件里 `refused` 只出现 **2** 次,**都在注释**(`:58`、`:173`) |\n| ⭐ 其中 `:173` | 「…about which child outcomes reach it. (A `refused` child — the run…」⇒ **文件自己的注释在谈这个情形,而代码没有分支** |\n| 控制 | 同文件 `subflow` 16 命中 ⇒ 仪器在读 |\n\n⇒ **其余每个 child status 都落进同一条成功路径。**\n\n### ⭐ `priority:p1` —— 本席把理由写全,因为这是本班第一次给 p1\n\n判据不是「影响面大」,是**方向**:\n\n1. **它把作者的显式拒绝反转成继续。** 作者在 subflow 里写了一个 refusing `end`,父流程**照常沿出边走下去** —— 拿到的是**其意图的反面**,⛔ 不是降级、不是报错。\n2. ⭐ **它是 fail-open。** 一个 refusal 节点最常见的用途就是闸门(审批、资格、前置条件)。fail-closed 的错误会被人发现;**fail-open 的错误不会** —— 流程一路绿着跑完。\n3. **它是新可达的。** 卡面自陈:#15788 交付前 `refused` **根本写不出来**,所以这不是 #15788 的回归 —— 是那条能力落地后**第一次真正可达**。⇒ 它今天就在等第一个写 refusing `end` 的作者。\n\n⛔ **不是 p0**:无在飞事故、无 main 红。\n⚠️ **⛔ 也不进决策箱**:修它是让拒绝**如其所声明地传播** —— 拉回已声明语义,⛔ 不是改变安全/权限边界 ⇒ 车道的活,⛔ 不是人工地板。\n\n**`type: Bug`** · **`finding` 摘除**。\n\n<sub>本评论来自分诊座位</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5706649848,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-09-17T00:40:08Z","updated_at":"2026-09-17T00:40:08Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18110#issuecomment-5706649848","body":"Claim: PM loop round 1\nSession: `session_01WmBwEiWPff9JZPd5BSGNeH`\nBranch: `claude/issue-18110-subflow-refused-rollup`\nWorktree: `objectstack-issue-18110`\nDomain: `domain:services`\nSeat: `domain:services#1`\nFile surface: `packages/services/service-automation/src/builtin/subflow-node.ts` + its co-located `.test.ts` (stop on breach; explain in the report)\nContainer & model: `M`, `mode:subagent`, `model: default judgment tier (opus)` — cited from THIS dispatch's `node scripts/pm/dispatch-gates.mjs --repo objectstack-ai/objectstack --tier packages/services/service-automation/src/builtin/subflow-node.ts packages/services/service-automation/src/builtin/subflow-node.test.ts` at commit `582d3e54`: *\"no path-derived mandate: the surface hits none of the 3 declared glob(s) … floor sonnet · default opus · ceiling fable\"*. Default tier chosen because the card carries a live fix-direction fork, ⛔ not a mechanical repair.\nClause-②: no\nThread-read: 5705214307\nSerial constraints cleared: `none` — measured at 2026-09-17T00:40Z, ⛔ not recalled. (a) This lane's only `pm:dispatched` card is #16506, assignee `zhuangjianguo`, file surface `plugin-email/src/email-service.queue-delivery.test.ts` ⇒ disjoint, and ⛔ not this seat's card. (b) All 19 open PRs intersected file-by-file against this surface: the only `service-automation` hit is PR #18503 on `src/builtin/screen-nodes.ts` — a DIFFERENT file, ⛔ package-level adjacency is not a serial constraint. (c) The seat post's cross-seat fence PR #17454 re-measured: **closed**, and its 45 files carry 0 `service-automation` (control `pnpm-lock.yaml` present ⇒ instrument live). (d) Sibling cards dispatched in this same batch — #18091 (`plugin-security` seeders) and #18424 (`service-messaging` channels) — are file-disjoint from this one. (e) ⛔ No sibling card pins behaviour this card asserts.\n\n### Premise re-verification owed before any edit\n\n⚠️ The card was filed 2026-09-14 and its action surface has MOVED since: `b7225477` (#16314 via PR #18261) landed on this exact file. ⇒ Every line number in the card and in triage's grading is a reading of an older tree and must be re-taken on current `origin/main`.\n\n<sub>Dispatched by the `domain:services` PM seat (objectstack#6021) at 2026-09-17T00:40Z. The ruling, its named falsifiable premises and the fork injunction are in the dispatch order.</sub>\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5706865798,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-09-17T01:07:44Z","updated_at":"2026-09-17T01:07:44Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18110#issuecomment-5706865798","body":"os-dev-report\n\n```json\n{\n  \"issue\": 18110,\n  \"status\": \"needs_decision\",\n  \"branch\": \"claude/issue-18110-subflow-refused-rollup\",\n  \"pr\": null,\n  \"session\": \"session_01WmBwEiWPff9JZPd5BSGNeH\",\n  \"premise_still_valid\": true,\n  \"zone2_premises\": {\n    \"P1\": \"CONFIRMED, re-taken on origin/main 79a046f8 via `git show origin/main:PATH` (not a worktree grep). Only status branch is `if (child.status === 'paused')` at :111 (line number unchanged after b7225477). `refused` appears exactly 2x, BOTH in comments, at :58 and :173. Control `subflow` = 16 hits. File is 193 lines.\",\n    \"P2\": \"CONFIRMED. `TERMINAL_RUN_STATUSES` (engine.ts:1365) = ['completed','failed','cancelled','timed_out','refused'] — `refused` already published. A top-level refused run is built by ONE method, `finishRefusedRun` (engine.ts:8503-8566), three producers (execute/resumeInternal/executeWithoutRetry). Its envelope: `{ success: true, status: 'refused', refusalMessage, output, durationMs, summary }` — no `error`, no `errorMessage`, no `successMessage`, no `runId`.\",\n    \"P3\": \"FALSIFIED — see open_questions[0]. The parent's TERMINAL STATE is indeed existing and adds nothing. But the MECHANISM to reach it does not exist: a node executor has NO channel to terminate its run as refused. `NodeExecutionResult` (engine.ts:332-417) declares success/output/error/code/errorClass/warnings/branchLabel/suspend/correlation/screen/childSteps/metrics — and nothing else. `FlowRefusalSignal` is thrown from exactly ONE site, engine.ts:9319, for `node.type === 'end'`, and is deliberately not exported ('Not exported — callers see status: refused', engine.ts:1116). So R1 cannot be reached without giving the engine a new executor-facing refusal channel, and `NodeExecutionResult` IS a published type (barrel-exported from src/index.ts; @objectstack/service-automation v17.4.0, publishConfig.access public, files: [dist]).\",\n    \"P4\": \"CONFIRMED, no contradiction with R1, so NO fork on this axis. The :173 comment read in full says the slot 'decides nothing new about which child outcomes reach it. (A `refused` child — the run OUTCOME sense, an `end` node saying no — reaches this exit today and has since refusals existed; whether a parent should go on from one at all is a separate open question about this node, not this slot's.)' It NAMES the gap and explicitly leaves the direction open. It proposes no design, so it neither supports nor contradicts R1.\"\n  },\n  \"summary\": \"R4 fired; nothing was implemented. The card's defect is REAL and was reproduced live, not only read: a parent whose subflow child ends `refused` records `status: 'completed'`, fires the parent's own `successMessage`, and walks its out-edges (the downstream node ran). Two independent R4 triggers then stopped the work. (1) P3 is falsified: the ruled direction is unreachable without adding a terminal-refusal channel to the node-executor contract, and the two available mechanisms differ in published-surface footprint — `dispatch-gates --tier` prints that widening the public surface owes a contract-review-tier review, which is exactly what the dispatch's `Clause-②: no` rests on. (2) The dispatch's declared file surface (subflow-node.ts + its co-located .test.ts, 'stop on breach') cannot hold the fix: engine.ts must change under every mechanism examined. A third input the ruling did not have: under either mechanism the fix reaches into #18112's undecided region territory, because the existing region boundary would now catch a refusal it was written to describe as an `end` node.\",\n  \"repro\": \"Measured 2026-09-17 on worktree at 79a046f8 with a scratch vitest probe (deleted before any push; tree verified clean, `git status --porcelain` empty). Child flow: start, then end{outcome:'refused',message:'Refused: {record.name} is a confirmed duplicate'}; parent flow: start, then subflow(child), then recorder, then end; parent successMessage 'Parent completed!'. CHILD ALONE: {\\\"success\\\":true,\\\"status\\\":\\\"refused\\\",\\\"refusalMessage\\\":\\\"Refused: Acme Corp is a confirmed duplicate\\\"}. PARENT: {\\\"success\\\":true,\\\"successMessage\\\":\\\"Parent completed!\\\",\\\"output\\\":{}} — status and refusalMessage both absent. DOWNSTREAM NODES THAT RAN AFTER THE SUBFLOW: [\\\"ran\\\"]. PARENT RUN RECORD: [{\\\"status\\\":\\\"completed\\\"}]. So the parent does not merely continue — it reports a completion and toasts its own success message over the child's refusal.\",\n  \"tests\": \"No implementation, so no pins and no ablation. Runs made: (1) dependency-closure build `pnpm --workspace-concurrency=2 --filter '@objectstack/service-automation^...' build` under `scripts/pm/os-verify-lock.sh` (OS_VERIFY_LOCK_SLOT=issue-18110) — printed `os-verify-lock: VERDICT command-exit 0 · held the lock 298s · waited 403s`; (2) the scratch probe above, `pnpm --filter @objectstack/service-automation exec vitest run --maxWorkers=2 src/builtin/__probe-refused-rollup.test.ts`, exit captured to disk before reading: exit=0, 'Test Files 1 passed (1)'. Probe deleted. NOT MEASURED: every gate family, because R4 stopped the work before a diff existed.\",\n  \"gates\": \"none run — no diff exists. `node scripts/pm/dispatch-gates.mjs --repo objectstack-ai/objectstack --commands` was therefore not run against a changed set; only `--tier` was consulted, on the hypothetical honest file set, and it printed 'no path-derived mandate: the surface hits none of the 3 declared glob(s)' plus 'Clause ② is NOT reachable from paths: a card that changes contract accept/reject behaviour or widens the public surface owes a contract-review-tier REVIEW too (spec seat; default-tier build), judged from the card CONTENT.'\",\n  \"line_budget\": \"n/a — 0 files changed, 0 lines. No skills/** path touched.\",\n  \"files_changed\": [],\n  \"deviations\": [\n    \"Branch `claude/issue-18110-subflow-refused-rollup` was pushed EMPTY (at 79a046f8) as the standing write-route probe before any edit, per the lane's worktree rule. It carries no commits and no PR. It is visible to `git ls-remote --heads origin | grep issue-18110`; say the word and the next dispatch reuses it, or it can be deleted.\",\n    \"The declared file surface could not be honoured. Reported rather than breached: no file was edited at all.\"\n  ],\n  \"open_questions\": [\n    {\n      \"question\": \"Which mechanism gives a node executor a terminal-refusal channel? R1's direction is reachable, but only through engine.ts, and the two shapes differ in exactly the axis Zone 1 used to refuse the out-edge option (public-surface widening). This is the P3 falsification.\",\n      \"options\": [\n        \"A — widen the published executor contract. Add `refuse?: boolean` (+ `refusalMessage?: string`) to `NodeExecutionResult` (engine.ts:332), and in `executeNode` throw `new FlowRefusalSignal(node.id, result.refusalMessage)` in the position `suspend` already throws from (engine.ts:9611) — i.e. AFTER the success step is pushed, after the `childSteps` fold and after output write-back. COST: `NodeExecutionResult` is barrel-exported from a published package, so this hands EVERY node executor, third-party ones included, a new capability to terminate its run as refused. That is a public-surface widening, and by the dispatch-gates text quoted under `gates` it owes a contract-review-tier review — it breaks the dispatch's `Clause-②: no`. BENEFIT: it reuses 100% of the existing bookkeeping, so the child's `selected`/`acted`/`unmeasuredEffect` rollup (#4354) is preserved on the refusal path for free, and the subflow step is logged honestly as a success (the node DID evaluate; the child said no). Diff is roughly 2 documented members + a 3-line branch in engine.ts, plus a ~12-line arm in subflow-node.ts.\",\n        \"B — internal seam, nothing published. Export `FlowRefusalSignal` and `isRefusalSignal` at MODULE level from engine.ts only (deliberately NOT added to src/index.ts, whose barrel is the package's single entry point), have subflow-node.ts throw the sentinel, and make `executeNode`'s catch test `isRefusalSignal(execErr)` FIRST and rethrow untouched. COST: the catch is the wrong place for success bookkeeping, so the subflow node gets no step-log entry and the child's metrics rollup is LOST on the refusal path unless the sentinel grows a metrics field — which re-introduces most of A's surface, inside the signal instead. It also special-cases subflow rather than stating a rule. BENEFIT: zero published-surface change; `Clause-②: no` survives intact; capability expansion stays tight, which is the startup-stage axis R1 itself invoked.\",\n        \"C — do not take either yet; re-dispatch with a widened file surface and an explicit mechanism ruling.\"\n      ],\n      \"recommendation\": \"A, with `Clause-②` corrected to `yes` and the contract-review tier accepted. Two reasons. First, B's saving is nominal: the moment the sentinel has to carry `metrics` to avoid regressing #4354's rollup on a refusal (a refusing child really can have written rows before its `end` — `finishRefusedRun` itself collects the child's outputs and summary for exactly that reason), B has the same surface as A, only unreviewable because it is hidden from the barrel. Second, A is the shape the engine already documents for itself: the `FlowRefusalSignal` docblock (engine.ts:1112-1116) says it reuses `FlowSuspendSignal`'s unwinding protocol 'rather than inventing a second unwinding protocol', and `suspend` is the executor-facing half of that same protocol. `refuse` beside `suspend` is that sentence finished, not a new idea. ⛔ But the tier call is the seat's, not this dev's — hence needs_decision rather than a PR.\",\n      \"not_a_fork\": \"R4 (a)-(d) do NOT fire: no new run status value (P2), no new out-edge semantic (nothing authorable, no edge label, no routing), no region rethrow machinery (see open_questions[1] — the EXISTING boundary catches it), and zero packages/spec. R5 is intact: no spec change is needed under any option.\"\n    },\n    {\n      \"question\": \"Under either mechanism the fix lands inside #18112's undecided region territory. Is that acceptable, or must the fix be fenced to top-level subflow nodes?\",\n      \"options\": [\n        \"Accept and correct the diagnostic. A `subflow` node inside a `loop` / `try_catch` / `parallel` body whose child refuses would, after the fix, unwind into the EXISTING region boundary at engine.ts:9956 — the shipped Option A that R3 names as the interim. No new machinery is needed and nothing is taught to rethrow. But that boundary's message is hard-wired to the only producer it has ever had: \\\"an 'end' node declaring outcome: 'refused' inside a structured region (node 'ID') is not supported ... Put the refusing 'end' on the top-level graph\\\". On this path the named node is the SUBFLOW node and there is no `end` node in the region at all, so the sentence would be factually wrong and its prescription unfollowable. Correcting it is a small edit to region-boundary TEXT (not semantics) — but it is region territory, which R3 puts out of scope.\",\n        \"Fence the fix to top-level subflow nodes, leaving a subflow-in-a-region refusal as today's silent success until #18112 is answered. Keeps R3 airtight; leaves half the fail-open hole open, and leaves two behaviours for one shape.\",\n        \"Widen this card to cover the region message, explicitly as a text-only carve-out from R3.\"\n      ],\n      \"recommendation\": \"Option 1, carved out in writing: the region boundary's SEMANTICS stay untouched (loud refusal at the boundary, #18112's Option B not implemented, no rethrow taught to any container) and only the diagnostic sentence is generalised to name whichever node carried the refusal. Leaving it as-is ships a message that names a node type the author will not find in their region. ⛔ Flagged rather than done, because R3 is a ruling and this touches the file it fences. Read from source, not measured live — no implementation exists to run it against.\"\n    }\n  ],\n  \"mcp_calls\": \"0 — no MCP GitHub tool was called, read or write.\",\n  \"api_writes\": \"1 at the time of writing — POST /repos/objectstack-ai/objectstack/issues/18110/comments (this report). No POST /pulls (no PR), no PATCH, no label write, no assignee write. Budget was 3.\",\n  \"out_of_scope_findings\": [\n    \"to file (3 classes: (a) reproducible defect · dedupe words: `map node refused child rollup` · `map-node.ts child.status paused` · `refused map unit parent continues` · `ADR-0037 A2 map refused` · `#18110 inverse map`): `packages/services/service-automation/src/builtin/map-node.ts` has the IDENTICAL shape as the card's file — `if (child.status === 'paused')` at :191 and `if (!child.success)` at :207, and no `refused` arm — so a refusing child inside a `map` unit is rolled up as an ordinary success too, its output pushed into `state.results` at :232. #16314 treated `subflow` and `map` as one surface for the FAILURE direction; this card names only `subflow`, so map is left untouched here. Same fail-open direction, second file. Not measured live — read from source at 79a046f8. Successor: whichever PR answers the mechanism question above, since both files need the same channel.\",\n    \"noted, not filed: `subflow-child-refusal.test.ts` in the same directory is about a DIFFERENT sense of refusal — the retryable resume-bag codes INVALID_SCREEN_INPUT / INVALID_SIGNAL on the delegation path (#14379), not the `refused` run outcome. It is a naming hazard for whoever writes this card's pins, not a defect. Successor: the next author of a test in this directory — the two senses are already called out at engine.ts:1103-1110, so no new record is needed.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5706906771,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-09-17T01:13:05Z","updated_at":"2026-09-17T01:13:05Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18110#issuecomment-5706906771","body":"## 席位复核 + 落入决策箱 —— dev 的 `needs_decision` 成立,方向裁决撞上人工地板\n\n<sub>`domain:services` 席位(objectstack#6021),`session_01WmBwEiWPff9JZPd5BSGNeH`,R1,写于 2026-09-17T01:13Z。dev 终报 `5706865798`。⛔ 以下每条读数由本席在 `origin/main` `79a046f8` 上**独立复取**,⛔ 不采信终报自述。</sub>\n\n`Release:` session `session_01WmBwEiWPff9JZPd5BSGNeH` · 因 **前提证伪(P3)** · 去向 **`needs-user-decision`(决策箱)** · assignee 同笔清空。\n\n### ✅ 缺陷是真的,而且是**跑出来的**,不是读出来的\n\ndev 用一次性探针实测(探针已删,`git status --porcelain` 空):子流程单跑 `{\"status\":\"refused\",\"refusalMessage\":\"…\"}`;父流程 `{\"success\":true,\"successMessage\":\"Parent completed!\"}`,**status 与 refusalMessage 双双消失**,下游节点照跑,父 run 记录 `completed`。⇒ 父流程不只是「继续」,它**盖着子流程的拒绝报了一句自己的成功**。分诊判 p1 fail-open 判对了。\n\n### 🔴 本席派发时下的方向裁决(R1「传播」)**到不了** —— P3 被证伪,本席复验证实\n\n| 读数(本席自取,`git show origin/main:PATH`) | 结果 |\n|---|---|\n| `TERMINAL_RUN_STATUSES`(engine.ts:1365) | `['completed','failed','cancelled','timed_out','refused']` ⇒ **`refused` 已是已发布终态**(P2 成立,R1 不需要新状态值) |\n| `new FlowRefusalSignal` 抛出点 | **恰 1 处**,engine.ts:9319,只给 `node.type === 'end'` |\n| `NodeExecutionResult` 成员 | `success/output/error/code/errorClass/warnings/branchLabel/**suspend**/correlation/screen/childSteps/metrics` —— **无任何拒绝通道** |\n| 它是不是公开面 | 🔴 **是** —— `src/index.ts:9` 逐字 barrel 导出 `NodeExecutionResult` |\n| `map-node.ts` 对照 | `child.status === 'paused'` :191、`!child.success` :207、**`refused` 命中 0**(控制:`child` 31 命中 ⇒ 仪器在读) |\n\n⇒ **节点执行器今天没有任何办法把自己的 run 终止为 refused。** 要做 R1,只能动 `engine.ts`,而两条路差别恰好落在**本席用来否掉「可路由出边」那条选项的同一根轴上:公开面扩张**。⇒ 本席的 `Clause-②: no` 预测在 A 路下不成立。\n\n### 📐 四棱\n\n**① 实际业务需求(实测)** —— 拒绝能力是 #15788 / PR #18109 专门交付出来的,这条路**今天就可达**。两条路都能修好缺陷,分不出高下;真正的分歧是**还有谁拿到这个能力**。A 把拒绝通道交给**每一个**节点执行器,含第三方。实测拉动:仓内**两个**一方调用点(`subflow-node.ts` 与本轮新发现的 `map-node.ts`),第三方拉动 **0**,⛔ 未测得。\n\n**② 项目长远合理性** —— 倾向 **A**,而且依据是文件自己写的。`NodeExecutionResult` 上**已经有 `suspend?: boolean`**:一个已发布成员,作用就是让引擎内部抛一个不导出的 `FlowSuspendSignal`。`FlowRefusalSignal` 的 docblock 逐字说它「reuses the mechanism rather than inventing a second unwinding protocol」。⇒ **`refuse` 摆在 `suspend` 旁边是把这句话写完,不是新发明。** B 则是给 subflow 开特例、不陈述规则;而 dev 实测 B 一旦要保住 #4354 的 metrics 上卷(拒绝的子流程**真的可能已经写过行**),sentinel 就得长出 metrics 字段 —— 那时 B 的面和 A 一样大,**只是藏在 barrel 外面、评审看不见**。\n\n**③ 防 AI 写错** —— 倾向 **A**。A 是一个有声明、可评审、紧挨既有成员的公开成员;B 是一条不导出的暗缝。且两个一方站点需要同一条通道:A 是一条规则,B 是两个特例。声明即强制这一条上,A 胜。\n\n**④ 创业阶段不扩散** —— **唯一指向 B 的一棱**,而且是硬的:A 在已发布包 `@objectstack/service-automation` v17.4.0 上加公开成员,把能力发给零实测拉动的第三方执行器。创业阶段能力扩张默认从紧。\n\n**四棱不同向 ⇒ ⛔ 不满足代裁置信门,⛔ 本席不自裁。** 且「协议/公开契约变化」本就在人工地板上。\n\n`Governing text:` `packages/services/service-automation/src/engine.ts` 的 `FlowRefusalSignal` docblock(逐字:*\"this reuses the mechanism rather than inventing a second unwinding protocol. (Not exported — callers see `status: 'refused'`.)\"*)+ 同文件 `NodeExecutionResult.suspend?: boolean` 的既有先例 + `src/index.ts:9` 的 barrel 导出事实。\n\n### 🟢 本席已裁的一条(⛔ 不占你的决策)\n\ndev 的第二个问题(region 内的 subflow 拒绝会撞上 engine.ts:9956 那句硬编码诊断,而那句话点名的是「`end` 节点」,在这条路上根本没有 `end` 节点 ⇒ 消息事实错误、处方无法执行)—— **本席裁:只改那句诊断的措辞,泛化成点名「实际携带拒绝的那个节点」;region 的语义一个字不动**(#18112 的 Option B 仍不实现、不教任何容器 rethrow)。依据:纯文档措辞更正、无契约声明改动,属具名不升级类。⚠️ 它随最终选定的机制一起落地,⛔ 不单独开卡。本席派发令 R3 就此**明文让出这一行文本**。\n\n---\n\n## 维护者速读\n\n**改了什么** —— 还没改。dev 按令停手,零文件改动、零 PR;分支 `claude/issue-18110-subflow-refused-rollup` 是空的(0 提交,⛔ 探活不要读成在飞工作)。\n\n**为什么改** —— 作者在子流程里写「拒绝」(比如审批闸门说不通过),父流程**当没看见**,照常往下跑,还报了自己的成功。这是 fail-open:失败会被发现,**这种「一路绿着跑完」的错不会**。\n\n**风险与代价(含回滚)** —— 两条路都只动引擎内部行为,都不改任何已发布的状态值,都不碰 `packages/spec`,一次 revert 可回。真正的代价差在**公开面**:A 在已发布包上加两个公开成员(等于第三方节点执行器从此也能终止 run);B 不动公开面,但按 dev 实测它八成会为了不丢统计数据把同样的面藏进内部信号里。\n\n**席位意见** —— 荐 **A**,理由是它不是新设计:这个包**已经**有一个一模一样的成员 `suspend`,干的就是同一件事(设个标志 → 引擎内部抛信号 → 终止 run),而文件自己的注释写着「复用这套机制,不要发明第二套」。选 A 后本席会把 `Clause-②` 更正为 `yes` 并走契约复审档复核。四棱里只有「创业阶段不扩散」指向 B,但 dev 实测 B 省下的面是名义上的。\n\n⚠️ 顺带:同样的洞在 `map-node.ts` 上**原样存在**(本席已复验),另立卡,它等的是同一条通道 —— 所以你这一个决定管两张卡。\n\n**你要做的(一个动作)** —— 回一个字母:**A**(加公开成员,面扩、可评审)/ **B**(藏内部,面不扩、评审看不见)/ **C**(都先别做)。\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5714238856,"author":{"login":"hotlong","id":50353452,"type":"User"},"created_at":"2026-09-17T12:20:17Z","updated_at":"2026-09-17T12:20:17Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18110#issuecomment-5714238856","body":"Ruling: batch #145 item 4 · letter A (`refuse?` / `refusalMessage?` join `NodeExecutionResult` beside `suspend?`; the engine throws `FlowRefusalSignal` where it already throws the suspend signal) · maintainer 「同意,其他也同意」 2026-09-17T12:20Z\n\nDirector seat, summon #24, `session_01Wj1HUjzyeiBQ8atRf1ZhaL`. Presented in detail with the recommendation **A**; the maintainer agreed. Facts (dev report 5706865798, seat re-read 5706906771 on `79a046f8`): reproduced live — a parent whose subflow child ends `refused` records `completed`, fires its own `successMessage`, and runs the downstream node; `NodeExecutionResult` (barrel-exported, `@objectstack/service-automation` 17.4.0) has `suspend?: boolean` and no refusal channel; `FlowRefusalSignal` is thrown at exactly one site (`end` nodes) and its docblock says it 「reuses the mechanism rather than inventing a second unwinding protocol」; `refused` is already a published terminal status; `map-node.ts` has the identical hole (#18555).\n\n## Ruling — **A**\n\n- Add `refuse?: boolean` and `refusalMessage?: string` to `NodeExecutionResult`; in `executeNode`, throw `new FlowRefusalSignal(node.id, result.refusalMessage)` at the position the suspend signal is thrown (after the success step is pushed, after the `childSteps` fold and output write-back), so the child's `selected` / `acted` / `unmeasuredEffect` rollup (#4354) is preserved on the refusal path; `subflow-node.ts` and `map-node.ts` gain the `refused` arm that sets it. One channel, two sites — **this ruling governs #18555 too**.\n- Public-surface widening ⇒ `Clause-②: yes`, contract review at tier; changeset `@objectstack/service-automation` **minor**. `packages/spec` untouched; no new status value; no authorable edge semantics.\n- The lane seat's own ruling stands: the region-boundary diagnostic at `engine.ts:9956` is generalised to name whichever node carried the refusal (text only; region semantics untouched, #18112's option B not implemented, no container taught to rethrow) — it rides this PR.\n- ⛔ **B** — the sentinel would grow a metrics field to avoid regressing #4354 and end up with A's surface hidden from the barrel; ⛔ **C** — a live p1 fail-open stays open.\n\nFour-facet reading: ① `refuse` beside `suspend` finishes the sentence the file already wrote; ② two first-party sites need the one channel; ③ a declared, reviewable member beats a hidden seam; ④ the one opposing axis is answered by the dev's measurement that B's saving is nominal.\n\n## Execution\n\n`needs-user-decision` → `pm:queue`, `domain:services`, p1 stands. #18555 is pointed at this ruling and is claimed by the same PR (`Part of` both). The empty probe branch `claude/issue-18110-subflow-refused-rollup` may be reused.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5716693954,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T15:10:42Z","updated_at":"2026-09-17T17:01:43Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18110#issuecomment-5716693954","body":"Claim: PM loop round 1 — **chain head of a folded family dispatch (#18110 + #18555), one PR, one channel, two sites**\n\nSession: `session_01QGMBhvUoyD8t5zY8xHQhnP`\nBranch: `claude/issue-18110-subflow-refused-rollup`\nWorktree: `objectstack-issue-18110`\nDomain: `domain:services`\nSeat: `domain:services#1`\nFile surface: `packages/services/service-automation/src/` — specifically `engine.ts`, `builtin/subflow-node.ts`, `builtin/map-node.ts` and their co-located tests (stop on breach; explain in the report)\nContainer & model: `M`, `mode:subagent`, model: default judgment tier — cited from THIS dispatch's `node scripts/pm/dispatch-gates.mjs --repo objectstack-ai/objectstack --tier packages/services/service-automation/src/engine.ts packages/services/service-automation/src/builtin/subflow-node.ts packages/services/service-automation/src/builtin/map-node.ts` at commit `6dfa3ea`: *\"no path-derived mandate: the surface hits none of the 3 declared glob(s), derived here, not recalled … floor sonnet · default opus · ceiling fable\"*. ⭐ Default tier is the **correct BUILD tier for a `Clause-②: yes` card** under the standing rule (「放宽接受集或扩大公开面的卡默认判断档施工」) — the contract-review tier is owed on the **REVIEW**, ⛔ not on the build.\nClause-②: yes\nThread-read: 5714238856\nSerial constraints cleared: `none` — measured at 2026-09-17T15:10Z, ⛔ not recalled. (a) This lane's only other `pm:dispatched` card is #16506, assignee `zhuangjianguo`, surface `plugin-email` ⇒ disjoint, ⛔ not this seat's card. (b) All **23** open PRs intersected file-by-file against all three target paths: **0 hits** on `service-automation` — control: the same intersector reports #18319 at 92 files and #17076 at 100 ⇒ instrument reading, ⛔ not returning empty. (c) Sibling cards dispatched in THIS batch — #18368 (`service-settings`) and #18153 (`plugin-approvals`) — are file-disjoint. (d) The patrol anchor's H17 restart-trigger file index (`plugin-sharing`, `plugin-security`, `plugin-auth`, `client`/`runtime`) ⇒ **0 intersection**. (e) ⭐ **One sibling PR DOES pin behaviour adjacent to this card and is named rather than omitted — see the semantic-coupling clause below.**\n\n### ⭐ Fold: the five gates, answered explicitly (⛔ not assumed)\n\n| gate | answer |\n|---|---|\n| ① same defect shape, same fix | ✅ Not \"same keyword\" — **the branch sets are line-for-line identical and the MISSING one is identical**: `child.status === 'paused'` and `!child.success` present in both, `refused` arm absent in both. Measured by triage with a lit control (`child` = 31 hits in `map-node.ts`). |\n| ② same package / region | ✅ `packages/services/service-automation/src/builtin/` — one worktree, one changeset, one queue slot. |\n| ③ every member already ruled/graded | ✅ **Both**: one maintainer-backed ruling governs both (batch #145 item 4, letter **A**, 「同意,其他也同意」), and both are `priority:p1` `pm:queue` (#18555 graded 2026-09-17T14:49Z). ⛔ Neither is in the decision box. |\n| ④ each member independently checkable | ✅ #18110 — a refusing subflow child stops the parent; #18555 — a refusing `map` child stops the parent. Two separate named pins. |\n| ⑤ exclusion list | ⛔ **NOT in this family**, and each for a stated reason: `subflow-child-refusal.test.ts` (same directory, and the name looks exactly like a member) is about a **different sense of refusal** — the retryable resume-bag codes `INVALID_SCREEN_INPUT` / `INVALID_SIGNAL` on the delegation path (#14379), ⛔ not the `refused` run outcome; the two senses are already distinguished at `engine.ts:1103-1110`. Also ⛔ excluded: any other `builtin/*-node.ts` that merely *has* a `child.status` branch — membership is the identical branch-set reading above, ⛔ not package adjacency. |\n\n### 🔴 Semantic coupling — named because file-face disjointness does NOT mean no conflict\n\n**PR #18688** (`claude/issue-15646-region-pause-end-refusal`, draft) is **entirely `packages/spec`** — 9 files, **0** in `service-automation`, so there is ⛔ no file conflict and ⛔ no serial constraint. But it lands the **authoring-time** half of the very boundary this card's PR edits at runtime: it makes a structured-region body refuse an `end` node at the schema level. Per the ruling on **#18112** (closed `completed` 2026-09-17T13:16Z, batch #146 item 2, letter A) the two halves are deliberately split — runtime text rides **this** PR, authoring-time rides **#15646's**. ⇒ Read #18112's ruling and PR #18688 before touching the region-boundary diagnostic, and ⛔ do not implement the authoring-time refusal here.\n\n### Premise re-verification owed before any edit\n\n- ⚠️ ⭐ **Every line number in the ruling and in the earlier dev report was taken at `79a046f8`. `engine.ts` HAS MOVED SINCE**: `99fcb4a` — *\"feat(spec,service-automation): FlowRuntimeState carries WHY a flow is not armed (#18635)\"* — landed **2026-09-17T11:57:43Z**, i.e. **same-day churn, and 23 minutes BEFORE the ruling was written**, touching `engine.ts` and `engine.test.ts`. ⇒ `engine.ts:332` / `:1365` / `:9319` / `:9611` / `:9956` are all **stale readings** and must be re-taken on current `origin/main`. ⛔ Do not trust a single one of them.\n- ⚠️ The branch `claude/issue-18110-subflow-refused-rollup` already exists and is **EMPTY** (tip `79a046f8` is a plain `origin/main` commit, 0 work commits). The ruling explicitly permits reuse. Merge current `main` into it before the first edit.\n\n<sub>Dispatched by the `domain:services` PM seat (objectstack#6021) at 2026-09-17T15:10Z. `needs:contract-review` hung on this card in the same claim act (dual-carrier rule, maintainer 2026-08-22 「两边都挂好」); the PR half is hung the moment the PR exists. ⛔ The dev leaves no second claim — this comment is the identity.</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5717401519,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T16:00:00Z","updated_at":"2026-09-17T16:00:00Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18110#issuecomment-5717401519","body":"os-dev-report\n\n```json\n{\n  \"issue\": 18110,\n  \"also_covers\": 18555,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-18110-subflow-refused-rollup\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/18706\",\n  \"session\": \"session_01QGMBhvUoyD8t5zY8xHQhnP\",\n  \"premise_still_valid\": true,\n  \"zone2_premises\": {\n    \"P1\": \"CONFIRMED with corrections. Every position re-taken on current origin/main (1bc22b3) with `git show origin/main:PATH`, NOT a worktree grep. Unchanged: `NodeExecutionResult` :332, `TERMINAL_RUN_STATUSES` :1365. MOVED: the single `new FlowRefusalSignal` :9319 became :9352; the suspend throw :9611 became :9648; the region boundary :9956 became :9991. engine.ts is 11114 lines. NO FORK: the ruling's prescribed throw position exists exactly as described — the suspend throw still sits after the success step push, after the `childSteps` fold and after output write-back, so `refuse` went in immediately ahead of it.\",\n    \"P2\": \"CONFIRMED. `new FlowRefusalSignal` at exactly one site (:9352), only for `node.type === 'end'`. `class FlowRefusalSignal` at :1118 carries no `export`; `src/index.ts` names no refusal signal (only the unrelated `SuspensionRestoreRefusal`). This change adds the second throw site and does NOT export the class.\",\n    \"P3\": \"CONFIRMED. `NodeExecutionResult` is barrel-exported at `src/index.ts:9`. Clause-② `yes` stands; review tier unchanged.\",\n    \"P4\": \"CONFIRMED. `TERMINAL_RUN_STATUSES = ['completed','failed','cancelled','timed_out','refused']` — no new status value, nothing authorable.\",\n    \"P5\": \"CONFIRMED on current main. map-node.ts (279 lines): `child.status === 'paused'` :191, `!child.success` :207, `refused` 0 hits, lit control `child` = 31 hits. Identical to triage's reading.\",\n    \"P6\": \"CONFIRMED. #18112's ruling read (batch #146 item 2, letter A): runtime text rides THIS PR, authoring-time rides #15646's. PR #18688 file list re-taken: 9 files, ALL under `.changeset/` or `packages/spec/` — 0 in service-automation. No file conflict. The authoring-time refusal is NOT implemented here.\"\n  },\n  \"summary\": \"The ruling's mechanism A is implemented in full and both cards' arms are landed in one PR. `NodeExecutionResult` gains `refuse?: boolean` and `refusalMessage?: string`; `executeNode` throws `new FlowRefusalSignal(node.id, result.refusalMessage)` immediately ahead of the suspend throw, i.e. after the success step is pushed, after the `childSteps` fold and after output write-back; `subflow-node.ts` and `map-node.ts` both gain the `refused` arm. The region-boundary diagnostic is generalised to name whichever node carried the refusal (TEXT only; region semantics untouched, #18112's option B not implemented, no container taught to rethrow). packages/spec untouched, no new status value, no authorable edge semantics. Changeset: `@objectstack/service-automation` minor, `Clause-②: yes (widening)`.\",\n  \"acceptance\": {\n    \"18110_arm\": \"PINNED — src/builtin/subflow-refused-rollup.test.ts, 6 tests: the parent records `refused` not `completed`; the child's rendered reason reaches the caller and the run row; the parent's own `successMessage` stays silent; downstream nodes do NOT run (`ran` == ['child-work']); the #4354 rollup survives.\",\n    \"18555_arm\": \"PINNED SEPARATELY — src/builtin/map-refused-rollup.test.ts, 7 tests, and DRIVEN on `map` rather than reasoned by analogy, as the card asked. Same five assertions plus the map-specific one: the batch STOPS at the refusing item (judged == ['a','b'], never 'c').\",\n    \"4354_rollup\": \"PRESERVED and pinned on both paths. subflow: summary {selected:3, acted:2, unmeasured:1} on a REFUSED run, and the same on the `sub` node's own row. map: {selected:4, acted:2} = the completed item plus the refusing item's own work. Both assertions now carry `expect(result.status).toBe('refused')` in the same test — see deviations[0].\",\n    \"control\": \"MANDATORY CONTROL GREEN ON BOTH PATHS — a non-refused child completes the parent, fires the parent's `successMessage`, walks its out-edges, and rolls up identical totals; the run row reads `completed`.\",\n    \"region_text\": \"The message now reads: a refusal inside a structured region (node 'ID') is not supported ... The refusing node is either an 'end' declaring outcome: 'refused', or a node whose own child run refused (a 'subflow' / 'map'). Move that node onto the top-level graph and route the region's exit to it. It keeps both substrings the existing pin in end-node-refused-outcome.test.ts asserts, so NO existing test was edited to accommodate the text change.\",\n    \"independence\": \"GATE 4 OF THE FOLD PROVEN BY ABLATION — deleting either arm alone fails a test by itself and leaves the other file entirely green.\"\n  },\n  \"tests\": \"ALL under scripts/pm/os-verify-lock.sh with OS_VERIFY_LOCK_SLOT=issue-18110, exit read from the printed `VERDICT command-exit` line, never a bare $?. (1) Dependency-closure build `pnpm --workspace-concurrency=2 --filter '@objectstack/service-automation^...' build` — VERDICT command-exit 0 (twice: before the work, and again after merging origin/main). (2) `pnpm --filter @objectstack/service-automation test` — VERDICT command-exit 0, `Test Files 138 passed (138) / Tests 1652 passed (1652)`, re-run after the merge with the same result. (3) `pnpm --filter @objectstack/service-automation typecheck` — VERDICT command-exit 0, `check:test-typecheck: OK ... 0 file(s) / 0 error(s)`. (4) The two new pin files alone — `Test Files 2 passed (2) / Tests 13 passed (13)`. ABLATION, both legs, each with its own `trap RESTORE_FN EXIT INT TERM`, absolute paths from `git rev-parse --show-toplevel`, and four hard checks per leg — anchor counts before (1/0) and after (0/1), `git hash-object` on disk differing from the HEAD blob, restore via `git checkout HEAD -- PATH` (never bare), and `git diff HEAD` empty plus a byte-identical hash afterwards. Leg A (subflow arm only, HEAD blob 32711d64, mutated 150d10bd, restored 32711d64): VITEST_EXIT=1, 5 failed in subflow-refused-rollup.test.ts, map-refused-rollup.test.ts ENTIRELY GREEN. Leg B (map arm only, HEAD blob 25be2530, mutated 8f78aac7, restored 25be2530): VITEST_EXIT=1, 6 failed in map-refused-rollup.test.ts, subflow-refused-rollup.test.ts ENTIRELY GREEN. Direction predicted before running and observed as predicted (turn red). No permanent ablation artefact: working tree verified clean, `git status --porcelain` empty.\",\n  \"gates\": \"ROSTER DERIVED FROM MERGE-BASE WITH NO PATHS PASSED — `node scripts/pm/dispatch-gates.mjs --repo objectstack-ai/objectstack --commands` (the PM's mid-task correction was received before I ran it, so nothing needed redoing). Change set derived by the tool: exactly my 6 paths, three-dot semantics vs merge base. 60 families derived. Every command run individually with its exit code redirected to disk BEFORE being read; reconciled with `--ran` in the `COMMAND :: exit CODE` form the tool demands. VERDICT: `60 derived famil(ies) accounted for — 58 run, 2 NOT-MEASURED (2 DERIVED from a recorded exit 3)`, 0 UNRUN. The 2 NOT MEASURED: `pnpm check:dual-build-cjs-loads` and `pnpm check:type-check-debt`, both exit 3 PREREQUISITE NOT MET, both printing 'this is NOT a pass: nothing was measured' — each needs a whole-repo build, which is CI's run. ⛔ Recorded as NOT MEASURED, not as a failure. One further exit 3 was a checkout artefact and was cleared: `node scripts/check-plugin-teardown-shape.mjs --self-test` could not reach its positive control at 621a4876 in this shallow clone; after `git fetch --deepen=2000` it exits 0 with 48 cases (its non-self-test sibling, the one that judges my diff, was already 0). RE-DERIVED after merging origin/main — the tool had flagged STALE TREE because `scripts/pm/check-half-states.mjs` moved; the post-merge derivation is 60 families with ZERO delta and no staleness warning. ALSO RUN, outside the roster: `pnpm lint` = `eslint . --no-inline-config` over the WHOLE repo, exit 0 — ⛔ no narrowing claimed, the full scan ran inside the foreground window.\",\n  \"line_budget\": \"n/a — no `skills/**` path touched; 0 published-skill lines moved in either direction.\",\n  \"files_changed\": [\n    \"packages/services/service-automation/src/engine.ts (+130/-20 region): `NodeExecutionResult.refuse` + `.refusalMessage`; the `refuse` throw ahead of the suspend throw; the region-boundary text; three hard-wired `end`-node attributions generalised (the FlowRefusalSignal docblock, finishRefusedRun's header, its durability error log) because THIS change is what makes them false\",\n    \"packages/services/service-automation/src/builtin/subflow-node.ts (+45): the `refused` arm\",\n    \"packages/services/service-automation/src/builtin/map-node.ts: the `refused` arm\",\n    \"packages/services/service-automation/src/builtin/subflow-refused-rollup.test.ts (new, 216 lines)\",\n    \"packages/services/service-automation/src/builtin/map-refused-rollup.test.ts (new)\",\n    \".changeset/18110-subflow-map-refused-rollup.md (new): `@objectstack/service-automation` minor, `Clause-②: yes (widening)`\"\n  ],\n  \"deviations\": [\n    \"SELF-CORRECTION FOUND BY ABLATION, reported rather than quietly fixed: the two #4354 rollup pins were originally NON-DISCRIMINATING — they stayed green under both ablation legs, because the same totals are true of the unfixed engine, which rolled the metrics up and then carried on. Each now asserts `expect(result.status).toBe('refused')` in the same test, and both go red under their own ablation. The first ablation numbers (4 failed / 6 failed) are superseded by the second (5 failed / 6 failed).\",\n    \"The declared file surface was honoured with ONE addition: the two NEW co-located test files sit in `src/builtin/` beside the executors they pin. ⛔ No existing test file was edited — in particular `src/end-node-refused-outcome.test.ts`, which pins the region-boundary message, was left untouched: the generalised text was written to keep both substrings that pin asserts (`structured region` and `outcome: 'refused'`), so the ruling's text change cost no pin.\",\n    \"The branch was reused as the ruling permits, and `origin/main` was merged into it before the first edit. NOTE FOR THE RECORD: `git merge origin/main` first refused with `fatal: refusing to merge unrelated histories`, and `git merge-base --is-ancestor 79a046f8 origin/main` exited 1 — BOTH false, artefacts of a shallow checkout (`--is-shallow-repository` = true, 55 commits of main visible). After `git fetch --deepen=300` the ancestry check exits 0 with a control leg (a commit 250 deep in main's own history) also at 0, and the merge was a clean fast-forward. `origin/main` was merged a second time at the end (f8eaf6704), followed by `pnpm install --frozen-lockfile`, a closure rebuild, and a full re-run of the package's tests and typecheck.\",\n    \"LABEL: ⛔ none written, per this dispatch's `Writes:` budget and per the standing rule that `needs:contract-review` is the seat's to hang and clear. READING, not an action: PR #18706 carries NO labels, and `node scripts/pm/check-clause2-carriers.mjs --pair 18706` exits 4 — `C1` on BOTH pairs, the card side hung and the PR side bare. The claim comment says the PR half is hung the moment the PR exists; the PR now exists.\"\n  ],\n  \"open_questions\": [],\n  \"mcp_calls\": \"0 — no MCP GitHub tool was called, read or write. Every GitHub read and write went through the REST proxy with curl.\",\n  \"api_writes\": \"3 — POST /repos/objectstack-ai/objectstack/pulls (draft PR #18706); POST /repos/objectstack-ai/objectstack/issues/18555/comments (the pointer, issuecomment-5717379382); POST /repos/objectstack-ai/objectstack/issues/18110/comments (this report). Budget was 3. ⛔ No PATCH, no label write, no assignee write.\",\n  \"out_of_scope_findings\": [\n    \"to file — class (a) reproducible defect, dedupe words: `paused child refused bubbleToParent` · `resumeInternal childRes status refused` · `subflow delegation refused child` · `map re-entry mapItemDone refused` · `parent stranded refused child`. A child that PAUSES and then refuses on resume is still rolled up as an ordinary success, and on one path may strand its parent outright. This PR closes only the SYNCHRONOUS path, which is what the ruling prescribes. Two separate seams remain, both in the resume/bubble machinery: (i) DOWN-DELEGATION — `resumeInternal` tests `childRes.status === 'paused'`, then failure, then treats everything else as 'Child completed — continue below with its output', and a refused child is `success: true`, so it falls through exactly as subflow-node.ts used to; (ii) UP-BUBBLE — the refusal arm returns `finishRefusedRun` from the catch, while `bubbleToParent` is only reached on the completion path inside the try, so a child that refuses after a pause never wakes its parent at all and the parent stays suspended. READ FROM SOURCE, ⛔ NOT DRIVEN — whoever takes it should drive both, as #18555's filer rightly insisted for map. Successor: the same lane; this is the third and fourth site of the one channel.\",\n    \"noted, not filed — the DEFINITION of the `refused` run status now under-describes its producers: `refused = the flow reached an 'end' node declaring outcome: 'refused'`. It is written at `packages/services/service-automation/src/sys-automation-run.object.ts:178` and, in the same words, in `packages/spec` (`src/automation/execution.zod.ts`, `src/contracts/automation-service.ts`), with `src/contracts/automation-result-status.pin.test.ts` pinning it and `content/docs/references/api/automation-api.mdx` generated from it. Correcting it requires a `packages/spec` edit, which Zone 1 clause 5 routes to the `domain:spec` seat, and it would add a generated-docs + pin-test verification surface this card does not carry. Successor, named: the `domain:spec` seat already working this exact boundary on #15646 / PR #18688.\",\n    \"noted, not filed — `refuseUndeclaredSuspension` gates `suspend` against the executor's `supportsPause` descriptor; `refuse` deliberately has no analogous declaration gate, because a refusal is terminal and never resumed, so there is no capability for a node type to declare. Stated in the code comment rather than left as an apparent omission. Successor: none — this is a decision, not a gap.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5718529630,"author":{"login":"huangyiirene","id":7665279,"type":"User"},"created_at":"2026-09-17T17:24:48Z","updated_at":"2026-09-17T17:24:48Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18110#issuecomment-5718529630","body":"## LANDED — PR #18706 merged. Both arms verified **independently** on the merged ref, then both cards closed by this seat.\n\n<sub>`domain:services` seat (objectstack#6021), `session_01QGMBhvUoyD8t5zY8xHQhnP`, R1, written at 2026-09-17T17:24Z. Judged on `origin/main` **after** the merge, ⛔ never on the PR-closed event.</sub>\n\n### Why this seat closed the card, and not the PR\n\nThe PR carried **`Part of #18110`** and **`Part of #18555`** — ⛔ deliberately **not** `Fixes`, per the governing ruling, so GitHub closed neither. The ruling put the close on the seat **after verifying the two arms independently**, which is the point of the fold: one channel, two sites, and a fold that lands only one site is the defect it exists to prevent.\n\n### Landing probe — delivery, positive control, negative control\n\n```\norigin/main tip: 5762eaf7e fix(service-automation): on the synchronous path, a child run that\n                 refuses stops its parent, in `subflow` and in `map` alike (#18706)\n\ncommit window (400):\n  (#18706) = 1   ← the delivery\n  (#18713) = 1   ← positive control (known merged)\n  (#99999) = 0   ← negative control\n```\n\n### The channel, and the two arms — measured SEPARATELY on `origin/main`\n\n| | reading |\n|---|---|\n| **channel** — `NodeExecutionResult.refuse?` member in `engine.ts` | **1** |\n| **channel** — `engine.ts` throws on `if (result.refuse)` | **1** |\n| ⭐ **ARM 1 (#18110, `subflow`)** — `refuse` in `builtin/subflow-node.ts` | **11** |\n| ⭐ **ARM 1** — `subflow-refused-rollup.test.ts` on main | **present** |\n| ⭐ **ARM 2 (#18555, `map`)** — `refuse` in `builtin/map-node.ts` | **6** |\n| ⭐ **ARM 2** — `map-refused-rollup.test.ts` on main | **present** |\n| negative control in `map-node.ts` | **0** |\n\n⇒ Both sites carry the arm and **each has its own pin file on main**, so deleting either arm fails a test by itself. That was gate ④ of the fold at review time and it is now true of the landed tree.\n\n### What shipped\n\nA node executor can now terminate its run as refused: `refuse?` / `refusalMessage?` join `NodeExecutionResult` beside `suspend?`, and `executeNode` throws `FlowRefusalSignal` **at the position the suspend signal is thrown** — after the success step is pushed, after the `childSteps` fold and output write-back — so a refusing child's `selected` / `acted` / `unmeasuredEffect` rollup (#4354) survives. `subflow` and `map` both read it. The region-boundary diagnostic now names whichever node carried the refusal (**text only**; region semantics untouched, #18112's option B not implemented).\n\nContract review of record (clause ②, **PASS**, at tier, tier confirmed from the reviewing round's transcript): `#issuecomment-5718253006` on the PR.\n\n### ⚠️ Read this before assuming the shape is fully closed\n\nThe fix covers the **synchronous** `engine.execute` return only. A child that **pauses and then refuses on its resumed leg** is ⛔ **not** covered — the delegated-resume path still rolls the refusal up as an ordinary success, and the up-bubble path leaves the parent `paused` in `listSuspendedRuns()`. ⛔ Neither is a regression of this change and ⛔ neither was in this ruling's scope; the changeset says so in its own scope paragraph, and the uncovered leg is filed as **#18714**.\n\n⭐ That limit was found by the at-tier contract review, ⛔ not by the card and ⛔ not by this seat — and the changeset headline was narrowed because of it, since a changeset becomes the release note.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
