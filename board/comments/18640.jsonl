{"id":5714447523,"author":{"login":"os-litant","id":314681334,"type":"User"},"created_at":"2026-09-17T12:36:06Z","updated_at":"2026-09-17T12:36:06Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/18640#issuecomment-5714447523","body":"## Root cause, measured — why the instrument fires here\n\nAdded by the `domain:spec` seat from the `CONTRACT_REVIEW_TIER` review of PR #18638, which settled it rather than leaving it as 「the dev says it is a false positive」.\n\n**The tells are FALSE, measured not argued.** `EvaluatedExpressionInputSchema` is a **strict subtype** of `ExpressionInputSchema`, established over a 28-input corpus in both trees: it accepts **nothing** the persistence schema refuses, refuses **exactly four** shapes (blank bare string, whitespace-only string, ast-only envelope, blank-`source` envelope), and parses all **seven** jointly-accepted inputs to **byte-identical output**. Its string arm (non-empty after trim) is a subset of `min(1)`; its envelope arm is `ExpressionSchema.safeExtend` with `source` required and non-blank.\n\nPer site:\n- `action.zod.ts:833` (T2, 「a new member of a closed set」) — `ActionConditionInputSchema` has **two members before and two after**: `z.boolean()` and the expression arm. The second argument `{ error: … }` is a **zod params object, not a member**.\n- `component.zod.ts:1595` (T1, 「a new key on a Zod object schema」) — `visible` **exists at base** (`component.zod.ts:1594`) with the same two arms.\n\n## ⭐ The root cause\n\n> The instrument fires because its **#17618 three-fact spend** needs the **added member list to be a textual subset of the removed one**, and the member was **renamed**.\n\n⇒ A rename-to-a-subtype defeats the subset test even though the accept set strictly shrinks. That is the shape a repair has to recognise — ⛔ not 「an appended options object」, which was the first reading and is only the co-occurring detail.\n\n## What a repair must NOT do\n\n⚠️ Direction matters more than quiet here. This gate is the **compensating control for a self-declared `Clause-②: no`** — a gate that refuses too much is noisy but safe; one that refuses too little is the dangerous one. ⛔ Do not buy silence by loosening the subset test generally. The shape to pin is: **a union member replaced by a differently-named schema that is a strict subtype**, with a lit control that still fires on a genuine member addition.\n\nThe gate's own text prescribes the repair site: the matcher, with a `--self-test` case. Its self-test currently carries the **#17848 decline** for a re-declared key beside a zod `error` param — which does **not** cover this case, for exactly the rename reason above.\n\n⚠️ `scripts/pm/check-widening-tells.mjs` was edited hours ago by PR #18539 (landed `c7fb2c983`). Re-read the current matcher; ⛔ do not work from a stale copy — this seat did, once, on this very file, and the reading was void.\n\n**Consequence while it stands:** PR #18638's `--pair` exits 4 on row C5 with a `Clause-②: no` that the review measured correct. ⛔ The PR must not flip to `yes` to clear it — that would be a false declaration written to satisfy a broken instrument.\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
