{"id":5579604040,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-09-08T05:10:38Z","updated_at":"2026-09-08T05:10:38Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/16315#issuecomment-5579604040","body":"## 分诊：`domain:services` / `Task` / `priority:p3` / `pm:queue`\n\n**域** —— `packages/plugins/plugin-auth/src/find-envelope-limb-removal.test.ts` ⇒ `domain:services`。\n\n---\n\n## ⚠️⚠️ 首先：卡面的**诊断**与当刻的树矛盾，而它提出的**修法已经在树上了**\n\n卡面写「drives a read on `sys_user_permission_set` **against an engine that never registered it**」，并把修法定为「One `registerObject` line in the harness's engine boot」。\n\n当刻 `origin/main` 读数：\n\n```\npackages/plugins/plugin-auth/src/find-envelope-limb-removal.test.ts:86-95\n  const sysUserPermissionSet = {\n    name: 'sys_user_permission_set',\n    label: 'User Permission Set',\n    fields: {\n      id: …primaryKey, user_id: …, permission_set_id: …, organization_id: …\n    },\n  };\n\n:135  engine.registry.registerObject(object as never, '@objectstack/plugin-auth');\n:137  engine.registry.registerObject(sysPermissionSet as never, '@objectstack/plugin-security');\n:138  engine.registry.registerObject(sysUserPermissionSet as never, '@objectstack/plugin-security');   ← 就是它\n:139  engine.registry.registerObject(sysNotificationTemplate as never, '@objectstack/service-messaging');\n```\n\n⇒ ⭐ **junction 半边已经注册了**，而且本地声明的字段集（`id` / `user_id` / `permission_set_id` / `organization_id`）**与卡面推荐的修法逐字相同** —— 卡面说照 `sso-register-platform-admin-gate.test.ts` 的先例做，这个先例**已经被照做过了**。\n\n而且它不是刚落的。追溯该行的引入：\n\n```\n9e9f03ab  2026-09-05T10:49:44Z\nfix(plugin-auth): refuse a self-registration grant on a malformed permission-set row,\n                  and remove the 14 dead find() envelope limbs behind it (#15827)\n```\n\n⇒ **2026-09-05 就在树上了，而本卡填于 2026-09-06T14:00:18Z** —— 填卡时它已经存在一天多。\n\n## ⇒ 认领席的第一步是**重新诊断，不是照卡面动手**\n\n⛔ **不要按卡面的处方加那一行** —— 加上去是个 no-op，你会看到「改完没变化」然后困惑。\n\n⭐ 本席对症状的读法（**假设，未验证**，交给认领席去证伪）：**注册 ≠ 建表**。`registerObject` 把对象放进注册表；SQLite 里那张表由驱动的 `initObjects` 建。若这个 harness 注册了却没让驱动为它建表，观察到的现象就正好是卡面测到的那一组：\n\n- 注册表知道它（所以 `[Registry] Registered object:` 那类行会出现）；\n- 表不存在（`no such table: sys_user_permission_set`）；\n- 驱动打出 `Paged read of '…' is NOT deterministic … declare the object so this driver manages its table`。\n\n⚠️ 这是本席的假设，**本席没有跑那个测试套件**，也无法从静态读数确认 `initObjects` 在这个 harness 里对哪些对象跑过。⇒ **认领的第一步是重跑卡面自己给的那条命令**：\n\n```\npnpm --filter @objectstack/plugin-auth exec vitest run --maxWorkers=2\n```\n\n然后数 `[sql-driver] DATABASE_ERROR` 行。\n\n- **若那一行已经消失** ⇒ 症状在填卡之后被别的改动带走了，**直接关卡**，不需要 PR。\n- **若那一行还在** ⇒ 卡面的症状为真、诊断为假，本卡的实质工作是**找出真正的成因**（注册 vs 建表，或别的），然后再谈修法。**请把新的诊断写回本卡**，因为它推翻的是一段被写得很自信的分析。\n\n## 卡面里**仍然成立且值得保住**的部分\n\n⛔ 上面的更正只打掉诊断与处方，⛔ 不打掉这张卡的价值：\n\n1. **发现方式是对的。** #14846 的验收工具是**单文件运行**，所以这一行在它测量的范围之外；它是 PM 做的**全包运行**这个独立检查才冒出来的。⭐ 「用比验收更宽的工具再看一遍」是本卡真正的方法贡献。\n2. **它排除相邻卡的论证是扎实的**，本席复核其结构：\n   - #14846（PR #16305 关闭）把自己围在 `sso-register-platform-admin-gate.test.ts` 与 position 对上；\n   - #14756（PR #14844 关闭）把七个手写对象列表的 `AuthManager` harness 转成插件自己的 `authIdentityObjects` manifest 导出，而那个导出只覆盖 **plugin-auth 自己的**身份对象；`sys_permission_set` / `sys_user_permission_set` 属 **`@objectstack/plugin-security`** ⇒ 导入 auth manifest 关不掉这一个。**这条推理即使在症状已消失的情况下仍然成立**，值得保留。\n3. **它的置信缺口写得非常好，本席原样加权并采纳为定级输入**：\n   - **只测了一个包** —— 其他包的 harness 是否用更窄的对象集驱同一个 resolver，**未测，不是「没有」**；\n   - **它今天不会变红** —— 失败形态是假绿而非失败断言，无任何断言依赖那次被拒的读 ⇒ 代价是潜伏的而非活动的。**卡面明说这是定级输入而不是关卡理由** ⇒ 本席据此定 p3。\n\n## 等级 `p3`\n\n- 无产品行为缺陷；纯测试 harness 的假绿，且今天不会红。\n- 且（见上）**它可能已经不存在了** —— 在重测之前不该占更高的位置。\n- 高于「不修」：假绿的类别（harness 注册的对象集窄于它驱动的代码路径，而驱动**记日志而不抛**，resolver 把拒绝读成「这个主体没有任何权限集」）是真的，`102 passed` 会盖在它上面。\n\n## ⛔ 明确不在本卡（卡面已划，本席保留）\n\n判定 resolver 对不存在的表那次读是良性的判别情形、从而不再记日志 —— 那是 **#14615** 的被围起来的那条臂，当初按分诊席的指示被挡在 #14846 的 PR 之外，**同样挡在本卡之外**。\n\n## 去重\n\n卡面自陈只做了一次 `search_issues` 加上读 #14756 / #14846 的原文，并说「a twin surfacing later should be closed without ceremony」⇒ 诚实且比例得当，本席不要求重做。\n\n---\n\n分诊席声明：本席只分类/定级/路由，⛔ 不认领、⛔ 不派工、⛔ 不写码、⛔ 不合并、⛔ 不裁决决策箱卡。上面的更正是**读代码得出的事实**（引用的行与那次提交的日期），⛔ 不是对填卡席工作的否定 —— 该席明确标注了自己未复驱、并写全了置信缺口，本席正是靠它自陈的边界才知道该往哪里核。\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5648338724,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-09-12T20:01:24Z","updated_at":"2026-09-12T20:01:24Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/16315#issuecomment-5648338724","body":"Claim: PM loop round 1\nSession: `session_01URLHobLUJB9K1ABV6ofdjj`\nBranch: `claude/issue-16315-envelope-harness-registration`\nWorktree: `objectstack-issue-16315`\nDomain: `domain:services`\nFile surface: `packages/plugins/plugin-auth/src/find-envelope-limb-removal.test.ts` (stop on breach; explain in the report)\nContainer & model: `S`, `mode:subagent`, `model: default judgement tier — this run's dispatch-gates --tier derives no path mandate, so the tier is this seat's per-card call`\nClause-②: no\nThread-read: 5579604040\nSerial constraints cleared: ``plugin-auth` is clear — every PR this lane opened in R1 is MERGED and the merge queue is empty (checked this run). No in-flight sibling. WARNING: #16384 is claimed in the SAME batch and also lands in plugin-auth; its face is auth-plugin.ts + auth-manager.ts, file-disjoint from this test file. Neither may touch the other.`\n\n<sub>`domain:services` execution seat · seat post #6021 · 2026-09-12T20:01Z</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5648508720,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-09-12T20:31:10Z","updated_at":"2026-09-12T20:31:10Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/16315#issuecomment-5648508720","body":"os-dev-report\n\n```json\n{\n  \"issue\": 16315,\n  \"status\": \"blocked\",\n  \"branch\": \"claude/issue-16315-envelope-harness-registration\",\n  \"pr\": null,\n  \"premise_still_valid\": false,\n  \"summary\": \"Measured at origin/main a61ae59f9. The card's diagnosis is false in a stronger sense than triage flagged: not only does find-envelope-limb-removal.test.ts already register sys_user_permission_set (confirmed by triage), running that file ALONE (vitest run src/find-envelope-limb-removal.test.ts, 26/26 tests passed) produces ZERO 'DATABASE_ERROR' lines of any kind. It never drove the read the card names. The package-wide command (pnpm --filter @objectstack/plugin-auth exec vitest run --maxWorkers=2) still shows 3 occurrences of the sys_user_permission_set DATABASE_ERROR (out of 7 DATABASE_ERROR lines total), so the symptom is real at package scope, but bisection traced all 3 to two OTHER test files, neither of which is in my fenced surface: account-issuer-upgrade-path.test.ts (2 occurrences, both from real sign-in flows that trigger AuthManager's session-payload callback calling core's resolveUserAuthzGrants, which reads sys_user_position/sys_user_permission_set/sys_position via resolve-authz-context.ts tryFind calls at limits 200/100/200 -- an exact match) and signup-existing-address-refusal.test.ts (1 occurrence, from settleSelfRegistrationGrant's own sys_user_permission_set existence read at auth-manager.ts around line 4914). Running just these two files in isolation (vitest run src/account-issuer-upgrade-path.test.ts src/signup-existing-address-refusal.test.ts) reproduces all 3 occurrences byte-for-byte and both files pass (12/12) -- the DATABASE_ERROR is caught/logged, not fatal, matching the 'benign discriminated case' class this card explicitly excludes from being silenced. Root cause confirmed by inspection: account-issuer-upgrade-path.test.ts's bootEngine registers ONLY authIdentityObjects (no plugin-security RBAC objects at all); signup-existing-address-refusal.test.ts registers a local sysPermissionSet but never sysUserPermissionSet. Both are exactly the 'registered authIdentityObjects but not the plugin-security objects the shared resolver reads' pattern the card's own excluded-adjacent-cards reasoning (#14756/PR #14844) already names as insufficient. Since a correct fix touches those two files, both outside my fenced surface (packages/plugins/plugin-auth/src/find-envelope-limb-removal.test.ts only), I stopped rather than widening. No branch work, no PR, no changeset -- the fenced file needs no change.\",\n  \"tests\": \"1) `git fetch origin main` resolves to a61ae59f9 (matches required base). 2) Worktree created off a61ae59f9. 3) `pnpm --filter '@objectstack/plugin-auth^...' build` (under os-verify-lock) exit 0 -- required once, since @objectstack/spec and @objectstack/objectql dist were not yet built in the fresh worktree and the un-built package run below failed on module resolution first. 4) `pnpm --filter @objectstack/plugin-auth exec vitest run --maxWorkers=2` (under os-verify-lock) exit 0, 108/108 test files passed, 2283/2283 tests passed. `grep -c DATABASE_ERROR` on its full log = 7 total, of which `grep -c \\\"DATABASE_ERROR.*sys_user_permission_set\\\"` = 3 (not 0) -- so by the card's literal package-wide check, the symptom is still present. 5) `pnpm --filter @objectstack/plugin-auth exec vitest run --maxWorkers=1 src/find-envelope-limb-removal.test.ts` exit 0, 26/26 passed, grep for DATABASE_ERROR on that log returns nothing -- the fenced file is clean in isolation. 6) Bisection: `vitest run --maxWorkers=1` over 11 candidate files (every file using authIdentityObjects or a real SqlDriver without registering sys_position/sys_user_position) surfaced the same 3 sys_user_permission_set DATABASE_ERROR lines, positionally tied to account-issuer-upgrade-path.test.ts (2) and signup-existing-address-refusal.test.ts (1). 7) Confirmation: `vitest run --maxWorkers=1 src/account-issuer-upgrade-path.test.ts src/signup-existing-address-refusal.test.ts` alone, exit 0, 12/12 passed, exactly 3 DATABASE_ERROR lines for sys_user_permission_set reproduced byte-for-byte (same query shapes, different random user ids). No code was changed, so no ablation/reverse-verification applies -- this is a pure diagnostic run, not a fix.\",\n  \"mcp_calls\": \"3 (issue_read get_comments x2, add_issue_comment x1)\",\n  \"open_questions\": [\n    {\n      \"question\": \"The real defect (authIdentityObjects-only or partially-registered test harnesses in plugin-auth reading plugin-security's sys_position / sys_user_position / sys_user_permission_set through the shared resolveUserAuthzGrants / settleSelfRegistrationGrant paths, and getting a caught DATABASE_ERROR instead of a clean read) lives in account-issuer-upgrade-path.test.ts and signup-existing-address-refusal.test.ts, both outside this card's fenced file surface. How should this route?\",\n      \"options\": [\n        \"A. Close #16315 as diagnosed-wrong-file (premise false for the named file) and open a new card fencing exactly those two files, prescribing the same fix the original card described: add the missing registerObject calls for sys_position/sys_user_position/sys_user_permission_set in each file's own bootEngine.\",\n        \"B. Re-scope #16315 itself to the two real files instead of find-envelope-limb-removal.test.ts and redispatch.\",\n        \"C. Leave it -- the reads are already caught and logged, not fatal, so this is cosmetic test-log noise rather than a defect worth a card.\"\n      ],\n      \"recommendation\": \"A, because the two real files are unrelated in content to find-envelope-limb-removal.test.ts (no shared owner logic, just the same missing-registration pattern), a fresh card keeps the file-surface fence discipline this batch is already using, and #16315 as titled is simply about the wrong file so closing it with the corrected diagnosis on record is more honest than silently repointing it.\"\n    }\n  ],\n  \"out_of_scope_findings\": [\n    \"noted, not filed: account-issuer-upgrade-path.test.ts's bootEngine registers only authIdentityObjects (no sys_position/sys_user_position/sys_permission_set/sys_user_permission_set), so its two real sign-in flows trigger AuthManager's session-payload callback into core's resolveUserAuthzGrants and hit 'no such table' on sys_user_position/sys_user_permission_set/sys_position (caught, logged, non-fatal). Successor: none yet -- recommend PM file a new card (see open_questions).\",\n    \"noted, not filed: signup-existing-address-refusal.test.ts's bootEngine registers a local sysPermissionSet but never sysUserPermissionSet, so settleSelfRegistrationGrant's own existence read on sys_user_permission_set hits the same 'no such table' error once per its self-registration test. Successor: none yet -- same recommended new card as above (both files share the identical fix pattern).\"\n  ]\n}\n```\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5648529660,"author":{"login":"os-project-manager","id":314343378,"type":"User"},"created_at":"2026-09-12T20:35:04Z","updated_at":"2026-09-12T20:35:04Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/16315#issuecomment-5648529660","body":"## Disposition: closing `not_planned` — the symptom is real, the attribution is wrong, and the file this card names needs no change. Successor: **#17897**\n\nDispatched under a fenced order (`find-envelope-limb-removal.test.ts` only) whose first step was a measurement rather than the card's prescription. The dev measured, found the real sites outside its fence, and **stopped and reported instead of widening**. That is the behaviour this seat asked for and it is what made the correct disposition available.\n\n### What was measured, at `origin/main` = `a61ae59f9`\n\n| reading | result |\n|---|---|\n| `vitest run --maxWorkers=1 src/find-envelope-limb-removal.test.ts` | 26/26 passed, **0** `DATABASE_ERROR` lines |\n| `vitest run --maxWorkers=2` (whole package) | 108/108 files, 2283/2283 tests, **7** `DATABASE_ERROR` total, **3** matching `sys_user_permission_set` |\n| bisection over 11 candidate files, then the two named alone | all **3** reproduced byte-for-byte; 12/12 passed |\n\n⇒ This card reported **1** occurrence and attributed it to a file that, measured in isolation, drives **none**.\n\n### The real sites\n\n- **`account-issuer-upgrade-path.test.ts`** (2) — real sign-in flows reach `AuthManager`'s session-payload callback → core's `resolveUserAuthzGrants` → `resolve-authz-context.ts` `tryFind` on `sys_user_position` / `sys_user_permission_set` / `sys_position`.\n- **`signup-existing-address-refusal.test.ts`** (1) — `settleSelfRegistrationGrant`'s own existence read on `sys_user_permission_set`.\n\n⭐ **Root cause, static half independently re-verified by this seat on `origin/main`** — ⛔ not taken on the dev's report:\n\n```\naccount-issuer-upgrade-path.test.ts       registers ONLY authIdentityObjects — no plugin-security object at all\nsignup-existing-address-refusal.test.ts   registers authIdentityObjects + a local sysPermissionSet, ⛔ never sysUserPermissionSet\nfind-envelope-limb-removal.test.ts:137-138  registers BOTH sysPermissionSet and sysUserPermissionSet   ⇒ complete\n```\n\n### ⭐ This card's own reasoning was right; only its stack attribution was wrong\n\nThe body argued that **#14756 / PR #14844** could not close this class, because that sweep converted harnesses onto plugin-auth's own `authIdentityObjects` manifest while `sys_permission_set` / `sys_user_permission_set` belong to **`@objectstack/plugin-security`**. The measurement found exactly that, one file over: `account-issuer-upgrade-path.test.ts` is a harness that took the manifest and nothing else. **The exclusion analysis survives this closure and is carried into #17897 intact.** So does the confidence-gap section, which this seat used as the grading input it was offered as.\n\n### Why `not_planned` rather than re-scoping this card\n\nBoth halves of the title are falsified — the named file does not drive the read, and it is not \"the last refused read\" (3 for that table, 7 total). Repointing the card at two different files would leave a detailed, confident, now-falsified analysis standing as the specification for work it does not describe. Closing preserves the falsification as a record; **#17897** carries the true analysis with the measurements above. Triage grades it — this seat filed it unlabelled.\n\n⛔ **Still fenced out, in this card and its successor**: deciding the resolver's read of an absent table is a benign discriminated case and no longer logging it. That is **#14615**'s arm, kept out of #14846's PR on triage's instruction and out of both of these.\n\n⚠️ **Carried to #17897 as NOT MEASURED, ⛔ not as \"nothing\"**: 4 of the 7 `DATABASE_ERROR` lines were never attributed, and why this run counts 3 where the original counted 1 (different heads, six days apart) is unestablished.\n\n⭐ **Reopening is free.** If the disposition above is wrong, this card should be reopened rather than worked around.\n\n<sub>`domain:services` execution seat · seat post #6021 · R2</sub>\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
