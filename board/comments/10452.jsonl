{"id":5363619392,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-21T00:15:41Z","updated_at":"2026-08-21T00:15:41Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/10452#issuecomment-5363619392","body":"Triage: lands in `scripts/check-cross-package-test-inputs.mjs` (literal collector) → `domain:devx`, type Task. Queued — closes the class (escaping relative import specifiers), the single instance is already declared by hand in PR #10450. The ⚠️ boundary in the body is the load-bearing constraint: bare package specifiers must stay excluded; a self-test case per new spelling is part of the acceptance. Size/model suggestion: M, opus.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5365380305,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-21T04:57:04Z","updated_at":"2026-08-21T04:57:04Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/10452#issuecomment-5365380305","body":"Claim: `/pm-dispatch` **domain:devx** execution seat (bug-first, batched round).\nSession: `session_01DdCnBGcHeufjrq7drTD3wt`\nBranch: `claude/issue-10452-cross-package-import-specifier`\nWorktree: `objectstack-issue-10452`\nDomain: `domain:devx`\nFile surface: `scripts/check-cross-package-test-inputs.mjs` (the literal collector) + roster entries it newly derives\n\n**Batched with #10549** (the `.d.mts` hand-maintained mirror with nothing gating it) — both are about the same file pair's undeclared coupling, and #10452's own subject *is* the import that #10549's mirror types.\n\n⭐ Unblocked as of now: **PR #10450 merged 04:53:22Z**, so `scripts/check-cross-package-test-inputs.mjs` is no longer held by in-flight work and the hand-added declaration this card was filed against is on main. No open PR touches that file — verified against the queue at dispatch time.\n\n⛔ Governed surface out of scope for the diff (`docs/adr/**`, `.claude/**`, `skills/**`, `AGENTS.md`, `CLAUDE.md`, `content/docs/releases/**`) — propose wording in the report instead.\n⭐ Changeset: this lane's dispatch template was wrong about this and is now fixed. `AGENTS.md:943` — *\"Pure bug fixes do not require a changeset\"*. For a `scripts/**`-only diff that publishes nothing, the repo's own gate prescribes the **`skip-changeset` label** (applied via the additive `POST /labels`, never a whole-set PUT); `scripts/check-empty-changeset.mjs` rejects the empty-frontmatter route. Precedent: PR #10502, merged 2026-08-21.\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5365570965,"author":{"login":"os-zhuang","id":277994282,"type":"User"},"created_at":"2026-08-21T05:26:17Z","updated_at":"2026-08-21T05:26:17Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/10452#issuecomment-5365570965","body":"Triage (half-state heal): removed `pm:queue` — the card is claimed and dispatched in flight (assignee + `pm:dispatched`), and the queue/dispatched pair must swap atomically. Same heal applied to the other in-flight cards showing both (#10374 #10289 #10175 #10549). Note for the devx seat: the fold/claim write is leaving `pm:queue` behind on every member of this batch — worth fixing the write pattern (remove-queue and add-dispatched in ONE label write).\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
{"id":5365741610,"author":{"login":"claude[bot]","id":209825114,"type":"Bot"},"created_at":"2026-08-21T05:50:52Z","updated_at":"2026-08-21T05:51:09Z","html_url":"https://github.com/objectstack-ai/objectstack/issues/10452#issuecomment-5365741610","body":"<!-- os-dev-report -->\n\n```json\n{\n  \"issue\": 10452,\n  \"status\": \"done\",\n  \"branch\": \"claude/issue-10452-cross-package-import-specifier\",\n  \"pr\": \"https://github.com/objectstack-ai/objectstack/pull/10607\",\n  \"premise_still_valid\": true,\n  \"summary\": \"Both cards implemented on one branch; PR #10607 (draft) closes #10452 and #10549. #10452: the collector now walks static/type/re-export/side-effect/dynamic/require specifiers through the same walkLiteral, judged on the same shallowest point, with bare specifiers excluded as installed dependencies; it found six couplings nothing had ever declared (@objectstack/client imports five sibling packages' route ledgers with no graph edge to any of them). #10549: new scripts/check-declaration-mirrors.mjs asserts NAME, KIND and required ARITY per declared export over a DISCOVERED corpus of every scripts/**/*.d.mts, wired into lint.yml as a node step (root package.json is out of scope, and three neighbouring steps state that same precedent). ⚠️ The dispatch brief's premise that the killed agent did 'no work — branch empty, nothing to recover' is FALSE: the worktree held a 251-line uncommitted collector diff, a 491-line unstaged new gate, turbo.json edits and nine evidence files in the scratchpad. I recovered it, rebased it onto current main, then reviewed it as a draft rather than a result — which is how the three defects below were found.\",\n  \"tests\": \"All final verdicts measured at HEAD 505e9dd905, exit codes captured before any pipe.\\n\\nGATES (exit / the gate's own verdict line): node scripts/check-cross-package-test-inputs.mjs --self-test → 0, 'All 79 self-test cases passed.' · node scripts/check-cross-package-test-inputs.mjs → 0, 'OK: 13 package(s) read outside themselves, all declared, and turbo.json hashes every declared glob.' · node scripts/check-declaration-mirrors.mjs --self-test → 0, 'All 23 self-test cases passed.' · node scripts/check-declaration-mirrors.mjs → 0, 'OK: 2 hand-written declaration(s) agree with their modules on name, kind and required arity.' · pnpm check:nul-bytes → 0 (6205 files) · check:node-version → 0 · check:pm-half-states → 0 (746 cases) · check:required-contexts → 0 · check:shard-attestation → 0 (92 assertions) · check:workflow-status-functions → 0 (26 workflows) · check:type-check-coverage → 0 · eslint on both changed scripts → 0. Families derived with `node scripts/pm/dispatch-gates.mjs` (no paths), re-derived after the final commit; it names `node scripts/check-declaration-mirrors.mjs [lint.yml]` from the diff itself, which is the wiring proof.\\n\\nREPRODUCTION of the #10452 harm, positively controlled twice, with origin/main's gate copied INTO this tree so its REPO_ROOT resolves here. Control A (constructed escaping import in @objectstack/service-i18n, which has no declaration at all): main gate EXIT=0 'OK: 12 package(s) read outside themselves, all declared'; this branch EXIT=1 naming the package and the test file. Control B (the real specimen, only the hand-added scripts/js-comment-mask.mjs glob removed, removal confirmed on disk by anchor count 1→0): main gate EXIT=0 'OK: 12 package(s)…'; this branch EXIT=1 'scripts/js-comment-mask.mjs (named in packages/cli/src/commands/serve-audit-registration.contract.test.ts)'.\\n\\nABLATIONS — every mutation applied by anchor-counted replacement that aborts unless exactly 1 anchor matches, every restore re-counting BOTH the injected and the original anchor plus an empty `git diff --stat`. An editor exit code was never the evidence. (1) Import half disabled (`if (true) continue` for the relative-specifier test): 12 of 79 self-test cases FAIL and the live gate reddens on the now-stale @objectstack/client declaration. The 7 new cases that stay green are exactly the 'does NOT flag' boundary ones, still correctly true — expected asymmetry, stated rather than glossed. (2) Mirror gate, three directions: NAME (maskComments→maskCommentz in the .d.mts) EXIT=1 'declares `maskCommentz`, but scripts/js-comment-mask.mjs does not export it'; ARITY (declaration gains a required 2nd param) EXIT=1 'declares `maskComments` with 2 required parameter(s), but … implements 1'; KIND (check-regen-pending.mjs exports a value where a function is declared) EXIT=1 'declares `distIsStale` as a function, but … exports `object`'. (3) Arrow fix: reverting the `=>` exception fails exactly the 2 new arrow cases. (4) Resolver fix: with one hand-written glob dropped, the dot-segment resolver names only @objectstack/lint while the known-extension resolver names @objectstack/cli AND @objectstack/lint.\\n\\nNO BUILD/DIST was involved in any ablation — these are dependency-free node scripts read from source, so there is no dist/ for a mutation to fail to reach.\\n\\n⚠️ ONE DECLARED NARROWING: `pnpm check:type-check-debt` was not run to completion. It is an ENVIRONMENT refusal, not a red verdict — it exits 1 printing '--re-measure cannot run: 55 workspace dependenc(ies) of the ledgered packages have no built type entry point on disk' and demands a full `turbo run build` of the workspace in this fresh worktree. This diff touches ZERO TypeScript (0 files matching .tsx? in `git diff --name-only origin/main...HEAD`), so no ledger number can move; the half of that gate sensitive to my lint.yml edit is check:type-check-coverage, green above. Run under the shared lock (VERDICT command-exit 1 · held the lock 1s · waited 0s). CI runs the re-measure regardless.\",\n  \"open_questions\": [],\n  \"out_of_scope_findings\": [\n    \"filed as #10610: check-cross-package-test-inputs.mjs runs verify() as an import side effect (no isEntrypoint guard, while 35 sibling scripts have one), so importing it for its exported helpers runs the whole gate and can process.exit(1); check-examples-live-imports.mjs hand-mirrors its globToRegExp rather than importing it. Latent — nothing imports it today. Filed unassigned, `finding` label, no pm:queue.\"\n  ],\n  \"brief_corrections\": [\n    \"FALSE — 'a previous agent was killed before doing any work — branch empty, ahead=0, nothing to recover. Start clean.' The worktree held substantial uncommitted work: scripts/check-cross-package-test-inputs.mjs +251 lines, an unstaged 491-line scripts/check-declaration-mirrors.mjs, turbo.json edits, and nine evidence files in the scratchpad (timestamps 05:03–05:19, i.e. minutes before I started). 'ahead=0' was true of the BRANCH and false of the TREE. I backed it up, rebased it onto current main, and reviewed it as a draft.\",\n    \"FALSE as posed — 'verify the hand declarations become redundant rather than merely duplicated.' They cannot become redundant by construction: the roster is what the gate checks a declaration AGAINST, so the gate never writes one for you. What changed is that they stopped being VOLUNTARY — before, cli's scripts/js-comment-mask.mjs glob was a courtesy nothing demanded; now removing it reddens the gate (Control B). They are kept, and now derived rather than trusted. I did not remove them.\",\n    \"QUALIFIED — the issue's headline measurement ('with no roster entry the gate reported OK') still reproduces exactly, but only when the .mjs declaration alone is removed. On today's tree, removing BOTH hand declarations also reddens the pre-change gate — on scripts/js-comment-mask.d.mts, which a second cli test names in prose and the flat literal collector has always seen. The .mjs import itself is invisible to the pre-change gate either way, which is what Control B isolates.\",\n    \"Two measured claims IN THE RECOVERED CODE were false and are corrected in the PR. (a) importSpecifiers' docblock claimed 'every specifier this finds across the repo's test sources is a live import, none is commented out' — measured: across 2509 test sources, 6 of 4174 relative specifiers exist only inside comments; none of the 6 escapes its package, so none reaches the roster. (b) resolveImportTarget's docblock claimed 'three packages/cli tests import the showcase app' extensionlessly — no cli test imports examples/ at all; the 15 live extensionless escapes are packages/client's route-ledger conformance tests. Both docblocks now carry the measurement I actually took.\",\n    \"A third defect was IN the recovered code and is fixed: resolveImportTarget judged 'extensionless' with !/\\\\.[A-Za-z0-9]+$/, so this repo's authored metadata specifiers (contact.view, semantic-zoo.object, task-triage.page) read as already-extensioned, no candidate was tried, and they resolved to nothing — silently declining to hold three radii a human had already written into the roster. Now tested against the known module extensions.\",\n    \"A fail-green in the NEW #10549 gate, found by probing and fixed before merge: balanced() counted '>' as a closing bracket, but '>' is also the tail of '=>', so a callback parameter truncated the parameter list at the arrow and reported arity 0 instead of 2 — quietly, with nothing in `unrecognised`. Fixed in all three places the arrow bites (balanced, splitParams, and the default-value test in requiredArity), with 3 pinning cases.\",\n    \"TENSION RESOLVED, worth a rule note — the brief put root package.json out of scope, but a new gate needs wiring or it enforces nothing (cf. #10574, open, for exactly that failure). lint.yml already carries three steps invoked as `node scripts/…` with the stated reason that the pnpm alias 'belongs in root package.json, declared territory of the @changesets/cli v3 migration lane (#9465) while it runs', and notes dispatch-gates derives gate families from either spelling. I followed that precedent; dispatch-gates confirms it discovers the new gate.\",\n    \"TRUE and worth keeping in the template — `gh` is not installed here. Used the GitHub MCP tools and curl with $GITHUB_TOKEN throughout.\",\n    \"PLATFORM BEHAVIOUR, measured — the PR body write appends its own '_Generated by [Claude Code]…_' footer on EVERY write: session-URL form on create, bare form on edit. Carrying my own session-form footer therefore produced TWO footers. Final state is one platform-supplied footer; the body itself carries none. Worth knowing before the next seat 'fixes' a missing footer.\"\n  ]\n}\n```\n\n\n---\n_Generated by [Claude Code](https://claude.ai/code)_"}
