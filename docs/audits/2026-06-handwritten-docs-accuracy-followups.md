# Implementation-Accuracy Audit — Residual Follow-ups (hand-written docs, 2026-06)

Second pass of the doc-accuracy audit covering the remaining 82 hand-written docs 
(guides/protocol/releases/index). 486 fixes applied + 28 adversarial-verifier repairs, 
verified against `packages/` implementation; `next build` green (1113/1113 pages).

This file records items the audit deliberately **left unchanged** because they could not be 
confirmed wrong against code, or because the fix would touch out-of-scope code/spec. They are 
candidates for a follow-up pass, not known errors.

## Stale generated references (drift between committed `content/docs/references/` and current spec)

`gen:docs` regenerated 16 generated reference files with diffs, i.e. they are out of date vs the Zod specs. 
Out of scope here (generated, DO-NOT-EDIT); fix by regenerating from spec in a dedicated change:

- `content/docs/references/ai/meta.json`
- `content/docs/references/api/meta.json`
- `content/docs/references/api/metadata.mdx`
- `content/docs/references/automation/approval.mdx`
- `content/docs/references/automation/meta.json`
- `content/docs/references/automation/node-executor.mdx`
- `content/docs/references/cloud/meta.json`
- `content/docs/references/data/meta.json`
- `content/docs/references/kernel/meta.json`
- `content/docs/references/kernel/metadata-plugin.mdx`
- `content/docs/references/system/index.mdx`
- `content/docs/references/system/meta.json`
- `content/docs/references/ui/meta.json`
- `content/docs/references/ui/page.mdx`
- `content/docs/references/ui/report.mdx`
- `content/docs/references/ui/view.mdx`

`gen:docs` also **emits 3 reference files that are missing from the committed tree** entirely (newly-added spec types never regenerated into the repo): `content/docs/references/contracts/` (whole dir), `content/docs/references/system/book.mdx`, `content/docs/references/system/doc.mdx`. Same fix: a dedicated regenerate-references change.

## Per-doc residual notes

80 of 82 docs carry residual notes (210 items total).

### `guides/airtable-dashboard-analysis.mdx`
- Related Issues links (#712-714) point at github.com/objectstack-ai/spec/issues/* (header line 11 and Section 7). The established prior says the repo is objectstack-ai/objectstack, not objectstack-ai/spec. The audit left these unchanged as historical Feb-2026 issue-tracker references and I could not confirm whether a separate objectstack-ai/spec repo ever hosted issues #712-714, so I did not fabricate a rewrite. Flag for human follow-up: if no objectstack-ai/spec repo exists, these are dead cross-repo links.
- The doc frames colorVariant/actionType/header/globalFilters.optionsFrom as 'NEW'/'proposed' Feb-2026 enhancements, but these schemas are now actually IMPLEMENTED in packages/spec/src/ui/dashboard.zod.ts (WidgetColorVariantSchema:16, WidgetActionTypeSchema:30, DashboardHeaderActionSchema:42, header at :68, GlobalFilter optionsFrom). The proposed enum VALUES also differ from the implemented ones (e.g. actionType doc lists external|internal|custom vs real script|url|modal|flow|api; colorVariant doc lists green/red/yellow/indigo/pink vs real default/blue/teal/orange/purple/success/warning/danger; header action 'type: primary|secondary|ghost' has no counterpart field in DashboardHeaderActionSchema). Left unchanged because the opening <Callout> explicitly frames the section as the original gap-analysis proposals, not current authoring — defensible but the framing is now stale since the work shipped.
- Inline widget query fields (object/valueField/aggregate/categoryField) used throughout the examples are superseded by the ADR-0021 dataset/dimensions/values shape (dashboard.zod.ts ~148-160). The opening Callout documents this supersession, so left as-is.
- objectui-side package names (@objectstack/plugin-dashboard, plugin-report, plugin-charts) in Section 4 were not verified against the sibling objectui repo (out of scope for the framework repo); presented as proposed component homes.

### `guides/analytics-datasets.mdx`
- Line 181 lists `columns` among the REMOVED inline query fields on reports (echoing the schema comment at packages/spec/src/ui/report.zod.ts:120, which says the legacy inline `objectName`+`columns`+`groupings` query was removed). This is accurate, but `ReportSchema` now has a DIFFERENT, still-present `columns` field meaning 'dimension names across' for matrix reports (report.zod.ts:130). The doc itself references that surviving across-axis field in prose (line 124). A reader could conflate the removed inline `columns` with the current matrix `columns`. Not a factual error (the schema's own JSDoc uses the same word), so left unchanged — flagging the potential reader confusion for human follow-up.
- Unchanged from audit (independently re-confirmed): the final paragraph (line 187) recommends 'an inline anonymous dataset for a one-off single-object KPI.' Verified that none of the three authoring schemas (dashboard.zod.ts:156, report.zod.ts:123, view.zod.ts:364) accept an inline dataset — `dataset` is always a SnakeCaseIdentifierSchema named reference. Inline datasets exist only at the REST/query layer, not in widget/report/list-chart authoring metadata. The advisory wording overstates authoring-layer capability; audit correctly left it as advisory, but the authoring-vs-runtime nuance remains for follow-up.

### `guides/cheatsheets/backward-compatibility.mdx`
- Phase 1 deprecation-timeline claims — 'Runtime warning is emitted on first use (once per session)' and the `.describe('DEPRECATED: ...')` convention (lines 51-53) — remain unverified against any concrete runtime implementation. doctor.ts:scanDeprecatedPatterns gives partial backing for a deprecation-annotation convention, but I found no code emitting a once-per-session runtime warning. Left unchanged (no evidence it is wrong), as the audit also flagged.
- SemVer/governance prose (Zod schemas as public API, support-window table, RFC/30-day review, '@objectstack/spec is the public API') are policy statements with no single backing implementation; not verifiable as code claims.
- Line 28 says 'Zod schemas are part of the public API surface' while the import sample exports `FieldSchema`/`ObjectSchema` (confirmed re-exported via packages/spec/src/data/index.ts:6-7 → field.zod.ts:357 / object.zod.ts:819). Schemas are wrapped in lazySchema(); whether `.parse()` is callable directly on the lazy wrapper without resolution is not something I traced, but no doc claim depends on it beyond the generic `Schema.parse()` mention (which Fix 3 correctly grounded).

### `guides/cheatsheets/error-catalog.mdx`
- Doc header says '41+ standardized error codes' while the StandardErrorCode enum actually contains 51 entries (errors.zod.ts:51-120). '41+' is technically still true (51 >= 41+) so not a factual error, but it understates the count and could be tightened to '50+'. Left unchanged — not wrong.
- validation_error example JSON (doc lines 51-62) places `fieldErrors` at the top level of the error object. The spec's @example doc-comment (errors.zod.ts:174-202) nests fieldErrors under `details`, BUT the EnhancedApiErrorSchema (line 229) defines a top-level `fieldErrors` array, so the doc is consistent with the actual schema shape. No change needed — confirmed defensible.
- EnhancedApiError TS interface (doc lines 353-368) was independently re-verified field-by-field against EnhancedApiErrorSchema (errors.zod.ts:220-235) and matches exactly on required/optional status (code & message required, retryable required via .default(false), all others optional). Accurate.

### `guides/cheatsheets/field-type-decision-tree.mdx`
- Decision Summary row 'Is it a number I need to calculate with?' lists `slider` alongside number/currency/percent. `slider` IS a valid enum type (field.zod.ts:54), but it is a UI input affordance rather than a 'calculate with' numeric; framing is slightly loose, not a factual error — leave as-is.
- Decision Summary row 'Can the user type free text?' lists `html` (valid enum type, field.zod.ts:24) but `html` does not appear in the Text Types quick-reference table (lines 133-141). Pre-existing asymmetry, not introduced by this edit; tables need not be exhaustive.
- Structured Data table lists `code` (valid, field.zod.ts:50) but the flowchart STRUCT branch has no `code` node. Pre-existing flowchart/table asymmetry, not a regression; flowchart is illustrative not exhaustive.
- Several valid enum types are never surfaced anywhere in the doc (video, audio, signature, qrcode, progress, tags, composite, repeater, record). This is a coverage gap, not an inaccuracy — the doc is a decision aid, not an exhaustive reference.

### `guides/cheatsheets/field-type-gallery.mdx`
- composite/repeater/record sections (doc lines 474-493) describe these embedded types as having 'declared sub-fields' but the doc gives no config-key example for declaring the inner shape (only { name, label, type }). This matches the schema enum comments at field.zod.ts:44-46 verbatim and the audit honestly logged it as unresolved — no fabricated key was introduced, so it is not an inaccuracy per se, but the actual sub-field declaration mechanism remains undocumented. FieldSchema (field.zod.ts:357-556) exposes no dedicated sub-fields/subObject property for these types; a human should confirm whether sub-shapes are declared elsewhere (e.g. a nested object definition or ADR-0007) and document it.
- currencyConfig defaults (doc lines 157-159: precision 2, currencyMode 'dynamic', defaultCurrency 'CNY') were NOT in the fix log but ARE correct per CurrencyConfigSchema (field.zod.ts:112-114) — no action needed, noted only for completeness.
- vectorConfig defaults (doc lines 627-629) verified correct against VectorConfigSchema (field.zod.ts:169-172): distanceMetric 'cosine', normalized false, indexed true, indexType optional/no-default — not part of the fix set but consistent with code.

### `guides/cheatsheets/field-validation-rules.mdx`
- password section (line 96 + Quick Summary line 547) claims value is 'Stored encrypted'. field.zod.ts describes 'password' as a one-way hash owned by the auth subsystem (vs the reversible 'secret' type), so 'encrypted' is imprecise — a hash is not encryption. Could not locate runtime hashing/masking code to definitively confirm read-masking behavior; left unchanged (matches audit's unresolved note).
- Per-type 'format' defaults for email/url/phone (lines 68,77,85) are presented as schema Defaults, but FieldSchema.format is an optional string with no per-type default in field.zod.ts. Format validation exists via rule-validator so framing is defensible as feature docs rather than a literal schema default; not fixed without stronger evidence.
- Intro (line 8) and Universal table (line 25) claim '48 field types' and the doc enumerates per-type tables, but real types secret/composite/repeater/record are omitted from the per-type sections and Quick Summary. The count of 48 is accurate per the enum; this is incompleteness, not a factual error.
- currencyConfig.defaultCurrency default documented as 'CNY' (line 149) — audit verified against field.zod.ts:111-115; confirmed accurate but flagged as a notably opinionated default worth a maintainer sanity-check.

### `guides/cheatsheets/permissions-matrix.mdx`
- Section 'Security Evaluation Order' Callout claims steps 2-6 are 'compiled into SQL WHERE clauses (RLS) at query time, not evaluated record-by-record'. RLS infrastructure (RowLevelSecurityPolicySchema, rls.zod.ts) exists, but I did not verify that the full OWD/ownership/role-hierarchy/sharing-rule/manual-share chain is actually compiled to a single SQL WHERE clause vs. partially evaluated in app code. Plausible but unverified — recommend a human confirm against the sharing/RLS query-builder.
- Section 6 role example uses parent values like 'director_east' for sales_rep while the mermaid diagram nests reps under directors; harmless but the example only shows one rep, not matching the multi-rep diagram. Cosmetic, not an inaccuracy.
- The matrix in Section 2 mixes permission flags freely (e.g. audit_log System Admin = viewAllRecords) — all tokens are valid ObjectPermission flags, but the matrix is illustrative and not derived from any shipped seed/profile; readers should not treat exact cell values as canonical.

### `guides/cheatsheets/protocol-diagram.mdx`
- None blocking. The Mermaid diagrams' GraphQL and WebSocket/Realtime boxes are conceptual architecture labels (not concrete endpoint assertions) and were correctly left untouched. The Layer Overview row 'Roles & Profiles' aligns with the real isProfile-flagged PermissionSet model, so no change warranted.

### `guides/cheatsheets/query-cheat-sheet.mdx`
- Join `on` array form `['order.customer_id', '=', 'customer.id']` is schema-documented (query.zod.ts JSDoc lines 152/165/201-204/218/252) and recognized by parseFilterAST/isFilterAST (filter.zod.ts:352-405, '=' in VALID_AST_OPERATORS), but convertComparison (filter.zod.ts:442-449) lowers `=` to literal-string equality `{ 'order.customer_id': 'customer.id' }` rather than a column-to-column reference. The strictly-typed column-reference equivalent would be `{ 'order.customer_id': { $eq: { $field: 'customer.id' } } }` via FieldReferenceSchema (filter.zod.ts:37-39). The engine does not implement joins inline (delegated to drivers), so the canonical runtime form could not be confirmed end-to-end. The doc matches the schema's own JSDoc, so this is the most defensible representation, but a reader copying it for an in-memory join may get literal-string matching. Audit already flagged this; left unchanged (no evidence the documented array form is wrong).
- String Operators table (doc lines 60-64) omits `$notContains`, which exists in the schema (filter.zod.ts:117, 172; FILTER_OPERATORS line 543). Omission, not an inaccuracy; table is incomplete. Audit already flagged this.

### `guides/cheatsheets/quick-reference.mdx`
- Systematic internal-link base mismatch (pre-existing, NOT introduced by the two fixes). From this file's location guides/cheatsheets/, the table/next-steps relative links do not resolve: './data/field' etc. point at guides/cheatsheets/data/* but the protocol pages actually live under content/docs/references/ (verified references/data/field.mdx exists, guides/cheatsheets/data/ does not). '../introduction' resolves to guides/introduction which does not exist anywhere (find returned nothing). '../objectql' and '../objectui' resolve to guides/objectql / guides/objectui which do not exist (only guides/objectql-migration.mdx; real pages at protocol/objectql/ and protocol/objectui/). './index' resolves to guides/cheatsheets/index which does not exist. quick-reference.mdx is the only file in guides/ using the './data/' link style (grep -rl confirmed), reinforcing it mirrors the auto-generated references/ tree. Correctly left unchanged by the audit: out of scope (not an implementation-API claim), pre-existing, and the right rewrite depends on the fumadocs link-resolution base (content-root vs file-relative) which is unconfirmed; a blanket rewrite risks breaking currently-working links. Recommend a human confirm the fumadocs base and batch-correct.
- Hand-maintained tallies unverified: header '175 ObjectStack protocols' (line 9) and per-section counts; audit noted packages/spec/src contains 219 .zod.ts files incl. subdirs/tests. Counting rule unconfirmed; left as-is. Low priority, not an API-accuracy defect.
- Security 'Permission' row (line 178) lists Key Schema 'ObjectPermission' with purpose 'Object-level permissions'. ObjectPermissionSchema is real (permission.zod.ts:17), but the primary container metadata type is PermissionSetSchema (permission.zod.ts:80, has isProfile flag, objects map of ObjectPermission). Cell is arguably incomplete (could also list PermissionSet) but not wrong; no change warranted under minimal-edit rule.

### `guides/cheatsheets/wire-format.mdx`
- Lines 472-475 (Common Headers): X-Request-Id 'honored by the observability dispatcher', X-Environment-Id 'Targets a specific environment/project on unscoped routes', and Accept-Language 'Locale for translated labels' are descriptive claims not directly verified against code in this pass. The rest-server does read req.headers If-Match (confirmed at rest-server.ts:2789) and translateMetaItems sets Vary: Accept-Language (rest-server.ts:1877), lending support, but the exact request-header semantics for X-Request-Id/X-Environment-Id were not code-confirmed. Low risk — these match the audit's 'added real X-Environment-Id and If-Match' note.
- Line 459: 'Maximum 200 records per batch request' is correct (batch.zod.ts:88 .max(200)), and the streaming import endpoint POST /api/v1/data/:object/import exists (rest-server.ts:2923). No issue, noted only for completeness.
- Lines 482-485 (Response Headers: X-RateLimit-* and ETag) were not verified against code; they are plausible standard headers but their emission by the REST server was not confirmed in this pass. Out of the fix scope; flag only for a future audit.

### `guides/contracts/auth-service.mdx`
- The doc still describes `verify` as accepting 'a bearer token or session identifier'. The contract's verify JSDoc (auth-service.ts:69) says 'Verify a session token or cookie', and the param is documented as 'Bearer token or session identifier' (line 70-72) — so the doc wording is consistent with the contract. No change needed; noting only that the actual semantics depend on the concrete implementation (better-auth), which the doc does not claim to document. Low concern.

### `guides/contracts/cache-service.mdx`
- Frontmatter `description` still reads '...with TTL support, namespaces, and bulk operations', which now overstates the minimal 6-method contract (namespaces and bulk ops were removed as fabricated). Correctly left untouched per the hard rule to preserve frontmatter exactly; a follow-up should reword it to e.g. 'key-value caching with TTL support and observability stats'.
- The Cache-Aside and Write-Through examples call `metadataService.getObject(name)` / `metadataService.updateObject(name, changes)` / `metadataService.onChange(...)`. These are illustrative application-level helpers rather than ICacheService contract methods, so they are not contract inaccuracies, but they were not verified to match any real metadata-service API surface. If strict accuracy of the surrounding example code is desired, a reviewer should confirm these method names against the actual metadata service or relabel them as pseudocode.

### `guides/contracts/data-engine.mdx`
- Error Codes table (lines 338-345): only `RECORD_NOT_FOUND` exists as a string literal in source (4 hits, packages/**/*.ts excluding tests). The other seven codes — `DUPLICATE_KEY`, `VALIDATION_ERROR`, `REFERENCE_ERROR`, `DELETE_RESTRICTED`, `TRANSACTION_FAILED`, `QUERY_TIMEOUT`, `BULK_PARTIAL_FAILURE` — return zero grep hits and no DataEngineErrorCode enum was located. Correctly left unchanged per HARD RULE 4 (absence of a descriptive name is not positive proof the doc is wrong; they may be conceptual error-semantics documentation). Flagged for human follow-up only — not a confirmed inaccuracy.
- EngineQueryOptions doc snippet (lines 86-98) types `where?: FilterCondition`, `fields?: FieldNode[]`, etc. The audit notes FieldNode is referenced in source as FieldNodeSchema (z.ZodType<any>); the hand-written interface here is a readable approximation, acceptable as conceptual typing but not a 1:1 export. Not fixed — informational only.

### `guides/contracts/index.mdx`
- None found. All 11 fixes independently verified against live code. Every interface named in the doc exists in packages/spec/src/contracts/ (ISearchService search-service.ts:64, IDataDriver data-driver.ts:20, IRealtimeService realtime-service.ts:63, IPluginLifecycleEvents plugin-lifecycle-events.ts:15, Logger logger.ts:9 [no I-prefix, correct], IServiceRegistry service-registry.ts:13, IJobService job-service.ts:55, IAIService ai-service.ts:198, IKnowledgeService knowledge-service.ts:75, plus IDataEngine/IMetadataService/IAuthService/ISharingService/IStorageService/ICacheService/IEmailService/INotificationService). All fabricated identifiers (IQueryEngine, ISearchEngine, IDriver, IPermissionService, IEventBus, IPluginManager, IScheduler, IModelProvider, IRagPipeline, definePlugin) confirmed absent via grep over packages (non-test). The IDataEngine code sample at doc lines 96-103 matches data-engine.ts:29-52 exactly (find/findOne/insert/update/delete). No Symbol DI token exists in spec contracts or core (grep '= Symbol(' empty) — fix correctly replaced it with string-name registry. Plugin type (core/src/types.ts:91-133) confirms name/dependencies?/init/start?/destroy? with NO requires/onEnable/onInit/onDestroy. registerService/getService confirmed at types.ts:21,36. Import paths valid: '@objectstack/spec/contracts' subpath exists (spec/package.json:89) and re-exports IDataEngine (contracts/index.ts:11); '@objectstack/core' re-exports Plugin via export * from './types.js' (core/src/index.ts:13). Minor non-issue worth noting only for awareness: the plugin example uses dependencies: ['com.objectstack.cache'] (a plugin-name namespace) while consuming via ctx.getService('cache') (a service-name namespace) — these are correctly distinct namespaces per the Plugin.dependencies docstring (types.ts:109), so the example is internally consistent and accurate.

### `guides/contracts/metadata-service.mdx`
- Frontmatter description (line 3) still reads 'CRUD operations for object and field definitions' — a holdover phrasing from the old fabricated ORM/field-CRUD framing. Not strictly false (register/get/unregister are CRUD on whole definitions) and hard rule #2 requires preserving frontmatter exactly, so left untouched. A maintainer may want to retune it to '(type,name) schema registry, overlay management, and import/export' for accuracy.
- UI Metadata section (lines 262-322) was NOT part of the 6 audited fixes but was spot-checked: claim 'IUIService has been deprecated' is correct — packages/spec/src/contracts/ui-service.ts:16 carries '@deprecated Use IMetadataService instead' (removal slated for v4.0.0). listViews('account'), getEffective context arg, and list('view') filtering all match the live contract. No action needed.
- Doc lists getObject/listObjects under the core interface as non-optional (lines 40-41), matching contract lines 182-188 where they are required (no ? modifier). Consistent — noting only because most other convenience accessors (getView/listViews/getDashboard) are optional, which the doc correctly marks with ?.

### `guides/contracts/storage-service.mdx`
- The getInfo example (storage-service.mdx:134-136) prints info.contentType and info.metadata as if always populated, but the local-filesystem adapter's getInfo (packages/services/service-storage/src/local-storage-adapter.ts:185) returns only {key, size, lastModified} — contentType/metadata are undefined there (S3 adapter does populate them, s3-storage-adapter.ts:206). The contract types both fields optional (storage-service.ts:37,41), so the doc is not strictly wrong, but the example is optimistic for the local backend; a '// undefined on local FS' note would be more accurate. Not changed (no spec contradiction).
- Unresolved item from audit stands: contract-level upload validation (maxSize/allowedTypes) was correctly removed since it does not exist on IStorageService, but API-layer validation does exist (FileTypeValidationSchema in packages/spec/src/api/storage.zod.ts). A cross-link to the storage REST/validation docs would help readers, but is out of scope for a contract-only doc.
- acl 'public-read' enum is faithful to the contract (storage-service.ts:25), but the local-filesystem adapter ignores options entirely (upload's _options param is unused, local-storage-adapter.ts:133), so acl has no effect on the local backend. Could be worth a note that acl is honored only by object-store adapters (S3/GCS/Azure).

### `guides/data-flow.mdx`
- Section 7 'Insert Operation' mermaid subgraph still uses node IDs C4/C5/C6 (leftover numbering from the old beforeValidate..afterCommit chain). This is cosmetically odd but mermaid-valid and not a factual error — node IDs are arbitrary. Could be renamed to I1/I2/I3 for clarity but no build/accuracy impact.
- Sequence-diagram method labels (read/create/update, checkPermission, buildQuery, applyRLS, emit('task.created'), subscribe(...)) remain illustrative architecture names not tied to verified public API signatures. The audit correctly left these as conceptual; emitted-event constants like 'task.created'/'task.updated' could not be matched to a canonical event-name spec in packages/runtime (only 'data.updated' found in a core test). Reported, not fixed, per evidence-only rule.
- Section 2 read-flow diagram labels the API layer 'REST API' (audit narrowed from 'REST / GraphQL'); GraphQL appears only as an aspirational comment in packages/core/src/api-registry.ts. The narrowing is reasonable and consistent with no GraphQL endpoint existing in packages/rest.

### `guides/driver-configuration.mdx`
- Examples use `pnpm dev` (lines 17, 135, 198, 203) for launching — this is a monorepo/showcase invocation, not the user-facing CLI. The user-facing binary is `os dev` / `os serve` (packages/cli/package.json bin block exposes both `os` and `objectstack`). Pre-existing prose, not touched by the audit; left as-is. If standardizing per the established `os`-binary correction, these could be normalized, but `pnpm dev` is not strictly wrong in a monorepo context.
- Prose references `objectstack dev` / `objectstack serve` (lines 131, 173). Verified NOT a regression: packages/cli/package.json bin block aliases both `objectstack` and `os` to ./bin/run.js, so `objectstack` is a real binary name. Established corrections prefer `os`, but `objectstack` is valid. Out of scope of the applied fixes.
- The Multi-Datasource object example (line 239) uses `ObjectSchema.create({...})` without showing its import. Verified the API is real (packages/create-objectstack/src/templates/blank/src/objects/note.object.ts:5; platform-objects), but the snippet only imports `defineStack` from `@objectstack/spec` — a reader copy-pasting would need to also import ObjectSchema. Pre-existing doc shorthand, not introduced by the fixes.

### `guides/environment-variables.mdx`
- OS_OBS_EXPORTER row lists `json` as a distinct value, but serve.ts:76 maps both `console` and `json` to the same ConsoleMetricsRegistry — `json` currently behaves identically to `console`. Accurate (it is accepted) but the doc does not note they are equivalent; minor, not an error.
- Cloud control-plane vars OS_PLATFORM_SSO (default true), OS_RUNTIME_PORT, OS_PROVISION_SYNC and the Tuning vars (OS_KERNEL_CACHE_SIZE=32, OS_KERNEL_TTL_MS=900000, OS_ENV_CACHE_TTL_MS=300000, OS_ARTIFACT_CACHE_TTL_MS=300000) have no occurrences in this open-core repo; their defaults live in the closed cloud distribution and remain unverifiable here. The audit correctly left them unchanged — flagged only so a human with cloud-repo access can confirm.
- Email doc maps SMTP settings to 'Settings → Mail'; code uses the `mail` settings namespace (email-plugin.ts:312,327), so the label is consistent. No action needed.

### `guides/error-handling-client.mdx`
- Section 2 ObjectStackError constructor is typed against the REAL `import type { ErrorResponse } from '@objectstack/spec/api'`, whose `error.code` is the strict `StandardErrorCode` enum (errors.zod.ts:221), NOT `string`. Sections 4 and 7 use the client-synthesized literal 'NETWORK_ERROR', which is not a member of that enum (grep for NETWORK_ERROR in packages/ is empty). So `new ObjectStackError({ error: { code: 'NETWORK_ERROR', ... } })` and the `case 'NETWORK_ERROR':` switch arm would be TS type errors against the imported type. The audit added a clarifying comment ('client-synthesized') but did not reconcile the type. Not a regression (pre-existing pedagogy choice flagged by the audit). Honest fixes: either (a) widen the doc's own Section-1 `ErrorResponse.error.code` to `StandardErrorCode | 'NETWORK_ERROR'` / `string` and have the class accept that local interface rather than the imported type, or (b) drop NETWORK_ERROR in favor of a real server code. Left in place per minimal-edit rule.
- Section 1 standalone `ErrorResponse` interface declares `meta.traceId` and `error.retryStrategy`, which DO exist in the schema (errors.zod.ts:226,232,265) — correct. But note the JSDoc example in the spec (errors.zod.ts:182-198) nests `fieldErrors` INSIDE `details`, contradicting the actual `EnhancedApiErrorSchema` where `fieldErrors` is a top-level sibling of `details` (line 229). The doc correctly follows the SCHEMA (top-level), so the doc is right and the spec's own JSDoc example is the stale one. No doc change needed; flagging that the spec example at errors.zod.ts:182-198 is internally inconsistent with its schema.
- Out of scope (carried from audit's unresolved): content/docs/api/wire-format.mdx section 7 (## 7. Error Response Format at line 331) still documents the OLD shape (UPPERCASE codes, error.status, error.details[] arrays). The cross-reference link/anchor #7-error-response-format from this doc (line 11) is still VALID (target heading exists), but the cheatsheet content it points to now contradicts this corrected doc. Should be reconciled in a follow-up.
- Out of scope (carried from audit's unresolved): the actual REST runtime envelope from mapDataError (packages/rest/src/rest-server.ts) is reportedly a flatter third shape than ErrorResponseSchema. This doc documents the spec/ErrorResponseSchema shape, which is a defensible source-of-truth choice, but the divergence between spec schema and REST runtime output is worth a dedicated reconciliation pass.

### `guides/error-handling-server.mdx`
- Section 4 prose says the HookEvent enum 'only covers before*/after* of find, insert, update, delete (and their bulk variants)'. The enum (packages/spec/src/data/hook.zod.ts:12-26) also includes beforeCount/afterCount and beforeAggregate/afterAggregate. This is a harmless simplification, not a factual error, so left unchanged.
- import { Hook } from '@objectstack/spec/data' is a value import of a type-only export (hook.zod.ts:247 `export type Hook`). Under standard tsconfig this erases fine and matches the canonical business-logic.mdx:248 pattern, so it was left for consistency; only under verbatimModuleSyntax would it need `import type`. No change made to avoid diverging from the established doc pattern.

### `guides/index.mdx`
- AI Capabilities card description (line 48) lists 'NLQ' as a feature. ai-capabilities.mdx frontmatter/TOC confirm AI Agents, RAG/Knowledge Protocol, model registry, and intelligent automation, but I did not find an explicit 'NLQ'/'natural language query' section heading in the quick grep. Low-confidence; the term may appear deeper in the doc. Not changed — no evidence it is wrong, only unverified.

### `guides/kernel-services.mdx`
- Driver Support table (kernel-services.mdx:159-165) is now stale: it lists PostgreSQL/MySQL/MongoDB/SQLite all as 'TBD'/'—', but DB driver packages now exist under packages/plugins/: driver-sql, driver-mongodb, driver-sqlite-wasm (only driver-memory existed when this table was written). The audit correctly flagged this as unresolved and left it unchanged. NOT a regression (pre-existing staleness). I did not fix it because the mere existence of a driver package does not establish its CRUD/Aggregation/Ready status, which the table's columns assert; a human should verify each driver's maturity (note: no driver-mysql or driver-postgresql package exists yet — driver-sql may cover Postgres/MySQL collectively).
- Intro Callout aggregate counts ('57 total' protocol methods; '38 protocol methods to be delivered by plugins') do not map to a single source of truth — protocol.zod.ts ObjectStackProtocol defines ~67 z.function entries including feed/reactions/permission ops not enumerated as services in the doc. The audit left these roadmap-level estimates unchanged; confirmed they remain a soft spot but not safely correctable without a canonical count. Same applies to the '18 kernel-provided' figure (metadata 7 + data 9 + analytics 2 = 18, internally consistent).
- Doc section 10 'ai — 3 methods' (aiNlq/aiSuggest/aiInsights) is correct for the REST/default-route surface (DEFAULT_AI_ROUTES has 3 endpoints, plugin-rest-api.test.ts:574-578), but note aiChat still EXISTS in the wire-level ObjectStackProtocol interface (protocol.zod.ts:1140). The doc's 'methods' column tracks REST endpoint counts throughout, so removing aiChat is consistent with that framing and with the established prior (client.ai.chat removed); however a reader could conflate 'protocol methods' (intro) with 'REST methods' (per-service tables). Reporting for awareness; no change warranted.

### `guides/metadata/app.mdx`
- App Properties table types `objects` and `apis` as `string[]`, but AppSchema declares both as z.array(z.unknown()) (packages/spec/src/ui/app.zod.ts:576-577). The audit deliberately left this as a simplification; it is a defensible doc-level simplification (these commonly hold name strings) rather than a clear error, so no change. Flagging for human follow-up only.
- The `visible` type label was changed to `Expression`. Strictly, ExpressionInputSchema (packages/spec/src/shared/expression.zod.ts:83-86) accepts either a bare CEL string OR a canonical Expression envelope at input time; the doc's `Expression` label + 'CEL expression' description is accurate enough, but a reader might not realize a raw string shorthand is also accepted. Minor; not an inaccuracy.

### `guides/metadata/dashboard.mdx`
- Descriptive prose at /Users/oszhuang/Documents/GitHub/objectstack/content/docs/ui/dashboards.mdx:165-169 ('ObjectUI currently renders dataset-backed metric, chart, table/list, pivot, funnel, and gauge-style widgets ... drill-down affordances') is a runtime-renderer capability claim not directly backed by the spec schemas I read. It is not false on its face (all named chart types are in ChartTypeSchema) but the specific rendering/drill-down feature claims live in ObjectUI (objectui repo / client-react), which is outside packages/spec — not independently verified here. Low priority, no evidence it is wrong.
- Line 102: filter is documented as type `FilterCondition` with note '(runtimeFilter)'. Schema (dashboard.zod.ts:121-122) confirms widget.filter is FilterConditionSchema applied as runtimeFilter — accurate. No action needed; noting only that the parenthetical 'runtimeFilter' is internal naming that may confuse authors but is faithful to the schema describe() text.

### `guides/metadata/doc.mdx`
- doc.mdx:11-12 'Docs are also the grounding the AI assistant reads when answering questions about your package' — unverified. Grepped packages/mcp, packages/knowledge*, and broad src for any AI/grounding/RAG/embed ingestion path keyed on `doc` items; found none. Could not confirm nor disprove (no wiring found), so left unchanged per evidence-required rule. Human should confirm whether a doc-grounding ingestion path exists.
- Scope omission (not an inaccuracy): doc.zod.ts:64-99 defines `order`, `group`, and `translations` fields plus ADR-0046 §6 `book` grouping, and the Doc Properties table (doc.mdx:56-61) lists only name/label/description/content. The table is framed as 'the shape a *.md file becomes when collected', which legitimately omits programmatic-only/i18n fields, so this is a scope choice rather than a wrong claim — flagging for completeness only.

### `guides/metadata/field.mdx`
- Line 151 referenceFilters example uses `'account = {account}'` ({field} interpolation). Independently re-verified: field.zod.ts:403 and field.form.ts:48 describe only literal predicates (e.g. 'active = true'); field.test.ts:269,475 use literal predicates ('status = "active"'); no runtime consumer (objectql/runtime/rest/client) evaluating {field}-style interpolation in lookup filters was found. Interpolation support is unconfirmed in code — example may be inaccurate. Correctly left unchanged by audit (no evidence to fix). Human follow-up needed.
- Doc does not document the 'secret' field type (field.zod.ts FieldType enum), distinct from 'password'. Omission, not an inaccuracy — no change warranted.
- Line 64 uses `step` on Field.number(); field.zod.ts:483 describes `step` as 'Step increment for slider (default: 1)'. The prop exists on the shared FieldSchema so it is structurally valid on number, but the schema description is slider-scoped — minor potential mismatch in intended scope. Pre-existing doc content, not introduced by this audit; not changed.
- Line 240 rating description '(Star rating (1-5))' — maxRating defaults to 5 but is configurable (test uses 10). Description is slightly narrow but defensible as the default. Pre-existing, not a fix target.

### `guides/metadata/flow.mdx`
- Try/catch example (line 290) still uses the deprecated node type `type: 'http_request'`, while the audit canonicalized the standalone HTTP example to `http` and qualified `http_request` as deprecated (line 111). Not broken — `registerNodeAlias('http_request', HTTP_TYPE)` at http-nodes.ts:164 means the alias resolves to `http` — but it is internally inconsistent with the audit's own correction. Left unchanged because fix 4 was explicitly scoped to the standalone example and the alias is valid; flag for consistency follow-up.
- Error Handling table (lines 475-480) omits `backoffMultiplier`, `maxRetryDelayMs`, and `jitter`, which exist on FlowSchema.errorHandling (flow.zod.ts:255-259). Incompleteness, not inaccuracy — a documented subset — so not a fix; same item the audit flagged.
- Console Flow Viewer section (lines 516-531) names ObjectUI components `FlowViewer`, `FlowTestRunner`, `FlowRunsPanel`; grep finds none under packages/ or apps/ in this repo. The doc attributes them to ObjectUI (a separate repo), so they cannot be confirmed/refuted here — unverifiable, left unchanged.
- Basic-structure example: node `request_approval` is now `type: 'approval'` but its out-edge (e5) and in-edge (e3) use `type: 'default'` rather than the approval branch labels `approve`/`reject` (APPROVAL_BRANCH_LABELS in approval.zod.ts). Schema-valid (edges aren't validated against approval branch labels at the Zod level) and acceptable for an illustrative example, but a runtime approval node would normally branch on `approve`/`reject` labels — minor didactic imprecision.

### `guides/metadata/index.mdx`
- The ASCII 'How Metadata Works Together' diagram (lines ~83-88) still depicts 'Workflow (event triggers)' as a co-equal box alongside Flow and Permission, implying it is a distinct artifact. Left unchanged as a conceptual/illustrative diagram (not a code/API/file claim) per minimal-edit rule, and the now-corrected table directly above is authoritative. A maintainer may wish to relabel or merge that box for full consistency.
- The Doc row claims package docs render at `/docs/<name>`. Confirmed via doc.zod.ts that docs are package-scoped inert markdown, but no route handler in packages/rest or apps/console literally confirms the `/docs/<name>` URL — consistent with schema JSDoc but the exact route string is unverified. (Same item the audit left unresolved; agreed, left unchanged for lack of contradicting evidence.)

### `guides/metadata/object.mdx`
- titleFormat example uses single-brace '{name} - {code}' (doc line 70). The ObjectSchema describe-string (object.zod.ts:541) claims '{{record.field}}' interpolation, but all real object source files use single-brace {field} syntax (packages/services/service-storage/src/objects/system-file.object.ts:27 'titleFormat: {name}'; service-automation 'titleFormat: {flow_name} · {node_id}'). Doc matches real usage; the schema describe text is the outlier. Spec-level reconciliation, not a doc fix. Audit agent correctly left unchanged.
- apiMethods default documented as 'all' (doc line 98). Schema is .optional() with no explicit Zod .default() (object.zod.ts:61). 'all when omitted' is implied behavior, not a literal default. Descriptively reasonable; left as-is by audit, confirmed acceptable.

### `guides/metadata/page.mdx`
- fix #8 visibility example uses `os.user.profile == 'sales_manager'`. The CEL operator (`==`) and `os.` namespace are correct (packages/formula/src/stdlib.ts:118-122 binds `os.user`), but the `user` context shape (packages/formula/src/types.ts:27-32) declares only `id`, `role`, `email` plus a `[key: string]: unknown` index signature — there is NO declared `profile` field; `role` is the real field. Permitted by the index signature so not schema-invalid, but `os.user.role == 'sales_manager'` would be the truly accurate field. Not repaired because the broken `=` operator (the actual bug) was correctly fixed and `profile` is not strictly invalid.
- Action-expression functions `navigate_to`/`set_variable` (used in the events example) exist only as illustrative JSDoc strings in packages/spec/src/ui/page.zod.ts:81-82. grep of packages/formula/src found no runtime binding (empty), and no action registry was located. The example matches the documented form but the runtime action vocabulary remains unconfirmed — same unresolved status the audit recorded.
- Internal inconsistency the audit left in place: the doc now documents the real namespaced PageComponentType enum (`record:details`, `record:related_list`, etc., from page.zod.ts:32-47) but the Basic Structure and Complete Example code blocks still use flat alias type strings (`metric_card`, `list_view`, `chart`, `record_header`, `record_detail`, `activity_timeline`, `related_list`). The `type` union permits arbitrary strings (page.zod.ts:68-71) so these are not schema-invalid, but no runtime registry maps the flat aliases to renderers, so the examples may not render as written. Reader could be confused by examples not using any documented type. Left as-is per audit's unresolved note; no evidence the aliases resolve.
- Basic Structure / Complete Example component `properties` keys (`aggregate`, `format`, `chartType`, `categoryField`, `valueField`, `relationship`) live under the free-form `properties: z.record(z.string(), z.unknown())` (page.zod.ts:76) and could not be validated against any per-component prop schema (component.zod.ts not confirmed to define them). Not provably wrong, just unvalidated.

### `guides/metadata/permission.mdx`
- tabPermissions example uses bare app keys (crm/admin/reports/analytics); the schema JSDoc (permission.zod.ts:104-110) illustrates app_-prefixed keys (app_crm). Schema keys on z.string() so bare keys validate fine — left as-is, illustrative only, not an error.
- systemPermissions example strings (manage_users, customize_application, view_all_data, etc.) are free-form (z.array(z.string()), permission.zod.ts:97) with no enum; most appear in permission.test.ts (manage_users, view_all_data, modify_all_data, customize_application) but a few in the doc (manage_sharing, export_data, api_access, manage_integrations) are unverified against any consuming enum in plugin-security. Cannot confirm the security plugin recognizes all of them; documented as illustrative.
- The note 'editable: true requires readable: true' (line 124) is NOT enforced as a Zod refinement — FieldPermissionSchema (permission.zod.ts:50-55) has no superRefine. It may be a runtime/plugin-security behavior; not traced. Stated as a behavioral note, which is acceptable, but unverified at the schema level.

### `guides/metadata/validation.mdx`
- Frontmatter `description` (line 3) still reads 'formula conditions, uniqueness, format, state machine transitions, and more' — it lists 'uniqueness', which is no longer a validation type (it was deliberately moved to unique indexes). NOT fixed because HARD RULE 2 forbids modifying frontmatter. A human may want to reword 'uniqueness' -> 'unique constraints' or drop it; purely an SEO/description nit, body text is accurate.
- Doc intro (line 8) and the validation.zod.ts module doc both describe rules as 'synchronous, side-effect-free' yet the spec module header (validation.zod.ts:14-18) frames this as the contract; this is consistent, not an error — noting only that the doc does not mention the fail-open behavior for state_machine/format/json_schema rules (only script/cross_field/conditional broken-predicate fail-open is described). Low priority; no code contradiction found.

### `guides/metadata/view.mdx`
- Data Source section (lines 111-126) shows only three code examples (object, api, value) while the prose on line 109 correctly lists four providers including `schema`. Not an inaccuracy — the prose is accurate and `schema` is a standalone-form provider (view.zod.ts:48-54), so omitting it from List View examples is reasonable. Could optionally add a `schema` example for completeness, but no factual error.
- Calendar config example (lines 145-150) lists all four CalendarConfigSchema fields but does not flag that endDateField and colorField are optional (view.zod.ts:376,378). Same for Gantt progressField/dependenciesField (optional, view.zod.ts:388-389). The examples are valid; optionality is just not annotated. Minor, not an error.

### `guides/metadata/workflow.mdx`
- The start-node `triggerType: 'record-after-update'` value is validated only by a runtime regex (packages/triggers/trigger-record-change/src/record-change-trigger.ts:64, /^record-(before|after)-(create|insert|update|delete)$/), not by a Zod enum — FlowNodeSchema.config is an open record (flow.zod.ts:98). Correct and matches the canonical business-logic.mdx convention, but not schema-enforced (already noted by the audit as unresolved; not a doc error).
- Migration table rows 'Email alert -> notify node' and 'HTTP call -> http node' (workflow.mdx:133-134) reference valid FlowNodeAction ids (notify, http per flow.zod.ts:32,34), but the exact config shapes for those nodes were not deeply verified in this pass. No evidence of inaccuracy; flagged only because the table presents them without config examples.

### `guides/objectql-migration.mdx`
- Step 1 / Step 5 reference the migration-target version `@objectstack/objectql` `^3.1.0` and `v3.1`. Not cross-checked against packages/objectql/package.json — these are external/aspirational version statements rather than in-repo API claims; flagged for human follow-up only.
- All FROM-side claims about legacy `@objectql/core` (^4.x) and `@objectql/types` (MetadataRegistry sync register/get, ObjectQLPlugin as RuntimePlugin composing ValidatorPlugin/FormulaPlugin/QueryPlugin, install()/onStart()) describe external packages absent from github.com/objectstack-ai/objectstack and are unverifiable here. The TO-side equivalents were all confirmed against code.

### `guides/plugin-chatbot-integration.mdx`
- Line 122-124 troubleshooting: 'register models on the AIService.modelRegistry'. modelRegistry is a real property (ai-service.ts:120,152,191) but listModels() delegates to adapter.listModels() (ai-service.ts:494-498), so models registered on modelRegistry will NOT surface via GET /api/v1/ai/models unless the adapter also reports them. The guidance is slightly misleading but not strictly wrong since the property exists; audit correctly left as unresolved.
- Line 48 note '(also accepts a bare array)' for GET /agents: framework route returns only { agents: [...] } (agent-routes.ts:87). The bare-array tolerance is a client-side (@object-ui/plugin-chatbot, external objectui monorepo) behavior not verifiable in this repo; correctly left as-is by audit.
- Lines 55-58 CORS note conflates 'allow-list' with 'exposes'. X-Environment-Id is in the Hono adapter allowHeaders default (adapters/hono/src/index.ts:189), i.e. an allowed request header, not an exposed response header. The functional claim (cross-origin calls work) is correct; the allow-vs-expose wording is imprecise. Not fixed — minor and audit flagged it.

### `guides/production-readiness.mdx`
- Minor/cosmetic: the doc's HSTS sample uses maxAge 31536000 (1yr) while the implementation default when hsts is enabled without maxAge is 15552000 (180d) — security-headers.ts:84. Not an inaccuracy (the doc explicitly passes maxAge), just noting the default differs. No edit warranted.
- The 'durable storage and tamper-evidence are the host's choice' claim and 'no built-in audit immutability layer' are accurate per audit-writers.ts (records written to sys_audit_log via standard ObjectQL inserts; no append-only/WORM enforcement in code) — confirmed correct, left as-is.

### `guides/publish-and-preview.mdx`
- None affecting correctness. Note for context only: `os start` also exposes its own `--artifact` flag (packages/cli/src/commands/start.ts:79), but the doc's 'Production artifact host' row uses the env-var form `OS_ARTIFACT_PATH=./dist/objectstack.json os start`, which is valid (start.ts:145,297) — no change needed.

### `guides/runtime-services/audit-service.mdx`
- None. Independently re-verified every claim against live code: (1) canonical source packages/services/service-settings/src/settings-service.types.ts exists; (2) doc record(entry) signature lines 16-26 matches SettingsAuditSink.record EXACTLY at settings-service.types.ts:91-101 (namespace, key, scope:SpecifierScope, userId?, actor?, action:'set'|'reset', valueDigest, encrypted, requestId?, returns Promise<void>|void); (3) write-only/best-effort framing matches optional-sink design (line 89 comment, lines 186-187); (4) sys_audit_log object name confirmed at sys-audit-log.object.ts:17 and sys_activity at sys-activity.object.ts:20; header comment settings-service.types.ts:16 confirms settings writes emit sys_audit_log rows; (5) SpecifierScope is a real imported type (types.ts:26). The audit's two unresolved items are correctly classified as non-fixable: the `experimental` stability badge is a hand-asserted maturity label with no code enum, and `services.audit` is a doc-site descriptive convention consistent with sibling docs (data-service.mdx/settings-service.mdx confirmed present) rather than a literal registry key. No established-correction patterns (os studio, client.ai.chat, defineProfile, /_studio, /api/v1/{non-data}, CEL UPPERCASE, VITE_RUNTIME) appear in the doc (grep empty). fixCount 0 was correct; no edits were made so no regression is possible. Frontmatter (lines 1-4) intact; file contains no MDX/JSX components to break.

### `guides/runtime-services/data-service.mdx`
- Typical Errors section uses UPPERCASE codes RECORD_NOT_FOUND / VALIDATION_ERROR / PERMISSION_DENIED. The spec Zod StandardErrorCode enum is lowercase (packages/spec/src/api/errors.zod.ts:51+ — validation_error, etc.), but the REST/runtime layer emits UPPERCASE for PERMISSION_DENIED and RECORD_NOT_FOUND (packages/rest/src/rest-server.ts:93,137; packages/runtime/src/http-dispatcher.ts:2943; resolve-execution-context.ts:281). The doc's uppercase matches runtime emission, so left unchanged — consistent with the audit's decision. VALIDATION_ERROR uppercase emission is NOT found anywhere in rest/runtime/objectql source (grep empty), so its exact runtime casing remains unconfirmed; a human should verify whether the runtime ever emits a validation error code and in which case.
- The documented update/delete signatures omit the optional `opts?: { ifMatch?: string }` parameter that exists in the real client (packages/client/src/index.ts:3122-3123, 3177-3178). This is an acceptable doc simplification, not an inaccuracy, but noted for completeness if full fidelity is desired.

### `guides/runtime-services/email-service.mdx`
- NONE in the doc — doc is accurate. Note for code-side follow-up (NOT a doc issue): the contract comment at packages/spec/src/contracts/email-service.ts:11 claims 'Aligned with CoreServiceName email in core-services.zod.ts', but the CoreServiceName enum (packages/spec/src/system/core-services.zod.ts:20-43) has NO 'email' member — email is subsumed under 'notification' (line 38). The doc does not reference CoreServiceName, so the doc itself is correct; the stale code comment is out of scope here.
- Audit's two 'unresolved' items are actually resolvable and benign: (1) services.email accessor IS genuine — plugin-email registers the service under key 'email' via ctx.registerService('email', this.service) at packages/plugins/plugin-email/src/email-plugin.ts:117, and the accessor matches the chapter-wide convention in content/docs/kernel/runtime-services/index.mdx. (2) Stability 'stable' is a curation claim consistent with every sibling runtime-services doc (storage-service.mdx etc.); no code marker exists, correctly left unchanged.

### `guides/runtime-services/examples.mdx`
- Example #2 registers a data hook by exporting a top-level function named after the event (`export async function beforeUpdate(ctx)`). This convention is NOT confirmed in code: grep for `export ... beforeUpdate` across packages/ and examples/ returned no matches. The flow-node `export async function run` convention (Example #1) DOES appear in code (packages/objectql/src/build-probes.ts, packages/cli/src/lint/metadata-eval.ts), but the analogous hook-by-function-name registration is unverified. The audit agent correctly flagged this as unresolved and left it loosely-typed (ctx: any) — recommend a human confirm the actual hook-registration convention (top-level export vs. ctx.hook registration). `ctx.input.id`, `ctx.session.{userId,tenantId,roles}` match HookSchema per the audit's cited hook.zod.ts lines (not independently re-read here, but consistent with SharingExecutionContext which I did confirm).
- Examples #1 and #2 access services via `ctx.services?.data?` / `ctx.services?.sharing?` (flow-node / hook execution context). This is distinct from the PluginContext used in Example #3 and was not part of either fix. The shape of the flow/hook execution `ctx` (whether services are exposed under `ctx.services`) was not independently verified against code — left as-is since it was unchanged by the audit and is not the subject of a fix.

### `guides/runtime-services/index.mdx`
- Conceptual-only: the page lists service categories as `services.data`, `services.sharing`, etc. The core service registry (packages/core/src/kernel.ts:455, kernel-base.ts:76) resolves services via getService(name)/registerService(name, service), not property access on a `services` object. However, this is a bulleted category listing with NO executable code sample, and `.data` property access does in fact exist on the scoped client surface (packages/client/src/index.ts:1052 `scoped.data.find(...)`), so the shorthand is defensible. Correctly left unchanged by the audit; changing it to `services.get('data')` would risk introducing an unsupported convention. No evidence-backed fix warranted.
- The `description` frontmatter and body both reference per-service sub-pages ('Each page links the canonical TypeScript source'). I did not verify that sibling pages under content/docs/kernel/runtime-services/ actually exist; if they are missing the cross-reference prose would be aspirational, but this is out of scope for verifying the index page's factual claims and was not flagged as a fix.

### `guides/runtime-services/queue-service.mdx`
- Not a doc inaccuracy, but a code/doc-comment mismatch for awareness: the IQueueService.replay JSDoc at packages/spec/src/contracts/queue-service.ts:144 says it throws if the message 'is not in dlq status', whereas db-queue-adapter.ts:201 actually accepts both 'dlq' and 'failed'. The doc's INVALID_STATE description (non-dlq/failed) correctly matches the adapter, so the doc is fine.

### `guides/runtime-services/settings-service.mdx`
- The 'Error Types' section lists 3 types but the package exports at least 2 more public errors: SettingsValidationError (code SETTINGS_VALIDATION, settings-service.types.ts:244-255, exported index.ts:35; thrown by setMany/validatePatch on required/pattern failures) and VisibilityParseError (visibility-eval.ts:26, exported index.ts:42). Both are OMISSIONS, not inaccuracies — the 3 listed entries are each correct against the code — so no edit was made per the no-invented-changes rule. The audit flagged SettingsValidationError but missed VisibilityParseError. A maintainer may want to add one or both for completeness.

### `guides/runtime-services/storage-service.mdx`
- Doc still asserts '**Stability:** `stable`'. The canonical contract (packages/spec/src/contracts/storage-service.ts) carries no stability annotation (grep for stability/@stable/@experimental returned nothing), so the claim is neither confirmed nor contradicted by code. Left unchanged per the audit's unresolved note; flag for a human who knows the intended stability policy.
- Minor: the 'PERMISSION_DENIED ... from the security layer' note is accurate at the source level (plugin-security/src/errors.ts:9, runtime/src/security/resolve-execution-context.ts:281 both set readonly code = 'PERMISSION_DENIED'), but a storage caller only sees that error if a security check is wired in front of the storage route — the storage adapter itself never raises it. The doc already says 'not the storage adapter', so this is adequately qualified; noting only for completeness.

### `guides/runtime-services/versioning.mdx`
- services.storage matrix row maps to the runtime key file-storage (registerService('file-storage') at packages/services/service-storage/src/storage-service-plugin.ts:160), not a literal `storage` accessor. Correctly left unchanged: services.* is a chapter-wide docs abstraction used consistently in index.mdx line 14 and storage-service.mdx title/heading; fixing only this doc would create chapter inconsistency. Flag for follow-up only if the whole chapter ever reconciles docs notation with real registration keys.

### `guides/single-project-mode.mdx`
- Audit 'unresolved' item #1 is a FALSE ALARM, not a real inaccuracy: comparison-table line 30 `/api/v1/environments/:environmentId/...` IS correct per the in-repo REST server's request-scoped routing (packages/rest/src/rest-server.ts:1283-1293 buildScopedBasePath -> `${basePath}/environments/:environmentId`; rest-api-plugin.ts:200,206 registerPackageRoutes with `${versionedBase}/environments/:environmentId`). The audit agent conflated this request-routing path with the CLIENT cloud-management CRUD API `/api/v1/cloud/environments/...` (client/src/index.ts:641-700), which is a different surface. No edit needed; the doc value is accurate. Left unchanged (correct).
- Minor cosmetic mismatch (NOT in scope of this doc): `createStandaloneStack` defaults environmentId to 'proj_local' (packages/runtime/src/standalone-stack.ts:120) whereas the CLI `os dev`/`os start` default to 'env_local' (dev.ts:179). The doc's code samples always pass environmentId:'env_local' explicitly and its env-var example uses env_local, so the doc is internally consistent and not wrong — but the underlying runtime default differs from the CLI default. Reported for awareness only; no doc change warranted.
- Audit 'unresolved' item #2 (prose line 54-55 'The old project-scoped names are not aliases in current docs or examples') is correctly scoped to docs/examples and now true post-edit; code does still accept legacy aliases via readEnvWithDeprecation with a deprecation warning (auth-manager.ts:1079, serve.ts:148). Borderline phrasing but defensible; left unchanged.

### `guides/standards.mdx`
- Section 2 ObjectSchema example: the `enable: { trackHistory, searchable, apiEnabled, files, feeds }` block was not part of any audited fix and was not independently field-by-field verified against ObjectSchema's actual capability/enable shape. Low risk (pre-existing prose), but worth a future spot-check that these exact keys exist on the schema's enable/capability sub-object.
- Section 3 Dashboard 'Best Practices' uses widget `type` values 'metric'|'bar'|'table' and `layout: { x,y,w,h }`; these illustrative snippets were not verified against the real dashboard/widget schema (out of scope of the 8 fixes, no evidence either way).

### `guides/troubleshooting.mdx`
- client.data.find is documented but is @deprecated in favor of client.data.query() with QueryAST params (packages/client/src/index.ts:3017-3020). The corrected call is accurate and functional, but examples could be migrated to data.query() in a future pass. Not an error.
- The optimized-query object literal (troubleshooting.mdx:284-292) mixes spec QueryAST keys (object, where, orderBy, cursor) which match QuerySchema (query.zod.ts), but client.data.find's QueryOptionsV2 interface (index.ts:180) does not expose `object` or `cursor` keys. The snippet reads as a conceptual QueryAST example consistent with surrounding query snippets, so it is structurally fine, but a strict reader could conflate it with literal find() args. Left as-is.
- Audit Fix 4 evidence string mislabeled the JSON-schema top-level key as `definitions` (length 1667); the actual file uses `$defs` and has no top-level `definitions` key (definitions length parses to 0). This is an evidence-log inaccuracy only — the doc's genericized 'after' text references `x-schema-count` (verified = 1686) and the existing per-domain subfolders (data/, ui/, api/ all contain individual .json schemas), so the published doc is accurate.

### `index.mdx`
- content/docs/index.mdx:23 — the API Reference summary lists "generated REST, GraphQL, realtime, and SDK surfaces." GraphQL is a discovery feature flag defaulting to false (api-reference.mdx:38 features.graphql: false) plus a /graphql route alias in the client (packages/client/src/index.ts:3353), and realtime is explicitly plugin-gated (api-reference.mdx:385 'Realtime (/realtime) — Plugin Required'). These are documented-but-gated protocol surfaces, not fabrications, so the line was correctly left unchanged; the phrasing only slightly overstates GraphQL/realtime availability. Stylistic, no evidence-backed fix warranted.
- OUT OF SCOPE (separate file): the linked content/docs/getting-started/quick-start.mdx still titles a section 'Launch the Studio' while invoking 'os dev --ui'. Contradicts the no-os-studio / Console (/_console) convention and should be aligned in a follow-up pass, but is outside this single-file audit.

### `protocol/index.mdx`
- Design principle 'Database-Agnostic — works across PostgreSQL, MongoDB, and SQLite' omits MySQL, which packages/plugins/driver-sql/package.json explicitly advertises ('Supports PostgreSQL, MySQL, SQLite via Knex'). Not an inaccuracy (the three named are all real and backed by drivers driver-sql, driver-mongodb, driver-sqlite-wasm) — just an incomplete list. Left untouched as a deliberate simplification; flag for human if exhaustive DB coverage is desired.

### `protocol/knowledge.mdx`
- 3.2 KnowledgeHit field list omits the real `sourceId` and `title` fields (packages/spec/src/ai/knowledge-document.zod.ts:82,90). Pre-existing incompleteness, not introduced by the fixes; the audit already flagged it. Could be completed for accuracy but is not wrong as written.
- 3.2 also describes KnowledgeDocument as having a `permissions?` field which is correct (knowledge-document.zod.ts:50), but lists `sourceRecordId?` while the schema field is named `sourceRecordId` — verify the KnowledgeDocument schema field names if completeness is later desired (not a fix, left per minimal-edit rule).
- Spec code-comment Studio-vs-Console drift remains in packages/spec/src/contracts/knowledge-service.ts:13 and knowledge-adapter.ts:71 — out of scope for this doc but carries the same naming drift; flag for a code-comment cleanup pass.

### `protocol/objectos/config-resolution.mdx`
- The whole context.config.* API surface (get/set/require/getNamespace/has/inspect/keys/getSchema/setUserPreference/setTenant/merge/getAll) does not match the real SettingsService, whose model is get(namespace,key,ctx)/getNamespace(namespace,ctx) over a sys_setting K/V table with a cascade chain. The page's top Callout disclaims these as design intent, and the precedence/merge logic IS implemented, so left unchanged — but the API names are aspirational.
- defineStack nested shapes (database, http, plugins.enabled, secrets.provider/mappings, features) do not match the real StackSchema (plugins: z.array(z.unknown()); datasources is an array; no http/database/secrets keys). Disclaimed by the top Callout; left unchanged.
- SQL DDL for objectstack_tenant_config / objectstack_user_preferences / objectstack_secrets standalone tables does not reflect real persistence (single sys_setting K/V object, platform-objects/src/system/sys-setting.object.ts). Disclaimed as illustrative storage shapes; could not find these tables in code.
- Real setting keys are snake_case under snake_case namespaces (e.g. namespace 'feature_flags', key 'ai_enabled' -> OS_FEATURE_FLAGS_AI_ENABLED, per feature-flags.manifest.ts:21). The doc's camelCase examples (stripe.apiKey, features.newUI) are illustrative and differ from the real manifest key style; covered by the design-intent disclaimer but worth noting for a future rewrite.
- Structural oddity (not an inaccuracy): the 'Type Coercion' subsection documenting OS_* env coercion now sits under the '### 4. User Preferences' heading (lines 243-251) after the audit's reordering; content is correct but topically belongs under Environment Variables. Left as-is to honor the minimal-edit rule.
- The programmatic config.inspect() 'sources' object (lines 707-719) lists keys in runtime/user/tenant/environment/file/defaults order; inspect() does not exist in code and is disclaimed design intent, so not corrected.

### `protocol/objectos/error-handling.mdx`
- Error-envelope shape (`{ success: false, error: { code, message, details, request_id, timestamp } }`) and the entire string-code catalog (UNAUTHORIZED, VALIDATION_ERROR, NOT_FOUND, CONSTRAINT_VIOLATION, RATE_LIMITED, etc.) do not match the live packages/rest implementation, which emits a flat `{ error, code, fields }` shape with different codes (VALIDATION_FAILED, PERMISSION_DENIED, RECORD_NOT_FOUND, UNIQUE_VIOLATION, THROTTLED). Left unchanged by the audit for cross-doc consistency with http-protocol.mdx (PR #1866); confirmed still divergent. Needs a product decision on spec-vs-implementation authority — out of scope for an evidence-backed minimal fix.
- request_id and timestamp response fields (lines 67-69, 79-80, 887-905, 903) are not emitted by any packages/rest or packages/runtime response body. Part of the normative-spec framing, same as sibling; flagged for follow-up.
- Rate-limit X-RateLimit-* / Retry-After header contract and RATE_LIMITED code (lines 388-395, 711-718) could not be confirmed wired to data routes; a token-bucket limiter exists but no HTTP route emits these headers. Unverified, left unchanged.
- Next Steps hrefs `/docs/transport/http-api` (line 1050) and `/docs/transport/realtime` (line 1056) point to a non-existent /docs/transport/* tree; actual docs live at /docs/protocol/objectos/*. Left unchanged to stay consistent with the audited siblings (http-protocol.mdx:933,939; realtime-protocol.mdx) which use the same hrefs — needs a coordinated cross-doc link-tree fix, not a single-doc edit.
- WebSocket error codes (CONNECTION_LIMIT_EXCEEDED, SUBSCRIPTION_LIMIT_EXCEEDED, INVALID_MESSAGE, lines 551-616) and the GraphQL channel reference (line 10) have no located backing implementation. Not confirmed wrong; flagged.

### `protocol/objectos/http-protocol.mdx`
- Discovery response example uses placeholder values name:'Acme CRM Production' / version:'2.1.0' / environment:'production' whereas code emits name:'ObjectOS', version:'1.0.0', environment from NODE_ENV (default 'development'). These read as illustrative examples, not factual claims, so left unchanged — but a reader could mistake the version string for a real default.
- Discovery 'locale' block in the doc lists supported ['en-US','zh-CN','es-ES','fr-FR'] and timezone 'America/Los_Angeles'; code default locale is {default:'en', supported:['en'], timezone:'UTC'} derived from the i18n service (http-dispatcher.ts:838-848). Example values only, but the timezone/locale defaults differ from code defaults.
- Read-only-field update error example (doc lines 530-541) uses code 'VALIDATION_FAILED' with a fields[] entry {field, message}. This matches the generic validation envelope shape (rest-server.ts:71-80) but I did not locate the specific code path that rejects read-only-field updates and asserts this exact code/shape — it is plausible/consistent but unverified at file:line.
- Rate Limiting section (X-RateLimit-* headers, 429 THROTTLED envelope) is already qualified by a <Callout> noting it is not wired into the default REST path. The THROTTLED code itself is real (rest-server.ts:4415), but the X-RateLimit-* response headers and 'retry_after' field in the 429 body were not found wired in the default response path — the existing Callout adequately hedges this.

### `protocol/objectos/i18n-standard.mdx`
- Date/Time and Number Formatting sections still use illustrative `context.i18n.formatDate/formatNumber/...` syntax (lines ~366-537). These are inside explicit 'Design intent — not yet implemented' Callouts that disclaim the entire API and point to Intl.*, so not a factual regression, but the example syntax uses `context.i18n.` rather than the real `kernel.getService('i18n')` surface. Left as-is per minimal-edit rule since the blocks are clearly marked aspirational.
- ObjectUI Integration example uses `defineView({...})` — defineView DOES exist (packages/spec/src/ui/view.zod.ts:861), so this is accurate; noted only for completeness.
- Best Practices 'Test All Locales' uses `createContext`/`renderView` test helpers that were not verified against code; they read as illustrative pseudo-test helpers, low priority.
- Locale Resolution chain lists IP-geolocation as step 4 of the fallback chain; not verified against an i18n-resolver implementation — could not confirm IP-geolocation is actually wired. Flagged for human follow-up, not changed (no disconfirming evidence found).

### `protocol/objectos/index.mdx`
- Conceptual pseudo-code namespaces (ObjectOS.createContext, ObjectOS.installPlugin, ObjectQL.findById, ObjectUI.render, context.config.resolve, context.i18n.t) in the 'Request Handling' and 'Plugin Installation' sections do not map to real exported APIs (verified: no global ObjectOS namespace; createContext lives on the kernel/engine, not a top-level ObjectOS object). The audit correctly left them as illustrative — flagging only, not a fix, since they are framed as conceptual.
- Phase 4 boot comment 'HTTP / Studio / MCP server' retains the word 'Studio'. Per repo CLAUDE.md the console/portal path is /_console (not /_studio), but 'Studio' as an admin-console product name does exist (packages/platform-objects). In a descriptive comment this is ambiguous, not clearly wrong; left unchanged consistent with audit.
- The plugin.manifest.yml / objectstack.config.yml / i18n/*.json file conventions and the YAML manifest schema (objects/fields/permissions/dependencies blocks) were NOT deep-verified against the packages/spec plugin manifest schema. plugin-spec.mdx is the authoritative source and out of scope for this single-doc audit.
- Repo-wide: the plural href base '/docs/protocols/' (singular dir is 'protocol/') appears ~55 times in other docs (objectql/*, objectui/*) — same broken-link bug class, out of scope here but worth a sweep.

### `protocol/objectos/lifecycle.mdx`
- Health-check endpoints /health/live, /health/ready, /health/status (lifecycle.mdx:585-595) are not served by code — packages/cli/src/commands/serve.ts:892 and packages/runtime/src/http-dispatcher.ts:2836 only expose /health, /api/v1/health, /api/v1/ready. The three-way live/ready/status split and the detailed JSON health response (lines 599-633) are unverified. Audit correctly left unchanged as protocol-spec/design-intent; flagged here for human follow-up.
- definePlugin({...}) used in two examples (lines 274, 641) has no real export anywhere in packages/ (grep only hits markdown report files). The documented manifest fields lifecycle.{preInstall/postInstall/onEnable/onDisable/preUninstall/postUninstall} and top-level healthChecks are not on the real Plugin interface. Sibling doc plugin-spec.mdx states 'There is no definePlugin()'. A doc-wide convention; out of minimal scope to fix in one file, but inconsistent with code.
- Self-hosted env vars OS_HEALTH_TIMEOUT_MS, OS_AUTO_ROLLBACK, OS_MAINTENANCE_MESSAGE (lines 571-573) do not appear in packages/ or apps/. Section is explicitly framed as operational policy / design intent; left as-is by audit. Flagged for verification.

### `protocol/objectos/metadata-service.mdx`
- CLI section (lines 237-239) uses the `objectstack compile/publish/dev` prefix. This is a valid registered bin alias (packages/cli/package.json bin: objectstack + os), so not factually wrong, but it is inconsistent with the rest of the docs which prefer the `os` binary per the PR #1866 established corrections. The audit deliberately left it to avoid an unbacked style rewrite; flagging for human decision on doc-wide CLI-prefix consistency.
- Loader table (lines 64-67) 'Type' column uses descriptive labels (filesystem/memory/database/http). The RemoteLoader's contract protocol is 'http:' and its loader name is 'remote'; 'database' maps to protocol 'datasource:'. These are prose shorthands rather than exact API values, so not strictly wrong, but a reader could mistake them for protocol/contract identifiers. Left as-is (matches audit's unresolved note).
- Section 1 example calls `manager.load('object', 'account')` and Section 4 references `manager.save('object', ...)`; both method names and (type, name[, data, options]) signatures were independently confirmed against packages/metadata/src/metadata-manager.ts:1334 (load) and :1383 (save). No inaccuracy — recorded only to document that the verifier checked the unchanged code samples.

### `protocol/objectos/plugin-spec.mdx`
- The aspirational manifest snippet (definePlugin(), plugin.manifest.ts, optionalDependencies/peerDependencies/marketplace/lifecycle/permissions blocks) and the onInstall/onUninstall/onEnable/onDisable/onUpgrade/onBoot lifecycle-hook surface do NOT match the shipped Plugin interface (init/start/destroy in packages/core/src/types.ts:91,119,126,132) or the real ManifestSchema. The audit left this under 'unresolved' and the top-of-page warn-Callout already disclaims it as design intent — acceptable but a large block of unimplemented surface remains.
- The generated plugin scaffold src/index.ts (create.ts:50-63) types the plugin with `initialize`/`destroy` methods, but the actual Plugin interface in packages/core/src/types.ts uses `init`/`start`/`destroy`. This is a code-side inconsistency (the scaffold imports Plugin from @objectstack/spec/contracts), not a doc assertion, so not fixed — flagging for engineering follow-up.
- Doc's '### 2. Develop Locally' block uses `npm run dev`; the scaffold package.json maps dev → `tsc --watch` (create.ts:21), which only rebuilds and does not start a server. The `os dev --ui` line that follows is correct. The `npm run dev` reference is technically valid (script exists) but mildly misleading as a 'watch mode' for app dev; left unchanged as it is not factually wrong.

### `protocol/objectos/realtime-protocol.mdx`
- Fix 2's cited evidence line numbers are wrong/non-existent: packages/objectql/src/protocol.ts contains NO 'realtime' or 'routes' references at all (grep -ni empty; file is 5152 lines). The fix's substantive 'after' value (routes.realtime = '/api/v1/realtime', plus data='/api/v1/data', metadata='/api/v1/meta') is nonetheless CORRECT, corroborated by the real sources: packages/spec/src/api/discovery.zod.ts:78-150 (ApiRoutesSchema wrapped under top-level routes key, data required as '/api/v1/data', metadata as '/api/v1/meta'), discovery.zod.ts:116 (realtime 'e.g. /api/v1/realtime'), dispatcher.zod.ts:163, and packages/client/src/index.ts:3345. No edit needed; only the citation provenance is bad.
- Fix 4's cited evidence also references the non-existent protocol.ts:423 realtime route registration, but the underlying claim is independently TRUE: grep for 'realtime/events' across packages (minus dist) is empty, so the debug-log endpoint genuinely does not exist. The callout qualifying it as planned is correct.
- Omission (not an inaccuracy, out of fix scope): the doc never mentions the REAL HTTP realtime API that IS spec'd — POST /api/v1/realtime/{connect,disconnect,subscribe,unsubscribe} and presence GET/PUT /api/v1/realtime/presence/:channel (packages/spec/src/api/plugin-rest-api.zod.ts:1185-1260, DEFAULT_REALTIME_ROUTES). The doc presents only the aspirational WS/SSE wire protocol. This does not make any existing statement wrong, but a future pass could document the actual POST-based subscription endpoints. Note these are route SCHEMAS in spec; whether a handler is wired in service-realtime today was not separately confirmed.
- Event-type naming inconsistency persists inside the WS examples (bare 'created'/'updated'/'deleted' and JS 'task.created' handlers) vs the IRealtimeService contract's dotted 'record.created'/'record.updated' (packages/spec/src/contracts/realtime-service.ts:20). Audit correctly left this under the top-of-page design-spec callout since the WS transport is unimplemented; flagging only as residual.
- The 'restored' event type in the doc's Event Types table is not corroborated by the realtime-service contract (only 'record.created'/'record.updated' appear at contract line 20; no 'restored' literal found). Sits inside the now-callout-qualified aspirational WS section, so not a fix target, but unverified.

### `protocol/objectos/runtime-capabilities.mdx`
- The discovery example uses environment 'production', which is valid, but DiscoverySchema's environment enum is ['production','sandbox','development'] (packages/spec/src/api/discovery.zod.ts:145), whereas the ObjectOS capabilities table (doc line 173) lists 'development, test, staging, production' for its 'environment' field. These belong to two distinct schemas (ObjectOSCapabilitiesSchema vs DiscoverySchema), so it is not a self-contradiction the fix introduced, but a reader could be confused that the two 'environment' value sets differ. Not changed (no inaccuracy in either against its own schema).
- The discovery example shows graphql: '/graphql' unconditionally; in rest-server.ts the graphql route is only advertised when the GraphQL plugin/route is enabled (ApiRoutesSchema marks graphql optional). The doc presents it as illustrative JSON, so this is acceptable, but a note that graphql appears only when enabled would be more precise. Not fixed (illustrative, no factual error).

### `protocol/objectql/index.mdx`
- Architecture ASCII diagram and 'Write Once, Run Anywhere' card list Redis and Excel as compile/driver targets ('compiles to PostgreSQL, MongoDB, Redis, or Excel'). I confirmed driver-* packages exist generically but did NOT enumerate that a Redis or Excel driver actually ships; these may be illustrative/aspirational. Left unchanged (audit also flagged this as unresolved) — needs a maintainer to confirm which concrete drivers ship before claiming Excel/Redis support.
- Card 'Schema as Code' claims 'Automatic migrations. Rollback schema changes like Git commits.' An `os rollback` command exists (packages/cli/src/commands/rollback.ts) and the Schema Evolution section now describes diff-based reconciliation via `os publish`, but fully-automatic online schema migration was not traced to an implementation. Conceptual prose; left as-is per audit.
- 'Query Optimization' (index selection, hash vs nested-loop join, projection/filter pushdown) and 'Caching Strategy' (schema cache, query plan cache, Redis/Memcached result cache) remain unverified marketing-level claims. JoinStrategy enum (query.zod.ts:182 — auto/database/hash/loop) partially grounds the join-strategy claim only; the rest is conceptual and unconfirmed against an actual optimizer/cache implementation.
- Validation rule example (line 183-191) uses `condition: "account_type = 'Enterprise' AND contract_value = null"`. The established prior says conditions are CEL (not Salesforce). CEL equality is `==` and null-check is typically `== null`, so `=` (single) and `AND` (vs CEL `&&`) look potentially non-CEL. The audit did not touch these condition strings and I did not have a CEL grammar reference open to confirm whether the formula engine accepts `=`/`AND` sugar — flagging for a maintainer to verify against packages/formula rather than changing speculatively.

### `protocol/objectql/query-syntax.mdx`
- Join 'on' clause example (line 611): { 'order.customer_id': { $eq: { $field: 'c.id' } } }. The $field reference is schema-valid (FieldReferenceSchema in filter.zod.ts; on:FilterConditionSchema in query.zod.ts:268), but no driver-side $field resolution code was located in non-test driver-sql/objectql sources, so end-to-end runtime support in joins is schema-valid but runtime-unconfirmed. Left unchanged (no evidence it is wrong; would need a driver-level trace to confirm or refute).
- Audit's note that <Cards>/<Card> are used (lines ~812-828) without an import: confirmed NOT an issue — all sibling objectql docs use them the same way with no import (globally provided via fumadocs mdx-components). No action needed; recorded only for completeness.

### `protocol/objectql/schema.mdx`
- Unused import on line 6: `import { FileCode, Database, Layers, Package } from 'lucide-react'` — none of these icons are referenced in the doc body. Pre-existing (not introduced by the audit) and harmless to MDX build; flagged for cleanup only.
- Validation-rule `events` enum in code is `['insert','update','delete']` (validation.zod.ts:88), but the doc never documents the `events` field on validation rules, so no inaccuracy — noted only for completeness if a future edit adds it.

### `protocol/objectql/security.mdx`
- Doc line 49-70 uses <Cards>/<Card> with no explicit import line. These are Fumadocs MDX globals (consistent with other docs in the repo), so this is almost certainly fine, but I did not locate the global MDX-components registration to confirm. Low risk.
- sharing.zod.ts defines an OWDModel enum (private/public_read/public_read_write/controlled_by_parent) that is DISTINCT from and inconsistent with the object.zod.ts `sharingModel` enum (private/read/read_write/full) the doc uses. The doc correctly documents the object field (object.zod.ts:597), so this is NOT a doc error — but it flags a real spec-level inconsistency between the two schemas that a maintainer may want to reconcile. No doc change warranted.

### `protocol/objectql/state-machine.mdx`
- None blocking. Minor: the Order example's `order_approval` rule declares only `pending: ['approved','rejected']` while the surrounding prose lists approval as `pending -> approved -> rejected`; this is internally consistent (approved/rejected are intended dead-ends) and not an inaccuracy, just worth a glance if an author expects an edge out of `approved`. No code change warranted.

### `protocol/objectql/types.mdx`
- Type Philosophy intro block (content/docs/protocol/objectql/types.mdx:24-28) still shows a currency field with top-level `scale: 2` and `precision: 18`, inconsistent with the corrected canonical currency example (lines 252-260) that moved these into `currencyConfig`. Not a validation failure (scale/precision are generic optional field props per field.zod.ts:380-381) so it is left as-is, but it is stylistically inconsistent with the established correction. Left unfixed to avoid over-correcting an illustrative concept snippet.
- `percent` section (lines 282-296) claims 'Storage: As decimal (0.255 for 25.5%)' while the example uses `min: 0` / `max: 100`. This is an internal inconsistency (decimal storage would imply min/max 0-1). Pre-existing, not introduced by the audit; no code evidence found to confirm which is correct, so reported not fixed.
- `lookup` multiple-value example (line 527) comments `multiple: true # Many-to-many`. Per field.zod.ts:371 `multiple` stores an array of values on the field itself (Array/JSON), which is a multi-value reference, not a true junction-table many-to-many. Imprecise wording, pre-existing, left unfixed.

### `protocol/objectui/actions.mdx`
- The Flow Actions example (line ~106) uses params: - { name: approver, field: approver }. Redundant: per ActionParamSchema (action.zod.ts:38-39) name defaults to field when field is set, so the explicit name is unnecessary (not wrong, just non-idiomatic vs the field-backed examples elsewhere in the doc). Left as-is — valid, not an inaccuracy.
- URL examples (lines 87, 92) use the '/_console/...' path prefix while the Form Actions section (line 134) and schema describe text (action.zod.ts:102) use '/console/forms/:name' (no underscore). The established correction says the console path is '/_console'. The form-route '/console/forms/:name' comes verbatim from the schema's own describe string, so the doc faithfully mirrors code — but the schema text itself may be inconsistent with the '/_console' convention. Flagging for human follow-up; not fixed since it matches the cited source of truth.

### `protocol/objectui/concept.mdx`
- Line 467: `import { FormViewSchema } from '@objectstack/spec'` is inaccurate. The root package barrel (packages/spec/src/index.ts) only re-exports the `defineView`/`defineForm` helpers from ui/view.zod (line 74) and a single wildcard `export * from './stack.zod'` (line 71) — it does NOT re-export FormViewSchema. The schema is reachable only via the subpath `@objectstack/spec/ui` (ui/index.ts:17 `export * from './view.zod'`; root index comment at line 24 documents `import * as UI from '@objectstack/spec/ui'`). The doc's import should be from `@objectstack/spec/ui`. This line was NOT part of the 11 applied fixes (pre-existing); left unfixed to respect minimal-scope and because it could be an intentional doc simplification — flag for docs owner.
- Line 292: `GET /api/v1/ui/view/customer/form?recordId=12345` — the path is now correct, but the `?recordId=` query param is not consumed by the route handler (rest-server.ts:2603-2612 passes only `object` and `type` to `getUiView`; no recordId/query read). The Server Response example still shows a fully-resolved record ('Acme Corp', 'contact@acme.com'), implying record-aware resolution the route doesn't perform. Pre-existing illustrative detail, not introduced by the fixes; flag for docs owner to decide whether to drop the query param or note it as aspirational.
- Dashboard YAML (lines 183-201) still uses inline `object`/`groupBy`/`aggregate`/`field`/`filter` widget config rather than the ADR-0021 semantic-layer `dataset` binding (this matches the audit's own unresolved note). Reasonable to leave as a simplified conceptual illustration, but a docs owner should confirm whether to align with the dataset-bound shape in packages/spec/src/ui/dashboard.zod.ts / ListChartConfigSchema.
- Prose mentions of 'Studio' (section 3 heading 'Same Metadata Powers Studio Editors...' and 'version-rolled-back in Studio' bullet, lines 180/207) make no path claim, so the established /_console vs /_studio correction does not apply; left as-is intentionally. Studio reference docs do exist under content/docs/references/studio/. No action needed unless docs owner wants terminology consistency.

### `protocol/objectui/index.mdx`
- Line 165: the first 'customer.formview.yml' example uses 'mode: edit' (a top-level field that does not exist on FormViewSchema; 'edit' is not a valid value of any FormView enum). The audit's own fix #5 corrected an identical 'mode' error to 'type' in the FormView.create block (line 231) but left this one unchanged. For consistency it should be removed or changed to a valid construct. Evidence: packages/spec/src/ui/view.zod.ts:729-739 FormViewSchema has top-level 'type' enum ['simple','tabbed','wizard','split','drawer','modal'] and no 'mode' field. Left unfixed as it is a pre-existing illustrative-pseudocode issue not introduced by the edit, and matches the audit's documented decision to leave structural fabrications in the conceptual YAML blocks.
- Lines 163-171 and 230-247: the example FormView YAML wraps fields in a 'layout: { sections: [...] }' / 'layout: { type, tabs: [...] }' structure, but FormViewSchema has 'sections' as a TOP-LEVEL field and has no 'layout' or 'tabs' fields (view.zod.ts:734-743). The audit acknowledged this under 'unresolved'; field-name fixes were applied but the structural shape remains fabricated. Conceptual-illustration only.
- Lines 228, 259, 286, 342: 'FormView.create()', 'ListView.create()', 'Dashboard.create()' factory calls — ListView.create()/FormView.create() do not exist as exports (real helpers are defineView/defineForm, index.ts:74). Dashboard.create IS real (dashboard.zod.ts). The Dashboard example uses a 'layout.rows[].widgets[]' shape that does not match DashboardSchema's flat 'widgets' array with per-widget 'layout:{x,y,w,h}'. Audit deliberately left these as illustrative pseudo-code; flagged here for completeness, not a regression.
- Lines 161-176 FormView example fenced as ```yaml but contains JS-style 'FormView.create({...})' with '#' comments (lines 231,261) inside JS object literals — mismatched fence language is pre-existing and harmless to build but stylistically inaccurate.

### `protocol/objectui/layout-dsl.mdx`
- Page template framing 'standard'/'console'/'wizard' (lines ~43-176) is still presented as a set of built-in macro page templates. Verified PageSchema.template is a free string with default 'default' (page.zod.ts:300) and PageTypeSchema (page.zod.ts:170-186) has no 'standard'/'console'/'wizard' values ('wizard'/'console' exist only as FormView types in view.zod.ts). The conceptual YAML was correctly left in place per minimal-edit rule, but the narrative is not backed by the page schema. Audit already flagged this.
- Advanced Layouts section (lines ~629-690): layout types 'split_view', 'card_grid', 'kanban' and card types 'metric_card'/'chart_card' are still documented. Re-confirmed via grep in packages/spec/src — zero hits for split_view/card_grid/metric_card/chart_card. These are conceptual/aspirational shapes, not in the spec. Audit already flagged; left unrewritten.
- Conceptual YAML examples still use 'type: related_list' with props 'object'/'relationField' (lines ~520-574, 818-828). Real component type is 'record:related_list' with props objectName/relationshipField (component.zod.ts:290, 72-73). Only the authoritative schema-reference block was in scope; the narrative DSL examples were intentionally left, so this is a known residual not a regression.
- Section 'lazy'/'source'/'virtualScroll'/'itemHeight' (lines ~451-463, 896-920) and 'renderStrategy: progressive' (lines ~927-933) are documented as section/layout keys. FormSectionSchema (view.zod.ts:683-707) has no such fields; the real performance config lives in PerformanceConfigSchema (responsive.zod.ts:93-124, keys lazyLoad/virtualScroll{enabled,itemHeight,overscan}/cacheStrategy/prefetch) and is not a section-level YAML key. Not in the audit's corrected schema-reference scope; flagging for follow-up.
- Tab/badge/wizard-steps YAML constructs (lines ~404-481, 845-892) were not verified against a schema; 'steps' as a top-level page key and 'badgeVariant'/'badge:{count,variant}' shapes may not exist in the spec. Out of the audit's schema-reference scope; reported for human follow-up rather than changed without evidence.

### `protocol/objectui/record-alert.mdx`
- Severity->defaults table (line 122) lists `AlertCircle` as the error-severity default icon, but the file's lucide-react import (line 6) imports `XCircle`, not `AlertCircle`. The imported icons (AlertTriangle/Info/CheckCircle2/XCircle) are never actually rendered as JSX anyway (the table uses inline-code strings), so the import is decorative/unused. The renderer's real default-icon mapping lives in the sibling objectui repo (@object-ui/plugin-detail) and could not be verified here. Pre-existing, not introduced by the fixes; left unchanged per evidence-required rule.
- Doc example (line 62) now uses `visible: 'record.id == os.user.id ...'` matching buildScope (formula/stdlib.ts:120), but the repo's own sample page packages/platform-objects/src/pages/sys-user.page.ts:67 still uses `ctx.user.id`. The doc is correct against the implemented CEL scope; the sample page is the divergent artifact (likely an unwired/aspirational `ctx` alias). Worth reconciling the sample page separately, but the doc fix is correct.
- The 'Schema' block (lines 19-37), Dismissible-alerts localStorage key format (line 131), dismiss scoping, and ARIA-role column remain renderer-side claims unverifiable in this repo (renderer is @object-ui/plugin-detail in objectstack-ai/objectui). record:alert props flow through the generic `properties: z.record(...)` with no dedicated Zod schema in component.zod.ts, so the 'Schema' block is informal prop docs, not a formal schema — accurately reflecting the real sample page's prop usage. No disproving evidence available; left as-is.

### `protocol/objectui/widget-contract.mdx`
- lucide-react import (line 6) brings in `Component`, `Check`, `AlertCircle`, `Globe` which are never used in the body (only `Zap` and `Code` are used in <Card icon=...>). Harmless dead imports, likely pre-existing; not an accuracy defect.
- The Field Types table groups `code` under 'Rich content' and `slider`/`progress`/`rating` under 'Numbers', whereas the source FieldType enum comments classify `code`, `rating`, `slider`, `progress`, `tags`, `signature`, `qrcode` under 'Enhanced Types'. All listed type strings are valid enum members (field.zod.ts:13-61); only the human-facing grouping differs. Not a factual error, just an editorial categorization choice — flagged for awareness, not fixed.
- The WidgetManifest interface in the doc (lines 142-160) omits four real optional manifest fields present in WidgetManifestSchema: `screenshots`, `documentation`, `license`, `tags` (widget.zod.ts:353-368). Omission is not inaccurate (these are optional and tangential to the contract), but a future pass may want to mention them for completeness.

### `releases/index.mdx`
- Minor evidence discrepancy (not a doc error): the audit's unresolved note cites `packages/studio` as a distinct admin app directory, but `packages/studio` does NOT exist (git ls confirms missing). The real evidence for 'Studio' being live is packages/platform-objects/src/apps/studio.app.ts (name: 'studio', line 31) and v9.mdx:154 ('New in Console (Studio)'). This does not affect the doc — 'Studio/Console' prose at index.mdx:10 is correctly left unchanged — but the audit's supporting citation overstated the package layout. No action needed on the target doc.

### `releases/v9.mdx`
- Report breaking-change table (line 30) lists removed property names `groupingsDown` and `groupingsAcross`, but report.zod.ts's legacy comment refers to them generically as `groupings` (report.zod.ts:120, :88). Could not find evidence those exact prior property names ever existed in the spec. The substance is correct (legacy grouping props removed, replaced by `rows`/`columns` dimension-name arrays), so left unchanged — but a maintainer may want to verify the exact pre-cutover property names against git history.
- Doc line 30 lists `filter` as a removed top-level/joined-block Report property; report.zod.ts now has `runtimeFilter` (report.zod.ts:99,131) as the dataset-bound replacement. The claim that the old inline `filter` was removed is plausible but I could not locate the pre-9.0 schema to confirm a property literally named `filter` existed at top level. Substance (presentation filtering now via runtimeFilter) is consistent with the migration prose at lines 40-41.
