# ADR Prioritization Review

Superseded: what to do first is set by `docs/NORTH-STAR.md` (「优先级」); this review is retired.

One rule this file adopted stays in force until it has another home (ADR-0110 cites it as the PRIORITIZATION status-hygiene rule): a PR that lands an ADR's implementation flips that ADR's `Status:` line to `Accepted — implemented` with one line of evidence (key file + test).
